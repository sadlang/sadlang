# اختبار شامل للتوجيهات في sadc

دالة رئيسية()
    # اختبار @حجم
    اطبع_سطر("=== اختبار @حجم ===")
    متغير حجم_رقم = @حجم(رقم)
    اطبع_سطر("حجم رقم:")
    اطبع_سطر(حجم_رقم)
    
    متغير حجم_عشري = @حجم(عشري)
    اطبع_سطر("حجم عشري:")
    اطبع_سطر(حجم_عشري)
    
    متغير حجم_منطقي = @حجم(منطقي)
    اطبع_سطر("حجم منطقي:")
    اطبع_سطر(حجم_منطقي)
    
    متغير حجم_نص = @حجم(نص)
    اطبع_سطر("حجم نص:")
    اطبع_سطر(حجم_نص)
    
    اطبع_سطر("=== كل الاختبارات نجحت! ===")
    ارجع 0
نهاية
