# ═══════════════════════════════════════════════════════════════════════════
#  اختبار الواجهة الموحدة — يعمل على جميع المنصات بدون تعديل
#  Unified UI Test — runs on Desktop / Android / WASM without changes
# ═══════════════════════════════════════════════════════════════════════════

استورد واجهات
استورد واجهات_موحدة

# ─── معلومات المنصة ───
اطبع_سطر("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━")
اطبع_سطر("  لغة ص — اختبار الواجهة الموحدة")
اطبع_سطر("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━")
اطبع_سطر("المنصة: " + المنصة())
اطبع_سطر("المحرك: " + اسم_المحرك())

إذا (هل_سطح_المكتب())
    اطبع_سطر("✓ نعمل على سطح المكتب (SDL2)")
نهاية

إذا (هل_اندرويد())
    اطبع_سطر("✓ نعمل على أندرويد (EGL+GLES3)")
نهاية

إذا (هل_ويب())
    اطبع_سطر("✓ نعمل على الويب (Canvas2D)")
نهاية

# ─── إنشاء التطبيق ───
متغير تطبيق = أنشئ_تطبيق("تطبيقي الموحد ☀️"، 800، 600)
عين_خلفية_التطبيق(تطبيق، 245، 245، 250)

# ─── بناء الواجهة ───
متغير العمود_الرئيسي = عمود()
عين_حشوة(العمود_الرئيسي، 16)
عين_تباعد(العمود_الرئيسي، 12)

# شريط التطبيق
متغير الشريط = شريط_تطبيق("تطبيقي الموحد 🌍")

# عنوان الترحيب
متغير عنوان = نص_عرض("مرحباً بك في الواجهة الموحدة!")
عين_حجم_الخط(عنوان، 24)

# عرض معلومات المنصة
متغير معلومات = نص_عرض("المنصة: " + المنصة() + " | المحرك: " + اسم_المحرك())
عين_حجم_الخط(معلومات، 14)

# عداد بسيط
متغير العداد = 0
متغير نص_العداد = نص_عرض("العداد: 0")
عين_حجم_الخط(نص_العداد، 28)

# أزرار التحكم
متغير صف_الأزرار = صف()
عين_تباعد(صف_الأزرار، 8)

متغير زر_زيادة = زر("زيادة ➕")
متغير زر_نقصان = زر("نقصان ➖")
متغير زر_صفر = زر("تصفير 🔄")

اضف(صف_الأزرار، زر_زيادة)
اضف(صف_الأزرار، زر_نقصان)
اضف(صف_الأزرار، زر_صفر)

# بطاقة معلومات
متغير بطاقة_معلومات = بطاقة()
عين_حشوة(بطاقة_معلومات، 16)
متغير نص_بطاقة = نص_عرض("هذا التطبيق مكتوب بلغة ص ويعمل على:")
متغير نص_منصات = نص_عرض("• سطح المكتب (Windows/Linux/macOS)\n• أندرويد\n• الويب (WASM)")
اضف(بطاقة_معلومات، نص_بطاقة)
اضف(بطاقة_معلومات، نص_منصات)

# حقل إدخال
متغير حقل = حقل_نص("اكتب شيئاً هنا...")

# شريط تقدم
متغير شريط = شريط_تقدم()

# ─── تجميع الشجرة ───
اضف(العمود_الرئيسي، الشريط)
اضف(العمود_الرئيسي، عنوان)
اضف(العمود_الرئيسي، معلومات)
اضف(العمود_الرئيسي، نص_العداد)
اضف(العمود_الرئيسي، صف_الأزرار)
اضف(العمود_الرئيسي، بطاقة_معلومات)
اضف(العمود_الرئيسي، حقل)
اضف(العمود_الرئيسي، شريط)

عين_محتوى_التطبيق(تطبيق، العمود_الرئيسي)

# ─── حلقة التطبيق ───
بينما (التطبيق_يعمل(تطبيق))
    حدث_التطبيق(تطبيق)

    # معالجة النقرات
    إذا (تم_النقر(زر_زيادة))
        العداد = العداد + 1
        عين_نص(نص_العداد، "العداد: " + العداد)
        عين_قيمة(شريط، العداد)
    نهاية

    إذا (تم_النقر(زر_نقصان))
        العداد = العداد - 1
        عين_نص(نص_العداد، "العداد: " + العداد)
        عين_قيمة(شريط، العداد)
    نهاية

    إذا (تم_النقر(زر_صفر))
        العداد = 0
        عين_نص(نص_العداد، "العداد: 0")
        عين_قيمة(شريط، 0)
    نهاية
نهاية

اطبع_سطر("تم إغلاق التطبيق. شكراً لاستخدامك الواجهة الموحدة!")
