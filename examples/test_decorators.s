# اختبار المزخرفات (Decorators) - المرحلة 7
# Phase 7: Decorators Test

# تعريف دالة مزخرفة (حالياً لا تُطبّق المزخرفات فعلياً)
@توقيت
دالة عملية_بطيئة()
    اطبع_سطر("تنفيذ العملية...")
نهاية

# الدالة الرئيسية
دالة رئيسية()
    اطبع_سطر("اختبار المزخرفات")
    
    # استدعاء الدالة العادية (المزخرفات لم تُطبّق بعد)
    # عملية_بطيئة()
    
    اطبع_سطر("انتهى الاختبار")
    ارجع 0
نهاية
