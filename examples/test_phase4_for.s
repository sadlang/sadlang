# اختبار حلقة for (foreach)
# جمع أرقام من مصفوفة

دالة رئيسية()
    رقم مجموع = 0
    مصفوفة أرقام = [1، 2، 3، 4، 5، 6]
    
    لكل (ع في أرقام)
        مجموع = مجموع + ع
    نهاية
    
    ارجع مجموع * 2
نهاية
