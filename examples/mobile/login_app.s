# ===================================================
# تطبيق تسجيل الدخول - لغة ص
# ===================================================
# مثال عملي لتطبيق أندرويد بالكامل بالعربية
# ===================================================

استيراد واجهة من "مكتبة/هاتف/واجهة"
استيراد شبكة من "مكتبة/شبكة"

# ===================================================
#                    نموذج البيانات
# ===================================================

صنف مستخدم
    نص اسم_المستخدم
    نص البريد
    نص الصورة_الشخصية
    منطقي مفعل
    
    دالة منشئ()
        اسم_المستخدم = ""
        البريد = ""
        الصورة_الشخصية = ""
        مفعل = خطأ
    نهاية
نهاية

صنف نتيجة_تسجيل
    منطقي نجاح
    نص رسالة
    اختياري<مستخدم> المستخدم
    
    دالة منشئ()
        نجاح = خطأ
        رسالة = ""
    نهاية
نهاية

# ===================================================
#                    حالة التطبيق
# ===================================================

صنف حالة_تسجيل_دخول
    نص اسم_المستخدم
    نص كلمة_المرور
    منطقي جاري_التحميل
    نص رسالة_خطأ
    منطقي إظهار_كلمة_مرور
    منطقي تذكرني
    
    دالة منشئ()
        اسم_المستخدم = ""
        كلمة_المرور = ""
        جاري_التحميل = خطأ
        رسالة_خطأ = ""
        إظهار_كلمة_مرور = خطأ
        تذكرني = خطأ
    نهاية
    
    # التحقق من صحة البيانات
    دالة صالح() منطقي
        ارجع اسم_المستخدم.طول() >= 3 && كلمة_المرور.طول() >= 6
    نهاية
    
    دالة رسالة_تحقق() نص
        إذا (اسم_المستخدم.طول() < 3)
            ارجع "اسم المستخدم يجب أن يكون 3 أحرف على الأقل"
        وإلا_إذا (كلمة_المرور.طول() < 6)
            ارجع "كلمة المرور يجب أن تكون 6 أحرف على الأقل"
        وإلا
            ارجع ""
        نهاية
    نهاية
نهاية

# ===================================================
#                    خدمة المصادقة
# ===================================================

صنف خدمة_مصادقة
    نص رابط_الخادم
    
    دالة منشئ(نص رابط = "https://api.example.com")
        رابط_الخادم = رابط
    نهاية
    
    # تسجيل الدخول
    مزامن دالة تسجيل_دخول(نص مستخدم، نص كلمة_سر) نتيجة<نتيجة_تسجيل، خطأ>
        # محاكاة طلب الشبكة
        انتظر تأخير(1500)  # محاكاة تأخير الشبكة
        
        # للاختبار: قبول أي مستخدم بكلمة مرور "123456"
        إذا (كلمة_سر == "123456")
            نتيجة_تسجيل نت = نتيجة_تسجيل()
            نت.نجاح = صحيح
            نت.رسالة = "تم تسجيل الدخول بنجاح!"
            
            مستخدم م = مستخدم()
            م.اسم_المستخدم = مستخدم
            م.البريد = مستخدم + "@example.com"
            م.مفعل = صحيح
            نت.المستخدم = اختياري.بعض(م)
            
            ارجع نتيجة.نجاح(نت)
        وإلا
            نتيجة_تسجيل نت = نتيجة_تسجيل()
            نت.نجاح = خطأ
            نت.رسالة = "اسم المستخدم أو كلمة المرور غير صحيحة"
            ارجع نتيجة.نجاح(نت)
        نهاية
    نهاية
    
    # تسجيل مستخدم جديد
    مزامن دالة تسجيل_جديد(نص مستخدم، نص بريد، نص كلمة_سر) نتيجة<نتيجة_تسجيل، خطأ>
        انتظر تأخير(2000)
        
        نتيجة_تسجيل نت = نتيجة_تسجيل()
        نت.نجاح = صحيح
        نت.رسالة = "تم إنشاء الحساب بنجاح!"
        ارجع نتيجة.نجاح(نت)
    نهاية
    
    # نسيت كلمة المرور
    مزامن دالة استعادة_كلمة_مرور(نص بريد) نتيجة<منطقي، خطأ>
        انتظر تأخير(1000)
        ارجع نتيجة.نجاح(صحيح)
    نهاية
نهاية

# ===================================================
#                    واجهة تسجيل الدخول
# ===================================================

واجهة شاشة_تسجيل_دخول
    # الحالة
    حالة حالة = حالة_تسجيل_دخول()
    خدمة_مصادقة خدمة = خدمة_مصادقة()
    
    # معالج تسجيل الدخول
    مزامن دالة عند_تسجيل()
        إذا (!حالة.صالح())
            حالة.رسالة_خطأ = حالة.رسالة_تحقق()
            ارجع
        نهاية
        
        حالة.جاري_التحميل = صحيح
        حالة.رسالة_خطأ = ""
        
        نتيجة<نتيجة_تسجيل، خطأ> نت = انتظر خدمة.تسجيل_دخول(
            حالة.اسم_المستخدم،
            حالة.كلمة_المرور
        )
        
        حالة.جاري_التحميل = خطأ
        
        إذا (نت.ناجح())
            نتيجة_تسجيل ن = نت.قيمة()
            إذا (ن.نجاح)
                # الانتقال للشاشة الرئيسية
                انتقل_إلى(شاشة_رئيسية، ن.المستخدم.قيمة())
            وإلا
                حالة.رسالة_خطأ = ن.رسالة
            نهاية
        وإلا
            حالة.رسالة_خطأ = "حدث خطأ في الاتصال"
        نهاية
    نهاية
    
    # واجهة المستخدم
    عرض
        # خلفية متدرجة
        صندوق(
            تعديل: ملء_الشاشة()
                .خلفية_متدرجة(
                    من: لون(0x1A237E),
                    إلى: لون(0x3949AB)
                )
        )
            عمود(
                تعديل: ملء()
                    .حشوة(24)،
                محاذاة_أفقية: وسط،
                ترتيب_عمودي: وسط
            )
                # الشعار
                صندوق(
                    تعديل: حجم(120)
                        .خلفية(أبيض.شفافية(0.2))
                        .دائري(60)
                )
                    أيقونة(
                        "شخص"،
                        تعديل: حجم(60)،
                        لون: أبيض
                    )
                نهاية
                
                مسافة(ارتفاع: 32)
                
                # العنوان
                نص(
                    "مرحباً بعودتك!"،
                    تعديل: عرض_كامل()،
                    نمط: عنوان_كبير،
                    لون: أبيض،
                    محاذاة: وسط
                )
                
                مسافة(ارتفاع: 8)
                
                نص(
                    "سجل دخولك للمتابعة"،
                    نمط: نص_عادي،
                    لون: أبيض.شفافية(0.7)،
                    محاذاة: وسط
                )
                
                مسافة(ارتفاع: 48)
                
                # بطاقة تسجيل الدخول
                بطاقة(
                    تعديل: عرض_كامل()
                        .حشوة(24)،
                    ارتفاع: 8،
                    لون: أبيض
                )
                    عمود(تعديل: عرض_كامل())
                        # حقل اسم المستخدم
                        حقل_نص_مزين(
                            قيمة: حالة.اسم_المستخدم،
                            تلميح: "اسم المستخدم أو البريد الإلكتروني"،
                            أيقونة_بداية: "شخص"،
                            عند_تغيير: دالة(نص ق)
                                حالة.اسم_المستخدم = ق
                                حالة.رسالة_خطأ = ""
                            نهاية،
                            تعديل: عرض_كامل()
                        )
                        
                        مسافة(ارتفاع: 16)
                        
                        # حقل كلمة المرور
                        حقل_نص_مزين(
                            قيمة: حالة.كلمة_المرور،
                            تلميح: "كلمة المرور"،
                            أيقونة_بداية: "قفل"،
                            أيقونة_نهاية: إذا(حالة.إظهار_كلمة_مرور، "عين_مغلقة"، "عين")،
                            عند_ضغط_نهاية: دالة()
                                حالة.إظهار_كلمة_مرور = !حالة.إظهار_كلمة_مرور
                            نهاية،
                            كلمة_مرور: !حالة.إظهار_كلمة_مرور،
                            عند_تغيير: دالة(نص ق)
                                حالة.كلمة_المرور = ق
                                حالة.رسالة_خطأ = ""
                            نهاية،
                            تعديل: عرض_كامل()
                        )
                        
                        مسافة(ارتفاع: 12)
                        
                        # صف تذكرني ونسيت كلمة المرور
                        صف(
                            تعديل: عرض_كامل()،
                            ترتيب_أفقي: بين
                        )
                            صف(محاذاة_عمودية: وسط)
                                مربع_اختيار(
                                    محدد: حالة.تذكرني،
                                    عند_تغيير: دالة(منطقي ق)
                                        حالة.تذكرني = ق
                                    نهاية
                                )
                                مسافة(عرض: 8)
                                نص("تذكرني"، نمط: نص_صغير)
                            نهاية
                            
                            زر_نص(
                                "نسيت كلمة المرور؟"،
                                عند_ضغط: دالة()
                                    انتقل_إلى(شاشة_استعادة_كلمة_مرور)
                                نهاية،
                                لون: أزرق_داكن
                            )
                        نهاية
                        
                        مسافة(ارتفاع: 8)
                        
                        # رسالة الخطأ
                        إذا (حالة.رسالة_خطأ != "")
                            صندوق(
                                تعديل: عرض_كامل()
                                    .خلفية(أحمر.شفافية(0.1))
                                    .حشوة(12)
                                    .دائري(8)
                            )
                                صف(محاذاة_عمودية: وسط)
                                    أيقونة("خطأ"، لون: أحمر، حجم: 20)
                                    مسافة(عرض: 8)
                                    نص(
                                        حالة.رسالة_خطأ،
                                        لون: أحمر،
                                        نمط: نص_صغير
                                    )
                                نهاية
                            نهاية
                            مسافة(ارتفاع: 8)
                        نهاية
                        
                        مسافة(ارتفاع: 24)
                        
                        # زر تسجيل الدخول
                        زر(
                            عند_ضغط: عند_تسجيل،
                            تعديل: عرض_كامل()
                                .ارتفاع(56)،
                            مفعل: !حالة.جاري_التحميل،
                            الوان: الوان_زر(
                                خلفية: لون(0x3949AB)،
                                نص: أبيض
                            )،
                            شكل: دائري(28)
                        )
                            إذا (حالة.جاري_التحميل)
                                مؤشر_تحميل_دائري(
                                    لون: أبيض،
                                    حجم: 24
                                )
                            وإلا
                                نص(
                                    "تسجيل الدخول"،
                                    نمط: زر_نص،
                                    لون: أبيض
                                )
                            نهاية
                        نهاية
                    نهاية
                نهاية
                
                مسافة(ارتفاع: 24)
                
                # فاصل
                صف(
                    تعديل: عرض_كامل()،
                    محاذاة_عمودية: وسط
                )
                    خط(تعديل: وزن(1)، لون: أبيض.شفافية(0.3))
                    مسافة(عرض: 16)
                    نص("أو"، لون: أبيض.شفافية(0.7))
                    مسافة(عرض: 16)
                    خط(تعديل: وزن(1)، لون: أبيض.شفافية(0.3))
                نهاية
                
                مسافة(ارتفاع: 24)
                
                # أزرار تسجيل الدخول الاجتماعي
                صف(
                    تعديل: عرض_كامل()،
                    ترتيب_أفقي: وسط
                )
                    زر_اجتماعي("google"، "جوجل")
                    مسافة(عرض: 16)
                    زر_اجتماعي("apple"، "آبل")
                    مسافة(عرض: 16)
                    زر_اجتماعي("twitter"، "تويتر")
                نهاية
                
                مسافة(ارتفاع: 32)
                
                # رابط إنشاء حساب
                صف(محاذاة_عمودية: وسط)
                    نص(
                        "ليس لديك حساب؟ "،
                        لون: أبيض.شفافية(0.7)
                    )
                    زر_نص(
                        "أنشئ حساباً"،
                        عند_ضغط: دالة()
                            انتقل_إلى(شاشة_تسجيل_جديد)
                        نهاية،
                        لون: أبيض،
                        نمط: عريض
                    )
                نهاية
            نهاية
        نهاية
    نهاية
نهاية

# ===================================================
#                    مكون زر اجتماعي
# ===================================================

مكون زر_اجتماعي(نص منصة، نص اسم)
    زر(
        عند_ضغط: دالة()
            اطبع(ن"تسجيل دخول عبر {اسم}")
        نهاية،
        تعديل: حجم(56)
            .خلفية(أبيض)
            .دائري(28)
            .ظل(4)
    )
        صورة(
            ن"assets/icons/{منصة}.png"،
            تعديل: حجم(24)
        )
    نهاية
نهاية

# ===================================================
#                    شاشة التسجيل الجديد
# ===================================================

واجهة شاشة_تسجيل_جديد
    حالة اسم = ""
    حالة بريد = ""
    حالة كلمة_مرور = ""
    حالة تأكيد_كلمة_مرور = ""
    حالة جاري_التحميل = خطأ
    حالة رسالة_خطأ = ""
    خدمة_مصادقة خدمة = خدمة_مصادقة()
    
    مزامن دالة عند_تسجيل()
        إذا (اسم.طول() < 3)
            رسالة_خطأ = "الاسم يجب أن يكون 3 أحرف على الأقل"
            ارجع
        وإلا_إذا (!بريد.يحتوي("@"))
            رسالة_خطأ = "البريد الإلكتروني غير صالح"
            ارجع
        وإلا_إذا (كلمة_مرور.طول() < 6)
            رسالة_خطأ = "كلمة المرور يجب أن تكون 6 أحرف على الأقل"
            ارجع
        وإلا_إذا (كلمة_مرور != تأكيد_كلمة_مرور)
            رسالة_خطأ = "كلمتا المرور غير متطابقتين"
            ارجع
        نهاية
        
        جاري_التحميل = صحيح
        نتيجة<نتيجة_تسجيل، خطأ> نت = انتظر خدمة.تسجيل_جديد(اسم، بريد، كلمة_مرور)
        جاري_التحميل = خطأ
        
        إذا (نت.ناجح() && نت.قيمة().نجاح)
            أظهر_رسالة("تم إنشاء الحساب بنجاح!")
            انتقل_إلى(شاشة_تسجيل_دخول)
        وإلا
            رسالة_خطأ = "حدث خطأ، حاول مرة أخرى"
        نهاية
    نهاية
    
    عرض
        صندوق(
            تعديل: ملء_الشاشة()
                .خلفية_متدرجة(
                    من: لون(0x1A237E)،
                    إلى: لون(0x3949AB)
                )
        )
            عمود(
                تعديل: ملء()
                    .حشوة(24)
                    .قابل_للتمرير()،
                محاذاة_أفقية: وسط
            )
                # شريط العنوان
                صف(
                    تعديل: عرض_كامل()،
                    محاذاة_عمودية: وسط
                )
                    زر_أيقونة(
                        "سهم_رجوع"،
                        عند_ضغط: دالة()
                            رجوع()
                        نهاية،
                        لون: أبيض
                    )
                    
                    نص(
                        "إنشاء حساب جديد"،
                        تعديل: وزن(1)،
                        نمط: عنوان،
                        لون: أبيض،
                        محاذاة: وسط
                    )
                    
                    مسافة(عرض: 48)  # للتوازن
                نهاية
                
                مسافة(ارتفاع: 32)
                
                # بطاقة التسجيل
                بطاقة(
                    تعديل: عرض_كامل()
                        .حشوة(24)،
                    لون: أبيض
                )
                    عمود(تعديل: عرض_كامل())
                        حقل_نص_مزين(
                            قيمة: اسم،
                            تلميح: "الاسم الكامل"،
                            أيقونة_بداية: "شخص"،
                            عند_تغيير: دالة(نص ق)
                                اسم = ق
                                رسالة_خطأ = ""
                            نهاية
                        )
                        
                        مسافة(ارتفاع: 16)
                        
                        حقل_نص_مزين(
                            قيمة: بريد،
                            تلميح: "البريد الإلكتروني"،
                            أيقونة_بداية: "بريد"،
                            نوع_لوحة: بريد_إلكتروني،
                            عند_تغيير: دالة(نص ق)
                                بريد = ق
                                رسالة_خطأ = ""
                            نهاية
                        )
                        
                        مسافة(ارتفاع: 16)
                        
                        حقل_نص_مزين(
                            قيمة: كلمة_مرور،
                            تلميح: "كلمة المرور"،
                            أيقونة_بداية: "قفل"،
                            كلمة_مرور: صحيح،
                            عند_تغيير: دالة(نص ق)
                                كلمة_مرور = ق
                                رسالة_خطأ = ""
                            نهاية
                        )
                        
                        مسافة(ارتفاع: 16)
                        
                        حقل_نص_مزين(
                            قيمة: تأكيد_كلمة_مرور،
                            تلميح: "تأكيد كلمة المرور"،
                            أيقونة_بداية: "قفل_مؤكد"،
                            كلمة_مرور: صحيح،
                            عند_تغيير: دالة(نص ق)
                                تأكيد_كلمة_مرور = ق
                                رسالة_خطأ = ""
                            نهاية
                        )
                        
                        # رسالة الخطأ
                        إذا (رسالة_خطأ != "")
                            مسافة(ارتفاع: 16)
                            نص(رسالة_خطأ، لون: أحمر، نمط: نص_صغير)
                        نهاية
                        
                        مسافة(ارتفاع: 24)
                        
                        # زر التسجيل
                        زر(
                            عند_ضغط: عند_تسجيل،
                            تعديل: عرض_كامل()
                                .ارتفاع(56)،
                            مفعل: !جاري_التحميل،
                            الوان: الوان_زر(
                                خلفية: لون(0x3949AB)،
                                نص: أبيض
                            )،
                            شكل: دائري(28)
                        )
                            إذا (جاري_التحميل)
                                مؤشر_تحميل_دائري(لون: أبيض، حجم: 24)
                            وإلا
                                نص("إنشاء الحساب"، لون: أبيض)
                            نهاية
                        نهاية
                    نهاية
                نهاية
                
                مسافة(ارتفاع: 24)
                
                # رابط تسجيل الدخول
                صف(محاذاة_عمودية: وسط)
                    نص("لديك حساب بالفعل؟ "، لون: أبيض.شفافية(0.7))
                    زر_نص(
                        "سجل دخولك"،
                        عند_ضغط: دالة()
                            رجوع()
                        نهاية،
                        لون: أبيض،
                        نمط: عريض
                    )
                نهاية
            نهاية
        نهاية
    نهاية
نهاية

# ===================================================
#                    شاشة استعادة كلمة المرور
# ===================================================

واجهة شاشة_استعادة_كلمة_مرور
    حالة بريد = ""
    حالة تم_الإرسال = خطأ
    حالة جاري_التحميل = خطأ
    خدمة_مصادقة خدمة = خدمة_مصادقة()
    
    مزامن دالة عند_إرسال()
        إذا (!بريد.يحتوي("@"))
            أظهر_رسالة("البريد الإلكتروني غير صالح")
            ارجع
        نهاية
        
        جاري_التحميل = صحيح
        انتظر خدمة.استعادة_كلمة_مرور(بريد)
        جاري_التحميل = خطأ
        تم_الإرسال = صحيح
    نهاية
    
    عرض
        صندوق(
            تعديل: ملء_الشاشة()
                .خلفية_متدرجة(
                    من: لون(0x1A237E)،
                    إلى: لون(0x3949AB)
                )
        )
            عمود(
                تعديل: ملء()
                    .حشوة(24)،
                محاذاة_أفقية: وسط،
                ترتيب_عمودي: وسط
            )
                # شريط العنوان
                صف(
                    تعديل: عرض_كامل()،
                    محاذاة_عمودية: وسط
                )
                    زر_أيقونة(
                        "سهم_رجوع"،
                        عند_ضغط: دالة()
                            رجوع()
                        نهاية،
                        لون: أبيض
                    )
                نهاية
                
                مسافة(ارتفاع: 32)
                
                # أيقونة القفل
                صندوق(
                    تعديل: حجم(100)
                        .خلفية(أبيض.شفافية(0.2))
                        .دائري(50)
                )
                    أيقونة("قفل_مفتوح"، حجم: 50، لون: أبيض)
                نهاية
                
                مسافة(ارتفاع: 32)
                
                نص(
                    "نسيت كلمة المرور؟"،
                    نمط: عنوان_كبير،
                    لون: أبيض
                )
                
                مسافة(ارتفاع: 8)
                
                نص(
                    "أدخل بريدك الإلكتروني وسنرسل لك رابط استعادة كلمة المرور"،
                    لون: أبيض.شفافية(0.7)،
                    محاذاة: وسط،
                    تعديل: حشوة_أفقية(32)
                )
                
                مسافة(ارتفاع: 32)
                
                إذا (تم_الإرسال)
                    # رسالة النجاح
                    بطاقة(
                        تعديل: عرض_كامل()
                            .حشوة(24)،
                        لون: أخضر.شفافية(0.1)
                    )
                        عمود(محاذاة_أفقية: وسط)
                            أيقونة("تحقق_دائري"، حجم: 48، لون: أخضر)
                            مسافة(ارتفاع: 16)
                            نص(
                                "تم إرسال رابط الاستعادة!"،
                                نمط: عنوان،
                                لون: أخضر
                            )
                            مسافة(ارتفاع: 8)
                            نص(
                                "تحقق من بريدك الإلكتروني"،
                                لون: أخضر.شفافية(0.7)
                            )
                        نهاية
                    نهاية
                    
                    مسافة(ارتفاع: 24)
                    
                    زر(
                        عند_ضغط: دالة()
                            رجوع()
                        نهاية،
                        تعديل: عرض_كامل()
                            .ارتفاع(56)،
                        الوان: الوان_زر(خلفية: أبيض، نص: لون(0x3949AB))،
                        شكل: دائري(28)
                    )
                        نص("العودة لتسجيل الدخول")
                    نهاية
                وإلا
                    # نموذج الاستعادة
                    بطاقة(
                        تعديل: عرض_كامل()
                            .حشوة(24)،
                        لون: أبيض
                    )
                        عمود(تعديل: عرض_كامل())
                            حقل_نص_مزين(
                                قيمة: بريد،
                                تلميح: "البريد الإلكتروني"،
                                أيقونة_بداية: "بريد"،
                                نوع_لوحة: بريد_إلكتروني،
                                عند_تغيير: دالة(نص ق)
                                    بريد = ق
                                نهاية
                            )
                            
                            مسافة(ارتفاع: 24)
                            
                            زر(
                                عند_ضغط: عند_إرسال،
                                تعديل: عرض_كامل()
                                    .ارتفاع(56)،
                                مفعل: !جاري_التحميل،
                                الوان: الوان_زر(
                                    خلفية: لون(0x3949AB)،
                                    نص: أبيض
                                )،
                                شكل: دائري(28)
                            )
                                إذا (جاري_التحميل)
                                    مؤشر_تحميل_دائري(لون: أبيض، حجم: 24)
                                وإلا
                                    نص("إرسال رابط الاستعادة"، لون: أبيض)
                                نهاية
                            نهاية
                        نهاية
                    نهاية
                نهاية
            نهاية
        نهاية
    نهاية
نهاية

# ===================================================
#                    الشاشة الرئيسية (بعد تسجيل الدخول)
# ===================================================

واجهة شاشة_رئيسية(مستخدم المستخدم)
    عرض
        سقالة(
            شريط_علوي: شريط_تطبيق(
                عنوان: ن"مرحباً {المستخدم.اسم_المستخدم}!"،
                إجراءات: [
                    زر_أيقونة(
                        "إشعارات"،
                        عند_ضغط: دالة()
                            انتقل_إلى(شاشة_إشعارات)
                        نهاية
                    )،
                    زر_أيقونة(
                        "إعدادات"،
                        عند_ضغط: دالة()
                            انتقل_إلى(شاشة_إعدادات)
                        نهاية
                    )
                ]
            )
        )
            عمود(
                تعديل: ملء()
                    .حشوة(24)،
                محاذاة_أفقية: وسط،
                ترتيب_عمودي: وسط
            )
                # صورة المستخدم
                صندوق(
                    تعديل: حجم(120)
                        .خلفية(رمادي_فاتح)
                        .دائري(60)
                )
                    إذا (المستخدم.الصورة_الشخصية != "")
                        صورة(
                            المستخدم.الصورة_الشخصية،
                            تعديل: حجم(120)
                                .دائري(60)،
                            ملاءمة: تغطية
                        )
                    وإلا
                        أيقونة("شخص"، حجم: 60، لون: رمادي)
                    نهاية
                نهاية
                
                مسافة(ارتفاع: 24)
                
                نص(
                    ن"مرحباً، {المستخدم.اسم_المستخدم}!"،
                    نمط: عنوان_كبير
                )
                
                مسافة(ارتفاع: 8)
                
                نص(
                    المستخدم.البريد،
                    نمط: نص_عادي،
                    لون: رمادي
                )
                
                مسافة(ارتفاع: 48)
                
                نص(
                    "🎉 تم تسجيل الدخول بنجاح!"،
                    نمط: عنوان،
                    لون: أخضر
                )
                
                مسافة(ارتفاع: 16)
                
                نص(
                    "هذا مثال على تطبيق مكتوب بالكامل بلغة ص العربية"،
                    محاذاة: وسط،
                    لون: رمادي
                )
                
                مسافة(ارتفاع: 48)
                
                زر(
                    عند_ضغط: دالة()
                        انتقل_إلى(شاشة_تسجيل_دخول)
                    نهاية،
                    تعديل: عرض_كامل()
                        .ارتفاع(56)،
                    الوان: الوان_زر(خلفية: أحمر، نص: أبيض)،
                    شكل: دائري(28)
                )
                    صف(محاذاة_عمودية: وسط، ترتيب_أفقي: وسط)
                        أيقونة("خروج"، لون: أبيض)
                        مسافة(عرض: 8)
                        نص("تسجيل الخروج"، لون: أبيض)
                    نهاية
                نهاية
            نهاية
        نهاية
    نهاية
نهاية

# ===================================================
#                    نقطة الدخول الرئيسية
# ===================================================

تطبيق تطبيق_تسجيل_دخول
    الاسم: "تطبيق تسجيل الدخول"
    الحزمة: "com.sad.login"
    الإصدار: "1.0.0"
    
    الشاشة_الرئيسية: شاشة_تسجيل_دخول
    
    السمة: سمة(
        الألوان: الوان_تطبيق(
            أساسي: لون(0x3949AB)،
            ثانوي: لون(0x1A237E)،
            خلفية: أبيض،
            سطح: رمادي_فاتح،
            خطأ: أحمر
        )،
        الخطوط: خطوط_تطبيق(
            عنوان: "Cairo-Bold"،
            نص: "Cairo-Regular"
        )،
        الاتجاه: يمين_لليسار
    )
نهاية
