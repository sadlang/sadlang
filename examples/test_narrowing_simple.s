# مثال بسيط: فحص القيم الفارغة (Null Check)
# Example: Null Check

دالة معالجة_نص(قيمة)
    اذا (قيمة != فارغ)
        اطبع("النص موجود")
        إرجاع قيمة
    والا
        اطبع("القيمة فارغة")
        إرجاع "افتراضي"
    نهاية
نهاية

دالة رئيسية()
    اطبع("=== مثال 1: فحص القيم الفارغة ===")
    
    # اختبار 1: قيمة نصية
    نص نتيجة1 = معالجة_نص("مرحبا من لغة ص")
    اطبع(نتيجة1)
    
    # اختبار 2: قيمة فارغة
    نص نتيجة2 = معالجة_نص(فارغ)
    اطبع(نتيجة2)
نهاية
