# اختبار مدير السياق (باستخدام)
# Test Context Manager (with statement)

دالة رئيسية()
    اطبع("=== اختبار مدير السياق ===")
    
    # اختبار 1: باستخدام بسيط مع متغير
    نص مورد1 = "مورد_مُنشأ"
    
    باستخدام مورد1 كـ م
        اطبع("داخل كتلة الاستخدام")
        اطبع("المورد: " + م)
    نهاية_استخدام
    
    اطبع("خارج كتلة الاستخدام")
    
    # اختبار 2: باستخدام بدون اسم مستعار
    باستخدام "مورد_مباشر"
        اطبع("داخل كتلة بدون اسم مستعار")
    نهاية_استخدام
    
    # اختبار 3: باستخدام مع عمليات متعددة
    رقم عداد1 = 0
    باستخدام عداد1 كـ ع
        ع = ع + 1
        اطبع("العداد: " + ع)
        ع = ع + 1
        اطبع("العداد: " + ع)
    نهاية_استخدام
    
    اطبع("=== تم اختبار مدير السياق بنجاح ===")
نهاية
