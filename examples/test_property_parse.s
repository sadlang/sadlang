# Test Phase 6.3 - Property Parsing Only
# تجربة المرحلة 6.3 - تحليل الخصائص فقط

صنف حساب
    خاص رقم _الرصيد
    
    # خاصية للقراءة والكتابة
    خاصية رقم الرصيد
        احصل
            ارجع _الرصيد
        نهاية
        
        عيّن(قيمة)
            _الرصيد = قيمة
        نهاية
    نهاية
    
    # خاصية للقراءة فقط
    خاصية رقم الرصيد_المضاعف
        احصل
            ارجع _الرصيد * 2
        نهاية
    نهاية
    
    باني(رقم رصيد_اولي)
        _الرصيد = رصيد_اولي
    نهاية
نهاية

اطبع("تم تحليل الصنف بنجاح")
