# بسم الله الرحمن الرحيم
# ═══════════════════════════════════════════════════════════
# ملف أمثلة: القواميس/الخرائط (Maps/Dictionaries)
# File: Maps/Dictionaries Examples
# ═══════════════════════════════════════════════════════════

# القواعد النحوية المستخدمة / EBNF Rules Used:
# ═══════════════════════════════════════════════════════════
# map_literal ::= '{' [pair_list] '}'
# pair_list ::= pair (',' pair)*
# pair ::= expression ':' expression
# map_access ::= expression '[' expression ']'
# ═══════════════════════════════════════════════════════════

اطبع("=" * 60)
اطبع("      اختبار القواميس/الخرائط - Maps Test")
اطبع("=" * 60)

# 1. قاموس بسيط / Simple Map
اطبع("\n1. قاموس بسيط:")

خريطة شخص = {
    "الاسم": "أحمد"،
    "العمر": 25،
    "المدينة": "الرياض"
}

اطبع("معلومات الشخص: " + نص(شخص))

# 2. الوصول لقيم القاموس / Access Map Values
اطبع("\n2. الوصول لقيم القاموس:")

خريطة طالب = {
    "الاسم": "محمد"،
    "الرقم_الجامعي": "441234567"،
    "التخصص": "هندسة الحاسب"،
    "المعدل": 3.75
}

اطبع("اسم الطالب: " + طالب["الاسم"])
اطبع("الرقم الجامعي: " + طالب["الرقم_الجامعي"])
اطبع("التخصص: " + طالب["التخصص"])
اطبع("المعدل: " + نص(طالب["المعدل"]))

# 3. تعديل قيم القاموس / Modify Map Values
اطبع("\n3. تعديل قيم القاموس:")

خريطة منتج = {
    "الاسم": "هاتف ذكي"،
    "السعر": 2000،
    "الكمية": 50
}

اطبع("قبل التعديل: " + نص(منتج))

منتج["السعر"] = 1800
منتج["الكمية"] = 45

اطبع("بعد التعديل: " + نص(منتج))

# 4. إضافة عناصر جديدة / Add New Elements
اطبع("\n4. إضافة عناصر جديدة:")

خريطة سيارة = {
    "الماركة": "تويوتا"،
    "الموديل": "كامري"
}

اطبع("قبل الإضافة: " + نص(سيارة))

سيارة["السنة"] = 2023
سيارة["اللون"] = "أبيض"

اطبع("بعد الإضافة: " + نص(سيارة))

# 5. قاموس الدرجات / Grades Map
اطبع("\n5. قاموس الدرجات:")

خريطة درجات_المواد = {
    "الرياضيات": 95،
    "الفيزياء": 88،
    "الكيمياء": 92،
    "البرمجة": 98
}

اطبع("=== درجات المواد ===")
اطبع("الرياضيات: " + نص(درجات_المواد["الرياضيات"]))
اطبع("الفيزياء: " + نص(درجات_المواد["الفيزياء"]))
اطبع("الكيمياء: " + نص(درجات_المواد["الكيمياء"]))
اطبع("البرمجة: " + نص(درجات_المواد["البرمجة"]))

# 6. قاموس جهات الاتصال / Contacts Map
اطبع("\n6. قاموس جهات الاتصال:")

خريطة جهات_اتصال = {
    "أحمد": "0501234567"،
    "محمد": "0551234567"،
    "خالد": "0561234567"
}

نص اسم_للبحث = "محمد"
اطبع("رقم " + اسم_للبحث + ": " + جهات_اتصال[اسم_للبحث])

# 7. قاموس الأسعار / Prices Map
اطبع("\n7. قاموس الأسعار:")

خريطة أسعار_المنتجات = {
    "خبز": 5،
    "حليب": 10،
    "بيض": 15،
    "لبن": 8
}

نص منتج_مطلوب = "حليب"
رقم سعر = أسعار_المنتجات[منتج_مطلوب]
اطبع("سعر " + منتج_مطلوب + ": " + نص(سعر) + " ريال")

# 8. قاموس البيانات الشخصية / Personal Data Map
اطبع("\n8. قاموس البيانات الشخصية:")

خريطة بيانات_موظف = {
    "الاسم_الكامل": "عبدالله محمد أحمد"،
    "رقم_الهوية": "1234567890"،
    "الوظيفة": "مهندس برمجيات"،
    "القسم": "تقنية المعلومات"،
    "الراتب": 15000
}

اطبع("=== بيانات الموظف ===")
اطبع("الاسم: " + بيانات_موظف["الاسم_الكامل"])
اطبع("الهوية: " + بيانات_موظف["رقم_الهوية"])
اطبع("الوظيفة: " + بيانات_موظف["الوظيفة"])
اطبع("القسم: " + بيانات_موظف["القسم"])
اطبع("الراتب: " + نص(بيانات_موظف["الراتب"]) + " ريال")

# 9. قاموس الإحصائيات / Statistics Map
اطبع("\n9. قاموس الإحصائيات:")

خريطة إحصائيات_المبيعات = {
    "اليوم": 50،
    "الأسبوع": 320،
    "الشهر": 1250،
    "السنة": 15000
}

اطبع("=== إحصائيات المبيعات ===")
اطبع("مبيعات اليوم: " + نص(إحصائيات_المبيعات["اليوم"]))
اطبع("مبيعات الأسبوع: " + نص(إحصائيات_المبيعات["الأسبوع"]))
اطبع("مبيعات الشهر: " + نص(إحصائيات_المبيعات["الشهر"]))
اطبع("مبيعات السنة: " + نص(إحصائيات_المبيعات["السنة"]))

# 10. قاموس الإعدادات / Settings Map
اطبع("\n10. قاموس الإعدادات:")

خريطة إعدادات_التطبيق = {
    "اللغة": "عربي"،
    "الثيم": "داكن"،
    "الإشعارات": صحيح،
    "الموقع": صحيح،
    "حجم_الخط": 14
}

اطبع("=== إعدادات التطبيق ===")
اطبع("اللغة: " + إعدادات_التطبيق["اللغة"])
اطبع("الثيم: " + إعدادات_التطبيق["الثيم"])
اطبع("الإشعارات: " + نص(إعدادات_التطبيق["الإشعارات"]))
اطبع("الموقع: " + نص(إعدادات_التطبيق["الموقع"]))
اطبع("حجم الخط: " + نص(إعدادات_التطبيق["حجم_الخط"]))

# 11. قاموس متداخل / Nested Map
اطبع("\n11. قاموس متداخل:")

خريطة جامعة = {
    "الاسم": "جامعة الملك عبدالعزيز"،
    "الموقع": "جدة"،
    "الأقسام": {
        "هندسة": 5000،
        "طب": 3000،
        "علوم": 4000
    }
}

اطبع("اسم الجامعة: " + جامعة["الاسم"])
اطبع("الموقع: " + جامعة["الموقع"])
اطبع("عدد طلاب الهندسة: " + نص(جامعة["الأقسام"]["هندسة"]))

# 12. حساب المعدل من قاموس / Calculate Average from Map
اطبع("\n12. حساب المعدل من القاموس:")

خريطة نتائج_الامتحانات = {
    "امتحان_أول": 85،
    "امتحان_ثاني": 90،
    "امتحان_ثالث": 88
}

رقم مجموع_النتائج = نتائج_الامتحانات["امتحان_أول"] + نتائج_الامتحانات["امتحان_ثاني"] + نتائج_الامتحانات["امتحان_ثالث"]
عشري معدل_النتائج = مجموع_النتائج / 3.0

اطبع("الامتحان الأول: " + نص(نتائج_الامتحانات["امتحان_أول"]))
اطبع("الامتحان الثاني: " + نص(نتائج_الامتحانات["امتحان_ثاني"]))
اطبع("الامتحان الثالث: " + نص(نتائج_الامتحانات["امتحان_ثالث"]))
اطبع("المعدل: " + نص(معدل_النتائج))

اطبع("\n" + "=" * 60)
اطبع("      الحمد لله! اكتملت الاختبارات بنجاح")
اطبع("=" * 60)
