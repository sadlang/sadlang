# بسم الله الرحمن الرحيم
# ═══════════════════════════════════════════════════════════════════
# ملف أمثلة: معالجة الأخطاء المتقدمة - Advanced Error Handling
# الحمد لله رب العالمين
# ═══════════════════════════════════════════════════════════════════

# القواعد النحوية المستخدمة / EBNF Rules Used:
# ═══════════════════════════════════════════════════════════════════
# try_stmt ::= 'محاولة' ':' block exception_handlers ['أخيراً' ':' block]
# exception_handler ::= 'خطأ' [exception_type ['كـ' IDENTIFIER]] ':' block
# raise_stmt ::= 'ارفع' expression
# custom_exception ::= 'صنف' IDENTIFIER 'يرث' 'خطأ'
# assert_stmt ::= 'تأكد' expression ['،' string_literal]
# ═══════════════════════════════════════════════════════════════════

اطبع("=" * 70)
اطبع("     اختبار معالجة الأخطاء المتقدمة - Advanced Error Handling")
اطبع("=" * 70)

# ═══════════════════════════════════════════════════════════════════
# 1. أنواع الأخطاء الأساسية / Basic Exception Types
# ═══════════════════════════════════════════════════════════════════
اطبع("\n1️⃣  أنواع الأخطاء الأساسية:")
اطبع("-" * 50)

# خطأ القسمة على صفر
محاولة:
    النتيجة = 10 / 0
خطأ خطأ_قسمة_على_صفر كـ خ:
    اطبع("❌ خطأ القسمة على صفر: " + نص(خ))

# خطأ فهرس خارج النطاق
محاولة:
    القائمة = [1، 2، 3]
    العنصر = القائمة[10]
خطأ خطأ_الفهرس كـ خ:
    اطبع("❌ خطأ الفهرس: " + نص(خ))

# خطأ المفتاح غير موجود
محاولة:
    القاموس = {"الاسم": "أحمد"}
    القيمة = القاموس["العمر"]
خطأ خطأ_المفتاح كـ خ:
    اطبع("❌ خطأ المفتاح: " + نص(خ))

# خطأ نوع البيانات
محاولة:
    النتيجة = "نص" + 5
خطأ خطأ_النوع كـ خ:
    اطبع("❌ خطأ النوع: " + نص(خ))

# ═══════════════════════════════════════════════════════════════════
# 2. معالجة أخطاء متعددة / Multiple Exception Handling
# ═══════════════════════════════════════════════════════════════════
اطبع("\n2️⃣  معالجة أخطاء متعددة:")
اطبع("-" * 50)

دالة اقسم_آمن(البسط، المقام):
    """قسمة مع معالجة عدة أخطاء"""
    محاولة:
        # تحويل إلى أرقام
        البسط_رقم = عدد_عشري(البسط)
        المقام_رقم = عدد_عشري(المقام)
        
        # القسمة
        النتيجة = البسط_رقم / المقام_رقم
        إرجاع النتيجة
        
    خطأ خطأ_قسمة_على_صفر:
        اطبع("❌ لا يمكن القسمة على صفر!")
        إرجاع لا_شيء
        
    خطأ خطأ_القيمة:
        اطبع("❌ القيمة المدخلة ليست رقماً!")
        إرجاع لا_شيء
        
    خطأ خطأ_النوع:
        اطبع("❌ نوع البيانات غير صحيح!")
        إرجاع لا_شيء

اطبع("✅ اختبار القسمة الآمنة:")
اطبع("   10 / 2 = " + نص(اقسم_آمن(10، 2)))
اطبع("   10 / 0 = " + نص(اقسم_آمن(10، 0)))
اطبع("   'نص' / 2 = " + نص(اقسم_آمن("نص"، 2)))

# ═══════════════════════════════════════════════════════════════════
# 3. الأخطاء المخصصة / Custom Exceptions
# ═══════════════════════════════════════════════════════════════════
اطبع("\n3️⃣  الأخطاء المخصصة:")
اطبع("-" * 50)

صنف خطأ_الرصيد_غير_كاف يرث خطأ:
    """خطأ مخصص عندما يكون الرصيد غير كافٍ"""
    
    بناء(الرصيد_الحالي، المبلغ_المطلوب):
        الرسالة = "الرصيد غير كافٍ! الرصيد: " + نص(الرصيد_الحالي) + "، المطلوب: " + نص(المبلغ_المطلوب)
        أساسي.بناء(الرسالة)
        هذا.رصيد_حالي = الرصيد_الحالي
        هذا.مبلغ_مطلوب = المبلغ_المطلوب

صنف خطأ_الحساب_مغلق يرث خطأ:
    """خطأ مخصص عندما يكون الحساب مغلقاً"""
    
    بناء(رقم_الحساب):
        الرسالة = "الحساب " + رقم_الحساب + " مغلق!"
        أساسي.بناء(الرسالة)

صنف حساب_بنكي_محمي:
    """حساب بنكي مع معالجة أخطاء متقدمة"""
    
    بناء(رقم_الحساب، الرصيد_الأولي):
        هذا.رقم = رقم_الحساب
        هذا.رصيد = الرصيد_الأولي
        هذا.نشط = صحيح
        هذا.سجل_العمليات = []
    
    دالة سحب(المبلغ):
        """سحب مبلغ من الحساب"""
        # التحقق من حالة الحساب
        إذا ليس هذا.نشط:
            ارفع خطأ_الحساب_مغلق(هذا.رقم)
        
        # التحقق من المبلغ
        إذا المبلغ <= 0:
            ارفع خطأ_القيمة("المبلغ يجب أن يكون موجباً")
        
        # التحقق من الرصيد
        إذا المبلغ > هذا.رصيد:
            ارفع خطأ_الرصيد_غير_كاف(هذا.رصيد، المبلغ)
        
        # السحب
        هذا.رصيد = هذا.رصيد - المبلغ
        هذا.سجل_العمليات.أضف("سحب: " + نص(المبلغ))
        إرجاع صحيح
    
    دالة إيداع(المبلغ):
        """إيداع مبلغ في الحساب"""
        إذا ليس هذا.نشط:
            ارفع خطأ_الحساب_مغلق(هذا.رقم)
        
        إذا المبلغ <= 0:
            ارفع خطأ_القيمة("المبلغ يجب أن يكون موجباً")
        
        هذا.رصيد = هذا.رصيد + المبلغ
        هذا.سجل_العمليات.أضف("إيداع: " + نص(المبلغ))
        إرجاع صحيح
    
    دالة أغلق_الحساب():
        """إغلاق الحساب"""
        هذا.نشط = خطأ
        اطبع("✅ تم إغلاق الحساب " + هذا.رقم)

# اختبار الأخطاء المخصصة
الحساب = جديد حساب_بنكي_محمي("SA12345"، 5000)

اطبع("✅ عمليات الحساب:")
محاولة:
    الحساب.إيداع(2000)
    اطبع("   ✓ إيداع 2000 - الرصيد: " + نص(الحساب.رصيد))
    
    الحساب.سحب(3000)
    اطبع("   ✓ سحب 3000 - الرصيد: " + نص(الحساب.رصيد))
    
    # محاولة سحب أكثر من الرصيد
    الحساب.سحب(10000)
    
خطأ خطأ_الرصيد_غير_كاف كـ خ:
    اطبع("   ❌ " + نص(خ))
    اطبع("   الرصيد الحالي: " + نص(خ.رصيد_حالي))

# محاولة استخدام حساب مغلق
اطبع("\n✅ اختبار حساب مغلق:")
الحساب.أغلق_الحساب()
محاولة:
    الحساب.سحب(100)
خطأ خطأ_الحساب_مغلق كـ خ:
    اطبع("   ❌ " + نص(خ))

# ═══════════════════════════════════════════════════════════════════
# 4. finally - التنفيذ دائماً / Finally Block
# ═══════════════════════════════════════════════════════════════════
اطبع("\n4️⃣  كتلة finally:")
اطبع("-" * 50)

دالة اقرأ_ملف_آمن(اسم_الملف):
    """قراءة ملف مع تنظيف مضمون"""
    الملف = لا_شيء
    
    محاولة:
        اطبع("📂 فتح الملف: " + اسم_الملف)
        الملف = افتح(اسم_الملف، "ق")
        المحتوى = الملف.اقرأ()
        اطبع("✅ تم قراءة الملف بنجاح")
        إرجاع المحتوى
        
    خطأ خطأ_الملف كـ خ:
        اطبع("❌ خطأ في قراءة الملف: " + نص(خ))
        إرجاع لا_شيء
        
    أخيراً:
        اطبع("🔒 تنظيف الموارد...")
        إذا الملف:
            الملف.أغلق()
            اطبع("   ✓ تم إغلاق الملف")

اطبع("✅ اختبار قراءة ملف:")
المحتوى = اقرأ_ملف_آمن("اختبار.txt")

# ═══════════════════════════════════════════════════════════════════
# 5. التأكيدات (Assertions) / Assertions
# ═══════════════════════════════════════════════════════════════════
اطبع("\n5️⃣  التأكيدات:")
اطبع("-" * 50)

دالة احسب_الجذر_التربيعي(العدد):
    """حساب الجذر التربيعي مع تأكيدات"""
    تأكد العدد >= 0، "العدد يجب أن يكون موجباً أو صفراً"
    
    محاولة:
        النتيجة = العدد ** 0.5
        إرجاع النتيجة
    خطأ خطأ_التأكيد كـ خ:
        اطبع("❌ فشل التأكيد: " + نص(خ))
        إرجاع لا_شيء

اطبع("✅ اختبار التأكيدات:")
اطبع("   √16 = " + نص(احسب_الجذر_التربيعي(16)))
اطبع("   √-9 = " + نص(احسب_الجذر_التربيعي(-9)))

دالة اقسم_مع_تأكيد(أ، ب):
    """قسمة مع تأكيدات"""
    تأكد isinstance(أ، (رقم، عشري))، "أ يجب أن يكون رقماً"
    تأكد isinstance(ب، (رقم، عشري))، "ب يجب أن يكون رقماً"
    تأكد ب != 0، "المقام لا يمكن أن يكون صفراً"
    
    إرجاع أ / ب

اطبع("✅ القسمة مع التأكيد:")
محاولة:
    اطبع("   10 / 2 = " + نص(اقسم_مع_تأكيد(10، 2)))
    اطبع("   10 / 0 = " + نص(اقسم_مع_تأكيد(10، 0)))
خطأ خطأ_التأكيد كـ خ:
    اطبع("   ❌ " + نص(خ))

# ═══════════════════════════════════════════════════════════════════
# 6. سلسلة الأخطاء (Exception Chaining)
# ═══════════════════════════════════════════════════════════════════
اطبع("\n6️⃣  سلسلة الأخطاء:")
اطبع("-" * 50)

صنف خطأ_معالجة_البيانات يرث خطأ:
    """خطأ في معالجة البيانات"""
    مرر

دالة عالج_البيانات(البيانات):
    """معالجة بيانات مع سلسلة أخطاء"""
    محاولة:
        # محاولة تحويل إلى رقم
        القيمة = رقم(البيانات)
        إرجاع القيمة * 2
        
    خطأ خطأ_القيمة كـ خ_الأصلي:
        # رفع خطأ جديد مع الاحتفاظ بالخطأ الأصلي
        ارفع خطأ_معالجة_البيانات("فشل معالجة البيانات") من خ_الأصلي

اطبع("✅ اختبار سلسلة الأخطاء:")
محاولة:
    النتيجة = عالج_البيانات("نص غير صالح")
خطأ خطأ_معالجة_البيانات كـ خ:
    اطبع("❌ خطأ: " + نص(خ))
    إذا خ.__السبب__:
        اطبع("   السبب: " + نص(خ.__السبب__))

# ═══════════════════════════════════════════════════════════════════
# 7. معالج الأخطاء العام (Context Manager)
# ═══════════════════════════════════════════════════════════════════
اطبع("\n7️⃣  معالج السياق:")
اطبع("-" * 50)

صنف مؤقت_الأداء:
    """معالج سياق لقياس الأداء"""
    
    بناء(اسم_العملية):
        هذا.اسم = اسم_العملية
        هذا.وقت_البداية = لا_شيء
    
    دالة __ادخل__(هذا):
        اطبع("⏱️  بدء: " + هذا.اسم)
        هذا.وقت_البداية = احصل_على_الوقت()
        إرجاع هذا
    
    دالة __اخرج__(هذا، نوع_الخطأ، قيمة_الخطأ، تتبع):
        وقت_النهاية = احصل_على_الوقت()
        المدة = وقت_النهاية - هذا.وقت_البداية
        اطبع("✅ انتهى: " + هذا.اسم + " (المدة: " + نص(المدة) + ")")
        
        إذا نوع_الخطأ:
            اطبع("❌ حدث خطأ: " + نص(قيمة_الخطأ))
            إرجاع خطأ  # عدم تجاهل الخطأ

اطبع("✅ استخدام معالج السياق:")
مع مؤقت_الأداء("عملية حسابية") كـ مؤقت:
    المجموع = 0
    لكل ر في نطاق(1000000):
        المجموع = المجموع + ر
    اطبع("   المجموع: " + نص(المجموع))

# ═══════════════════════════════════════════════════════════════════
# 8. تطبيق عملي: نظام التحقق من صحة البيانات
# ═══════════════════════════════════════════════════════════════════
اطبع("\n8️⃣  تطبيق: نظام التحقق من صحة البيانات:")
اطبع("-" * 50)

صنف خطأ_التحقق يرث خطأ:
    """خطأ التحقق من البيانات"""
    مرر

صنف مُحقق:
    """صنف للتحقق من صحة البيانات"""
    
    @ثابت
    دالة حقق_البريد(البريد):
        """التحقق من صحة البريد الإلكتروني"""
        إذا "@" ليس في البريد:
            ارفع خطأ_التحقق("البريد الإلكتروني يجب أن يحتوي على @")
        
        إذا "." ليس في البريد.قسم("@")[1]:
            ارفع خطأ_التحقق("البريد الإلكتروني غير صحيح")
        
        إرجاع صحيح
    
    @ثابت
    دالة حقق_العمر(العمر):
        """التحقق من صحة العمر"""
        إذا ليس isinstance(العمر، رقم):
            ارفع خطأ_التحقق("العمر يجب أن يكون رقماً")
        
        إذا العمر < 0 أو العمر > 150:
            ارفع خطأ_التحقق("العمر يجب أن يكون بين 0 و 150")
        
        إرجاع صحيح
    
    @ثابت
    دالة حقق_الهاتف(الهاتف):
        """التحقق من صحة رقم الهاتف"""
        الهاتف_نظيف = الهاتف.استبدل(" "، "").استبدل("-"، "")
        
        إذا ليس الهاتف_نظيف.رقمي():
            ارفع خطأ_التحقق("رقم الهاتف يجب أن يحتوي على أرقام فقط")
        
        إذا الهاتف_نظيف.طول() < 9 أو الهاتف_نظيف.طول() > 15:
            ارفع خطأ_التحقق("طول رقم الهاتف غير صحيح")
        
        إرجاع صحيح

صنف مستخدم:
    """صنف مستخدم مع تحقق من البيانات"""
    
    بناء(الاسم، البريد، العمر، الهاتف):
        # التحقق من جميع البيانات
        الأخطاء = []
        
        محاولة:
            مُحقق.حقق_البريد(البريد)
        خطأ خطأ_التحقق كـ خ:
            الأخطاء.أضف("البريد: " + نص(خ))
        
        محاولة:
            مُحقق.حقق_العمر(العمر)
        خطأ خطأ_التحقق كـ خ:
            الأخطاء.أضف("العمر: " + نص(خ))
        
        محاولة:
            مُحقق.حقق_الهاتف(الهاتف)
        خطأ خطأ_التحقق كـ خ:
            الأخطاء.أضف("الهاتف: " + نص(خ))
        
        # إذا كان هناك أخطاء، ارفع خطأ واحد يحتوي على كل الأخطاء
        إذا الأخطاء:
            رسالة_الخطأ = "أخطاء التحقق:\n  - " + "\n  - ".ادمج(الأخطاء)
            ارفع خطأ_التحقق(رسالة_الخطأ)
        
        # إذا كانت البيانات صحيحة، حفظها
        هذا.اسم = الاسم
        هذا.بريد = البريد
        هذا.عمر = العمر
        هذا.هاتف = الهاتف

# اختبار التحقق
اطبع("✅ إنشاء مستخدم صحيح:")
محاولة:
    مستخدم1 = جديد مستخدم("أحمد محمد"، "ahmed@example.com"، 25، "0501234567")
    اطبع("   ✓ تم إنشاء المستخدم بنجاح")
خطأ خطأ_التحقق كـ خ:
    اطبع("   ❌ " + نص(خ))

اطبع("\n✅ إنشاء مستخدم ببيانات خاطئة:")
محاولة:
    مستخدم2 = جديد مستخدم("فاطمة"، "بريد-خاطئ"، 200، "123")
خطأ خطأ_التحقق كـ خ:
    اطبع("   ❌ " + نص(خ))

# ═══════════════════════════════════════════════════════════════════
# 9. معالجة الأخطاء في دوال Async
# ═══════════════════════════════════════════════════════════════════
اطبع("\n9️⃣  معالجة أخطاء الدوال غير المتزامنة:")
اطبع("-" * 50)

دالة غير_متزامنة جلب_بيانات_المستخدم(معرف_المستخدم):
    """جلب بيانات مستخدم بشكل غير متزامن"""
    محاولة:
        # محاكاة جلب البيانات
        انتظر نم(0.1)
        
        إذا معرف_المستخدم <= 0:
            ارفع خطأ_القيمة("معرف المستخدم يجب أن يكون موجباً")
        
        إذا معرف_المستخدم > 1000:
            ارفع خطأ_التحقق("معرف المستخدم غير موجود")
        
        # إرجاع بيانات وهمية
        إرجاع {
            "المعرف": معرف_المستخدم،
            "الاسم": "مستخدم " + نص(معرف_المستخدم)،
            "الحالة": "نشط"
        }
    
    خطأ خطأ كـ خ:
        اطبع("❌ خطأ في جلب البيانات: " + نص(خ))
        إرجاع لا_شيء

دالة غير_متزامنة اختبار_جلب_البيانات():
    """اختبار جلب البيانات"""
    اطبع("✅ جلب بيانات صحيحة:")
    البيانات = انتظر جلب_بيانات_المستخدم(5)
    إذا البيانات:
        اطبع("   " + نص(البيانات))
    
    اطبع("✅ جلب بيانات خاطئة:")
    البيانات = انتظر جلب_بيانات_المستخدم(-1)
    البيانات = انتظر جلب_بيانات_المستخدم(5000)

# تشغيل الاختبار
شغل(اختبار_جلب_البيانات())

# ═══════════════════════════════════════════════════════════════════
# 10. أفضل الممارسات في معالجة الأخطاء
# ═══════════════════════════════════════════════════════════════════
اطبع("\n🔟 أفضل الممارسات:")
اطبع("-" * 50)

# ✅ جيد: استخدام أخطاء محددة
دالة احفظ_المستخدم_جيد(البيانات):
    محاولة:
        # عمليات الحفظ
        مرر
    خطأ خطأ_قاعدة_البيانات كـ خ:
        # معالجة محددة لخطأ قاعدة البيانات
        اطبع("خطأ في قاعدة البيانات: " + نص(خ))
    خطأ خطأ_التحقق كـ خ:
        # معالجة محددة لخطأ التحقق
        اطبع("خطأ في التحقق: " + نص(خ))

# ❌ سيء: استخدام خطأ عام
دالة احفظ_المستخدم_سيء(البيانات):
    محاولة:
        # عمليات الحفظ
        مرر
    خطأ:  # أي خطأ - غير محدد
        اطبع("حدث خطأ!")  # رسالة غير مفيدة

اطبع("✅ أفضل الممارسات:")
اطبع("   1. استخدم أخطاء محددة، وليس catch-all")
اطبع("   2. أنشئ أخطاء مخصصة للمشاكل الخاصة بتطبيقك")
اطبع("   3. وفر رسائل خطأ واضحة ومفيدة")
اطبع("   4. استخدم finally للتنظيف المضمون")
اطبع("   5. لا تتجاهل الأخطاء بصمت")
اطبع("   6. سجل الأخطاء للمراجعة")
اطبع("   7. استخدم التأكيدات للتحقق من الافتراضات")
اطبع("   8. أعد رفع الأخطاء إذا لم تستطع معالجتها")

# ═══════════════════════════════════════════════════════════════════
اطبع("\n" + "=" * 70)
اطبع("🎉 الحمد لله! اكتملت أمثلة معالجة الأخطاء المتقدمة بنجاح")
اطبع("=" * 70)
اطبع("📚 تم اختبار:")
اطبع("   ✅ أنواع الأخطاء الأساسية")
اطبع("   ✅ معالجة أخطاء متعددة")
اطبع("   ✅ الأخطاء المخصصة")
اطبع("   ✅ كتلة finally")
اطبع("   ✅ التأكيدات (Assertions)")
اطبع("   ✅ سلسلة الأخطاء")
اطبع("   ✅ معالج السياق")
اطبع("   ✅ نظام التحقق من البيانات")
اطبع("   ✅ معالجة أخطاء Async")
اطبع("   ✅ أفضل الممارسات")
اطبع("")
اطبع("🤲 اللهم اجعل عملنا خالصاً لوجهك الكريم")
اطبع("=" * 70)

# الحمد لله رب العالمين
