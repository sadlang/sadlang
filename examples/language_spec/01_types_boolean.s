# بسم الله الرحمن الرحيم
# ═══════════════════════════════════════════════════════════
# ملف أمثلة: القيم المنطقية (Boolean)
# File: Boolean Examples
# ═══════════════════════════════════════════════════════════

# القواعد النحوية المستخدمة / EBNF Rules Used:
# ═══════════════════════════════════════════════════════════
# boolean_literal ::= 'صحيح' | 'خطأ'
# logical_expr ::= expr ('و' | 'أو' | 'ليس') expr
# comparison_expr ::= expr ('==' | '!=' | '<' | '>' | '<=' | '>=') expr
# ═══════════════════════════════════════════════════════════

اطبع("      اختبار القيم المنطقية - Boolean Test")


# 1. القيم المنطقية الأساسية / Basic Boolean Values
اطبع("\n1. القيم المنطقية الأساسية:")
منطقي صحيح_دائماً = صحيح
منطقي خطأ_دائماً = خطأ
منطقي هل_مطر = صحيح
منطقي هل_شمس = خطأ

اطبع("صحيح = " + صحيح_دائماً)
اطبع("خطأ = " + خطأ_دائماً)
اطبع("هل_مطر = " + هل_مطر)
اطبع("هل_شمس = " + هل_شمس)

# 2. العمليات المنطقية / Logical Operations
اطبع("\n2. العمليات المنطقية:")

منطقي أ = صحيح
منطقي ب = خطأ

اطبع("أ = صحيح، ب = خطأ")
اطبع("أ و ب = ")
إذا أ و ب 
    اطبع("صحيح")
وإلا 
    اطبع("خطأ")
نهاية

اطبع("أ أو ب = ")
إذا أ أو ب 
    اطبع("صحيح")
وإلا 
    اطبع("خطأ")
نهاية

اطبع("ليس أ = ")
إذا ليس أ 
    اطبع("صحيح")
وإلا 
    اطبع("خطأ")
نهاية

# 3. عمليات المقارنة / Comparison Operations
اطبع("\n3. عمليات المقارنة:")

رقم الأول = 10
رقم الثاني = 20

منطقي أصغر = الأول < الثاني
منطقي أكبر = الأول > الثاني
منطقي يساوي = الأول == الثاني
منطقي لا_يساوي = الأول != الثاني

اطبع("10 < 20: " + أصغر)
اطبع("10 > 20: " + أكبر)
اطبع("10 == 20: " + يساوي)
اطبع("10 != 20: " + لا_يساوي)

# 4. الشروط المركبة / Compound Conditions
اطبع("\n4. الشروط المركبة:")

رقم العمر = 25
رقم الدرجة = 85

منطقي بالغ = العمر >= 18
منطقي ناجح = الدرجة >= 60
منطقي بالغ_وناجح = بالغ و ناجح

اطبع("العمر: " + العمر)
اطبع("الدرجة: " + الدرجة)
اطبع("بالغ؟ " + بالغ)
اطبع("ناجح؟ " + ناجح)
اطبع("بالغ وناجح؟ " + بالغ_وناجح)

# 5. جداول الحقيقة / Truth Tables
اطبع("\n5. جداول الحقيقة - جدول AND:")
اطبع("صحيح و صحيح = ")
إذا صحيح و صحيح 
    اطبع("صحيح")
نهاية

اطبع("صحيح و خطأ = ")
إذا صحيح و خطأ 
    اطبع("صحيح")
وإلا 
    اطبع("خطأ")
نهاية

اطبع("خطأ و خطأ = ")
إذا خطأ و خطأ 
    اطبع("صحيح")
وإلا 
    اطبع("خطأ")
نهاية

اطبع("\nجدول OR:")
اطبع("صحيح أو صحيح = ")
إذا صحيح أو صحيح 
    اطبع("صحيح")
نهاية

اطبع("صحيح أو خطأ = ")
إذا صحيح أو خطأ 
    اطبع("صحيح")
نهاية

اطبع("خطأ أو خطأ = ")
إذا خطأ أو خطأ 
    اطبع("صحيح")
وإلا 
    اطبع("خطأ")
نهاية

# 6. أمثلة واقعية / Real-world Examples
اطبع("\n6. أمثلة واقعية:")

# مثال: التحقق من صلاحية الدخول
رقم عمر_المستخدم = 20
نص كلمة_المرور = "سري123"
منطقي لديه_حساب = صحيح

منطقي يمكنه_الدخول = (عمر_المستخدم >= 18) و (كلمة_المرور == "سري123") و لديه_حساب

اطبع("التحقق من صلاحية الدخول:")
اطبع("عمر المستخدم: " + عمر_المستخدم)
اطبع("لديه حساب؟ " + لديه_حساب)
اطبع("يمكنه الدخول؟ " + يمكنه_الدخول)

# مثال: التحقق من أهلية المنحة
رقم دخل_الأسرة = 3000
رقم معدل_الطالب = 3.5
منطقي مواطن = صحيح

منطقي مؤهل_للمنحة = (دخل_الأسرة < 5000) و (معدل_الطالب >= 3.0) و مواطن

اطبع("\nالتحقق من أهلية المنحة:")
اطبع("دخل الأسرة: " + دخل_الأسرة)
اطبع("معدل الطالب: " + معدل_الطالب)
اطبع("مواطن؟ " + مواطن)
اطبع("مؤهل للمنحة؟ " + مؤهل_للمنحة)

# 7. القصر الدائري (Short-circuit Evaluation)
اطبع("\n7. التقييم القصير:")
منطقي شرط_واحد = صحيح
منطقي شرط_اثنان = خطأ

اطبع("شرط_واحد أو شرط_اثنان:")
إذا شرط_واحد أو شرط_اثنان 
    اطبع("صحيح (لم يتم تقييم الشرط الثاني)")
نهاية

اطبع("شرط_اثنان و شرط_واحد:")
إذا شرط_اثنان و شرط_واحد 
    اطبع("صحيح")
وإلا 
    اطبع("خطأ (لم يتم تقييم الشرط الثاني)")
نهاية

اطبع("\n" + "=" * 60)
اطبع("      الحمد لله! اكتملت الاختبارات بنجاح")
اطبع("=" * 60)
