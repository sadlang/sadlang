# صفحة الترحيب الرسومية
# Welcome Screen Example
دالة رئيسية ()
اطبع("=== برنامج صفحة الترحيب ===\n");

# إنشاء النافذة الرئيسية
متغير نافذتي = نافذة_جديد("مرحبا بكم", 800, 600);
اطبع("تم إنشاء النافذة\n");

# إنشاء الرسام
متغير رسامي = رسام_ثنائي_جديد(نافذتي);
اطبع("تم إنشاء الرسام\n");

# عرض النافذة
نافذة_عرض(نافذتي);
اطبع("تم عرض النافذة\n");

# رسم الإطار الأول
رسام_بدء_إطار(رسامي);

اطبع("\n=== اكتمل البرنامج بنجاح ===\n");
نهاية