# ═══════════════════════════════════════════════
# اختبار صيغة Flutter — لغة ص
# Flutter-style declarative UI test
# ═══════════════════════════════════════════════

استورد واجهات

اطبع_سطر("=== اختبار صيغة Flutter ===")
اطبع_سطر("")

# ─── اختبار 1: نص بسيط مع خصائص مسمّاة ───
اطبع_سطر("--- اختبار 1: نص_واجهة مع خصائص ---")
متغير ن = نص_واجهة("مرحبا بالعالم"، لون: "احمر"، حجم_خط: 24)
اطبع_سطر("معرّف النص: " + ن)

# ─── اختبار 2: زر مع خصائص مسمّاة ───
اطبع_سطر("")
اطبع_سطر("--- اختبار 2: زر مع خصائص ---")
متغير ز = زر("اضغط هنا"، لون_زر: "ازرق"، حجم_خط: 18)
اطبع_سطر("معرّف الزر: " + ز)

# ─── اختبار 3: عمود يحتوي على أبناء ───
اطبع_سطر("")
اطبع_سطر("--- اختبار 3: عمود مع أبناء ---")
متغير ع = عمود(
    تباعد: 10،
    خلفية: "ابيض"،
    نص_واجهة("العنوان"، حجم_خط: 28، عريض: صحيح)،
    نص_واجهة("الوصف"، لون: "رمادي")،
    زر("ابدأ"، لون_زر: "اخضر")
)
اطبع_سطر("معرّف العمود: " + ع)

# ─── اختبار 4: صف مع أزرار ───
اطبع_سطر("")
اطبع_سطر("--- اختبار 4: صف مع أزرار ---")
متغير ص = صف(
    تباعد: 8،
    زر("حفظ"، لون_زر: "اخضر")،
    زر("إلغاء"، لون_زر: "احمر")،
    زر("تعديل"، لون_زر: "ازرق")
)
اطبع_سطر("معرّف الصف: " + ص)

# ─── اختبار 5: واجهة متداخلة بنمط Flutter ───
اطبع_سطر("")
اطبع_سطر("--- اختبار 5: واجهة متداخلة ---")
متغير التطبيق = عمود(
    خلفية: "ابيض"،
    حشوة: 16،
    عمود(
        تباعد: 12،
        نص_واجهة("أهلاً بك!"، حجم_خط: 32، عريض: صحيح)،
        نص_واجهة("هذا تطبيق بلغة ص"، لون: "رمادي"، حجم_خط: 16)
    )،
    صف(
        تباعد: 8،
        زر("تسجيل دخول"، لون_زر: "ازرق"، عرض: 150)،
        زر("إنشاء حساب"، لون_زر: "اخضر"، عرض: 150)
    )
)
اطبع_سطر("معرّف التطبيق: " + التطبيق)

# ─── اختبار 6: الصيغة القديمة ما زالت تعمل ───
اطبع_سطر("")
اطبع_سطر("--- اختبار 6: صيغة قديمة ---")
متغير ق = نص_واجهة("نص قديم"، "لون"، "اسود"، "حجم_خط"، 14)
اطبع_سطر("معرّف النص القديم: " + ق)

اطبع_سطر("")
اطبع_سطر("✓ جميع اختبارات Flutter نجحت!")
