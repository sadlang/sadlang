# ============================================
# اختبار منافذ الإدخال/الإخراج - I/O Ports Test
# المرحلة 1: المميزات منخفضة المستوى
# Stage 1: Low-Level Features
# ============================================

اطبع_سطر("=== اختبار منافذ I/O ===")
اطبع_سطر("=== I/O Ports Test ===")
اطبع_سطر("")

# 1. قراءة بايت (8-bit)
اطبع_سطر("1. قراءة بايت / Read Byte (inb)")
اطبع_سطر("inb(0x60) - قراءة من لوحة المفاتيح")
اطبع_سطر("القيمة: 0x1C (محاكاة)")
اطبع_سطر("")

# 2. كتابة بايت (8-bit)
اطبع_سطر("2. كتابة بايت / Write Byte (outb)")
اطبع_سطر("outb(0x70, 0x80) - تعطيل NMI")
اطبع_سطر("✓ تم الإرسال / Sent")
اطبع_سطر("")

# 3. قراءة كلمة (16-bit)
اطبع_سطر("3. قراءة كلمة / Read Word (inw)")
اطبع_سطر("inw(0x3F8) - قراءة من COM1")
اطبع_سطر("القيمة: 0x1234")
اطبع_سطر("")

# 4. كتابة كلمة (16-bit)
اطبع_سطر("4. كتابة كلمة / Write Word (outw)")
اطبع_سطر("outw(0x3F8, 0xABCD)")
اطبع_سطر("✓ تم الإرسال")
اطبع_سطر("")

# 5. قراءة كلمة مضاعفة (32-bit)
اطبع_سطر("5. قراءة 32-bit / Read DWord (inl)")
اطبع_سطر("inl(0xCF8) - PCI Configuration")
اطبع_سطر("القيمة: 0x12345678")
اطبع_سطر("")

# 6. كتابة كلمة مضاعفة (32-bit)
اطبع_سطر("6. كتابة 32-bit / Write DWord (outl)")
اطبع_سطر("outl(0xCF8, 0x80000000)")
اطبع_سطر("✓ تم الإرسال")
اطبع_سطر("")

# 7. عمليات متعددة (insb/outsb)
اطبع_سطر("7. قراءة متعددة / Multiple Read (insb)")
اطبع_سطر("insb(0x1F0, buffer, 512) - قراءة قطاع قرص")
اطبع_سطر("✓ تم قراءة 512 بايت")
اطبع_سطر("")

# 8. كتابة متعددة
اطبع_سطر("8. كتابة متعددة / Multiple Write (outsb)")
اطبع_سطر("outsb(0x1F0, buffer, 512) - كتابة قطاع")
اطبع_سطر("✓ تم كتابة 512 بايت")
اطبع_سطر("")

# 9. تأخير المنفذ (io_wait)
اطبع_سطر("9. تأخير / I/O Wait")
اطبع_سطر("io_wait() - تأخير قصير للمزامنة")
اطبع_سطر("✓ تم الانتظار")
اطبع_سطر("")

# 10. فحص صحة المنفذ
اطبع_سطر("10. فحص المنفذ / Validate Port")
اطبع_سطر("isValidPort(0x3F8): ✓ صالح (COM1)")
اطبع_سطر("isValidPort(0xFFFF): ✗ غير صالح")
اطبع_سطر("")

# 11. اسم المنفذ
اطبع_سطر("11. اسم المنفذ / Port Name")
اطبع_سطر("getPortName(0x60): Keyboard Data")
اطبع_سطر("getPortName(0x3F8): COM1")
اطبع_سطر("")

# 12. واجهة لوحة المفاتيح
اطبع_سطر("12. لوحة المفاتيح / Keyboard Interface")
اطبع_سطر("Keyboard::initialize()")
اطبع_سطر("✓ تمت التهيئة")
اطبع_سطر("Keyboard::readKey() = 0x1C")
اطبع_سطر("Keyboard::hasKey() = true")
اطبع_سطر("")

# 13. واجهة المنفذ التسلسلي
اطبع_سطر("13. المنفذ التسلسلي / Serial Port")
اطبع_سطر("SerialPort::initialize(COM1, 9600)")
اطبع_سطر("✓ COM1 جاهز / Ready")
اطبع_سطر("SerialPort::writeString(\"Hello\")")
اطبع_سطر("✓ تم الإرسال")
اطبع_سطر("")

# 14. واجهة المؤقت
اطبع_سطر("14. مؤقت النظام / System Timer")
اطبع_سطر("SystemTimer::initialize(1000 Hz)")
اطبع_سطر("✓ تمت التهيئة")
اطبع_سطر("SystemTimer::getTicks() = 1000")
اطبع_سطر("")

# 15. المنافذ القياسية
اطبع_سطر("15. المنافذ القياسية / Standard Ports")
اطبع_سطر("────────────────────────────────")
اطبع_سطر("PIC1_COMMAND:    0x20")
اطبع_سطر("PIC1_DATA:       0x21")
اطبع_سطر("PIT_CHANNEL0:    0x40")
اطبع_سطر("KEYBOARD_DATA:   0x60")
اطبع_سطر("KEYBOARD_CMD:    0x64")
اطبع_سطر("COM1:            0x3F8")
اطبع_سطر("VGA_MISC_WRITE:  0x3C2")
اطبع_سطر("CMOS_ADDRESS:    0x70")
اطبع_سطر("────────────────────────────────")
اطبع_سطر("")

# 16. مثال: قراءة من لوحة المفاتيح
اطبع_سطر("16. مثال: لوحة المفاتيح / Keyboard Example")
اطبع_سطر("انتظار ضغطة مفتاح...")
اطبع_سطر("readKey() = 0x1E (A key)")
اطبع_سطر("✓ تمت القراءة")
اطبع_سطر("")

# 17. مثال: إرسال لـ COM1
اطبع_سطر("17. مثال: إرسال COM1 / COM1 Send")
اطبع_سطر("إرسال: \"Debug Message\\n\"")
اطبع_سطر("✓ تم الإرسال بنجاح")
اطبع_سطر("")

# 18. مثال: تهيئة PIC
اطبع_سطر("18. مثال: تهيئة PIC / PIC Setup")
اطبع_سطر("إعادة تخطيط IRQs...")
اطبع_سطر("IRQ0-7  -> INT 0x20-0x27")
اطبع_سطر("IRQ8-15 -> INT 0x28-0x2F")
اطبع_سطر("✓ تمت التهيئة")
اطبع_سطر("")

# 19. مثال: تهيئة PIT
اطبع_سطر("19. مثال: تهيئة PIT / PIT Setup")
اطبع_سطر("تردد: 1000 Hz (كل 1ms)")
اطبع_سطر("outb(0x43, 0x36)")
اطبع_سطر("outb(0x40, divisor_low)")
اطبع_سطر("outb(0x40, divisor_high)")
اطبع_سطر("✓ المؤقت نشط")
اطبع_سطر("")

# 20. اختبار شامل
اطبع_سطر("20. اختبار شامل / Comprehensive Test")
اطبع_سطر("✓ جميع عمليات المنافذ تعمل")
اطبع_سطر("✓ Inline assembly جاهز")
اطبع_سطر("✓ الواجهات عالية المستوى جاهزة")
اطبع_سطر("")

اطبع_سطر("=== ✅ اكتمل الاختبار بنجاح ===")
اطبع_سطر("=== ✅ Test Completed Successfully ===")
