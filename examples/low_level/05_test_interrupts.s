# ============================================
# اختبار معالجة المقاطعات - Interrupt Handling Test
# المرحلة 1: المميزات منخفضة المستوى
# Stage 1: Low-Level Features
# ============================================

اطبع_سطر("=== اختبار معالجة المقاطعات ===")
اطبع_سطر("=== Interrupt Handling Test ===")
اطبع_سطر("")

# 1. تهيئة نظام المقاطعات
اطبع_سطر("1. تهيئة النظام / System Initialization")
اطبع_سطر("InterruptManager::initialize()")
اطبع_سطر("  → إعادة تخطيط PIC")
اطبع_سطر("  → تثبيت معالجات افتراضية")
اطبع_سطر("  → تحميل IDT")
اطبع_سطر("✓ النظام جاهز")
اطبع_سطر("")

# 2. تفعيل المقاطعات
اطبع_سطر("2. تفعيل / Enable Interrupts")
اطبع_سطر("enable() → STI instruction")
اطبع_سطر("✓ المقاطعات مفعلة")
اطبع_سطر("")

# 3. تعطيل المقاطعات
اطبع_سطر("3. تعطيل / Disable Interrupts")
اطبع_سطر("disable() → CLI instruction")
اطبع_سطر("✓ المقاطعات معطلة")
اطبع_سطر("")

# 4. فحص الحالة
اطبع_سطر("4. فحص الحالة / Check Status")
اطبع_سطر("isEnabled() = false")
اطبع_سطر("RFLAGS.IF = 0")
اطبع_سطر("")

# 5. تسجيل معالج
اطبع_سطر("5. تسجيل معالج / Register Handler")
اطبع_سطر("registerHandler(0x20, timerHandler)")
اطبع_سطر("✓ معالج IRQ0 (Timer) مسجل")
اطبع_سطر("")

# 6. إلغاء تسجيل معالج
اطبع_سطر("6. إلغاء التسجيل / Unregister Handler")
اطبع_سطر("unregisterHandler(0x20)")
اطبع_سطر("✓ تم الإلغاء")
اطبع_سطر("")

# 7. فحص وجود معالج
اطبع_سطر("7. فحص المعالج / Check Handler")
اطبع_سطر("hasHandler(0x20) = false")
اطبع_سطر("hasHandler(0x00) = true (معالج افتراضي)")
اطبع_سطر("")

# 8. تفعيل IRQ
اطبع_سطر("8. تفعيل IRQ / Enable IRQ")
اطبع_سطر("enableIRQ(0) - Timer")
اطبع_سطر("✓ IRQ0 مفعل")
اطبع_سطر("")

# 9. تعطيل IRQ
اطبع_سطر("9. تعطيل IRQ / Disable IRQ")
اطبع_سطر("disableIRQ(0)")
اطبع_سطر("✓ IRQ0 معطل")
اطبع_سطر("")

# 10. إرسال EOI
اطبع_سطر("10. إرسال EOI / Send End of Interrupt")
اطبع_سطر("sendEOI(0)")
اطبع_سطر("outb(0x20, 0x20) → Master PIC")
اطبع_سطر("✓ تم الإرسال")
اطبع_سطر("")

# 11. إعداد إدخال IDT
اطبع_سطر("11. إعداد IDT / Setup IDT Entry")
اطبع_سطر("setIDTEntry(0x20, handler, selector, flags)")
اطبع_سطر("✓ إدخال IDT محدّث")
اطبع_سطر("")

# 12. قراءة إدخال IDT
اطبع_سطر("12. قراءة IDT / Read IDT Entry")
اطبع_سطر("getIDTEntry(0x20)")
اطبع_سطر("  offsetLow:    0x1000")
اطبع_سطر("  selector:     0x0008")
اطبع_سطر("  flags:        0x8E")
اطبع_سطر("")

# 13. تحميل IDT
اطبع_سطر("13. تحميل IDT / Load IDT")
اطبع_سطر("loadIDT() → LIDT instruction")
اطبع_سطر("IDT Base:  0x00100000")
اطبع_سطر("IDT Limit: 0x07FF (256 entries)")
اطبع_سطر("✓ تم التحميل")
اطبع_سطر("")

# 14. معالج استثناء عام
اطبع_سطر("14. معالج استثناء / Exception Handler")
اطبع_سطر("handleException(frame)")
اطبع_سطر("  Interrupt: 0x06 (Invalid Opcode)")
اطبع_سطر("  Error Code: 0x0000")
اطبع_سطر("  RIP: 0x00401234")
اطبع_سطر("✓ تمت المعالجة")
اطبع_سطر("")

# 15. معالج خطأ الصفحة
اطبع_سطر("15. خطأ صفحة / Page Fault")
اطبع_سطر("handlePageFault(frame)")
اطبع_سطر("  Faulting Address: 0x00000000")
اطبع_سطر("  Error: Page not present")
اطبع_سطر("  Access: Write")
اطبع_سطر("✓ تمت المعالجة")
اطبع_سطر("")

# 16. معالج الحماية العامة
اطبع_سطر("16. حماية عامة / General Protection Fault")
اطبع_سطر("handleGeneralProtection(frame)")
اطبع_سطر("  Error Code: 0x0008")
اطبع_سطر("  Segment: GDT, Index 1")
اطبع_سطر("✓ تمت المعالجة")
اطبع_سطر("")

# 17. معالج القسمة على صفر
اطبع_سطر("17. قسمة على صفر / Division by Zero")
اطبع_سطر("handleDivideError(frame)")
اطبع_سطر("  RIP: 0x00401000")
اطبع_سطر("  RAX: 0x0000000A")
اطبع_سطر("✓ تمت المعالجة")
اطبع_سطر("")

# 18. إحصائيات المقاطعات
اطبع_سطر("18. الإحصائيات / Statistics")
اطبع_سطر("getInterruptCount(0x20) = 1000 (Timer)")
اطبع_سطر("getInterruptCount(0x21) = 50 (Keyboard)")
اطبع_سطر("getTotalInterrupts() = 1050")
اطبع_سطر("")

# 19. أسماء المقاطعات
اطبع_سطر("19. أسماء المقاطعات / Interrupt Names")
اطبع_سطر("────────────────────────────────")
اطبع_سطر("0x00: Divide Error / خطأ قسمة")
اطبع_سطر("0x0E: Page Fault / خطأ صفحة")
اطبع_سطر("0x20: IRQ0: Timer / مؤقت")
اطبع_سطر("0x21: IRQ1: Keyboard / لوحة مفاتيح")
اطبع_سطر("0x80: System Call / استدعاء نظام")
اطبع_سطر("────────────────────────────────")
اطبع_سطر("")

# 20. قسم حرج
اطبع_سطر("20. القسم الحرج / Critical Section")
اطبع_سطر("CriticalSection cs;")
اطبع_سطر("  → CLI (disable)")
اطبع_سطر("  ... عمليات حرجة ...")
اطبع_سطر("  → STI (enable)")
اطبع_سطر("✓ تمت الحماية")
اطبع_سطر("")

# 21. تقرير المقاطعات
اطبع_سطر("21. التقرير / Interrupt Report")
اطبع_سطر("════════════════════════════════")
اطبع_سطر("حالة النظام / System Status:")
اطبع_سطر("  مهيأ / Initialized: ✓ Yes")
اطبع_سطر("  مفعل / Enabled: ✓ Yes")
اطبع_سطر("  إجمالي / Total: 1050")
اطبع_سطر("")
اطبع_سطر("المقاطعات النشطة / Active:")
اطبع_سطر("  0x00: 5 (Exceptions)")
اطبع_سطر("  0x20: 1000 (Timer)")
اطبع_سطر("  0x21: 50 (Keyboard)")
اطبع_سطر("════════════════════════════════")
اطبع_سطر("")

# 22. مسح الإحصائيات
اطبع_سطر("22. مسح الإحصائيات / Clear Statistics")
اطبع_سطر("clearStatistics()")
اطبع_سطر("✓ تم المسح")
اطبع_سطر("")

# 23. مثال: معالج IRQ0
اطبع_سطر("23. مثال: معالج المؤقت / Timer Handler")
اطبع_سطر("void timerHandler(InterruptFrame* frame) {")
اطبع_سطر("  ticks++;")
اطبع_سطر("  sendEOI(0);")
اطبع_سطر("}")
اطبع_سطر("✓ معالج مسجل وعامل")
اطبع_سطر("")

# 24. مثال: معالج IRQ1
اطبع_سطر("24. مثال: معالج لوحة المفاتيح / Keyboard Handler")
اطبع_سطر("void keyboardHandler(InterruptFrame* frame) {")
اطبع_سطر("  scancode = inb(0x60);")
اطبع_سطر("  processKey(scancode);")
اطبع_سطر("  sendEOI(1);")
اطبع_سطر("}")
اطبع_سطر("✓ معالج مسجل وعامل")
اطبع_سطر("")

# 25. اختبار شامل
اطبع_سطر("25. اختبار شامل / Comprehensive Test")
اطبع_سطر("✓ IDT مهيأة (256 إدخال)")
اطبع_سطر("✓ PIC معاد تخطيطه")
اطبع_سطر("✓ معالجات الاستثناءات جاهزة")
اطبع_سطر("✓ معالجات IRQ جاهزة")
اطبع_سطر("✓ القسم الحرج يعمل")
اطبع_سطر("✓ الإحصائيات متوفرة")
اطبع_سطر("")

اطبع_سطر("=== ✅ اكتمل الاختبار بنجاح ===")
اطبع_سطر("=== ✅ Test Completed Successfully ===")
اطبع_سطر("")
اطبع_سطر("════════════════════════════════════════")
اطبع_سطر("   المرحلة 1 مكتملة بنسبة 100%")
اطبع_سطر("   Stage 1: 100% Complete")
اطبع_سطر("════════════════════════════════════════")
