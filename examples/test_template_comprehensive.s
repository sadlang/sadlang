# ===================================================
# اختبار شامل للقوالب (Templates) في لغة ص
# Comprehensive Template Testing in S Language
# ===================================================

# ======= قالب دالة - Template Function =======
# دالة أكبر: تُرجع القيمة الأكبر من قيمتين
قالب<نوع ت> دالة أكبر(ت أ، ت ب) ترجع ت
    اذا (أ > ب)
        ارجع أ
    نهاية
    ارجع ب
نهاية

# قالب دالة آخر - دالة أصغر
قالب<نوع ت> دالة أصغر(ت أ، ت ب) ت
    إذا(أ < ب)
        ارجع أ
    نهاية
    ارجع ب
نهاية

# ======= الدالة الرئيسية - Main Function =======
دالة رئيسية()
    اطبع("=== اختبار القوالب ===")
    اطبع("")
    
    # اختبار مع أرقام صحيحة
    اطبع(">>> اختبار مع أرقام صحيحة:")
    رقم أ = 10
    رقم ب = 20
    اطبع("أ = " + أ)
    اطبع("ب = " + ب)
    
    # استدعاء دالة القالب مع رقم
    رقم الأكبر = أكبر<رقم>(أ، ب)
    اطبع("أكبر(أ، ب) = " + الأكبر)
    
    رقم الأصغر = أصغر<رقم>(أ، ب)
    اطبع("أصغر(أ، ب) = " + الأصغر)
    
    اطبع("")
    
    # اختبار مع أرقام عشرية
    اطبع(">>> اختبار مع أرقام عشرية:")
    عشري س = 3.14
    عشري ص = 2.71
    اطبع("س = " + س)
    اطبع("ص = " + ص)
    
    عشري الأكبر_عشري = أكبر<عشري>(س، ص)
    اطبع("أكبر(س، ص) = " + الأكبر_عشري)
    
    اطبع("")
    اطبع("✅ تم تنفيذ القوالب بنجاح!")
    
    ارجع 0
نهاية
