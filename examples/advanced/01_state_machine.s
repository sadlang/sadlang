# ═══════════════════════════════════════════════════════════════════════════════
# المثال المتقدم ١: آلة حالة معالج الطلبات (Request State Machine)
# يوضح: تتبع حالة طلب HTTP مع انتقالات معقدة
# ═══════════════════════════════════════════════════════════════════════════════

دالة رئيسية()
    اطبع("═══════════════════════════════════════════════════\n")
    اطبع("   آلة حالة معالج الطلبات\n")
    اطبع("   Request State Machine\n")
    اطبع("═══════════════════════════════════════════════════\n\n")

    # محاكاة سلسلة من حالات الطلب
    اطبع("📡 معالجة طلب HTTP...\n\n")
    
    # الحالة 1: طلب جديد
    رقم حالة١ = 0
    اطبع("الحالة: ")
    طابق حالة١
        حالة 0: اطبع("⏳ في الانتظار (PENDING)\n")
        حالة 1: اطبع("🔄 قيد المعالجة (PROCESSING)\n")
        حالة 2: اطبع("✅ مكتمل (COMPLETED)\n")
        حالة 3: اطبع("❌ فاشل (FAILED)\n")
        حالة 4: اطبع("⏰ انتهت المهلة (TIMEOUT)\n")
        حالة _: اطبع("❓ حالة غير معروفة\n")
    نهاية
    
    # الحالة 2: قيد المعالجة
    رقم حالة٢ = 1
    اطبع("الحالة: ")
    طابق حالة٢
        حالة 0: اطبع("⏳ في الانتظار (PENDING)\n")
        حالة 1: اطبع("🔄 قيد المعالجة (PROCESSING)\n")
        حالة 2: اطبع("✅ مكتمل (COMPLETED)\n")
        حالة 3: اطبع("❌ فاشل (FAILED)\n")
        حالة 4: اطبع("⏰ انتهت المهلة (TIMEOUT)\n")
        حالة _: اطبع("❓ حالة غير معروفة\n")
    نهاية
    
    # الحالة 3: مكتمل
    رقم حالة٣ = 2
    اطبع("الحالة: ")
    طابق حالة٣
        حالة 0: اطبع("⏳ في الانتظار (PENDING)\n")
        حالة 1: اطبع("🔄 قيد المعالجة (PROCESSING)\n")
        حالة 2: اطبع("✅ مكتمل (COMPLETED)\n")
        حالة 3: اطبع("❌ فاشل (FAILED)\n")
        حالة 4: اطبع("⏰ انتهت المهلة (TIMEOUT)\n")
        حالة _: اطبع("❓ حالة غير معروفة\n")
    نهاية

    اطبع("\n✅ انتهى مثال آلة الحالة!\n")
    إرجاع 0
نهاية
