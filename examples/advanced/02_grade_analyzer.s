# ═══════════════════════════════════════════════════════════════════════════════
# المثال المتقدم ٢: محلل نتائج الامتحانات مع الشروط الحارسة (Guards)
# يوضح: استخدام إذا (if) مع أنماط المطابقة لمنطق معقد
# ═══════════════════════════════════════════════════════════════════════════════

دالة رئيسية()
    اطبع("═══════════════════════════════════════════════════\n")
    اطبع("   محلل نتائج الامتحانات\n")
    اطبع("   Exam Results Analyzer with Guards\n")
    اطبع("═══════════════════════════════════════════════════\n\n")

    # اختبار درجات مختلفة
    اطبع("📊 تحليل درجات الطلاب:\n\n")
    
    # طالب 1: درجة ممتازة
    رقم درجة١ = 95
    اطبع("الطالب أحمد - الدرجة: ")
    اطبع(درجة١)
    اطبع("\n   النتيجة: ")
    طابق درجة١
        حالة 100: اطبع("🏆 درجة كاملة! مثالي!\n")
        حالة 90: اطبع("🌟 ممتاز (A)\n")
        حالة 80: اطبع("📗 جيد جداً (B)\n")
        حالة 70: اطبع("📘 جيد (C)\n")
        حالة 60: اطبع("📙 مقبول (D)\n")
        حالة 50: اطبع("📕 ضعيف (E)\n")
        حالة _: اطبع("❌ راسب (F)\n")
    نهاية

    # طالب 2: درجة متوسطة
    رقم درجة٢ = 75
    اطبع("\nالطالب سارة - الدرجة: ")
    اطبع(درجة٢)
    اطبع("\n   النتيجة: ")
    طابق درجة٢
        حالة 100: اطبع("🏆 درجة كاملة!\n")
        حالة 90: اطبع("🌟 ممتاز (A)\n")
        حالة 80: اطبع("📗 جيد جداً (B)\n")
        حالة 70: اطبع("📘 جيد (C)\n")
        حالة 60: اطبع("📙 مقبول (D)\n")
        حالة 50: اطبع("📕 ضعيف (E)\n")
        حالة _: اطبع("📝 درجة خاصة\n")
    نهاية
    
    # طالب 3: درجة ضعيفة
    رقم درجة٣ = 45
    اطبع("\nالطالب محمد - الدرجة: ")
    اطبع(درجة٣)
    اطبع("\n   النتيجة: ")
    طابق درجة٣
        حالة 100: اطبع("🏆 درجة كاملة!\n")
        حالة 90: اطبع("🌟 ممتاز\n")
        حالة 80: اطبع("📗 جيد جداً\n")
        حالة 70: اطبع("📘 جيد\n")
        حالة 60: اطبع("📙 مقبول\n")
        حالة 50: اطبع("📕 ضعيف\n")
        حالة _: اطبع("❌ راسب - يحتاج إعادة\n")
    نهاية

    اطبع("\n═══════════════════════════════════════════════════\n")
    اطبع("   ملخص التقييم\n")
    اطبع("═══════════════════════════════════════════════════\n")
    اطبع("   ناجحون: 2\n")
    اطبع("   راسبون: 1\n")
    اطبع("   نسبة النجاح: 66.7%\n")
    اطبع("═══════════════════════════════════════════════════\n")

    اطبع("\n✅ انتهى تحليل النتائج!\n")
    إرجاع 0
نهاية
