# مثال 4: فحص القيمة قبل الاستخدام (Safe Usage)
# Example 4: Safe Usage with Null Check

دالة معالجة_بيانات(بيانات، الطول_المطلوب)
    # التحقق من null أولاً
    إذا (بيانات == فارغ)
        عرض("لا توجد بيانات")
        إرجاع خطأ
    نهاية
    
    # الآن نعرف أن بيانات ليست فارغة
    # يمكن استخدامها بأمان
    الطول = طول(بيانات)
    عرض("طول البيانات: ")
    عرض(الطول)
    
    إذا (الطول >= الطول_المطلوب)
        عرض("البيانات صالحة: ")
        عرض(بيانات)
        إرجاع صح
    وإلا
        عرض("البيانات قصيرة جداً")
        إرجاع خطأ
    نهاية
نهاية

دالة رئيسية()
    عرض("=== مثال 4: فحص القيمة قبل الاستخدام ===")
    
    # اختبار 1: بيانات صالحة
    عرض("--- اختبار 1: بيانات طويلة ---")
    نتيجة1 = معالجة_بيانات("مرحبا بك في لغة ص البرمجية", 10)
    عرض("النتيجة 1: ")
    عرض(نتيجة1)
    
    # اختبار 2: بيانات قصيرة
    عرض("--- اختبار 2: بيانات قصيرة ---")
    نتيجة2 = معالجة_بيانات("قصير", 10)
    عرض("النتيجة 2: ")
    عرض(نتيجة2)
    
    # اختبار 3: قيمة فارغة
    عرض("--- اختبار 3: قيمة فارغة ---")
    نتيجة3 = معالجة_بيانات(فارغ، 10)
    عرض("النتيجة 3: ")
    عرض(نتيجة3)
نهاية
