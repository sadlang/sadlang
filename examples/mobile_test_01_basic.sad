اطبع("")
اطبع("  اختبار تطبيقات الهاتف - المستوى الأساسي")
اطبع("")

اطبع("")
اطبع("--- معلومات الجهاز ---")
متغير معلومات = هاتف_معلومات()
اطبع(معلومات)

متغير منصة = هاتف_منصة()
اطبع("المنصة: " + منصة)

اطبع("")
اطبع("--- الرسائل ---")
هاتف_رسالة("مرحباً", "هذا تطبيق بلغة ص!")
هاتف_رسالة_سريعة("تم التحميل بنجاح")

اطبع("")
اطبع(" تم اختبار المستوى الأساسي بنجاح!")