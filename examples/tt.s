دالة رئيسية()
    اطبع("البرنامج يعمل\n");
    نص ن = نافذة_جديد("الترحيب", 800, 600);
    اطبع(ن); اطبع("\n");
    نص ر = رسام_ثنائي_جديد(ن);
    اطبع(ر); اطبع("\n");
    نافذة_عرض(ن);
    رسام_بدء_إطار(ر);
    رسام_مسح(ر, 173, 216, 230, 255);
    رسام_مستطيل(ر, 250.0, 200.0, 300.0, 150.0, 255, 255, 255, 200, صحيح);
    رسام_مستطيل(ر, 250.0, 200.0, 300.0, 150.0, 0, 100, 200, 255, خطأ);
    رسام_دائرة(ر, 200.0, 275.0, 40.0, 255, 180, 100, 255, صحيح);
    رسام_دائرة(ر, 600.0, 275.0, 40.0, 255, 180, 100, 255, صحيح);
    رسام_نص(ر, "مرحبا", 320.0, 250.0, 28.0, 0, 0, 128, 255);
    رسام_إنهاء_إطار(ر);
    نافذة_تبديل_buffers(ن);
    اطبع("تم الرسم\n");
    نص ع = 0;
    بينما(ع < 50)
        نافذة_استقبال_أحداث(ن);
        رسام_بدء_إطار(ر);
        رسام_مسح(ر, 173, 216, 230, 255);
        رسام_مستطيل(ر, 250.0, 200.0, 300.0, 150.0, 255, 255, 255, 200, صحيح);
        رسام_نص(ر, "ترحيب", 320.0, 250.0, 28.0, 0, 0, 128, 255);
        رسام_إنهاء_إطار(ر);
        نافذة_تبديل_buffers(ن);
        ع = ع + 1;
    نهاية
    نافذة_إغلاق(ن);
nهاية