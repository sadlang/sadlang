# اختبار القوالب مع كلمة "ترجع" الاختيارية

# 1. قالب بدون كلمة ترجع
قالب<نوع ت> دالة جمع1(ت أ، ت ب) ت
    ارجع أ + ب
نهاية

# 2. قالب مع كلمة ترجع
قالب<نوع س> دالة جمع2(س أ، س ب) ترجع س
    ارجع أ + ب
نهاية

# 3. دالة عادية بدون ترجع
دالة ضرب1(رقم أ، رقم ب) رقم
    ارجع أ * ب
نهاية

# 4. دالة عادية مع ترجع
دالة ضرب2(رقم أ، رقم ب) ترجع رقم
    ارجع أ * ب
نهاية

دالة رئيسية() رقم
    اطبع("=== اختبار كلمة ترجع الاختيارية ===")
    
    # اختبار القوالب
    رقم نتيجة1 = جمع1<رقم>(5، 3)
    رقم نتيجة2 = جمع2<رقم>(5، 3)
    اطبع("جمع1<رقم>(5, 3) = " + نتيجة1)
    اطبع("جمع2<رقم>(5, 3) = " + نتيجة2)
    
    # اختبار الدوال العادية
    رقم نتيجة3 = ضرب1(4، 5)
    رقم نتيجة4 = ضرب2(4، 5)
    اطبع("ضرب1(4, 5) = " + نتيجة3)
    اطبع("ضرب2(4, 5) = " + نتيجة4)
    
    اطبع("✅ جميع الاختبارات نجحت!")
    ارجع 0
نهاية
