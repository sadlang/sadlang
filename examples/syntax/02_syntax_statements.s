# ==========================================
# اختبار 2: العبارات (Statements)
# Test 2: Statements
# الصعوبة: أساسي / Difficulty: Basic
# ==========================================

اطبع("=== عبارات التصريح (Declaration Statements) ===\n")

# تصريح المتغيرات
رقم عدد = 42
عشري سعر = 99.99
نص اسم = "أحمد"
منطقي نشط = صحيح
أي قيمة = 100

اطبع("عدد:", عدد, "\n")
اطبع("سعر:", سعر, "\n")
اطبع("اسم:", اسم, "\n")
اطبع("نشط:", نشط, "\n")
اطبع("قيمة:", قيمة, "\n")

# تصريح مصفوفات
مصفوفة أرقام = [1، 2، 3، 4، 5]
مصفوفة نصوص = ["أ"، "ب"، "ج"]
مصفوفة مختلطة = [1، "نص"، صحيح، 3.14]

اطبع("\nأرقام:", أرقام, "\n")
اطبع("نصوص:", نصوص, "\n")
اطبع("مختلطة:", مختلطة, "\n")

# تصريح خرائط
خريطة شخص = {"الاسم": "سارة"، "العمر": 28}
خريطة إعدادات = {"اللون": "أزرق"، "الحجم": 12}

اطبع("\nشخص:", شخص, "\n")
اطبع("إعدادات:", إعدادات, "\n")

اطبع("\n=== عبارات الإسناد (Assignment Statements) ===\n")

# إسناد بسيط
رقم س = 10
اطبع("س الأولية:", س, "\n")

س = 20
اطبع("س بعد الإسناد:", س, "\n")

س = س + 5
اطبع("س بعد الزيادة:", س, "\n")

# إسناد متعدد للنوع نفسه
رقم ص = 100
ص = ص * 2
اطبع("ص بعد المضاعفة:", ص, "\n")

# إسناد لمصفوفة
مصفوفة قائمة = [1، 2، 3]
قائمة = قائمة + [4، 5]
اطبع("القائمة بعد الإضافة:", قائمة, "\n")

# إسناد لخريطة
خريطة بيانات = {"مفتاح": "قيمة"}
اطبع("البيانات:", بيانات, "\n")

# إسناد عناصر المصفوفة
مصفوفة أعداد = [10، 20، 30، 40، 50]
أعداد[0] = 99
أعداد[2] = 88
اطبع("بعد تعديل العناصر:", أعداد, "\n")

# إسناد عناصر الخريطة
خريطة معلومات = {"الاسم": "محمد"، "المدينة": "القاهرة"}
معلومات["الاسم"] = "أحمد"
معلومات["العمر"] = 30
اطبع("بعد تعديل الخريطة:", معلومات, "\n")

اطبع("\n=== عبارات الطباعة (Print Statements) ===\n")

# طباعة بسيطة
اطبع("نص بسيط\n")

# طباعة متغيرات
رقم رقم = 42
نص كلمة = "مرحبا"
اطبع("الرقم:", رقم, "\n")
اطبع("الكلمة:", كلمة, "\n")

# طباعة تعابير
اطبع("5 + 3 =", 5 + 3, "\n")
اطبع("10 > 5 هو", 10 > 5, "\n")

# طباعة متعددة
اطبع("عدة"، "كلمات"، "في"، "سطر"، "واحد\n")

# طباعة مصفوفات وخرائط
مصفوفة مصفوفة = [1، 2، 3]
خريطة خريطة = {"مفتاح": "قيمة"}
اطبع("المصفوفة:", مصفوفة, "\n")
اطبع("الخريطة:", خريطة, "\n")

اطبع("\n=== عبارات الدوال (Function Statements) ===\n")

# تعريف دالة بسيطة
دالة لا_شيء تحية(نص اسم)
    اطبع("مرحبا", اسم, "\n")
نهاية

تحية("أحمد")
تحية("سارة")

# دالة بإرجاع
دالة رقم مربع(رقم ع)
    إرجاع ع * ع
نهاية

رقم نتيجة_المربع = مربع(5)
اطبع("مربع 5 =", نتيجة_المربع, "\n")

# دالة بعدة معاملات
دالة رقم جمع(رقم أ، رقم ب)
    إرجاع أ + ب
نهاية

اطبع("3 + 7 =", جمع(3، 7), "\n")

اطبع("\n=== عبارات الإرجاع (Return Statements) ===\n")

# إرجاع بسيط
دالة رقم دالة_بسيطة()
    إرجاع 42
نهاية

اطبع("القيمة المرجعة:", دالة_بسيطة(), "\n")

# إرجاع تعبير
دالة منطقي هل_موجب(رقم ع)
    إرجاع ع > 0
نهاية

اطبع("هل 10 موجب؟", هل_موجب(10), "\n")
اطبع("هل -5 موجب؟", هل_موجب(-5), "\n")

# إرجاع مبكر
دالة نص حالة_عدد(رقم ع)
    إذا (ع > 0)
        إرجاع "موجب"
    نهاية
    إذا (ع < 0)
        إرجاع "سالب"
    نهاية
    إرجاع "صفر"
نهاية

اطبع("حالة 5:", حالة_عدد(5), "\n")
اطبع("حالة -3:", حالة_عدد(-3), "\n")
اطبع("حالة 0:", حالة_عدد(0), "\n")

# إرجاع مصفوفة
دالة مصفوفة أنشئ_مصفوفة(رقم حجم)
    مصفوفة نتيجة = []
    رقم ع = 0
    بينما (ع < حجم)
        نتيجة = نتيجة + [ع]
        ع = ع + 1
    نهاية
    إرجاع نتيجة
نهاية

اطبع("مصفوفة بحجم 5:", أنشئ_مصفوفة(5), "\n")

اطبع("\n=== عبارات معقدة ===\n")

# عبارة معقدة تجمع كل شيء
دالة نص معالج_بيانات(رقم قيمة)
    رقم مربع_القيمة = قيمة * قيمة
    منطقي كبير = مربع_القيمة > 100
    
    إذا (كبير)
        إرجاع "قيمة كبيرة"
    نهاية
    
    إرجاع "قيمة صغيرة"
نهاية

اطبع("معالجة 5:", معالج_بيانات(5), "\n")
اطبع("معالجة 15:", معالج_بيانات(15), "\n")

# دالة بعدة عبارات
دالة رقم حساب_معقد(رقم أ، رقم ب)
    رقم مجموع = أ + ب
    رقم حاصل_ضرب = أ * ب
    رقم نتيجة = مجموع + حاصل_ضرب
    
    اطبع("المجموع:", مجموع, "\n")
    اطبع("حاصل الضرب:", حاصل_ضرب, "\n")
    
    إرجاع نتيجة
نهاية

اطبع("النتيجة النهائية:", حساب_معقد(5، 3), "\n")

اطبع("\n✅ اختبار العبارات اكتمل\n")
