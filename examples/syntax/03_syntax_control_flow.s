# ==========================================
# اختبار 3: التحكم بالتدفق (Control Flow)
# Test 3: Control Flow
# الصعوبة: متوسط / Difficulty: Intermediate
# ==========================================

اطبع("=== عبارات إذا البسيطة (Simple If Statements) ===\n")

# إذا بسيطة
رقم عمر = 18
إذا (عمر >= 18)
    اطبع("بالغ\n")
نهاية

# إذا مع else
رقم درجة = 75
إذا (درجة >= 60)
    اطبع("نجح\n")
آخر
    اطبع("رسب\n")
نهاية

# إذا مع elseif
رقم علامة = 85
إذا (علامة >= 90)
    اطبع("ممتاز\n")
آخر_إذا (علامة >= 80)
    اطبع("جيد جداً\n")
آخر_إذا (علامة >= 70)
    اطبع("جيد\n")
آخر
    اطبع("مقبول\n")
نهاية

اطبع("\n=== عبارات إذا متداخلة (Nested If) ===\n")

# إذا متداخلة
رقم س = 10
رقم ص = 20

إذا (س > 0)
    اطبع("س موجب\n")
    إذا (ص > 0)
        اطبع("ص موجب أيضاً\n")
        إذا (س < ص)
            اطبع("س أصغر من ص\n")
        نهاية
    نهاية
نهاية

# تداخل معقد
رقم عدد = 15
إذا (عدد > 0)
    إذا (عدد % 2 == 0)
        اطبع(عدد, "موجب وزوجي\n")
    آخر
        اطبع(عدد, "موجب وفردي\n")
    نهاية
آخر
    إذا (عدد % 2 == 0)
        اطبع(عدد, "سالب وزوجي\n")
    آخر
        اطبع(عدد, "سالب وفردي\n")
    نهاية
نهاية

اطبع("\n=== حلقات بينما (While Loops) ===\n")

# بينما بسيطة
رقم عداد = 1
بينما (عداد <= 5)
    اطبع("العداد:", عداد, "\n")
    عداد = عداد + 1
نهاية

# بينما مع شرط معقد
رقم مجموع = 0
رقم ع = 1
بينما (مجموع < 20)
    مجموع = مجموع + ع
    اطبع("المجموع:", مجموع, "\n")
    ع = ع + 1
نهاية

# بينما مع break
رقم بحث = 0
بينما (بحث < 100)
    إذا (بحث == 7)
        اطبع("وجدنا 7!\n")
        اقطع
    نهاية
    بحث = بحث + 1
نهاية

# بينما مع continue
رقم تخطي = 0
بينما (تخطي < 10)
    تخطي = تخطي + 1
    إذا (تخطي % 2 == 0)
        استمر
    نهاية
    اطبع("عدد فردي:", تخطي, "\n")
نهاية

اطبع("\n=== حلقات بينما متداخلة (Nested While) ===\n")

# بينما متداخلة - جدول الضرب
رقم ع = 1
بينما (ع <= 3)
    رقم س = 1
    بينما (س <= 3)
        اطبع(ع, "×", س, "=", ع * س, "\n")
        س = س + 1
    نهاية
    اطبع("\n")
    ع = ع + 1
نهاية

اطبع("=== حلقات لكل (For-Each Loops) ===\n")

# لكل على مصفوفة أرقام
مصفوفة أرقام = [1، 2، 3، 4، 5]
لكل (رقم رقم في أرقام)
    اطبع("الرقم:", رقم, "\n")
نهاية

# لكل على مصفوفة نصوص
مصفوفة أسماء = ["أحمد"، "سارة"، "محمد"]
لكل (نص اسم في أسماء)
    اطبع("مرحبا", اسم, "\n")
نهاية

# لكل على مصفوفة مختلطة
مصفوفة مختلطة = [1، "نص"، صحيح، 3.14]
لكل (أي عنصر في مختلطة)
    اطبع("العنصر:", عنصر, "\n")
نهاية

اطبع("\n=== حلقات لكل متداخلة (Nested For-Each) ===\n")

# لكل متداخلة
مصفوفة مجموعات = [[1، 2]، [3، 4]، [5، 6]]
لكل (أي مجموعة في مجموعات)
    لكل (أي عنصر في مجموعة)
        اطبع("عنصر:", عنصر, "\n")
    نهاية
نهاية

اطبع("\n=== break و continue ===\n")

# break في لكل
مصفوفة قائمة_بحث = [1، 2، 3، 7، 5، 6]
لكل (رقم عدد في قائمة_بحث)
    إذا (عدد == 7)
        اطبع("وجدنا 7 في لكل!\n")
        اقطع
    نهاية
    اطبع("نتحقق من:", عدد, "\n")
نهاية

# continue في لكل
مصفوفة أعداد = [1، 2، 3، 4، 5، 6، 7، 8]
لكل (رقم ع في أعداد)
    إذا (ع % 2 == 0)
        استمر
    نهاية
    اطبع("فردي:", ع, "\n")
نهاية

اطبع("\n=== دمج if و while و for ===\n")

# دالة معقدة تستخدم كل هياكل التحكم
دالة رقم معالج_مصفوفة(مصفوفة أرقام)
    رقم مجموع = 0
    رقم عدد_موجب = 0
    
    لكل (رقم رقم في أرقام)
        إذا (رقم < 0)
            اطبع("تخطي رقم سالب:", رقم, "\n")
            استمر
        نهاية
        
        إذا (رقم > 100)
            اطبع("رقم كبير جداً:", رقم, "\n")
            اقطع
        نهاية
        
        مجموع = مجموع + رقم
        عدد_موجب = عدد_موجب + 1
    نهاية
    
    إذا (عدد_موجب > 0)
        إرجاع مجموع / عدد_موجب
    نهاية
    
    إرجاع 0
نهاية

مصفوفة بيانات = [10، -5، 20، 30، -10، 15]
رقم متوسط = معالج_مصفوفة(بيانات)
اطبع("المتوسط:", متوسط, "\n")

اطبع("\n=== أمثلة عملية ===\n")

# مثال 1: البحث في مصفوفة
دالة منطقي يحتوي(مصفوفة قائمة، رقم قيمة)
    لكل (رقم عنصر في قائمة)
        إذا (عنصر == قيمة)
            إرجاع صحيح
        نهاية
    نهاية
    إرجاع خطأ
نهاية

مصفوفة أعداد_اختبار = [1، 2، 3، 4، 5]
اطبع("هل 3 موجود؟", يحتوي(أعداد_اختبار، 3), "\n")
اطبع("هل 10 موجود؟", يحتوي(أعداد_اختبار، 10), "\n")

# مثال 2: عد العناصر المطابقة
دالة رقم عد_مطابقات(مصفوفة قائمة، رقم قيمة)
    رقم عدد = 0
    لكل (رقم عنصر في قائمة)
        إذا (عنصر == قيمة)
            عدد = عدد + 1
        نهاية
    نهاية
    إرجاع عدد
نهاية

مصفوفة تكرارات = [1، 2، 3، 2، 4، 2، 5]
اطبع("عدد 2 في المصفوفة:", عد_مطابقات(تكرارات، 2), "\n")

# مثال 3: مضاعفة حتى شرط
دالة رقم ضاعف_حتى(رقم قيمة، رقم حد)
    بينما (قيمة < حد)
        قيمة = قيمة * 2
    نهاية
    إرجاع قيمة
نهاية

اطبع("مضاعفة 1 حتى >= 100:", ضاعف_حتى(1، 100), "\n")

# مثال 4: مرشح مخصص
دالة مصفوفة مرشح_أكبر_من(مصفوفة قائمة، رقم حد)
    مصفوفة نتيجة = []
    لكل (رقم عنصر في قائمة)
        إذا (عنصر > حد)
            نتيجة = نتيجة + [عنصر]
        نهاية
    نهاية
    إرجاع نتيجة
نهاية

مصفوفة اختبار_مرشح = [5، 12، 8، 20، 3، 15]
اطبع("الأعداد > 10:", مرشح_أكبر_من(اختبار_مرشح، 10), "\n")

# مثال 5: حلقة مع عدة شروط خروج
دالة لا_شيء معالج_معقد()
    رقم محاولات = 0
    رقم نجاح = 0
    
    بينما (صحيح)
        محاولات = محاولات + 1
        
        إذا (محاولات > 10)
            اطبع("وصلنا للحد الأقصى من المحاولات\n")
            اقطع
        نهاية
        
        إذا (نجاح >= 3)
            اطبع("حققنا الهدف!\n")
            اقطع
        نهاية
        
        # محاكاة نجاح
        إذا (محاولات % 2 == 0)
            نجاح = نجاح + 1
        نهاية
    نهاية
نهاية

معالج_معقد()

اطبع("\n✅ اختبار التحكم بالتدفق اكتمل\n")
