# بسم الله الرحمن الرحيم
# ═══════════════════════════════════════════════════════════════════
# ملف أمثلة: مكتبة قاعدة البيانات - Database Library Examples
# الحمد لله رب العالمين، والصلاة والسلام على أشرف المرسلين
# ═══════════════════════════════════════════════════════════════════

# استيراد المكتبة / Import Library
استخدم "database"

# القواعد النحوية المستخدمة / EBNF Rules Used:
# ═══════════════════════════════════════════════════════════════════
# import_stmt ::= 'استخدم' string_literal
# call_expr ::= expression '.' IDENTIFIER '(' [arg_list] ')'
# try_stmt ::= 'محاولة' ':' block 'خطأ' [IDENTIFIER] ':' block
# ═══════════════════════════════════════════════════════════════════

اطبع("=" * 70)
اطبع("        اختبار مكتبة قاعدة البيانات - Database Library Test")
اطبع("=" * 70)
اطبع("بسم الله الرحمن الرحيم - نبدأ باختبار مكتبة SQLite")
اطبع("=" * 70)

# ═══════════════════════════════════════════════════════════════════
# 1. إنشاء اتصال بقاعدة البيانات / Create Database Connection
# ═══════════════════════════════════════════════════════════════════
اطبع("\n1️⃣  إنشاء اتصال بقاعدة البيانات:")
اطبع("-" * 50)

محاولة:
    قاعدة_البيانات = database.اتصل("مدرسة.db")
    اطبع("✅ تم الاتصال بقاعدة البيانات بنجاح")
    اطبع("📁 اسم الملف: مدرسة.db")
خطأ كـ خطأ:
    اطبع("❌ فشل الاتصال بقاعدة البيانات: " + نص(خطأ))

# ═══════════════════════════════════════════════════════════════════
# 2. إنشاء جدول / Create Table
# ═══════════════════════════════════════════════════════════════════
اطبع("\n2️⃣  إنشاء جدول الطلاب:")
اطبع("-" * 50)

استعلام_إنشاء_الجدول = """
CREATE TABLE IF NOT EXISTS الطلاب (
    المعرف INTEGER PRIMARY KEY AUTOINCREMENT,
    الاسم TEXT NOT NULL,
    العمر INTEGER,
    المعدل REAL,
    الصف TEXT,
    تاريخ_التسجيل TEXT DEFAULT CURRENT_TIMESTAMP
)
"""

محاولة:
    قاعدة_البيانات.نفذ(استعلام_إنشاء_الجدول)
    اطبع("✅ تم إنشاء جدول الطلاب بنجاح")
    اطبع("📊 الأعمدة: المعرف، الاسم، العمر، المعدل، الصف، تاريخ_التسجيل")
خطأ كـ خطأ:
    اطبع("❌ فشل إنشاء الجدول: " + نص(خطأ))

# ═══════════════════════════════════════════════════════════════════
# 3. إدراج بيانات واحدة / Insert Single Record
# ═══════════════════════════════════════════════════════════════════
اطبع("\n3️⃣  إدراج طالب واحد:")
اطبع("-" * 50)

استعلام_إدراج = """
INSERT INTO الطلاب (الاسم, العمر, المعدل, الصف)
VALUES (?, ?, ?, ?)
"""

محاولة:
    قاعدة_البيانات.نفذ(استعلام_إدراج, ["محمد أحمد", 16, 85.5, "الصف العاشر"])
    اطبع("✅ تم إدراج الطالب: محمد أحمد")
    اطبع("📝 العمر: 16، المعدل: 85.5، الصف: العاشر")
خطأ كـ خطأ:
    اطبع("❌ فشل إدراج البيانات: " + نص(خطأ))

# ═══════════════════════════════════════════════════════════════════
# 4. إدراج بيانات متعددة / Insert Multiple Records
# ═══════════════════════════════════════════════════════════════════
اطبع("\n4️⃣  إدراج عدة طلاب:")
اطبع("-" * 50)

قائمة_الطلاب = [
    ["فاطمة علي", 15, 92.0, "الصف التاسع"],
    ["أحمد حسن", 17, 88.5, "الصف الحادي عشر"],
    ["مريم خالد", 16, 90.0, "الصف العاشر"],
    ["عمر سعيد", 15, 78.5, "الصف التاسع"],
    ["سارة محمود", 17, 95.5, "الصف الحادي عشر"]
]

محاولة:
    لكل طالب في قائمة_الطلاب:
        قاعدة_البيانات.نفذ(استعلام_إدراج, طالب)
    اطبع("✅ تم إدراج " + نص(قائمة_الطلاب.طول()) + " طلاب")
    اطبع("👥 جميع البيانات تم إضافتها بنجاح")
خطأ كـ خطأ:
    اطبع("❌ فشل إدراج البيانات المتعددة: " + نص(خطأ))

# ═══════════════════════════════════════════════════════════════════
# 5. استعلام بسيط - عرض جميع الطلاب / Simple Query - All Students
# ═══════════════════════════════════════════════════════════════════
اطبع("\n5️⃣  عرض جميع الطلاب:")
اطبع("-" * 50)

محاولة:
    النتيجة = قاعدة_البيانات.استعلام("SELECT * FROM الطلاب")
    اطبع("📊 عدد الطلاب: " + نص(النتيجة.طول()))
    اطبع("")
    اطبع("قائمة الطلاب:")
    لكل طالب في النتيجة:
        اطبع("  🎓 " + طالب["الاسم"] + " - العمر: " + نص(طالب["العمر"]) + 
              " - المعدل: " + نص(طالب["المعدل"]) + " - " + طالب["الصف"])
خطأ كـ خطأ:
    اطبع("❌ فشل الاستعلام: " + نص(خطأ))

# ═══════════════════════════════════════════════════════════════════
# 6. استعلام بشرط / Query with WHERE Clause
# ═══════════════════════════════════════════════════════════════════
اطبع("\n6️⃣  الطلاب ذوو المعدل الممتاز (>= 90):")
اطبع("-" * 50)

محاولة:
    النتيجة = قاعدة_البيانات.استعلام(
        "SELECT * FROM الطلاب WHERE المعدل >= ?", [90.0]
    )
    اطبع("⭐ عدد الطلاب الممتازين: " + نص(النتيجة.طول()))
    لكل طالب في النتيجة:
        اطبع("  🌟 " + طالب["الاسم"] + " - المعدل: " + نص(طالب["المعدل"]))
خطأ كـ خطأ:
    اطبع("❌ فشل الاستعلام: " + نص(خطأ))

# ═══════════════════════════════════════════════════════════════════
# 7. حساب المتوسط / Calculate Average
# ═══════════════════════════════════════════════════════════════════
اطبع("\n7️⃣  حساب معدل جميع الطلاب:")
اطبع("-" * 50)

محاولة:
    النتيجة = قاعدة_البيانات.استعلام(
        "SELECT AVG(المعدل) as متوسط_المعدل FROM الطلاب"
    )
    المعدل_العام = النتيجة[0]["متوسط_المعدل"]
    اطبع("📊 المعدل العام لجميع الطلاب: " + نص(المعدل_العام))
    
    إذا المعدل_العام >= 85:
        اطبع("✅ مستوى ممتاز!")
    وإلا إذا المعدل_العام >= 75:
        اطبع("👍 مستوى جيد جداً")
    وإلا:
        اطبع("⚠️  يحتاج تحسين")
خطأ كـ خطأ:
    اطبع("❌ فشل الحساب: " + نص(خطأ))

# ═══════════════════════════════════════════════════════════════════
# 8. عدد الطلاب في كل صف / Count Students by Grade
# ═══════════════════════════════════════════════════════════════════
اطبع("\n8️⃣  عدد الطلاب في كل صف:")
اطبع("-" * 50)

محاولة:
    النتيجة = قاعدة_البيانات.استعلام("""
        SELECT الصف, COUNT(*) as العدد
        FROM الطلاب
        GROUP BY الصف
        ORDER BY الصف
    """)
    
    اطبع("📚 توزيع الطلاب حسب الصفوف:")
    لكل صف في النتيجة:
        اطبع("  📖 " + صف["الصف"] + ": " + نص(صف["العدد"]) + " طالب")
خطأ كـ خطأ:
    اطبع("❌ فشل الاستعلام: " + نص(خطأ))

# ═══════════════════════════════════════════════════════════════════
# 9. تحديث بيانات / Update Data
# ═══════════════════════════════════════════════════════════════════
اطبع("\n9️⃣  تحديث معدل طالب:")
اطبع("-" * 50)

محاولة:
    قاعدة_البيانات.نفذ(
        "UPDATE الطلاب SET المعدل = ? WHERE الاسم = ?",
        [87.0, "محمد أحمد"]
    )
    اطبع("✅ تم تحديث معدل محمد أحمد إلى: 87.0")
    
    # التحقق من التحديث
    النتيجة = قاعدة_البيانات.استعلام(
        "SELECT * FROM الطلاب WHERE الاسم = ?", ["محمد أحمد"]
    )
    اطبع("📝 المعدل الجديد: " + نص(النتيجة[0]["المعدل"]))
خطأ كـ خطأ:
    اطبع("❌ فشل التحديث: " + نص(خطأ))

# ═══════════════════════════════════════════════════════════════════
# 10. حذف بيانات / Delete Data
# ═══════════════════════════════════════════════════════════════════
اطبع("\n🔟 حذف الطلاب ذوي المعدل المنخفض:")
اطبع("-" * 50)

محاولة:
    # عرض الطلاب الذين سيتم حذفهم
    النتيجة = قاعدة_البيانات.استعلام(
        "SELECT * FROM الطلاب WHERE المعدل < ?", [75.0]
    )
    اطبع("⚠️  الطلاب الذين سيتم حذفهم:")
    لكل طالب في النتيجة:
        اطبع("  ❌ " + طالب["الاسم"] + " - المعدل: " + نص(طالب["المعدل"]))
    
    # ملاحظة: في الواقع قد لا نريد حذف الطلاب، هذا فقط للتوضيح
    # قاعدة_البيانات.نفذ("DELETE FROM الطلاب WHERE المعدل < ?", [75.0])
    اطبع("ℹ️  تم تخطي الحذف الفعلي (للتوضيح فقط)")
خطأ كـ خطأ:
    اطبع("❌ فشل في العملية: " + نص(خطأ))

# ═══════════════════════════════════════════════════════════════════
# 11. الترتيب والتحديد / ORDER BY and LIMIT
# ═══════════════════════════════════════════════════════════════════
اطبع("\n1️⃣1️⃣  أفضل 3 طلاب (حسب المعدل):")
اطبع("-" * 50)

محاولة:
    النتيجة = قاعدة_البيانات.استعلام("""
        SELECT * FROM الطلاب
        ORDER BY المعدل DESC
        LIMIT 3
    """)
    
    اطبع("🏆 قائمة الشرف:")
    ترتيب = 1
    لكل طالب في النتيجة:
        إذا ترتيب == 1:
            ميدالية = "🥇"
        وإلا إذا ترتيب == 2:
            ميدالية = "🥈"
        وإلا:
            ميدالية = "🥉"
        
        اطبع("  " + ميدالية + " المركز " + نص(ترتيب) + ": " + 
              طالب["الاسم"] + " - المعدل: " + نص(طالب["المعدل"]))
        ترتيب = ترتيب + 1
خطأ كـ خطأ:
    اطبع("❌ فشل الاستعلام: " + نص(خطأ))

# ═══════════════════════════════════════════════════════════════════
# 12. تطبيق عملي: نظام إدارة الطلاب / Student Management System
# ═══════════════════════════════════════════════════════════════════
اطبع("\n1️⃣2️⃣  تطبيق عملي: نظام إدارة الطلاب:")
اطبع("-" * 50)

صنف نظام_الطلاب:
    """صنف لإدارة الطلاب في قاعدة البيانات"""
    
    بناء(قاعدة_البيانات):
        هذا.قاعدة = قاعدة_البيانات
    
    دالة أضف_طالب(الاسم، العمر، المعدل، الصف):
        """إضافة طالب جديد"""
        محاولة:
            هذا.قاعدة.نفذ(
                "INSERT INTO الطلاب (الاسم, العمر, المعدل, الصف) VALUES (?, ?, ?, ?)",
                [الاسم، العمر، المعدل، الصف]
            )
            إرجاع صحيح
        خطأ:
            إرجاع خطأ
    
    دالة احصل_على_طالب(الاسم):
        """البحث عن طالب بالاسم"""
        محاولة:
            النتيجة = هذا.قاعدة.استعلام(
                "SELECT * FROM الطلاب WHERE الاسم = ?", [الاسم]
            )
            إذا النتيجة.طول() > 0:
                إرجاع النتيجة[0]
            وإلا:
                إرجاع لا_شيء
        خطأ:
            إرجاع لا_شيء
    
    دالة احسب_الإحصائيات():
        """حساب إحصائيات الطلاب"""
        محاولة:
            النتيجة = هذا.قاعدة.استعلام("""
                SELECT 
                    COUNT(*) as العدد_الكلي،
                    AVG(المعدل) as المعدل_العام،
                    MAX(المعدل) as أعلى_معدل،
                    MIN(المعدل) as أقل_معدل
                FROM الطلاب
            """)
            إرجاع النتيجة[0]
        خطأ:
            إرجاع {}

# اختبار النظام
اطبع("🔧 اختبار نظام إدارة الطلاب:")
النظام = جديد نظام_الطلاب(قاعدة_البيانات)

# الحصول على الإحصائيات
الإحصائيات = النظام.احسب_الإحصائيات()
اطبع("📊 الإحصائيات الكاملة:")
اطبع("   👥 عدد الطلاب: " + نص(الإحصائيات["العدد_الكلي"]))
اطبع("   📈 المعدل العام: " + نص(الإحصائيات["المعدل_العام"]))
اطبع("   ⬆️  أعلى معدل: " + نص(الإحصائيات["أعلى_معدل"]))
اطبع("   ⬇️  أقل معدل: " + نص(الإحصائيات["أقل_معدل"]))

# ═══════════════════════════════════════════════════════════════════
# 13. المعاملات (Transactions) / Database Transactions
# ═══════════════════════════════════════════════════════════════════
اطبع("\n1️⃣3️⃣  المعاملات (Transactions):")
اطبع("-" * 50)

محاولة:
    # بدء معاملة
    قاعدة_البيانات.ابدأ_معاملة()
    اطبع("🔄 بدأت المعاملة...")
    
    # تنفيذ عدة عمليات
    قاعدة_البيانات.نفذ(
        "UPDATE الطلاب SET المعدل = المعدل + 5 WHERE المعدل < 80"
    )
    
    # تأكيد المعاملة
    قاعدة_البيانات.أكد_المعاملة()
    اطبع("✅ تم تأكيد المعاملة بنجاح")
    اطبع("📝 تم زيادة 5 نقاط لجميع الطلاب دون 80")
خطأ كـ خطأ:
    # إلغاء المعاملة في حالة الخطأ
    قاعدة_البيانات.ألغ_المعاملة()
    اطبع("❌ تم إلغاء المعاملة بسبب خطأ: " + نص(خطأ))

# ═══════════════════════════════════════════════════════════════════
# 14. جدول متقدم: المواد الدراسية / Advanced Table: Subjects
# ═══════════════════════════════════════════════════════════════════
اطبع("\n1️⃣4️⃣  إنشاء جدول المواد:")
اطبع("-" * 50)

محاولة:
    قاعدة_البيانات.نفذ("""
        CREATE TABLE IF NOT EXISTS المواد (
            معرف_المادة INTEGER PRIMARY KEY,
            اسم_المادة TEXT NOT NULL,
            معرف_الطالب INTEGER,
            الدرجة REAL,
            FOREIGN KEY (معرف_الطالب) REFERENCES الطلاب(المعرف)
        )
    """)
    اطبع("✅ تم إنشاء جدول المواد بنجاح")
    
    # إدراج بعض المواد
    المواد_البيانات = [
        [1, "الرياضيات", 1, 90.0],
        [2, "الفيزياء", 1, 85.0],
        [3, "الكيمياء", 2, 92.0],
        [4, "البرمجة", 2, 98.0]
    ]
    
    لكل مادة في المواد_البيانات:
        قاعدة_البيانات.نفذ(
            "INSERT INTO المواد VALUES (?, ?, ?, ?)", مادة
        )
    اطبع("📚 تم إضافة " + نص(المواد_البيانات.طول()) + " مواد")
خطأ كـ خطأ:
    اطبع("⚠️  تنبيه: " + نص(خطأ))

# ═══════════════════════════════════════════════════════════════════
# 15. JOIN - ربط الجداول / Joining Tables
# ═══════════════════════════════════════════════════════════════════
اطبع("\n1️⃣5️⃣  ربط جدول الطلاب بالمواد:")
اطبع("-" * 50)

محاولة:
    النتيجة = قاعدة_البيانات.استعلام("""
        SELECT 
            الطلاب.الاسم,
            المواد.اسم_المادة,
            المواد.الدرجة
        FROM الطلاب
        INNER JOIN المواد ON الطلاب.المعرف = المواد.معرف_الطالب
        ORDER BY الطلاب.الاسم
    """)
    
    اطبع("📋 درجات الطلاب في المواد:")
    لكل سجل في النتيجة:
        اطبع("  📝 " + سجل["الاسم"] + " - " + 
              سجل["اسم_المادة"] + ": " + نص(سجل["الدرجة"]))
خطأ كـ خطأ:
    اطبع("⚠️  تنبيه: " + نص(خطأ))

# ═══════════════════════════════════════════════════════════════════
# الختام / Closing
# ═══════════════════════════════════════════════════════════════════
اطبع("\n" + "=" * 70)
اطبع("🎉 الحمد لله! اكتملت جميع اختبارات مكتبة قاعدة البيانات بنجاح")
اطبع("=" * 70)
اطبع("📚 تم اختبار:")
اطبع("   ✅ إنشاء الاتصال وإنشاء الجداول")
اطبع("   ✅ إدراج البيانات (مفردة ومتعددة)")
اطبع("   ✅ الاستعلامات البسيطة والمعقدة")
اطبع("   ✅ WHERE, GROUP BY, ORDER BY, LIMIT")
اطبع("   ✅ تحديث وحذف البيانات")
اطبع("   ✅ الدوال التجميعية (COUNT, AVG, MAX, MIN)")
اطبع("   ✅ المعاملات (Transactions)")
اطبع("   ✅ ربط الجداول (JOINS)")
اطبع("   ✅ نظام إدارة عملي")
اطبع("")
اطبع("🤲 اللهم انفعنا بما علمتنا وعلمنا ما ينفعنا وزدنا علماً")
اطبع("=" * 70)

# إغلاق الاتصال
محاولة:
    قاعدة_البيانات.أغلق()
    اطبع("✅ تم إغلاق الاتصال بقاعدة البيانات")
خطأ:
    اطبع("ℹ️  تم إغلاق الاتصال تلقائياً")

# الحمد لله رب العالمين
# سبحانك اللهم وبحمدك، أشهد أن لا إله إلا أنت، أستغفرك وأتوب إليك
