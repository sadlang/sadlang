# بسم الله الرحمن الرحيم
# ═══════════════════════════════════════════════════════════════════
# ملف أمثلة: مكتبة التشفير والأمان - Crypto Library Examples
# الحمد لله رب العالمين، والصلاة والسلام على أشرف المرسلين
# ═══════════════════════════════════════════════════════════════════

# استيراد المكتبة / Import Library
استخدم "crypto"

# القواعد النحوية المستخدمة / EBNF Rules Used:
# ═══════════════════════════════════════════════════════════════════
# import_stmt ::= 'استخدم' string_literal
# call_expr ::= expression '.' IDENTIFIER '(' [arg_list] ')'
# string_literal ::= '"' string_char* '"'
# ═══════════════════════════════════════════════════════════════════

اطبع("=" * 70)
اطبع("      اختبار مكتبة التشفير والأمان - Crypto Library Test")
اطبع("=" * 70)
اطبع("بسم الله الرحمن الرحيم - نبدأ باختبار عمليات التشفير")
اطبع("=" * 70)

# ═══════════════════════════════════════════════════════════════════
# 1. دالة التجزئة MD5 / MD5 Hash Function
# ═══════════════════════════════════════════════════════════════════
اطبع("\n1️⃣  دالة التجزئة MD5:")
اطبع("-" * 50)

محاولة:
    النص = "بسم الله الرحمن الرحيم"
    التجزئة = crypto.md5(النص)
    
    اطبع("✅ النص الأصلي: " + النص)
    اطبع("🔐 تجزئة MD5: " + التجزئة)
    اطبع("📏 طول التجزئة: " + نص(التجزئة.طول()) + " حرف")
خطأ كـ خطأ:
    اطبع("❌ فشل التجزئة: " + نص(خطأ))

# ═══════════════════════════════════════════════════════════════════
# 2. دالة التجزئة SHA-256 / SHA-256 Hash Function
# ═══════════════════════════════════════════════════════════════════
اطبع("\n2️⃣  دالة التجزئة SHA-256:")
اطبع("-" * 50)

محاولة:
    النص = "الحمد لله رب العالمين"
    التجزئة = crypto.sha256(النص)
    
    اطبع("✅ النص الأصلي: " + النص)
    اطبع("🔐 تجزئة SHA-256: " + التجزئة)
    اطبع("🔒 أكثر أماناً من MD5")
خطأ كـ خطأ:
    اطبع("❌ فشل التجزئة: " + نص(خطأ))

# ═══════════════════════════════════════════════════════════════════
# 3. دالة التجزئة SHA-512 / SHA-512 Hash Function
# ═══════════════════════════════════════════════════════════════════
اطبع("\n3️⃣  دالة التجزئة SHA-512:")
اطبع("-" * 50)

محاولة:
    النص = "اللهم صل وسلم على نبينا محمد"
    التجزئة = crypto.sha512(النص)
    
    اطبع("✅ النص الأصلي: " + النص)
    اطبع("🔐 تجزئة SHA-512: ")
    اطبع("   " + التجزئة)
    اطبع("🔒 أعلى مستوى أمان")
خطأ كـ خطأ:
    اطبع("❌ فشل التجزئة: " + نص(خطأ))

# ═══════════════════════════════════════════════════════════════════
# 4. تطبيق عملي: التحقق من كلمة المرور / Password Verification
# ═══════════════════════════════════════════════════════════════════
اطبع("\n4️⃣  تطبيق: التحقق من كلمة المرور:")
اطبع("-" * 50)

دالة تجزئة_كلمة_المرور(كلمة_المرور):
    """تجزئة كلمة المرور للتخزين الآمن"""
    إرجاع crypto.sha256(كلمة_المرور)

دالة تحقق_من_كلمة_المرور(كلمة_المرور، التجزئة_المخزنة):
    """التحقق من كلمة المرور"""
    التجزئة_الجديدة = تجزئة_كلمة_المرور(كلمة_المرور)
    إرجاع التجزئة_الجديدة == التجزئة_المخزنة

# تسجيل مستخدم
كلمة_المرور = "كلمة_سر_قوية_123"
التجزئة_المخزنة = تجزئة_كلمة_المرور(كلمة_المرور)

اطبع("👤 تسجيل مستخدم جديد:")
اطبع("   كلمة المرور: " + كلمة_المرور)
اطبع("   التجزئة المخزنة: " + التجزئة_المخزنة[:32] + "...")

# محاولة دخول صحيحة
اطبع("\n🔓 محاولة دخول بكلمة مرور صحيحة:")
إذا تحقق_من_كلمة_المرور("كلمة_سر_قوية_123", التجزئة_المخزنة):
    اطبع("   ✅ تم الدخول بنجاح!")
وإلا:
    اطبع("   ❌ كلمة المرور خاطئة")

# محاولة دخول خاطئة
اطبع("\n🔒 محاولة دخول بكلمة مرور خاطئة:")
إذا تحقق_من_كلمة_المرور("كلمة_خاطئة", التجزئة_المخزنة):
    اطبع("   ✅ تم الدخول بنجاح!")
وإلا:
    اطبع("   ❌ كلمة المرور خاطئة")

# ═══════════════════════════════════════════════════════════════════
# 5. التشفير المتماثل - AES / Symmetric Encryption - AES
# ═══════════════════════════════════════════════════════════════════
اطبع("\n5️⃣  التشفير المتماثل (AES):")
اطبع("-" * 50)

محاولة:
    الرسالة_الأصلية = "هذه رسالة سرية - بسم الله الرحمن الرحيم"
    المفتاح = "مفتاح_سري_قوي_جداً_12345678"
    
    # تشفير
    الرسالة_المشفرة = crypto.تشفير_aes(الرسالة_الأصلية، المفتاح)
    اطبع("✅ تم التشفير بنجاح")
    اطبع("📝 الرسالة الأصلية: " + الرسالة_الأصلية)
    اطبع("🔐 الرسالة المشفرة: " + الرسالة_المشفرة[:40] + "...")
    
    # فك التشفير
    الرسالة_المفككة = crypto.فك_تشفير_aes(الرسالة_المشفرة، المفتاح)
    اطبع("🔓 الرسالة بعد فك التشفير: " + الرسالة_المفككة)
    
    # التحقق
    إذا الرسالة_الأصلية == الرسالة_المفككة:
        اطبع("✅ التشفير وفك التشفير ناجحان!")
    وإلا:
        اطبع("❌ خطأ في التشفير!")
خطأ كـ خطأ:
    اطبع("❌ فشل التشفير: " + نص(خطأ))

# ═══════════════════════════════════════════════════════════════════
# 6. توليد مفاتيح RSA / Generate RSA Keys
# ═══════════════════════════════════════════════════════════════════
اطبع("\n6️⃣  توليد مفاتيح RSA:")
اطبع("-" * 50)

محاولة:
    # توليد زوج المفاتيح
    المفاتيح = crypto.ولد_مفاتيح_rsa(2048)
    المفتاح_العام = المفاتيح["عام"]
    المفتاح_الخاص = المفاتيح["خاص"]
    
    اطبع("✅ تم توليد مفاتيح RSA (2048 بت)")
    اطبع("🔑 المفتاح العام: " + المفتاح_العام[:50] + "...")
    اطبع("🔐 المفتاح الخاص: " + المفتاح_الخاص[:50] + "...")
    اطبع("⚠️  احتفظ بالمفتاح الخاص في مكان آمن!")
خطأ كـ خطأ:
    اطبع("❌ فشل توليد المفاتيح: " + نص(خطأ))

# ═══════════════════════════════════════════════════════════════════
# 7. التشفير غير المتماثل - RSA / Asymmetric Encryption - RSA
# ═══════════════════════════════════════════════════════════════════
اطبع("\n7️⃣  التشفير غير المتماثل (RSA):")
اطبع("-" * 50)

محاولة:
    # الرسالة
    الرسالة = "السلام عليكم ورحمة الله وبركاته"
    
    # التشفير بالمفتاح العام
    الرسالة_المشفرة = crypto.شفر_rsa(الرسالة، المفتاح_العام)
    اطبع("✅ تم التشفير بالمفتاح العام")
    اطبع("📝 الرسالة الأصلية: " + الرسالة)
    اطبع("🔐 الرسالة المشفرة: [بيانات ثنائية]")
    
    # فك التشفير بالمفتاح الخاص
    الرسالة_المفككة = crypto.فك_rsa(الرسالة_المشفرة، المفتاح_الخاص)
    اطبع("🔓 الرسالة بعد فك التشفير: " + الرسالة_المفككة)
    
    إذا الرسالة == الرسالة_المفككة:
        اطبع("✅ التشفير RSA ناجح!")
خطأ كـ خطأ:
    اطبع("❌ فشل التشفير RSA: " + نص(خطأ))

# ═══════════════════════════════════════════════════════════════════
# 8. التوقيع الرقمي / Digital Signature
# ═══════════════════════════════════════════════════════════════════
اطبع("\n8️⃣  التوقيع الرقمي:")
اطبع("-" * 50)

محاولة:
    الرسالة = "هذا عقد رسمي - أنا أحمد محمد أوافق على الشروط"
    
    # إنشاء التوقيع بالمفتاح الخاص
    التوقيع = crypto.وقع(الرسالة، المفتاح_الخاص)
    اطبع("✅ تم إنشاء التوقيع الرقمي")
    اطبع("📝 الرسالة: " + الرسالة)
    اطبع("✍️  التوقيع: " + التوقيع[:40] + "...")
    
    # التحقق من التوقيع بالمفتاح العام
    صحيح = crypto.تحقق_من_التوقيع(الرسالة، التوقيع، المفتاح_العام)
    
    إذا صحيح:
        اطبع("✅ التوقيع صحيح! الرسالة موثوقة")
    وإلا:
        اطبع("❌ التوقيع غير صحيح!")
    
    # محاولة تغيير الرسالة
    اطبع("\n🔍 محاولة تزوير الرسالة:")
    رسالة_مزورة = "هذا عقد رسمي - أنا أحمد محمد لا أوافق على الشروط"
    صحيح2 = crypto.تحقق_من_التوقيع(رسالة_مزورة، التوقيع، المفتاح_العام)
    
    إذا صحيح2:
        اطبع("   ✅ التوقيع صحيح")
    وإلا:
        اطبع("   ❌ كشف التزوير! التوقيع لا يطابق الرسالة")
خطأ كـ خطأ:
    اطبع("❌ فشل التوقيع: " + نص(خطأ))

# ═══════════════════════════════════════════════════════════════════
# 9. توليد أرقام عشوائية آمنة / Secure Random Numbers
# ═══════════════════════════════════════════════════════════════════
اطبع("\n9️⃣  توليد أرقام عشوائية آمنة:")
اطبع("-" * 50)

محاولة:
    # توليد رقم عشوائي
    رقم_عشوائي = crypto.رقم_عشوائي(1، 1000)
    اطبع("🎲 رقم عشوائي (1-1000): " + نص(رقم_عشوائي))
    
    # توليد بايتات عشوائية
    بايتات_عشوائية = crypto.بايتات_عشوائية(16)
    اطبع("🔐 16 بايت عشوائية: " + نص(بايتات_عشوائية))
    
    # توليد نص عشوائي
    نص_عشوائي = crypto.نص_عشوائي(32)
    اطبع("🔑 نص عشوائي (32 حرف): " + نص_عشوائي)
    اطبع("💡 يمكن استخدامه كرمز سري أو token")
خطأ كـ خطأ:
    اطبع("❌ فشل التوليد: " + نص(خطأ))

# ═══════════════════════════════════════════════════════════════════
# 10. Base64 Encoding/Decoding
# ═══════════════════════════════════════════════════════════════════
اطبع("\n🔟 ترميز وفك ترميز Base64:")
اطبع("-" * 50)

محاولة:
    النص_الأصلي = "بسم الله الرحمن الرحيم - لا إله إلا الله"
    
    # ترميز
    النص_المرمز = crypto.base64_ترميز(النص_الأصلي)
    اطبع("✅ النص الأصلي: " + النص_الأصلي)
    اطبع("🔠 النص المرمز (Base64): " + النص_المرمز)
    
    # فك الترميز
    النص_المفكوك = crypto.base64_فك_ترميز(النص_المرمز)
    اطبع("📝 النص بعد فك الترميز: " + النص_المفكوك)
    
    إذا النص_الأصلي == النص_المفكوك:
        اطبع("✅ الترميز وفك الترميز ناجحان!")
خطأ كـ خطأ:
    اطبع("❌ فشل الترميز: " + نص(خطأ))

# ═══════════════════════════════════════════════════════════════════
# 11. HMAC للتحقق من سلامة البيانات / HMAC for Data Integrity
# ═══════════════════════════════════════════════════════════════════
اطبع("\n1️⃣1️⃣  HMAC للتحقق من سلامة البيانات:")
اطبع("-" * 50)

محاولة:
    الرسالة = "هذه بيانات مهمة يجب التحقق من سلامتها"
    المفتاح = "مفتاح_سري_hmac"
    
    # إنشاء HMAC
    الـhmac = crypto.hmac_sha256(الرسالة، المفتاح)
    اطبع("✅ الرسالة: " + الرسالة)
    اطبع("🔐 HMAC: " + الـhmac)
    
    # التحقق من السلامة
    اطبع("\n🔍 التحقق من سلامة البيانات:")
    الـhmac_جديد = crypto.hmac_sha256(الرسالة، المفتاح)
    
    إذا الـhmac == الـhmac_جديد:
        اطبع("   ✅ البيانات سليمة ولم تتغير")
    وإلا:
        اطبع("   ❌ البيانات تم تعديلها!")
    
    # محاولة تغيير البيانات
    رسالة_معدلة = "هذه بيانات تم تعديلها"
    الـhmac_معدل = crypto.hmac_sha256(رسالة_معدلة، المفتاح)
    
    اطبع("\n⚠️  محاولة تعديل البيانات:")
    إذا الـhmac == الـhmac_معدل:
        اطبع("   ✅ البيانات سليمة")
    وإلا:
        اطبع("   ❌ كشف التعديل! HMAC لا يتطابق")
خطأ كـ خطأ:
    اطبع("❌ فشل HMAC: " + نص(خطأ))

# ═══════════════════════════════════════════════════════════════════
# 12. تطبيق عملي: نظام تشفير الملفات / File Encryption System
# ═══════════════════════════════════════════════════════════════════
اطبع("\n1️⃣2️⃣  تطبيق عملي: نظام تشفير الملفات:")
اطبع("-" * 50)

صنف مشفر_الملفات:
    """صنف لتشفير وفك تشفير الملفات"""
    
    بناء(كلمة_المرور):
        # توليد مفتاح من كلمة المرور
        هذا.مفتاح = crypto.sha256(كلمة_المرور)
    
    دالة شفر_نص(النص):
        """تشفير نص"""
        محاولة:
            إرجاع crypto.تشفير_aes(النص، هذا.مفتاح)
        خطأ:
            إرجاع لا_شيء
    
    دالة فك_نص(النص_المشفر):
        """فك تشفير نص"""
        محاولة:
            إرجاع crypto.فك_تشفير_aes(النص_المشفر، هذا.مفتاح)
        خطأ:
            إرجاع لا_شيء
    
    دالة احسب_التجزئة(النص):
        """حساب تجزئة SHA-256"""
        إرجاع crypto.sha256(النص)

# اختبار النظام
اطبع("🔧 اختبار نظام تشفير الملفات:")

المشفر = جديد مشفر_الملفات("كلمة_مرور_قوية_جداً")

# تشفير بيانات سرية
البيانات_السرية = """
معلومات سرية:
اسم المستخدم: أحمد محمد
رقم الحساب: 123456789
الرصيد: 50000 ريال
"""

البيانات_المشفرة = المشفر.شفر_نص(البيانات_السرية)
إذا البيانات_المشفرة:
    اطبع("✅ تم تشفير البيانات السرية")
    اطبع("📊 حجم البيانات الأصلية: " + نص(البيانات_السرية.طول()) + " حرف")
    
    # فك التشفير
    البيانات_المفككة = المشفر.فك_نص(البيانات_المشفرة)
    إذا البيانات_المفككة == البيانات_السرية:
        اطبع("✅ تم فك التشفير بنجاح")
        اطبع("🔒 البيانات محمية بشكل آمن")

# حساب التجزئة للتحقق
التجزئة = المشفر.احسب_التجزئة(البيانات_السرية)
اطبع("🔐 تجزئة البيانات: " + التجزئة[:32] + "...")

# ═══════════════════════════════════════════════════════════════════
# 13. مقارنة خوارزميات التجزئة / Compare Hash Algorithms
# ═══════════════════════════════════════════════════════════════════
اطبع("\n1️⃣3️⃣  مقارنة خوارزميات التجزئة:")
اطبع("-" * 50)

النص_للمقارنة = "الحمد لله رب العالمين"

اطبع("📝 النص: " + النص_للمقارنة)
اطبع("")

محاولة:
    md5 = crypto.md5(النص_للمقارنة)
    sha1 = crypto.sha1(النص_للمقارنة)
    sha256 = crypto.sha256(النص_للمقارنة)
    sha512 = crypto.sha512(النص_للمقارنة)
    
    اطبع("🔐 MD5    (128 بت): " + md5)
    اطبع("🔐 SHA-1  (160 بت): " + sha1)
    اطبع("🔐 SHA-256 (256 بت): " + sha256[:64])
    اطبع("🔐 SHA-512 (512 بت): " + sha512[:64] + "...")
    اطبع("")
    اطبع("💡 ملاحظة: SHA-256 و SHA-512 أكثر أماناً")
خطأ كـ خطأ:
    اطبع("❌ فشل المقارنة: " + نص(خطأ))

# ═══════════════════════════════════════════════════════════════════
# الختام / Closing
# ═══════════════════════════════════════════════════════════════════
اطبع("\n" + "=" * 70)
اطبع("🎉 الحمد لله! اكتملت جميع اختبارات مكتبة التشفير بنجاح")
اطبع("=" * 70)
اطبع("📚 تم اختبار:")
اطبع("   ✅ دوال التجزئة (MD5, SHA-1, SHA-256, SHA-512)")
اطبع("   ✅ التشفير المتماثل (AES)")
اطبع("   ✅ التشفير غير المتماثل (RSA)")
اطبع("   ✅ التوقيع الرقمي")
اطبع("   ✅ توليد الأرقام العشوائية الآمنة")
اطبع("   ✅ ترميز Base64")
اطبع("   ✅ HMAC للتحقق من السلامة")
اطبع("   ✅ نظام تشفير عملي")
اطبع("   ✅ التحقق من كلمات المرور")
اطبع("")
اطبع("🔒 تذكير مهم:")
اطبع("   • استخدم SHA-256 أو أعلى للتجزئة")
اطبع("   • احفظ المفاتيح الخاصة في مكان آمن")
اطبع("   • استخدم كلمات مرور قوية")
aطبع("   • لا تشارك المفاتيح السرية")
اطبع("")
اطبع("🤲 اللهم احفظنا وبياناتنا من كل سوء")
اطبع("🤲 سبحان الله وبحمده، سبحان الله العظيم")
اطبع("=" * 70)

# الحمد لله رب العالمين
# سبحانك اللهم وبحمدك، أشهد أن لا إله إلا أنت، أستغفرك وأتوب إليك
