# بسم الله الرحمن الرحيم
# ═══════════════════════════════════════════════════════════
# ملف أمثلة: مكتبة JSON
# File: JSON Library Examples
# ═══════════════════════════════════════════════════════════

# المكتبة المستخدمة / Library Used:
# ═══════════════════════════════════════════════════════════
# import "json"  # مكتبة JSON للتعامل مع البيانات
# ═══════════════════════════════════════════════════════════

استخدم "json"

اطبع("=" * 60)
اطبع("       اختبار مكتبة JSON - JSON Library Test")
اطبع("=" * 60)

# 1. تحويل خريطة إلى JSON / Convert Map to JSON
اطبع("\n1. تحويل خريطة إلى JSON:")

خريطة بيانات_شخص = {
    "الاسم": "أحمد محمد"،
    "العمر": 28،
    "المدينة": "الرياض"،
    "متزوج": صحيح
}

نص json_نص = json.إلى_نص(بيانات_شخص)
اطبع("البيانات الأصلية: " + نص(بيانات_شخص))
اطبع("JSON النص: " + json_نص)

# 2. تحويل JSON إلى خريطة / Parse JSON to Map
اطبع("\n2. تحويل JSON إلى خريطة:")

نص json_بيانات = "{\"المنتج\": \"هاتف\"، \"السعر\": 2000، \"متوفر\": true}"
خريطة منتج = json.من_نص(json_بيانات)

اطبع("JSON النص: " + json_بيانات)
اطبع("المنتج: " + منتج["المنتج"])
اطبع("السعر: " + نص(منتج["السعر"]))
اطبع("متوفر: " + نص(منتج["متوفر"]))

# 3. تحويل مصفوفة إلى JSON / Convert Array to JSON
اطبع("\n3. تحويل مصفوفة إلى JSON:")

مصفوفة أسماء_الطلاب = ["أحمد"، "محمد"، "خالد"، "سارة"]
نص json_أسماء = json.إلى_نص(أسماء_الطلاب)

اطبع("المصفوفة الأصلية: " + نص(أسماء_الطلاب))
اطبع("JSON النص: " + json_أسماء)

# 4. JSON معقد مع قوائم / Complex JSON With Lists
اطبع("\n4. JSON معقد:")

خريطة بيانات_جامعة = {
    "اسم_الجامعة": "جامعة الملك سعود"،
    "الموقع": "الرياض"،
    "عدد_الطلاب": 50000،
    "الكليات": ["الهندسة"، "الطب"، "العلوم"، "الآداب"]
}

نص json_جامعة = json.إلى_نص(بيانات_جامعة)
اطبع("JSON الجامعة:")
اطبع(json_جامعة)

# 5. قراءة JSON من ملف / Read JSON from File
اطبع("\n5. قراءة JSON من ملف:")

نص محتوى_json = json.اقرأ_من_ملف("data/config.json")
خريطة إعدادات = json.من_نص(محتوى_json)

اطبع("تم قراءة الإعدادات من الملف")
اطبع("الإعدادات: " + نص(إعدادات))

# 6. كتابة JSON إلى ملف / Write JSON to File
اطبع("\n6. كتابة JSON إلى ملف:")

خريطة بيانات_للحفظ = {
    "التاريخ": "2025-12-05"،
    "البرنامج": "لغة ص"،
    "الإصدار": "1.0.0"
}

نص json_للحفظ = json.إلى_نص(بيانات_للحفظ)
json.اكتب_إلى_ملف("data/output.json"، json_للحفظ)

اطبع("تم حفظ البيانات إلى الملف: data/output.json")

# 7. JSON مع تنسيق جميل / Pretty Print JSON
اطبع("\n7. JSON مع تنسيق جميل:")

خريطة بيانات_موظف = {
    "الاسم": "عبدالله"،
    "الوظيفة": "مبرمج"،
    "الراتب": 15000،
    "المهارات": ["Python"، "Java"، "C++"]
}

نص json_منسق = json.إلى_نص_منسق(بيانات_موظف، مسافة_بادئة = 2)
اطبع("JSON منسق:")
اطبع(json_منسق)

# 8. التحقق من صحة JSON / Validate JSON
اطبع("\n8. التحقق من صحة JSON:")

نص json_صحيح = "{\"key\": \"value\"}"
نص json_خاطئ = "{key: value}"

منطقي صحة_الأول = json.هل_صحيح(json_صحيح)
منطقي صحة_الثاني = json.هل_صحيح(json_خاطئ)

اطبع("JSON الأول صحيح؟ " + نص(صحة_الأول))
اطبع("JSON الثاني صحيح؟ " + نص(صحة_الثاني))

# 9. دمج كائنات JSON / Merge JSON Objects
اطبع("\n9. دمج كائنات JSON:")

خريطة بيانات_أساسية = {
    "الاسم": "محمد"،
    "العمر": 30
}

خريطة بيانات_إضافية = {
    "المدينة": "جدة"،
    "المهنة": "طبيب"
}

خريطة بيانات_مدمجة = json.دمج(بيانات_أساسية، بيانات_إضافية)

اطبع("البيانات المدمجة: " + نص(بيانات_مدمجة))

# 10. استخراج قيمة من JSON عميق / Extract Value from Nested JSON
اطبع("\n10. استخراج من JSON متداخل:")

خريطة بيانات_متداخلة = {
    "شركة": {
        "الاسم": "تقنية المستقبل"،
        "العنوان": {
            "المدينة": "الدمام"،
            "الشارع": "شارع الملك فهد"
        }
    }
}

نص اسم_الشركة = json.احصل(بيانات_متداخلة، "شركة.الاسم")
نص مدينة_الشركة = json.احصل(بيانات_متداخلة، "شركة.العنوان.المدينة")

اطبع("اسم الشركة: " + اسم_الشركة)
اطبع("المدينة: " + مدينة_الشركة)

اطبع("\n" + "=" * 60)
اطبع("      الحمد لله! اكتملت الاختبارات بنجاح")
اطبع("=" * 60)
