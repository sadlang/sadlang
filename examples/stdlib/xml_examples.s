# بسم الله الرحمن الرحيم
# ═══════════════════════════════════════════════════════════════════
# ملف أمثلة: مكتبة XML - XML Library Examples
# الحمد لله رب العالمين، والصلاة والسلام على أشرف المرسلين
# ═══════════════════════════════════════════════════════════════════

# استيراد المكتبة / Import Library
استخدم "xml"

# القواعد النحوية المستخدمة / EBNF Rules Used:
# ═══════════════════════════════════════════════════════════════════
# import_stmt ::= 'استخدم' string_literal
# call_expr ::= expression '.' IDENTIFIER '(' [arg_list] ')'
# string_literal ::= '"' string_char* '"'
# ═══════════════════════════════════════════════════════════════════

اطبع("=" * 70)
اطبع("              اختبار مكتبة XML - XML Library Test")
اطبع("=" * 70)
اطبع("بسم الله الرحمن الرحيم - نبدأ باختبار معالجة XML")
اطبع("=" * 70)

# ═══════════════════════════════════════════════════════════════════
# 1. إنشاء مستند XML بسيط / Create Simple XML Document
# ═══════════════════════════════════════════════════════════════════
اطبع("\n1️⃣  إنشاء مستند XML بسيط:")
اطبع("-" * 50)

محاولة:
    # إنشاء عنصر جذر
    الجذر = xml.عنصر("كتاب")
    xml.عين_نص(الجذر، "القرآن الكريم")
    
    # تحويل إلى نص
    نص_xml = xml.إلى_نص(الجذر)
    اطبع("✅ تم إنشاء عنصر XML:")
    اطبع(نص_xml)
خطأ كـ خطأ:
    اطبع("❌ فشل إنشاء XML: " + نص(خطأ))

# ═══════════════════════════════════════════════════════════════════
# 2. إنشاء XML مع خصائص / XML with Attributes
# ═══════════════════════════════════════════════════════════════════
اطبع("\n2️⃣  إنشاء XML مع خصائص:")
اطبع("-" * 50)

محاولة:
    الطالب = xml.عنصر("طالب")
    xml.أضف_خاصية(الطالب، "المعرف", "1001")
    xml.أضف_خاصية(الطالب، "الجنسية", "سعودي")
    xml.عين_نص(الطالب، "محمد أحمد")
    
    نص_xml = xml.إلى_نص(الطالب)
    اطبع("✅ عنصر مع خصائص:")
    اطبع(نص_xml)
خطأ كـ خطأ:
    اطبع("❌ فشل الإنشاء: " + نص(خطأ))

# ═══════════════════════════════════════════════════════════════════
# 3. إنشاء XML متداخل / Nested XML
# ═══════════════════════════════════════════════════════════════════
اطبع("\n3️⃣  إنشاء XML متداخل:")
اطبع("-" * 50)

محاولة:
    المكتبة = xml.عنصر("مكتبة")
    
    # كتاب أول
    الكتاب1 = xml.عنصر_فرعي(المكتبة، "كتاب")
    xml.أضف_خاصية(الكتاب1، "رقم", "1")
    
    العنوان1 = xml.عنصر_فرعي(الكتاب1، "العنوان")
    xml.عين_نص(العنوان1، "رياض الصالحين")
    
    المؤلف1 = xml.عنصر_فرعي(الكتاب1، "المؤلف")
    xml.عين_نص(المؤلف1، "الإمام النووي")
    
    # كتاب ثاني
    الكتاب2 = xml.عنصر_فرعي(المكتبة، "كتاب")
    xml.أضف_خاصية(الكتاب2، "رقم", "2")
    
    العنوان2 = xml.عنصر_فرعي(الكتاب2، "العنوان")
    xml.عين_نص(العنوان2، "صحيح البخاري")
    
    المؤلف2 = xml.عنصر_فرعي(الكتاب2، "المؤلف")
    xml.عين_نص(المؤلف2، "الإمام البخاري")
    
    نص_xml = xml.إلى_نص_منسق(المكتبة)
    اطبع("✅ XML متداخل:")
    اطبع(نص_xml)
خطأ كـ خطأ:
    اطبع("❌ فشل الإنشاء: " + نص(خطأ))

# ═══════════════════════════════════════════════════════════════════
# 4. تحليل نص XML / Parse XML String
# ═══════════════════════════════════════════════════════════════════
اطبع("\n4️⃣  تحليل نص XML:")
اطبع("-" * 50)

نص_xml_للتحليل = """<?xml version="1.0" encoding="UTF-8"?>
<مسجد>
    <الاسم>المسجد النبوي</الاسم>
    <المدينة>المدينة المنورة</المدينة>
    <السعة>600000</السعة>
</مسجد>
"""

محاولة:
    الجذر = xml.من_نص(نص_xml_للتحليل)
    اطبع("✅ تم تحليل XML بنجاح")
    
    # قراءة العناصر
    الاسم = xml.احصل_على_نص(xml.ابحث(الجذر، "الاسم"))
    المدينة = xml.احصل_على_نص(xml.ابحث(الجذر، "المدينة"))
    السعة = xml.احصل_على_نص(xml.ابحث(الجذر، "السعة"))
    
    اطبع("🕌 الاسم: " + الاسم)
    اطبع("📍 المدينة: " + المدينة)
    اطبع("👥 السعة: " + السعة + " مصلي")
خطأ كـ خطأ:
    اطبع("❌ فشل التحليل: " + نص(خطأ))

# ═══════════════════════════════════════════════════════════════════
# 5. قراءة XML من ملف / Read XML from File
# ═══════════════════════════════════════════════════════════════════
اطبع("\n5️⃣  قراءة XML من ملف:")
اطبع("-" * 50)

# أولاً: إنشاء ملف XML للاختبار
محتوى_xml = """<?xml version="1.0" encoding="UTF-8"?>
<طلاب>
    <طالب المعرف="1">
        <الاسم>أحمد محمد</الاسم>
        <العمر>16</العمر>
        <المعدل>92.5</المعدل>
    </طالب>
    <طالب المعرف="2">
        <الاسم>فاطمة علي</الاسم>
        <العمر>15</العمر>
        <المعدل>95.0</المعدل>
    </طالب>
</طلاب>
"""

محاولة:
    # حفظ XML في ملف
    xml.اكتب_إلى_ملف(محتوى_xml، "طلاب.xml")
    اطبع("✅ تم حفظ XML في ملف")
    
    # قراءة من الملف
    الجذر = xml.من_ملف("طلاب.xml")
    اطبع("✅ تم قراءة XML من الملف")
    
    # عرض الطلاب
    الطلاب = xml.ابحث_الكل(الجذر، "طالب")
    اطبع("👥 عدد الطلاب: " + نص(الطلاب.طول()))
خطأ كـ خطأ:
    اطبع("❌ فشل: " + نص(خطأ))

# ═══════════════════════════════════════════════════════════════════
# 6. البحث عن عناصر متعددة / Find Multiple Elements
# ═══════════════════════════════════════════════════════════════════
اطبع("\n6️⃣  البحث عن عناصر متعددة:")
اطبع("-" * 50)

محاولة:
    الجذر = xml.من_ملف("طلاب.xml")
    الطلاب = xml.ابحث_الكل(الجذر، "طالب")
    
    اطبع("📚 قائمة الطلاب:")
    لكل طالب في الطلاب:
        المعرف = xml.احصل_على_خاصية(طالب، "المعرف")
        الاسم = xml.احصل_على_نص(xml.ابحث(طالب، "الاسم"))
        المعدل = xml.احصل_على_نص(xml.ابحث(طالب، "المعدل"))
        
        اطبع("  🎓 [" + المعرف + "] " + الاسم + " - المعدل: " + المعدل)
خطأ كـ خطأ:
    اطبع("❌ فشل البحث: " + نص(خطأ))

# ═══════════════════════════════════════════════════════════════════
# 7. تعديل XML / Modify XML
# ═══════════════════════════════════════════════════════════════════
اطبع("\n7️⃣  تعديل عناصر XML:")
اطبع("-" * 50)

محاولة:
    الجذر = xml.من_ملف("طلاب.xml")
    
    # تعديل معدل الطالب الأول
    الطالب_الأول = xml.ابحث(الجذر، "طالب")
    عنصر_المعدل = xml.ابحث(الطالب_الأول، "المعدل")
    xml.عين_نص(عنصر_المعدل، "94.0")
    
    اطبع("✅ تم تحديث المعدل إلى: 94.0")
    
    # حفظ التغييرات
    xml.احفظ(الجذر، "طلاب_محدث.xml")
    اطبع("💾 تم حفظ التغييرات في ملف جديد")
خطأ كـ خطأ:
    اطبع("❌ فشل التعديل: " + نص(خطأ))

# ═══════════════════════════════════════════════════════════════════
# 8. إضافة عناصر جديدة / Add New Elements
# ═══════════════════════════════════════════════════════════════════
اطبع("\n8️⃣  إضافة عناصر جديدة:")
اطبع("-" * 50)

محاولة:
    الجذر = xml.من_ملف("طلاب.xml")
    
    # إضافة طالب جديد
    طالب_جديد = xml.عنصر_فرعي(الجذر، "طالب")
    xml.أضف_خاصية(طالب_جديد، "المعرف", "3")
    
    اسم = xml.عنصر_فرعي(طالب_جديد، "الاسم")
    xml.عين_نص(اسم، "عمر سعيد")
    
    عمر = xml.عنصر_فرعي(طالب_جديد، "العمر")
    xml.عين_نص(عمر، "17")
    
    معدل = xml.عنصر_فرعي(طالب_جديد، "المعدل")
    xml.عين_نص(معدل، "88.5")
    
    اطبع("✅ تم إضافة طالب جديد: عمر سعيد")
    
    # عرض عدد الطلاب الجديد
    الطلاب = xml.ابحث_الكل(الجذر، "طالب")
    اطبع("👥 عدد الطلاب الآن: " + نص(الطلاب.طول()))
خطأ كـ خطأ:
    اطبع("❌ فشل الإضافة: " + نص(خطأ))

# ═══════════════════════════════════════════════════════════════════
# 9. حذف عناصر / Delete Elements
# ═══════════════════════════════════════════════════════════════════
اطبع("\n9️⃣  حذف عناصر:")
اطبع("-" * 50)

محاولة:
    الجذر = xml.من_ملف("طلاب.xml")
    الطلاب = xml.ابحث_الكل(الجذر، "طالب")
    
    اطبع("📊 عدد الطلاب قبل الحذف: " + نص(الطلاب.طول()))
    
    # حذف آخر طالب
    إذا الطلاب.طول() > 0:
        الطالب_الأخير = الطلاب[الطلاب.طول() - 1]
        xml.احذف_عنصر(الجذر، الطالب_الأخير)
        اطبع("✅ تم حذف طالب")
        
        الطلاب_بعد = xml.ابحث_الكل(الجذر، "طالب")
        اطبع("📊 عدد الطلاب بعد الحذف: " + نص(الطلاب_بعد.طول()))
خطأ كـ خطأ:
    اطبع("❌ فشل الحذف: " + نص(خطأ))

# ═══════════════════════════════════════════════════════════════════
# 10. XPath للبحث المتقدم / XPath for Advanced Search
# ═══════════════════════════════════════════════════════════════════
اطبع("\n🔟 البحث المتقدم باستخدام XPath:")
اطبع("-" * 50)

محاولة:
    الجذر = xml.من_ملف("طلاب.xml")
    
    # البحث باستخدام XPath
    # الطلاب ذوي المعدل أكثر من 90
    استعلام = "//طالب[المعدل > 90]"
    النتائج = xml.xpath(الجذر، استعلام)
    
    اطبع("⭐ الطلاب ذوو المعدل الممتاز (> 90):")
    لكل طالب في النتائج:
        الاسم = xml.احصل_على_نص(xml.ابحث(طالب، "الاسم"))
        المعدل = xml.احصل_على_نص(xml.ابحث(طالب، "المعدل"))
        اطبع("  🌟 " + الاسم + " - المعدل: " + المعدل)
خطأ كـ خطأ:
    اطبع("⚠️  XPath غير مدعوم أو حدث خطأ: " + نص(خطأ))

# ═══════════════════════════════════════════════════════════════════
# 11. تطبيق عملي: نظام إدارة XML / XML Management System
# ═══════════════════════════════════════════════════════════════════
اطبع("\n1️⃣1️⃣  تطبيق عملي: نظام إدارة XML:")
اطبع("-" * 50)

صنف مدير_xml:
    """صنف لإدارة عمليات XML"""
    
    بناء(مسار_الملف):
        هذا.مسار = مسار_الملف
        محاولة:
            هذا.جذر = xml.من_ملف(مسار_الملف)
        خطأ:
            هذا.جذر = xml.عنصر("بيانات")
    
    دالة أضف_عنصر(اسم_العنصر، البيانات):
        """إضافة عنصر جديد مع بيانات"""
        محاولة:
            عنصر_جديد = xml.عنصر_فرعي(هذا.جذر، اسم_العنصر)
            
            لكل مفتاح في البيانات:
                عنصر_فرعي = xml.عنصر_فرعي(عنصر_جديد، مفتاح)
                xml.عين_نص(عنصر_فرعي، نص(البيانات[مفتاح]))
            
            إرجاع صحيح
        خطأ:
            إرجاع خطأ
    
    دالة احفظ():
        """حفظ التغييرات في الملف"""
        محاولة:
            xml.احفظ(هذا.جذر، هذا.مسار)
            إرجاع صحيح
        خطأ:
            إرجاع خطأ
    
    دالة احصل_على_عدد_العناصر(اسم_العنصر):
        """حساب عدد عناصر معينة"""
        محاولة:
            العناصر = xml.ابحث_الكل(هذا.جذر، اسم_العنصر)
            إرجاع العناصر.طول()
        خطأ:
            إرجاع 0

# اختبار النظام
اطبع("🔧 اختبار مدير XML:")

المدير = جديد مدير_xml("بيانات_جديدة.xml")

# إضافة منتج
بيانات_المنتج = {
    "الاسم": "حاسوب محمول",
    "السعر": "3500",
    "الكمية": "10"
}

إذا المدير.أضف_عنصر("منتج", بيانات_المنتج):
    اطبع("✅ تم إضافة منتج جديد")
وإلا:
    اطبع("❌ فشل إضافة المنتج")

# حفظ
إذا المدير.احفظ():
    اطبع("💾 تم حفظ البيانات")

# ═══════════════════════════════════════════════════════════════════
# 12. XML مع مسافات الأسماء / XML with Namespaces
# ═══════════════════════════════════════════════════════════════════
اطبع("\n1️⃣2️⃣  XML مع مسافات الأسماء:")
اطبع("-" * 50)

xml_مع_namespace = """<?xml version="1.0"?>
<كتاب xmlns:مؤلف="http://example.com/author">
    <العنوان>البرمجة بلغة ص</العنوان>
    <مؤلف:الاسم>أحمد محمد</مؤلف:الاسم>
    <مؤلف:البريد>ahmed@example.com</مؤلف:البريد>
</كتاب>
"""

اطبع("📝 مثال XML مع namespace:")
اطبع(xml_مع_namespace)

محاولة:
    الجذر = xml.من_نص(xml_مع_namespace)
    اطبع("✅ تم تحليل XML مع namespace بنجاح")
خطأ كـ خطأ:
    اطبع("⚠️  تنبيه: " + نص(خطأ))

# ═══════════════════════════════════════════════════════════════════
# 13. التحقق من صحة XML / XML Validation
# ═══════════════════════════════════════════════════════════════════
اطبع("\n1️⃣3️⃣  التحقق من صحة XML:")
اطبع("-" * 50)

xml_صحيح = """<?xml version="1.0"?>
<رسالة>
    <المرسل>أحمد</المرسل>
    <المستقبل>محمد</المستقبل>
    <المحتوى>السلام عليكم</المحتوى>
</رسالة>
"""

xml_خاطئ = """<?xml version="1.0"?>
<رسالة>
    <المرسل>أحمد
    <المستقبل>محمد</المستقبل>
"""

اطبع("✅ XML صحيح:")
محاولة:
    xml.من_نص(xml_صحيح)
    اطبع("   ✓ تم التحليل بنجاح")
خطأ:
    اطبع("   ✗ فشل التحليل")

اطبع("\n❌ XML خاطئ:")
محاولة:
    xml.من_نص(xml_خاطئ)
    اطبع("   ✓ تم التحليل؟")
خطأ كـ خطأ:
    اطبع("   ✗ فشل كما هو متوقع")

# ═══════════════════════════════════════════════════════════════════
# الختام / Closing
# ═══════════════════════════════════════════════════════════════════
اطبع("\n" + "=" * 70)
اطبع("🎉 الحمد لله! اكتملت جميع اختبارات مكتبة XML بنجاح")
اطبع("=" * 70)
اطبع("📚 تم اختبار:")
اطبع("   ✅ إنشاء عناصر XML بسيطة ومعقدة")
اطبع("   ✅ إضافة الخصائص (Attributes)")
اطبع("   ✅ XML متداخل (Nested)")
اطبع("   ✅ تحليل XML من نص وملف")
اطبع("   ✅ البحث عن عناصر (فردية ومتعددة)")
اطبع("   ✅ تعديل وإضافة وحذف العناصر")
اطبع("   ✅ XPath للبحث المتقدم")
اطبع("   ✅ نظام إدارة عملي")
اطبع("   ✅ مسافات الأسماء (Namespaces)")
اطبع("   ✅ التحقق من الصحة")
اطبع("")
اطبع("🤲 سبحان الله وبحمده، سبحان الله العظيم")
اطبع("🤲 اللهم انفعنا بما علمتنا وعلمنا ما ينفعنا وزدنا علماً")
اطبع("=" * 70)

# الحمد لله رب العالمين
# سبحانك اللهم وبحمدك، أشهد أن لا إله إلا أنت، أستغفرك وأتوب إليك
