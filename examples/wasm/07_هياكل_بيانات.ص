# البرنامج 7: هياكل البيانات
# تطبيق هياكل بيانات متعددة

# ═══════════════════════════════════════════
#  هياكل البيانات — لغة ص
# ═══════════════════════════════════════════

اطبع_سطر("═══════════════════════════════")
اطبع_سطر("  🗂️ هياكل البيانات — لغة ص")
اطبع_سطر("═══════════════════════════════")

# ═══ المكدس (Stack) ═══
اطبع_سطر("")
اطبع_سطر("📚 المكدس:")
اطبع_سطر("─────────────────────────────")

متغير المكدس = []

دالة ادفع(القيمة)
    أضف(المكدس، القيمة)
    اطبع_سطر("  ⬆️ دفع: " + القيمة)
نهاية

دالة انزع()
    إذا (طول(المكدس) == 0)
        اطبع_سطر("  ⚠️ المكدس فارغ!")
        ارجع لاشيء
    نهاية
    متغير القيمة = المكدس[طول(المكدس) - 1]
    أزل(المكدس، طول(المكدس) - 1)
    اطبع_سطر("  ⬇️ نزع: " + القيمة)
    ارجع القيمة
نهاية

دالة قمة_المكدس()
    إذا (طول(المكدس) == 0)
        ارجع لاشيء
    نهاية
    ارجع المكدس[طول(المكدس) - 1]
نهاية

ادفع(10)
ادفع(20)
ادفع(30)
ادفع(40)
اطبع_سطر("  📍 القمة: " + قمة_المكدس())
اطبع_سطر("  📏 الحجم: " + طول(المكدس))
انزع()
انزع()
اطبع_سطر("  📍 القمة الآن: " + قمة_المكدس())

# ═══ الطابور (Queue) ═══
اطبع_سطر("")
اطبع_سطر("🚶 الطابور:")
اطبع_سطر("─────────────────────────────")

متغير الطابور = []

دالة أدخل_طابور(القيمة)
    أضف(الطابور، القيمة)
    اطبع_سطر("  ➡️ إدخال: " + القيمة)
نهاية

دالة أخرج_طابور()
    إذا (طول(الطابور) == 0)
        اطبع_سطر("  ⚠️ الطابور فارغ!")
        ارجع لاشيء
    نهاية
    متغير القيمة = الطابور[0]
    أزل(الطابور، 0)
    اطبع_سطر("  ⬅️ إخراج: " + القيمة)
    ارجع القيمة
نهاية

أدخل_طابور("أحمد")
أدخل_طابور("سارة")
أدخل_طابور("محمد")
أدخل_طابور("فاطمة")
اطبع_سطر("  📏 الحجم: " + طول(الطابور))
أخرج_طابور()
أخرج_طابور()
اطبع_سطر("  التالي: " + الطابور[0])

# ═══ الخريطة (Map) ═══
اطبع_سطر("")
اطبع_سطر("🗺️ الخريطة:")
اطبع_سطر("─────────────────────────────")

متغير الطلاب = خريطة()
الطلاب = map_set(الطلاب، "أحمد"، 95)
الطلاب = map_set(الطلاب، "سارة"، 88)
الطلاب = map_set(الطلاب، "محمد"، 92)
الطلاب = map_set(الطلاب، "فاطمة"، 97)
الطلاب = map_set(الطلاب، "خالد"، 85)

اطبع_سطر("  📊 درجات الطلاب:")
متغير المفاتيح = خريطة_مفاتيح(الطلاب)
لكل اسم في المفاتيح
    متغير الدرجة = خريطة_احصل(الطلاب، اسم)
    
    متغير التقدير = "❓"
    إذا (الدرجة >= 90)
        التقدير = "🌟 ممتاز"
    وإلا
        إذا (الدرجة >= 80)
            التقدير = "✅ جيد جداً"
        وإلا
            إذا (الدرجة >= 70)
                التقدير = "📗 جيد"
            وإلا
                التقدير = "📙 مقبول"
            نهاية
        نهاية
    نهاية
    
    اطبع_سطر("    " + اسم + ": " + الدرجة + " — " + التقدير)
نهاية

اطبع_سطر("  📏 عدد الطلاب: " + خريطة_حجم(الطلاب))

# ═══ المجموعة (Set) — محاكاة ═══
اطبع_سطر("")
اطبع_سطر("🔗 المجموعات:")
اطبع_سطر("─────────────────────────────")

متغير المجموعة_أ = [1، 2، 3، 4، 5، 6]
متغير المجموعة_ب = [4، 5، 6، 7، 8، 9]

اطبع_سطر("  المجموعة أ: " + المجموعة_أ)
اطبع_سطر("  المجموعة ب: " + المجموعة_ب)

# التقاطع
متغير نتيجة_التقاطع = []
لكل عنصر في المجموعة_أ
    إذا (يحتوي_عنصر(المجموعة_ب، عنصر))
        أضف(نتيجة_التقاطع، عنصر)
    نهاية
نهاية
اطبع_سطر("  التقاطع:   " + نتيجة_التقاطع)

# الاتحاد
متغير نتيجة_الاتحاد = []
لكل عنصر في المجموعة_أ
    أضف(نتيجة_الاتحاد، عنصر)
نهاية
لكل عنصر في المجموعة_ب
    إذا (ليس يحتوي_عنصر(نتيجة_الاتحاد، عنصر))
        أضف(نتيجة_الاتحاد، عنصر)
    نهاية
نهاية
اطبع_سطر("  الاتحاد:   " + نتيجة_الاتحاد)

# الفرق
متغير نتيجة_الفرق = []
لكل عنصر في المجموعة_أ
    إذا (ليس يحتوي_عنصر(المجموعة_ب، عنصر))
        أضف(نتيجة_الفرق، عنصر)
    نهاية
نهاية
اطبع_سطر("  الفرق أ-ب: " + نتيجة_الفرق)

اطبع_سطر("")
اطبع_سطر("═══════════════════════════════")
اطبع_سطر("✅ تم اختبار هياكل البيانات بنجاح!")
