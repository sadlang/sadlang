دالة رئيسية()
اطبع("=== صفحة الترحيب ===\n");

# إنشاء النافذة
ن = نافذة_جديد("مرحبا", 800, 600);
# إنشاء الرسام
ر = رسام_ثنائي_جديد(ن);
# عرض النافذة
نافذة_عرض(ن);

# رسم الإطار
رسام_بدء_إطار(ر);

# خلفية زرقاء فاتحة
رسام_مسح(ر, 173, 216, 230, 255);

# مستطيل ترحيبي أبيض
رسام_مستطيل(ر, 250.0, 200.0, 300.0, 120.0, 255, 255, 255, 220, صحيح);

# إطار المستطيل
رسام_مستطيل(ر, 250.0, 200.0, 300.0, 120.0, 100, 100, 200, 255, خطأ);

# دوائر زخرفية
رسام_دائرة(ر, 200.0, 250.0, 30.0, 255, 200, 100, 255, صحيح);
رسام_دائرة(ر, 600.0, 250.0, 30.0, 255, 200, 100, 255, صحيح);

# خط علوي
رسام_خط(ر, 100.0, 100.0, 700.0, 100.0, 100, 100, 200, 255);

# خط سفلي
رسام_خط(ر, 100.0, 500.0, 700.0, 500.0, 100, 100, 200, 255);

# نص الترحيب الرئيسي
رسام_نص(ر, "مرحبا بكم", 310.0, 230.0, 32.0, 0, 0, 100, 255);

# نص فرعي
رسام_نص(ر, "لغة S", 360.0, 270.0, 24.0, 50, 50, 50, 255);

رسام_إنهاء_إطار(ر);

# معالجة الأحداث
نافذة_استقبال_أحداث(ن);
نافذة_تبديل_buffers(ن);
نهاية