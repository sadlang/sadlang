# اختبار المولدات - المرحلة 7
# Test Generators - Phase 7

دالة رئيسية()
    اطبع_سطر("اختبار المولدات")
    
    # اختبار 1: مصفوفة مع لكل
    مصفوفة أرقام = [1، 2، 3، 4، 5]
    
    رقم مجموع = 0
    لكل(ق في أرقام)
        مجموع = مجموع + ق
    نهاية
    
    اطبع_سطر("المجموع: " + مجموع)
    
    # اختبار 2: List Comprehension
     مصفوفة مربعات = [س * س لكل س في أرقام]
    
    اطبع_سطر("انتهى الاختبار")
    ارجع 0
نهاية
