// ============================================
// اختبار دوال المصفوفات
// Test Array Functions
// ============================================

اطبع_سطر("=== اختبار دوال المصفوفات ===")
اطبع_سطر("=== Testing Array Functions ===")
اطبع_سطر("")

// --------------------------------------------
// 1. إنشاء مصفوفة وإضافة عناصر
// Create array and append elements
// --------------------------------------------
اطبع_سطر("1. إضافة العناصر / Append Elements")
مصفوفة قائمة = [1, 2, 3]
اطبع("المصفوفة الأصلية / Original: ")
اطبع_سطر(قائمة)

قائمة = إضافة(قائمة, 4)
قائمة = إضافة(قائمة, 5)
اطبع("بعد الإضافة / After append: ")
اطبع_سطر(قائمة)
اطبع_سطر("")

// --------------------------------------------
// 2. الحصول على الحجم
// Get size
// --------------------------------------------
اطبع_سطر("2. حجم المصفوفة / Array Size")
رقم الحجم = حجم(قائمة)
اطبع("الحجم / Size: ")
اطبع_سطر(الحجم)
اطبع_سطر("")

// --------------------------------------------
// 3. الوصول للعناصر
// Access elements
// --------------------------------------------
اطبع_سطر("3. الوصول للعناصر / Access Elements")
رقم أول_عنصر = أول(قائمة)
رقم آخر_عنصر = آخر(قائمة)
اطبع("أول عنصر / First: ")
اطبع_سطر(أول_عنصر)
اطبع("آخر عنصر / Last: ")
اطبع_سطر(آخر_عنصر)
اطبع_سطر("")

// --------------------------------------------
// 4. البحث عن عنصر
// Search for element
// --------------------------------------------
اطبع_سطر("4. البحث / Search")
رقم موقع = فهرس(قائمة, 3)
اطبع("موقع الرقم 3 / Index of 3: ")
اطبع_سطر(موقع)

منطقي موجود = يحتوي(قائمة, 4)
اطبع("يحتوي 4؟ / Contains 4?: ")
اطبع_سطر(موجود)
اطبع_سطر("")

// --------------------------------------------
// 5. قلب المصفوفة
// Reverse array
// --------------------------------------------
اطبع_سطر("5. قلب الترتيب / Reverse")
مصفوفة معكوسة = قلب(قائمة)
اطبع("المصفوفة المعكوسة / Reversed: ")
اطبع_سطر(معكوسة)
اطبع_سطر("")

// --------------------------------------------
// 6. فرز المصفوفة
// Sort array
// --------------------------------------------
اطبع_سطر("6. الفرز / Sort")
مصفوفة أرقام = [15, 3, 9, 1, 20, 7]
اطبع("قبل الفرز / Before sort: ")
اطبع_سطر(أرقام)

مصفوفة مرتبة_تصاعدي = فرز(أرقام, صحيح)
اطبع("تصاعدي / Ascending: ")
اطبع_سطر(مرتبة_تصاعدي)

مصفوفة مرتبة_تنازلي = فرز(أرقام, خطأ)
اطبع("تنازلي / Descending: ")
اطبع_سطر(مرتبة_تنازلي)
اطبع_سطر("")

// --------------------------------------------
// 7. استخراج شريحة
// Extract slice
// --------------------------------------------
اطبع_سطر("7. الشريحة / Slice")
مصفوفة جزء = شريحة(مرتبة_تصاعدي, 1, 4)
اطبع("شريحة [1:4] / Slice [1:4]: ")
اطبع_سطر(جزء)
اطبع_سطر("")

// --------------------------------------------
// 8. إزالة آخر عنصر
// Remove last element
// --------------------------------------------
اطبع_سطر("8. إزالة العنصر / Pop Element")
مصفوفة مؤقتة = [10, 20, 30, 40]
اطبع("قبل الإزالة / Before pop: ")
اطبع_سطر(مؤقتة)

رقم محذوف = إزالة(مؤقتة)
اطبع("العنصر المحذوف / Popped: ")
اطبع_سطر(محذوف)
اطبع_سطر("")

// --------------------------------------------
// 9. مثال عملي: درجات الطلاب
// Practical example: Student grades
// --------------------------------------------
اطبع_سطر("9. مثال عملي: درجات الطلاب")
اطبع_سطر("   Practical: Student Grades")
اطبع_سطر("")

مصفوفة درجات = [85, 92, 78, 95, 88, 73, 90]
اطبع("الدرجات / Grades: ")
اطبع_سطر(درجات)

رقم عدد_الطلاب = حجم(درجات)
اطبع("عدد الطلاب / Number: ")
اطبع_سطر(عدد_الطلاب)

مصفوفة مرتبة = فرز(درجات, صحيح)
رقم أدنى_درجة = أول(مرتبة)
رقم أعلى_درجة = آخر(مرتبة)

اطبع("أدنى درجة / Lowest: ")
اطبع_سطر(أدنى_درجة)
اطبع("أعلى درجة / Highest: ")
اطبع_سطر(أعلى_درجة)

// المتفوقون (85+)
مصفوفة درجات_عالية = شريحة(مرتبة, 3, 7)
اطبع("الدرجات العالية (85+): ")
اطبع_سطر(درجات_عالية)
اطبع_سطر("")

// --------------------------------------------
// 10. مصفوفة متعددة الأنواع
// Mixed-type array
// --------------------------------------------
اطبع_سطر("10. مصفوفة متعددة الأنواع / Mixed Types")
مصفوفة متنوعة = [42, "مرحبا", 3.14, صحيح]
اطبع("المصفوفة / Array: ")
اطبع_سطر(متنوعة)
اطبع("الحجم / Size: ")
اطبع_سطر(حجم(متنوعة))
اطبع_سطر("")

اطبع_سطر("=== انتهى الاختبار ===")
اطبع_سطر("=== Test Complete ===")
