# اختبار دالة المولد - المرحلة 7
# Phase 7: Generator Function Test

# تعريف دالة مولد بسيطة
دالة مولد عد_تنازلي(رقم بداية)
    رقم ع = بداية
    بينما (ع > 0)
        أنتج ع
        ع = ع - 1
    نهاية
نهاية

# الدالة الرئيسية
دالة رئيسية()
    اطبع_سطر("اختبار دالة المولد")
    
    # حالياً: اختبار بسيط بدون استخدام المولد
    رقم مجموع = 0
    لكل(س في [5، 4، 3، 2، 1])
        مجموع = مجموع + س
    نهاية
    
    اطبع_سطر("المجموع: " + مجموع)
    ارجع 0
نهاية
