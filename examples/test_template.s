# ===================================================
# اختبار القوالب (Templates) في لغة ص
# Template Testing in S Language
# ===================================================

# قالب دالة - دالة تعيد أكبر قيمة
# Template function - returns the larger value
قالب<نوع ت> دالة أكبر(ت أ، ت ب) ت
    إذا( أ > ب)
        ارجع أ
    نهاية
    ارجع ب
نهاية

# دالة رئيسية للاختبار
دالة رئيسية()
    اطبع("اختبار القوالب - Template Test")
    
    # الاختبار الأساسي
    رقم نتيجة = 10
    اطبع("النتيجة: " + نتيجة)
    
    ارجع 0
نهاية
