# اختبار دعم اسم الربط الخارجي (FFI Link Name)
# Test FFI link name support

# الصيغة القديمة (لا تزال تعمل) - Old syntax (still works)
خارجي دالة رقم strlen(سلسلة: نص)

# الصيغة الجديدة مع اسم الربط - New syntax with link name
خارجي("puts") دالة اطبع_نص(سلسلة: نص)

# اختبار آخر - Another test
خارجي("memset") دالة رقم امسح_الذاكرة(عنوان: رقم، قيم: رقم، حجم: رقم)

اطبع("تم التحليل بنجاح!")
