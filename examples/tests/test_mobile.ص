# اختبار وحدة الهاتف المحمول
# Mobile module test

اطبع_سطر("=== اختبار وحدة الهاتف ===")

# 1. معلومات الجهاز
اطبع_سطر("--- معلومات الجهاز ---")
متغير منصة = هاتف_منصة()
اطبع_سطر("المنصة: " + منصة)

# 2. معلومات كاملة
هاتف_معلومات()

# 3. توفر الكاميرا
اطبع_سطر("--- الكاميرا ---")
متغير كاميرا = كاميرا_متوفرة()
اطبع_سطر("الكاميرا متوفرة: " + كاميرا)

# 4. الموقع
اطبع_سطر("--- الموقع ---")
متغير موقع = هاتف_موقع()
اطبع_سطر("الموقع: " + موقع)

# 5. حساب المسافة (القاهرة → الرياض)
اطبع_سطر("--- المسافة ---")
متغير مسافة = احسب_المسافة(30.0444, 31.2357, 24.7136, 46.6753)
اطبع_سطر("المسافة القاهرة-الرياض: " + مسافة + " كم")

# 6. المستشعرات
اطبع_سطر("--- المستشعرات ---")
متغير تسارع = هاتف_تسارع()
اطبع_سطر("التسارع: " + تسارع)

متغير دوران = هاتف_دوران()
اطبع_سطر("الدوران: " + دوران)

# 7. واجهة المستخدم
اطبع_سطر("--- واجهة المستخدم ---")
متغير نص = واجهة_نص("مرحباً بالعالم")
اطبع_سطر("نص UI: " + نص)

متغير زر = واجهة_زر("اضغط هنا")
اطبع_سطر("زر UI: " + زر)

# 8. تصدير Compose
اطبع_سطر("--- Jetpack Compose ---")
متغير كومبوز = صدر_كومبوز("نص تجريبي")
اطبع_سطر(كومبوز)

# 9. تصدير SwiftUI
اطبع_سطر("--- SwiftUI ---")
متغير سويفت = صدر_سويفت("نص تجريبي")
اطبع_سطر(سويفت)

# 10. توليد Manifest
اطبع_سطر("--- AndroidManifest ---")
متغير مانيفست = انشئ_manifest("com.example.myapp", "تطبيقي")
اطبع_سطر(مانيفست)

# 11. توليد Plist
اطبع_سطر("--- Info.plist ---")
متغير بلست = انشئ_plist("com.example.myapp", "تطبيقي")
اطبع_سطر(بلست)

اطبع_سطر("=== انتهى الاختبار بنجاح ===")
