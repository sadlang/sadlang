# ============================================================================
# ملف: variables.s
# File: variables.s
# ============================================================================
# الوصف بالعربية:
#   مثال شامل على استخدام المتغيرات في لغة ص. يوضح أنواع البيانات المختلفة
#   (أرقام، نصوص، منطقية)، العمليات الحسابية، والتعامل مع المتغيرات.
#
# English Description:
#   Comprehensive example of using variables in S Language. Demonstrates
#   different data types (numbers, strings, booleans), arithmetic operations,
#   and variable manipulation.
#
# المؤلف / Author: S Language Team
# التاريخ / Date: 22 نوفمبر 2025
# ============================================================================

# ────────────────────────────────────────────────────────────────────────────
# أنواع البيانات الأساسية / Basic Data Types
# ────────────────────────────────────────────────────────────────────────────

اطبع("════════════════════════════════════════")
اطبع("    أنواع البيانات / Data Types")
اطبع("════════════════════════════════════════")
اطبع("")

# أرقام صحيحة / Integers
رقم العمر = 25
رقم السنة = 2025
رقم عدد_الطلاب = 150

اطبع("أرقام صحيحة / Integers:\n")
اطبع("  العمر = " + العمر+ "\n")

اطبع("  السنة = " + السنة+  "\n")
اطبع("  عدد الطلاب = " + عدد_الطلاب+ "\n")


# أرقام عشرية / Floating-point numbers
عشري الطول = 1.75
عشري الوزن = 72.5
عشري درجة_الحرارة = 22.8

اطبع("أرقام عشرية / Floating-point:\n")
اطبع("  الطول = " + الطول + " متر"+ "\n")
اطبع("  الوزن = " + الوزن + " كجم"+ "\n")
اطبع("  درجة الحرارة = " + درجة_الحرارة + "°"+ "\n")


# نصوص / Strings
نص الاسم = "أحمد محمد"
نص المدينة = "الرياض"
نص البريد = "ahmed@example.com"

اطبع("نصوص / Strings:\n")
اطبع("  الاسم: " + الاسم+ "\n")
اطبع("  المدينة: " + المدينة+ "\n")
اطبع("  البريد الإلكتروني: " + البريد+ "\n")
اطبع(" \n")

# قيم منطقية / Boolean values
منطقي مسجل = صحيح
منطقي نشط = صحيح
منطقي محظور = خطأ

اطبع("قيم منطقية / Boolean values:")
اطبع("  مسجل؟ " + مسجل+ "\n")
اطبع("  نشط؟ " + نشط+ "\n")
اطبع("  محظور؟ " + محظور+ "\n")
اطبع( "\n")

# ────────────────────────────────────────────────────────────────────────────
# العمليات الحسابية / Arithmetic Operations
# ────────────────────────────────────────────────────────────────────────────

اطبع("════════════════════════════════════════")
اطبع("  العمليات الحسابية / Arithmetic")
اطبع("════════════════════════════════════════")
اطبع("")

رقم أ = 100
رقم ب = 25

اطبع("الأعداد / Numbers:")
اطبع("  أ = " + أ)
اطبع("  ب = " + ب)
اطبع("")

# العمليات الأساسية / Basic operations
رقم الجمع = أ + ب
رقم الطرح = أ - ب
رقم الضرب = أ * ب
رقم القسمة = أ / ب
رقم الباقي = أ % ب

اطبع("النتائج / Results:")
اطبع("  " + أ + " + " + ب + " = " + الجمع)
اطبع("  " + أ + " - " + ب + " = " + الطرح)
اطبع("  " + أ + " × " + ب + " = " + الضرب)
اطبع("  " + أ + " ÷ " + ب + " = " + القسمة)
اطبع("  " + أ + " % " + ب + " = " + الباقي)
اطبع("")

# ────────────────────────────────────────────────────────────────────────────
# عمليات معقدة / Complex Operations
# ────────────────────────────────────────────────────────────────────────────

اطبع("════════════════════════════════════════")
اطبع("  عمليات معقدة / Complex Operations")
اطبع("════════════════════════════════════════")
اطبع("")

رقم س = 10
رقم ص = 20
رقم ع = 30

# معادلة رياضية / Mathematical equation
رقم النتيجة = (س + ص) * ع / 2
اطبع("(س + ص) × ع ÷ 2 = " + النتيجة)

# حساب المعدل / Calculate average
رقم المجموع = س + ص + ع
رقم المعدل = المجموع / 3
اطبع("المعدل / Average = " + المعدل)
اطبع("")

# ────────────────────────────────────────────────────────────────────────────
# تحديث المتغيرات / Updating Variables
# ────────────────────────────────────────────────────────────────────────────

اطبع("════════════════════════════════════════")
اطبع("  تحديث المتغيرات / Updating Variables")
اطبع("════════════════════════════════════════")
اطبع("")

رقم العداد = 0
اطبع("العداد الأولي / Initial counter: " + العداد)

العداد = العداد + 1
اطبع("بعد +1: " + العداد)

العداد = العداد + 5
اطبع("بعد +5: " + العداد)

العداد = العداد * 2
اطبع("بعد ×2: " + العداد)
اطبع("")

# ────────────────────────────────────────────────────────────────────────────
# دمج النصوص / String Concatenation
# ────────────────────────────────────────────────────────────────────────────

اطبع("════════════════════════════════════════")
اطبع("    دمج النصوص / String Concatenation")
اطبع("════════════════════════════════════════")
اطبع("")

نص الاسم_الأول = "محمد"
نص الاسم_الأخير = "أحمد"
نص الاسم_الكامل = الاسم_الأول + " " + الاسم_الأخير

اطبع("الاسم الأول: " + الاسم_الأول)
اطبع("الاسم الأخير: " + الاسم_الأخير)
اطبع("الاسم الكامل: " + الاسم_الكامل)
اطبع("")

# ────────────────────────────────────────────────────────────────────────────
# مثال تطبيقي: حساب مؤشر كتلة الجسم
# Practical Example: BMI Calculator
# ────────────────────────────────────────────────────────────────────────────

اطبع("════════════════════════════════════════")
اطبع("  مثال: حساب مؤشر كتلة الجسم (BMI)")
اطبع("  Example: Body Mass Index Calculator")
اطبع("════════════════════════────────════════")
اطبع("")

عشري الوزن_بالكيلو = 75.5
عشري الطول_بالمتر = 1.78

# BMI = الوزن ÷ (الطول × الطول)
عشري مؤشر_كتلة_الجسم = الوزن_بالكيلو / (الطول_بالمتر * الطول_بالمتر)

اطبع("الوزن / Weight: " + الوزن_بالكيلو + " kg")
اطبع("الطول / Height: " + الطول_بالمتر + " m")
اطبع("مؤشر كتلة الجسم / BMI: " + مؤشر_كتلة_الجسم)
اطبع("")

# ────────────────────────────────────────────────────────────────────────────
# النهاية / End
# ────────────────────────────────────────────────────────────────────────────

اطبع("════════════════════════════════════════")
اطبع("  ✅ انتهى البرنامج بنجاح!")
اطبع("  ✅ Program completed successfully!")
اطبع("════════════════════════════════════════")

# ============================================================================
# نهاية الملف / End of File
# ============================================================================
