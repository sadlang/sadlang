# ============================================================================
# ملف: conditions.s
# File: conditions.s
# ============================================================================
# الوصف بالعربية:
#   مثال شامل على استخدام الجمل الشرطية في لغة ص. يوضح if, else if, else
#   والعمليات المنطقية والشروط المتداخلة.
#
# English Description:
#   Comprehensive example of conditional statements in S Language. Demonstrates
#   if, else if, else, logical operations, and nested conditions.
#
# المؤلف / Author: S Language Team
# التاريخ / Date: 22 نوفمبر 2025
# ============================================================================

اطبع("════════════════════════════════════════")
اطبع("/n")

اطبع("   الجمل الشرطية / Conditional Statements")
اطبع("/n")

اطبع("════════════════════════════════════════")
اطبع("/n")

# ────────────────────────────────────────────────────────────────────────────
# مثال 1: جملة شرطية بسيطة / Example 1: Simple if statement
# ────────────────────────────────────────────────────────────────────────────

اطبع("───────────────────────────────────────")
اطبع("مثال 1: جملة if بسيطة / Simple if")
اطبع("───────────────────────────────────────")
اطبع("")

رقم العمر = 18

إذا (العمر >= 18)
اطبع("/n")

    اطبع("✓ أنت بالغ / You are an adult")
    اطبع("/n")

نهاية

اطبع(" 5")

# ────────────────────────────────────────────────────────────────────────────
# مثال 2: if-else / Example 2: if-else
# ────────────────────────────────────────────────────────────────────────────

اطبع("───────────────────────────────────────")
اطبع("/n")

اطبع("مثال 2: if-else")
اطبع("/n")

اطبع("───────────────────────────────────────")
اطبع("/n")

اطبع("")

رقم درجة_الحرارة = 25

إذا (درجة_الحرارة > 30) 
    اطبع("🌡️  الجو حار / It's hot")


 وإلا 
    اطبع("🌡️  الجو معتدل / It's moderate")
نهاية
اطبع("/n")

اطبع("")

# ────────────────────────────────────────────────────────────────────────────
# مثال 3: if-else if-else / Example 3: if-else if-else
# ────────────────────────────────────────────────────────────────────────────

اطبع("───────────────────────────────────────")
اطبع("مثال 3: if-else if-else")
اطبع("───────────────────────────────────────")
اطبع("")

رقم الدرجة = 85

اطبع("الدرجة / Grade: " + الدرجة)

إذا (الدرجة >= 90) 
    اطبع("التقدير: A (ممتاز / Excellent)")
نهاية وإلا إذا (الدرجة >= 80) 
    اطبع("التقدير: B (جيد جداً / Very Good)")
نهاية وإلا إذا (الدرجة >= 70) 
    اطبع("التقدير: C (جيد / Good)")
نهاية وإلا إذا (الدرجة >= 60) 
    اطبع("التقدير: D (مقبول / Pass)")
نهاية وإلا 
    اطبع("التقدير: F (راسب / Fail)")
نهاية

اطبع("")

# ────────────────────────────────────────────────────────────────────────────
# مثال 4: عمليات المقارنة / Example 4: Comparison operations
# ────────────────────────────────────────────────────────────────────────────

اطبع("───────────────────────────────────────")
اطبع("مثال 4: عمليات المقارنة / Comparisons")
اطبع("───────────────────────────────────────")
اطبع("")

رقم س = 10
رقم ص = 20

اطبع("س = " + س)
اطبع("ص = " + ص)
اطبع("")

إذا (س == ص) 
    اطبع("س يساوي ص / x equals y")
نهاية

إذا (س != ص) 
    اطبع("✓ س لا يساوي ص / x not equals y")
نهاية

إذا (س < ص) 
    اطبع("✓ س أصغر من ص / x less than y")
نهاية

إذا (س <= ص) 
    اطبع("✓ س أصغر من أو يساوي ص / x less than or equal y")
نهاية

إذا (ص > س) 
    اطبع("✓ ص أكبر من س / y greater than x")
نهاية

إذا (ص >= س) 
    اطبع("✓ ص أكبر من أو يساوي س / y greater than or equal x")
نهاية

اطبع("")

# ────────────────────────────────────────────────────────────────────────────
# مثال 5: العمليات المنطقية / Example 5: Logical operations
# ────────────────────────────────────────────────────────────────────────────

اطبع("───────────────────────────────────────")
اطبع("مثال 5: العمليات المنطقية / Logical Ops")
اطبع("───────────────────────────────────────")
اطبع("")

رقم العمر_للقيادة = 20
منطقي لديه_رخصة = صح

اطبع("العمر: " + العمر_للقيادة)
اطبع("لديه رخصة؟ " + لديه_رخصة)
اطبع("")

# عملية AND (&&)
إذا (العمر_للقيادة >= 18 && لديه_رخصة == صح) 
    اطبع("✓ يمكنه القيادة / Can drive (AND condition)")
نهاية

اطبع("")

# عملية OR (||)
رقم درجة_الاختبار_1 = 85
رقم درجة_الاختبار_2 = 65

إذا (درجة_الاختبار_1 >= 80 || درجة_الاختبار_2 >= 80) 
    اطبع("✓ نجح في أحد الاختبارين / Passed one test (OR condition)")
نهاية

اطبع("")

# عملية NOT (!)
منطقي ممنوع = خطأ

إذا (!ممنوع) 
    اطبع("✓ غير ممنوع / Not banned (NOT condition)")
نهاية

اطبع("")

# ────────────────────────────────────────────────────────────────────────────
# مثال 6: شروط متداخلة / Example 6: Nested conditions
# ────────────────────────────────────────────────────────────────────────────

اطبع("───────────────────────────────────────")
اطبع("مثال 6: شروط متداخلة / Nested Conditions")
اطبع("───────────────────────────────────────")
اطبع("")

رقم الراتب = 15000
رقم سنوات_الخبرة = 5

اطبع("الراتب: " + الراتب)
اطبع("سنوات الخبرة: " + سنوات_الخبرة)
اطبع("")

إذا (الراتب >= 10000) 
    اطبع("✓ الراتب جيد / Good salary")
    
    إذا (سنوات_الخبرة >= 3) 
        اطبع("  ✓ خبرة مناسبة / Suitable experience")
        
        إذا (سنوات_الخبرة >= 5) 
            اطبع("    ✓ خبرة ممتازة! / Excellent experience!")
        نهاية
    نهاية وإلا 
        اطبع("  ⚠ يحتاج لمزيد من الخبرة / Needs more experience")
    نهاية
نهاية وإلا 
    اطبع("⚠ الراتب منخفض / Low salary")
نهاية

اطبع("")

# ────────────────────────────────────────────────────────────────────────────
# مثال 7: شروط معقدة / Example 7: Complex conditions
# ────────────────────────────────────────────────────────────────────────────

اطبع("───────────────────────────────────────")
اطبع("مثال 7: شروط معقدة / Complex Conditions")
اطبع("───────────────────────────────────────")
اطبع("")

رقم العمر_للجامعة = 19
رقم معدل_الثانوية = 92
منطقي لديه_شهادة_ثانوية = صح

اطبع("العمر: " + العمر_للجامعة)
اطبع("المعدل: " + معدل_الثانوية)
اطبع("لديه شهادة ثانوية؟ " + لديه_شهادة_ثانوية)
اطبع("")

إذا ((العمر_للجامعة >= 17 && العمر_للجامعة <= 25) && 
    معدل_الثانوية >= 70 && 
    لديه_شهادة_ثانوية == صح) 
    اطبع("✅ مؤهل للقبول في الجامعة")
    اطبع("✅ Qualified for university admission")
    
    إذا (معدل_الثانوية >= 90) 
        اطبع("  🎓 يستحق منحة دراسية!")
        اطبع("  🎓 Deserves scholarship!")
    نهاية
نهاية وإلا 
    اطبع("❌ غير مؤهل للقبول")
    اطبع("❌ Not qualified for admission")
نهاية

اطبع("")

# ────────────────────────────────────────────────────────────────────────────
# مثال 8: تحديد فئة عمرية / Example 8: Age category
# ────────────────────────────────────────────────────────────────────────────

اطبع("───────────────────────────────────────")
اطبع("مثال 8: تحديد الفئة العمرية / Age Category")
اطبع("───────────────────────────────────────")
اطبع("")

رقم العمر_للتصنيف = 35

اطبع("العمر: " + العمر_للتصنيف)
اطبع("الفئة: ")

إذا (العمر_للتصنيف < 13) 
    اطبع("  طفل / Child (< 13)")
نهاية وإلا إذا (العمر_للتصنيف < 20) 
    اطبع("  مراهق / Teenager (13-19)")
نهاية وإلا إذا (العمر_للتصنيف < 40) 
    اطبع("  شاب / Young Adult (20-39)")
نهاية وإلا إذا (العمر_للتصنيف < 60) 
    اطبع("  كهل / Middle-aged (40-59)")
نهاية وإلا 
    اطبع("  كبير السن / Senior (60+)")
نهاية

اطبع("")

# ────────────────────────────────────────────────────────────────────────────
# النهاية / End
# ────────────────────────────────────────────────────────────────────────────

اطبع("════════════════════════════════════════")
اطبع("  ✅ انتهى البرنامج بنجاح!")
اطبع("  ✅ Program completed successfully!")
اطبع("════════════════════════════════════════")

# ============================================================================
# نهاية الملف / End of File
# ============================================================================
