# اختبار شامل للمرحلة 5 - نظام الأنواع
# Comprehensive Phase 5 Test - Type System

دالة رئيسية() 
    # اختبار 1: استدلال نوع الأرقام الصحيحة
    س = 42
    اطبع(س)
    
    # اختبار 2: العمليات الحسابية
    ص = س + 8
    اطبع(ص)
    
    # اختبار 3: متغيرات متعددة
    أ = 10
    ب = 20
    ج = أ + ب
    اطبع(ج)
    
    # اختبار 4: استدلال نوع نصي
    اسم = "مرحبا"
    اطبع(اسم)
    
    # اختبار 5: طول السلسلة النصية
    كلمة = "Hello World"
    طول_الكلمة = طول(كلمة)
    اطبع(طول_الكلمة)
    
    # اختبار 6: تحويل الأنواع
    رقم_نصي = "123"
    قيمة = لرقم(رقم_نصي)
    اطبع(قيمة)
نهاية
