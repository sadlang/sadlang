# اختبار استدعاءات متعددة ومتداخلة

دالة  رقم مضاعفة(رقم ن)
    ارجع ن * 2
نهاية

دالة  رقم جمع(رقم أ، رقم ب)
    ارجع أ + ب
نهاية

دالة رقم  رئيسية()
    # مضاعفة(5) = 10
    # جمع(10، 32) = 42
    رقم نتيجة = جمع(مضاعفة(5)، 32)
    ارجع نتيجة
نهاية
