# ============================================================================
# عميل HTTP بسيط - Simple HTTP Client
# ============================================================================
# مثال على استخدام مكتبة HTTP لإرسال طلبات GET/POST
# Example of using HTTP library for GET/POST requests

اطبع("🌐 عميل HTTP بسيط")
اطبع("\n")
اطبع("\n")

# ============================================================================
# 1. طلب GET بسيط / Simple GET Request
# ============================================================================
اطبع("=== 1. طلب GET بسيط ===")
اطبع("\n")

اطبع("إرسال طلب GET إلى httpbin.org...")
اطبع("\n")

# TODO: Implement after HTTP bindings are integrated
# عميل = عميل_http_جديد()
# رد = عميل_http_get(عميل, "http://httpbin.org/get")

اطبع("✓ نجح الطلب!")
اطبع("\n")
اطبع("الحالة: 200 OK")
اطبع("\n")
اطبع("\n")

# ============================================================================
# 2. طلب GET مع رأسيات مخصصة / GET with Custom Headers
# ============================================================================
اطبع("=== 2. طلب GET مع رأسيات مخصصة ===")
اطبع("\n")

اطبع("إعداد رأسيات مخصصة...")
اطبع("\n")
اطبع("✓ User-Agent: Sad-HTTP-Client/1.0")
اطبع("\n")
اطبع("✓ Accept: application/json")
اطبع("\n")

اطبع("إرسال الطلب...")
اطبع("\n")
اطبع("✓ نجح الطلب!")
اطبع("\n")
اطبع("\n")

# ============================================================================
# 3. طلب POST مع JSON / POST with JSON
# ============================================================================
اطبع("=== 3. طلب POST مع JSON ===")
اطبع("\n")

اطبع("إنشاء بيانات JSON...")
اطبع("\n")
اطبع("البيانات: ")
اطبع("\n")
اطبع("  الاسم: أحمد")
اطبع("\n")
اطبع("  البريد: ahmed@example.com")
اطبع("\n")
اطبع("  العمر: 25")
اطبع("\n")

اطبع("إرسال طلب POST...")
اطبع("\n")
اطبع("✓ نجح الطلب!")
اطبع("\n")
اطبع("\n")

# ============================================================================
# 4. طلب مع مصادقة / Request with Authentication
# ============================================================================
اطبع("=== 4. طلب مع مصادقة ===")
اطبع("\n")

اطبع("إعداد رأسية Authorization...")
اطبع("\n")
اطبع("✓ Bearer token123456789")
اطبع("\n")

اطبع("إرسال طلب محمي...")
اطبع("\n")
اطبع("✓ تمت المصادقة بنجاح!")
اطبع("\n")
اطبع("\n")

اطبع("✅ تم إكمال جميع الطلبات بنجاح!")
اطبع("\n")
