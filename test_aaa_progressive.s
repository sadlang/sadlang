# محاكاة aaa.s بالتدريج

# دالة 1: قوة_افتراضية
دالة رقم قوة_افتراضية(رقم أساس، رقم أس = 2)
    إرجاع أساس ** أس
نهاية

اطبع("=== اختبار 1 ===")
اطبع("قوة(5) =", قوة_افتراضية(5))

# دالة 2: تحية
دالة تحية(نص اسم، نص تحية = "مرحبا"، نص نهاية_النص = "!")
    اطبع(تحية + " يا " + اسم + نهاية_النص)
نهاية

اطبع("\n=== اختبار 2 ===")
تحية("أحمد")

# دالة 3: أنشئ_قائمة
دالة مصفوفة أنشئ_قائمة(رقم حجم، رقم قيمة = 0)
    مصفوفة نتيجة = []
    رقم ع = 0
    بينما (ع < حجم)
        أضف(نتيجة، قيمة)
        ع = ع + 1
    نهاية
    إرجاع نتيجة
نهاية

اطبع("\n=== اختبار 3 ===")
مصفوفة أصفار = أنشئ_قائمة(3)
اطبع("أصفار:", أصفار)

# دالة 4: طباعة_معلومات
دالة طباعة_معلومات(نص اسم، رقم عمر = 0، نص مدينة = "غير معروف"، منطقي نشط = صحيح)
    اطبع("الاسم:", اسم)
    اطبع("العمر:", عمر)
    اطبع("المدينة:", مدينة)
    اطبع("نشط:", نشط)
    اطبع("---")
نهاية

اطبع("\n=== اختبار 4 ===")
طباعة_معلومات("أحمد")

# دالة 5: مسافة
دالة عشري مسافة(عشري س1، عشري ص1، عشري س2 = 0.0، عشري ص2 = 0.0)
    عشري فرق_س = س2 - س1
    عشري فرق_ص = ص2 - ص1
    عشري مربع_مجموع = (فرق_س ** 2.0) + (فرق_ص ** 2.0)
    إرجاع مربع_مجموع ** 0.5
نهاية

اطبع("\n=== اختبار 5 ===")
اطبع("مسافة:", مسافة(3.0، 4.0))

# دالة 6: تنسيق_نص
دالة نص تنسيق_نص(نص محتوى، نص بادئة = ""، نص لاحقة = ""، منطقي أحرف_كبيرة = خطأ)
    نص نتيجة = بادئة + محتوى + لاحقة
    إرجاع نتيجة
نهاية

اطبع("\n=== اختبار 6 ===")
اطبع(تنسيق_نص("مرحبا"))
اطبع(تنسيق_نص("عنوان"، "=== "، " ==="))
اطبع(تنسيق_نص("ملاحظة"، "["، "]"))

# دالة 7: سعر_بعد_الخصم
دالة عشري سعر_بعد_الخصم(عشري سعر_أصلي، عشري نسبة_خصم = 10.0)
    عشري خصم = سعر_أصلي * (نسبة_خصم / 100.0)
    إرجاع سعر_أصلي - خصم
نهاية

اطبع("\n=== اختبار 7 ===")
اطبع("السعر:", سعر_بعد_الخصم(100.0))

# دالة 8: نطاق (هنا يفشل aaa.s)
دالة مصفوفة نطاق(رقم نهاية_نطاق = 10، رقم بداية = 0، رقم خطوة = 1)
    اطبع("نطاق: بداية =", بداية، "نهاية =", نهاية_نطاق، "خطوة =", خطوة)
    مصفوفة نتيجة = []
    اطبع("قبل الحلقة: نتيجة =", نتيجة)
    رقم ع = بداية
    بينما (ع < نهاية_نطاق)
        اطبع("  في الحلقة: ع =", ع)
        أضف(نتيجة، ع)
        ع = ع + خطوة
    نهاية
    اطبع("بعد الحلقة: نتيجة =", نتيجة)
    إرجاع نتيجة
نهاية

اطبع("\n=== اختبار 8: نطاق ===")
مصفوفة نطاق1 = نطاق()
اطبع("نطاق1:", نطاق1)

اطبع("الآن سنجرب نطاق(5)")
مصفوفة نطاق2 = نطاق(5)
اطبع("نطاق2:", نطاق2)

اطبع("الآن سنجرب نطاق(20، 0، 2)")
مصفوفة نطاق3 = نطاق(20، 0، 2)
اطبع("نطاق3:", نطاق3)
