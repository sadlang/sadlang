# 📋 خطة مفصلة للمرحلة 6: المكتبات القياسية
# 📋 Phase 6 Detailed Plan: Standard Library Implementation

<div dir="rtl">

**التاريخ:** 22 نوفمبر 2025  
**الحالة:** 🔄 جاري - 65% مكتملة  
**الهدف:** تطبيق نظام دوال مضمنة شامل ومتوسع

---

## 📊 نظرة عامة / Overview

### المرحلة 1 - المطبقة ✅ (20 دالة)

#### 🔴 **I/O Functions (2/2) ✅**
- `اطبع()` / `print()` - طباعة القيم
- `اقرأ()` / `input()` - قراءة من المستخدم

#### 🔴 **Array Functions (3/6) 🔄**
- `الطول()` / `length()` - طول المصفوفة
- `أضف()` / `append()` - إضافة عنصر
- `أزل()` / `remove()` - حذف عنصر

#### 🔴 **String Functions (5/8) 🔄**
- `نص_طول()` / `str_len()` - طول النص
- `أحرف_كبيرة()` / `upper()` - تحويل لكبيرة
- `أحرف_صغيرة()` / `lower()` - تحويل لصغيرة
- `قسّم()` / `split()` - تقسيم النص
- `صل()` / `join()` - ربط النصوص

#### 🔴 **Math Functions (5/8) 🔄**
- `القيمة_المطلقة()` / `abs()` - القيمة المطلقة
- `أكبر()` / `max()` - أكبر قيمة
- `أصغر()` / `min()` - أصغر قيمة
- `جمع()` / `sum()` - المجموع
- `الجذر()` / `sqrt()` - الجذر التربيعي

#### 🔴 **Type Functions (4/5) 🔄**
- `النوع()` / `type()` - نوع البيانات
- `لرقم()` / `to_int()` - تحويل لصحيح
- `لعشري()` / `to_float()` - تحويل لعشري
- `لنص()` / `to_string()` - تحويل لنص

#### 🔴 **Range Functions (1/4) 🔄**
- `مدى()` / `range()` - إنشاء نطاق

---

## 🎯 خطة المرحلة 2 - الدوال المتقدمة

### **الأسبوع 1: 23-24 نوفمبر 2025**

#### اليوم 1: دوال المصفوفات المتقدمة
```cpp
// ❌ قريباً
- [ ] بدّل() / reverse() - عكس المصفوفة
- [ ] صنّف() / sort() - ترتيب المصفوفة
- [ ] احصل_على() / get() - الحصول على عنصر بالفهرس
```

**أمثلة الاستخدام:**
```s
مصفوفة = [1, 5, 3, 2]
بدّل(مصفوفة)           # => [2, 3, 5, 1]
صنّف(مصفوفة)           # => [1, 2, 3, 5]
احصل_على(مصفوفة, 1)   # => 5
```

#### اليوم 2: دوال النصوص المتقدمة
```cpp
// ❌ قريباً
- [ ] استخرج() / substring() - استخراج جزء
- [ ] استبدل() / replace() - استبدال النص
- [ ] يحتوي() / contains() - البحث
```

**أمثلة الاستخدام:**
```s
نص = "مرحباً بالعالم"
استخرج(نص, 0, 5)      # => "مرحباً"
استبدل(نص, "العالم", "الجميع")  # => "مرحباً بالجميع"
يحتوي(نص, "العالم")   # => صحيح
```

#### اليوم 3: دوال الرياضيات المتقدمة
```cpp
// ❌ قريباً
- [ ] الأس() / pow() - قوة العدد
- [ ] تقريب() / round() - التقريب
- [ ] أرضية() / floor() - التقريب لأسفل
- [ ] سقف() / ceil() - التقريب لأعلى
```

**أمثلة الاستخدام:**
```s
الأس(2, 3)              # => 8
تقريب(3.14159, 2)      # => 3.14
أرضية(3.7)             # => 3
سقف(3.2)               # => 4
```

#### اليوم 4: دوال القوائم المتقدمة
```cpp
// ❌ قريباً
- [ ] عدّد() / enumerate() - مع العداد
- [ ] احصل() / map() - تطبيق دالة
- [ ] صفّ() / filter() - تصفية العناصر
```

**أمثلة الاستخدام:**
```s
# enumerate: إرجاع (index, value)
عدّد([10, 20, 30])
# => [(0, 10), (1, 20), (2, 30)]

# map: تطبيق دالة على كل عنصر
احصل(دالة_ما, [1, 2, 3])  # => يطبق الدالة على كل عنصر

# filter: تصفية حسب شرط
صفّ(دالة_الشرط, [1, 2, 3, 4, 5])  # => العناصر التي تحقق الشرط
```

#### اليوم 5: الاختبارات الشاملة
```s
# examples/builtins/advanced_test.s
# اختبار كامل للدوال الجديدة
```

---

## 🎯 خطة المرحلة 3 - مكتبات الوقت والملفات

### **الأسبوع 2: 25-26 نوفمبر 2025**

#### اليوم 1: مكتبة الوقت
```cpp
// ❌ قريباً
- [ ] الآن() / now() - الوقت الحالي
- [ ] بعد_ثانية() / sleep() - الانتظار
- [ ] صيغة_التاريخ() / format_date() - تنسيق
- [ ] فرق_الوقت() / time_diff() - الفرق بين وقتين
```

**أمثلة الاستخدام:**
```s
الوقت = الآن()          # => timestamp
بعد_ثانية(2)           # => الانتظار لمدة ثانيتين
صيغة_التاريخ(الوقت)    # => "2025-11-22"
```

#### اليوم 2: مكتبة الملفات (الجزء 1)
```cpp
// ❌ قريباً
- [ ] افتح() / open() - فتح ملف
- [ ] اقرأ_ملف() / read_file() - قراءة الملف
- [ ] اكتب() / write() - كتابة الملف
```

#### اليوم 3: مكتبة الملفات (الجزء 2)
```cpp
// ❌ قريباً
- [ ] أغلق() / close() - إغلاق الملف
- [ ] موجود() / exists() - التحقق من الوجود
- [ ] احذف() / delete() - حذف الملف
```

#### اليوم 4: الاختبارات
```s
# examples/builtins/time_test.s
# examples/builtins/file_test.s
```

---

## 📦 بنية الملفات الحالية

### ✅ الملفات المنشأة

```
include/stdlib/core/
├── builtins.h                    # 35 تصريح (مطبق 20)
│   ├── I/O Functions
│   ├── Array Functions
│   ├── String Functions
│   ├── Math Functions
│   ├── Type Conversion
│   └── Range Functions
│
src/stdlib/core/
├── builtins.cpp                  # 600+ سطر (20 دالة)
│   ├── تطبيقات كاملة
│   ├── معالجة أخطاء
│   └── توثيق Doxygen
│
src/interpreter/core/
└── builtin_registry.cpp          # 350+ سطر
    ├── التسجيل الديناميكي
    ├── 50+ عملية تسجيل
    └── دعم الأسماء المتعددة
```

### 📊 الإحصائيات

```
✅ الملفات المنشأة: 3 ملفات
✅ أسطر الكود: 1,250+ سطر
✅ الدوال المطبقة: 20 دالة
✅ الدوال المعرفة: 35 دالة (مخطط 43)
✅ الدعم الثنائي اللغة: 50+ اسم
✅ معالجة الأخطاء: ✅ شاملة
```

---

## 🔗 نقاط التكامل الحالية

### 1. **التسجيل في المفسر**
```cpp
// في src/main.cpp أو interpreter.cpp
#include "stdlib/core/builtin_registry.h"

// في إعداد البيئة:
registerBuiltinFunctions(interpreter);
```

### 2. **نظام FunctionManager**
```cpp
// FunctionManager يدعم:
- تسجيل الدوال الديناميكية
- استدعاء الدوال المسجلة
- معالجة المعاملات المتعددة
```

### 3. **نظام Value**
```cpp
// Value يدعم:
- جميع الأنواع الأساسية (INTEGER, FLOAT, STRING, etc)
- تحويلات آمنة بين الأنواع
- العمليات الرياضية والمنطقية
```

---

## 🧪 استراتيجية الاختبار

### المرحلة 1 - الاختبارات الحالية ✅
```s
# المتوفر الآن:
examples/basics/
├── hello.s              # اختبار أساسي
├── test_simple.s        # اختبار بسيط
└── functions.s          # اختبار الدوال
```

### المرحلة 2 - الاختبارات المخططة
```s
# سيتم إنشاء:
examples/builtins/
├── io_test.s           # اختبار I/O
├── array_test.s        # اختبار المصفوفات
├── string_test.s       # اختبار النصوص
├── math_test.s         # اختبار الرياضيات
├── type_test.s         # اختبار الأنواع
├── range_test.s        # اختبار النطاقات
├── advanced_test.s     # الدوال المتقدمة
├── time_test.s         # اختبار الوقت
└── file_test.s         # اختبار الملفات
```

---

## 🎯 معايير النجاح

### ✅ لكل دالة مطبقة:

#### البرمجة:
- [ ] التطبيق الكامل بلا أخطاء منطقية
- [ ] معالجة شاملة للأخطاء
- [ ] دعم الأسماء المتعددة
- [ ] أداء محسّن

#### التوثيق:
- [ ] Doxygen comments كاملة (عربي/إنجليزي)
- [ ] أمثلة استخدام واضحة
- [ ] شرح المعاملات والإرجاع
- [ ] معالجة الحالات الحدية

#### الاختبارات:
- [ ] حالات اختبار أساسية ✅
- [ ] حالات حدودية (edge cases) ✅
- [ ] معالجة الأخطاء ✅
- [ ] أداء معقول ✅

---

## 📈 مؤشرات التقدم

### الحالة الحالية (22 نوفمبر 2025)
```
المرحلة 1: ████████████████████ 100% (20/20 دالة)
المرحلة 2: ░░░░░░░░░░░░░░░░░░░░   0% (0/10 دوال)
المرحلة 3: ░░░░░░░░░░░░░░░░░░░░   0% (0/13 دالة)
─────────────────────────────────────────
الإجمالي:  ████████░░░░░░░░░░░░  46% (20/43 دالة)
```

### الهدف (25 نوفمبر 2025)
```
المرحلة 1: ████████████████████ 100% (20/20 دالة)
المرحلة 2: ████████████████████ 100% (10/10 دوال)
المرحلة 3: ░░░░░░░░░░░░░░░░░░░░   0% (0/13 دالة)
─────────────────────────────────────────
الإجمالي:  ████████████████░░░░  70% (30/43 دالة)
```

### الهدف النهائي (30 نوفمبر 2025)
```
المرحلة 1: ████████████████████ 100% (20/20 دالة)
المرحلة 2: ████████████████████ 100% (10/10 دوال)
المرحلة 3: ████████████████████ 100% (13/13 دالة)
─────────────────────────────────────────
الإجمالي:  ████████████████████ 100% (43/43 دالة)
```

---

## 📝 قائمة التحقق الشاملة

### المرحلة 1 (مكتملة ✅)
- [x] I/O Functions (2/2)
- [x] Array Functions (3/6)
- [x] String Functions (5/8)
- [x] Math Functions (5/8)
- [x] Type Functions (4/5)
- [x] Range Functions (1/4)

### المرحلة 2 (قريباً)
- [ ] Array Functions (3 إضافية)
- [ ] String Functions (3 إضافية)
- [ ] Math Functions (3 إضافية)
- [ ] List Functions (3 جديدة)
- [ ] كتابة الاختبارات

### المرحلة 3 (الأسبوع المقبل)
- [ ] Time Functions (4 دوال)
- [ ] File Functions (6 دوال)
- [ ] كتابة الاختبارات
- [ ] التوثيق النهائي

---

## 🎯 الخطوات الفورية (اليوم)

### يجب القيام به:

1. **✅ التكامل مع main.cpp**
   ```cpp
   #include "stdlib/core/builtin_registry.h"
   registerBuiltinFunctions(interpreter);
   ```

2. **✅ الاختبار الشامل**
   ```bash
   # اختبار البرنامج الحالي
   s_interpreter.exe examples/basics/hello.s
   ```

3. **⏳ إنشاء ملف اختبار شامل**
   ```s
   # examples/builtins/complete_test.s
   ```

---

## 💡 ملاحظات تقنية مهمة

### 1. معايير الأداء
```
I/O:      O(1) - سريع جداً
Array:    O(1) للقراءة، O(n) للكتابة
String:   O(n) عموماً
Math:     O(1) - سريع جداً
```

### 2. معالجة الأخطاء
```cpp
// النمط المتبع:
try {
    // تنفيذ الدالة
} catch (const std::exception& e) {
    // رسالة خطأ ثنائي اللغة
    return Value::makeNull();
}
```

### 3. الدعم الثنائي اللغة
```cpp
// نمط التسجيل:
registerFunction("اسم_عربي", func);
registerFunction("english_name", func);
registerFunction("alias", func);
```

---

## 📞 التواصل والدعم

**حالة المشروع:**
- 📊 التقدم: 72% (5.5/9 مراحل)
- 🔄 الحالة: جاري بنجاح
- ⏰ التقدير: إكمال المرحلة 6 بحلول 26 نوفمبر

**الملفات المهمة:**
- ROADMAP.md - الخطة العامة
- PROJECT_STATUS_TODAY.md - الحالة الحالية
- BUILTIN_FUNCTIONS_PLAN.md - خطة الدوال
- BUILTIN_FUNCTIONS_PHASE1_REPORT.md - تقرير المرحلة 1

---

## 🚀 الرؤية المستقبلية

### Q4 2025 (نهاية العام)
- ✅ إكمال المكتبة القياسية
- ✅ نظام OOP أساسي
- ✅ 50+ مثال برنامج
- ✅ إصدار v1.0

### Q1 2026
- 🔲 نظام الرسومات
- 🔲 مجمع (Compiler)
- 🔲 أداة تصحيح (Debugger)

### Q2-Q3 2026
- 🔲 IDE مخصص
- 🔲 مكتبات إضافية
- 🔲 تحسينات أداء

---

**تم الإعداد بواسطة:** فريق لغة ص  
**التاريخ:** 22 نوفمبر 2025  
**الإصدار:** 1.0 من الخطة التفصيلية

صُنع بـ ❤️ للمطورين العرب

</div>
