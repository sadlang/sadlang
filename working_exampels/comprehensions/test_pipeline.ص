# اختبار عامل الأنبوب |>
# Test pipeline operator |>

دالة ضاعف(ع)
    ارجع ع * 2
نهاية

دالة اضف(ع، ق)
    ارجع ع + ق
نهاية

دالة مربع(ع)
    ارجع ع * ع
نهاية

دالة رئيسية()
    # 1. أنبوب بسيط: 5 |> ضاعف → ضاعف(5)
    اطبع_سطر("--- 1. أنبوب بسيط ---")
    متغير نتيجة = 5 |> ضاعف
    اطبع_سطر(نتيجة)

    # 2. سلسلة أنابيب: 3 |> ضاعف |> مربع → مربع(ضاعف(3))
    اطبع_سطر("--- 2. سلسلة أنابيب ---")
    متغير نتيجة_ب = 3 |> ضاعف |> مربع
    اطبع_سطر(نتيجة_ب)

    # 3. أنبوب مع معاملات: 5 |> اضف(10) → اضف(5, 10)
    اطبع_سطر("--- 3. أنبوب مع معاملات ---")
    متغير نتيجة_ج = 5 |> اضف(10)
    اطبع_سطر(نتيجة_ج)

    # 4. سلسلة مع معاملات
    اطبع_سطر("--- 4. سلسلة مع معاملات ---")
    متغير نتيجة_د = 3 |> ضاعف |> اضف(100)
    اطبع_سطر(نتيجة_د)

    ارجع 0
نهاية
