# ═══════════════════════════════════════════════════════════
# اختبار أساسي لدوال Embedded المتقدمة
# Basic test for Advanced Embedded Functions (18)
# ═══════════════════════════════════════════════════════════

اطبع_سطر("=== اختبار دوال Embedded المتقدمة ===")
اطبع_سطر("")

# ─── 1. Serial I/O ──────────────────────────
اطبع_سطر("--- اختبار التسلسلي (Serial I/O) ---")
تسلسلي_هيئ(0x3F8, 9600)
تسلسلي_ارسل(0x3F8, 72)
تسلسلي_ارسل(0x3F8, 105)
متغير جاهز = تسلسلي_جاهز(0x3F8)
اطبع("جاهز: ")
اطبع_سطر(لنص(جاهز))
متغير مستقبل = تسلسلي_استقبل(0x3F8)
اطبع("استقبال: ")
اطبع_سطر(لنص(مستقبل))
اطبع_سطر("")

# ─── 2. GPIO ────────────────────────────────
اطبع_سطر("--- اختبار GPIO ---")
حدد_وضع_منفذ(13, 1)
منفذ_رقمي_اكتب(13, 1)
متغير قيمة_منفذ = منفذ_رقمي_اقرأ(13)
اطبع("قيمة المنفذ: ")
اطبع_سطر(لنص(قيمة_منفذ))
اطبع_سطر("")

# ─── 3. Timer ───────────────────────────────
اطبع_سطر("--- اختبار المؤقت (Timer) ---")
مؤقت_هيئ(1000)
متغير وقت1 = مؤقت_قراءة()
اطبع("وقت: ")
اطبع_سطر(لنص(وقت1))
مؤقت_انتظر(100)
متغير وقت2 = مؤقت_قراءة()
اطبع("وقت بعد الانتظار: ")
اطبع_سطر(لنص(وقت2))
اطبع_سطر("")

# ─── 4. System Control ──────────────────────
اطبع_سطر("--- اختبار التحكم بالنظام ---")
متغير معالج = معرف_المعالج()
اطبع("معرف المعالج: ")
اطبع_سطر(لنص(معالج))
متغير دورات = عداد_الدورات()
اطبع("عداد الدورات: ")
اطبع_سطر(لنص(دورات))
اطبع_سطر("")

# ─── 5. Memory Barriers ────────────────────
اطبع_سطر("--- اختبار حواجز الذاكرة ---")
حاجز_ذاكرة()
حاجز_قراءة()
حاجز_كتابة()
اطبع_سطر("")

# ─── 6. DMA ─────────────────────────────────
اطبع_سطر("--- اختبار DMA ---")
نقل_مباشر_هيئ(0, 0x1000, 0x2000, 512)
نقل_مباشر_ابدأ(0)
اطبع_سطر("")

اطبع_سطر("=== جميع اختبارات Embedded نجحت! ===")
