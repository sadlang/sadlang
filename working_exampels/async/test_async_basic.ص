# ═══════════════════════════════════════════════════════════
# اختبار أساسي لـ Async/Await والتزامن
# Basic Async/Await & Concurrency Test
# ═══════════════════════════════════════════════════════════

اطبع_سطر("=== اختبار Async/Await الأساسي ===")
اطبع_سطر("")

# ─── 1. إنشاء مهمة ──────────────────────────
اطبع_سطر("--- 1. إنشاء مهمة غير متزامنة ---")
متغير مهمة1 = أنشئ_مهمة("عملية_حسابية")
اطبع("معرف المهمة: ")
اطبع_سطر(لنص(مهمة1))

# ─── 2. انتظار المهمة ──────────────────────
اطبع_سطر("--- 2. انتظار المهمة ---")
متغير نتيجة = انتظر_مهمة(مهمة1)
اطبع("نتيجة الانتظار: ")
اطبع_سطر(لنص(نتيجة))
اطبع_سطر("")

# ─── 3. Futures (مستقبل/وعد) ─────────────────
اطبع_سطر("--- 3. اختبار المستقبل (Future) ---")
متغير مستقبل1 = أنشئ_مستقبل()
اطبع("معرف المستقبل: ")
اطبع_سطر(لنص(مستقبل1))

أوف_مستقبل(مستقبل1, 42)
متغير قيمة = احصل_مستقبل(مستقبل1)
اطبع("قيمة المستقبل: ")
اطبع_سطر(لنص(قيمة))
اطبع_سطر("")

# ─── 4. القنوات ────────────────────────────
اطبع_سطر("--- 4. اختبار القنوات ---")
متغير قناة1 = أنشئ_قناة(10)
اطبع("معرف القناة: ")
اطبع_سطر(لنص(قناة1))

أرسل_قناة(قناة1, 100)
أرسل_قناة(قناة1, 200)
متغير مستلم1 = استقبل_قناة(قناة1)
اطبع("مستلم: ")
اطبع_سطر(لنص(مستلم1))
متغير مستلم2 = استقبل_قناة(قناة1)
اطبع("مستلم: ")
اطبع_سطر(لنص(مستلم2))
أغلق_قناة(قناة1)
اطبع_سطر("")

# ─── 5. القفل المتبادل (Mutex) ────────────
اطبع_سطر("--- 5. اختبار القفل المتبادل ---")
متغير قفل1 = أنشئ_قفل()
اطبع("معرف القفل: ")
اطبع_سطر(لنص(قفل1))
اقفل(قفل1)
افتح_قفل(قفل1)
اطبع_سطر("")

# ─── 6. الخيوط ────────────────────────────
اطبع_سطر("--- 6. اختبار الخيوط ---")
متغير خيط1 = أنشئ_خيط("دالة_معالجة")
اطبع("معرف الخيط: ")
اطبع_سطر(لنص(خيط1))
انضم_خيط(خيط1)
اطبع_سطر("")

# ─── 7. Yield ───────────────────────────────
اطبع_سطر("--- 7. اختبار Yield ---")
async_yield(0)
اطبع_سطر("")

# ─── 8. Async Sleep ─────────────────────────
اطبع_سطر("--- 8. اختبار النوم ---")
نوم_غير_متزامن(50)
اطبع_سطر("")

اطبع_سطر("=== جميع اختبارات Async الأساسية نجحت! ===")
