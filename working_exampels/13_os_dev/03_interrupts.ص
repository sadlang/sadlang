# ===================================================================
# مثال 3: المقاطعات والتحكم بالمعالج
# Example 3: Interrupts and CPU Control
# ===================================================================
دالة رئيسية()
اطبع("=== المقاطعات ===")

اطبع("تعطيل المقاطعات:")
تعطيل_مقاطعات()

اطبع("  تعديل IDT...")
اطبع("  تعديل GDT...")
اطبع("  إعداد PIC...")

اطبع("تفعيل المقاطعات:")
تفعيل_مقاطعات()

اطبع("مقاطعات BIOS:")
اطبع("  int 0x10:")
مقاطعة(0x10)
اطبع("  int 0x16:")
مقاطعة(0x16)
اطبع("  int 0x15:")
مقاطعة(0x15)

اطبع("حلقة خمول:")
اطبع("  دورة 1:")
تعطيل_مقاطعات()
اطبع("    فحص...")
تفعيل_مقاطعات()
توقف()

اطبع("  دورة 2:")
تعطيل_مقاطعات()
اطبع("    فحص...")
تفعيل_مقاطعات()
توقف()

اطبع("  دورة 3:")
تعطيل_مقاطعات()
اطبع("    فحص...")
تفعيل_مقاطعات()
توقف()

اطبع("=== انتهى مثال المقاطعات ===")
نهاية