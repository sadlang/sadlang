دالة مصفوفة اختبار_loop()
    مصفوفة ق = []
    اطبع("بداية، ق =", ق)
    
    رقم ع = 0
    اطبع("قبل اللوب، ع =", ع)
    
    بينما (ع < 5)
        اطبع("داخل اللوب، ع =", ع، "، ق =", ق)
        أضف(ق، ع)
        اطبع("بعد أضف، ق =", ق)
        ع = ع + 1
        اطبع("بعد ع++، ع =", ع)
    نهاية
    
    اطبع("بعد اللوب، ق =", ق)
    إرجاع ق
نهاية

مصفوفة نتيجة = اختبار_loop()
اطبع("النتيجة النهائية =", نتيجة)
