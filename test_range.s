# اختبار بسيط لدالة نطاق
دالة مصفوفة اختبار_نطاق()
    اطبع("بداية الدالة")
    مصفوفة نتيجة = []
    اطبع("بعد إنشاء المصفوفة:", نتيجة)
    
    رقم ع = 0
    بينما (ع < 3)
        اطبع("قبل أضف، ع =", ع)
        أضف(نتيجة، ع)
        اطبع("بعد أضف")
        ع = ع + 1
    نهاية
    
    اطبع("النتيجة النهائية:", نتيجة)
    إرجاع نتيجة
نهاية

اطبع("=== اختبار ===")
مصفوفة قائمة = اختبار_نطاق()
اطبع("القائمة المُرجعة:", قائمة)
