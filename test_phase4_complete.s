# ═══════════════════════════════════════════════════════════════════
# اختبار المرحلة 4 - الطرق / Phase 4 Test - Methods
# ═══════════════════════════════════════════════════════════════════

صنف مستطيل
    رقم طول
    رقم عرض
    
    # طريقة لتعيين الأبعاد
    دالة عين_ابعاد(رقم ط, رقم ع)
        طول = ط
        عرض = ع
    نهاية
    
    # طريقة لحساب المساحة
    دالة احسب_مساحة() رقم
        ارجع طول * عرض
    نهاية
    
    # طريقة لمضاعفة الأبعاد
    دالة ضاعف()
        طول = طول * 2
        عرض = عرض * 2
    نهاية
نهاية

# إنشاء كائن
مستطيل م = جديد مستطيل()

# تعيين قيم أولية
م.طول = 5
م.عرض = 3

# استدعاء طريقة لتغيير القيم
م.عين_ابعاد(10, 4)

# استدعاء طريقة لمضاعفة الأبعاد
م.ضاعف()

# النتيجة المتوقعة: طول = 20، عرض = 8
