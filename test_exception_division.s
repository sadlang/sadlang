# Test Division by Zero Exception
# اختبار استثناء القسمة على صفر

اطبع("=== اختبار القسمة على صفر ===");

# Test 1 Integer division by zero
اطبع("\n--- الاختبار 1: قسمة أعداد صحيحة على صفر ---");

حاول 
    رقم نتيجة = 10 / 0 ;
    اطبع("هذا لن يُطبع");
نهاية
امسك (استثناء) 
    اطبع("✅ تم اصطياد خطأ القسمة: ", استثناء);
نهاية

# Test 2 Float division by zero
اطبع("\n--- الاختبار 2: قسمة أعداد عشرية على صفر ---");

حاول 
    عشري نتيجة = 5.5 / 0.0 ;
    اطبع("هذا لن يُطبع");
نهاية
امسك (استثناء2) 
    اطبع("✅ تم اصطياد خطأ القسمة العشرية: ", استثناء2);
نهاية

# Test 3 Division works normally
اطبع("\n--- الاختبار 3: القسمة العادية تعمل بشكل صحيح ---");

رقم ع1 = 20 / 4 ;
اطبع("20 / 4 = ", ع1);

عشري ع2 = 15.0 / 3.0 ;
اطبع("15.0 / 3.0 = ", ع2);

اطبع("\n✅ جميع اختبارات القسمة اكتملت بنجاح");
