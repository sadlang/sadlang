# اختبار المصفوفة الفارغة

اطبع("=== اختبار 1: إنشاء مصفوفة فارغة ===")
مصفوفة قائمة = []
اطبع("المصفوفة:", قائمة)
اطبع("الطول:", طول(قائمة))

اطبع("\n=== اختبار 2: إضافة عنصر للمصفوفة الفارغة ===")
أضف(قائمة، 10)
اطبع("بعد الإضافة:", قائمة)
اطبع("الطول:", طول(قائمة))

اطبع("\n=== اختبار 3: إضافة عناصر متعددة ===")
أضف(قائمة، 20)
أضف(قائمة، 30)
اطبع("النتيجة:", قائمة)
اطبع("الطول:", طول(قائمة))
