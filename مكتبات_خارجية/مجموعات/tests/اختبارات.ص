# اختبارات مكتبة المجموعات
# ═══════════════════════════════════════

من مكتبات_خارجية.مجموعات.src.مكتبة استورد مكدس, طابور, طابور_أولويات, مجموعة_فريدة
من مكتبات_خارجية.مجموعات.src.مكتبة استورد قائمة_مرتبطة, خريطة_مرتبة, عداد, مكدس_محدود

اطبع_سطر("╔══════════════════════════════════════╗")
اطبع_سطر("║ اختبارات مكتبة المجموعات            ║")
اطبع_سطر("╚══════════════════════════════════════╝")

متغير _نجح = 0
متغير _فشل = 0

دالة _تحقق(الوصف, الشرط)
   إذا (الشرط)
      _نجح = _نجح + 1
      اطبع_سطر("  ✓ " + الوصف)
   وإلا
      _فشل = _فشل + 1
      اطبع_سطر("  ✗ " + الوصف)
   نهاية
نهاية

# ═══ المكدس ═══
اطبع_سطر("")
اطبع_سطر("── المكدس ──")
متغير م = جديد مكدس()
_تحقق("مكدس جديد فارغ", م.فارغ() == صحيح)
_تحقق("الحجم = 0", م.الحجم() == 0)

م.ادفع(10)
م.ادفع(20)
م.ادفع(30)
_تحقق("بعد 3 دفعات: الحجم = 3", م.الحجم() == 3)
_تحقق("القمة = 30", م.القمة() == 30)

متغير مسحوب = م.اسحب()
_تحقق("سحب = 30", مسحوب == 30)
_تحقق("بعد السحب: الحجم = 2", م.الحجم() == 2)
_تحقق("القمة الجديدة = 20", م.القمة() == 20)

م.امسح()
_تحقق("بعد المسح: فارغ", م.فارغ() == صحيح)

# ═══ الطابور ═══
اطبع_سطر("")
اطبع_سطر("── الطابور ──")
متغير ط = جديد طابور()
_تحقق("طابور جديد فارغ", ط.فارغ() == صحيح)

ط.أضف(1)
ط.أضف(2)
ط.أضف(3)
_تحقق("بعد 3 إضافات: الحجم = 3", ط.الحجم() == 3)
_تحقق("الأمام = 1 (FIFO)", ط.الأمام() == 1)

متغير مزال = ط.أزل()
_تحقق("إزالة = 1", مزال == 1)
_تحقق("الأمام الجديد = 2", ط.الأمام() == 2)

ط.امسح()
_تحقق("بعد المسح: فارغ", ط.فارغ() == صحيح)

# ═══ طابور الأولويات ═══
اطبع_سطر("")
اطبع_سطر("── طابور_أولويات ──")
متغير طأ = جديد طابور_أولويات()
_تحقق("طابور أولويات جديد فارغ", طأ.فارغ() == صحيح)

طأ.أضف("منخفض", 3)
طأ.أضف("عالي", 1)
طأ.أضف("متوسط", 2)
_تحقق("الحجم = 3", طأ.الحجم() == 3)

متغير أول = طأ.أزل()
_تحقق("الأعلى أولوية أولاً (1)", أول == "عالي")

متغير ثاني = طأ.أزل()
_تحقق("ثم الأولوية 2", ثاني == "متوسط")

# ═══ المجموعة الفريدة ═══
اطبع_سطر("")
اطبع_سطر("── مجموعة_فريدة ──")
متغير مف = جديد مجموعة_فريدة()
مف.أضف(1)
مف.أضف(2)
مف.أضف(3)
مف.أضف(2)
مف.أضف(1)
_تحقق("3 عناصر فريدة فقط", مف.الحجم() == 3)
_تحقق("يحتوي 1", مف.يحتوي(1) == صحيح)
_تحقق("يحتوي 2", مف.يحتوي(2) == صحيح)
_تحقق("لا يحتوي 5", مف.يحتوي(5) == خطأ)

مف.أزل(2)
_تحقق("بعد إزالة 2: الحجم = 2", مف.الحجم() == 2)
_تحقق("لا يحتوي 2 بعد الإزالة", مف.يحتوي(2) == خطأ)

# ═══ الاتحاد والتقاطع ═══
اطبع_سطر("")
اطبع_سطر("── الاتحاد والتقاطع ──")
متغير م1 = جديد مجموعة_فريدة()
م1.أضف(1)
م1.أضف(2)
م1.أضف(3)

متغير م2 = جديد مجموعة_فريدة()
م2.أضف(2)
م2.أضف(3)
م2.أضف(4)

متغير اتحاد_ن = م1.اتحاد(م2)
_تحقق("الاتحاد: 4 عناصر", اتحاد_ن.الحجم() == 4)

متغير تقاطع_ن = م1.تقاطع(م2)
_تحقق("التقاطع: 2 عناصر", تقاطع_ن.الحجم() == 2)
_تحقق("التقاطع يحتوي 2", تقاطع_ن.يحتوي(2) == صحيح)
_تحقق("التقاطع يحتوي 3", تقاطع_ن.يحتوي(3) == صحيح)

# ═══ إلى مصفوفة ═══
اطبع_سطر("")
اطبع_سطر("── إلى_مصفوفة ──")
متغير مص = م1.إلى_مصفوفة()
_تحقق("التحويل لمصفوفة: 3 عناصر", طول(مص) == 3)

# ═══ القائمة المرتبطة ═══
اطبع_سطر("")
اطبع_سطر("── قائمة_مرتبطة ──")
متغير قم = جديد قائمة_مرتبطة()
_تحقق("قائمة جديدة فارغة", قم.فارغة() == صحيح)

قم.أضف(10)
قم.أضف(20)
قم.أضف(30)
_تحقق("الحجم = 3", قم.الحجم() == 3)
_تحقق("الأول = 10", قم.الأول() == 10)
_تحقق("الأخير = 30", قم.الأخير() == 30)

قم.أضف_أولاً(5)
_تحقق("إضافة أولاً: الأول = 5", قم.الأول() == 5)
_تحقق("الحجم = 4", قم.الحجم() == 4)

_تحقق("يحتوي 20", قم.يحتوي(20) == صحيح)
_تحقق("لا يحتوي 100", قم.يحتوي(100) == خطأ)

# ═══ الإزالة من القائمة ═══
اطبع_سطر("")
اطبع_سطر("── الإزالة من القائمة ──")
متغير أزيل_أ = قم.أزل_أولاً()
_تحقق("إزالة أولاً = 5", أزيل_أ == 5)
_تحقق("الأول الجديد = 10", قم.الأول() == 10)

متغير أزيل_خ = قم.أزل_أخيراً()
_تحقق("إزالة أخيراً = 30", أزيل_خ == 30)

# ═══ إلى مصفوفة ═══
اطبع_سطر("")
اطبع_سطر("── قائمة إلى مصفوفة ──")
متغير قم2 = جديد قائمة_مرتبطة()
قم2.أضف(1)
قم2.أضف(2)
قم2.أضف(3)
متغير مص2 = قم2.إلى_مصفوفة()
_تحقق("إلى مصفوفة: 3 عناصر", طول(مص2) == 3)
_تحقق("الأول = 1", مص2[0] == 1)
_تحقق("الأخير = 3", مص2[2] == 3)

متغير معكوسة = قم2.إلى_مصفوفة_معكوسة()
_تحقق("معكوسة: الأول = 3", معكوسة[0] == 3)
_تحقق("معكوسة: الأخير = 1", معكوسة[2] == 1)

# ═══ الخريطة المرتبة ═══
اطبع_سطر("")
اطبع_سطر("── خريطة_مرتبة ──")
متغير خم = جديد خريطة_مرتبة()
خم.عيّن("أ", 1)
خم.عيّن("ب", 2)
خم.عيّن("ج", 3)
_تحقق("الحجم = 3", خم.الحجم() == 3)
_تحقق("احصل أ", خم.احصل("أ") == 1)
_تحقق("يحتوي ب", خم.يحتوي("ب") == صحيح)
_تحقق("لا يحتوي د", خم.يحتوي("د") == خطأ)

خم.أزل("ب")
_تحقق("بعد إزالة ب: الحجم = 2", خم.الحجم() == 2)

متغير المفاتيح = خم.المفاتيح()
_تحقق("المفاتيح: 2 عناصر", طول(المفاتيح) == 2)

متغير القيم = خم.القيم()
_تحقق("القيم: 2 عناصر", طول(القيم) == 2)

# ═══ العداد ═══
اطبع_سطر("")
اطبع_سطر("── عداد ──")
متغير عد = جديد عداد()
عد.أضف("تفاح")
عد.أضف("برتقال")
عد.أضف("تفاح")
عد.أضف("تفاح")
عد.أضف("برتقال")
_تحقق("عدد تفاح = 3", عد.عدّ("تفاح") == 3)
_تحقق("عدد برتقال = 2", عد.عدّ("برتقال") == 2)
_تحقق("عدد موز = 0", عد.عدّ("موز") == 0)
_تحقق("الأكثر تكراراً = تفاح", عد.الأكثر_تكراراً() == "تفاح")
_تحقق("المجموع = 5", عد.المجموع() == 5)

متغير فريدة = عد.العناصر_الفريدة()
_تحقق("العناصر الفريدة = 2", طول(فريدة) == 2)

# ═══ المكدس المحدود ═══
اطبع_سطر("")
اطبع_سطر("── مكدس_محدود ──")
متغير مم = جديد مكدس_محدود(3)
_تحقق("مكدس محدود فارغ", مم.فارغ() == صحيح)

مم.ادفع(1)
مم.ادفع(2)
مم.ادفع(3)
_تحقق("ممتلئ بعد 3", مم.ممتلئ() == صحيح)
_تحقق("الحجم = 3", مم.الحجم() == 3)

# تجاوز الحد يرمي خطأ — نتخطاه
_تحقق("لا يتجاوز الحد", مم.ممتلئ() == صحيح)

متغير قمة = مم.اسحب()
_تحقق("سحب = 3 (آخر ناجح)", قمة == 3)

# ═══ التقرير ═══
اطبع_سطر("")
اطبع_سطر("═══════════════════════════════")
اطبع_سطر("النتائج: " + _نجح + " نجح، " + _فشل + " فشل")
إذا (_فشل == 0)
   اطبع_سطر("✓ جميع الاختبارات ناجحة!")
وإلا
   اطبع_سطر("✗ بعض الاختبارات فشلت")
نهاية
اطبع_سطر("═══════════════════════════════")
