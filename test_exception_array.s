# Test Array Index Out of Bounds Exception
# اختبار استثناء الفهرس خارج نطاق المصفوفة

اطبع("=== اختبار فهرس المصفوفة خارج النطاق ===");

# Create array
مصفوفة أرقام = [10, 20, 30, 40, 50] ;

# Test 1: Valid index access
اطبع("\n--- الاختبار 1: الوصول الصحيح للفهرس ---");
اطبع("العنصر [2] = ", أرقام[2]);

# Test 2: Index out of bounds (positive)
اطبع("\n--- الاختبار 2: فهرس أكبر من الحجم ---");

حاول 
    رقم قيمة = أرقام[10] ;
    اطبع("هذا لن يُطبع");
نهاية
امسك (استثناء) 
    اطبع("✅ تم اصطياد خطأ الفهرس: ", استثناء);
نهاية

# Test 3: Negative index
اطبع("\n--- الاختبار 3: فهرس سالب ---");

حاول 
    رقم قيمة = أرقام[-1] ;
    اطبع("هذا لن يُطبع");
نهاية
امسك (استثناء2) 
    اطبع("✅ تم اصطياد خطأ الفهرس السالب: ", استثناء2);
نهاية

# Test 4: Access continues after exception
اطبع("\n--- الاختبار 4: البرنامج يستمر بعد معالجة الخطأ ---");
اطبع("الوصول الطبيعي: أرقام[0] = ", أرقام[0]);

اطبع("\n✅ جميع اختبارات فهرس المصفوفة اكتملت بنجاح");
