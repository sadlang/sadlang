# اختبار مكتبة الرسومات الأساسية
# Basic graphics library test
دالة رئيسية ()
اطبع("==== اختبار مكتبة الرسومات ====\n");
اطبع("==== Graphics Library Test ====\n\n");

# اختبار إنشاء نافذة
اطبع("1. اختبار إنشاء نافذة...\n");
متغير نافذة = نافذة_جديد("تطبيق الرسومات", 800, 600);
اطبع("   تم إنشاء نافذة رقم: ");

اطبع("\n");

# اختبار إنشاء رسام
اطبع("\n2. اختبار إنشاء رسام...\n");
متغير رسام = رسام_ثنائي_جديد(نافذة);
اطبع("   تم إنشاء رسام رقم: ");
اطبع(رسام);
اطبع("\n");

# اختبار عمليات الرسم
اطبع("\n3. اختبار عمليات الرسم...\n");
رسام_بدء_إطار(رسام);
رسام_مسح(رسام, 30, 30, 40, 255);
رسام_خط(رسام, 100.0, 100.0, 200.0, 200.0, 255, 255, 255, 255);
رسام_مستطيل(رسام, 50.0, 50.0, 100.0, 80.0, 0, 255, 0, 255, صحيح);
رسام_دائرة(رسام, 400.0, 300.0, 50.0, 255, 0, 0, 255, صحيح);
رسام_نص(رسام, "مرحبا بالرسومات", 100.0, 400.0, 24.0, 255, 255, 0, 255);
رسام_إنهاء_إطار(رسام);
اطبع("   تم تنفيذ أوامر الرسم\n");

# اختبار إنشاء عناصر الواجهة
اطبع("\n4. اختبار عناصر الواجهة...\n");

# تسمية
متغير تسمية = تسمية_جديدة("نص التسمية", 10.0, 10.0, 200.0, 30.0);
اطبع("   تم إنشاء تسمية رقم: ");
اطبع(تسمية);
اطبع("\n");
اطبع("   نص التسمية: ");
اطبع(تسمية_الحصول_على_نص(تسمية));
اطبع("\n");

# زر
متغير زر = زر_جديد("اضغط هنا", 10.0, 50.0, 150.0, 40.0);
اطبع("   تم إنشاء زر رقم: ");
اطبع(زر);
اطبع("\n");
اطبع("   حالة النقر: ");
اطبع(زر_تم_النقر(زر));
اطبع("\n");

# حقل نص
متغير حقل_نص = حقل_نص_جديد("أدخل نصا", 10.0, 100.0, 200.0, 30.0);
اطبع("   تم إنشاء حقل نص رقم: ");
اطبع(حقل_نص);
اطبع("\n");
حقل_نص_تعيين_قيمة(حقل_نص, "نص تجريبي");
اطبع("   قيمة حقل النص: ");
اطبع(حقل_نص_الحصول_على_قيمة(حقل_نص));
اطبع("\n");

# مربع اختيار
متغير مربع_اختيار = مربع_اختيار_جديد("خيار 1", 10.0, 140.0, 150.0, 30.0);
اطبع("   تم إنشاء مربع اختيار رقم: ");
اطبع(مربع_اختيار);
اطبع("\n");
مربع_اختيار_تعيين_حالة(مربع_اختيار, صحيح);
اطبع("   حالة مربع الاختيار: ");
اطبع(مربع_اختيار_محدد(مربع_اختيار));
اطبع("\n");

# اختبار رسم عناصر الواجهة
اطبع("\n5. اختبار رسم عناصر الواجهة...\n");
رسام_بدء_إطار(رسام);
عنصر_رسم(تسمية, رسام);
عنصر_رسم(زر, رسام);
عنصر_رسم(حقل_نص, رسام);
عنصر_رسم(مربع_اختيار, رسام);
رسام_إنهاء_إطار(رسام);
اطبع("   تم رسم جميع العناصر\n");

# اختبار التحديث
اطبع("\n6. اختبار تحديث العناصر...\n");
عنصر_تحديث(تسمية, 0.016);
عنصر_تحديث(زر, 0.016);
اطبع("   تم تحديث العناصر\n");

# اختبار إدارة النافذة
اطبع("\n7. اختبار إدارة النافذة...\n");
نافذة_عرض(نافذة);
اطبع("   تم عرض النافذة\n");
نافذة_استقبال_أحداث(نافذة);
اطبع("   تم استقبال الأحداث\n");
نافذة_تبديل_buffers(نافذة);
اطبع("   تم تبديل البافرات\n");

اطبع("\n==== اكتمل الاختبار بنجاح! ====\n");
اطبع("==== Test Completed Successfully! ====\n");
نهاية 