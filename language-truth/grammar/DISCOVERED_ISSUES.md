# سجل المشاكل المكتشفة أثناء كتابة اختبارات المطابقة

> سجلّ **يُحرَّر يدوياً** لتتبّع ثغرات اللغة/سلوكياتها المكتشفة أثناء تأليف اختبارات
> `rules_matrix`. الغرض: فحصها وإصلاحها — لا تجنّبها. أيّ تباعد يظهر في
> [CONFORMANCE_REPORT.md](./CONFORMANCE_REPORT.md) يُضاف هنا.
>
> الحالة: `مفتوح` (يحتاج إصلاحاً) · `سلوك مقصود` (موثَّق، لا إصلاح) · `مُصلَح`.

| المعرّف | الوصف | أين اكتُشف | سلوك المفسر | سلوك المترجم | الخطورة | الحالة |
|---|---|---|---|---|---|---|
| ISSUE-054 | `حالة` بـ«افتراضي» فقط بلا أيّ «عندما» — المترجم لا يُنفّذ الذراع الافتراضيّ | تغطية gr.stmt.switch (edge) | يطبع «افتراضي» ✅ | كان يبني بلا مخرج ⇒ تباعد | متوسطة | **مُصلَح** (2026-07-04: [statement_main.cpp](../../compiler/src/frontend/builders/statement_main.cpp) — حلقة المقارنة لا تُنفَّذ حين لا فروع فتبقى كتلة المدخل بلا تفرّع للافتراضيّ ⇒ كودٌ ميت؛ أُضيف قفزٌ صريح `cases.empty() ⇒ br defaultLabel`. **تكافؤ مزدوج** — كلاهما «افتراضي») |
| ISSUE-055 | `ارجع` من داخل ذراع `حالة` (switch) لا يُرجع القيمة في المترجم | تغطية gr.stmt.switch (interaction) | يُرجع القيمة الصحيحة ✅ | كان يعيد 0 (الدالة تُستنتَج void) ⇒ تباعد | متوسطة | **مُصلَح** (2026-07-04: **ليس جذر ISSUE-054** — بل استنتاج نوع الإرجاع لا يفتّش داخل أذرع switch، فيُعلَن `define void @دالة` ويُقرأ الإرجاع 0. أُضيف SwitchStmt للدوال الثلاث: `hasReturnWithValue`+`findFirstReturnExpr` في [sir_builder_helpers.cpp](../../compiler/src/frontend/sir_builder_helpers.cpp) و`collectReturnTypes` في [template_infer_return.cpp](../../compiler/src/frontend/builders/template_infer_return.cpp). **تكافؤ مزدوج** — عدديّ 99/11 ونصّيّ) |
| ISSUE-056 | `ارجع` فارغ يليه جملة تعبير في السطر التالي — المحلّل النهِم يبتلع التعبير كقيمة إرجاع؛ **كان** المترجم يُنفّذ أثره الجانبيّ مرّتين | تغطية gr.stmt.return (edge) | يطبع `قبل\nبعد` (يُقيّم `اطبع_سطر("بعد")` مرّة كقيمة إرجاع) | كان يطبع `قبل\nبعد\nبعد` ⇒ تباعد | متوسطة | **مُصلَح** (2026-07-04: مسار FIX X06 في [statement_control_branch.cpp](../../compiler/src/frontend/builders/statement_control_branch.cpp) كان يبني تعبير الإرجاع لأثره الجانبيّ ثمّ لا يُطالب به حين يُرجع فراغاً (registerName فارغ) فيُعيد المسار العاديّ بناءه؛ أُضيفت علامة `voidSideEffectPrebuilt` تمنع إعادة البناء وتُعامله إرجاعاً فارغاً. **تكافؤ مزدوج مُثبَت** — `rules_matrix/…/gr.stmt.return/edge/052_empty_return_greedy.ص`. ملاحظة: البلع النهِم نفسه سلوك محلّل متّسق الآن بين المحرّكين؛ `ارجع` الفارغ الصريح يُوضَع آخر الكتلة قبل `نهاية`) |
| ISSUE-057 | **تباعد مفسر/مترجم:** قيمة منطقيّة تُمرَّر كوسيط دالة ثمّ تُحوَّل بـ`نص()` داخلها — المترجم يفقد نوعها في المعامل فيطبعها رقماً | تغطية gr.decl.arg_list (edge) | `نص(حالة)` ⇒ «صحيح»/«خطأ» ✅ | كان عبر معامل ⇒ «1»/«0» ⇒ تباعد | متوسطة | **مُصلَح** (2026-07-04: المعامل غير المُصرَّح يُخزَّن i64 عمداً — FIX X04 يمنع قصّ i64→i1 الذي يُفسد `لاشيء`؛ لكنّ `functionTable_` يعرف نوعه الدلاليّ Boolean. أُصلح في [expression_basic.cpp](../../compiler/src/frontend/builders/expression_basic.cpp) `buildVariableAccess`: يُبلَّغ Boolean في BuildResult للمعامل (ليُنسّقه `نص()` صحيحاً) **دون تغيير تخزين i64** فتبقى حماية null. **تكافؤ مزدوج** — عدديّ/منطقيّ/null/شرط جميعها سليمة؛ اختبار `…/gr.decl.parameters/edge/051_bool_param_to_string.ص`) |
| ISSUE-058 | **انهيار مترجم:** حقل صنفٍ **بلا مُهيّئ ولا باني** يُضبط خارجيّاً ثمّ يُقرأ ⇒ Segfault | تغطية gr.oop.class/field | يطبع القيمة ✅ | كان Segfault — الحقل غير المُصرَّح يُنمَّط Pointer فيفكّ عددًا كمؤشّر | عالية | **مُصلَح** (2026-07-04: الجذر محو النوع — Pointer الافتراضيّ يصلح للنصوص/الكائنات (مؤشّرات) ويكسر الأعداد. الحلّ الجذريّ: **مسح مسبق Phase 0.5** في [sir_builder_module.cpp](../../compiler/src/frontend/sir_builder_module.cpp) يجمع أنواع إسنادات كلّ حقل عبر البرنامج؛ إن اتّفقت على بدائيّ واحد يُستنتَج للحقل غير المُصرَّح بلا مُهيّئ في [class_main.cpp](../../compiler/src/frontend/builders/class_main.cpp) (وإلّا يبقى Pointer — آمن للتضارب/النصوص). **تكافؤ مزدوج**؛ عدد/نصّ/باني سليمة؛ اختبار `…/gr.oop.field/edge/051`) |
| ISSUE-059 | **فجوة مترجم:** حقل صنفٍ بمُهيّئ **نصّيّ** بلا باني ⇒ فشل ربط | تغطية gr.oop.field | يطبع النصّ ✅ | كان undefined symbol | عالية | **مُصلَح** (2026-07-04: الجذر — تهيئة الحقل النصّيّة كانت تستدعي `sad_string_new_cstr` **غير المعرَّفة** في الـruntime ⇒ `undefined symbol` (وخطأ msvcprt كان cascade من fallback clang). النصوص تُمثَّل مؤشّرَ char* خامًا (كالحرفيّات)؛ خُزِّن المؤشّر مباشرةً في [objects_arrays_ops.cpp](../../compiler/src/backend/llvm/builders/oop/objects_arrays_ops.cpp) و[mem_alloca.cpp](../../compiler/src/backend/llvm/builders/memory/mem_alloca.cpp). **تكافؤ مزدوج**؛ اختبار `…/gr.oop.field/basic/051`) |
| ISSUE-060 | **تباعد مفسر/مترجم:** `بنية` (struct) — الباني لا يُطبَّق والطرق تقرأ الحقول صفرًا في المترجم | تغطية gr.oop.struct (edge) | باني/طريقة يعملان (`ن(4).س`⇒4، `ن().ضعف()`⇒10) ✅ | كان يعيد 0 | عالية | **مُصلَح** (2026-07-04: الجذر — مسار بناء البنية في [statement_types.cpp](../../compiler/src/frontend/builders/statement_types.cpp) كان يبني الحقول فقط ويمرّر الباني/الطرق إلى `buildStatement` العامّ **بلا سياق صنف** (لا `currentClassName_`، لا ربط `هذا`/تخطيط الحقول، لا `بنية.طريقة`). الإصلاح: (1) تسجيل الصنف مبكرًا قبل بناء الأعضاء؛ (2) `buildClassConstructor` للباني عبر ClassDecl خفيف؛ (3) استُخرجت `buildClassMethod` من buildClass إلى [class_main.cpp](../../compiler/src/frontend/builders/class_main.cpp) وتُستدعى لطرق البنية بسياق الصنف الكامل. **تكافؤ مزدوج** — باني/طريقة/مركّب؛ اختبار `…/gr.oop.struct/edge/051`) |
| ISSUE-061 | **انهيار مترجم:** الباني يستدعي طريقة نسخة عبر `هذا.طريقة(...)` أثناء التهيئة | تغطية gr.oop.class (edge) | يعمل (`باني(ن) هذا.ق = هذا.ضعف(ن)` ⇒ 10) ✅ | **Segfault** (المترجم لا يوزّع نداء الطريقة على `هذا` داخل الباني) | عالية | **مفتوح** (كاشف: `grammar_gaps/باني_يستدعي_طريقة/001.ص`؛ يعمل: استدعاء الطريقة بعد الإنشاء) |
| ISSUE-001 | طريقة المصفوفة `.اضف(عنصر)` تفشل | تأليف gr.stmt.expression | يُخطئ (لا تُعرَّف الطريقة أو تختلف التسمية) | لم يُختبر بعد | متوسطة | **مفتوح** |
| ISSUE-002 | `مدى(أ، ب)` غير مضمَّنة تلقائياً | تأليف gr.stmt.for | يتطلب `استورد أساسيات` | كذلك | منخفضة | **مفتوح** (توثيق؟) |
| ISSUE-003 | ~~الأقواس اختيارية في `إذا`~~ **مصحَّح:** الأقواس **إلزامية** في `إذا` و`بينما` (consume) | تأليف gr.stmt.if (أُعيد التحقّق 2026-07-04) | `إذا 5>3 ... نهاية` بلا أقواس ⇒ **استرداد SYN003** (إدراج «(» و«)») وخروج **1** (لا قبول نظيف)؛ `بينما` بلا أقواس ⇒ تحليل منحرف (يبتلع أوّل جملة) وSEM001 | كذلك | منخفضة | **سلوك مقصود** (خطأ + استرداد؛ صُحِّح الادّعاء الأصليّ — الأقواس اختيارية نظيفة فقط في `حالة`/`طابق`) |
| ISSUE-004 | `ارجع` في المستوى الأعلى مسموح | تأليف gr.stmt.return | لا يُخطئ | لا يُخطئ | منخفضة | سلوك مقصود؟ (يُراجَع) |
| ISSUE-005 | كلمة محجوزة كاسم متغير (`متغير دالة = 1`) لا تُخطئ | تأليف gr.program.declaration | لا يُخطئ | — | متوسطة | **مفتوح** (يُراجَع) |
| ISSUE-006 | قوس إغلاق مفقود `اطبع_سطر((1+2)` لا يُعدّ خطأً | تأليف gr.stmt.expression | يتعافى ويُدرج `)` تلقائياً | — | — | سلوك مقصود (تعافٍ) |
| ISSUE-007 | طريقة النص `.تقسيم(فاصل)` تفشل | اختبار كاشف grammar_gaps | يُخطئ | لم يُختبر (فشل المفسر أولاً) | متوسطة | **مفتوح** |

| ISSUE-008 | طريقة المصفوفة `.اول()` تفشل (بينما `.آخر()` تعمل) | كاشف grammar_gaps | يُخطئ | — | متوسطة | **مفتوح** |
| ISSUE-009 | طريقة النص `.قص_أطراف()` تفشل | كاشف grammar_gaps | يُخطئ | — | متوسطة | **مفتوح** |
| ISSUE-010 | طرق الخريطة `.عيّن()/.احصل()` تفشل (الفهرسة `خ["ك"]` تعمل) | كاشف grammar_gaps | يُخطئ | — | متوسطة | **مفتوح** |
| ISSUE-012 | النشر `[...أ، 3]` في المصفوفة الحرفية غير مدعوم | كاشف grammar_gaps | خطأ نحوي «رمز غير متوقع '...'» | — | متوسطة | **مفتوح** |
| ISSUE-013 | **تباعد مفسر/مترجم:** `.احذف_اخير()` لا يحذف في المترجم | كاشف grammar_gaps (تباعد) | يحذف ✓ (`طول`=2) | **لا يحذف** (`طول`=3) — عملية فارغة | **عالية** | **مفتوح** |
| ISSUE-014 | تفكيك في التصريح `متغير أ، ب = 1، 2` غير مدعوم | كاشف grammar_gaps | يُخطئ «توقع اسم متغير بعد الفاصلة» | — | متوسطة | **مفتوح** |
| ISSUE-015 | طريقة النص `.استخراج(بداية، نهاية)` تفشل | كاشف grammar_gaps | يُخطئ | — | متوسطة | **مفتوح** |
| ISSUE-016 | **فجوة مترجم:** استيعاب القائمة `[..لكل..]` كان يفشل ربطه — codegen يُصدر `CALL __sad_array_push` **غير المعرَّف** + `ALLOC`/`LOAD` عامّة | مصفوفة القواعد TEST-008 (طبقة advanced) | يعمل ✓ | كان FAIL link | **عالية** | **مُصلَح** (2026-06-23: [expression_comprehensions.cpp](../../compiler/src/frontend/builders/expression_comprehensions.cpp) يستخدم `ARRAY_NEW`/`ARRAY_GET`/`ARRAY_APPEND`/`ARRAY_LEN` المُلوَّنة؛ **تكافؤ مزدوج مُثبَت** — مع شرط «إذا» أيضًا) |
| ISSUE-017 | **فجوة مترجم:** استيعاب الخريطة `{..لكل..}` كان يفشل ربطه — codegen يُصدر `CALL __sad_dict_set` **غير المعرَّف** | مصفوفة القواعد TEST-008 (طبقة advanced) | يعمل ✓ | كان FAIL link | **عالية** | **مُصلَح** (2026-06-23: يستخدم `__sad_map_create`/`__sad_map_set_typed` كالخريطة الحرفيّة؛ **تكافؤ مزدوج بمفاتيح نصّيّة** — المفاتيح العدديّة محجوزة بـISSUE-044) |
| ISSUE-018 | **فجوة مترجم:** `.مسطح()` لا يُترجَم | كاشف grammar_gaps | يعمل ✓ | **FAIL_COMPILE** | **عالية** | **مفتوح** |
| ISSUE-019 | استدعاء طريقة الأساس `الأساس.طريقة()` — كان `visitSuperExpr` يُرجع اسم الأب نصًّا فيُحسَب استدعاءً ثابتًا ويُرفض (RUN050) في المفسّر؛ وفي المترجم كان توزيع vtable يعود للابن ⇒ تكرار لانهائيّ وانهيار | كاشف grammar_gaps (كائني) | كان يُخطئ | كان ينهار | عالية | **مُصلَح مزدوجًا** (2026-06-23: المفسّر [expression_evaluator_oop.cpp](../../interpreter/src/visitors/expression_evaluator_oop.cpp) يكتشف معامل `الأساس` فيستبدله بـ«هذا» ويعيد توجيه البحث للأب؛ المترجم [call_method_dispatch.cpp](../../compiler/src/frontend/builders/call_method_dispatch.cpp) يوجّه className للأب **ويُجبر نداءً مباشرًا غير افتراضيّ** (isObjectCall=false) فلا يعود vtable للابن. **تكافؤ مزدوج مُثبَت** — 2، «صوت+مياو»، حقل=50؛ وتوزيع `هذا` الافتراضيّ سليم. اختبار `rules_matrix/30_oop/gr.oop.this_super/002`) |
| ISSUE-020 | الجنيسات `دالة هوية<ت>(..)` صياغة `<ت>` تفشل | كاشف grammar_gaps | يُخطئ (تعافٍ خاطئ) | — | متوسطة | **مفتوح** (صياغة؟) |
| ISSUE-021 | `غير_متزامن`/`انتظر` تُرجع `لاشيء` ثم تُخطئ | كاشف grammar_gaps | لاشيء + خطأ | — | عالية | **مفتوح** |
| ISSUE-025 | **فجوة مترجم:** دالة تُرجع خريطة + فهرسة | كاشف تصريحات | يُرجع 1 ✓ | **فراغ** (تباعد) | **عالية** | **مفتوح** |
| ISSUE-026 | **فجوة مترجم:** `صدّر بنية` لا يُترجَم | كاشف تصريحات | يقبل ✓ | **FAIL_COMPILE** | عالية | **مفتوح** |
| ISSUE-027 | الاسم المستعار `حساب` لرياضيات لا يُعرَّف | كاشف تصريحات | `استورد حساب` يُخطئ | — | منخفضة | **مفتوح** |
| ISSUE-028 | تظليل المتغير في الكتلة لا يعزل النطاق | كاشف تصريحات | `متغير` داخلي يطبع 2 ثم الخارجي 2 (لا 1) | — | متوسطة | **مفتوح** (قرار نطاق) |
| ISSUE-029 | تساهل: `دالة د` بلا أقواس مقبولة | كاشف تصريحات | لا يُخطئ | — | منخفضة | **مفتوح** |
| ISSUE-030 | `متغير ثابت` (صفة) لا يمنع إعادة الإسناد | كاشف تصريحات | يقبل `ص = 20` | — | متوسطة | **مفتوح** |
| ISSUE-031 | تركيب صفتين `متغير ثابت عام` يفشل | كاشف تصريحات | يُخطئ (بينما «متغير ساكن عام» يعمل) | — | متوسطة | **مفتوح** |
| ISSUE-032 | **فجوة مترجم:** `متغير ثابت/عام/خاص/ساكن ص` (صفة بلا نوع، مستوى أعلى) | كاشف تصريحات | يُرجع القيمة ✓ | **فراغ** (تباعد) — يعمل مع نوع أو داخل صنف | **عالية** | **مفتوح** |
| ISSUE-033 | **انجراف SoT↔مُحلِّل:** نمط البدائل `gr.pattern.or` (`عندما 1 \| 2 \| 3:`) كان المُحلِّل يقبل `OP_OR` (`\|\|`) فقط ويرفض `\|` المفردة | مصفوفة القواعد TEST-008 (طبقة patterns) | كان FAIL_INTERP | — | عالية | **مُصلَح** (2026-06-23: parsePattern يقبل OP_BITWISE_OR؛ تكافؤ مزدوج مُثبَت) |
| ISSUE-034 | **خلل دلالة المفسّر:** نمط البنية `gr.pattern.struct` لا يطابق — `StructPattern::matches` كان يستخدم `operator[]` (للخرائط، يرمي على الكائنات) | مصفوفة القواعد TEST-008 (طبقة patterns) | كان لا يطابق | — | عالية | **مُصلَح (المفسّر)** (2026-06-23: `Value::tryGetField` للوصول الكائنيّ؛ المفسّر يطبع 10) — راجع ISSUE-035 للمترجم |
| ISSUE-035 | **فجوة مترجم (codegen):** نمط البنية `gr.pattern.struct` — باني SIR كان يُصدر `CALL __sad_get_field` غير معرَّف (lld-link: undefined symbol) | مصفوفة القواعد TEST-008 | كان يعمل في المفسّر فقط | كان FAIL link | عالية | **مُصلَح** (2026-06-23: [sir_builder_match_patterns.cpp](../../compiler/src/frontend/sir_builder_match_patterns.cpp) يُصدر `LOAD(كائن، اسم الحقل)` وقت الترجمة كـ`buildMemberAccess`؛ **تكافؤ مزدوج مُثبَت** — كلا المحرّكين 10) |
| ISSUE-036 | **تباعد مترجم:** القيم الافتراضية لحقول البنية تُتجاهَل — `بنية نقطة رقم س = 7` ثم `نقطة().س` يُرجع **0** في المترجم بينما **7** في المفسّر (والباني الموضعيّ `نقطة(1،2)` كان FAIL_COMPILE) | كُشف أثناء إصلاح ISSUE-035 (طبقة patterns) | يُرجع الافتراضيّ 7 ✓ | كان يُرجع 0 (تباعد) | متوسطة | **مُصلَح** (2026-06-23: [expression_objects.cpp](../../compiler/src/frontend/builders/expression_objects.cpp) `buildNewObject` يُهيّئ الحقول من `structFieldDefaults_` بـ`STORE` بعد `ALLOC` كمسار إسناد العضو؛ + تجاوز الباني الوهميّ حين لا باني فعليّ. **تكافؤ مزدوج مُثبَت** — الأعداد الصحيحة/النصوص + الباني الموضعيّ + الإسناد فوق الافتراضيّ، كلّها 7/3·99/3) — راجع ISSUE-037 للحقل العشريّ |
| ISSUE-038 | **فجوة مترجم (codegen):** عامل الزيادة/الإنقاص `++`/`--` كان **عمليّة فارغة** في المترجم — `buildUnaryOp` يفتقد حالتَي `OP_INCREMENT`/`OP_DECREMENT` فيسقطان في default («غير مدعوم») وتُعاد القيمة دون تعديل | مصفوفة القواعد TEST-008 (طبقة expressions، gr.expr.unary/postfix) | يطبّقه (`س++`→6) | كان عمليّة فارغة (5) | عالية | **مُصلَح** (2026-06-23: [expression_basic.cpp](../../compiler/src/frontend/builders/expression_basic.cpp) `buildUnaryOp` يُصدر `ADD/SUB ±1` ثم `STORE` للمتغيّر ويُرجع القيمة الجديدة؛ **تكافؤ مزدوج مُثبَت** — `س++`=6، `س--`=4، `++س` تعبيرًا=6، عشريّ 3.5، في الحلقة 3) — راجع ISSUE-043 للبادئ في بداية الجملة |
| ISSUE-043 | **تعارض تحليليّ (محلّل مشترك):** بسبب عدم حساسية الأسطر، `++س`/`--س` في **بداية جملة** كان `++` يُلتقَط لاحقيًّا على تعبير الجملة السابقة (`اطبع_سطر(..)++` ⇒ TypeError)، فيتباعد المحرّكان | مصفوفة القواعد TEST-008 (أثناء إصلاح 038) | كان `لاشيء` | كان القيمة الجديدة | متوسطة | **مُصلَح** (2026-06-23: [parser_expressions.cpp](../../shared/parser/src/core/parser_expressions.cpp) `parsePostfix` لا يربط `++`/`--` اللاحق إلا إن كان على **سطر معاملِه نفسِه**؛ **تكافؤ مزدوج مُثبَت** — `++س` جملةً=6 حتى بعد استدعاء، و`س++` سليم) |
| ISSUE-039 | **انجراف اسم في المحلّل:** القيمة المطلقة `\|تعبير\|` كان المحلّل يحوّلها لاستدعاء `abs(...)` (لاتينيّ) وهو **غير مُسجَّل في المفسّر ومكسور في المترجم (يُرجع 0)**، بينما الدالة العربيّة `مطلق` تعمل في الاثنين | مصفوفة القواعد TEST-008 (طبقة expressions) | كان يُخطئ | كان 0 | متوسطة | **مُصلَح** (2026-06-23: [parser_expressions.cpp](../../shared/parser/src/core/parser_expressions.cpp) يحوّل `\|x\|` إلى `مطلق(x)`؛ **تكافؤ مزدوج مُثبَت** — `\|-5\|`=5، `\|0-8\|`=8، عشريّ 3.5) |
| ISSUE-044 | **تباعد مترجم:** المفاتيح **العدديّة** في الخرائط لا تُقرأ — `{1: 10، 2: 20}[2]` كان يُرجع فراغًا في المترجم (الخريطة تُخزّن المفاتيح كـ`char*` عبر `strdup` فالمفتاح العدديّ يُعامَل مؤشّرًا) | كُشف أثناء إصلاح ISSUE-017 | يُرجع 20 ✓ | كان فراغًا | متوسطة | **مُصلَح** (2026-06-23: تحويل المفتاح العدديّ/العشريّ/المنطقيّ إلى نص بـ`I64_TO_STRING` عند الإدراج [expression_collections.cpp](../../compiler/src/frontend/builders/expression_collections.cpp)+[expression_comprehensions.cpp](../../compiler/src/frontend/builders/expression_comprehensions.cpp) والقراءة [expression_index.cpp](../../compiler/src/frontend/builders/expression_index.cpp)؛ **تكافؤ مزدوج** — حرفيّ واستيعاب 20/20) |
| ISSUE-040 | **فجوة مشتركة:** المولّدات عبر `أنتج` (gr.adv.yield) غير منفَّذة — دالة تحوي `أنتج` تُخطئ في المفسّر وتفشل ترجمتها؛ صيغة `دالة* اسم()` تُرفَض | مصفوفة القواعد TEST-008 (طبقة advanced) | يُخطئ | FAIL_COMPILE | متوسطة | **مُصلَح/بائت** (2026-07-06، تحقّق حملة الملفّ 60: المولّدات **تعمل مزدوجًا** بصيغة `مولد دالة اسم()` + `أنتج قيمة` ثمّ `لكل ع في اسم()` — تكافؤ مزدوج مُثبَت في `rules_matrix/60_advanced/gr.adv.yield/{basic,edge}`؛ الصيغة القديمة `دالة*` لم تكن الصحيحة. **المتبقّي = ISSUE-073** (بارّ `أنتج` بالمستوى الأعلى في المترجم)) |
| ISSUE-041 | **فجوة مشتركة (تجريبيّ):** كتل الربط الأجنبيّ `خارجي "C" { دالة ... }` (gr.adv.ffi_extern_block/linkage/ctype) غير موصولة بمسار التحليل — المُحلّل الرئيسيّ يتوقّع `دالة` بعد `خارجي` مباشرةً ولا يفوّض لـExternParser | مصفوفة القواعد TEST-008 (طبقة advanced) | كان يُخطئ | كان FAIL | منخفضة (تجريبيّ) | **مُصلَح** (2026-06-25: [parser_main.cpp](../../shared/parser/src/core/parser_main.cpp) — عند `خارجي` متبوعًا بنصّ ربط بلا أقواس (`خارجي "C"`) يُحلَّل بصيغة كتلة: حلقة تقرأ تصاريح دوال خارجيّة (`دالة ...`) حتى `نهاية`، فتُلفّ في `BlockStmt`. كان المُحلّل يتوقّع `دالة` فورًا فيرفض الكتلة. **تكافؤ مزدوج مُثبَت** على المحرّكين (تصريحات خارجيّة لا تُستدعى ⇒ لا أثر، البرنامج يطبع «ok») — `_interactions/_generated/adv.ffi_extern_block__*`. تغيير في المحلّل المشترك ⇒ أُعيد بناء المحرّكين؛ انحدار كامل 1394/1394، 0 فشل. أكمل تغطية الأزواج إلى **166/166**) |
| ISSUE-042 | **تشابك تحميل-كسول + تحليل دلاليّ + رمز خروج:** دوال التأكيد (`تأكد`، `تأكد_صحيح`، `تحقق`) **لا تُحمَّل تلقائيًّا** خارج سياق خاصّ ⇒ استدعاؤها منفردًا يُعطي SEM004 ويُوقف التنفيذ (خروج 1)؛ وداخل `اختبر(...)` تعمل (يُطبَع `✓ اسم`) لكنّ العمليّة تخرج برمز **1 رغم النجاح**. (تشخيص مُنقَّح 2026-06-23: ليس إصلاح سطر واحد — يحتاج: تحميل وحدة التأكيدات تلقائيًّا أو تسجيلها في وحدة دائمة، + عدم رفع رمز الخروج عند نجاح كتل الاختبار. ملاحظة تضارب: `تحقق` مُسجَّل كاسم أمنيّ `SEC_0`) | مصفوفة القواعد TEST-008 (طبقة advanced) | ✓ + خروج 1 | لا يُنفّذ | متوسطة | **مفتوح** (كاشف `grammar_gaps/متقدمة/003_property_test_exit.ص`) |
| ISSUE-037 | **تباعد مترجم (سابق، مستقلّ):** حقل بنية **عشريّ** يُبتر إلى صحيح — `عشري معدل = 1.5` ثم `.معدل` يطبع **1** في المترجم بينما **1.5** في المفسّر؛ **يقع مع الإسناد الصريح `ك.معدل = 1.5` أيضًا** | كُشف أثناء إصلاح ISSUE-036 | يطبع 1.5 ✓ | كان يطبع 1 (بتر) | متوسطة | **مُصلَح** (2026-06-23: مسار LOAD للعضو كان يثبّت النوع `Integer` ويعالج Array/String فقط؛ أُضيف Float وBoolean في [expression_members.cpp](../../compiler/src/frontend/builders/expression_members.cpp) `buildExprMember` و[expression_objects.cpp](../../compiler/src/frontend/builders/expression_objects.cpp) `buildMemberAccess`. **تكافؤ مزدوج مُثبَت** — عشريّ افتراضيّ/مُسنَد 1.5·2.75، منطقيّ صحيح/خطأ؛ regression في `rules_matrix/30_oop/gr.oop.struct/basic/003,004`) |
| ISSUE-045 | **تباعد مترجم:** النصّ ذو البايت الصفريّ المُضمَّن `"\0"` يُبتَر عند أوّل `\0` (دلالة سلاسل C) — `طول("\0")` يُرجع **0** و`طول("ا\0ب")` يُرجع **1** في المترجم، بينما **1** و**3** في المفسّر (الذي يعدّ المحرف الصفريّ ضمن الطول) | مصفوفة القواعد TEST-008 (طبقة lexical، gr.lex.string/edge — حالات حدّية إضافية) | يعدّ `\0` محرفًا (طول=1·3) ✓ | يبتر عند `\0` (طول=0·1) | متوسطة | **مفتوح (معماريّ)** (المترجم يحتاج تمثيل نصّ بطول مُخزَّن لا منتهٍ بصفر؛ الاختبار `rules_matrix/70_lexical/gr.lex.string/edge/009_null_escape.ص` مُعلَّم `@skip_compiler` حتى الإصلاح. **تحقيق 2026-06-25:** `طول`→`emitFFIStrlen`→`__sad_utf8_strlen` يقف عند `\0`؛ جُرِّب طيٌّ ثابت لـ`طول(حرفيّ)` وقت الترجمة لكنّه فشل لأنّ قيمة الحرفيّ نفسها مبتورة عند `\0` في مسار المترجم (`token.getValue()`/`constantValue` تتوقّف عند البايت الصفريّ، فحتى الطيّ الثابت لا يستعيد الطول). الإصلاح الكامل يتطلّب حفظ البايتات الصفريّة المُضمَّنة عبر كامل المسار: المحلّل/الشجرة (قيمة النصّ) ← الثابت العامّ ← تمثيل نصّ بطول مخزَّن وقت التشغيل + جعل الطول/الوصل/الطباعة طول‑واعية — تغيير عبر‑طبقات عالي الخطر، مؤجَّل) |
| ISSUE-046 | **تباعد مترجم:** التجميع الفارغ `؟؟` مع معامل/نتيجة **منطقيّة** يتباعد — `صحيح ؟؟ خطأ` يُرجع **خطأ** (الأيمن) في المترجم بدل **صحيح** (الأيسر غير-الفارغ)؛ ويظهر حين تُغذّى نتيجة `؟؟` إلى `\|\|` مثل `(لاشيء ؟؟ صحيح) \|\| خطأ`. الأعداد/النصوص/لاشيء تعمل صحيحًا (`5 ؟؟ 9`=5، `لاشيء ؟؟ 7`=7) فالعلّة محصورة بالمنطقيّ | مصفوفة القواعد TEST-008 (أزواج expr: logical_or×null_coalesce) | يُرجع الأيسر غير-الفارغ ✓ | كان يُرجع الأيمن مع منطقيّ | متوسطة | **مُصلَح** (2026-06-24: [expression_nullsafety.cpp](../../compiler/src/frontend/builders/expression_nullsafety.cpp) `buildExprNullCoalesce` — ثلاثة إصلاحات: ① المعامل المنطقيّ يُرجَع مباشرةً (لا يكون لاشيء، ومقارنة i1 بحارس i64 كانت تبتر الحارس لبِتّه الأدنى=1)؛ ② الأيسر `Null` يعتمد نوع الأيمن (كـVoid/Unknown)؛ ③ فرع الأيمن MOVE يدعم `Boolean` بـ`ConstantBool` لا `stoll`. **تكافؤ مزدوج مُثبَت** للحالات الأربع + لا انحدار (1309/1309). رُفِع `@skip_compiler` عن الاختبارات الثلاثة) |
| ISSUE-047 | **فجوة مترجم:** الخريطة الحرفيّة **المتداخلة** مع فهرسة مزدوجة `{"خارج": {"داخل": 9}}["خارج"]["داخل"]` تُرجع **فراغًا** في المترجم بينما **9** في المفسّر | مصفوفة القواعد TEST-008 (أزواج expr: map_literal×primary) | يُرجع 9 ✓ | كان فراغًا | متوسطة | **مُصلَح** (2026-06-24: [expression_index.cpp](../../compiler/src/frontend/builders/expression_index.cpp) — الجذر تصنيفُ نوعٍ لا ABI: الـruntime يحفظ مؤشّر الخريطة المتداخلة سليمًا (typeTag=0 ⇒ `__sad_map_get` يُرجع inttoptr بلا نسخ)، لكن الاسترجاع كان يُرجِعه **نصًّا** فتفشل إعادة الفهرسة. أُضيف فرعٌ لـ`mapElemType ∈ {Map, Array, Struct}` يُعيد القيمة بنوعها المركّب (وelementType=Void للوصول الذكيّ التالي). **تكافؤ مزدوج مُثبَت** — `خ["أ"]["ب"]`=9·2؛ الخريطة المسطّحة سليمة. انحدار 1309/1309؛ رُفِع `@skip_compiler`) |
| ISSUE-048 | **فجوة مترجم:** الاستدعاء الفوريّ للامدا (IIFE) `(لامدا(س) => س * س)(5)` لا يُنتج خرجًا في المترجم بينما **25** في المفسّر (اللامدا المُسنَدة لمتغيّر ثمّ المُستدعاة تعمل مزدوجًا) | مصفوفة القواعد TEST-008 (أزواج expr: lambda×primary) | يُرجع 25 ✓ | كان فراغًا | منخفضة | **مُصلَح** (2026-06-24: [call_main.cpp](../../compiler/src/frontend/builders/call_main.cpp) `buildFunctionCall` — أُضيف فرعٌ لـ`callee` من نوع `LambdaExpr`: يبني اللامدا (`CLOSURE_CREATE`) ثم يُصدر `CLOSURE_CALL` نظيرَ مسار `IndexExpr`؛ كان يسقط في «else» غير المدعوم. **تكافؤ مزدوج مُثبَت** — مباشر/بلا وسائط/بوسيطين=25·99·7؛ رُفِع `@skip_compiler`) |
| ISSUE-049 | **فجوة مترجم:** بناء/قراءة **بنية (struct) داخل سياق طريقة أو باني صنف** يفشل ربطًا أو يتباعد: `ن().س` (بناء بنية) داخل دالة صنف ⇒ **فشل ربط**؛ قراءة حقل بنية مُمرَّرة `ب.ق` داخل باني ⇒ تباعد؛ إسناد قيمة تعداد لحقل في باني `هذا.ق = ط.واحد` ⇒ **فراغ**. البناء/الاستعمال في **المستوى الأعلى** أو تمرير البنية المبنيّة خارجًا لطريقة تقرأ حقلها يعمل مزدوجًا | مصفوفة القواعد TEST-008 (أزواج oop: class/method/constructor/struct/enum) | يبني/يقرأ صحيحًا ✓ | كان فشل ربط في سياق الطريقة | متوسطة | **مُصلَح** (2026-06-24: [sir_builder_module.cpp](../../compiler/src/frontend/sir_builder_module.cpp) — الجذر: المرحلة 2A تبني أجسام طرق الأصناف **قبل** تسجيل البُنى (StructDecl كان يُبنى لاحقًا في المرحلة 2) فلا يجد `getClass("ن")` نوعَها أثناء بناء الطريقة ⇒ يُصدَر نداء دالة غير معرَّف `@"ن"`. الحلّ: مرحلة **2A-0** تسجّل كل البُنى قبل بناء الأصناف، مع تخطّيها في المرحلة 2 اللاحقة (منع تسجيل مزدوج). **تكافؤ مزدوج مُثبَت** — بناء بنية داخل طريقة/باني، وتمريرها لطريقة. انحدار 1309/1309. رُفِع `@skip_compiler` عن اختبارَي `oop.class__oop.struct/003` و`oop.method__oop.struct/002`؛ بقي تباعدا الباني في ISSUE-050) |
| ISSUE-050 | **تباعد مترجم (سياق جسم الباني):** عمليّتان على الأعضاء داخل **جسم الباني** تتباعدان رغم إصلاح 049: ① قراءة حقل بنية مُمرَّرة كمعامل `باني(ب) هذا.ق = ب.ق` ⇒ فراغ بالمترجم؛ ② إسناد قيمة عضو تعداد لحقل `باني() هذا.ق = ط.واحد` ⇒ فراغ. (الإنشاء في الباني يعمل بعد 049؛ المتبقّي هو الوصول للعضو/التعداد في الباني) | مصفوفة القواعد TEST-008 (أزواج oop؛ كُشف أثناء إصلاح 049) | يقرأ/يُسنِد صحيحًا ✓ | كان فراغًا في جسم الباني | متوسطة | **مُصلَح** (2026-06-25: الجذر المشترك: استنتاج نوع الحقل في الباني كان يطمس النوع المُعلَن بـ`Pointer` الافتراضيّ حين يتعذّر استنتاج الطرف الأيمن، فتُكتب القيمة i64 صحيحًا لكنّ القراءة `كائن.ق` تُحمَّل ptr وتُطبَع %s. **②a (التعداد) — [class_constructor.cpp](../../compiler/src/frontend/builders/class_constructor.cpp):** ① لا نطمس حقلًا موجودًا إلا بنوعٍ مُستنتَجٍ مؤكَّد؛ ② `هذا.حقل = تعداد.عضو` يأخذ نوع العضو عبر `lookupVariable`/`staticFields_` (تعداد بسيط=Integer). **①b (قراءة حقل بنية من معامِل باني غير مُنوَّع) — استنتاج متعدٍّ:** أُضيفت خريطة `fieldFromParamMember_` (حقل ← معامِل.عضو) في [sir_module.h](../../compiler/include/frontend/sir_module.h)؛ تُسجَّل في الباني؛ ويحلّها Phase 2B في [sir_builder_module.cpp](../../compiler/src/frontend/sir_builder_module.cpp) من نوع عضو وسيط النداء (بنية/صنف) — مع تعميق مسح التعابير ليبلغ بناءً متداخلًا `م(د()).ق()` (كان لا يدخل MethodCall/Member). **تكافؤ مزدوج مُثبَت لكليهما** — `د().ق`=1، `م(د()).ق()`=4؛ رُفِع `@skip_compiler` عن `oop.class__oop.enum/003` و`oop.constructor__oop.struct/002`. انحدار كامل 1309/1309، 0 فشل) |
| ISSUE-051 | **تباعد مترجم:** التصدير على بنية/تعداد `صدّر بنية`/`صدّر تعداد` يتباعد — `صدّر بنية ن رقم ق=4 نهاية` ثمّ `ن().ق` يفشل/يُرجع فراغًا في المترجم، و`صدّر تعداد ل ... نهاية` ثمّ `ل.ب` يُرجع 0 بدل الفهرس. (`صدّر صنف`/`صدّر دالة`/`صدّر متغير` تعمل مزدوجًا.) | مصفوفة القواعد TEST-008 (أزواج decl×oop) | يُصدّر ويعمل ✓ | كان فراغًا/خطأ | منخفضة | **مُصلَح** (2026-06-25: [sir_builder_module.cpp](../../compiler/src/frontend/sir_builder_module.cpp) — معالِج `ExportDecl`/`ExportStmt` في المرحلة 2C كان يوجّه الدالة/الصنف الداخليَّين فقط ويُسقط البنية/التعداد في `else` (دفعهما كجملة تنفيذ بلا تسجيل نوع) ⇒ `ن().ق` يفشل و`ل.ب`=0. **الحلّ الكامل (يحاكي تصميم 049):** فكّ ExportDecl/ExportStmt في المرحلة **2A-0** لتسجيل البنية المُصدَّرة مبكرًا، وفي المرحلة **2A** لبناء التعداد المُصدَّر مبكرًا، ثمّ تخطّيهما في 2C تفاديًا للتسجيل المزدوج. **مراجعة وكيل (Amelia) كشفت** أنّ إصلاحًا أوّليًّا في 2C وحده يترك عرض 049 حيًّا للبنية المُصدَّرة المستعملة **داخل تابع صنف** (فشل ربط `@"ن"`) — فأُكمل بالتسجيل المبكر. **تكافؤ مزدوج مُثبَت** — توبلفل (بنية=4، تعداد=1) **وداخل تابع صنف** (بنية=7، تعداد=1)؛ الصنف/الدالة سليمان؛ انحدار كامل) |
| ISSUE-052 | **تباعد مترجم:** جمع عناصر **صفّ (tuple)** متعدّدة في تعبير حسابيّ `ص[0] + ص[1] + ص[2]` يُرجع قيمة قمامة (≈ الحارس -9223372036854775748) في المترجم بينما **60** في المفسّر؛ الفهرسة المفردة `ص[2]` تعمل مزدوجًا (=3)، والمصفوفات تعمل (`م[0]+م[1]`=30). فالعلّة محصورة بالوصول المتعدّد لعناصر الصفّ في الحساب | مصفوفة القواعد TEST-008 (combos دفعة 2) | يجمع صحيحًا (60) ✓ | قمامة (حارس)/خطأ نوع | متوسطة | **مؤجَّل — معماريّ** (2026-06-25: الجذر = مخطّط وسم الصفوف يحجز bit63 للنوع (1=عدد، 0=مؤشّر)، وهو **يصطدم ببت إشارة العدد** — العدد الموجب الموسوم والعدد السالب الخام لا يمكن التمييز بينهما قراءةً (كلاهما bit63=1). الحساب لا يفكّ الوسم أصلًا، فزوجٌ يعمل صدفةً (إلغاء وسمَين 2^63+2^63=0) وعددٌ فرديّ يُنتج قمامة. **مراجعة وكيل (Amelia) أثبتت** أنّ فكّ bit63 عند القراءة يُصلح الموجب لكنه يفسد السالب (`ص[0]` من `(-5,…)` ⇒ `صحيح`) ويتجاهل bit62 للمنطقيّ — فأُعيد الإصلاح (revert). الإصلاح الصحيح يتطلّب إعادة تصميم الوسم (نوع منفصل + قيمة 62-بتًّا بمدّ إشارة) عبر الكتابة والقراءة معًا، شبيهٌ بـ045. اختبار `b2_tuple_function_unpack` مُعلَّم `@skip_compiler`. **متبقٍّ مرتبط:** الصفّ كمعامِل دالة غير مُنوَّع يفقد نوع Tuple أيضًا (شبيه 050b)) |
| ISSUE-053 | **تباعد مترجم:** الإغلاق (لامدا) الذي يلتقط متغيّرًا محلّيًّا **قابلًا للتعديل** لا تدوم تعديلاته بين الاستدعاءات في المترجم — مصنع عدّاد `دالة عدّاد() متغير ق=0؛ ارجع لامدا()=>ق=ق+1` ثمّ ثلاث نداءات يُرجع **3** في المفسّر و**1** في المترجم. الالتقاط للقراءة فقط يعمل مزدوجًا (دفعة 2 `nested_closures`/`higher_order_return_lambda`). | مصفوفة القواعد TEST-008 (combos دفعة 3) | يحفظ الحالة (3) ✓ | يلتقط بالقيمة (1) | متوسطة | **مُصلَح** (2026-06-25: الجذر = `collectFreeVarsExpr` كان **يتجاهل تعبير الإسناد** (`AssignExpr`) عمدًا، فمتغيّر العدّاد المُسنَد إليه `ق` لا يُكتشَف متغيّرًا حرًّا ⇒ يُنشأ الإغلاق بـ`env=0` بلا التقاط ⇒ تُفقد الحالة. آليّة الكومة (env_load/env_store + تحديث alloca وENV_STORE معًا) كانت موجودةً سلفًا؛ نقصها فقط اكتشاف المتغيّر. **الحلّ:** نلتقط هدف الإسناد كمتغيّر حرّ في [template_type_inference.cpp](../../compiler/src/frontend/builders/template_type_inference.cpp) `collectFreeVarsExpr`، فتُخزَّن قيمته في env الكومة الدائم بـ`CLOSURE_CREATE` وتُكتب تعديلاته عبر `ENV_STORE` ⇒ تدوم الحالة. **حصر دقيق:** `collectFreeVars` مشتركة مع **defer** الذي يجب أن يُعدّل المتغيّر الخارجيّ عبر المكدّس المشترك لا بالقيمة؛ فحُصِر الالتقاط براية حالة `captureAssignTargetsInClosure_` ([sir_builder_context.h](../../compiler/include/frontend/sir_builder_context.h)) تُرفَع حول تحليل اللامدا فقط ([expression_functional.cpp](../../compiler/src/frontend/builders/expression_functional.cpp)) وتبقى مخفوضة لـdefer — وعلى مسار defer **لا نزور تعبير الإسناد إطلاقًا** (وإلّا التُقط `سجل` من الجانب الأيمن `سجل+"ج"` فانهار defer). **تكافؤ مزدوج مُثبَت** — العدّاد 3/3 وdefer أبج/أبج؛ رُفِع `@skip_compiler` عن `b3_closure_counter_state.ص`. انحدار كامل 1464/1464، 0 فشل، تكافؤ مزدوج 1462) |
| ISSUE-062 | **تباعد مترجم:** الوصول النقطيّ الرقميّ للصفّ (tuple) `ص.0`/`ص.1` يُرجع **0** في المترجم بينما القيمة الصحيحة (7/8) في المفسّر؛ الفهرسة بالأقواس `ص[0]` تعمل مزدوجًا. فالعلّة محصورة بلاحقة النقطة الرقميّة على الصفوف في codegen | حملة قواعد SoT (ملفّ 40، gr.expr.postfix، دفعة تغطية 2) | يُرجع 7/8 ✓ | كان يُرجع 0 (تباعد صامت) | متوسطة | **مُصلَح** (2026-07-05: المحلّل يحوّل `ص.0` إلى `MemberExpr` باسم عضوٍ رقميّ، فكان `buildExprMember` [expression_members.cpp](../../compiler/src/frontend/builders/expression_members.cpp) يُصدر `LOAD` حقلٍ باسمٍ نصّيّ «0» على بنية المصفوفة ⇒ 0. أُضيف فرعٌ مبكّر: إذا كان نوع الكائن `Tuple`/`Array` والعضو أرقامٌ فقط ⇒ يُخفَّض إلى `ARRAY_GET` بفهرسٍ عدديّ مجسَّد، نظير `ص[0]`. **تكافؤ مزدوج مُثبَت** — `ص.0`=7؛ كاشف `grammar_gaps/صف_وصول_نقطي/001`) |
| ISSUE-063 | **تباعد مترجم:** الشريحة المفتوحة النهاية `[ب..]` تُرجع **عنصرًا واحدًا فقط** `[20]` في المترجم بينما الذيل الكامل `[20, 30]` في المفسّر؛ الشرائح المغلقة `[0:2]`/`[1:3]` والخطوة `[0:5:2]` تعمل مزدوجًا. فالعلّة محصورة بحدّ النهاية المحذوف في codegen للشريحة | حملة قواعد SoT (ملفّ 40، gr.expr.postfix/range، دفعة تغطية 2) | يُرجع [20, 30] ✓ | كان يُرجع [20] | متوسطة | **مُصلَح** (2026-07-05: `buildExprSlice` [expression_nullsafety.cpp](../../compiler/src/frontend/builders/expression_nullsafety.cpp) كان يضع افتراض النهاية `-1` حين تُحذَف؛ لكنّ الخلفية `emitBuiltinArraySlice` تفسّر `-1` **فهرسًا سالبًا صريحًا** ⇒ `srcLen-1` فيقتطع الذيل لعنصر، بينما تفسّر `INT64_MIN` «حتى آخر المصفوفة». صُحِّح الافتراض إلى `std::numeric_limits<int64_t>::min()`. **تكافؤ مزدوج مُثبَت** — `[10،20،30][1..]`=[20, 30]؛ كاشف `grammar_gaps/شريحة_مفتوحة/001`) |
| ISSUE-066 | **فجوة مترجم:** صيغة الدالة الحرّة للشريحة `شريحة(م، بداية[، نهاية])` غير موصولة في المترجم ⇒ **فشل ربط** (رمز `شريحة` غير معرَّف)، حتى بصيغة مغلقة `شريحة(م،١،٣)`. الصيغة الطريقيّة `م.شريحة(...)` والفهرسة `م[أ..ب]` تعملان مزدوجًا | كشفتها مراجعة Amelia أثناء إصلاح ISSUE-063 | يُرجع الشريحة ✓ | فشل ربط | منخفضة | **مفتوح** (بانِي `Ba::SLICE` في [builtins_strings_arrays.cpp](../../compiler/src/frontend/builders/builtins_strings_arrays.cpp) موجودٌ ويدفع 3 معاملات صحيحة بعد الإصلاح لكنّه **غير مُستدعى** — نداء `شريحة(...)` الحرّ لا يُوجَّه إليه فيُخفَّض CALL عامًّا لرمزٍ غير معرَّف؛ كاشف `grammar_gaps/شريحة_دالة/001`) |
| ISSUE-065 | **تباعد مترجم (معماريّ):** السلسلة الآمنة `؟.` على **كائنٍ حقيقيّ** (غير فارغ) بحقلٍ **نصّيّ/عشريّ** — `كائن؟.اسم` (حقل نصّيّ) يطبع قيمة المؤشّر رقمًا و`كائن؟.كسر` (عشريّ) يُبتَر، لأنّ `buildExprOptionalChain` يُثبّت نوع `LOAD` وPHI على `Integer` متجاهلًا استنتاج نوع الحقل. مرتبط بتوحيد حارس العدم (i64) مع المؤشّرات في PHI | كشفتها مراجعة Amelia أثناء إصلاح ISSUE-064 (اختبارا `_regression/test_optional_null_coalesce/advanced.ص`) | يُرجع القيمة بنوعها الصحيح ✓ | يطبع مؤشّرًا رقمًا / يبتر العشريّ + أخطاء تحقّق PHI | متوسطة | **مفتوح (معماريّ)** (يتطلّب استنتاج نوع الحقل في فرع الوصول + توحيد نوع PHI بين قيمة العضو (قد تكون ptr/double) وحارس العدم (i64) — شبيه ISSUE-045/052؛ إصلاح ISSUE-064 عالج مسار «لاشيء» الساكن فقط) |
| ISSUE-064 | **تباعد مترجم:** السلسلة الآمنة `؟.` على **لاشيء** — `ك؟.س` حيث `ك=لاشيء` تُرجع **0** في المترجم بينما **لاشيء** في المفسّر (دلالة الوصول الآمن: قصر الدائرة إلى لاشيء) | حملة قواعد SoT (ملفّ 40، gr.expr.postfix، دفعة تغطية 2) | يُرجع لاشيء ✓ | كان يُرجع 0 | متوسطة | **مُصلَح** (2026-07-05: `buildExprOptionalChain` [expression_nullsafety.cpp](../../compiler/src/frontend/builders/expression_nullsafety.cpp) — ثلاثة إصلاحات: ① فحص العدم كان `obj != 0` بينما «لاشيء» = `kSadNullSentinel` (≠0) فيُؤخَذ فرع الوصول خطأً؛ صُحِّح إلى `!= kSadNullSentinel`؛ ② الفرع الفارغ كان يُرجع `0` (يُطبَع رقمًا) فصار يُرجع `kSadNullSentinel` (يطبعه مسار الطباعة «لاشيء»)؛ ③ حين يكون الكائن معروف العدم وقت الترجمة (نوعه `Null`) نقصر الدائرة مباشرةً دون توليد فرع الوصول الذي يتطلّب تخطيط صنف — كان يُنتج «No class mapping». **تكافؤ مزدوج مُثبَت** — `ك؟.س`=لاشيء؛ كاشف `grammar_gaps/سلسلة_آمنة_لاشيء/001`) |
| ISSUE-067 | **فجوة مترجم:** نمط القائمة **المتداخل** في `طابق` — `[[أ، ب]، [ج، د]]` يفكّك في المفسّر (=10) ويُرجع **0** في المترجم (المتغيّرات المتداخلة لا تُربَط)؛ نمط القائمة أحاديّ المستوى `[أ، ب]` يعمل مزدوجًا | حملة قواعد SoT (ملفّ 50، gr.pattern.list، دفعة تغطية) | يفكّك متداخلًا (=10) ✓ | يُرجع 0 (يتجاهل العناصر المتداخلة) | متوسطة | **مُصلَح** (2026-07-06، حلّ جذريّ): كان `buildMatchPatternCondition` [sir_builder_match_patterns.cpp](../../compiler/src/frontend/sir_builder_match_patterns.cpp) يحسب شرطًا **منطقيًّا مسطّحًا** ويعالج العناصر البسيطة فقط (حرفيّ/متغيّر/شامل) في حلقة `listPat->elements`؛ العنصر المركّب (`ListPattern`/`StructPattern`) يسقط دون فرع فلا يُربَط. الاستخراج المتداخل في كتلة الاختبار **غير آمن** (قراءة خارج الحدود تُجهِض المحرّكين حتى حين لا يطابق الطول). **الحلّ:** مُطابِق **قاصر الدائرة بالتفريع** `emitPatternMatchShortCircuit` — يفحص الطول/النوع ويتفرّع إلى `failLabel` (الحالة التالية) عند الفشل، ثمّ (بعد التحقّق ⇒ آمن) يستخرج الأبناء ويربطهم متعاوِدًا للأنماط المركّبة (قائمة داخل قائمة، قائمة داخل حقل بنية…). **يُفعَّل فقط حين وجود ابنٍ مركّب** (`patternHasCompositeChild`) فيبقى كلّ نمط أحاديّ المستوى على المسار المسطّح ⇒ صفر انحدار. **تكافؤ مزدوج مُثبَت** — `[[أ،ب]،[ج،د]]`=10، عدم تطابق متداخل ⇒ افتراضيّ بلا OOB، حرفيّ متداخل يشترط، `{ط:[أ،ب]}`=12؛ رُقِّيت الكواشف إلى `rules_matrix/50_patterns/.../edge` (003_nested/006_nested_literal/007…). **تحصين بعد نقد Amelia (2026-07-06):** المترجم بلا وسوم نوع تشغيليّة، فمطابقة نمطٍ مركّب على قيمة غير مطابقة النوع كانت تُصدر `ARRAY_LEN`/`instanceof` على عددٍ مُعامَل كمؤشّر ⇒ **Segfault** (وبنية بلا اسم نوع تطابق زورًا). أُضيفت **حراسة نوع ساكنة**: (أ) حارس أعلى — قيمةٌ قياديّة مؤكَّدة ⇒ فشلٌ فوريّ (افتراضيّ)؛ (ب) بوّابة عنصر — عنصرٌ نمطُه قائمة/بنية بينما نوع عنصر المصفوفة الساكن قياديّ ⇒ فشلٌ ساكن (قائمة ترفض القياديّ فقط تفاؤلًا للتداخل العميق؛ بنية ترفض القياديّ والمجهول تحفّظًا لمنع المطابقة الزائفة). مُمرَّر `matchValueElementType` من `matchResult.elementType`. **مُثبَت مزدوجًا:** `[[أ،ب]]` على `5`، `[[أ،ب]،ج]`/`[{..}،ج]` على `[1،2]`، `[[أ،ب]،{س:ج}]` على `[[1،2]،9]` ⇒ كلّها «افتراضيّ» بلا تحطّم (كواشف edge/008–011). **تحصين ثانٍ (بعد إعادة نقد Amelia التي كشفت 3 متّجهات تحطّم باقية):** البوّابة صارت **صارمة** — لا ننزل في ابنٍ مركّب إلّا بنوعٍ ساكنٍ مُثبَت (مصفوفة للقائمة، كائن للبنية)؛ المجهول (Void) ⇒ فشلٌ ساكن ⇒ **صفر تحطّم قطعًا**. الثمن: **التداخل المتباين النوع/العميق وحقل البنية المتداخل** يسقط للافتراضيّ في المترجم (لا يُطابَق) — سُجِّل **ISSUE-070**. **النطاق العامل المؤكَّد: نمط القائمة المتداخل المتجانس** (`[[أ،ب]،[ج،د]]` على `[[1،2]،[3،4]]`=10). انحدار كامل صفر إخفاق) |
| ISSUE-068 | **فجوة مترجم:** نمط الباقي `[أ، *بقية]` في `طابق` — يعمل في المفسّر (يربط الأوّل ويجمع الذيل، =1) ويفشل **الربط** في المترجم؛ الشريحة العاديّة `م[أ..]` (نفس `__sad_array_slice`) تربط بنجاح، فالعلّة محصورة بمسار توليد نمط الباقي داخل المطابقة | حملة قواعد SoT (ملفّ 50، gr.pattern.list، كشفتها مراجعة Amelia) | يربط الباقي (=1) ✓ | فشل ربط (مكتبات CRT مفقودة على LIB) | منخفضة | **مُصلَح** (2026-07-06، حلّ جذريّ): كان فرع `has_rest` في [sir_builder_match_patterns.cpp](../../compiler/src/frontend/sir_builder_match_patterns.cpp) يُصدر `CALL` لرمزٍ خارجيّ `__sad_array_slice` **لا يعرّفه أيّ runtime** ⇒ يفشل الرابط الأساسيّ `lld-link` بـ`undefined symbol` ثمّ يسقط المترجم إلى `clang` احتياطيًّا **بلا `/LIBPATH`** فتظهر أخطاء `msvcprt.lib…` **الزائفة** (لا علاقة لها بالـCRT). التشخيص الأوّل (حقن `.drectve`) كان مغلوطًا. **الجذر الحقيقيّ:** الشريحة العاديّة `م[أ..]` لا تنادي رمزًا بل تستخدم أوبكود `BUILTIN_ARRAY_SLICE` المُخفَّض inline في `emitBuiltinArraySlice`. **الحلّ:** توحيد نمط الباقي على الأوبكود نفسه (شريحة `[elements.size()..len]`). **تكافؤ مزدوج مُثبَت** — `[أ، *بقية]` على `[1،2،3،4]` ⇒ `بقية`=[2, 3, 4]؛ رُقِّي الكاشف إلى `rules_matrix/50_patterns/gr.pattern.list/edge/005_rest`) |
| ISSUE-069 | **~~تباعد مترجم~~ مُصلَح (حارس ذراع على نمط تفكيكيّ):** `عندما [أ، ب] إذا أ + ب > 25` كان يُرجع الذراع الصحيح في المفسّر («كبير») ويسقط للافتراضيّ في المترجم («صغير») لأنّ المسار المسطّح **يؤجّل** ربط عناصر القائمة (أ/ب) لكتلة الجسم، فلا تراها كتلة الحارس (تُقيَّم قبل الجسم بأعضاء = 0). **الإصلاح (مُطبَّق، موحَّد مع ISSUE-081):** استُخرِج منطق الاستخراج المؤجَّل (عناصر القائمة `ARRAY_GET` + حمولة التعداد `ENUM_GET_PAYLOAD`) في مُساعِد `emitDeferredExtractions` يُستدعى في **كتلة الحارس** (قبل `buildExpression(guard)`) وكتلة الجسم كليهما بنطاقٍ منفصل؛ كلتاهما تُبلَغ فقط بعد نجاح فحص المميّز/الطول (condReg=true) فالاستخراج آمن | كُشف أثناء إصلاح ISSUE-067 (اختبار حارس+قائمة)، أُصلح 2026-07-07 | يُقيّم الحارس بعناصر مربوطة ✓ | يُقيّمه بعناصر مربوطة ✓ (تكافؤ مزدوج) | — | **✅ مُغلَق** (اختبار مُرقّى من كاشف `حارس_نمط_تفكيكيّ` المحذوف: `rules_matrix/50_patterns/gr.pattern.list/edge/018_guard_destructure_reads_elems` + اختبار جديد مؤلَّف `019_guard_destructure_two_vars` [فرق العنصرين == 10 يشترط ربطهما معًا]) |
| ISSUE-070 | **حدّ معماريّ (مترجم بلا وسوم نوع تشغيليّة):** مطابقة الأنماط المركّبة المتداخلة على حاويات ص **الديناميّة** حيث يختلف النوع الساكن عن التشغيليّ. المفسّر (ديناميّ) يطابق بنيةً؛ المترجم (ساكن، بلا وسوم) لا يميّز مصفوفةً من عددٍ وقت التشغيل. بعد تحصين ISSUE-067 صار **آمنًا (يسقط للافتراضيّ بلا تحطّم)** لكن لا يُطابِق في: ① تداخل أعمق من عمق تتبّع النوع (`[[[س]]]` على مصفوفة عمقها 2)؛ ② مصفوفة مختلطة الأنواع يُستنتَج عنصرها `Void`؛ ③ **حقل بنية نمطُه مركّب** (`{ط:[أ،ب]}`) — النوع المُعلَن للحقل غير متاح لنمط بنيةٍ غير مُنوَّع | كشفتها إعادة نقد Amelia لإصلاح ISSUE-067 (2026-07-06) | يطابق بنيةً (مثلًا 12) ✓ | يسقط للافتراضيّ (آمن، لا تطابق) | منخفضة (نادر) | **مفتوح (معماريّ)** (الحلّ الجذريّ = وسوم نوع تشغيليّة / بدائيّات فحص آمنة النوع في الـABI — شبيه ISSUE-045/052؛ أو تتبّع النوع الساكن العميق كاملًا وقت الترجمة. كواشف: `grammar_gaps/نمط_حقل_بنية_متداخل/001` + متّجهات D1/D2/O6 في تقرير Amelia) |
| ISSUE-071 | **تباعد مترجم:** تخصيص القالب الصريح بوسيط **نوعٍ نصّيّ** `هوية<نص>("ن")` يُرجع `-39` (قمامة) في المترجم بينما `ن` في المفسّر؛ التخصيص بوسيط **رقم** `هوية<رقم>(4)` يعمل مزدوجًا. فالعلّة محصورة بتمرير قيمةٍ نصّيّة عبر قالبٍ ممحوّ النوع في المترجم | حملة قواعد SoT (ملفّ 60، gr.adv.template_args، دفعة تغطية 1) | يُرجع «ن» ✓ | يُرجع «-39» (تباعد صامت) | منخفضة | **مفتوح** (المفسّر يمحو النوع فيمرّر القيمة كما هي؛ المترجم مع القالب الممحوّ يعامل الوسيط النصّيّ معاملة عددٍ (يطبع مؤشّرًا/قمامة) — شبيه عائلة أخطاء تصنيف النوع في القوالب؛ كاشف `grammar_gaps/قالب_وسيط_نصّ/001`) |
| ISSUE-072 | **تباعد مترجم:** جملة `أطلق` (goroutine) **بسطرٍ واحد** `أطلق ق.أرسل(4)` تعمل في المفسّر (=4) ولا تُطلق في المترجم (خرجٌ فارغ)؛ صيغة **الكتلة** `أطلق ... نهاية` تعمل مزدوجًا | حملة قواعد SoT (ملفّ 60، gr.adv.go، دفعة تغطية 2) | يُطلق ويُرسل (=4) ✓ | لا يُطلق (خرج فارغ، تباعد صامت) | منخفضة | **مفتوح** (مسار `أطلق` أحاديّ-التعبير في codegen لا يُنشئ/يجدول goroutine كما تفعل صيغة الكتلة؛ الإصلاح = توحيد تخفيض الصيغتين؛ كاشف `grammar_gaps/أطلق_سطر_واحد/001`؛ **⚠️ سِبَاقيّ: قد يمرّ الكاشف تحت الحِمل المتوازي — لا يُرقَّى لبوّابة**) |
| ISSUE-073 | **علّة مترجم (رفض ناقص):** جملة `أنتج` **بارّة بالمستوى الأعلى** (خارج مولّد/دالة) — المفسّر يرفضها صحيحًا بـSEM014 (خروج=1)، لكنّ المترجم يطبع «LLVM CodeGen Error: التعليمة GENERATOR_YIELD وصلت بعدد معاملات غير متوقَّع» ثمّ **يخرج بـ0** (لا يرفض ولا يُنهي بخطأ) ⇒ نجاح صامت خاطئ | كشفتها مراجعة Amelia لحملة الملفّ 60 (اختبار gr.adv.yield/negative) | يرفض SEM014 (خروج 1) ✓ | خطأ codegen داخليّ ثمّ خروج 0 | متوسطة | **مفتوح** (التحليل الدلاليّ للمترجم لا يفرض arity/سياق `أنتج` قبل codegen؛ الإصلاح = فرض SEM014 في الواجهة الأماميّة كالمفسّر، أو إنهاء بخطأ عند خطأ codegen الداخليّ بدل الخروج 0؛ كاشف `grammar_gaps/أنتج_مستوى_أعلى/001`. **⚠️ فخّ harness: اختبارات `@expect_error` تُشغّل المفسّر وحده فتُخفي هذا — يلزم مسار يفرض رفض المترجم أيضًا**. **⚠️ تحديث 2026-07-07: بوّابة `if (llvm_codegen_->hasErrors())` في `compiler_driver_backend.cpp` تُصلح هذا الصنف كلّه، لكنّها كاشفة — جُرّبت فأظهرت 5 حالات مُقنَّعة في rules_matrix كانت تخرج 0 زائفًا: `006_equality_not_range` + `50_yield/{050,051}_{basic,edge}` (خطأ codegen داخليّ «No class mapping» في مسار المولّدات/yield). البوّابة أُجِّلت (تُراجَع عنها في فرع إصلاح 075/077) حتّى تُصلَح الخمس معًا؛ الإصلاح الكامل لـISSUE-073 = البوّابة + إصلاح الحالات الخمس المكشوفة معًا في PR واحد**) |
| ISSUE-074 | **تضارب SoT↔محلّل (يحتاج قرار مالك):** قاعدة `gr.adv.type` تُجيز لاحقة `؟` على النوع **عمومًا** (EBNF: `Type = TypeCore ['؟']`)، لكنّ المحلّل (المشترك) **يرفض** `؟` في موضع **بادئة المعامل** `رقم؟ س` بينما يقبلها في موضع **التعليق** `س: رقم؟`. كذلك النوع العامّ `مصفوفة<رقم> س` بادئةً يُرفَض. **ليس تباعدًا مزدوجًا** (المحلّل مشترك ⇒ المحرّكان يرفضان) | حملة قواعد SoT (ملفّ 60، gr.adv.type) + مراجعة Amelia | — | — | منخفضة | **مفتوح (قرار لغويّ)** (إمّا توضيح SoT صراحةً أنّ `؟`/العامّ صالحان في موضع التعليق فقط لا البادئة، أو الاعتراف بها فجوة محلّل وتوسيع القبول. **لم يُرسَّخ كاختبار سالب** تفاديًا لتثبيت قيدٍ غير محسوم — انظر [[report-failures-dont-avoid]]) |
| ISSUE-075 | **~~فجوة مترجم~~ مُصلَحة (نمط مركّب متداخل + `guard`):** `طابق(ز) عندما [[أ، ب]] إذا شرط:` — كان وجود `guard` يُعطّل توجيه قصر الدائرة (`patternFailLabel=""`) فيُسلَك المسار المسطّح الذي **يتجاهل الابن المركّب المتداخل صامتًا** ولا يربط متغيّراته؛ الذراع يبقى **حيًّا** ⇒ خطأ ترجمة «Undefined variable أ». **الإصلاح الجذريّ (مُطبَّق):** تمرير `patternFailLabel=nextLabel` **دائمًا** (لا `""` مع guard) فيُوجَّه `guard+مركّب` لقصر الدائرة الذي يربط متغيّرات النمط المتداخلة في كتلة النجاح المُهيمِنة على كتلتَي الحارس والجسم؛ فشل النمط بنيويًّا يتفرّع لـnextLabel متجاوزًا الحارس (صحيحٌ دلاليًّا — لا يُقيَّم حارسٌ لنمطٍ لم يطابق، المفسّر يوافق). +ربط صوريّ للحارس مقصورٌ على الذراع الميت ساكنًا (`matchArmStaticallyDead_`) نظير الجسم. الأنماط غير المركّبة لا تُفعِّل التوجيه ⇒ صفر انحدار | حلّ فجوتَي المترجم (نقدا Amelia العدائيّان، 2026-07-06) + إصلاح 075/077 (2026-07-07) | يفكّك ويطبع 10 ✓ | يفكّك ويطبع 10 ✓ (تكافؤ مزدوج) | — | **✅ مُغلَق** (اختبار موجب مُرقّى من الكاشف؛ حدّ باقٍ: `guard`+نمط OR يحوي مركّبًا [OR يبقى مسطّحًا] وتداخل أعمق من مستوًى [ISSUE-070]) |
| ISSUE-076 | **فجوة مترجم (حمولة ADT عشريّة):** إنشاء متغيّر تعداد جبريّ بحمولة **عشريّة** `نتيجة.نجاح(3.5)` — استخراج الحمولة مُصلَّبٌ على `Integer` (`ENUM_GET_PAYLOAD` + `statement_match.cpp`/`expression_objects.cpp`) فتُقرأ بتات `double` عددًا صحيحًا ⇒ **Segfault** وقت الاستخراج. **محجوبٌ الآن بخطأ ترجمةٍ واضح** في اعتراض بانِي ADT (`call_method_dispatch.cpp`) بدل فتح مسار التحطّم؛ الحمولة **الصحيحة والنصّيّة تعملان** مزدوجًا | حلّ فجوتَي المترجم (نقد Amelia العدائيّ، 2026-07-06) | يبني ويستخرج 3.5 ✓ | كان Segfault ⇐ الآن خطأ ترجمة صريح | متوسطة | **مفتوح** (يحتاج تتبّع أنواع حقول حمولة المتغيّرات في `adtEnumTable_`/`ADTVariantInfo` ثمّ تمريرها لـ`ENUM_GET_PAYLOAD` — عائلة «لا وسوم نوع تشغيليّة» ISSUE-070. حُجب لمنع تحطّم قابل للوصول لا لإخفاء الفجوة) |
| ISSUE-077 | **~~فجوة مترجم~~ مُصلَحة (وصول حقل مباشر على قيمة ADT مبنيّة):** `متغير ش = شكل.دائرة(7)` ثمّ `ش.نق` — كان المترجم يطبع «No class mapping for %ش» + خروج 0. **تصحيح الجذر (بعد #161):** نشر `className` **يعمل** (classInstanceTypes_["ش"]="شكل")؛ الجذر الفعليّ أنّ `ش.نق` = عقدة `MemberExpr` تُوجَّه إلى `buildExprMember` (`expression_members.cpp`) الذي **كان يفتقر لفرع ADT** فيبعث LOAD صنفٍ عامًّا، بينما الفرع موجود في المسار التوأم الخامل `buildMemberAccess` (`expression_objects.cpp`). **الإصلاح (مُطبَّق):** إضافة فرع ADT إلى `buildExprMember` (فحص `adtEnumTable_`+`findFieldIndex` ⇒ `ENUM_GET_PAYLOAD` مع operand[2]=اسم التعداد) + إصلاح operand[2] الناقص في المسار التوأم. **المطابقة تعمل مزدوجًا سلفًا** — والآن الوصول المباشر أيضًا | صار قابلًا للوصول بعد إصلاح بانِي ADT (2026-07-06) + إصلاح 075/077 (2026-07-07) | يطبع 7 ✓ | يطبع 7 ✓ (تكافؤ مزدوج) | — | **✅ مُغلَق** (اختبار موجب مُرقّى من الكاشف؛ **ملاحظة:** خروج 0 الصامت العامّ [صنف ISSUE-073] لم يعد يُصيب هذه الحالة [لا خطأ codegen بعد الإصلاح]؛ بوّابة الخروج العامّة أُجِّلت لـISSUE-073 لأنّها كاشفة لـ5 حالات yield/equality سابقة) |
| ISSUE-078 | **~~فجوة مترجم~~ مُصلَحة (نمط تعداد داخل قائمة بلا `guard`):** `طابق(م) عندما [نتيجة.نجاح(ق)]:` على `[نتيجة.نجاح(42)]` — كان المترجم يرمي ربط الحمولة المؤجّل للابن التعداديّ داخل قصر الدائرة (`handleChild` يمرّر متجهًا يُهمَل) بينما يبقى الذراع **حيًّا** ⇒ «ق» غير مربوط ⇒ تباعد صامت «ق=0» (ثمّ مرحلةٌ وسطى: خطأ ترجمة صاخب). **الإصلاح الجذريّ (مُطبَّق):** بعد `branchOnCond` (كتلة النجاح تُهيمن على الجسم) نستخرج كلّ حقلٍ من المتّجه المؤجّل `d` عبر `ENUM_GET_PAYLOAD` من **سجلّ العنصر** `childReg` ونربطه — نظير الاستخراج الأعلى في `buildMatchStatement`. الآن `ق=42` مزدوجًا. **⚠️مراجعة Amelia عدائيّة لهذا الإصلاح كشفت حاجبين أُصلحا:** الربط كان مشروطًا بـ`lookupVariable==null` ⇒ (أ) تظليل معاملٍ خارجيّ بنفس الاسم يتخطّى الاستخراج فيُقرأ المعامل (تباعد صامت «ق=قيمة المعامل»)، (ب) إعادة اسم الحمولة عبر ذراعين يُبقي سجلّ ذراعٍ سابق غير مُهيمِن ⇒ انهيار codegen. **الإصلاح: إزالة شرط التخطّي** (ربطٌ دائم + إعادة ربط لكلّ ذراع، نظير مسار متغيّرات القائمة المتداخلة القائم؛ الحلقة «لكلّ ذراع: شرط ثمّ جسم» تضمن الهيمنة) | حلّ فجوتَي المترجم (نقدا Amelia الثاني والثالث، 2026-07-06) | يفكّك ويطبع ق=42 ✓ | يفكّك ويطبع ق=42 ✓ (تكافؤ مزدوج، بما فيه التظليل وإعادة الاسم) | — | **✅ مُغلَق** (اختبارات موجبة: `edge/012_enum_in_list` + `013_enum_in_list_shadow_param` + `014_enum_in_list_reuse_name`؛ حدّ باقٍ: مصفوفة **داخل** حمولة التعداد تبقى فجوة عائلة ISSUE-070) |
| ISSUE-079 | **عمر متغيّر نمط المطابقة بعد الجملة — تباعد دلالة لغويّة (يحتاج قرار SoT/RFC، لا إصلاح مترجم أحاديّ):** استعمال اسمٍ مربوطٍ بنمطٍ **بعد** جملة المطابقة. **دلالة المفسّر (مرجع، لكنّها ثنائيّة):** (١) اسمٌ **طازج** (غير مُصرَّح قبلًا) ⇒ مقصورٌ على الذراع ⇒ **SEM001 «المتغير غير معرَّف»** بعد المطابقة (ثمّ لاشيء)؛ (٢) اسمٌ **يُظلّل متغيّرًا مُصرَّحًا سابقًا** (`متغير ق=100` ثمّ `عندما […نجاح(ق)]`) ⇒ المفسّر **يُعيد إسناد** الخارجيّ فيُبقيه (`بعد=42` لا 100). **المترجم في الحالة (٢):** «LLVM CodeGen Error: Undefined register %N» + `بعد=0` (السجلّ المربوط في كتلة الذراع لا يُهيمن على كتلة الدمج). **السؤال اللغويّ غير المحسوم:** هل نمطٌ باسمٍ موجودٍ **يُظلّل** (bind جديد مقصور) أم **يُعيد الإسناد** (كما المفسّر)؟ عابرٌ لكلّ أنماط الربط لا خاصّ بـADT | تحرٍّ أثناء تقييم اقتراح Amelia لـISSUE-078 (2026-07-06؛ فرضيّة «تسريب للنطاق المُحيط» صُحِّحت: المفسّر يقصر الطازج ويُعيد إسناد المُظلَّل) | (١) SEM001؛ (٢) يُعيد الإسناد `بعد=42` | (١) غير معرَّف؛ (٢) خطأ codegen داخليّ + `بعد=0` | منخفضة | **مفتوح (قرار لغويّ)** — لم يُطبَّق إصلاح مترجم: قصر النطاق للذراع (المحاولة الأولى) يُنتج `بعد=100` فيُباعد المفسّر (٢) لا يوافقه؛ التطابق الحقّ يلزمه حسمُ SoT لدلالة التظليل/إعادة-الإسناد ثمّ إمّا (أ) جعل نمطٍ باسمٍ موجودٍ يُعيد الإسناد لتخزين الخارجيّ، أو (ب) قصرٌ صريح + رفض الاستعمال بعد المطابقة في المحرّكين. **لم يُرسَّخ كاختبار سالب** تفاديًا لتثبيت سلوكٍ غير محسوم (انظر [[report-failures-dont-avoid]] و[[tools-cannot-expand-language-sot]]). **📄 تحقيق مُعمَّق (8 حالات حيّة، تقرير `تقارير_مؤقته/07.2026/06.07.2026/تقرير_تحقيق_ISSUE-079_...`):** المفسّر متّسق (جذر الإعادة-إسناد = `VariableManager::define` يصعد للأب = نفس جذر **ISSUE-028**)؛ **المترجم ثلاثيّ السلوك:** نمط بسيط **يتسرّب** (بعد=قيمة المُطابَق، تباعد جديد)، نمط ADT مباشر **مقصورٌ صحيحًا** (بعد=100، المعيار مُطبَّقًا)، قائمة/تعداد-داخل-قائمة **ينهار** (Undefined register + خروج 0 صامت). **التوصية: الخيار (أ) — قصر صارم للذراع بتظليل بلا إعادة إسناد + موت بعد الذراع في المحرّكين (يوافق Rust/ML/Swift، والمترجم يطبّقه سلفًا لـADT)، عبر RFC + بند SoT دلاليّ** (RFC مفتوح: sadlang/rfcs PR #33). **ملاحظة (إصلاح 075، 2026-07-07):** توجيه `guard+مركّب` لقصر الدائرة يربط الآن متغيّرات النمط المتداخلة في نطاق المطابقة الأساس (نظير مسار ISSUE-067 بلا-guard القائم)، فيوسّع اتّساق سلوك التسريب هذا لأذرع guard المركّبة أيضًا — يبقى محكومًا بقرار ISSUE-079 نفسه (لا سلوك جديد، اتّساقٌ فقط) |
| ISSUE-080 | **فجوة مترجم (وصول حقل ADT مباشر بلا توزيع حسب وسم الحالة):** الوصول المباشر `قيمة.حقل` على قيمة تعدادٍ جبريّ يستعمل `ADTEnumInfo::findFieldIndex` **الساكن الأحاديّ** الذي يُرجع فهرس **أوّل** حالة تحوي الاسم، دون فحص وسم الحالة وقت التشغيل. علّتان: **(أ) تصادم الاسم عبر الحالات** — `نق` في `دائرة`(فهرس0) و`مربع`(فهرس1)؛ `ص.نق` على `مربع` يطبع 7 بالمفسّر و**3** بالمترجم (يقرأ فهرس0). **(ب) وصول حقلٍ لحالةٍ خاطئة** — `ك.عرض` على `دائرة` (و`عرض` حقل `مستطيل` فقط) يرفع **RUN005 «المفتاح غير موجود»** بالمفسّر بينما المترجم يقرأ خانةً صامتة (=7 قمامة) دون تحقّق وسم. برز بعد إصلاح ISSUE-077 الذي جعل الوصول المباشر يعمل للحالة الشائعة (حقلٌ مفردٌ على الحالة الصحيحة، مُثبَت مزدوجًا) لكنّه ورث قصور `findFieldIndex` للحواف؛ قبل الإصلاح كانت الحالتان تُخطئان «No class mapping» | مراجعة Amelia العدائيّة لـPR #162 (2026-07-07) — تحقّق تجريبيّ مزدوج | (أ) 7 ✓ (ب) RUN005 ✓ | (أ) 3 صامت (ب) 7 صامت | متوسطة ((أ) منخفضة/نادرة؛ (ب) **قمامة صامتة بدل خطأ** ⇒ متوسطة بعُرف المستودع للتباعد الصامت — نظير ISSUE-013/025/032) | **مفتوح (معماريّ — عائلة ISSUE-070/076 «لا وسوم نوع تشغيليّة»)** (الحلّ الجذريّ: توزيع فهرس الحقل حسب وسم الحالة وقت التشغيل + بثّ `ENUM_IS_VARIANT` قبل `ENUM_GET_PAYLOAD` في الوصول المباشر لرفع خطأ يطابق RUN005 عند حالةٍ خاطئة. كواشف: `grammar_gaps/تعداد_تصادم_حقل/001_field_name_collision` + `002_wrong_variant_field`) |
| ISSUE-081 | **~~فجوة مترجم~~ مُصلَحة (حمولة تعداد غير مرئيّة في حارس الذراع):** `عندما شكل.دائرة(ق) إذا ق > 0` — كان المفسّر يطبع 7 (الحمولة `ق` متاحة للحارس)، بينما المترجم يؤجّل استخراج `ق` لكتلة الجسم فيبقى غير مربوطٍ عند تقييم الحارس ⇒ «Undefined variable 'ق'». **الإصلاح (مُطبَّق، موحَّد مع ISSUE-069):** المُساعِد `emitDeferredExtractions` يستخرج حمولة التعداد (`ENUM_GET_PAYLOAD`) في كتلة الحارس قبل تقييمه — نفس الجذر والحلّ كعناصر القائمة في 069. إصلاح ISSUE-075 عالج حرّاس أنماط **القوائم المتداخلة** `[[أ،ب]] إذا` فقط عبر قصر الدائرة؛ هذا يغطّي المسار المسطّح (حمولة تعداد مفردة + قائمة أحاديّة المستوى) | مراجعة Amelia العدائيّة لـPR #162 (2026-07-07)، أُصلح 2026-07-07 | يطبع 7 ✓ | يطبع 7 ✓ (تكافؤ مزدوج) | — | **✅ مُغلَق** (اختبار مُرقّى من كاشف `تعداد_حمولة_حارس` المحذوف: `rules_matrix/50_patterns/gr.pattern.primary/edge/016_guard_enum_payload` + اختبار جديد مؤلَّف `017_guard_enum_multifield` [فرق الحقلين == 5 يشترط ربطهما معًا]) |
| ISSUE-082 | **فجوة مترجم (قيمة نصّيّة/منطقيّة مُستخرَجة من نمطٍ تُصلَّب Integer فتفشل المقارنة النوعيّة):** `عندما رسالة.نص_م(ن): نص(ن == "مرحبا")` — `ENUM_GET_PAYLOAD` (حمولة التعداد) **و**`ARRAY_GET` (عنصر القائمة) كلاهما يُصلّب النوع `Integer` على القيمة المُستخرَجة، فمقارنة النصّ `ن == "مرحبا"` تُخفَّض كمقارنة أعداد (بتات المؤشّر) ⇒ المترجم=«خطأ»، المفسّر=«صحيح». **عابرٌ للجسم والحارس، وللحمولة وعنصر القائمة سواءً** (كشفتها مراجعة Amelia لإصلاح 069/081، لكنّها موروثة تصيب الجسم/بلا-حارس أيضًا). الطباعة المباشرة `اطبع_سطر(ن)` **تعمل** عبر علَم `__is_ptr` جانبيّ، لكنّ المقارنة/العمليّات النوعيّة لا | مراجعة Amelia العدائيّة لإصلاح ISSUE-069/081 (2026-07-07) — تحقّق تجريبيّ مزدوج (جسم + حارس، حمولة + عنصر قائمة) | «صحيح»/«مطابق» ✓ | «خطأ»/«افتراضي» (تباعد صامت) | متوسطة | **مفتوح (معماريّ — عائلة ISSUE-070/076/080 «لا وسوم نوع تشغيليّة»)** (الحلّ الجذريّ: تتبّع نوع القيمة المُستخرَجة [حقل الحمولة في `ADTVariantInfo`/`adtEnumTable_`، ونوع عنصر المصفوفة] وتمريره لـ`ENUM_GET_PAYLOAD`/`ARRAY_GET` ليُبلَّغ النوع الصحيح للمقارنة النازلة؛ نظير حلّ 076/080. كاشفان: `grammar_gaps/تعداد_حمولة_نصّيّة_مقارنة/001_string_payload_compare` [حمولة] + `قائمة_عنصر_نصّيّ_مقارنة/001_string_element_compare` [عنصر قائمة]) |
| ISSUE-083 | **فجوة مترجم (عنصر قائمة حرفيّ غير مفحوص في المسار المسطّح):** `عندما [1، أ]` على `[9، 5]` — المسار المسطّح لأنماط القوائم يطابق **بطول القائمة وحده**؛ العنصر الحرفيّ `1` يُؤجَّل كـ`kListPatternLiteralSentinel` **بلا مقارنة** (no-op في `emitDeferredExtractions`) فيطابق النمط زورًا (المترجم=«طابق») بينما المفسّر يرفض (1≠9 ⇒ «لا»). **مستقلّ عن الحارس** (سابق، يظهر بلا guard أيضًا) | مراجعة Amelia العدائيّة لإصلاح ISSUE-069/081 (2026-07-07) — تحقّق تجريبيّ مزدوج (بحارس وبلا) | «لا» ✓ | «طابق» (مطابقة زائفة صامتة) | متوسطة | **مفتوح** (الحلّ: مقارنة العنصر الحرفيّ المؤجّلة في كتلة الاختبار/النجاح — بثّ `EQ` بين العنصر المُستخرَج والقيمة الحرفيّة ودمجه في `condReg`، بدل الاكتفاء بفحص الطول؛ كاشف `grammar_gaps/قائمة_عنصر_حرفيّ_مسطّح/001_literal_element_unchecked`. TODO في `statement_match.cpp` فرع `kListPatternLiteralSentinel`) |

> **تحديث ISSUE-001:** طريقة `.الطول()` على المصفوفة **تعمل**؛ المشكلة محصورة في `.اضف()`.
> **ISSUE-013 مهمّة:** كشفتها المقارنة المزدوجة وحدها (تعمل في المفسر، تفشل صامتةً في المترجم) —
> هذا جوهر فحص «مفسر≡مترجم». فحص المفسر وحده كان سيخفيها.
> **عاملة مؤكَّدة مزدوجاً (متقدّمة):** لامدا، إغلاق، أنبوب `|>`، `.اختزل/.لكل`، tuple،
> `.شريحة`، فهرسة سالبة `[-1]`، نمط نطاق `1..10`، `.تحويل_صغير`، خريطة `.قيم/.حجم`.
> **عاملة مؤكَّدة مزدوجاً (OOP/تزامن/متقدّمة):** صنف+باني+طريقة+حقل، وراثة، بنية، تعداد
> (وبقيمة)، سمة+نفّذ، دالة ساكنة، تحميل عامل `+`، خاصية احصل/عيّن؛ قناة أرسل/استقبل،
> اختر، مجموعة_انتظار؛ أجّل، ماكرو، امتداد، عقد `يتطلب`/`يضمن`، توجيه `@حجم`.
> **عاملة مؤكَّدة (ليست ثغرات):** مصفوفة `.عكس/.رتب/.خريطة/.رشح/.فهرس/.يحتوي/.آخر`؛
> نص `.يبدأ_بـ/.ينتهي_بـ/.استبدل/.تحويل_كبير/.يحتوي`؛ خريطة حرفية وفهرسة وإسناد بالفهرس و`.مفاتيح`؛
> فهرسة متداخلة. (`$"..."` ليست صيغة f-string المعتمدة — تحتاج توضيح صياغة، لم تُدرَج كثغرة.)
> اختبارات كاشفة لكل ما سبق في `tests/behavior/grammar_gaps/` — نتائجها في قسم
> «اختبارات كاشفة للثغرات» بـ[CONFORMANCE_REPORT.md](./CONFORMANCE_REPORT.md) (5 تكشف ثغرة، 3 تعمل).

## ملاحظات

- ISSUE-001/005 مرشَّحة لاختبارات كاشفة في منطقة `_gaps` تُظهر الفشل صراحةً في قسم
  «التباعدات والإخفاقات» بتقرير المطابقة، ثم تُصلَح في طبقة المفسر/المترجم المناسبة.
- ISSUE-004/005 تحتاج قرار تصميم (هل هو سلوك مقصود أم ثغرة تساهل في المُحلِّل؟).
- التواريخ والأرقام تُحدَّث عند الإصلاح مع ربط الإيداع المُصلِح.
