{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://sadlang.org/schemas/v5/ui_vocab.schema.json",
  "title": "SadUI String Vocabulary Entry Schema",
  "description": "مخطّط مدخلة واحدة في مفردة SadUI نصّيّة (ui_animations/ui_easings/ui_events.yaml)",
  "type": "object",
  "required": ["id", "canonical", "description_ar", "description_en"],
  "additionalProperties": false,
  "comment_ar": "الأسماء القانونيّة العربيّة فقط بلا تشكيل — لا حقل aliases (additionalProperties:false يرفض أيّ اسمٍ بديل، إنجليزيًّا كان أو عربيًّا).",
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^[A-Za-z][A-Za-z0-9]*$",
      "description": "قيمة enum C++ المقابلة (مطابقة لتعريف الـenum في المكتبة)"
    },
    "canonical": {
      "type": "string",
      "minLength": 1,
      "description": "الاسم العربيّ القانونيّ بلا تشكيل (تُجرَّد المطابقةُ في المكتبة)"
    },
    "description_ar": { "type": "string", "minLength": 1 },
    "description_en": { "type": "string", "minLength": 1 }
  }
}
