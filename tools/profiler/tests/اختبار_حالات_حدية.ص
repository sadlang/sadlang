# ═══════════════════════════════════════════════════════════════════
# اختبار مصحح الأداء — حالات حدية واستثناءات
# Profiler Test — Edge cases and exceptions
# ═══════════════════════════════════════════════════════════════════

# === دالة فارغة ===
دالة فارغة()
نهاية

# === دالة عميقة الاستدعاء ===
دالة عد(ن)
    إذا (ن <= 0)
        ارجع 0
    نهاية
    ارجع 1 + عد(ن - 1)
نهاية

# === دوال متبادلة الاستدعاء ===
دالة زوجي(ن)
    إذا (ن == 0)
        ارجع صحيح
    نهاية
    ارجع فردي(ن - 1)
نهاية

دالة فردي(ن)
    إذا (ن == 0)
        ارجع خطأ
    نهاية
    ارجع زوجي(ن - 1)
نهاية

# === دالة مع حلقة ثقيلة ===
دالة حلقة_ثقيلة(تكرارات)
    متغير مجموع = 0
    متغير ع = 0
    بينما (ع < تكرارات)
        مجموع = مجموع + ع
        ع = ع + 1
    نهاية
    ارجع مجموع
نهاية

# === دالة مع عمليات نصية ===
دالة بناء_نص(طول_مطلوب)
    متغير النتيجة = ""
    متغير ع = 0
    بينما (ع < طول_مطلوب)
        النتيجة = النتيجة + "أ"
        ع = ع + 1
    نهاية
    ارجع النتيجة
نهاية

# === البرنامج الرئيسي ===
اطبع_سطر("═══ اختبار حالات حدية ═══")

# استدعاء دالة فارغة 200 مرة
متغير ع = 0
بينما (ع < 200)
    فارغة()
    ع = ع + 1
نهاية
اطبع_سطر("✓ 200 استدعاء لدالة فارغة")

# استدعاء تكراري عميق
متغير عمق = عد(30)
اطبع_سطر("✓ عمق الاستدعاء: " + عمق)

# استدعاء متبادل
اطبع_سطر("10 زوجي = " + زوجي(10))
اطبع_سطر("7 فردي = " + فردي(7))

# حلقة ثقيلة
متغير نتيجة = حلقة_ثقيلة(10000)
اطبع_سطر("✓ مجموع حلقة 10000: " + نتيجة)

# بناء نص
متغير نص = بناء_نص(100)
اطبع_سطر("✓ طول نص مبني: " + طول(نص))

# حاول/امسك
حاول
    متغير س = 1 / 0
امسك (خطأ)
    اطبع_سطر("✓ تم التعامل مع خطأ القسمة على صفر")
نهاية

اطبع_سطر("═══ انتهى اختبار حالات حدية ═══")
