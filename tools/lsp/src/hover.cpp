// بسم الله الرحمن الرحيم
/**
 * ═══════════════════════════════════════════════════════════════════════════════
 * ملف: hover.cpp
 * الوصف: مزود معلومات التمرير للغة ص - Hover Information Provider
 * المرحلة: Phase 8 - US6 - أدوات التطوير
 * المهمة: T095
 * ═══════════════════════════════════════════════════════════════════════════════
 * 
 * هذا الملف يوفر معلومات التمرير (Hover) عند وضع المؤشر فوق عنصر في الكود.
 * 
 * ╔═══════════════════════════════════════════════════════════════════════════╗
 * ║  ما هي معلومات التمرير؟                                                  ║
 * ╠═══════════════════════════════════════════════════════════════════════════╣
 * ║  عندما يضع المطور مؤشر الفأرة فوق عنصر في الكود، تظهر نافذة            ║
 * ║  صغيرة تحتوي على معلومات مفيدة مثل:                                      ║
 * ║  - نوع المتغير                                                           ║
 * ║  - توقيع الدالة ووصفها                                                   ║
 * ║  - توثيق الصنف أو الوحدة                                                 ║
 * ║  - أمثلة على الاستخدام                                                   ║
 * ╚═══════════════════════════════════════════════════════════════════════════╝
 * 
 * ═══════════════════════════════════════════════════════════════════════════════
 * 
 * مثال على معلومات التمرير:
 * 
 * عند التمرير فوق "اطبع":
 * ┌────────────────────────────────────────────────────────────────────────────┐
 * │ دالة اطبع(قيمة: كائن) -> فارغ                                             │
 * │                                                                            │
 * │ طباعة قيمة على الشاشة.                                                    │
 * │                                                                            │
 * │ المعلمات:                                                                  │
 * │   قيمة - القيمة المراد طباعتها (أي نوع)                                   │
 * │                                                                            │
 * │ مثال:                                                                      │
 * │   اطبع("مرحباً بالعالم")                                                   │
 * │   اطبع(42)                                                                 │
 * └────────────────────────────────────────────────────────────────────────────┘
 * 
 * ═══════════════════════════════════════════════════════════════════════════════
 * 
 * @author فريق لغة ص
 * @date يناير 2026
 * @version 1.0.0
 * 
 * الحمد لله رب العالمين
 */

#include "hover.hpp"
#include "../lsp_types.h"

#include <unordered_map>
#include <sstream>
#include <regex>

namespace sad {
namespace lsp {

// ═══════════════════════════════════════════════════════════════════════════════
// توثيق الدوال المدمجة - Built-in Functions Documentation
// ═══════════════════════════════════════════════════════════════════════════════

/**
 * @brief معلومات دالة مدمجة
 */
struct BuiltinInfo {
    std::string signature;       // توقيع الدالة
    std::string description;     // الوصف
    std::string parameters;      // المعلمات
    std::string returns;         // القيمة المرجعة
    std::string example;         // مثال
};

/**
 * قاموس الدوال المدمجة مع توثيقها
 */
const std::unordered_map<std::string, BuiltinInfo> BUILTIN_DOCS = {
    // الإدخال والإخراج
    {"اطبع", {
        "دالة اطبع(قيمة: كائن) -> فارغ",
        "طباعة قيمة على الشاشة بدون سطر جديد.",
        "قيمة - القيمة المراد طباعتها (أي نوع)",
        "لا شيء",
        R"(اطبع("مرحباً ")
اطبع("بالعالم")
// الناتج: مرحباً بالعالم)"
    }},
    {"اطبع_سطر", {
        "دالة اطبع_سطر(قيمة: كائن) -> فارغ",
        "طباعة قيمة على الشاشة مع سطر جديد.",
        "قيمة - القيمة المراد طباعتها (أي نوع)",
        "لا شيء",
        R"(اطبع_سطر("مرحباً")
اطبع_سطر("بالعالم")
// الناتج:
// مرحباً
// بالعالم)"
    }},
    {"ادخل", {
        "دالة ادخل(رسالة: نص = \"\") -> نص",
        "قراءة سطر من المستخدم.",
        "رسالة - رسالة اختيارية تُعرض قبل الإدخال",
        "نص - النص الذي أدخله المستخدم",
        R"(نص اسم = ادخل("أدخل اسمك: ")
اطبع_سطر("مرحباً " + اسم))"
    }},
    
    // التحويلات
    {"إلى_رقم", {
        "دالة إلى_رقم(قيمة: كائن) -> رقم",
        "تحويل قيمة إلى رقم.",
        "قيمة - القيمة المراد تحويلها",
        "رقم - القيمة كرقم",
        R"(رقم س = إلى_رقم("42")  // 42
رقم ص = إلى_رقم(صحيح)   // 1)"
    }},
    {"إلى_نص", {
        "دالة إلى_نص(قيمة: كائن) -> نص",
        "تحويل قيمة إلى نص.",
        "قيمة - القيمة المراد تحويلها",
        "نص - القيمة كنص",
        R"(نص س = إلى_نص(42)      // "42"
نص ص = إلى_نص(صحيح)   // "صحيح")"
    }},
    
    // العمليات على القوائم
    {"طول", {
        "دالة طول(مجموعة: قائمة|نص|خريطة) -> رقم",
        "الحصول على عدد العناصر في قائمة أو أحرف في نص أو أزواج في خريطة.",
        "مجموعة - القائمة أو النص أو الخريطة",
        "رقم - عدد العناصر",
        R"(رقم ط = طول([1, 2, 3])    // 3
رقم ن = طول("مرحباً")     // 6)"
    }},
    {"فارغة", {
        "دالة فارغة(مجموعة: قائمة|نص|خريطة) -> منطقي",
        "التحقق من أن المجموعة فارغة.",
        "مجموعة - القائمة أو النص أو الخريطة",
        "منطقي - صحيح إذا كانت فارغة",
        R"(فارغة([])        // صحيح
فارغة([1, 2])   // خطأ
فارغة("")       // صحيح)"
    }},
    {"أضف", {
        "دالة أضف(قائمة: قائمة, عنصر: كائن) -> فارغ",
        "إضافة عنصر إلى نهاية القائمة.",
        "قائمة - القائمة المراد الإضافة إليها\nعنصر - العنصر المراد إضافته",
        "لا شيء (تعديل القائمة مباشرة)",
        R"(قائمة أرقام = [1, 2]
أضف(أرقام, 3)  // [1, 2, 3])"
    }},
    {"احذف", {
        "دالة احذف(قائمة: قائمة, فهرس: رقم) -> كائن",
        "حذف عنصر من القائمة بالفهرس وإرجاعه.",
        "قائمة - القائمة المراد الحذف منها\nفهرس - موقع العنصر (يبدأ من 0)",
        "كائن - العنصر المحذوف",
        R"(قائمة أرقام = [1, 2, 3]
رقم محذوف = احذف(أرقام, 1)  // 2
// أرقام الآن: [1, 3])"
    }},
    
    // الرياضيات
    {"جذر", {
        "دالة جذر(رقم: رقم) -> رقم",
        "حساب الجذر التربيعي.",
        "رقم - الرقم المراد حساب جذره",
        "رقم - الجذر التربيعي",
        R"(رقم ج = جذر(16)  // 4
رقم ج = جذر(2)   // 1.414...)"
    }},
    {"قوة", {
        "دالة قوة(أساس: رقم, أس: رقم) -> رقم",
        "رفع رقم لقوة.",
        "أساس - الرقم الأساسي\nأس - القوة",
        "رقم - النتيجة",
        R"(رقم ق = قوة(2, 8)   // 256
رقم ق = قوة(10, 3)  // 1000)"
    }},
    {"مطلق", {
        "دالة مطلق(رقم: رقم) -> رقم",
        "الحصول على القيمة المطلقة.",
        "رقم - الرقم",
        "رقم - القيمة المطلقة",
        R"(رقم م = مطلق(-5)   // 5
رقم م = مطلق(5)    // 5)"
    }},
    {"أقصى", {
        "دالة أقصى(أ: رقم, ب: رقم) -> رقم",
        "الحصول على القيمة الأكبر.",
        "أ - الرقم الأول\nب - الرقم الثاني",
        "رقم - القيمة الأكبر",
        R"(رقم م = أقصى(5, 10)  // 10)"
    }},
    {"أدنى", {
        "دالة أدنى(أ: رقم, ب: رقم) -> رقم",
        "الحصول على القيمة الأصغر.",
        "أ - الرقم الأول\nب - الرقم الثاني",
        "رقم - القيمة الأصغر",
        R"(رقم م = أدنى(5, 10)  // 5)"
    }},
    {"عشوائي", {
        "دالة عشوائي(من: رقم = 0, إلى: رقم = 1) -> رقم",
        "توليد رقم عشوائي.",
        "من - الحد الأدنى (شامل)\nإلى - الحد الأقصى (غير شامل)",
        "رقم - رقم عشوائي",
        R"(رقم ع = عشوائي(1, 100)  // رقم بين 1 و 99)"
    }},
    
    // النصوص
    {"قسّم", {
        "دالة قسّم(نص: نص, فاصل: نص) -> قائمة[نص]",
        "تقسيم نص إلى قائمة باستخدام فاصل.",
        "نص - النص المراد تقسيمه\nفاصل - الفاصل",
        "قائمة[نص] - قائمة الأجزاء",
        R"(قائمة أجزاء = قسّم("أ,ب,ج", ",")  // ["أ", "ب", "ج"])"
    }},
    {"اربط", {
        "دالة اربط(قائمة: قائمة[نص], فاصل: نص) -> نص",
        "ربط قائمة نصوص إلى نص واحد.",
        "قائمة - قائمة النصوص\nفاصل - الفاصل بين العناصر",
        "نص - النص المربوط",
        R"(نص م = اربط(["أ", "ب", "ج"], "-")  // "أ-ب-ج")"
    }},
    {"استبدل", {
        "دالة استبدل(نص: نص, قديم: نص, جديد: نص) -> نص",
        "استبدال جميع تكرارات نص فرعي.",
        "نص - النص الأصلي\nقديم - النص المراد استبداله\nجديد - النص البديل",
        "نص - النص بعد الاستبدال",
        R"(نص ن = استبدل("مرحبا عالم", "عالم", "ص")  // "مرحبا ص")"
    }},
    
    // الملفات
    {"اقرأ_ملف", {
        "دالة اقرأ_ملف(مسار: نص) -> نص",
        "قراءة محتوى ملف نصي.",
        "مسار - مسار الملف",
        "نص - محتوى الملف",
        R"(نص محتوى = اقرأ_ملف("بيانات.txt"))"
    }},
    {"اكتب_ملف", {
        "دالة اكتب_ملف(مسار: نص, محتوى: نص) -> منطقي",
        "كتابة محتوى لملف نصي.",
        "مسار - مسار الملف\nمحتوى - المحتوى المراد كتابته",
        "منطقي - صحيح إذا نجحت الكتابة",
        R"(اكتب_ملف("ناتج.txt", "مرحباً"))"
    }},
    {"ملف_موجود", {
        "دالة ملف_موجود(مسار: نص) -> منطقي",
        "التحقق من وجود ملف.",
        "مسار - مسار الملف",
        "منطقي - صحيح إذا كان الملف موجوداً",
        R"(إذا (ملف_موجود("بيانات.txt"))
    // الملف موجود
نهاية)"
    }},
    
    // النوع
    {"نوع", {
        "دالة نوع(قيمة: كائن) -> نص",
        "الحصول على نوع القيمة كنص.",
        "قيمة - القيمة المراد معرفة نوعها",
        "نص - اسم النوع",
        R"(نص ن = نوع(42)       // "رقم"
نص ن = نوع("مرحباً")  // "نص"
نص ن = نوع([1,2,3])  // "قائمة")"
    }},
};

/**
 * قاموس الكلمات المفتاحية مع توثيقها
 */
const std::unordered_map<std::string, std::string> KEYWORD_DOCS = {
    {"إذا", 
R"(**إذا** - جملة شرطية

تنفذ الكود إذا كان الشرط صحيحاً.

```ص
إذا (شرط)
    // كود يُنفذ إذا كان الشرط صحيحاً
نهاية
```

مثال:
```ص
رقم عمر = 18
إذا (عمر >= 18)
    اطبع_سطر("بالغ")
نهاية
```)"},

    {"وإلا", 
R"(**وإلا** - الفرع البديل

يُنفذ إذا كان الشرط في `إذا` خاطئاً.

```ص
إذا (شرط)
    // إذا صحيح
وإلا
    // إذا خطأ
نهاية
```)"},

    {"وإلا_إذا", 
R"(**وإلا_إذا** - شرط إضافي

يُفحص إذا كان الشرط السابق خاطئاً.

```ص
إذا (شرط1)
    // ...
وإلا_إذا (شرط2)
    // ...
وإلا
    // ...
نهاية
```)"},

    {"طالما", 
R"(**طالما** - حلقة شرطية

تكرر الكود طالما الشرط صحيح.

```ص
طالما (شرط)
    // كود يتكرر
نهاية
```

مثال:
```ص
رقم ع = 0
طالما (ع < 5)
    اطبع_سطر(ع)
    ع = ع + 1
نهاية
```)"},

    {"لكل", 
R"(**لكل** - حلقة على المجموعات

تمر على كل عنصر في قائمة أو مجموعة.

```ص
لكل عنصر في مجموعة
    // معالجة العنصر
نهاية
```

مثال:
```ص
قائمة أسماء = ["أحمد", "سارة", "علي"]
لكل اسم في أسماء
    اطبع_سطر("مرحباً " + اسم)
نهاية
```)"},

    {"دالة", 
R"(**دالة** - تعريف دالة

تعريف وحدة كود قابلة لإعادة الاستخدام.

```ص
دالة اسم_الدالة(معلمة1: نوع1, معلمة2: نوع2) -> نوع_الإرجاع
    // جسم الدالة
    إرجاع قيمة
نهاية
```

مثال:
```ص
دالة مجموع(أ: رقم, ب: رقم) -> رقم
    إرجاع أ + ب
نهاية
```)"},

    {"صنف", 
R"(**صنف** - تعريف صنف

تعريف نوع بيانات مخصص مع خصائص ودوال.

```ص
صنف اسم_الصنف
    // الخصائص
    رقم خاصية1
    نص خاصية2
    
    // المنشئ
    دالة إنشاء(...)
        // تهيئة
    نهاية
    
    // الدوال
    دالة اسم_دالة()
        // ...
    نهاية
نهاية
```)"},

    {"إرجاع", 
R"(**إرجاع** - إرجاع قيمة

إرجاع قيمة من الدالة والخروج منها.

```ص
دالة مثال() -> رقم
    إرجاع 42
نهاية
```

ملاحظة: أي كود بعد `إرجاع` لن يُنفذ.)"},

    {"استورد", 
R"(**استورد** - استيراد وحدة

استيراد وحدة خارجية لاستخدام محتوياتها.

```ص
استورد رياضيات
استورد ملفات
استورد شبكة
```

أو استيراد عناصر محددة:
```ص
من رياضيات استورد جذر, قوة
```)"},
};

/**
 * قاموس الأنواع مع توثيقها
 */
const std::unordered_map<std::string, std::string> TYPE_DOCS = {
    {"رقم", 
R"(**رقم** - نوع رقمي

يمثل الأعداد الصحيحة والعشرية.

```ص
رقم صحيح = 42
رقم عشري = 3.14
رقم سالب = -10
```

العمليات المدعومة:
- الجمع: `+`
- الطرح: `-`
- الضرب: `*`
- القسمة: `/`
- باقي القسمة: `%`
- الأس: `^`)"},

    {"نص", 
R"(**نص** - نوع نصي

يمثل سلسلة من الأحرف (Unicode مدعوم).

```ص
نص تحية = "مرحباً بالعالم"
نص متعدد = "سطر أول\nسطر ثاني"
```

العمليات المدعومة:
- الربط: `+`
- الطول: `طول(نص)`
- الوصول: `نص[فهرس]`
- القطع: `نص[بداية:نهاية]`)"},

    {"منطقي", 
R"(**منطقي** - نوع منطقي

يمثل قيمة صحيح أو خطأ.

```ص
منطقي نشط = صحيح
منطقي متوقف = خطأ
```

العمليات المدعومة:
- و: `و`
- أو: `أو`
- ليس: `ليس` أو `!`)"},

    {"قائمة", 
R"(**قائمة** - نوع قائمة

مجموعة مرتبة من العناصر (يمكن أن تحتوي أنواعاً مختلفة).

```ص
قائمة أرقام = [1, 2, 3, 4, 5]
قائمة مختلطة = [1, "نص", صحيح]
```

الدوال:
- `أضف(قائمة, عنصر)` - إضافة عنصر
- `احذف(قائمة, فهرس)` - حذف عنصر
- `طول(قائمة)` - عدد العناصر)"},

    {"خريطة", 
R"(**خريطة** - نوع خريطة (قاموس)

مجموعة من أزواج المفتاح-القيمة.

```ص
خريطة شخص = {
    "اسم": "أحمد",
    "عمر": 25
}
```

الوصول:
```ص
نص اسم = شخص["اسم"]
شخص["مدينة"] = "الرياض"
```)"},
};


// ═══════════════════════════════════════════════════════════════════════════════
// صنف مزود معلومات التمرير - Hover Provider Class
// ═══════════════════════════════════════════════════════════════════════════════

/**
 * @class HoverProvider
 * @brief يوفر معلومات التمرير
 */
class HoverProvider {
public:
    /**
     * @brief الحصول على معلومات التمرير
     * @param doc المستند
     * @param pos الموقع
     * @return معلومات التمرير
     */
    std::optional<Hover> get_hover(const DocumentInfo& doc, const Position& pos) {
        // الحصول على الكلمة تحت المؤشر
        std::string word = get_word_at_position(doc, pos);
        
        if (word.empty()) {
            return std::nullopt;
        }
        
        // البحث في الدوال المدمجة
        auto builtin_it = BUILTIN_DOCS.find(word);
        if (builtin_it != BUILTIN_DOCS.end()) {
            return create_builtin_hover(builtin_it->second);
        }
        
        // البحث في الكلمات المفتاحية
        auto keyword_it = KEYWORD_DOCS.find(word);
        if (keyword_it != KEYWORD_DOCS.end()) {
            return create_keyword_hover(keyword_it->second);
        }
        
        // البحث في الأنواع
        auto type_it = TYPE_DOCS.find(word);
        if (type_it != TYPE_DOCS.end()) {
            return create_type_hover(type_it->second);
        }
        
        // البحث في رموز المستند
        for (const auto& sym : doc.symbols) {
            if (sym.name == word) {
                return create_symbol_hover(sym);
            }
        }
        
        return std::nullopt;
    }

private:
    /**
     * @brief الحصول على الكلمة في موقع محدد
     */
    std::string get_word_at_position(const DocumentInfo& doc, const Position& pos) {
        if (pos.line < 0 || pos.line >= static_cast<int>(doc.lines.size())) {
            return "";
        }
        
        const std::string& line = doc.lines[pos.line];
        
        if (pos.character < 0 || pos.character >= static_cast<int>(line.length())) {
            return "";
        }
        
        // إيجاد بداية الكلمة
        int start = pos.character;
        while (start > 0 && is_word_char(line[start - 1])) {
            start--;
        }
        
        // إيجاد نهاية الكلمة
        int end = pos.character;
        while (end < static_cast<int>(line.length()) && is_word_char(line[end])) {
            end++;
        }
        
        if (start == end) {
            return "";
        }
        
        return line.substr(start, end - start);
    }
    
    /**
     * @brief التحقق من حرف الكلمة
     */
    bool is_word_char(char c) {
        return (c >= 'a' && c <= 'z') ||
               (c >= 'A' && c <= 'Z') ||
               (c >= '0' && c <= '9') ||
               c == '_' ||
               (static_cast<unsigned char>(c) >= 0x80); // UTF-8 للعربية
    }
    
    /**
     * @brief إنشاء hover للدالة المدمجة
     */
    Hover create_builtin_hover(const BuiltinInfo& info) {
        Hover hover;
        
        std::ostringstream content;
        content << "```ص\n" << info.signature << "\n```\n\n";
        content << info.description << "\n\n";
        
        if (!info.parameters.empty()) {
            content << "**المعلمات:**\n" << info.parameters << "\n\n";
        }
        
        if (!info.returns.empty()) {
            content << "**الإرجاع:** " << info.returns << "\n\n";
        }
        
        if (!info.example.empty()) {
            content << "**مثال:**\n```ص\n" << info.example << "\n```";
        }
        
        hover.contents.kind = MarkupKind::MARKDOWN;
        hover.contents.value = content.str();
        
        return hover;
    }
    
    /**
     * @brief إنشاء hover للكلمة المفتاحية
     */
    Hover create_keyword_hover(const std::string& doc) {
        Hover hover;
        hover.contents.kind = MarkupKind::MARKDOWN;
        hover.contents.value = doc;
        return hover;
    }
    
    /**
     * @brief إنشاء hover للنوع
     */
    Hover create_type_hover(const std::string& doc) {
        Hover hover;
        hover.contents.kind = MarkupKind::MARKDOWN;
        hover.contents.value = doc;
        return hover;
    }
    
    /**
     * @brief إنشاء hover لرمز من المستند
     */
    Hover create_symbol_hover(const SymbolInfo& sym) {
        Hover hover;
        
        std::ostringstream content;
        
        // النوع
        switch (sym.kind) {
            case SymbolKind::FUNCTION:
                content << "```ص\nدالة " << sym.name << "(...)\n```\n\n";
                break;
            case SymbolKind::VARIABLE:
                content << "```ص\n" << sym.detail << " " << sym.name << "\n```\n\n";
                break;
            case SymbolKind::CLASS:
                content << "```ص\nصنف " << sym.name << "\n```\n\n";
                break;
            case SymbolKind::CONSTANT:
                content << "```ص\nثابت " << sym.name << "\n```\n\n";
                break;
            default:
                content << "```ص\n" << sym.name << "\n```\n\n";
        }
        
        // التوثيق إن وجد
        if (!sym.documentation.empty()) {
            content << sym.documentation;
        } else {
            content << "*لا يوجد توثيق*";
        }
        
        hover.contents.kind = MarkupKind::MARKDOWN;
        hover.contents.value = content.str();
        
        return hover;
    }
};

} // namespace lsp
} // namespace sad
