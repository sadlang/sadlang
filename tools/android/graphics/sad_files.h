/**
 * ═══════════════════════════════════════════════════════════════════════════════
 *  نظام الملفات — واجهة عربية لعمليات الملفات والمجلدات
 *  File System — Arabic API for file and directory operations
 * ═══════════════════════════════════════════════════════════════════════════════
 *
 *  استخدام:
 *    مقبض_ملفات* م = أنشئ_نظام_ملفات(مسار_التطبيق);
 *    اكتب_ملف_نصي(م, "بيانات.txt", "مرحباً بالعالم");
 *    char* محتوى = اقرأ_ملف_نصي(م, "بيانات.txt");
 *    دمر_نظام_ملفات(م);
 */

#ifndef SAD_FILES_H
#define SAD_FILES_H

#include <stddef.h>
#include <stdbool.h>
#include <stdint.h>

#ifdef __cplusplus
extern "C" {
#endif

// ═══════════════════════════════════════════════════════════════════════════════
//  أنواع الملفات
// ═══════════════════════════════════════════════════════════════════════════════

/** نوع العنصر في المسار */
typedef enum {
    نوع_ملف = 0,      // ملف عادي
    نوع_مجلد,          // مجلد
    نوع_رابط,          // رابط رمزي
    نوع_مجهول          // نوع غير معروف
} نوع_عنصر;

/** وضع فتح الملف */
typedef enum {
    وضع_قراءة = 0,    // قراءة فقط
    وضع_كتابة,        // كتابة (يمسح المحتوى)
    وضع_إضافة,        // إضافة للنهاية
    وضع_قراءة_كتابة   // قراءة وكتابة
} وضع_ملف;

/** حالة العملية على الملفات */
typedef enum {
    ملف_نجاح = 0,
    ملف_غير_موجود,
    ملف_لا_صلاحية,
    ملف_ممتلئ,
    ملف_خطأ_قراءة,
    ملف_خطأ_كتابة,
    ملف_مسار_خاطئ,
    ملف_خطأ_عام
} حالة_ملف;

// ═══════════════════════════════════════════════════════════════════════════════
//  هياكل البيانات
// ═══════════════════════════════════════════════════════════════════════════════

/** معلومات عنصر في مجلد */
typedef struct {
    char* الاسم;           // اسم الملف أو المجلد
    char* المسار_الكامل;   // المسار الكامل
    نوع_عنصر النوع;       // ملف أو مجلد
    size_t الحجم;           // الحجم بالبايت
    int64_t تاريخ_التعديل;  // Unix timestamp
} معلومات_عنصر;

/** قائمة عناصر المجلد */
typedef struct {
    معلومات_عنصر* العناصر;
    int العدد;
} قائمة_عناصر;

/** مقبض نظام الملفات */
typedef struct مقبض_ملفات مقبض_ملفات;

// ═══════════════════════════════════════════════════════════════════════════════
//  إنشاء وتدمير
// ═══════════════════════════════════════════════════════════════════════════════

/** إنشاء مقبض نظام الملفات */
مقبض_ملفات* أنشئ_نظام_ملفات(const char* مسار_التطبيق);

/** تدمير مقبض نظام الملفات */
void دمر_نظام_ملفات(مقبض_ملفات* الملفات);

// ═══════════════════════════════════════════════════════════════════════════════
//  قراءة وكتابة ملفات نصية
// ═══════════════════════════════════════════════════════════════════════════════

/** قراءة ملف نصي — يجب تحرير النص بـ free() */
char* اقرأ_ملف_نصي(مقبض_ملفات* م, const char* المسار);

/** كتابة نص في ملف (يمسح المحتوى القديم) */
حالة_ملف اكتب_ملف_نصي(مقبض_ملفات* م, const char* المسار, const char* المحتوى);

/** إضافة نص لنهاية ملف */
حالة_ملف أضف_لملف(مقبض_ملفات* م, const char* المسار, const char* المحتوى);

// ═══════════════════════════════════════════════════════════════════════════════
//  قراءة وكتابة ملفات ثنائية
// ═══════════════════════════════════════════════════════════════════════════════

/** قراءة ملف ثنائي — يجب تحرير البيانات بـ free() */
uint8_t* اقرأ_ملف_ثنائي(مقبض_ملفات* م, const char* المسار, size_t* الحجم);

/** كتابة بيانات ثنائية في ملف */
حالة_ملف اكتب_ملف_ثنائي(مقبض_ملفات* م, const char* المسار,
                          const uint8_t* البيانات, size_t الحجم);

// ═══════════════════════════════════════════════════════════════════════════════
//  عمليات الملفات
// ═══════════════════════════════════════════════════════════════════════════════

/** هل الملف موجود؟ */
bool ملف_موجود(مقبض_ملفات* م, const char* المسار);

/** حذف ملف */
حالة_ملف احذف_ملف(مقبض_ملفات* م, const char* المسار);

/** نقل أو إعادة تسمية ملف */
حالة_ملف انقل_ملف(مقبض_ملفات* م, const char* المسار_القديم, const char* المسار_الجديد);

/** نسخ ملف */
حالة_ملف انسخ_ملف(مقبض_ملفات* م, const char* المصدر, const char* الهدف);

/** حجم الملف بالبايت، أو -1 عند الخطأ */
int64_t حجم_ملف(مقبض_ملفات* م, const char* المسار);

// ═══════════════════════════════════════════════════════════════════════════════
//  عمليات المجلدات
// ═══════════════════════════════════════════════════════════════════════════════

/** إنشاء مجلد (بما في ذلك المجلدات الوسيطة) */
حالة_ملف أنشئ_مجلد(مقبض_ملفات* م, const char* المسار);

/** هل المجلد موجود؟ */
bool مجلد_موجود(مقبض_ملفات* م, const char* المسار);

/** سرد محتويات مجلد */
قائمة_عناصر استعرض_مجلد(مقبض_ملفات* م, const char* المسار);

/** حذف مجلد (فارغ فقط) */
حالة_ملف احذف_مجلد(مقبض_ملفات* م, const char* المسار);

/** تحرير قائمة العناصر */
void حرر_قائمة_عناصر(قائمة_عناصر* ق);

// ═══════════════════════════════════════════════════════════════════════════════
//  مسارات مساعدة
// ═══════════════════════════════════════════════════════════════════════════════

/** دمج مسارين — يجب تحرير النتيجة بـ free() */
char* ادمج_مسار(const char* أساس, const char* فرعي);

/** استخراج اسم الملف من المسار — يجب تحرير النتيجة بـ free() */
char* اسم_الملف(const char* المسار);

/** استخراج امتداد الملف — يجب تحرير النتيجة بـ free() */
char* امتداد_ملف(const char* المسار);

/** المسار الداخلي للتطبيق (Android internal storage) */
const char* مسار_التطبيق_الداخلي(مقبض_ملفات* م);

#ifdef __cplusplus
}
#endif

#endif // SAD_FILES_H
