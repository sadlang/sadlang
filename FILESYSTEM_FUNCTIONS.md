# بسم الله الرحمن الرحيم
# ═══════════════════════════════════════════════════════════════════
#     📁 دوال مكتبة نظام الملفات - Filesystem Library Functions
# ═══════════════════════════════════════════════════════════════════
# التاريخ: 5 ديسمبر 2025
# الحمد لله رب العالمين
# ═══════════════════════════════════════════════════════════════════

## 📖 كيفية الاستخدام / How to Use

```sad
# استيراد المكتبة / Import library
استورد نظام_الملفات
```

---

## 📂 دوال قراءة/كتابة الملفات / File I/O Functions

### 1. `اقرأ_ملف(المسار)` / `read_file(path)`
**الوصف:** قراءة محتوى ملف نصي كاملاً  
**المعاملات:**
- `المسار` (نص): مسار الملف
**الإرجاع:** نص - محتوى الملف

```sad
محتوى = اقرأ_ملف("مثال.txt")
اطبع(محتوى)
```

---

### 2. `اقرأ_أسطر(المسار)` / `read_lines(path)`
**الوصف:** قراءة ملف نصي كمصفوفة من الأسطر  
**المعاملات:**
- `المسار` (نص): مسار الملف
**الإرجاع:** مصفوفة - كل عنصر سطر من الملف

```sad
أسطر = اقرأ_أسطر("مثال.txt")
لكل سطر في أسطر أعمل
    اطبع(سطر)
نهاية
```

---

### 3. `اقرأ_بايتات(المسار)` / `read_bytes(path)`
**الوصف:** قراءة ملف ثنائي كمصفوفة بايتات  
**المعاملات:**
- `المسار` (نص): مسار الملف
**الإرجاع:** مصفوفة أرقام - بايتات الملف

```sad
بايتات = اقرأ_بايتات("صورة.png")
اطبع("حجم الملف: " + نص(طول(بايتات)) + " بايت")
```

---

### 4. `اكتب_ملف(المسار، المحتوى، استبدال؟)` / `write_file(path, content, truncate?)`
**الوصف:** كتابة نص إلى ملف  
**المعاملات:**
- `المسار` (نص): مسار الملف
- `المحتوى` (نص): النص المراد كتابته
- `استبدال` (منطقي، اختياري): استبدال الملف؟ (افتراضي: صحيح)

```sad
اكتب_ملف("مثال.txt"، "مرحباً بالعالم!")
اكتب_ملف("سجل.txt"، "سطر جديد\n"، خطأ)  # إلحاق
```

---

### 5. `اكتب_أسطر(المسار، الأسطر)` / `write_lines(path, lines)`
**الوصف:** كتابة مصفوفة من الأسطر إلى ملف  
**المعاملات:**
- `المسار` (نص): مسار الملف
- `الأسطر` (مصفوفة): مصفوفة النصوص

```sad
أسطر = ["السطر الأول"، "السطر الثاني"، "السطر الثالث"]
اكتب_أسطر("مثال.txt"، أسطر)
```

---

### 6. `اكتب_بايتات(المسار، البايتات)` / `write_bytes(path, bytes)`
**الوصف:** كتابة بيانات ثنائية إلى ملف  
**المعاملات:**
- `المسار` (نص): مسار الملف
- `البايتات` (مصفوفة): مصفوفة البايتات

```sad
بايتات = [72، 101، 108، 108، 111]  # "Hello"
اكتب_بايتات("بيانات.bin"، بايتات)
```

---

### 7. `أضف_إلى_ملف(المسار، المحتوى)` / `append_to_file(path, content)`
**الوصف:** إلحاق نص إلى نهاية ملف موجود  
**المعاملات:**
- `المسار` (نص): مسار الملف
- `المحتوى` (نص): النص المراد إلحاقه

```sad
أضف_إلى_ملف("سجل.txt"، "سطر جديد\n")
```

---

### 8. `انسخ_ملف(المصدر، الوجهة)` / `copy_file(source, destination)`
**الوصف:** نسخ ملف من مكان لآخر  
**المعاملات:**
- `المصدر` (نص): مسار الملف المصدر
- `الوجهة` (نص): مسار الوجهة

```sad
انسخ_ملف("أصلي.txt"، "نسخة.txt")
```

---

### 9. `انقل_ملف(المصدر، الوجهة)` / `move_file(source, destination)`
**الوصف:** نقل أو إعادة تسمية ملف  
**المعاملات:**
- `المصدر` (نص): مسار الملف الحالي
- `الوجهة` (نص): المسار الجديد

```sad
انقل_ملف("قديم.txt"، "جديد.txt")
```

---

### 10. `احذف_ملف(المسار)` / `delete_file(path)`
**الوصف:** حذف ملف  
**المعاملات:**
- `المسار` (نص): مسار الملف

```sad
احذف_ملف("مؤقت.txt")
```

---

## 📁 دوال المجلدات / Directory Functions

### 11. `أنشئ_مجلد(المسار)` / `create_directory(path)`
**الوصف:** إنشاء مجلد جديد  
**المعاملات:**
- `المسار` (نص): مسار المجلد

```sad
أنشئ_مجلد("مجلد_جديد")
أنشئ_مجلد("مجلد/مجلد_فرعي")  # متداخل
```

---

### 12. `اسرد_مجلد(المسار)` / `list_directory(path)`
**الوصف:** الحصول على قائمة بأسماء محتويات مجلد  
**المعاملات:**
- `المسار` (نص): مسار المجلد
**الإرجاع:** مصفوفة - أسماء الملفات والمجلدات

```sad
محتويات = اسرد_مجلد(".")
لكل عنصر في محتويات أعمل
    اطبع(عنصر)
نهاية
```

---

### 13. `اسرد_مجلد_كامل(المسار)` / `list_directory_full(path)`
**الوصف:** الحصول على معلومات تفصيلية عن محتويات مجلد  
**المعاملات:**
- `المسار` (نص): مسار المجلد
**الإرجاع:** مصفوفة - كل عنصر قاموس بمعلومات الملف

```sad
محتويات = اسرد_مجلد_كامل(".")
لكل عنصر في محتويات أعمل
    اطبع(عنصر["اسم"] + " - " + عنصر["نوع"])
نهاية
```

---

### 14. `اسرد_مجلد_تكراري(المسار)` / `list_directory_recursive(path)`
**الوصف:** سرد المجلد وجميع المجلدات الفرعية بشكل تكراري  
**المعاملات:**
- `المسار` (نص): مسار المجلد
**الإرجاع:** مصفوفة - جميع الملفات في المجلد والمجلدات الفرعية

```sad
كل_الملفات = اسرد_مجلد_تكراري(".")
اطبع("عدد الملفات: " + نص(طول(كل_الملفات)))
```

---

### 15. `احذف_مجلد(المسار)` / `remove_directory(path)`
**الوصف:** حذف مجلد فارغ  
**المعاملات:**
- `المسار` (نص): مسار المجلد

```sad
احذف_مجلد("مجلد_مؤقت")
```

---

### 16. `احصل_على_مجلد_العمل()` / `get_current_directory()`
**الوصف:** الحصول على مجلد العمل الحالي  
**الإرجاع:** نص - مسار المجلد الحالي

```sad
مجلد_حالي = احصل_على_مجلد_العمل()
اطبع("المجلد الحالي: " + مجلد_حالي)
```

---

### 17. `غير_مجلد_العمل(المسار)` / `change_directory(path)`
**الوصف:** تغيير مجلد العمل الحالي  
**المعاملات:**
- `المسار` (نص): المجلد الجديد

```sad
غير_مجلد_العمل("مجلد_المشروع")
```

---

## 🛤️ دوال معالجة المسارات / Path Manipulation Functions

### 18. `اجمع_مسار(مسار1، مسار2، ...)` / `join_path(...)`
**الوصف:** جمع عدة أجزاء مسار في مسار واحد  
**المعاملات:** عدد غير محدود من النصوص
**الإرجاع:** نص - المسار المجموع

```sad
مسار = اجمع_مسار("مجلد"، "مجلد_فرعي"، "ملف.txt")
# النتيجة: "مجلد/مجلد_فرعي/ملف.txt"
```

---

### 19. `احصل_على_مسار_مطلق(المسار)` / `get_absolute_path(path)`
**الوصف:** تحويل مسار نسبي إلى مسار مطلق  
**المعاملات:**
- `المسار` (نص): المسار النسبي
**الإرجاع:** نص - المسار المطلق

```sad
مسار_مطلق = احصل_على_مسار_مطلق("ملف.txt")
اطبع(مسار_مطلق)
```

---

### 20. `احصل_على_اسم_ملف(المسار)` / `get_filename(path)`
**الوصف:** استخراج اسم الملف من مسار كامل  
**المعاملات:**
- `المسار` (نص): المسار الكامل
**الإرجاع:** نص - اسم الملف فقط

```sad
اسم = احصل_على_اسم_ملف("/مجلد/ملف.txt")
# النتيجة: "ملف.txt"
```

---

### 21. `احصل_على_امتداد(المسار)` / `get_extension(path)`
**الوصف:** استخراج امتداد الملف  
**المعاملات:**
- `المسار` (نص): مسار الملف
**الإرجاع:** نص - الامتداد (مع النقطة)

```sad
امتداد = احصل_على_امتداد("مثال.txt")
# النتيجة: ".txt"
```

---

### 22. `احصل_على_مجلد_أب(المسار)` / `get_parent_path(path)`
**الوصف:** الحصول على المجلد الأب (الحاوي) للمسار  
**المعاملات:**
- `المسار` (نص): المسار
**الإرجاع:** نص - مسار المجلد الأب

```sad
أب = احصل_على_مجلد_أب("/مجلد/مجلد_فرعي/ملف.txt")
# النتيجة: "/مجلد/مجلد_فرعي"
```

---

## ℹ️ دوال معلومات الملف / File Information Functions

### 23. `هل_موجود(المسار)` / `exists(path)`
**الوصف:** التحقق من وجود ملف أو مجلد  
**المعاملات:**
- `المسار` (نص): المسار للتحقق منه
**الإرجاع:** منطقي - صحيح إذا كان موجوداً

```sad
إذا هل_موجود("ملف.txt") أعمل
    اطبع("الملف موجود")
نهاية
```

---

### 24. `هل_ملف(المسار)` / `is_file(path)`
**الوصف:** التحقق إذا كان المسار يشير إلى ملف  
**المعاملات:**
- `المسار` (نص): المسار
**الإرجاع:** منطقي - صحيح إذا كان ملفاً

```sad
إذا هل_ملف("test.txt") أعمل
    اطبع("هذا ملف")
نهاية
```

---

### 25. `هل_مجلد(المسار)` / `is_directory(path)`
**الوصف:** التحقق إذا كان المسار يشير إلى مجلد  
**المعاملات:**
- `المسار` (نص): المسار
**الإرجاع:** منطقي - صحيح إذا كان مجلداً

```sad
إذا هل_مجلد("مجلد") أعمل
    اطبع("هذا مجلد")
نهاية
```

---

### 26. `احصل_على_حجم_ملف(المسار)` / `get_file_size(path)`
**الوصف:** الحصول على حجم ملف بالبايتات  
**المعاملات:**
- `المسار` (نص): مسار الملف
**الإرجاع:** رقم - الحجم بالبايتات

```sad
حجم = احصل_على_حجم_ملف("صورة.png")
اطبع("حجم الملف: " + نص(حجم) + " بايت")
```

---

## 🔍 دوال مطابقة الأنماط / Pattern Matching Functions

### 27. `طابق_نمط(النمط، المجلد_الأساسي؟)` / `glob(pattern, base_path?)`
**الوصف:** البحث عن ملفات تطابق نمط معين  
**المعاملات:**
- `النمط` (نص): نمط البحث (مثل: `*.txt`)
- `المجلد_الأساسي` (نص، اختياري): المجلد للبحث فيه (افتراضي: `.`)
**الإرجاع:** مصفوفة - الملفات المطابقة

```sad
# البحث عن جميع ملفات txt
ملفات_txt = طابق_نمط("*.txt")

# البحث عن ملفات بنمط معقد
ملفات = طابق_نمط("test_*.py"، "مجلد_الاختبار")
```

---

## 📝 أمثلة عملية / Practical Examples

### مثال 1: نسخ احتياطي للملفات
```sad
استورد نظام_الملفات

# إنشاء مجلد النسخ الاحتياطي
أنشئ_مجلد("نسخ_احتياطي")

# نسخ جميع ملفات txt
ملفات = طابق_نمط("*.txt")
لكل ملف في ملفات أعمل
    اسم_جديد = اجمع_مسار("نسخ_احتياطي"، ملف)
    انسخ_ملف(ملف، اسم_جديد)
    اطبع("تم نسخ: " + ملف)
نهاية
```

### مثال 2: تحليل محتويات مجلد
```sad
استورد نظام_الملفات

دالة حلل_مجلد(مسار_المجلد):
    محتويات = اسرد_مجلد_كامل(مسار_المجلد)
    
    عدد_ملفات = 0
    عدد_مجلدات = 0
    حجم_إجمالي = 0
    
    لكل عنصر في محتويات أعمل
        إذا عنصر["نوع"] == "ملف" أعمل
            عدد_ملفات = عدد_ملفات + 1
            حجم_إجمالي = حجم_إجمالي + احصل_على_حجم_ملف(عنصر["مسار"])
        وإلا أعمل
            عدد_مجلدات = عدد_مجلدات + 1
        نهاية
    نهاية
    
    اطبع("عدد الملفات: " + نص(عدد_ملفات))
    اطبع("عدد المجلدات: " + نص(عدد_مجلدات))
    اطبع("الحجم الإجمالي: " + نص(حجم_إجمالي) + " بايت")
نهاية

حلل_مجلد(".")
```

### مثال 3: سجل بسيط
```sad
استورد نظام_الملفات

دالة اكتب_سجل(رسالة):
    # إضافة التاريخ والوقت
    سطر = "[" + الآن() + "] " + رسالة + "\n"
    أضف_إلى_ملف("سجل.txt"، سطر)
نهاية

اكتب_سجل("بدء البرنامج")
اكتب_سجل("معالجة البيانات...")
اكتب_سجل("انتهى البرنامج")
```

---

## ⚠️ ملاحظات مهمة / Important Notes

1. **الترميز:** جميع الملفات النصية تُقرأ وتُكتب بترميز UTF-8
2. **الأخطاء:** جميع الدوال ترفع أخطاء إذا فشلت العملية
3. **المسارات:** يمكن استخدام مسارات مطلقة أو نسبية
4. **الأمان:** احذر عند حذف الملفات - العملية نهائية!

---

## 🔗 روابط ذات صلة

- [مكتبة JSON](JSON_FUNCTIONS.md)
- [مكتبة HTTP](HTTP_FUNCTIONS.md)
- [مكتبة قاعدة البيانات](DATABASE_FUNCTIONS.md)

---

**الحمد لله رب العالمين** 🤲
**تم التوثيق بتاريخ:** 5 ديسمبر 2025
