# لغة ص — مسار المحرّك الواحد

هذا المستودع نسخةٌ كاملةُ التاريخ من `sadlang/s-programming-language` عند الإيداع
`d6baa0d8` (٢ أيلول ٢٠٢٦)، غرضُها واحدٌ ومحدَّد:

> **حذفُ المفسّر، وإسنادُ كلِّ ما كان يقدّمه إلى المترجم.**

الأصلُ يبقى قائمًا ومستقلًّا. هذا المسارُ لا يستبدلُه حتى يَثبُتَ أنّه يفي بما كان
يفي به المحرّكان معًا. الأصلُ مربوطٌ هنا بعيدًا باسم `upstream`.

---

## ١ · القاعدةُ الحاكمة

**محرّكٌ واحد.** لا سلوكَ تُعرِّفه اللغةُ ولا يبلغُه المترجم. وكلُّ ما كان يُعتذَرُ
عنه بـ«المفسّرُ يفي به» يصيرُ دَينًا مكتوبًا في عدّادٍ منشور، لا عذرًا مقبولًا.

**والمرجعُ قاعدةٌ في اللغةِ لا مُخرَجُ مُنفِّذ** (§٤). فما يُطلَبُ من المترجمِ
يُشتقُّ من `language-truth/`؛ ومُنفِّذٌ يُتَّخَذُ مرجعًا يُثبِّتُ عللَه عقدًا.

### العدّاداتُ الخمسة

مقيسةً على `dist/Release/sad-build.exe` بوضعِ المترجم-فقط (٣ أيلول ٢٠٢٦):

| العدّاد | القيمة | ما يقيسه |
|---|---:|---|
| **أ · بذورٌ متخطّية** `@skip_compiler` | **٧٨** ⟳ | سلوكٌ مُختبَرٌ لا يبلغُه المترجم |
| **ب · مدمَجاتٌ بلا ذراعِ إرسال** | **٥٦٤** من ١٢٠٥ | سطحُ المكتبةِ الذي لا يُصرَّف |
| **ج · مدخلاتٌ بلا مواصفة** | **١٠٣٥** بلا `returns` · **١٠٥٤** بلا `params` · **٢٠** معاملًا مكتوبَ النوع | ما لا قاعدةَ له تُطلَبُ من المترجم |
| **د · سوالبُ يتيمة** | **٣٣٠** | `@expect_error` بلا مقابلٍ مترجَم |
| **هـ · بذورٌ بلا عقد** | **١٦٩** ⟳ | لا `@expected` ولا سالب — تُشغَّلُ ولا تُحاكَم |

**تفصيلُ أ**: `sections` ٦٦ · `_regression` ٦ · `rules_matrix` ٥ · `grammar_gaps` ١.
**تفصيلُ د**: **٢٧٧** بلا نمطٍ أصلًا · **٤١** برمزِ خطأٍ من SoT · **١٢** بنثرِ محرّك.
**تفصيلُ هـ**: `sections` ٩٣ · `_regression` ٧٦.

> ⟳ **يُعادُ قياسُه بأمرٍ واحد** — والتعريفُ في الشفرةِ لا في هذه الفقرة:
> ```
> python scripts/codegen/measure_seed_contract_gap.py
> ```
> ويقبلُ `--أ N` و`--هـ N` فيُخفِقُ إن خالفَه المقيس، فيصيرُ العددُ **محروسًا**
> لا منقولًا. وبقيّةُ العدّاداتِ (ب · ج · د) لا أمرَ منشورًا لها بعدُ — **دَينٌ
> مُقيَّدٌ** لا سكوتٌ عنه.

> ⚠️ **تصويبٌ مُدوَّنٌ ودرسُه أهمُّ من رقمِه.** كان «هـ = ١٨٥ (`sections` ١٠٨ ·
> `_regression` ٧٦ · `rules_matrix` ١)». وقد قِيسَ العدّادُ **ثلاثَ مرّاتٍ
> بثلاثةِ تعريفاتٍ أُعيدَ بناؤها من الوصفِ النثريِّ نفسِه** فأعطى ١٨٥ و١٨٤
> و١٩٠ — و`_regression ٧٦` وحدَه تطابقَ في الثلاثة. ولم يُعَدْ إنتاجُ ١٨٥
> بأيِّ تعريف، فلم يُصحَّحْ إلى رقمٍ رابعٍ منثورٍ بل إلى **رقمٍ له سكربت**:
> ١٦٩ = يُشغَّلُ (ليس `@skip_compiler`) ولا `@expected` ولا وسمَ سالب.
> وبرهانُ سلامةِ المنهج أنّ العدّادَ **أ** خرجَ من السكربتِ نفسِه **٨٠
> بتفصيلِه ٦٨·٦·٥·١ حرفًا بحرف** كما في الوثيقة. والدرس: **رقمٌ تعريفُه في
> نثرٍ ليس رقمًا مقيسًا بل رقمًا يُعادُ اختراعُه في كلِّ قراءة.**

> ⚠️ **تصويبٌ مُدوَّن.** كان الجدولُ يقول «د · سوالبُ بنثرِ محرّك = ٤٢، وثمانيةٌ
> برمزِ SoT». والمقيسُ **معكوس**: ٤١ برمزِ SoT و١٢ بنثر. وكان يقول «هـ = ٨٧ في
> `sections`» فأسقطَ `_regression` كلَّه وثلاثةُ أرباعِه بلا عقد. وكان يقول
> «٧٢ داخلَ المستويَين» ولا قراءةَ تُنتِجُ هذا الرقم. والدرسُ أنّ **رقمًا في نثرٍ
> نسخةٌ ثانيةٌ من حقيقةٍ تتباعدُ عن أصلِها** — فكلُّ رقمٍ هنا يُعادُ قياسُه بأمرٍ
> يُذكَرُ معه، ولا يُنقَلُ من فقرةٍ إلى فقرة.

**ودرسُ التوجيه**: العقدُ المرسى **بقاعدةٍ** ينجو من حذفِ المحرّك، والمرسى
**بصياغةٍ** لا ينجو. لكنّ الرمزَ ليس دائمًا مستقلًّا عن المحرّك: خمسةُ رموزٍ في
الكتالوجِ كان **تعريفُها نفسُه** هو المفسّر (`INT008/9/10` · `RUN059` · `SEM027`)،
فتوجيهُ سالبٍ إليها يحملُ سلوكَ محرّكٍ محذوفٍ في ثوبِ قاعدةٍ مُعلَنة. وقد
قُوعِدَت بحقلِ `retired` يحرسُه `scripts/codegen/check_retired_error_codes.py`.

### الحمرةُ المقيسةُ الآن

شوطٌ كاملٌ بـ`--compiler` وحدَه على `dist/Release/sad-build.exe` (٣ أيلول ٢٠٢٦).
**وعمودُ المتخطَّى جزءٌ من الجدولِ لا حاشيةٌ له**: صفرُ إخفاقٍ بلا عددِ المتخطَّى
رقمٌ لا معنى له.

| المجلّد | إجمالي | نجح | **فشل** | متخطًّى |
|---|---:|---:|---:|---:|
| `rules_matrix` | ٣٬٠٣٩ | ٣٬٠١٩ | **١** | ١٩ |
| `sections` | ١٬٢٧٦ | ١٬١٠٦ | **٦** | ١٦٤ |
| `_regression` | ٩٢ | ١٠ | **١٣** | ٦٩ |
| `grammar_gaps` | ١١٥ | ٨٤ | **٣٠** | ١ |
| **المجموع** | **٤٬٥٢٢** | **٤٬٢١٩** | **٥٠** | **٢٥٣** |

`_regression` و`grammar_gaps` (١٣ + ٣٠ = **٤٣**) هما نطاقُ `DECLARED_REDS.tsv`:
**٤٣ مقيسًا = ٤٣ مسجَّلًا**، والحارسُ أخضر.

> 🔑 **صفرُ انهياراتٍ داخليّةٍ في ٤٬٥٢٢ بذرة** (كانت ١٤) — وهي ما يراه العدّاءُ
> في المجلّداتِ الأربعةِ أعلاه؛ وملفّاتُ `tests/behavior` كلُّها ٤٬٥٢٩، والفرقُ
> سبعةٌ خارجَ هذه المجلّدات (`P0_smoke` ووحداتٌ مساعِدة). الجدولُ أعلاه
> هو شوطُ ٣ أيلول ٢٠٢٦ بعدَ حملةِ السدّ؛ الأرقامُ التي سبقتْها محفوظةٌ في
> «قبل» أدناه ليبقى الفرقُ مقيسًا لا مُدَّعًى.

#### تصنيفُ ما هو خارجَ السجلّ (`rules_matrix` + `sections`): **٢٥ ← ٧**

عمودُ «بعد» **مقروءٌ من سجلِّ الشوطِ صفًّا صفًّا**، لا محسوبًا من عمودِ «قبل»:

| الصنف | قبل | بعد | الوصف |
|---|---:|---:|---|
| **انهيارٌ داخليٌّ في المترجم** | **١٤** | **٠** | كان يطبعُ «خطأ مترجم داخلي» ثمّ **يرجعُ صفرًا ويُنتِجُ ثنائيًّا** |
| مكتبةُ الشبكات | (ضمنَ الـ١٤) | **٠** | حُذفت الأغلفةُ الأربعةُ ⇒ ٩ مقابسِ ويبٍ و`080_end_to_end_udp_oop` **خضراء** |
| **يرفضُ بلا رمزِ SoT** | ٥ | **٢** | بقيَ `N10`→RUN002 و`N45`→RUN053 — **وهما رمزا وقتِ تشغيلٍ لا ترجمة**، فطبقتُهما غيرُ التي عولجت |
| يقبلُ ما تطلبُ البذرةُ رفضَه | ٣ | **٢** | `060_ext_unknown_type` · `N06` — **ولا سندَ لهما في `language-truth/`** |
| رفضُ SEM050 | ٠ | **١** | `055_ns04_unsafe_optional_access` — تعذّرَ ربطُ المستقبِلِ بصنف |
| انهيارُ الثنائيِّ المُنتَجِ بإشارة | ١ | **١** | `N17` — رمزُ خروجٍ ‎3221225477 (وصولٌ غيرُ مشروع) |
| متفرّقة | ٢ | **١** | `087_null_object_property_port_raises` |

> ✅ **جذرٌ واحدٌ سُدَّ فأخضرَ أربعًا.** كان `buildBilingualMessage` — وهي
> **المصبُّ الواحدُ** لكلِّ رسالةِ كتالوجٍ أماميّةً كانت أو خلفيّة — تُخرِجُ
> النصَّ **بلا رمزِه**، بخلافِ مسارِ المحلّلِ الذي يطبعُ «`⛔ [SYN001] …`».
> فصارت تُصدِّرُ كلَّ رسالةٍ بـ`[الرمز]`. ومعها رُدَّ نصّانِ حرّانِ إلى الكتالوج:
> `Error: Undefined variable '…'` (إنجليزيٌّ خامٌّ بلا رمزٍ ولا عربيّةٍ ولا
> علاج) ⇒ `SEM001`، ونصٌّ عربيٌّ مؤلَّفٌ في `call_main.cpp` ⇒ `SEM004`.
>
> والأثرُ المقيس: **`N02` · `N04` · `N08` · `N09` خضراء، وصفرُ انحدار**.
> وبذرةٌ واحدةٌ احمرَّت ثمّ أُعيد إرساؤها: `057_new_without_operand` كانت
> تُوكِّدُ «`Undefined variable 'جديد'`» — **النصَّ الإنجليزيَّ الحرَّ نفسَه**
> الذي حُذف، أي أنّها كانت مرساةً على **أثرِ عطبٍ لا على قاعدة**؛ فأُرسيت على
> `SEM001` كما تقولُ ملحوظتُها أصلًا.
>
> ⚠️ وبقيَ `N10`→`RUN002` و`N45`→`RUN053`: رمزا **وقتِ تشغيل** لا ترجمة،
> فطبقتُهما غيرُ التي عولجت — دَينٌ مُقيَّدٌ مستقلّ.
>
> ⚠️ وذراعُ «مدمَجةٌ تلزمُها وحدةٌ لم تُستورَد» في `call_main.cpp` بقيت نصًّا
> مؤلَّفًا **بسببٍ مُدوَّن**: لا رمزَ لها في الكتالوج، وإعلانُ رمزٍ جديدٍ قرارُ
> مصدرِ حقيقةٍ لا ترقيعُ باني.

> ⚠️ **تصويبٌ مُدوَّنٌ في التصنيفِ نفسِه**: كان `N10` مصنَّفًا «يقبلُ ما تطلبُ
> البذرةُ رفضَه»، والمقيسُ أنّه **يرفضُ بلا الرمزِ المطلوب** (`RUN002`) — أي في
> الصنفِ الذي يليه. وكان `093_http_client_reuse_two_paths` في «متفرّقة» وهو
> اليومَ ليس بين الإخفاقات. والعمودُ أُعيدَ بناؤه من السجلِّ لهذا السبب.

#### حملةُ سدِّ الانهياراتِ الداخليّة — ٣ أيلول ٢٠٢٦

الانهياراتُ كانت **جذورًا معدودةً لا عددَ بذورِها**. والأشواطُ ثلاثةٌ مُدوَّنة:

| الشوط | انهيارات | فاشل |
|---|---:|---:|
| قبلَ الحملة | **١٤** | ٦٩ |
| بعدَ `SEM014`/`SEM015`/`أزل`/`SEM049` | **١٤** (تركيبُها تبدّل) | ٦٥ |
| بعدَ رقعةِ تِلْوِ الشبكاتِ وفكِّ الصفّ | **٢** | ٦٣ |
| بعدَ رقعةِ تِلْوِ الوصولِ إلى الحقل | **٠** | ٦٣ |

> ⚠️ **الأربعةَ عشرَ في الشوطَين الأوّلَين ليست المجموعةَ نفسَها.** تركيبُ
> المجموعةِ الثانيةِ مقيسٌ بدقّة (١٠ شبكات · ٢ فكُّ صفٍّ · ٢ وصولُ حقل)؛ أمّا
> تركيبُ الأولى فلم يُحفَظْ بدقّةٍ ولا يُعادُ بناؤه، فلا يُدَّعى. المقيسُ هو
> **العددُ في كلِّ شوطٍ**، لا نَسَبُ كلِّ بذرةٍ إلى جذرِها عبرَ الأشواط.

| الجذر | البذور المقيسة | العلاج | الأثر المقيس |
|---|---:|---|---|
| `SEM014` مُعلَنٌ في مصدرِ الحقيقةِ **بلا باعث** | ١ | حارسٌ في `statement_generators.cpp` | `gr.adv.yield` من «فجوة مترجم» إلى **مطلقة** |
| `SEM015` مُعلَنٌ **بلا باعث** — والسقوطُ إلى `%#self` يبثُّ سجلًّا لا وجودَ له | ١ | حارسٌ مزدوجُ الشرطِ في `expression_main.cpp` | `gr.oop.this_super` من «فجوة مترجم» إلى **مطلقة** |
| تِلْوُ رفضٍ في تسجيلِ المسار | ١٠ | `builtinErrorSentinel` — مُعينٌ قائمٌ في الشجرة | ١٠ → صفر |
| تِلْوُ رفضٍ في الوصولِ إلى الحقل | ٢ | المُعينُ نفسُه في `oop_ops.cpp` | ٢ → صفر |
| تِلْوُ رفضٍ في حارسِ الفهرسِ النصّيّ | ١ | المُعينُ نفسُه في `array_ops.cpp` — **فاتني حتّى كشفتْه المراجعة** | `N19` صارت **خضراء** |
| فكُّ الصفِّ — ISSUE-124 | ٢ | **سُحبت الرقعةُ** ⇒ `SEM052` رفضٌ مُشخَّص | البذرتانِ حمراوانِ **بصدق** |

> 🔑 **«التِّلْوُ» هو الدرسُ.** موضعٌ في الخلفيّةِ يُشخِّصُ خطأَ المستعمِلِ
> تشخيصًا صحيحًا ثمّ ينصرفُ بـ`nullptr` **بغيرِ أن يملأَ خانةَ نتيجةِ
> التعليمة**، فتقرأُ التعليمةُ التاليةُ سجلًّا لا وجودَ له، فيُطبَعُ بعدَ
> التشخيصِ الصحيحِ **اتّهامٌ كاذبٌ للمترجّم**: «يُرجى الإبلاغ». مقيسٌ:
> `استورد شبكات` وحدَه أخرجَ ٨ أخطاء = ٤ تشخيصاتٍ صحيحةٍ + ٤ انهياراتٍ تِلْوًا.
> وملءُ الخانةِ ليس تسترًا: البوّابةُ العامّةُ تُفشلُ الترجمةَ حتمًا فلا ثنائيَّ
> يُسلَّم؛ غايتُها بقاءُ الخفضِ سليمَ البنيةِ حتّى تُجمَعَ بقيّةُ الأخطاءِ الحقيقيّة.

##### خمسةُ رموزٍ جديدةٍ في مصدرِ الحقيقة

| الرمز | متى | لماذا لا «خطأ مترجم داخلي» |
|---|---|---|
| `SEM049 SEM_ROUTE_HANDLER_NOT_LITERAL` | معالجُ المسارِ مُرِّرَ متغيّرًا أو معاملًا | بناءُ الثانكِ يلزمُه معرفةُ الدالّةِ **وقتَ التوليد** — قيدٌ معلوم |
| `SEM050 SEM_MEMBER_RECEIVER_CLASS_UNKNOWN` | تعذّرَ ربطُ سجلِّ المستقبِلِ بصنف | قيدٌ قائمٌ مُتتبَّعٌ (ISSUE-080) — **ولا يحكمُ على البرنامج** |
| `SEM051 SEM_ROUTE_HANDLER_HAS_PARAMS` | معالجُ المسارِ مُصرَّحٌ بمعاملاتٍ والعقدُ `void()` | قيدُ شكلِ واجهةٍ — والعددُ يمضي خانةً **عدديّة** |
| `SEM052 SEM_TUPLE_DESTRUCTURE_UNSUPPORTED` | `متغير (أ، ب) = …` | غيرُ مُعلَنٍ في `gr.decl.variable`، والخفضُ السابقُ كان **يكذب** |
| `SEM053 SEM_TYPE_METHOD_ABSENT_IN_COMPILER` | طريقةٌ معلَنةٌ على نوعٍ بلا ذراعِ توزيع | فجوةُ تغطيةٍ تُعلَن، لا تُصرَفُ بدلالةِ طريقةٍ أخرى |

> ⛔ **ورمزٌ سادسٌ وُلِدَ ثمّ حُذِف: `SEM_UNKNOWN_MEMBER`.** كان يقول «ليس عضوًا
> في أيِّ صنفٍ معرَّف» ويُنتقى بـ`fieldExistsInAnyClass`. ودحضتْه مراجعةٌ خصميّةٌ
> بالقياس: تلك الدالّةُ تقرأُ **جدولَ حقولِ الأصنافِ وحدَه**، فلا ترى `الطول`
> (طريقةٌ **مستقرّةٌ معلَنةٌ** في `type_methods.yaml`) ولا `نق` (حمولةُ تعدادٍ
> مُصرَّحةٌ في السطرِ نفسِه) ولا الوصولَ الآمنَ `؟.` في بذرةِ P1 مُعلَنُ مخرَجِها
> «OK». فكان يتّهمُ برامجَ **صحيحة**. والدرسُ أنّ **تحويلَ انهيارٍ إلى رفضٍ
> يبدو مقصودًا أسوأُ من الانهيار**: الانهيارُ يُقرأُ عطبًا، والرفضُ الكاذبُ
> يُقرأُ قاعدةَ لغة.

> ⚠️ **وSEM052 وُلِدَ من مراجعةٍ لا من قياس**: كانت هذه الحالُ تُبلَّغُ بـSEM049
> نفسِه وخانتُه `{got}` **مؤلَّفةً في الـ`.cpp`** بضمِّ نصٍّ عربيٍّ‑إنجليزيٍّ إلى
> الاسم — فحملَ كلُّ عرضٍ **لغةَ الآخَر**، وخرجَ نصُّ تشخيصٍ من خارجِ الكتالوج،
> وناقضَ تعليقًا فوقَه بأسطرٍ يقولُ «والنصُّ من الكتالوج». والمسحُ الآليُّ بعدَ
> العلاج: **صفرُ** سلاسلَ نصّيّةٍ عربيّةٍ خارجَ التعليقاتِ في الملفّاتِ الستّة.

الحارسُ في `oop_ops.cpp` **لم يُمَسّ** — نصُّه المُدوَّنُ يقولُ إنّ إسقاطَه
يحوّلُ إخفاقَ بناءٍ إلى كذبٍ صامتٍ يطبعُ صفرًا برمزِ نجاح. المتغيّرُ هو **ما
يُقالُ للمستعمِل**، و`fieldExistsInAnyClass` — التي كان حكمُها يُهدَرُ بين
رمزَين داخليَّين كلاهما يقولُ «أبلِغْ» — صارت هي الفاصلَ بين الرمزَين.
ورمزانِ لا خانةُ نصٍّ في الكود: كلُّ النصِّ يبقى في الكتالوجِ ثنائيَّ اللغةِ
مولَّدًا، ولا تدخلُ سلسلةٌ نصّيّةٌ واحدةٌ في شفرةِ المترجّم.

> ⚠️ **وأصدقُ ما كشفتْه الحملة**: أبسطُ برنامجٍ كان يُطلقُ «الانهيارَ الداخليَّ»
> لا علّةَ مترجِمٍ فيه أصلًا — «متغير س = "أبج"» ثمّ «س.الطول»، و«الطول» ليست
> عضوًا معلَنًا في مصدرِ الحقيقةِ (المعلَنُ دالّةٌ: «طول(س)»). فكان المترجّمُ
> يطلبُ من كاتبِ البرنامجِ أن يُبلِّغَ عن علّةٍ لا وجودَ لها.

##### أربعةُ صفوفٍ خرجت من `DECLARED_REDS.tsv`

اخضرَّت مقيسةً **والحارسُ هو من سمّاها**: `001_toplevel_yield_compiler` ·
`0015_tuple_destructure_compiler_crash` · `0016_tuple_destructure_in_function_crash` ·
`008_arr_pop`. ولم يُحذَفْ صفٌّ لأنّ انهيارَه سكتَ فحسب: الأربعةُ تُنتِجُ
المخرَجَ المطلوبَ أو الرفضَ المطلوب. السجلُّ **٤١ = ٤١** والحارسُ أخضر.

> 🔑 **أخطرُ سطرٍ في هذا الجدول هو الأوّل.** الأربعةَ عشرَ انهيارًا كانت
> **تُحسَبُ خضراءَ** قبلَ إصلاحِ المِقياس: العدّاءُ يحكمُ برمزِ الخروجِ وحدَه،
> والمترجّمُ ينهارُ داخليًّا ويرجعُ صفرًا ويُنتِجُ ثنائيًّا. أي أنّ **أداةَ
> القياسِ كانت تُخفي أعطابَ المحرّكِ الباقي**، وهو أخطرُ من أيِّ عطبٍ تُخفيه.

> ⚠️ والثلاثةُ في الصنفِ الرابع **بلا سندٍ في مصدرِ الحقيقة**: النحوُ يعلنُ ما
> يُقبَل ولا يعلنُ ما يجبُ رفضُه. فهي دعوى بذرةٍ لا قاعدةُ لغة، ولا تُعالَجُ
> بتغييرِ المترجّمِ بل بكتابةِ القاعدةِ أوّلًا — وهذا هو الترتيبُ في §٤‑د.

الترتيبُ سببيّ: **ج** تسبقُ **ب** تسبقُ **أ**. و**د** و**هـ** دَينُ *عقدٍ* لا
*تنفيذ*: بذرةٌ بلا عقدٍ تُشغَّلُ خضراءَ ولا تُثبِتُ شيئًا.

---

## ٢ · الحالةُ المقيسة **عند الانطلاق** (٣ أيلول ٢٠٢٦)

> 📌 هذا القسمُ **سِجلٌّ لا وصفُ حال**: يصفُ الشجرةَ *قبلَ* الحذفِ ويُحفَظُ
> كما هو. فالقياسُ الذي بُنيَ عليه القرارُ لا يُعادُ كتابتُه بعدَ تنفيذِه، وإلّا
> ضاعَ سندُ القرار. أمّا حالُ اليومِ فالعدّاداتُ في §١ والبوّاباتُ في §٦.

### الدَّين — بذورُ `@skip_compiler`

| المجموعة | العدد | الملاحظة |
|---|---:|---|
| **التصريحاتُ الحقيقيّة** (`^# @skip_compiler`) | **١١٢** | لا ١٢٠؛ العدُّ النصّيُّ الخامُّ يعطي **١٤٨** (كان ١٤٦ يومَ كُتب) لأنّه يبتلعُ ذكرَ الوسمِ في نثرِ التعليقات |
| منها في `tests/_archive/` | ٣٢ | **لا تُشغَّل**: `runner.py` جذرُه `tests/behavior` |
| **الحيّةُ فعلًا** | **٨٠** | |
| ├─ الشبكة `09_المكتبة_القياسية/05_شبكة` | ٣٤ | أكبرُ كتلةٍ مفردة |
| ├─ الاستيراد `10_الاستيراد/01_أساسي` | ٢٤ | |
| └─ متفرّقة | ٢٢ | إحداها (`وحدة_مصدر.ص`) وحدةٌ مساعِدةٌ لا اختبار ⇒ **٢١** عيبًا حقيقيًّا |

كتلتانِ تحملانِ **٥٨ من ٨٠**. الدَّينُ ليس مبعثرًا: هو مشروعانِ اثنانِ وذيل.

وسومٌ شقيقة: `@skip_interpreter` ٥٥ · `@known_red` ٤ · `@expect_compile_error` ٦٤ ·
`@expect_error_compiled` ٥٨. الخمسةُ والخمسونَ تتحرَّرُ مجّانًا بحذفِ المفسّر.

### الكتلةُ المحذوفة (نُفِّذت)

| المقياس | الرقم |
|---|---:|
| `interpreter/` | **١١٩** ملفًّا · **٦١٬٢١٣** سطرًا |
| منها `interpreter/src` | ٥٤٬٢٨٩ سطرًا |
| `INTERPRETER_SOURCES` في CMake | ٦٧ ملفًّا · ٤٢٬٤٠٦ سطرًا |

ومعها: `apps/sad-run/` · `tools/repl/` · `tools/wasm/` · `tools/profiler/` ·
`cmake/wasm.cmake` · `language-truth/tools/repl/` · `tests/unit/interpreter/`.

---

## ٣ · خرائطُ لا تُخمَّن (سِجلٌّ ما قبلَ الحذف)

خمسةُ مواضعَ كان اسمُها يخالفُ مضمونَها. قِيسَت فبُنيَ عليها الحذف، وقد نُفِّذَ:
`sad_interp` و`sad_core` و`sad_runtime` و`sad_graphics_bridge` و`ALL_SOURCES`
لم تعُدْ في الشجرة، و١٩ هدفَ GTest أُعيدَ توجيهُها إلى
`sad_shared sad_frontend sad_builtins sad_lowlevel sad_type_system`.
ويبقى القسمُ مكتوبًا لأنّ **سندَ القرارِ يُحفَظُ لا يُمحى**.

**١. `sad_interp` ليس المفسّر.** هو مبنيٌّ من `ALL_SOURCES`
(`cmake/sources.cmake:372`)، وفيها `${COMPILER_FRONTEND_SOURCES}` — ستّةُ ملفّاتِ
SIR (٤٬٦٧٧ سطرًا) من واجهةِ المترجمِ الأماميّة. و`sad_core` **كنيةٌ** له
(`cmake/libraries.cmake:27`). فمن ربطَ `sad_core` ربطَ المفسّرَ والواجهةَ معًا.

**٢. الملفّاتُ الستّةُ مترجَمةٌ مرّتين.** `compiler/CMakeLists.txt:42` يلتقطُها بـ
`file(GLOB "src/frontend/*.cpp")` أيضًا. فنزعُها من `ALL_SOURCES` **لا يكسِرُ**
`sad-build`.

**٣. `sad-build` لا يربطُ المفسّرَ أصلًا.** يربطُ `sad_compiler + sad_shared`
(`apps/CMakeLists.txt`). فحذفُ المفسّرِ **لن يُصغِّرَ** ثنائيَّ المترجم — من ادّعى
غيرَ ذلك فلم يقرأ سطرَ الربط.

**٤. أداتانِ فقط تعتمدانِ المفسّر**، لا خمس:

| الأداة | ما تربطُه | الحالة |
|---|---|---|
| `tools/repl` | `sad_core` … | ⛔ ضحيّةٌ بنيويّة — الحلقةُ التفاعليّةُ عملُ مفسّرٍ بطبعِها |
| `tools/lsp` | `sad_core` … | ⚠️ يحتاجُ نقلًا إلى الواجهةِ الأماميّة |
| `tools/check` | `sad_ownership`, `sad_semantic_shared`, `sad_shared` | ✅ حرٌّ سلفًا |
| `tools/analyze` | `sad_analyze_engine`, `sad_shared`, `sad_type_system` | ✅ حرٌّ سلفًا |

**٥. `x.py` يمنعُ الحذفَ عمدًا.** بوّابةُ البناءِ تبني `sad-run` و`sad-build`
**ذرّيًّا في التهيئةِ نفسِها** — وهذا حارسٌ مقصودٌ ضدّ تذبذبِ Debug/Release وضدّ
الثنائيِّ البائت. حذفُ هدفٍ منهما ليس حذفَ سطر: هو إعادةُ تصميمِ الحارس.

**ملاحظةٌ جانبيّةٌ مقيسة**: `README.md` يُعلِنُ ثلاثةَ مساراتِ تنفيذٍ منها «آلةٌ
افتراضيّة (Bytecode VM + JIT)» في `vm/`. **لا وجودَ لمجلّد `vm/` في الشجرة.**
المساراتُ اثنانِ لا ثلاثة.

---

## ٤ · المرجع — قاعدةٌ في اللغة، لا مُخرَجُ مُنفِّذ

**القاعدةُ الحاكمة**: ما يُطلَبُ من المترجمِ يُشتقُّ من **قاعدةٍ مُعلَنةٍ في
`language-truth/`**. لا من سلوكِ المفسّر، ولا من ثنائيٍّ مجمَّد، ولا من `@expected`
كتبَه أحدُهم يومًا لأنّ المحرّكَ أخرجَه هكذا.

مُنفِّذٌ يُتَّخَذُ مرجعًا يُثبِّتُ عللَه عقدًا. والمفسّرُ ليس المواصفة؛ هو تطبيقٌ له
أخطاؤه، وقد قِيسَت. فلا يُحفَظُ `sad-run.exe` مرجعًا، ولا يُشتقُّ منه توقُّع.

### ٤‑أ · برهانٌ مقيسٌ على أنّ المرساةَ تصنعُ الفرق

| مجموعةُ البذور | البذور | متخطّية | النسبة |
|---|---:|---:|---:|
| `rules_matrix` — مرساتُها **قاعدةُ لغةٍ** (`gr.stmt.break` …) | ٣٬٠٣٩ | ٥ | **٠٫١٦٪** |
| `sections` — مرساتُها موضوعٌ ومحرّك | ١٬٢٧٧ | ٦٨ | **٥٫٣٢٪** |
| `_regression` | ٩٣ | ٦ | ٦٫٤٥٪ |

> ⚠️ **العمودُ الأوّلُ هنا ملفّاتٌ، وفي §١ حالاتٌ يراها العدّاء** — ولذلك
> `sections` ١٬٢٧٧ هنا و١٬٢٧٦ هناك، و`_regression` ٩٣ هنا و٩٢ هناك. الرقمانِ
> صحيحانِ ويقيسانِ شيئَين، والفرقُ ملفٌّ في كلٍّ لا يُشغَّلُ حالةً مستقلّة.

**٣٣ ضعفًا.** المجموعةُ المربوطةُ بقاعدةٍ أنظفُ أمامَ المترجمِ ثلاثًا وثلاثين مرّةً
وهي أكبرُ بمرّتين ونصف. الدَّينُ يتراكمُ حيث لا قاعدةَ تحكم — لا حيث المترجمُ أضعف.

### ٤‑ب · لماذا لا يصلحُ مصدرُ الحقيقةِ مرجعًا **اليوم**

القاعدةُ صحيحةٌ، والبنيةُ قائمة، لكنّ عمقَ القواعدِ لا يكفيها بعد. مقيسٌ على
١٢٠٥ مدخلاتِ `language-truth/builtins/`:

| الحقل | مُعلَنٌ على | النسبة |
|---|---:|---:|
| اسم · فضاء · وحدة · صنف | ١٢٠٥ | ١٠٠٪ |
| `arity` | ٤٥٩ | ٣٨٪ |
| `returns` | ١٧٠ | ١٤٪ |
| `params` غيرُ فارغة | ١٥١ | ١٢٫٥٪ |
| **معاملاتٌ بنوعٍ مصرَّح** | **٢٠** في السطحِ كلِّه | — |
| `examples` | ٢٢ | ١٫٨٪ |
| سلوك · استثناءات · حدود · حالاتٌ حدّيّة | **٠** | **٠٪** |

و`language-truth/tests/` فيه ملفٌّ واحدٌ (`test_schema_validation.py`) وقائمةُ
`YAML_SCHEMA_PAIRS` فيه **فارغة**، فـ`parametrize` تجمعُ **صفرَ حالات**: الخانةُ
موجودةٌ ولم تُملأ قطّ. (وليس «مجلّدًا فارغًا» كما قيلَ هنا سابقًا — الفرقُ أنّ
البنيةَ قائمةٌ والمحتوى غائب.)

فالسطحُ النحويُّ **مواصَفٌ**: **١٠٧** قاعدةً مُعلَنةً (معرّفاتٍ فريدة) في
`language-truth/grammar/`، منها **١٠٣** لها مجلّدُ مرساةٍ في `rules_matrix`
(والمجلّداتُ ١٢٦، الفرقُ ٢٣ مجلّدَ تراكيبَ وتوليدٍ لا معرّفَ قاعدةٍ لها).

> ⚠️ **تصويبٌ مُدوَّن.** كان هنا «٢١٤ قاعدةً» و«١٢٦ مرساة». و٢١٤ = عدُّ سطورِ
> `id:` **مرّتين** (`lowers_to.yaml` يعيدُ المعرّفاتِ نفسَها)، و١٢٦ عدُّ مجلّداتٍ
> لا قواعد. أمرُ القياس: عدُّ المعرّفاتِ الفريدةِ في `grammar/*.yaml` باستثناءِ
> `lowers_to.yaml`، وتقاطُعُها مع أسماءِ مجلّداتِ `rules_matrix/*/`.

وحتّى النحوُ يعلنُ **ما يُقبَل** ولا يعلنُ **ما يجبُ رفضُه**: ولذلك جاءت أربعٌ من
«الحمراوات» بلا سندٍ في مصدرِ الحقيقة، واثنتانِ **ضدَّ** السند. أمّا سطحُ المكتبةِ
فـ**سجلُّ أسماء**: يقولُ إنّ `جذر` موجودةٌ في وحدةِ الرياضيّات، ولا يقولُ ماذا
تُرجِعُ لسالبٍ.

### ٤‑ج · التشخيصُ الموحَّد

> حيثُ للّغةِ قواعد (النحو): المترجمُ نظيفٌ ٩٩٫٨٤٪ والبذورُ مرساتُها قاعدة.
> وحيثُ لها سجلُّ أسماءٍ فقط (المكتبة): المترجمُ يغطّي ٥٣٪ (٦٤١ من ١٢٠٥)،
> و٨٥٪ من الدَّينِ هناك.

**القيدُ ليس المفسّر. القيدُ أنّ سطحَ المكتبةِ غيرُ مُواصَف.** حذفُ المفسّرِ لا
يُنشئُ المواصفةَ؛ كتابتُها هي التي تُنشئها. وبها يصيرُ للمترجمِ هدفٌ ليس
«ما كان يطبعُه المحرّكُ الآخر».

### ٤‑د · ترتيبُ العمل

1. **عمّق القاعدةَ قبل أن تطلبَ الذراع**: `arity` و`returns` وأنواعُ المعاملات
   لكلّ مدخلٍ يُقصَدُ تنفيذُه — ثمّ السلوكُ والحدودُ والاستثناءات.
2. **`language-truth/tests/` تُملأ**: حالةُ مطابقةٍ معلَّقةٌ بمعرّفِ قاعدة، لا ببذرةٍ
   يتيمة. كما فعلَ `rules_matrix` بالنحو.
3. **كلُّ بذرةٍ جديدةٍ تُرسى بقاعدة.** و`sections` تُهاجَرُ تدريجًا أو تُعادُ إرساؤها.
4. **لا `@expected` بلا سندٍ**: إن لم تكن خلفَه قاعدة، فهو رأيٌ لا عقد — تُكتَبُ
   القاعدةُ أوّلًا.

---

## ٥ · قواعدُ العمل

1. **لا `@expected` بلا سند.** إن لم تكن خلفَ التوقُّعِ قاعدةٌ في
   `language-truth/`، فهو رأيٌ لا عقد — تُكتَبُ القاعدةُ أوّلًا ثمّ يُطلَبُ
   التنفيذ. ولا يُشتَقُّ توقُّعٌ من مُخرَجِ مُنفِّذٍ أبدًا.
2. **كلُّ بذرةٍ تُرسى بمعرّفِ قاعدة.** النمطُ قائمٌ في `rules_matrix` ومقيسٌ
   أنظفَ ٣٣ ضعفًا؛ فلا تُضَفْ بذرةٌ يتيمةٌ إلى `sections`.
3. **لا حذفَ قبلَ نقلِ المستهلك.** أيّ هدفٍ أو أداةٍ تربطُ المحذوفَ تُنقَلُ أوّلًا،
   ويُبنى المستودعُ أخضرَ، ثمّ يُحذَف.
4. **كلُّ حذفٍ يقابلُه برهان.** لا يُغلَقُ عملٌ إلّا ببذرةٍ انتقلت من
   `@skip_compiler` إلى خضراءَ **مقيسة** — لا «ينبغي أن تعمل».
5. **العدّاداتُ الخمسةُ تُنشَرُ في كلِّ إيداع**، مُعادَ قياسِها لا منقولةً:
   `أ ٨٠ · ب ٥٦٤ · ج ١٠٣٥ · د ٣٣٠ · هـ ١٨٥ → …`. وأمرُ القياسِ يُذكَرُ معها.
6. **قِسْ، لا تُصرِّح.** حقلٌ في مصدرِ الحقيقةِ لا يقرؤه مستهلكٌ ليس عقدًا
   (`compiler_strategy` مثالٌ مقيس)، ووسمُ `@skip_compiler` دعوى: البرهانُ
   تشغيلُ المترجمِ عليها.
7. **بذرةُ ضابطٍ قبلَ أيّ نسبةِ أداء.** أرضيّةُ إقلاعِ العمليّةِ هنا ٧٠–١٢٠ms،
   وقد ابتلعت قياسًا كاملًا من قبل.
8. **العربيّةُ لغةُ التوثيقِ والتعليقِ والإيداع.**

---

## ٦ · البوّابات

```bash
python x.py build --config Release   # بناءُ الهدفَين معًا (حارسٌ ذرّيّ)
python x.py verify --config Release  # بصمةُ dist/ — يكشفُ الثنائيَّ البائت
python x.py gen --check              # حارسُ انجرافِ المولَّدِ عن language-truth/
python x.py test --config Release    # runner
```

`tests/runner.py` يعملُ اليومَ بـ**وضعِ المترجم-فقط**: `--compiler` وحدَه، بلا
`--interp`. ويُوجِّهُ السالبَ اليتيمَ إلى المترجمِ حين يحملُ رمزَ SoT أو لا يحملُ
نمطًا، ويتخطّاه مُعلَّلًا حين يحملُ نثرَ محرّكٍ لا مقابلَ له.

> 🔑 **وتمريرُ `--interp sad-build` يُبطِلُ هذا الوضعَ كلَّه.** فالعدّاءُ لا يدخلُه
> إلّا حين **يغيبُ** المفسّر، و`sad-build ملفّ.ص` يُترجِمُ ولا يُشغّل — فتُقارَنُ
> مخرَجاتٌ فارغةٌ بمخرَجاتٍ حقيقيّة. كانت ثلاثةَ عشرَ موضعًا في `ci.yml` تفعلُ
> ذلك، فكان كلُّ ما بُنيَ لهذا الوضعِ **شفرةً لا تُنفَّذُ في أيِّ بوّابة**.

### حرّاسٌ أُعيدَ إرساؤهم لا تقاعدُهم

| الحارس | كان مرجعُه | صارَ مرجعُه |
|---|---|---|
| `check_null_receiver_parity.py` | تكافؤُ المحرّكَين | القاعدة `RUN003` في الكتالوج |
| `nfr_gate.py` | زمنُ تفسيرِ برامجَ صغيرة | زمنُ **ترجمتِها** (مقيسٌ: تشتُّتُ التشغيل ١٠٤٪ مقابل ١٦٪ للترجمة) |
| `NFR_Gate` في CMake | `TARGET sad-run` | `TARGET sad-build` — وكان يسقطُ **صامتًا** |
| `check_retired_error_codes.py` | — (جديد) | حقلُ `retired` في الكتالوج، اتّجاهَين |

### قدراتٌ فُقِدَت — مُقيَّدةٌ لا مطويّة

> ✅ **سطحُ الشبكاتِ: انفرجَ بحذفِ ما لا يُمكِنُ كتابتُه.** كانت `شبكات.ص`
> تُعرّفُ أربعةَ أغلفةٍ (`عند_جلب` · `عند_إرسال` · `عند_استبدال` · `عند_حذف`)
> تُمرِّرُ المعالجَ **معاملًا** إلى مدمَجةِ تسجيلِ المسار. واستيرادُ الوحدةِ
> يُخفِضُ دوالَّها **كلَّها**، فكان `استورد شبكات` وحدَه يُنتِجُ أربعةَ تشخيصاتٍ
> ويُسقِطُ كلَّ مستورِدٍ ولو لم يمسَّ HTTP.
>
> 🔑 **والحاسمُ أنّ الأغلفةَ لم تكن ناقصةَ التنفيذِ بل غيرَ قابلةٍ
> للتمثيلِ أصلًا** — وهذا دحضٌ لِما كان مُدوَّنًا هنا من أنّ «إعادةَ صوغِ
> الأغلفة» أحدُ ثلاثةِ مخارج. مقيسٌ (مراجعةُ أميليا، ٣ أيلول): حتّى دالّةٌ
> **حرّةٌ** `دالة سجل(خادم، مسار، معالج)` تُمرّرُ المعالجَ تفشلُ بالرمزِ نفسِه.
> فالحرفيّةُ شرطُ **موضعٍ** — في موضعِ نداءِ المدمَجةِ نفسِه — لا شرطُ معرفةٍ
> بالقيمة، لأنّ الثانكَ `void()` يُبنى وقتَ التوليد.
>
> فحُذفت الأربعةُ، والتوثيقُ الذي كان يضربُ المثلَ بـ`لامدا(طلب، رد)` — ويخالفُ
> العقدَ في موضعَين معًا (لامدا لا اسمٌ حرفيّ، ومعاملانِ والعقدُ `void()`) —
> استُبدلَ بالصيغةِ العاملةِ المقيسة. والأثرُ: **`sections` ٢٠ ← ١٠ إخفاقًا**،
> وتسعُ بذورِ مقابسِ ويبٍ و`080_end_to_end_udp_oop` صارت **خضراء** — وهي التي
> كانت تسقطُ وهي **لا تمسُّ مساراتِ HTTP إطلاقًا**.
>
> والباقي دَينٌ مُسمًّى: **المعالجُ غيرُ المباشرِ (متغيّر · معامل · لامدا)
> يلزمُه ترامبولينُ وقتِ تشغيلٍ يعرفُ اتّفاقيّةَ المُغلَقات** — وهو ما يقولُه
> تلميحُ SEM049 نفسُه. وقد صارَ للقيدِ **بذرةٌ تقيسُه لأوّلِ مرّة**
> (`050_route_handler_not_literal.ص`): قبلَها لم تكن في المستودعِ كلِّه بذرةٌ
> واحدةٌ تمسُّ SEM049، فكان رمزًا يُبعَثُ ولا يُحاكَم.
>
> ⛔ **والخفضُ الكسولُ رُفِضَ بفخٍّ مقيسٍ لا بحدس**: `collectReferencedNamesFromExpr`
> عندَ `MemberExpr` **يزورُ الكائنَ لا اسمَ العضو**، و`استورد م` يفتحُ فضاءَ
> أسماءٍ فـ`م.اسم()` تُنتِجُ `م` وحدَها ويسقطُ `اسم` من بذرةِ الإغلاق ⇒ رمزٌ لا
> يُبنى ⇒ خطأُ رابطٍ إنجليزيّ. وذلك على **٣٧٧ بذرةً** تستعملُ `استورد` المجرَّدة.

| القدرة | لماذا | البديل |
|---|---|---|
| ٤٩ اختبارَ `sad-repl` (أنابيب · توجيه · آبلتات) | الأداةُ محذوفة | لا بديلَ اليوم — تلزمُ صدفةٌ مبنيّةٌ على المترجم |
| طرفُ المصنعِ في `ui_props_consistency` | لا نظيرَ له في المترجم | ٤٦ إعلانَ `primary_prop` بلا طرفٍ ثانٍ |
| دخانُ `sad-profiler` | الأداةُ محذوفة | — |
| زمنُ تشغيلِ الثنائيِّ المُنتَج في NFR | يقيسُ إقلاعَ العمليّةِ لا المترجم | يلزمُه برامجُ حِملٍ وعتباتٌ منفصلة |
| زمنُ تشغيلِ أندرويد | كان يشحنُ المفسّر | موقوفٌ خلفَ `FATAL_ERROR` مُسمّى، ووظيفتُه في CI `if: false` بدلَ خُضرةٍ بلا APK |
| `sad run` و`sad test` | تنفيذُهما شفرةٌ ميّتةٌ مُصرَّفة (`CommandManager` لا يُنشَأ) | `sad build x.ص -o x.exe` ثمّ `./x.exe` · و`tests/runner.py` للاختبار |
| ١١ عَلَمَ CLI `engines: [interpreter]` | لا ثنائيَّ يقرؤها | مُعلَنةٌ في `cli_flags.yaml` بدَينٍ مُقيَّد — وأيُّها يعرضُه `sad-build` قرارُ سطحِ لغةٍ لم يُحسَم |
| نطاقُ `gen_tool_sot.py` | `repl` كان مستهلكَه الوحيد | الآليّةُ باقيةٌ بصفرِ أدواتٍ مُسجَّلة — مُقيَّدٌ في `language-truth/tools/README.md` |
| ~~مكتبةُ الشبكاتِ كلُّها~~ **انفرجت** | تسجيلُ المسارِ بمعالِجٍ غيرِ مباشر | حُذفت الأغلفةُ الأربعة ⇒ `استورد شبكات` يُترجَمُ ويعمل. والباقي: معالِجٌ في متغيّرٍ أو معاملٍ أو لامدا يلزمُه ترامبولينُ وقتِ تشغيل — **مقيسٌ ببذرةٍ سالبةٍ واحدة** |
