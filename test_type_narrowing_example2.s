# مثال 2: فحص نوع القيمة (Type Check)
# Example 2: Type Check using typeof

دالة معالجة_قيمة(قيمة)
    نوع_القيمة = نوع(قيمة)
    
    إذا (نوع_القيمة == "رقم")
        # هنا القيمة من نوع رقم
        # Value is a number
        نتيجة = قيمة * 2
        عرض("القيمة رقم: ")
        عرض(نتيجة)
        إرجاع نتيجة
    وإلا
        # هنا القيمة من نوع آخر (نفترض نص)
        # Value is another type (assume string)
        عرض("القيمة نص: ")
        عرض(قيمة)
        نتيجة_نص = قيمة + " - معالج"
        إرجاع نتيجة_نص
    نهاية
نهاية

دالة رئيسية()
    عرض("=== مثال 2: فحص نوع القيمة ===")
    
    # اختبار مع رقم
    عرض("اختبار 1: رقم")
    ر1 = معالجة_قيمة(42)
    عرض("النتيجة 1: ")
    عرض(ر1)
    
    # اختبار مع نص
    عرض("اختبار 2: نص")
    ر2 = معالجة_قيمة("مرحبا")
    عرض("النتيجة 2: ")
    عرض(ر2)
نهاية
