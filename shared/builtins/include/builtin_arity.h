// ============================================================================
// (AR) shared/builtins/include/builtin_arity.h — غلافٌ لجدولِ رتبِ المدمجات
//      مُولَّد تلقائياً من language-truth/builtins/*.yaml (حقل `arity`).
//      لإجراء تعديل: عدّل YAML ثم أعد البناء.
//
// (EN) Wrapper for the generated builtin arity table (SoT field `arity`).
//      To modify: edit YAML then rebuild.
// ============================================================================

#pragma once

#include "../generated/builtin_arity_generated.h"
