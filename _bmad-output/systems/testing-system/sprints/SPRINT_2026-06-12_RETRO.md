---
title: "SPRINT RETRO — نظام الاختبارات الموحّد (EPIC-TEST-01)"
date: 2026-06-12
sprint: "testing-system — الملحمة الوحيدة"
status: CLOSED
---

# RETRO — إغلاق ملحمة نظام الاختبارات الموحّد

> الملحمة EPIC-TEST-01 اكتملت: **7/7 قصص منجَزة**. هذا RETRO إلزامي قبل أي عمل
> جديد على النظام (GR-03).

## ما أُنجز (بأدلة — GR-01)

| القصة | الناتج المقيس |
|---|---|
| TEST-001 | جرد 51 مجلداً + baseline **83.2%** مصنّف + كشف خطأ مترجم حقيقي |
| TEST-002 | هيكل الطبقات الثلاث + رفع المشغّل للجذر (موقع مفصول عن المحتوى) |
| TEST-007 | وضع التطبيع + مصنّف `--classify` + بوّابة `--gate` → **83.2%→86.6%** · unit 10/10 |
| TEST-003 | ترحيل 452 ملفاً → behavior/sections + أرشفة 380 قديماً · 0 تراجع |
| TEST-004 | دمج طبقة الوحدة (~135 ملفاً) + إصلاحا ربط جذريان · 52 ناجحاً (+11) |
| TEST-005 | طبقة system + بوّابة NFR · جذر tests = **5 مجلدات** (كان 51) |
| TEST-006 | ربط CI هرمي + بوّابة قرار + burn-in + nightly |

**الهدف الأكبر تحقّق:** 51 مجلداً مبعثراً → بنية ثلاث طبقات (unit/behavior/system) +
أرشيف، بمشغّل موحّد وبوّابة قرار، وتغطية قابلة للتدقيق.

## ما سار جيداً

- **القرار القائم على المخاطر (Murat/ADR-004)** أثبت قيمته: مخاطرة الدرجة 9 (رفرفة
  التزامن) عُولجت قبل الترحيل، فمرّ ترحيل التزامن بلا رفرفة.
- **التحقق بالأدلة** كشف أخطاءً حقيقية (خطأ المترجم `لاشيء`→بِتّات double) بدل تقنيعها.
- **الترحيل التدريجي** (git mv + بوّابة عدم تراجع بعد كل خطوة) حافظ على 0 تراجع.

## ما تعلّمناه (دروس)

1. **الـ commits المحلية هشّة أمام إعادة ضبط الفرع على origin** — فُقد عمل مرتين
   بسبب `git reset --hard origin/graphic` خارجي. الدرس: العمل غير المدفوع/الملتزَم
   عبر حدود الجلسات عُرضة للضياع. (الوثائق في `_bmad-output` نجت لأنها مُتجاهَلة.)
2. **الحجم ≠ جودة التغطية** — `09_المكتبة` أكبر قسم وأكثرها فشلاً (تصحيح RISK_DRAFT).
3. **«فضّل المستويات الأدنى»** — دفع منطق parser/types للوحدة كشف عللاً كانت مخفية في E2E.

## ديون متبقّية (قصص مرشّحة مستقبلية)

- **5 علل تكافؤ حقيقية في التزامن** (057/058/089/091/099: `لاشيء`→بِتّات double) — تحتاج
  قصة إصلاح في فريق المترجم (SIR/LLVM، خارج نطاق testing-system).
- **استدلال الأنواع بلا اختبارات وحدة** (فاحص الأنواع) — فجوة موثّقة في TEST-004.
- **ParserTests معطّل** كان مخفياً، أصبح مرئياً — يحتاج إصلاح أو حذف موثّق.

## القرار

الملحمة **مُغلقة**. النظام انتقل من PROPOSAL إلى **ACTIVE**. الديون أعلاه تُدار كقصص
مستقلة عند الحاجة، لا تَحجز إغلاق الملحمة.
