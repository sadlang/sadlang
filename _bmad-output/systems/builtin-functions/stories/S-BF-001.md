---
id: S-BF-001
title: "YAML الكامل: Core + TypeCtor + Concurrency"
type: implementation-story
epic: Epic-BF-001
status: in-progress
estimate: S
date: 2026-06-06
---

# S-BF-001 — YAML: Core + TypeCtor + Concurrency

## القصة
بصفتي **مطوّر اللغة**، أريد **ملفات YAML كاملة الحقول لـ 13 دالة أساسية** (Core×3 + TypeCtor×4 + Concurrency×6) حتى **يستطيع `gen_builtins_registry.py` توليد الـ header منها**.

## معايير القبول
- **Given** `language-truth/builtins/core.yaml` موجود بحقول ناقصة
- **When** نُكمِل الحقول: `cpp_id`, `category`, `module`, `compiler_strategy`, `since`, `status`
- **Then** كل إدخال يطابق الـ schema المحدد في `_schemas/builtin_function.schema.json`

## المهام
- [x] `core.yaml`: 5 دوال (اطبع، اطبع_سطر، اقرأ، طول، نوع) — موجودة لكن ناقصة الحقول
- [ ] إضافة `cpp_id`, `category: CORE_IO`, `require_import: false`, `compiler_strategy: INLINE`
- [ ] `type_ctor.yaml`: 4 دوال (رقم، عشري، نص، منطقي) — إضافة `category: TYPE_CONSTRUCTOR`
- [ ] `concurrency.yaml`: 6 دوال — إضافة `category: CORE_IO`, `require_import: false`
- [ ] تحديث `_schemas/builtin_function.schema.json` لإلزام `cpp_id` + `category`
- [ ] اختبار التحقق: `pytest language-truth/tests/test_schema_validation.py`

## المصدر في الكود
- `shared/builtins/include/builtin_registry.h` (الأسطر 878-910) — الأعداد المرجعية
- `shared/builtins/include/builtin_categories.h` — قيم Category و CompilerStrategy

## تعريف "تم"
كل إدخال في الملفات الثلاثة يحوي: `cpp_id` + `canonical` + `namespace` + `category` + `require_import` + `module` + `compiler_strategy` + `since` + `status`.
