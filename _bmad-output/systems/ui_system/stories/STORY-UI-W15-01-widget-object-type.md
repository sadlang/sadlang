---
storyId: STORY-UI-W15-01
epic: EPIC-UI-W15
title: "تصنيف WidgetBuilder ككائن في نوع()"
status: READY
priority: P0
date: 2026-06-08
author: Amelia (bmad-agent-dev)
estimate: "3 ساعات"
---

# STORY-UI-W15-01 — تصنيف WidgetBuilder ككائن في `نوع()`

## القصة
بصفتي مطوّر تطبيقات بلغة ص، أريد أن يُرجع `نوع(زر("x"))` القيمة `"object"` (لا `"مجهول"`)،
حتى أتمكن من التحقق من نوع العناصر والتعامل معها كقيم من الدرجة الأولى.

## السياق التقني (من الكود)
- `WidgetBuilder` يرث `Data::ObjectInstance` بِـ `CLASS_NAME` وهمي (`widget_builder.h:251`).
- الدالة المدمجة `نوع()` تصنّف القيمة بناءً على `ValueType`/`getClassName`؛ حالياً
  `WidgetBuilder` يسقط في فرع «مجهول» بدل «object».
- **الدليل:** `نوع(زر("اختبار"))` → `"مجهول"` (تحقق وظيفي 2026-06-08).

## معايير القبول
- AC-1: `نوع(زر("x")) == "object"` يعود `صحيح`.
- AC-2: `نوع(عمود)` و`نوع(حقل_نص("h"))` ترجع `"object"` أيضاً.
- AC-3: `نوع()` لكائن صنف عادي (`جديد نقطة()`) يبقى `"object"` دون تغيير (لا تراجع).
- AC-4: `نوع()` للأنواع البدائية (رقم/نص/منطقي/مصفوفة) لا يتأثر.

## المهام/المهام الفرعية
- [ ] T1: تتبّع مسار `نوع()` في `interpreter/src/builtins/` وتحديد فرع تصنيف `ObjectInstance`.
- [ ] T2: التعرّف على `WidgetBuilder` عبر `isWidgetBuilder()` (موجود في `widget_builder.h:284`) وإرجاع `"object"`.
  - [ ] اختبار وحدة: `نوع(زر(...)) == "object"`.
- [ ] T3: فحص أن جميع العناصر الـ15 (عمود/صف/.../فاصل) تُصنّف ككائن.
  - [ ] اختبار وحدة لكل فئة (تخطيط/عرض/إدخال/هيكل/فراغ).
- [ ] T4: اختبار تراجع: تصنيف الأصناف العادية والبدائيات سليم.
- [ ] T5: تشغيل `test_event_system.ص` والتأكد من مرور فحوص «تم الإنشاء».

## ملاحظات
- صلّح في طبقة `نوع()` (الدلالة) لا في `WidgetBuilder` (BF-10 الطبقة الصحيحة).
- لا تكسر `getClassName()` لأنه يُستخدم في `isWidgetBuilder` (BF-15).

## Dev Agent Record
*(يُملأ أثناء التنفيذ)*

## File List
*(يُحدّث بعد كل مهمة)*
