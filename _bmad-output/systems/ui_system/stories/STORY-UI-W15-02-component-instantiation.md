---
storyId: STORY-UI-W15-02
epic: EPIC-UI-W15
title: "تسجيل واجهة كصنف قابل للإنشاء"
status: DONE
priority: P0
date: 2026-06-08
author: Amelia (bmad-agent-dev)
estimate: "5 ساعات"
---

# STORY-UI-W15-02 — تسجيل `واجهة` كصنف قابل للإنشاء

> **ملاحظة تحديث (2026-06-18):** الكلمة المعتمدة لتعريف المكوّن هي `واجهة`
> (لا `واجهة_تصريحية`)، والإنشاء بالنداء المباشر `المكوّن()` (يُوجَّه داخليًا
> عبر `visitNewExpr`)، لا بكلمة `جديد`. راجع قرار الكلمة الواحدة في §4.5 من
> `status/implementation_status.md`.

## القصة
بصفتي مطوّر تطبيقات، أريد إنشاء كائن من مكوّن `واجهة_تصريحية` عبر `جديد المكوّن()`،
حتى أربط حالته وأستدعي دوال البناء عليه (مثل أي صنف).

## السياق التقني (من الكود)
- `parseUIDeclaration` ينتج `UIDeclarationNode` بحقول: `name`, `parentName`, `stateDecls`, `methods`.
- المفسر **لا يسجّل** `UIDeclarationNode` في سجل الأصناف، لذا `جديد المكوّن()` يفشل بـ
  «متغير غير معرّف: جديد» (SEM001) — **الدليل:** `test_event_system.ص:132`.
- منطق إنشاء الأصناف موجود في `statement_executor_oop.cpp`.

## معايير القبول
- AC-1: `متغير م = جديد مكوّن()` لمكوّن `واجهة_تصريحية نموذج_أحداث` ينجح دون SEM001.
- AC-2: `نوع(م) == "object"`.
- AC-3: حقول `@حالة` تُهيّأ بقيمها الابتدائية (`@حالة عداد = 0` → `م.عداد == 0`).
- AC-4: دوال المكوّن قابلة للاستدعاء (`م.عند_زيادة()` تعمل وتعدّل الحقل).
- AC-5: الوراثة `يرث أب` تُحترم (المكوّن يرث حقول/دوال الأب).

## المهام/المهام الفرعية
- [x] T1: عند تنفيذ `UIDeclarationNode`، تسجيله في سجل الأصناف كصنف معادل.
  - [x] تحويل `stateDecls` (@حالة) إلى حقول، و`methods` إلى دوال الصنف.
  - [x] اختبار: `عداد()` لا يرمي SEM001 (إنشاء بالنداء المباشر).
- [x] T2: تهيئة حقول `@حالة` بقيمها الابتدائية في الباني الضمني.
  - [x] اختبار: `ع.عدد == 5` بعد الإنشاء (test 012).
- [x] T3: ربط دوال المكوّن (بما فيها `بناء()`) كدوال صنف قابلة للاستدعاء.
  - [x] اختبار: `أ.زِد()` ثم `أ.عدد == 1` (test 013).
- [x] T4: دعم الوراثة `يرث` (إعادة استخدام آلية وراثة الأصناف القائمة).
  - [x] اختبار: `عداد_مزدوج يرث عداد` يصل لدالة الأب `زِد` (test 013).
- [x] T5: التحقق عبر اختبارات السلوك 012/013 (إنشاء + حالة + دوال + وراثة).

## ملاحظات
- أعد استخدام آلية الأصناف القائمة (`statement_executor_oop.cpp`) بدل بناء مسار جديد (CW-19/CW-20).
- `@حالة` في هذه الستوري تُعامل كحقل عادي مُهيّأ؛ التفاعلية (إعادة البناء) في STORY-03.

## Dev Agent Record
**النتيجة (2026-06-18):** الستوري مُحقَّقة بالكامل — أصبحت عاملة بعد إصلاح P0-1
(وسم الكائن بنوع صنفه في `value.cpp`) واعتماد `واجهة` كلمةً مفتاحية تُوجَّه إلى
`parseUIDeclaration`. التسلسل: `عداد()` → `visitNewExpr` → (صنف موجود) تهيئة الحقول
واستدعاء الباني → `markConstructed` → القيمة كائن.

**معايير القبول:** AC-1 (إنشاء بلا SEM001) ✓ · AC-2 (`نوع=كائن`) ✓ ·
AC-3 (تهيئة @حالة) ✓ · AC-4 (دوال قابلة للاستدعاء وتُعدِّل الحقول) ✓ ·
AC-5 (الوراثة `يرث`) ✓.

**الدليل (GR-01):**
- `interpreter/src/visitors/statement_executor_oop_struct_test.cpp:503` —
  `visitUIDeclaration` يسجّل الصنف (`isUIComponent=true`, `uiStateFields`, الحقول، الدوال).
- `interpreter/src/visitors/expression_evaluator_calls_dispatch.cpp:428-443` —
  النداء المباشر `المكوّن()` يُوجَّه إلى `visitNewExpr`.
- اختبار 012: `نوع(ع)=كائن`, `ع.عدد=5`.
- اختبار 013: `أ.زِد()→1`, `عداد_مزدوج يرث عداد` → `ب.زِد_مرتين()→2`.

## File List
- `tests/behavior/grammar_gaps/كائني/012_ui_decl_instantiation.ص` (إنشاء + قراءة @حالة)
- `tests/behavior/grammar_gaps/كائني/013_ui_decl_methods_inherit.ص` (دوال + وراثة) — جديد
