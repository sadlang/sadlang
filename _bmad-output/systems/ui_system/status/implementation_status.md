---
title: "حالة تنفيذ نظام الواجهات (SadUI) — مقابل الكود الفعلي"
date: 2026-06-08
status: ACTIVE
author: Amelia (bmad-agent-dev)
methodology: "قياس مباشر (wc -l + فحص ملفات) — لا تشغيل اختبارات ولا بناء"
auditableClaim: "كل نسبة/حالة مرفقة بعدد أسطر وملفات قابلة للتدقيق (GR-01)"
---

# حالة تنفيذ نظام الواجهات (SadUI)

> **التزام GR-01:** لا يُذكر «منفّذ» إلا بدليل (ملف + عدد أسطر). الأرقام من `wc -l`
> بتاريخ 2026-06-08. هذا **ليس** تقرير اختبار — لم يُبنَ المشروع ولم تُشغَّل اختبارات؛
> الحالة تعكس **وجود الكود وحجمه** لا نجاحه الوظيفي.

> **🔗 ربط RFC (مضاف 2026-06-18):** خطّة تطوير هذا النظام معتمَدة علناً كـ **RFC-0001**
> (`sadlang/rfcs`، PR #1 مدموج)، وتتبُّع تنفيذها في **Issue #2**. الجذور السطرية للفجوات
> أدناه (P0 «نوع()=أي»، `واجهة` غير قابلة للإنشاء، فشل `sadc`) محقَّقة بالتشغيل وموثّقة في
> [`CODE_PATH_REPORT_2026-06-17.md`](./CODE_PATH_REPORT_2026-06-17.md) —
> مثلاً الجذر الدقيق لـP0-1 في `shared/types/src/value.cpp:217-218`.

## 1. ملخص الطبقات

| الطبقة | الحالة | الدليل (أسطر/ملفات) |
|--------|--------|---------------------|
| المحلل النحوي (UI) | ✅ منفّذ | `parser_ui.cpp` (1029) + `parser_ui_maps.cpp` (1204) = 2233 سطر |
| نواة sad_ui (core) | ✅ منفّذ | 28 ملف `.cpp` = 10790 سطر |
| جسر المفسر (interpreter/src/ui) | ✅ منفّذ | 6529 سطر |
| باطن سطح المكتب (SDL2+OpenGL) | ✅ منفّذ | 6 ملفات = 6669 سطر |
| باطن الويب (HTML/CSS/JS) | ✅ منفّذ | 8 ملفات = 4082 سطر |
| باطن iOS (SwiftUI + Metal) | ✅ منفّذ | 5 ملفات = 2752 سطر |
| باطن أندرويد (JNI Views) | 🟡 جزئي | 5 ملفات C++ = 903 سطر + Java factory |

## 2. تفصيل الباطن لكل منصة

### سطح المكتب — ✅ (6669 سطر)
```
renderer.cpp        2207   ← رسم العناصر
window.cpp          2255   ← النافذة وحلقة الأحداث
renderer_draw.cpp    712   ← عمليات الرسم منخفضة المستوى
text_editing.cpp     555   ← تحرير النص
system_bridge.cpp    540   ← جسر النظام (صوت/مستشعرات)
renderer_canvas.cpp  400   ← لوحة الرسم المخصصة
```
يُفعّل بـ `SAD_UI_USE_SDL2` (cmake/sdl2_platforms.cmake).

### الويب — ✅ (4082 سطر)
```
web_renderer.cpp                  1017
html_element_advanced_codegen.cpp  637   ← عناصر متقدمة
system_bridge.cpp                  580
html_element_codegen.cpp           567
html_css_codegen.cpp               457
html_script_codegen.cpp            417   ← توليد JS تفاعلي
html_style_codegen.cpp             286
html_codegen.cpp                   121   ← المنسّق الرئيسي
```

### iOS — ✅ (2752 سطر) ⚠️ تصحيح
```
swiftui_codegen.cpp   1645   ← توليد كود SwiftUI من IR
ios_renderer.mm        664   ← عارض أصلي (Objective-C++)
system_bridge.cpp      443
```
> **تصحيح حوكمي:** ادّعت تقارير سابقة أن iOS «هيكل فقط» — القياس يُظهر تنفيذاً جوهرياً
> (مولّد SwiftUI كامل + عارض Metal/UIKit). صُحّح في `../planning/docs/نظام_الواجهات_الرسومي_ص.md`.

### أندرويد — 🟡 جزئي (903 سطر C++ + Java)
```
sad_app_main.cpp        323   ← نقطة الدخول
sad_android_runtime.cpp 275   ← وقت التشغيل
native_ui_builder.cpp   198   ← بناء Views أصلية عبر JNI
sad_android_bridge.cpp   70
native_ui_storage.cpp    37
```
+ Java: `SadViewFactory.java`, `SadEngine.java`, `MainActivity.java`, `SadViewFactory`.
يدعم خصائص: text, fontSize, color, padding, backgroundColor, alignment, spacing,
elevation, bold/italic, checked, value, opacity, visible. يُبنى بـ `build_apk.ps1`.
**سبب التصنيف «جزئي»:** حجم باطن أندرويد أصغر بكثير من باقي المنصات (903 مقابل 4000–6600)،
وقائمة الخصائص المدعومة في `SadViewFactory` محدودة — يلزم تدقيق تغطية العناصر الـ15.

## 3. القدرات النحوية (من المحلل)

| القدرة | الحالة | الدالة |
|--------|--------|--------|
| إعلان `واجهة_تصريحية` + وراثة | ✅ | `parseUIDeclaration` |
| حالة `@حالة/@ربط/@بيئة/@محسوب` | ✅ | `parseUIStateDecl` |
| تعبير عنصر + وسائط مسماة/موضعية | ✅ | `parseWidgetExpression(Typed)` |
| سلسلة معدّلات | ✅ | `parseModifierChain` |
| أحداث (سهم/كتلة/لامدا) | ✅ | `parseUIEventHandler` |
| رسم شرطي `إذا/وإلا` | ✅ | `parseUIConditional` (ADR-UI-01) |
| حلقات `لكل/بينما` | ✅ | `parseUILoop` (ADR-UI-01) |
| 15 عنصراً أولياً + 55+ مُهملاً | ✅ | `knownWidgets`/`deprecatedWidgets` (ADR-UI-02) |

## 3.5 نتائج التحقّق الوظيفي (P0 — 2026-06-08)

بُني المفسر (`cmake --build build --config Debug --target sad`) وشُغّل
`tests/integration/test_event_system.ص`. **النتيجة: التحليل النحوي يعمل، لكن تكامل
التنفيذ ناقص.**

| الاختبار | النتيجة | التفسير |
|----------|---------|---------|
| تحليل `=>` و`لامدا` للأحداث | ✅ نجح | المحلل يقبل الصيغ الثلاث |
| تحليل حاويات متداخلة بأحداث | ✅ نجح | شجرة الأبناء تُبنى |
| `نوع(زر(...)) == "object"` | ❌ فشل | يرجع `"مجهول"` لا `"object"` |
| `نوع(حقل_نص(...).عند_التغيير(...))` | ❌ فشل | نفس السبب |
| `جديد نموذج_أحداث()` لمكوّن `واجهة_تصريحية` | ❌ خطأ دلالي SEM001 | «متغير غير معرّف: جديد» — المكوّن غير مسجّل كنوع قابل للإنشاء |

**الأدلة (مخرجات فعلية):**
```
النوع: مجهول                    ← نوع(زر("اختبار"))
النوع بعد المعدّل: مجهول         ← نوع(زر(...).عند_النقر(لامدا...))
❌ error [SEM001]: ...:132:15   متغير غير معرّف: جديد
   132 │ متغير نموذج = جديد نموذج_أحداث()
```

**الاستنتاج الجذري:** طبقة المحلل النحوي (Parser) مكتملة، لكن:
1. تعبيرات العناصر (`زر(...)`) تُقيّم في المفسر إلى قيمة بنوع `"مجهول"` بدل كائن واجهة حقيقي.
2. مكوّنات `واجهة_تصريحية` غير مربوطة بالطبقة الدلالية كأنواع قابلة للإنشاء عبر `جديد`.

هذا يؤكد ملاحظة §"المنهجية": **حجم الكود الكبير ≠ تكامل وظيفي**. الباطن موجود (آلاف الأسطر)
لكن جسر «المفسر ↔ شجرة UI» غير مكتمل.

## 4. الفجوات والمخاطر

1. **🔴 جسر المفسر↔UI ناقص (مؤكَّد وظيفياً):** تعبيرات العناصر تُقيَّم لنوع `"مجهول"`،
   ومكوّنات `واجهة_تصريحية` لا تُنشأ بـ `جديد` (خطأ SEM001). أهم فجوة.
2. **أندرويد ناقص العمق لا التغطية:** يغطي الـ15 إنشاءً (`SadViewFactory` 87 case)، لكن
   عمق الخصائص أقل من باقي المنصات — انظر [`platform_support_matrix.md`](platform_support_matrix.md).
3. **أمثلة قديمة محذوفة:** أُزيلت من `examples/`؛ يلزم كتابة أمثلة جديدة بالصيغة المعتمدة.
4. **ADRs 03–06:** مذكورة ضمنياً في الكود دون نص صريح — غير موثّقة (GR-01).

## 5. التوصية

| الأولوية | الإجراء |
|---------|---------|
| P0 ✅ | ~~بناء المفسر + تشغيل `test_event_system.ص`~~ — **تم 2026-06-08**؛ كشف فجوة جسر المفسر↔UI (§3.5) |
| P0 🔴 | إصلاح تقييم تعبير العنصر في المفسر ليُرجع كائناً (لا `"مجهول"`) + تسجيل `واجهة_تصريحية` كنوع قابل لـ `جديد` |
| P1 | تدقيق تغطية العناصر الـ15 في باطن أندرويد |
| P2 | كتابة أمثلة `examples/` جديدة بالصيغة المعتمدة |

---
*قياس مباشر للكود 2026-06-08 — Amelia. للتفاصيل: `../planning/docs/نظام_الواجهات_الرسومي_ص.md`.*
