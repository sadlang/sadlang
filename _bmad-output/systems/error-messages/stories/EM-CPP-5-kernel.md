# Story EM-CPP-5 — ترحيل kernel

**Epic**: [EPIC-EM-CPP](../epics/EPIC-EM-CPP-MIGRATION.md) | **النقاط**: 5 | **الأولوية**: منخفضة | **الحالة**: محجوبة (تبعية EM-CPP-1)
**التبعيات**: EM-CPP-0 + EM-CPP-1 | **المُسنَد إلى**: TBD

---

## الهدف
ترحيل **42 موقع رمي خام**:
| الملف | المواقع |
|------|:------:|
| `builtin_kernel_cpu.cpp` | 41 |
| `builtin_kernel_acpi.cpp` | 1 |

## ملاحظة خاصة
أخطاء النواة/العتاد (CPU/APIC/ACPI) — قد لا يكون لها رموز `ErrorCode` مطابقة (النظام صُمّم للغة
لا للنواة). خياران لكل موقع:
1. تعيين رمز عام مناسب (`RUN_*`/`INT_*`).
2. أو إضافة فئة/رموز نواة جديدة لـ YAML إن لزم (قرار مع المالك — قد تكون أخطاء النواة منخفضة الأولوية).

## التنفيذ
- [ ] جرد الـ42 → تقييم: هل تستحق رموزاً مخصّصة أم رمز عام؟
- [ ] استبدال بـ`ctx.error` (التوقيع الجديد BuiltinContext) + بناء + `grep = 0`.

## معايير القبول
- **0 رمي خام** في الملفين · بناء أخضر · لا تراجع.

## تعريف "تم"
kernel مُرحَّل (42→0). **ملاحظة:** الأولوية منخفضة — يمكن تأجيله بعد الوحدات اللغوية الأساسية.
