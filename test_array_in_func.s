# اختبار مصفوفة فارغة داخل دالة بمعاملات افتراضية

دالة مصفوفة اختبار_مصفوفة_في_دالة(رقم حجم = 3)
    اطبع("داخل الدالة، حجم =", حجم)
    مصفوفة نتيجة = []
    اطبع("بعد إنشاء المصفوفة:", نتيجة)
    
    رقم ع = 0
    بينما (ع < حجم)
        اطبع("قبل أضف، ع =", ع، "، نتيجة =", نتيجة)
        أضف(نتيجة، ع * 10)
        اطبع("بعد أضف، نتيجة =", نتيجة)
        ع = ع + 1
    نهاية
    
    إرجاع نتيجة
نهاية

اطبع("=== اختبار 1: استدعاء بدون معاملات ===")
مصفوفة ق1 = اختبار_مصفوفة_في_دالة()
اطبع("النتيجة المُرجعة:", ق1)

اطبع("\n=== اختبار 2: استدعاء مع معامل ===")
مصفوفة ق2 = اختبار_مصفوفة_في_دالة(2)
اطبع("النتيجة المُرجعة:", ق2)
