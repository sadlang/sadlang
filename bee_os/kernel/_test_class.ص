# اختبار: هل الأصناف تعمل في freestanding mode؟

خارجي دالة kernel_print_string(عنوان)
خارجي دالة kernel_print_number(قيمة)

صنف نقطة
    باني(س_ق، ص_ق)
        هذا.س = س_ق
        هذا.ص = ص_ق
    نهاية
    
    دالة مجموع()
        ارجع هذا.س + هذا.ص
    نهاية
نهاية

صدّر دالة __sad_init_99()
    kernel_print_string("class test")
    kernel_print_number(10)
    ارجع 0
نهاية
