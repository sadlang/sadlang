; ══════════════════════════════════════════════════════════════════════════════
; منافذ.تراب - منافذ الإدخال والإخراج لنواة خلية النحل
; ══════════════════════════════════════════════════════════════════════════════
; الوصف: عمليات إدخال/إخراج على المنافذ (Port I/O)
; المعمارية: x86_64
; ══════════════════════════════════════════════════════════════════════════════

bits 64
default rel

; ──────────────────────────────────────────────────────────────────────────────
; الرموز المصدّرة
; ──────────────────────────────────────────────────────────────────────────────
global إدخال_بايت
global إخراج_بايت
global إدخال_كلمة
global إخراج_كلمة
global إدخال_كلمة_مزدوجة
global إخراج_كلمة_مزدوجة
global انتظار_منفذ

; ══════════════════════════════════════════════════════════════════════════════
; قسم النص
; ══════════════════════════════════════════════════════════════════════════════
section .text

; ──────────────────────────────────────────────────────────────────────────────
; إدخال_بايت - قراءة بايت من منفذ
; المدخلات: edi = رقم المنفذ
; المخرجات: al = البايت المقروء
; ──────────────────────────────────────────────────────────────────────────────
إدخال_بايت:
    mov dx, di
    in al, dx
    ret

; ──────────────────────────────────────────────────────────────────────────────
; إخراج_بايت - كتابة بايت على منفذ
; المدخلات: edi = رقم المنفذ، sil = البايت
; ──────────────────────────────────────────────────────────────────────────────
إخراج_بايت:
    mov dx, di
    mov al, sil
    out dx, al
    ret

; ──────────────────────────────────────────────────────────────────────────────
; إدخال_كلمة - قراءة كلمة (16 بت)
; المدخلات: edi = رقم المنفذ
; المخرجات: ax = الكلمة
; ──────────────────────────────────────────────────────────────────────────────
إدخال_كلمة:
    mov dx, di
    in ax, dx
    ret

; ──────────────────────────────────────────────────────────────────────────────
; إخراج_كلمة - كتابة كلمة (16 بت)
; المدخلات: edi = رقم المنفذ، si = الكلمة
; ──────────────────────────────────────────────────────────────────────────────
إخراج_كلمة:
    mov dx, di
    mov ax, si
    out dx, ax
    ret

; ──────────────────────────────────────────────────────────────────────────────
; إدخال_كلمة_مزدوجة - قراءة كلمة مزدوجة (32 بت)
; المدخلات: edi = رقم المنفذ
; المخرجات: eax = القيمة
; ──────────────────────────────────────────────────────────────────────────────
إدخال_كلمة_مزدوجة:
    mov dx, di
    in eax, dx
    ret

; ──────────────────────────────────────────────────────────────────────────────
; إخراج_كلمة_مزدوجة - كتابة كلمة مزدوجة (32 بت)
; المدخلات: edi = رقم المنفذ، esi = القيمة
; ──────────────────────────────────────────────────────────────────────────────
إخراج_كلمة_مزدوجة:
    mov dx, di
    mov eax, esi
    out dx, eax
    ret

; ──────────────────────────────────────────────────────────────────────────────
; انتظار_منفذ - تأخير بين عمليات المنفذ
; ──────────────────────────────────────────────────────────────────────────────
انتظار_منفذ:
    xor eax, eax
    out 0x80, al
    ret
