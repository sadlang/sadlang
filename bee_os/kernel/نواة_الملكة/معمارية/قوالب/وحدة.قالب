# ══════════════════════════════════════════════════════════════════════════════
# قالب الوحدة (Module Template)
# ══════════════════════════════════════════════════════════════════════════════
# نظام خلية النحل - نواة الملكة
# الوصف: قالب لإنشاء وحدة جديدة في النواة
# ══════════════════════════════════════════════════════════════════════════════

# ─────────────────────────────────────────────────────────────────────────────
# معلومات الوحدة
# ─────────────────────────────────────────────────────────────────────────────
# الاسم: {{اسم_الوحدة}}
# القسم: {{القسم}} (نواة/ذاكرة/ملفات/تشغيل/صدفة)
# التبعيات: {{قائمة_التبعيات}}
# الأولوية: {{رقم_الأولوية}} (1=حرجة، 2=عالية، 3=عادية)
# ─────────────────────────────────────────────────────────────────────────────

# ═══════════════════════════════════════════════════════════════════════════
# هيكل الوحدة
# ═══════════════════════════════════════════════════════════════════════════
#
# كل وحدة في نواة الملكة تتبع هذا النمط:
#
# 1. معلومات الوحدة (اسم، إصدار، وصف)
# 2. الثوابت والتعريفات
# 3. هياكل البيانات
# 4. واجهة التهيئة
# 5. واجهة التشغيل
# 6. واجهة الإيقاف
# 7. الدوال الداخلية
# 8. نقطة الدخول الرئيسية
#
# ═══════════════════════════════════════════════════════════════════════════

# ─────────────────────────────────────────────────────────────────────────────
# القسم 1: معلومات الوحدة
# ─────────────────────────────────────────────────────────────────────────────

# صنف معلومات_الوحدة
#     نص الاسم
#     نص الإصدار
#     نص الوصف
#     رقم الأولوية
#     رقم الحالة        # 0=متوقفة، 1=جاري_التهيئة، 2=عاملة، 3=متوقفة
#
#     باني(نص اسم_و، نص إصدار_و، نص وصف_و، رقم أولوية_و)
#         الاسم = اسم_و
#         الإصدار = إصدار_و
#         الوصف = وصف_و
#         الأولوية = أولوية_و
#         الحالة = 0
#     نهاية
# نهاية

# ─────────────────────────────────────────────────────────────────────────────
# القسم 2: واجهة الوحدة
# ─────────────────────────────────────────────────────────────────────────────

# صنف {{اسم_الوحدة}}
#     معلومات_الوحدة المعلومات
#
#     باني()
#         المعلومات = جديد معلومات_الوحدة(
#             "{{اسم_الوحدة}}",
#             "1.0.0",
#             "{{وصف_الوحدة}}",
#             3
#         )
#     نهاية
#
#     # تهيئة الوحدة - تُستدعى عند إقلاع النواة
#     دالة رقم هيئ()
#         # كود التهيئة هنا
#         ارجع 0
#     نهاية
#
#     # تشغيل الوحدة
#     دالة رقم شغل()
#         # كود التشغيل هنا
#         ارجع 0
#     نهاية
#
#     # إيقاف الوحدة
#     دالة رقم أوقف()
#         # كود الإيقاف هنا
#         ارجع 0
#     نهاية
# نهاية

# ─────────────────────────────────────────────────────────────────────────────
# نهاية القالب
# ─────────────────────────────────────────────────────────────────────────────
