دالة مصفوفة نطاق(رقم نهاية_نطاق = 10، رقم بداية = 0، رقم خطوة = 1)
    مصفوفة نتيجة = []
    رقم ع = بداية
    بينما (ع < نهاية_نطاق)
        أضف(نتيجة، ع)
        ع = ع + خطوة
    نهاية
    إرجاع نتيجة
نهاية

اطبع("نطاق() =", نطاق())
اطبع("نطاق(5) =", نطاق(5))
اطبع("نطاق(10، 0، 2) =", نطاق(10، 0، 2))
