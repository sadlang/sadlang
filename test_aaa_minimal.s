دالة رقم قوة_افتراضية(رقم أساس، رقم أس = 2)
    إرجاع أساس ** أس
نهاية

دالة مصفوفة أنشئ_قائمة(رقم حجم، رقم قيمة = 0)
    مصفوفة نتيجة = []
    رقم ع = 0
    بينما (ع < حجم)
        أضف(نتيجة، قيمة)
        ع = ع + 1
    نهاية
    إرجاع نتيجة
نهاية

دالة مصفوفة نطاق(رقم نهاية_نطاق = 10، رقم بداية = 0، رقم خطوة = 1)
    مصفوفة نتيجة = []
    رقم ع = بداية
    بينما (ع < نهاية_نطاق)
        أضف(نتيجة، ع)
        ع = ع + خطوة
    نهاية
    إرجاع نتيجة
نهاية

اطبع("=== اختبار 1 ===")
اطبع("قوة(2، 3) =", قوة_افتراضية(2، 3))

اطبع("\n=== اختبار 2 ===")
مصفوفة أصفار = أنشئ_قائمة(3)
اطبع("أصفار =", أصفار)

اطبع("\n=== اختبار 3 ===")
مصفوفة نطاق1 = نطاق()
اطبع("نطاق() =", نطاق1)
