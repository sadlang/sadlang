# تقرير المشاكل في ملف 01_syntax_expressions.s

## المشكلة الأساسية
ملف الأمثلة `examples/syntax/01_syntax_expressions.s` يحتوي على syntax غير مدعوم في Parser الحالي، مما يسبب توقف Parser بدون رسالة خطأ واضحة.

## المشاكل المكتشفة

### 1. ✅ العوامل المنطقية العربية (تم الإصلاح)
**المشكلة**: الملف يستخدم `و` و `أو` و `ليس` لكن Parser يدعم فقط `&&` و `||` و `!`

**الأسطر المشكلة**:
- السطر 60-65: `صحيح و صحيح`, `صحيح أو خطأ`, `ليس صحيح`
- السطر 75-77: تعابير منطقية معقدة
- السطر 120-121: أولوية العوامل
- السطر 166: تعبير منطقي معقد

**الحل**: تم استبدال:
- `و` → `&&`
- `أو` → `||`
- `ليس` → `!`

**المرجع**: حسب `docs/language_spec/rules/04_syntax.md`, العوامل المنطقية هي: `&&`, `||`, `!` فقط.

### 2. ⚠️ مشكلة غير محددة تسبب توقف Parser
**الأعراض**:
- Parser يتوقف بعد parsing عدة declarations
- لا توجد رسالة خطأ واضحة
- Exit code 1

**الحاجة**: تحديد السطر/التركيب الذي يسبب المشكلة

## التوصيات

### أولاً: إضافة دعم العوامل العربية (اختياري)
إذا أردنا دعم `و` و `أو` و `ليس`:
1. إضافة keywords في `lexer_keywords.cpp`:
```cpp
keywords_["و"] = TokenType::OP_AND;
keywords_["أو"] = TokenType::OP_OR;
keywords_["ليس"] = TokenType::OP_NOT;
```
2. تحديث `processOperator()` في lexer

### ثانياً: تحسين معالجة الأخطاء في Parser
- إضافة error handling أفضل عند مواجهة syntax غير مدعوم
- طباعة رسالة خطأ واضحة بدلاً من التوقف الصامت
- منع Access Violation crashes

### ثالثاً: تدقيق ملفات الأمثلة
جميع ملفات `examples/` يجب أن تطابق القواعد في `docs/language_spec/`

## الحالة
- ✅ تم إصلاح العوامل المنطقية
- ⏳ يوجد مشكلة إضافية قيد التحديد
- ❌ الملف لا يعمل بعد

## الخطوات التالية
1. استخدام binary search لتحديد السطر المشكل
2. إصلاح السطر أو إضافة الدعم له
3. اختبار الملف كاملاً
4. تدقيق بقية ملفات الأمثلة
