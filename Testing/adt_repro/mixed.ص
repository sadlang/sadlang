تعداد شجرة
    ورقة(قيمة)
    عقدة(يسار، يمين، وزن)
نهاية

دالة اجمع(ش)
    طابق (ش)
        عندما ورقة(ق):
            ارجع ق
        عندما عقدة(يس، يم، ز):
            ارجع اجمع(يس) + اجمع(يم) + ز
    نهاية
نهاية

اطبع_سطر(نص(اجمع(عقدة(ورقة(1)، ورقة(2)، 100))))
