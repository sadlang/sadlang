تعداد شجرة
    ورقة(قيمة)
    فرع(يسار، يمين)
نهاية
دالة عمق(ش)
    طابق (ش)
        عندما ورقة(ق):
            ارجع 1
        عندما فرع(يس، يم):
            متغير ل = عمق(يس)
            متغير ر = عمق(يم)
            طابق (ل)
                عندما 0:
                    ارجع ر
                عندما _:
                    إذا (ل > ر)
                        ارجع ل + 1
                    نهاية
                    ارجع ر + 1
            نهاية
    نهاية
نهاية
اطبع_سطر(نص(عمق(فرع(ورقة(1)، فرع(ورقة(2)، ورقة(3))))))
