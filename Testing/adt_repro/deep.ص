تعداد تعبير
    عدد(قيمة)
    جمع(يسار، يمين)
نهاية

دالة قيّم(ع)
    طابق (ع)
        عندما عدد(ق):
            ارجع ق
        عندما جمع(يس، يم):
            ارجع قيّم(يس) + قيّم(يم)
    نهاية
نهاية

اطبع_سطر(نص(قيّم(جمع(جمع(عدد(1)، عدد(2))، عدد(3)))))
