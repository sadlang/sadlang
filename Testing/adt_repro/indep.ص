تعداد ألف
    واحد(س)
    اثنان(أ، ب)
نهاية

تعداد باء
    فرد(ص)
    زوج(ج، د)
نهاية

دالة قيمة_ألف(ع)
    طابق (ع)
        عندما واحد(س): ارجع س
        عندما اثنان(أ، ب): ارجع أ + ب
    نهاية
نهاية

دالة قيمة_باء(ع)
    طابق (ع)
        عندما فرد(ص): ارجع ص
        عندما زوج(ج، د): ارجع ج * د
    نهاية
نهاية

متغير م1 = اثنان(3، 4)
متغير م2 = زوج(5، 6)
اطبع_سطر(نص(قيمة_ألف(م1)))
اطبع_سطر(نص(قيمة_باء(م2)))
