تعداد تعبير
    ورقة
    عقدة(يسار، يمين)
نهاية

دالة عدّ(ع)
    طابق (ع)
        عندما ورقة:
            ارجع 1
        عندما عقدة(يس، يم):
            ارجع عدّ(يس) + عدّ(يم)
    نهاية
نهاية

اطبع_سطر(نص(عدّ(عقدة(ورقة، عقدة(ورقة، ورقة)))))
