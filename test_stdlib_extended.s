متغير نص_اختبار = "مرحبا بك في لغة ص";

اطبع("النص الأصلي:");
اطبع(نص_اختبار);

متغير الطول = طول(نص_اختبار);
اطبع("الطول:");
اطبع(الطول);

متغير قائمة = [1, 2, 3, 4, 5];
اطبع("القائمة:");
اطبع(قائمة);

متغير طول_قائمة = طول(قائمة);
اطبع("طول القائمة:");
اطبع(طول_قائمة);
