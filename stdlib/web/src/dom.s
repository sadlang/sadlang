# بسم الله الرحمن الرحيم
# dom.s - واجهة DOM العربية للغة ص
# Arabic DOM Interface for Sad Language

# ============================================================================
# استعلام العناصر
# ============================================================================

دالة استعلم(محدد)
    # البحث عن عنصر بمحدد CSS
    عودة __sad_dom_query_selector(محدد)
نهاية

دالة استعلم_الكل(محدد)
    # البحث عن كل العناصر بمحدد CSS
    عودة __sad_dom_query_selector_all(محدد)
نهاية

دالة بالمعرف(معرف)
    # البحث عن عنصر بالمعرف
    عودة __sad_dom_get_element_by_id(معرف)
نهاية

دالة بالصنف(صنف)
    # البحث عن عناصر بالصنف
    عودة __sad_dom_get_elements_by_class(صنف)
نهاية

دالة بالوسم(وسم)
    # البحث عن عناصر بالوسم
    عودة __sad_dom_get_elements_by_tag(وسم)
نهاية

دالة بالاسم(اسم)
    # البحث عن عناصر بالاسم
    عودة __sad_dom_get_elements_by_name(اسم)
نهاية

# ============================================================================
# إنشاء العناصر
# ============================================================================

دالة أنشئ_عنصر(وسم)
    # إنشاء عنصر HTML جديد
    عودة __sad_dom_create_element(وسم)
نهاية

دالة أنشئ_نص(نص)
    # إنشاء عقدة نصية
    عودة __sad_dom_create_text_node(نص)
نهاية

دالة أنشئ_تعليق(نص)
    # إنشاء تعليق HTML
    عودة __sad_dom_create_comment(نص)
نهاية

دالة أنشئ_قطعة()
    # إنشاء DocumentFragment
    عودة __sad_dom_create_document_fragment()
نهاية

# عناصر HTML الشائعة
دالة أنشئ_قسم()
    عودة أنشئ_عنصر("div")
نهاية

دالة أنشئ_فقرة()
    عودة أنشئ_عنصر("p")
نهاية

دالة أنشئ_امتداد()
    عودة أنشئ_عنصر("span")
نهاية

دالة أنشئ_زر()
    عودة أنشئ_عنصر("button")
نهاية

دالة أنشئ_رابط()
    عودة أنشئ_عنصر("a")
نهاية

دالة أنشئ_صورة()
    عودة أنشئ_عنصر("img")
نهاية

دالة أنشئ_إدخال()
    عودة أنشئ_عنصر("input")
نهاية

دالة أنشئ_نموذج()
    عودة أنشئ_عنصر("form")
نهاية

دالة أنشئ_قائمة()
    عودة أنشئ_عنصر("ul")
نهاية

دالة أنشئ_عنصر_قائمة()
    عودة أنشئ_عنصر("li")
نهاية

دالة أنشئ_جدول()
    عودة أنشئ_عنصر("table")
نهاية

دالة أنشئ_صف()
    عودة أنشئ_عنصر("tr")
نهاية

دالة أنشئ_خلية()
    عودة أنشئ_عنصر("td")
نهاية

دالة أنشئ_عنوان(مستوى)
    إذا (مستوى < 1)
        مستوى = 1
    وإلا_إذا (مستوى > 6)
        مستوى = 6
    نهاية
    
    عودة أنشئ_عنصر("h" + مستوى)
نهاية

# ============================================================================
# التلاعب بالعناصر
# ============================================================================

دالة أضف_ابن(أب، ابن)
    # إضافة عنصر ابن
    __sad_dom_append_child(أب، ابن)
    عودة ابن
نهاية

دالة أدرج_قبل(أب، جديد، مرجع)
    # إدراج عنصر قبل آخر
    __sad_dom_insert_before(أب، جديد، مرجع)
    عودة جديد
نهاية

دالة أدرج_بعد(أب، جديد، مرجع)
    # إدراج عنصر بعد آخر
    رقم التالي = اقرأ_الأخ_التالي(مرجع)
    
    إذا (التالي == عدم)
        __sad_dom_append_child(أب، جديد)
    وإلا
        __sad_dom_insert_before(أب، جديد، التالي)
    نهاية
    
    عودة جديد
نهاية

دالة أزل_ابن(أب، ابن)
    # إزالة عنصر ابن
    __sad_dom_remove_child(أب، ابن)
    عودة ابن
نهاية

دالة أزل_عنصر(عنصر)
    # إزالة العنصر من DOM
    __sad_dom_remove(عنصر)
نهاية

دالة استبدل_ابن(أب، جديد، قديم)
    # استبدال عنصر بآخر
    __sad_dom_replace_child(أب، جديد، قديم)
    عودة جديد
نهاية

دالة انسخ_عنصر(عنصر، عميق)
    # نسخ عنصر
    عودة __sad_dom_clone_node(عنصر، عميق)
نهاية

# ============================================================================
# السمات
# ============================================================================

دالة اقرأ_سمة(عنصر، اسم)
    # قراءة سمة
    عودة __sad_dom_get_attribute(عنصر، اسم)
نهاية

دالة عيّن_سمة(عنصر، اسم، قيمة)
    # تعيين سمة
    __sad_dom_set_attribute(عنصر، اسم، قيمة)
نهاية

دالة أزل_سمة(عنصر، اسم)
    # إزالة سمة
    __sad_dom_remove_attribute(عنصر، اسم)
نهاية

دالة له_سمة(عنصر، اسم)
    # التحقق من وجود سمة
    عودة __sad_dom_has_attribute(عنصر، اسم)
نهاية

دالة عيّن_سمات(عنصر، سمات)
    # تعيين سمات متعددة من قاموس
    لكل مفتاح، قيمة في سمات
        عيّن_سمة(عنصر، مفتاح، قيمة)
    نهاية
نهاية

# سمات شائعة
دالة عيّن_معرف(عنصر، معرف)
    عيّن_سمة(عنصر، "id"، معرف)
نهاية

دالة عيّن_رابط(عنصر، رابط)
    عيّن_سمة(عنصر، "href"، رابط)
نهاية

دالة عيّن_مصدر(عنصر، مصدر)
    عيّن_سمة(عنصر، "src"، مصدر)
نهاية

دالة عيّن_عنوان_بديل(عنصر، نص)
    عيّن_سمة(عنصر، "alt"، نص)
نهاية

دالة عيّن_عنوان(عنصر، نص)
    عيّن_سمة(عنصر، "title"، نص)
نهاية

دالة عيّن_نوع(عنصر، نوع)
    عيّن_سمة(عنصر، "type"، نوع)
نهاية

دالة عيّن_اسم(عنصر، اسم)
    عيّن_سمة(عنصر، "name"، اسم)
نهاية

دالة عيّن_قيمة(عنصر، قيمة)
    عيّن_سمة(عنصر، "value"، قيمة)
نهاية

دالة عيّن_مكان_نص(عنصر، نص)
    عيّن_سمة(عنصر، "placeholder"، نص)
نهاية

# ============================================================================
# الأصناف CSS
# ============================================================================

دالة أضف_صنف(عنصر، صنف)
    # إضافة صنف CSS
    __sad_dom_class_add(عنصر، صنف)
نهاية

دالة أزل_صنف(عنصر، صنف)
    # إزالة صنف CSS
    __sad_dom_class_remove(عنصر، صنف)
نهاية

دالة بدّل_صنف(عنصر، صنف)
    # تبديل صنف CSS
    __sad_dom_class_toggle(عنصر، صنف)
نهاية

دالة يحوي_صنف(عنصر، صنف)
    # التحقق من وجود صنف
    عودة __sad_dom_class_contains(عنصر، صنف)
نهاية

دالة استبدل_صنف(عنصر، قديم، جديد)
    # استبدال صنف بآخر
    أزل_صنف(عنصر، قديم)
    أضف_صنف(عنصر، جديد)
نهاية

دالة أضف_أصناف(عنصر، أصناف)
    # إضافة أصناف متعددة
    لكل صنف في أصناف
        أضف_صنف(عنصر، صنف)
    نهاية
نهاية

# ============================================================================
# الأنماط CSS
# ============================================================================

دالة عيّن_نمط(عنصر، خاصية، قيمة)
    # تعيين نمط CSS
    __sad_dom_style_set(عنصر، خاصية، قيمة)
نهاية

دالة اقرأ_نمط(عنصر، خاصية)
    # قراءة نمط CSS
    عودة __sad_dom_style_get(عنصر، خاصية)
نهاية

دالة عيّن_أنماط(عنصر، أنماط)
    # تعيين أنماط متعددة من قاموس
    لكل خاصية، قيمة في أنماط
        عيّن_نمط(عنصر، خاصية، قيمة)
    نهاية
نهاية

# أنماط شائعة
دالة عيّن_عرض(عنصر، قيمة)
    عيّن_نمط(عنصر، "width"، قيمة)
نهاية

دالة عيّن_ارتفاع(عنصر، قيمة)
    عيّن_نمط(عنصر، "height"، قيمة)
نهاية

دالة عيّن_لون(عنصر، لون)
    عيّن_نمط(عنصر، "color"، لون)
نهاية

دالة عيّن_خلفية(عنصر، لون)
    عيّن_نمط(عنصر، "backgroundColor"، لون)
نهاية

دالة عيّن_حشوة(عنصر، قيمة)
    عيّن_نمط(عنصر، "padding"، قيمة)
نهاية

دالة عيّن_هامش(عنصر، قيمة)
    عيّن_نمط(عنصر، "margin"، قيمة)
نهاية

دالة عيّن_حدود(عنصر، قيمة)
    عيّن_نمط(عنصر، "border"، قيمة)
نهاية

دالة عيّن_عرض_نمط(عنصر، قيمة)
    عيّن_نمط(عنصر، "display"، قيمة)
نهاية

دالة أظهر(عنصر)
    عيّن_نمط(عنصر، "display"، "block")
نهاية

دالة أخفِ(عنصر)
    عيّن_نمط(عنصر، "display"، "none")
نهاية

دالة عيّن_شفافية(عنصر، قيمة)
    عيّن_نمط(عنصر، "opacity"، قيمة)
نهاية

# ============================================================================
# المحتوى
# ============================================================================

دالة اقرأ_نص(عنصر)
    # قراءة المحتوى النصي
    عودة __sad_dom_text_content_get(عنصر)
نهاية

دالة عيّن_نص(عنصر، نص)
    # تعيين المحتوى النصي
    __sad_dom_text_content_set(عنصر، نص)
نهاية

دالة اقرأ_html(عنصر)
    # قراءة HTML الداخلي
    عودة __sad_dom_inner_html_get(عنصر)
نهاية

دالة عيّن_html(عنصر، html)
    # تعيين HTML الداخلي
    __sad_dom_inner_html_set(عنصر، html)
نهاية

دالة اقرأ_html_خارجي(عنصر)
    # قراءة HTML الخارجي
    عودة __sad_dom_outer_html_get(عنصر)
نهاية

دالة اقرأ_قيمة(عنصر)
    # قراءة قيمة حقل إدخال
    عودة __sad_dom_value_get(عنصر)
نهاية

دالة عيّن_قيمة_إدخال(عنصر، قيمة)
    # تعيين قيمة حقل إدخال
    __sad_dom_value_set(عنصر، قيمة)
نهاية

# ============================================================================
# التنقل في DOM
# ============================================================================

دالة اقرأ_الأب(عنصر)
    # الحصول على العنصر الأب
    عودة __sad_dom_parent_node(عنصر)
نهاية

دالة اقرأ_الأبناء(عنصر)
    # الحصول على الأبناء
    عودة __sad_dom_child_nodes(عنصر)
نهاية

دالة اقرأ_الأبناء_عناصر(عنصر)
    # الحصول على الأبناء العناصر فقط
    عودة __sad_dom_children(عنصر)
نهاية

دالة اقرأ_الابن_الأول(عنصر)
    # الحصول على الابن الأول
    عودة __sad_dom_first_child(عنصر)
نهاية

دالة اقرأ_الابن_الأخير(عنصر)
    # الحصول على الابن الأخير
    عودة __sad_dom_last_child(عنصر)
نهاية

دالة اقرأ_الأخ_التالي(عنصر)
    # الحصول على الأخ التالي
    عودة __sad_dom_next_sibling(عنصر)
نهاية

دالة اقرأ_الأخ_السابق(عنصر)
    # الحصول على الأخ السابق
    عودة __sad_dom_previous_sibling(عنصر)
نهاية

# ============================================================================
# الأحداث
# ============================================================================

دالة عند(عنصر، حدث، معالج)
    # إضافة مستمع حدث
    __sad_dom_add_event_listener(عنصر، حدث، معالج)
نهاية

دالة أزل_عند(عنصر، حدث، معالج)
    # إزالة مستمع حدث
    __sad_dom_remove_event_listener(عنصر، حدث، معالج)
نهاية

دالة أطلق_حدث(عنصر، حدث)
    # إطلاق حدث
    __sad_dom_dispatch_event(عنصر، حدث)
نهاية

# أحداث شائعة
دالة عند_نقر(عنصر، معالج)
    عند(عنصر، "click"، معالج)
نهاية

دالة عند_نقر_مزدوج(عنصر، معالج)
    عند(عنصر، "dblclick"، معالج)
نهاية

دالة عند_تمرير_فوق(عنصر، معالج)
    عند(عنصر، "mouseover"، معالج)
نهاية

دالة عند_تمرير_خارج(عنصر، معالج)
    عند(عنصر، "mouseout"، معالج)
نهاية

دالة عند_ضغط_مفتاح(عنصر، معالج)
    عند(عنصر، "keydown"، معالج)
نهاية

دالة عند_رفع_مفتاح(عنصر، معالج)
    عند(عنصر، "keyup"، معالج)
نهاية

دالة عند_إدخال(عنصر، معالج)
    عند(عنصر، "input"، معالج)
نهاية

دالة عند_تغيير(عنصر، معالج)
    عند(عنصر، "change"، معالج)
نهاية

دالة عند_إرسال(عنصر، معالج)
    عند(عنصر، "submit"، معالج)
نهاية

دالة عند_تركيز(عنصر، معالج)
    عند(عنصر، "focus"، معالج)
نهاية

دالة عند_فقد_تركيز(عنصر، معالج)
    عند(عنصر، "blur"، معالج)
نهاية

# ============================================================================
# الأبعاد والموقع
# ============================================================================

دالة اقرأ_أبعاد(عنصر)
    # الحصول على أبعاد العنصر
    عودة __sad_dom_get_bounding_client_rect(عنصر)
نهاية

دالة اقرأ_عرض_عنصر(عنصر)
    رقم أبعاد = اقرأ_أبعاد(عنصر)
    عودة أبعاد.عرض
نهاية

دالة اقرأ_ارتفاع_عنصر(عنصر)
    رقم أبعاد = اقرأ_أبعاد(عنصر)
    عودة أبعاد.ارتفاع
نهاية

دالة اقرأ_موقع_س(عنصر)
    رقم أبعاد = اقرأ_أبعاد(عنصر)
    عودة أبعاد.يسار
نهاية

دالة اقرأ_موقع_ص(عنصر)
    رقم أبعاد = اقرأ_أبعاد(عنصر)
    عودة أبعاد.أعلى
نهاية

دالة مرر_إلى_العنصر(عنصر)
    # التمرير لإظهار العنصر
    __sad_dom_scroll_into_view(عنصر)
نهاية

# ============================================================================
# الوثيقة
# ============================================================================

دالة جسم_الوثيقة()
    # الحصول على body
    عودة __sad_dom_document_body()
نهاية

دالة رأس_الوثيقة()
    # الحصول على head
    عودة __sad_dom_document_head()
نهاية

دالة جذر_الوثيقة()
    # الحصول على documentElement
    عودة __sad_dom_document_element()
نهاية

دالة عنوان_الوثيقة()
    # قراءة عنوان الصفحة
    عودة __sad_dom_document_title_get()
نهاية

دالة عيّن_عنوان_وثيقة(عنوان)
    # تعيين عنوان الصفحة
    __sad_dom_document_title_set(عنوان)
نهاية

دالة عند_جاهزية_وثيقة(معالج)
    # تنفيذ عند جاهزية DOM
    عند(__sad_dom_document()، "DOMContentLoaded"، معالج)
نهاية
