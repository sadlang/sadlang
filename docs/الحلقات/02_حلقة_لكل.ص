# الحلقات المتقدمة - أنماط متقدمة باستخدام بينما
# يوضح هذا الملف ثمانية أنماط متقدمة لاستخدام حلقة بينما
# (بديل عن حلقة لكل باستخدام عدّادات وأنماط متقدمة)

دالة رئيسية()

    # مثال 1: محاكاة التكرار على نطاق أعداد
    اطبع_سطر("=== مثال 1: نطاق من 5 إلى 15 ===")
    متغير ع = 5
    بينما (ع <= 15)
        اطبع_سطر("القيمة: " + ع)
        ع = ع + 1
    نهاية

    # مثال 2: حلقات متداخلة - جدول الضرب
    اطبع_سطر("=== مثال 2: جدول الضرب (1-5) ===")
    متغير ص = 0
    متغير سطر = ""
    متغير حاصل = 0
    ع = 1
    بينما (ع <= 5)
        ص = 1
        سطر = ""
        بينما (ص <= 5)
            حاصل = ع * ص
            سطر = سطر + حاصل + "  "
            ص = ص + 1
        نهاية
        اطبع_سطر(سطر)
        ع = ع + 1
    نهاية

    # مثال 3: تكرار مشروط - الأعداد الزوجية فقط
    اطبع_سطر("=== مثال 3: الأعداد الزوجية من 1 إلى 20 ===")
    ع = 1
    بينما (ع <= 20)
        إذا (ع % 2 == 0)
            اطبع_سطر("زوجي: " + ع)
        نهاية
        ع = ع + 1
    نهاية

    # مثال 4: التكرار العكسي مع خطوة
    اطبع_سطر("=== مثال 4: عد تنازلي بخطوة 3 ===")
    ع = 30
    بينما (ع >= 0)
        اطبع_سطر(ع)
        ع = ع - 3
    نهاية

    # مثال 5: التكرار بخطوة تتضاعف
    اطبع_سطر("=== مثال 5: خطوة تتضاعف ===")
    ع = 1
    بينما (ع <= 1000)
        اطبع_سطر("القيمة: " + ع)
        ع = ع * 2
    نهاية

    # مثال 6: تجميع مع شرط - مجموع الأعداد الفردية
    اطبع_سطر("=== مثال 6: مجموع الأعداد الفردية من 1 إلى 50 ===")
    متغير مجموع = 0
    ع = 1
    بينما (ع <= 50)
        إذا (ع % 2 != 0)
            مجموع = مجموع + ع
        نهاية
        ع = ع + 1
    نهاية
    اطبع_سطر("المجموع: " + مجموع)

    # مثال 7: إيجاد عدد يحقق شرطاً مركباً
    # أول عدد يقبل القسمة على 3 و 7 معاً وأكبر من 50
    اطبع_سطر("=== مثال 7: بحث عن عدد بشرط مركب ===")
    ع = 51
    متغير وجد = خطأ
    بينما (!وجد)
        إذا (ع % 3 == 0)
            إذا (ع % 7 == 0)
                وجد = صحيح
            نهاية
        نهاية
        إذا (!وجد)
            ع = ع + 1
        نهاية
    نهاية
    اطبع_سطر("أول عدد يقبل القسمة على 3 و 7 وأكبر من 50: " + ع)

    # مثال 8: عدّادان يتحركان من الطرفين
    اطبع_سطر("=== مثال 8: عدّادان متقابلان ===")
    متغير يسار = 1
    متغير يمين = 10
    بينما (يسار <= يمين)
        اطبع_سطر("يسار=" + يسار + " يمين=" + يمين)
        يسار = يسار + 1
        يمين = يمين - 1
    نهاية

    اطبع_سطر("=== انتهت الأمثلة المتقدمة ===")
نهاية
