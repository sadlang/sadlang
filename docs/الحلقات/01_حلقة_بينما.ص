# حلقة بينما - أساسيات الحلقات في لغة ص
# يوضح هذا الملف ثمانية أمثلة عملية لاستخدام حلقة بينما

# دالة مساعدة: حساب العاملي (مُعرّفة خارج الدالة الرئيسية)
دالة عاملي(ن)
    متغير نتيجة = 1
    متغير ع = 1
    بينما (ع <= ن)
        نتيجة = نتيجة * ع
        ع = ع + 1
    نهاية
    ارجع نتيجة
نهاية

دالة رئيسية()

    # مثال 1: العد التصاعدي من 1 إلى 10
    اطبع_سطر("=== مثال 1: العد التصاعدي ===")
    متغير ع = 1
    بينما (ع <= 10)
        اطبع_سطر("العدد: " + ع)
        ع = ع + 1
    نهاية

    # مثال 2: العد التنازلي من 10 إلى 1
    اطبع_سطر("=== مثال 2: العد التنازلي ===")
    ع = 10
    بينما (ع >= 1)
        اطبع_سطر(ع)
        ع = ع - 1
    نهاية
    اطبع_سطر("انطلق!")

    # مثال 3: مجموع الأعداد من 1 إلى 100
    اطبع_سطر("=== مثال 3: المجموع التراكمي ===")
    متغير مجموع = 0
    ع = 1
    بينما (ع <= 100)
        مجموع = مجموع + ع
        ع = ع + 1
    نهاية
    اطبع_سطر("مجموع الأعداد من 1 إلى 100 = " + مجموع)

    # مثال 4: حساب العاملي باستخدام دالة مساعدة
    اطبع_سطر("=== مثال 4: العاملي ===")
    متغير ن = 1
    بينما (ن <= 10)
        اطبع_سطر(ن + "! = " + عاملي(ن))
        ن = ن + 1
    نهاية

    # مثال 5: عدّ خانات عدد صحيح
    اطبع_سطر("=== مثال 5: عدّ الخانات ===")
    متغير عدد = 123456
    متغير خانات = 0
    متغير نسخة = عدد
    بينما (نسخة > 0)
        نسخة = (نسخة - نسخة % 10) / 10
        خانات = خانات + 1
    نهاية
    اطبع_سطر("عدد خانات " + عدد + " هو " + خانات)

    # مثال 6: إيجاد أول مضاعف لـ 7 أكبر من 100
    اطبع_سطر("=== مثال 6: أول مضاعف ===")
    ع = 1
    بينما (ع * 7 <= 100)
        ع = ع + 1
    نهاية
    اطبع_سطر("أول مضاعف لـ 7 أكبر من 100 هو " + ع * 7)

    # مثال 7: قوى العدد 2 بالمضاعفة المتكررة
    اطبع_سطر("=== مثال 7: قوى العدد 2 ===")
    متغير قيمة = 1
    ع = 0
    بينما (ع <= 10)
        اطبع_سطر("2 أس " + ع + " = " + قيمة)
        قيمة = قيمة * 2
        ع = ع + 1
    نهاية

    # مثال 8: متتالية فيبوناتشي
    اطبع_سطر("=== مثال 8: متتالية فيبوناتشي ===")
    متغير أ = 0
    متغير ب = 1
    متغير مؤقت = 0
    ع = 0
    بينما (ع < 15)
        اطبع_سطر("فيبوناتشي(" + ع + ") = " + أ)
        مؤقت = أ + ب
        أ = ب
        ب = مؤقت
        ع = ع + 1
    نهاية

    اطبع_سطر("=== انتهت أمثلة حلقة بينما ===")
نهاية
