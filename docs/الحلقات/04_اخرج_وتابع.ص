# اخرج وتابع - التحكم في سير الحلقات
# يوضح هذا الملف ثمانية أمثلة لاستخدام اخرج (break) وتابع (continue)

دالة رئيسية()

    # مثال 1: اخرج عند إيجاد عدد مطلوب
    اطبع_سطر("=== مثال 1: اخرج عند إيجاد العدد ===")
    متغير ع = 1
    بينما (ع <= 100)
        إذا (ع * ع > 200)
            اطبع_سطر("أول عدد مربعه أكبر من 200: " + ع)
            اخرج
        نهاية
        ع = ع + 1
    نهاية

    # مثال 2: تابع لتخطي الأعداد الزوجية
    اطبع_سطر("=== مثال 2: طباعة الفردية فقط (1-20) ===")
    ع = 0
    بينما (ع < 20)
        ع = ع + 1
        إذا (ع % 2 == 0)
            تابع
        نهاية
        اطبع_سطر("فردي: " + ع)
    نهاية

    # مثال 3: اخرج عند تجاوز حدّ المجموع
    اطبع_سطر("=== مثال 3: جمع حتى تجاوز الحد ===")
    متغير مجموع = 0
    ع = 1
    بينما (ع <= 1000)
        مجموع = مجموع + ع
        إذا (مجموع > 100)
            اطبع_سطر("توقفنا عند ع=" + ع + " المجموع=" + مجموع)
            اخرج
        نهاية
        ع = ع + 1
    نهاية

    # مثال 4: تابع لتخطي مضاعفات 3
    اطبع_سطر("=== مثال 4: تخطي مضاعفات 3 ===")
    ع = 0
    بينما (ع < 30)
        ع = ع + 1
        إذا (ع % 3 == 0)
            تابع
        نهاية
        اطبع_سطر(ع)
    نهاية

    # مثال 5: اخرج في حلقة داخلية فقط
    اطبع_سطر("=== مثال 5: اخرج في الحلقة الداخلية ===")
    متغير ص = 0
    متغير سطر = ""
    ع = 1
    بينما (ع <= 5)
        ص = 1
        سطر = "الصف " + ع + ": "
        بينما (ص <= 10)
            إذا (ع * ص > 15)
                اخرج
            نهاية
            سطر = سطر + ع * ص + " "
            ص = ص + 1
        نهاية
        اطبع_سطر(سطر)
        ع = ع + 1
    نهاية

    # مثال 6: تابع مع عدّاد التخطي
    اطبع_سطر("=== مثال 6: عدّ التخطيات ===")
    متغير تخطيات = 0
    متغير طبع = 0
    ع = 0
    بينما (ع < 50)
        ع = ع + 1
        إذا (ع % 4 == 0)
            تخطيات = تخطيات + 1
            تابع
        نهاية
        طبع = طبع + 1
    نهاية
    اطبع_سطر("طُبع: " + طبع + " تُخطي: " + تخطيات)

    # مثال 7: اخرج عند إيجاد عدد يقبل القسمة على 13 و 17
    اطبع_سطر("=== مثال 7: بحث عن مضاعف مشترك ===")
    ع = 1
    بينما (ع <= 1000)
        إذا (ع % 13 == 0)
            إذا (ع % 17 == 0)
                اطبع_سطر("أصغر عدد يقبل القسمة على 13 و 17: " + ع)
                اخرج
            نهاية
        نهاية
        ع = ع + 1
    نهاية

    # مثال 8: اخرج وتابع معاً - جمع الفردية حتى حد
    اطبع_سطر("=== مثال 8: اخرج وتابع معاً ===")
    مجموع = 0
    ع = 0
    بينما (ع < 100)
        ع = ع + 1
        # تخطي الزوجية
        إذا (ع % 2 == 0)
            تابع
        نهاية
        مجموع = مجموع + ع
        # توقف إذا تجاوز المجموع 200
        إذا (مجموع > 200)
            اطبع_سطر("آخر عدد فردي أُضيف: " + ع)
            اطبع_سطر("المجموع: " + مجموع)
            اخرج
        نهاية
    نهاية

    اطبع_سطر("=== انتهت أمثلة اخرج وتابع ===")
نهاية
