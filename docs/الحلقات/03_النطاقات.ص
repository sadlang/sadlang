# النطاقات والتسلسلات - محاكاة النطاقات باستخدام بينما
# يوضح هذا الملف ثمانية أمثلة لمحاكاة النطاقات والتسلسلات المختلفة

دالة رئيسية()

    # مثال 1: نطاق بسيط من 1 إلى 20
    اطبع_سطر("=== مثال 1: نطاق بسيط 1-20 ===")
    متغير ع = 1
    بينما (ع <= 20)
        اطبع_سطر(ع)
        ع = ع + 1
    نهاية

    # مثال 2: نطاق بخطوة 5
    اطبع_سطر("=== مثال 2: نطاق بخطوة 5 (0 إلى 50) ===")
    ع = 0
    بينما (ع <= 50)
        اطبع_سطر(ع)
        ع = ع + 5
    نهاية

    # مثال 3: تسلسل الأعداد الزوجية
    اطبع_سطر("=== مثال 3: الأعداد الزوجية من 2 إلى 30 ===")
    ع = 2
    بينما (ع <= 30)
        اطبع_سطر("زوجي: " + ع)
        ع = ع + 2
    نهاية

    # مثال 4: تسلسل الأعداد الفردية
    اطبع_سطر("=== مثال 4: الأعداد الفردية من 1 إلى 25 ===")
    ع = 1
    بينما (ع <= 25)
        اطبع_سطر("فردي: " + ع)
        ع = ع + 2
    نهاية

    # مثال 5: نطاق عكسي من 100 إلى 80
    اطبع_سطر("=== مثال 5: نطاق عكسي 100 إلى 80 ===")
    ع = 100
    بينما (ع >= 80)
        اطبع_سطر(ع)
        ع = ع - 1
    نهاية

    # مثال 6: مضاعفات العدد 7
    اطبع_سطر("=== مثال 6: مضاعفات 7 حتى 100 ===")
    ع = 7
    بينما (ع <= 100)
        اطبع_سطر("مضاعف 7: " + ع)
        ع = ع + 7
    نهاية

    # مثال 7: تسلسل هندسي - قوى العدد 3
    اطبع_سطر("=== مثال 7: قوى العدد 3 ===")
    متغير قيمة = 1
    متغير أس = 0
    بينما (قيمة <= 10000)
        اطبع_سطر("3 أس " + أس + " = " + قيمة)
        قيمة = قيمة * 3
        أس = أس + 1
    نهاية

    # مثال 8: متتالية حسابية مخصصة (بداية=10، خطوة=7، نهاية=80)
    اطبع_سطر("=== مثال 8: متتالية حسابية مخصصة ===")
    متغير بداية = 10
    متغير خطوة = 7
    متغير حد = 80
    متغير عداد = 0
    ع = بداية
    بينما (ع <= حد)
        عداد = عداد + 1
        اطبع_سطر("الحد " + عداد + ": " + ع)
        ع = ع + خطوة
    نهاية
    اطبع_سطر("عدد الحدود: " + عداد)

    اطبع_سطر("=== انتهت أمثلة النطاقات ===")
نهاية
