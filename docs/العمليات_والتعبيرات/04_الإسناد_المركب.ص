# الإسناد المركب (Compound Assignment Operators)
# يشرح هذا الملف عوامل الإسناد المركب: += و -= و *= و /= و %=
# هذه العوامل تختصر العمليات الحسابية مع الإسناد في تعبير واحد

دالة رئيسية()

    # --- مثال 1: عامل الجمع والإسناد += ---
    اطبع_سطر("=== مثال 1: عامل += ===")
    متغير عدد = 10
    اطبع_سطر("القيمة الأصلية: " + عدد)
    عدد += 5
    اطبع_سطر("بعد += 5: " + عدد)
    عدد += 20
    اطبع_سطر("بعد += 20: " + عدد)

    # --- مثال 2: عامل الطرح والإسناد -= ---
    اطبع_سطر("")
    اطبع_سطر("=== مثال 2: عامل -= ===")
    متغير رصيد = 1000
    اطبع_سطر("الرصيد الأصلي: " + رصيد)
    رصيد -= 250
    اطبع_سطر("بعد سحب 250: " + رصيد)
    رصيد -= 100
    اطبع_سطر("بعد سحب 100: " + رصيد)

    # --- مثال 3: عامل الضرب والإسناد *= ---
    اطبع_سطر("")
    اطبع_سطر("=== مثال 3: عامل *= ===")
    متغير حاصل = 3
    اطبع_سطر("القيمة الأصلية: " + حاصل)
    حاصل *= 4
    اطبع_سطر("بعد *= 4: " + حاصل)
    حاصل *= 2
    اطبع_سطر("بعد *= 2: " + حاصل)

    # --- مثال 4: عامل القسمة والإسناد /= ---
    اطبع_سطر("")
    اطبع_سطر("=== مثال 4: عامل /= ===")
    متغير قيمة = 100
    اطبع_سطر("القيمة الأصلية: " + قيمة)
    قيمة /= 5
    اطبع_سطر("بعد /= 5: " + قيمة)
    قيمة /= 4
    اطبع_سطر("بعد /= 4: " + قيمة)

    # --- مثال 5: عامل باقي القسمة والإسناد %= ---
    اطبع_سطر("")
    اطبع_سطر("=== مثال 5: عامل %= ===")
    متغير باقي = 17
    اطبع_سطر("القيمة الأصلية: " + باقي)
    باقي = باقي % 5
    اطبع_سطر("بعد %= 5: " + باقي)
    باقي = 29
    باقي = باقي % 7
    اطبع_سطر("29 %= 7 = " + باقي)

    # --- مثال 6: تراكم باستخدام += مع حلقة بينما ---
    اطبع_سطر("")
    اطبع_سطر("=== مثال 6: تراكم بحلقة بينما ===")
    متغير مجموع = 0
    متغير عداد = 1
    بينما (عداد <= 10)
        مجموع += عداد
        عداد += 1
    نهاية
    اطبع_سطر("مجموع الأعداد من 1 إلى 10: " + مجموع)

    # --- مثال 7: مضاعفة متكررة باستخدام *= ---
    اطبع_سطر("")
    اطبع_سطر("=== مثال 7: مضاعفة متكررة ===")
    متغير قاعدة = 2
    متغير ع = 0
    اطبع_سطر("البداية: " + قاعدة)
    بينما (ع < 5)
        قاعدة *= 2
        ع += 1
        اطبع_سطر("بعد المضاعفة " + ع + ": " + قاعدة)
    نهاية

    # --- مثال 8: تطبيق عملي - حساب المتوسط ---
    اطبع_سطر("")
    اطبع_سطر("=== مثال 8: حساب المتوسط ===")
    متغير مجموع_درجات = 0
    متغير عدد_الدرجات = 0

    مجموع_درجات += 85
    عدد_الدرجات += 1

    مجموع_درجات += 92
    عدد_الدرجات += 1

    مجموع_درجات += 78
    عدد_الدرجات += 1

    مجموع_درجات += 95
    عدد_الدرجات += 1

    مجموع_درجات += 88
    عدد_الدرجات += 1

    متغير المتوسط = مجموع_درجات / عدد_الدرجات
    اطبع_سطر("مجموع الدرجات: " + مجموع_درجات)
    اطبع_سطر("عدد الدرجات: " + عدد_الدرجات)
    اطبع_سطر("المتوسط: " + المتوسط)

    اطبع_سطر("")
    اطبع_سطر("تم الانتهاء من أمثلة الإسناد المركب")

نهاية
