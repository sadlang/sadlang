# مثال 3: العمليات المنطقية
# المستوى: مبتدئ
# يوضح العمليات المنطقية بالرموز وبالكلمات العربية


دالة رئيسية()
    اطبع_سطر("=== العمليات المنطقية بالرموز ===")

    # AND بالرمز &&
    اطبع_سطر("صحيح && صحيح: " + (صحيح && صحيح))
    اطبع_سطر("صحيح && خطأ: " + (صحيح && خطأ))
    اطبع_سطر("خطأ && خطأ: " + (خطأ && خطأ))

    اطبع_سطر("")

    # OR بالرمز ||
    اطبع_سطر("صحيح || خطأ: " + (صحيح || خطأ))
    اطبع_سطر("خطأ || خطأ: " + (خطأ || خطأ))

    اطبع_سطر("")

    # NOT بالرمز !
    اطبع_سطر("!صحيح: " + (!صحيح))
    اطبع_سطر("!خطأ: " + (!خطأ))

    # === الطريقة العربية ===
    اطبع_سطر("")
    اطبع_سطر("=== العمليات المنطقية بالعربية ===")

    # و (AND)
    اطبع_سطر("صحيح و صحيح: " + (صحيح و صحيح))
    اطبع_سطر("صحيح و خطأ: " + (صحيح و خطأ))

    # أو (OR)
    اطبع_سطر("صحيح أو خطأ: " + (صحيح أو خطأ))
    اطبع_سطر("خطأ أو خطأ: " + (خطأ أو خطأ))

    # ليس (NOT)
    اطبع_سطر("ليس صحيح: " + (ليس صحيح))
    اطبع_سطر("ليس خطأ: " + (ليس خطأ))

    # === مثال عملي: التحقق من الأهلية ===
    اطبع_سطر("")
    اطبع_سطر("=== التحقق من أهلية القبول ===")

    متغير العمر = 22
    متغير المعدل = 3.5
    متغير لديه_شهادة = صحيح

    # باستخدام الرموز
    متغير مؤهل = العمر >= 18 && المعدل >= 3.0 && لديه_شهادة
    اطبع_سطر("العمر: " + العمر + "، المعدل: " + المعدل)
    اطبع_سطر("مؤهل (بالرموز): " + مؤهل)

    # باستخدام الكلمات العربية
    متغير مؤهل2 = العمر >= 18 و المعدل >= 3.0 و لديه_شهادة
    اطبع_سطر("مؤهل (بالعربية): " + مؤهل2)

    # === شروط مركبة ===
    اطبع_سطر("")
    اطبع_سطر("=== شروط مركبة ===")

    متغير درجة_حرارة = 35
    إذا (درجة_حرارة > 40 أو درجة_حرارة < 0)
        اطبع_سطر("طقس متطرف!")
    وإلا_إذا (درجة_حرارة >= 25 و درجة_حرارة <= 40)
        اطبع_سطر("طقس حار")
    وإلا
        اطبع_سطر("طقس معتدل")
    نهاية
نهاية
