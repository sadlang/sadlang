# مثال 4: الكتابات البديلة للشروط
# المستوى: متوسط
# يوضح أن لغة ص تدعم عدة طرق لكتابة نفس الجمل الشرطية


دالة رئيسية()
    متغير الدرجة = 85

    # === الطريقة 1: بالهمزة (إذا / وإلا_إذا / وإلا) ===
    اطبع_سطر("=== الطريقة 1: بالهمزة ===")
    إذا (الدرجة >= 90)
        اطبع_سطر("ممتاز")
    وإلا_إذا (الدرجة >= 80)
        اطبع_سطر("جيد جداً")
    وإلا
        اطبع_سطر("جيد")
    نهاية

    # === الطريقة 2: بدون همزة (اذا / والا_اذا / والا) ===
    اطبع_سطر("")
    اطبع_سطر("=== الطريقة 2: بدون همزة ===")
    اذا (الدرجة >= 90)
        اطبع_سطر("ممتاز")
    والا_اذا (الدرجة >= 80)
        اطبع_سطر("جيد جداً")
    والا
        اطبع_سطر("جيد")
    نهاية

    # === مزج الطريقتين (يعمل أيضاً) ===
    اطبع_سطر("")
    اطبع_سطر("=== المزج بين الطريقتين ===")
    إذا (الدرجة > 50)
        اطبع_سطر("ناجح")
    والا
        اطبع_سطر("راسب")
    نهاية

    # === مثال عملي مع كلا الطريقتين ===
    اطبع_سطر("")
    اطبع_سطر("=== تصنيف الموظفين ===")

    متغير سنوات_الخبرة = 8
    متغير التقييم = "جيد"

    # الطريقة الأولى (بالهمزة)
    إذا (سنوات_الخبرة >= 10)
        اطبع_سطر("موظف خبير")
    وإلا_إذا (سنوات_الخبرة >= 5)
        اطبع_سطر("موظف متمرس")
    وإلا
        اطبع_سطر("موظف مبتدئ")
    نهاية

    # الطريقة الثانية (بدون همزة)
    اذا (التقييم == "ممتاز")
        اطبع_سطر("يستحق ترقية")
    والا_اذا (التقييم == "جيد")
        اطبع_سطر("أداء مرضي")
    والا
        اطبع_سطر("يحتاج تحسين")
    نهاية
نهاية
