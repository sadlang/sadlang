# مثال 3: شروط متداخلة
# المستوى: مبتدئ


دالة رئيسية()
    اطبع_سطر("=== نظام القبول الجامعي ===")

    متغير المعدل = 92
    متغير العمر = 19
    متغير لديه_شهادة = صحيح

    إذا (لديه_شهادة)
        اطبع_سطر("✓ لديه شهادة ثانوية")

        إذا (المعدل >= 90)
            اطبع_سطر("✓ معدل ممتاز: " + المعدل)

            إذا (العمر >= 17 && العمر <= 25)
                اطبع_سطر("✓ العمر مناسب: " + العمر)
                اطبع_سطر("═══ مقبول في الجامعة! ═══")
            وإلا
                اطبع_سطر("✗ العمر خارج النطاق المطلوب")
            نهاية

        وإلا_إذا (المعدل >= 80)
            اطبع_سطر("المعدل جيد جداً، مقبول مع شروط")
        وإلا
            اطبع_سطر("✗ المعدل أقل من الحد الأدنى")
        نهاية
    وإلا
        اطبع_سطر("✗ يجب الحصول على شهادة ثانوية أولاً")
    نهاية

    # === التحقق من رقم ===
    اطبع_سطر("")
    اطبع_سطر("=== تحليل العدد ===")

    متغير الرقم = -24

    إذا (الرقم != 0)
        إذا (الرقم > 0)
            اطبع_سطر(الرقم + " عدد موجب")
        وإلا
            اطبع_سطر(الرقم + " عدد سالب")
        نهاية

        إذا (الرقم % 2 == 0)
            اطبع_سطر(الرقم + " عدد زوجي")
            إذا (الرقم % 4 == 0)
                اطبع_سطر(الرقم + " قابل للقسمة على 4")
            نهاية
        وإلا
            اطبع_سطر(الرقم + " عدد فردي")
        نهاية
    وإلا
        اطبع_سطر("العدد هو صفر")
    نهاية
نهاية
