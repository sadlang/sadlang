# البنية الصحيحة لنظام الأخطاء + خطة التنفيذ

> **هذه الوثيقة تصحّح خلطَين معماريَّين كبيرَين** كشفهما المستخدم:
>
> 1. **خلط Phase F-1:** الأعلام `--gc/--learn/--prod` تخص **إدارة الذاكرة وأخطاء الملكية فقط**، ولا يجوز ربطها بأخطاء وقت التشغيل العامة.
> 2. **خلط البنية الكلية (الأهم):** المفسر والمترجم **لا يجوز أن يكون لكلٍّ منهما نظام أخطاء داخلي**. المسؤولية الكاملة لإدارة الأخطاء (تجميع، رمي، إيقاف، طباعة، توحيد رسائل، مستوى شرح) يجب أن تكون في الطبقة المشتركة `shared/errors`. المفسر والمترجم يستدعيان فقط `ErrorManager.report(...)` و `ErrorManager.throw(...)`.

> **انظر القسم 1.5** للبنية النهائية المعتمدة بناءً على هذا التصحيح.

---

## 1. التشخيص: ما الخطأ في التصميم الحالي؟

### 1.1 الخلط الذي يجب تصحيحه

في Phase F-1 رُبطت `DivisionByZero` بدالة `dispatch(RuntimeErrorKind, settings, ...)`
وأصبحت تتأثر بـ`--gc/--learn/--prod`:

```cpp
// ❌ خاطئ معمارياً (Phase F-1 الحالي)
if (r == 0) return handleDivByZero(...);  // يتأثر بسياسة الذاكرة
// النتيجة: --gc يُرجع 0 صامتاً، --prod يرمي خطأ
```

**لماذا هذا خطأ:** القسمة على صفر **خطأ رياضي مطلق**، لا يتغير بتغير سياسة الذاكرة.
لا معنى لقول "في وضع `--gc` لا توجد قسمة على صفر".

### 1.2 الفصل الصحيح بين ثلاثة مفاهيم منفصلة

```mermaid
flowchart LR
    subgraph Conceptual["3 مفاهيم منفصلة"]
        A["1. سياسة الذاكرة<br/>--gc / --learn / --prod<br/>(يحدد طريقة إدارة الذاكرة<br/>وصرامة فحص الملكية)"]
        B["2. مستوى شرح الأخطاء<br/>--explain=brief/normal/detailed/teacher<br/>(يحدد كم تفصيل في رسالة الخطأ)"]
        C["3. توحيد الرسائل<br/>(نفس النص للخطأ نفسه<br/>بين المفسر والمترجم)"]
    end
    A -.->|"مستقل"| B
    B -.->|"مستقل"| C
    A -.->|"مستقل"| C

    style A fill:#fce4ec
    style B fill:#e1f5fe
    style C fill:#e8f5e9
```

| المفهوم | يحكم | المسؤول | المثال |
|---|---|---|---|
| **سياسة الذاكرة** | استراتيجية GC + صرامة فحص الملكية | `MemoryModeSettings` + `dispatch(OwnershipErrorKind, ...)` | `use after move` تحت `--gc` يُتجاهل، تحت `--prod` خطأ قاتل |
| **مستوى الشرح** | كم تفصيل في رسالة الخطأ | `ExplanationLevel` (موجود مسبقاً في `teacher_mode.h`) | BRIEF: سطر واحد. TEACHER: 10 أسطر مع أمثلة |
| **توحيد الرسائل** | نفس نص الخطأ نفسه أينما حدث | `ErrorCatalog` (يجب إنشاؤه) | `RUN_DIVISION_BY_ZERO` → رسالة واحدة في المفسر والمترجم |

### 1.3 حجم المشكلة الفعلي

- **152 موقع `throw <SomeError>("نص...")`** في `interpreter/src/visitors/`
- كل واحد يكتب رسالته يدوياً → نصوص مختلفة لنفس الخطأ بين visitors.
- المترجم سيكتب رسائله في مكان آخر → **عدم تطابق مؤكَّد** بين المفسر والمترجم.
- مستوى التفصيل ثابت دائماً (لا BRIEF ولا DETAILED).
- لا أحد يعتبر `ExplanationLevel` رغم وجوده!

---

## 1.4 نظام الأخطاء الحالي في المترجم (`sadc` / `sad-build`) — توثيق مفصَّل

> هذا القسم أُضيف بطلب المستخدم لتوثيق ما هو موجود فعلياً قبل البدء في توحيده.

### 1.4.1 المكوِّنات الأربعة في المترجم

```mermaid
flowchart TB
    subgraph Driver["tools/compiler/ — driver layer"]
        CLI["CommandLineParser<br/>(compiler_driver_cli.cpp)"]
        Front["compiler_driver_frontend.cpp<br/>(فتح/كشف نوع الملفات)"]
        Anal["compiler_driver_analysis.cpp<br/>(borrow check + dispatch)"]
        Andr["compiler_driver_android.cpp"]
    end

    subgraph DiagSys["نظام التشخيص المحلي للمترجم"]
        DE["DiagnosticEngine<br/>(compiler_driver_diagnostics.cpp)"]
        D["struct Diagnostic<br/>{severity, message, message_ar,<br/>file, line, column,<br/>source_line, notes[]}"]
        Sev["enum DiagnosticSeverity<br/>{NOTE, WARNING, ERROR, FATAL}"]
        Print["Diagnostic::print(os, use_colors)<br/>+ DiagnosticEngine::print_diagnostics()"]
        DE -->|"يجمع"| D
        D -->|"يُصنَّف بـ"| Sev
        D -->|"يُطبع عبر"| Print
    end

    subgraph Shared["الطبقة المشتركة (تُستخدم جزئياً)"]
        EM["Sad::Errors::ErrorManager<br/>(singleton)"]
        Disp["Sad::Errors::dispatch(OwnershipErrorKind, ...)"]
        SES["SmartErrorSystem"]
    end

    subgraph CrashSys["مسار الانهيار الفوري"]
        TRE["throw std::runtime_error(...)<br/>(main_simple.cpp + c_types.cpp)"]
    end

    CLI -->|"errors"| DE
    Front -->|"errors"| DE
    Anal -->|"warnings"| DE
    Anal -->|"ownership errors"| Disp
    Disp -->|"DispatchResult.messageAr/En"| DE
    Andr -->|"errors"| DE

    Front -.->|"لم يُوصل بعد"| EM
    Anal -.->|"يتجاهل"| EM
    Front -->|"file not found etc"| TRE

    style DiagSys fill:#e1f5fe
    style Shared fill:#fff9c4
    style CrashSys fill:#ffebee
```

### 1.4.2 المكوِّنات بالتفصيل

#### (أ) `DiagnosticEngine` — نظام تشخيصات المترجم الأساسي

**الموقع:** [compiler_driver.h](tools/compiler/compiler_driver.h#L387-L415) + [compiler_driver_diagnostics.cpp](tools/compiler/compiler_driver_diagnostics.cpp#L299-L370)

**المسؤولية:**
- تجميع batch من رسائل التشخيص (ليس إيقافاً فورياً).
- 4 مستويات شدّة عبر `enum DiagnosticSeverity { NOTE, WARNING, ERROR, FATAL }`.
- 4 دوال `report_*`:
  ```cpp
  void report_note   (msg, file, line, column);
  void report_warning(msg, file, line, column);
  void report_error  (msg, file, line, column);
  void report_fatal  (msg, file, line, column);
  ```
- إحصاءات: `get_error_count()`, `get_warning_count()`, `has_errors()`.
- خيارات: `set_warnings_as_errors(bool)`, `set_color_diagnostics(bool)`.
- طباعة منسَّقة عبر `print_diagnostics(os, use_colors)`.

**هيكل `Diagnostic`:**
```cpp
struct Diagnostic {
    DiagnosticSeverity severity;
    std::string message;        // الإنجليزية
    std::string message_ar;     // العربية (اختيارية)
    std::string file;
    int line = -1;
    int column = -1;
    std::string source_line;    // السطر الكامل لطباعة caret
    std::vector<std::string> notes;
};
```

**نمط الطباعة الناتج:**
```
file.s:12:8: error: division by zero / قسمة على صفر
    ن = س / ع
           ^
note: hint message...
```

ملاحظة: المترجم يدعم **رسالة ثنائية اللغة في نفس السطر** بصيغة `"english / عربي"`،
بينما المفسر يستخدم `"(AR) ... \n(EN) ..."` على سطرين منفصلين — هذا تباين يجب
أن يحلّه `ErrorCatalog`.

#### (ب) `dispatch(OwnershipErrorKind, ...)` — مدخل أخطاء الملكية

**الموقع:** [compiler_driver_analysis.cpp:546](tools/compiler/compiler_driver_analysis.cpp#L546)

**التدفق:**
1. مرحلة `borrow_check` تُنتج `result.errors` (قائمة `BorrowError`).
2. لكل خطأ:
   - يُبنى `Sad::Errors::SourceLocation`.
   - يُستدعى `dispatch(error.kind, options_.memory_policy, loc, error.variableName)`.
   - بحسب `DispatchResult.action`:
     - `Ignore` (`--gc`) → يُحصى ويُتجاهَل.
     - `Warn` (`--learn`) → `diagnostics_.report_warning()` + إضافة `teachingNote`.
     - `Stop` (`--prod`) → `diagnostics_.report_error()` + رفع `fatal_count`.
3. اختيار اللغة عبر `options_.arabic_borrow_messages` (يبدو منفصلاً عن `--lang` العام).

**هذا الجزء صحيح ولا يحتاج تعديلاً** — فقط نصوص الرسائل ستأتي لاحقاً من `ErrorCatalog`.

#### (ج) `Sad::Errors::ErrorManager` — استخدام جزئي

**الموقع:** يُستخدم في [main_simple.cpp:550-554](tools/compiler/main_simple.cpp#L550) و [run_command.cpp:237](tools/compiler/src/run_command.cpp#L237) و [test_command.cpp:451](tools/compiler/src/test_command.cpp#L451).

**الوضع الحالي:**
- يُنادى عليه `clear()` و `setSourceCode()` فقط.
- نتائج `ErrorManager::hasErrors()` تُفحص في حالات قليلة.
- **لا** يُستخدم لتجميع أخطاء الترجمة الرئيسية (تذهب إلى `DiagnosticEngine` بدلاً منه).
- هذا ازدواجية واضحة: سيُحلّها `ErrorCatalog` بأن يصبح هو المصدر الوحيد للنصوص،
  بينما `ErrorManager` يبقى للإثراء و`DiagnosticEngine` للعرض.

#### (د) `throw std::runtime_error(...)` — مسار انهيار غير مُوحَّد

**الموقع:** [main_simple.cpp:105](tools/compiler/main_simple.cpp#L105) و [c_types.cpp:136,203,319,428](compiler/src/types/c_types.cpp).

**الوضع:** يُستخدم لأخطاء "لا يمكن المتابعة" (ملف غير موجود، نوع C غير مدعوم).
- لا يمر عبر `DiagnosticEngine` ولا `ErrorManager`.
- يُلتقط في `try/catch` في `main()` ويُطبع كرسالة بسيطة.
- نصوص حرفية (مثل `"لم يتم العثور على الملف: ..."`).

**هذا أيضاً يجب توحيده** — يجب أن يستخدم `ErrorCatalog` لتوليد النص ثم يُلقي.

### 1.4.3 موجز الفرق بين نظامَي المفسر والمترجم اليوم

| العنصر | المفسر (`sad`) | المترجم (`sadc`) |
|---|---|---|
| **آلية الخطأ الأساسية** | `throw SadException` (إيقاف فوري) | `DiagnosticEngine::report_*` (تجميع batch) |
| **عدد المستويات** | exception types (Type/Value/...) | `NOTE/WARNING/ERROR/FATAL` |
| **شكل الرسالة ثنائية اللغة** | `"(AR) ...\n(EN) ..."` | `"en / ar"` على سطر واحد |
| **`try/catch` للمستخدم** | نعم (`حاول/امسك`) | لا |
| **عدد رسائل مكتوبة يدوياً** | 152 موقع `throw <Error>("...")` | يُقدَّر بعشرات المواقع `report_error("...")` |
| **يدعم `--gc/--learn/--prod`** | ❌ (Phase F-1 خاطئ يحاول إضافتها) | ✅ لأخطاء الملكية فقط (صحيح) |
| **يستخدم `ErrorCatalog`** | ❌ غير موجود | ❌ غير موجود |
| **يستخدم `Sad::Errors::dispatch`** | ❌ غير موصول | ✅ لأخطاء الملكية فقط |
| **يستخدم `ErrorManager`** | ✅ كاملاً | ⚠️ جزئياً (clear/hasErrors فقط) |
| **`SmartErrorSystem` enrich** | ✅ عبر `enrichDiagnostic` | ⚠️ غير مُفعَّل بشكل ظاهر |

### 1.4.4 المشاكل التي يجب على `ErrorCatalog` حلّها

1. **تباين شكل ثنائية اللغة:** سطر واحد vs سطرين → `ErrorCatalog.render()` يوحِّد التنسيق.
2. **نصوص يدوية في الجانبين:** ~152 في المفسر + عشرات في `report_error("...")` المباشر بالمترجم → كلها تذهب إلى قوالب catalog.
3. **`std::runtime_error` خام:** بعض الأخطاء (file not found) لا تمر عبر أي نظام موحَّد → ستحصل على `ErrorCode` مثل `SYS_FILE_NOT_FOUND`.
4. **ازدواج `DiagnosticEngine` ↔ `ErrorManager`:** الأول للعرض CLI، الثاني للإثراء — `ErrorCatalog` يُلغي الازدواج بأن يكون المصدر الوحيد للنص.
5. **مستوى الشرح ثابت:** لا يوجد `--explain` → سيضيفه `ErrorCatalog` بدون كسر `DiagnosticEngine`.

### 1.4.5 خريطة الملفات المعنيّة بالتعديل في المترجم

| الملف | التعديل المتوقَّع | المرحلة |
|---|---|---|
| [compiler_driver_diagnostics.cpp](tools/compiler/compiler_driver_diagnostics.cpp) | لا تغيير في `DiagnosticEngine` نفسه | — |
| [compiler_driver_analysis.cpp](tools/compiler/compiler_driver_analysis.cpp) | استبدال نصوص `report_error/warning` بـ `Catalog::render(...)` | 4 |
| [compiler_driver_frontend.cpp](tools/compiler/compiler_driver_frontend.cpp) | استبدال `report_error("file not found ...")` بـ `Catalog::render(SYS_FILE_NOT_FOUND, ...)` | 4 |
| [compiler_driver_cli.cpp](tools/compiler/compiler_driver_cli.cpp) | إضافة `--explain=<level>` و `--lang=<ar/en/both>` | 2 |
| [main_simple.cpp](tools/compiler/main_simple.cpp) | استبدال `throw std::runtime_error(...)` بنفس النمط | 4 |
| [c_types.cpp](compiler/src/types/c_types.cpp) | استبدال `throw std::runtime_error(...)` بنفس النمط | 4 |
| [src/run_command.cpp](tools/compiler/src/run_command.cpp) + [src/test_command.cpp](tools/compiler/src/test_command.cpp) | لا تغييرات كبرى — فقط ضمان init للـ catalog | 1 |

---

## 1.5 التصحيح المعماري الكلي: لا نظام أخطاء داخلي في المفسر ولا في المترجم

> **هذا التصحيح أعمق من القسم 1.1 ويغيّر شكل الخطة بأكملها.**
>
> طلب المستخدم الحرفي:
> > «لا حاجة للمفسر أن يكون لديه نظام أخطاء أبداً وكذلك المترجم. لكنهم يستخدمون نظاماً
> > خاصاً بهم وهذا خطأ كبير. من المفروض عند حدوث خطأ في المفسر، المفسر يستدعي نظام
> > الأخطاء من الطبقة المشتركة... `errorManager.throw(نوع الخطأ, ...)` ويتوقف.
> > والمترجم في وقت الترجمة يجمع الأخطاء من نفس الطبقة المشتركة، وفي وقت التشغيل
> > يستعمل دالة الرمي من مدير الأخطاء. وبذلك المفسر والمترجم ليس لديهم أي تعامل أو
> > تنفيذ لإدارة الأخطاء أبداً، إنما يستدعون كل شيء من الطبقة المشتركة.»

### 1.5.1 الوضع الراهن (مخالف للمبدأ)

```mermaid
flowchart LR
    subgraph Interp["المفسر (يحتوي نظاماً داخلياً)"]
        IExc["throw SadException<br/>+ DivisionByZeroError<br/>+ TypeError + ..."]
        IExc -.->|"152 موقع<br/>برسائل يدوية"| IExc
    end
    subgraph Comp["المترجم (يحتوي نظاماً داخلياً)"]
        DE["DiagnosticEngine<br/>(محرك تشخيص خاص)"]
        Sev["DiagnosticSeverity"]
        Diag["Diagnostic struct"]
        Print["print() مع caret"]
        DE --> Sev
        DE --> Diag
        Diag --> Print
    end
    subgraph Shared["shared/errors (مُهمَّش)"]
        EM["ErrorManager"]
        Disp["dispatch(Ownership)"]
    end

    Interp -.->|"لا يستخدم"| Shared
    Comp -.->|"يستخدم جزئياً<br/>(للملكية فقط)"| Disp
    Comp -.->|"clear/hasErrors فقط"| EM

    style Interp fill:#ffebee
    style Comp fill:#ffebee
    style Shared fill:#e8f5e9
```

**المشاكل:**
- كل من المفسر والمترجم يحمل نظاماً داخلياً مكتفياً ذاتياً.
- ازدواجية كاملة في تنسيق الرسائل (سطرين `(AR)/(EN)` vs `en / ar`).
- 152 + عشرات المواقع تكتب نصوص حرفية بدل استدعاء طبقة موحَّدة.
- `ErrorManager` المشترك مُهمَّش ولا يُستخدم لرمي الأخطاء.

### 1.5.2 البنية المعتمدة الجديدة (هدف التصحيح)

```mermaid
flowchart TB
    subgraph Interp["المفسر (نحيف — لا نظام أخطاء داخلي)"]
        IDet["كشف الخطأ:<br/>r == 0"]
        ICall["EM.throwRuntime(<br/>RUN_DIVISION_BY_ZERO,<br/>loc, ctx)"]
        IDet --> ICall
    end

    subgraph Comp["المترجم (نحيف — لا نظام أخطاء داخلي)"]
        CDet["كشف الخطأ:<br/>parse error<br/>borrow error<br/>type mismatch"]
        CRep["EM.report(<br/>SEM_TYPE_MISMATCH,<br/>WARNING/ERROR,<br/>loc, ctx)"]
        CFlush["EM.flush()<br/>(في نهاية المرحلة)"]
        CRun["// وقت تشغيل البرنامج المُترجَم<br/>EM.throwRuntime(<br/>RUN_INDEX_OOB, ...)"]
        CDet --> CRep
        CRep --> CFlush
        CDet --> CRun
    end

    subgraph Shared["shared/errors (المسؤول الوحيد)"]
        Cat["ErrorCatalog<br/>(قوالب رسائل موحَّدة)"]
        Mgr["ErrorManager<br/>(نقطة دخول وحيدة)"]
        Form["Formatter<br/>(عرض ثنائي اللغة + caret + notes)"]
        Disp["dispatch(Ownership)<br/>(يبقى هنا)"]
        Smart["SmartErrorSystem<br/>(إثراء + fix suggestions)"]

        Mgr -->|"render text"| Cat
        Mgr -->|"enrich"| Smart
        Mgr -->|"display"| Form
        Mgr --> Disp
    end

    Interp ==> Mgr
    Comp ==> Mgr

    style Interp fill:#e8f5e9
    style Comp fill:#e8f5e9
    style Shared fill:#fff9c4
```

### 1.5.3 الواجهة الموحَّدة لـ`ErrorManager` (نقطة الدخول الوحيدة)

```cpp
// shared/errors/include/error_manager.h
namespace Sad::Errors {

class ErrorManager {
public:
    static ErrorManager& instance();

    // ───────────────────────────────────────────────────────
    // (1) للمفسر + لوقت تشغيل البرنامج المُترجَم
    //     يبني الرسالة من Catalog ويرمي SadException المناسب
    //     يوقف التنفيذ فوراً (للمفسر) أو يستدعي runtime panic
    // ───────────────────────────────────────────────────────
    [[noreturn]] void throwRuntime(
        ErrorCode code,
        const SourceLocation& loc,
        const RenderContext& ctx = {}) const;

    // اختصارات شائعة (تستدعي throwRuntime داخلياً):
    [[noreturn]] void throwDivisionByZero(const SourceLocation& loc) const;
    [[noreturn]] void throwIndexOutOfRange(const SourceLocation&, int idx, int size) const;
    [[noreturn]] void throwTypeError(const SourceLocation&, const std::string& expected, const std::string& got) const;
    // ... إلخ

    // ───────────────────────────────────────────────────────
    // (2) للمترجم — وضع التجميع batch
    //     لا يرمي، يجمع في قائمة داخلية، يُطبع بـflush()
    // ───────────────────────────────────────────────────────
    void report(
        ErrorCode code,
        Severity sev,
        const SourceLocation& loc,
        const RenderContext& ctx = {});

    void reportNote   (ErrorCode code, const SourceLocation&, const RenderContext& = {});
    void reportWarning(ErrorCode code, const SourceLocation&, const RenderContext& = {});
    void reportError  (ErrorCode code, const SourceLocation&, const RenderContext& = {});
    void reportFatal  (ErrorCode code, const SourceLocation&, const RenderContext& = {});

    // ───────────────────────────────────────────────────────
    // (3) إدارة الـbatch
    // ───────────────────────────────────────────────────────
    bool hasErrors() const;
    bool hasFatals() const;
    std::size_t errorCount() const;
    std::size_t warningCount() const;
    void flush(std::ostream& os = std::cerr) const;  // طباعة منسَّقة (caret + colors)
    void clear();

    // ───────────────────────────────────────────────────────
    // (4) إعدادات
    // ───────────────────────────────────────────────────────
    void setExplanationLevel(ExplanationLevel level);
    void setLanguage(Language lang);
    void setSourceCode(const std::string& src, const std::string& filename);
    void setMemoryPolicy(const MemoryModeSettings& policy);  // لـdispatch(Ownership)
    void setColors(bool enabled);
    void setWarningsAsErrors(bool enabled);
};

}  // namespace Sad::Errors
```

### 1.5.4 ما يبقى في المفسر وما يبقى في المترجم بعد التصحيح

| المكون | قبل التصحيح | بعد التصحيح |
|---|---|---|
| **المفسر:** `throw RuntimeError("نص")` | ✅ موجود (152 موقع) | ❌ يُحذف. يُستبدل بـ`EM.throwRuntime(code, loc, ctx)` |
| **المفسر:** أصناف SadException | ✅ موجودة | ✅ تبقى — لكن تُرمى **فقط من داخل** `ErrorManager.throwRuntime` |
| **المفسر:** `try/catch` للمستخدم (`حاول/امسك`) | ✅ يلتقط `SadException` | ✅ يبقى — يلتقط نفس الأصناف (شفّاف) |
| **المترجم:** `DiagnosticEngine` | ✅ موجود (~150 سطر) | ❌ يُحذف بالكامل. الوظائف تُنقل إلى `ErrorManager` |
| **المترجم:** `Diagnostic` struct + Severity + print | ✅ موجود | ❌ يُحذف. يُستبدل بـمكافئاته داخل `shared/errors` |
| **المترجم:** `report_error/warning/...` | ✅ موجود (عشرات النداءات) | ❌ يُستبدل بـ`EM.reportError/Warning(code, loc, ctx)` |
| **المترجم:** `throw std::runtime_error(...)` خام | ✅ موجود | ❌ يُستبدل بـ`EM.throwRuntime(SYS_FILE_NOT_FOUND, ...)` |
| **المشترك:** `ErrorCatalog` | ❌ غير موجود | ✅ يُنشأ — مصدر كل النصوص |
| **المشترك:** `ErrorManager` | ⚠️ موجود لكن مُهمَّش | ✅ نقطة الدخول الوحيدة لكل العمليات |
| **المشترك:** `Formatter` (caret + colors + ar/en) | ❌ مبعثر | ✅ يُنقل من `Diagnostic::print` إلى الطبقة المشتركة |
| **المشترك:** `dispatch(Ownership)` | ✅ موجود | ✅ يبقى — يُستدعى من داخل `ErrorManager.report` تلقائياً عند الكشف |

### 1.5.5 مثال "قبل/بعد" كامل

#### المفسر — DivByZero

**قبل (الحالي + Phase F-1 الخاطئ):**
```cpp
// expression_evaluator_binary_ops.cpp
auto handleDivByZero = [&](Position pos) -> Value {
    auto disp = dispatch(RuntimeErrorKind::DivisionByZero,
                         statementExecutor_.getMemoryPolicy(), loc, "");
    if (disp.shouldStop()) throw DivisionByZeroError(disp.messageAr + ...);
    return Value::makeNull();
};
if (r == 0) return handleDivByZero(pos);
```

**بعد (البنية الجديدة):**
```cpp
// expression_evaluator_binary_ops.cpp
if (r == 0) {
    Sad::Errors::ErrorManager::instance().throwDivisionByZero(toLoc(pos));
    // [[noreturn]] — لا حاجة لـreturn
}
```

كل المنطق (بناء الرسالة، اختيار اللغة، مستوى الشرح، رمي الاستثناء الصحيح) داخل `ErrorManager`.

#### المترجم — file not found

**قبل:**
```cpp
// compiler_driver_frontend.cpp
diagnostics_.report_error("file not found: " + input_file +
                          " / لم يتم العثور على الملف: " + input_file);
```

**بعد:**
```cpp
Sad::Errors::ErrorManager::instance().reportError(
    ErrorCode::SYS_FILE_NOT_FOUND,
    {input_file, 0, 0},
    {{"path", input_file}});
```

#### المترجم — وقت تشغيل البرنامج المُترجَم (runtime panic)

**قبل:** غير موحَّد (يُولَّد runtime call مخصَّص).

**بعد:** يُولَّد استدعاء `__sad_runtime_throw(code, file, line, col, ctx_json)`
الذي ينادي بدوره `ErrorManager::throwRuntime` داخل runtime لغة ص.

### 1.5.6 الفوائد الإضافية لهذا التصحيح

1. **سطر واحد فقط في المفسر/المترجم لكل خطأ** — لا منطق نص، لا منطق سياسة، لا منطق طباعة.
2. **توحيد كامل بدون مجهود إضافي** — لأن النص يُولَّد من مكان واحد.
3. **اختبار `ErrorManager` مرة واحدة يغطي الجميع** — بدلاً من اختبار 152 موقع.
4. **دعم لغات/مستويات شرح جديدة بتعديل ملف واحد** — `ErrorCatalog`.
5. **حذف ~500 سطر** من `DiagnosticEngine` + استثناءات MSG-اليد + ازدواجية printing.
6. **إعادة استخدام مباشر** للنظام في: LSP server، REPL، runtime البرنامج المُترجَم، أدوات IDE.

---

## 2. البنية المستهدفة

### 2.1 المخطط الكلي للنظام بعد التصحيح

```mermaid
flowchart TB
    subgraph CLI["الواجهات"]
        Sad["sad-run.exe"]
        Sadc["sad-build.exe"]
    end

    subgraph Flags["أعلام سطر الأوامر — منفصلة"]
        F1["سياسة الذاكرة:<br/>--gc / --learn / --prod"]
        F2["مستوى الشرح:<br/>--explain=brief/normal/detailed/teacher"]
        F3["لغة الإخراج:<br/>--lang=ar/en/both"]
    end

    subgraph Catalog["📚 ErrorCatalog (جديد)"]
        EC["جدول مركزي:<br/>ErrorCode → {ar, en, hint, example, fix}"]
        Render["renderMessage(code, level, lang, ctx)"]
        EC --> Render
    end

    subgraph Memory["إدارة الذاكرة فقط"]
        MMS["MemoryModeSettings<br/>(--gc/--learn/--prod)"]
        OwnDisp["dispatch(OwnershipErrorKind, settings, ...)"]
        MMS --> OwnDisp
    end

    subgraph Runtime["أخطاء وقت التشغيل العامة"]
        Throws["throw RuntimeError/<br/>DivisionByZeroError/<br/>IndexOutOfRangeError/..."]
        Throws -->|"يستدعي ErrorCatalog لبناء الرسالة"| Render
    end

    subgraph SmartLayer["الإثراء الذكي"]
        EM["ErrorManager"]
        SES["SmartErrorSystem"]
        EM --> SES
        SES -->|"يقرأ ExplanationLevel"| Render
    end

    Sad --> F1
    Sad --> F2
    Sad --> F3
    Sadc --> F1
    Sadc --> F2
    Sadc --> F3

    F1 --> MMS
    F2 -->|"يحدد level للـrender"| Render
    F3 -->|"يحدد lang للـrender"| Render

    Sad -->|"runtime errors"| Throws
    Sad -->|"ownership errors"| OwnDisp
    Sadc -->|"compile errors"| EM
    Sadc -->|"ownership errors"| OwnDisp

    Throws --> EM
    OwnDisp --> EM
    EM --> SES

    style Catalog fill:#fff9c4
    style Memory fill:#fce4ec
    style Runtime fill:#fff3e0
    style Flags fill:#e1f5fe
```

### 2.2 الفروقات الجوهرية مع التصميم الحالي

| البند | الحالي (خاطئ) | المستهدف (صحيح) |
|---|---|---|
| DivByZero يحترم `--gc/--learn/--prod` | ✅ نعم (Phase F-1) | ❌ لا — خطأ قاتل دائماً |
| رسائل الأخطاء متطابقة بين sad/sadc | ❌ لا (152 رسالة مكتوبة يدوياً) | ✅ نعم — `ErrorCatalog` المركزي |
| مستوى تفصيل قابل للضبط | ❌ ثابت | ✅ `--explain=brief/normal/detailed/teacher` |
| `dispatch(RuntimeErrorKind)` موجود | ✅ (Phase F-1 جديد) | ❌ يُحذف — كان خلطاً |
| `dispatch(OwnershipErrorKind)` موجود | ✅ (Phase E-3) | ✅ يبقى — هذا صحيح |

---

## 3. تصميم `ErrorCatalog` — القلب الجديد

### 3.1 المفهوم

```cpp
// shared/errors/include/error_catalog.h
namespace Sad::Errors {

struct ErrorTemplate {
    ErrorCode code;
    std::string titleAr;        // "قسمة على صفر"
    std::string titleEn;        // "Division by zero"
    std::string briefAr;        // سطر واحد
    std::string briefEn;
    std::string detailedAr;     // فقرة كاملة
    std::string detailedEn;
    std::string teacherAr;      // شرح + مثال + حل
    std::string teacherEn;
    std::string fixHintAr;      // اقتراح إصلاح
    std::string fixHintEn;
    std::string codeExample;    // مثال كود لغة ص
};

class ErrorCatalog {
public:
    static ErrorCatalog& instance();

    /// (AR) يبني رسالة خطأ موحَّدة وفق المستوى واللغة
    /// (EN) Builds a unified error message per level and language
    std::string render(ErrorCode code,
                      ExplanationLevel level,
                      Language lang,
                      const RenderContext& ctx) const;

    /// (AR) يسجِّل قالباً جديداً (يُستدعى من initialization)
    void registerTemplate(ErrorTemplate tmpl);

private:
    std::unordered_map<ErrorCode, ErrorTemplate> templates_;
};

struct RenderContext {
    SourceLocation location;
    std::map<std::string, std::string> placeholders;  // {x} → "س", {y} → "ع"
};

}  // namespace Sad::Errors
```

### 3.2 الاستخدام من المفسر (بدلاً من الـ152 موقع)

**قبل (الحالي):**
```cpp
// 152 موقع مماثل، كل واحد بصياغة مختلفة:
throw RuntimeError(
    "(AR) خطأ: إزاحة بقيمة " + std::to_string(r) + " غير صالحة. يجب أن تكون بين 0 و 31.\n"
    "(EN) Error: Shift amount " + std::to_string(r) + " is invalid. Must be between 0 and 31.",
    pos);
```

**بعد (المستهدف):**
```cpp
RenderContext ctx{pos, {{"value", std::to_string(r)}, {"min", "0"}, {"max", "31"}}};
throw RuntimeError(
    ErrorCatalog::instance().render(
        ErrorCode::RUN_INVALID_SHIFT_AMOUNT,
        ExplanationLevel::current(),  // يقرأ من إعدادات المستخدم
        Language::current(),
        ctx),
    pos);
```

### 3.3 الاستخدام من المترجم (نفس النص بالضبط)

```cpp
// في compiler_driver_diagnostics.cpp
diagnostics_.report_error(
    ErrorCatalog::instance().render(
        ErrorCode::RUN_INVALID_SHIFT_AMOUNT,
        opts.explainLevel,
        opts.language,
        RenderContext{loc, {{"value", std::to_string(r)}, ...}}),
    file, loc.line);
```

**النتيجة:** نفس نص الرسالة بالضبط في كلا الأداتين، يتغير فقط بحسب
`--explain` و`--lang`.

---

## 4. تدفق خطأ DivByZero بعد التصحيح

```mermaid
sequenceDiagram
    participant U as المستخدم
    participant V as ExpressionEvaluator
    participant Cat as ErrorCatalog
    participant E as DivisionByZeroError
    participant EM as ErrorManager
    participant Out as stderr

    Note over U: sad-run --explain=teacher --lang=ar divzero.s
    U->>V: تنفيذ 5/0
    V->>V: r == 0
    V->>Cat: render(RUN_DIVISION_BY_ZERO,<br/>TEACHER, ARABIC, ctx)
    Cat-->>V: نص موحَّد + شرح + مثال
    V->>E: throw DivisionByZeroError(نص, pos)
    E->>EM: registered via report
    EM-->>Out: print enriched
    Note over U: --gc و --prod لا فرق هنا<br/>الخطأ قاتل دائماً
```

**ملاحظة:** `--gc/--learn/--prod` **لا تظهر في المسار** — هذا خطأ رياضي،
سياسة الذاكرة لا علاقة لها به.

---

## 5. تدفق خطأ ملكية (use after move) — يبقى مرتبطاً بسياسة الذاكرة

```mermaid
sequenceDiagram
    participant V as Visitor
    participant OD as dispatch(OwnershipErrorKind)
    participant MMS as MemoryModeSettings
    participant Cat as ErrorCatalog
    participant EM as ErrorManager

    V->>OD: dispatch(UseAfterMove, settings, loc, "اسم")
    OD->>MMS: قراءة المستوى
    MMS-->>OD: --learn → Warnings
    OD->>Cat: render(OWN_USE_AFTER_MOVE,<br/>currentLevel, currentLang, ctx)
    Cat-->>OD: نص موحَّد
    OD->>EM: reportWarning(...)
    OD-->>V: DispatchResult{Warn}
    V->>V: تستمر التنفيذ
```

هنا `--gc/--learn/--prod` **مهمة** لأن مستوى صرامة الملكية قرار سياسي حقيقي.

---

## 6. التراتبية بعد التصحيح

```mermaid
flowchart TB
    subgraph Layer1["الطبقة 1: قاعدة الرسائل"]
        EC["ErrorCatalog<br/>(قوالب الرسائل + render)"]
        EL["ExplanationLevel<br/>(BRIEF/NORMAL/DETAILED/TEACHER)"]
        Lang["Language (ar/en/both)"]
    end

    subgraph Layer2["الطبقة 2: قنوات الإصدار"]
        Throws["throw SadException<br/>(للمفسر — إيقاف فوري)"]
        Reports["reportError/Warning<br/>(تسجيل + إثراء)"]
        Diag["DiagnosticEngine<br/>(للمترجم — تجميع + تنسيق CLI)"]
    end

    subgraph Layer3["الطبقة 3: قرارات السياسة"]
        OwnDisp["dispatch(OwnershipErrorKind)<br/>↔ --gc/--learn/--prod"]
        Note["⚠️ لا dispatch لـRuntimeErrorKind"]
    end

    Throws --> EC
    Reports --> EC
    Diag --> EC
    OwnDisp --> EC
    OwnDisp --> Reports

    EL --> EC
    Lang --> EC

    style Layer1 fill:#fff9c4
    style Layer2 fill:#e3f2fd
    style Layer3 fill:#fce4ec
```

---

## 7. خطة التنفيذ المُحدَّثة — 7 مراحل (بناءً على البنية الجديدة في القسم 1.5)

> **ملاحظة:** هذه الخطة تستبدل الخطة الأصلية ذات الـ5 مراحل أعلاه.
> السبب: تصحيح القسم 1.5 يلغي الحاجة إلى `DiagnosticEngine` ويغيّر شكل المراحل 2-5.

### نظرة عامة على المراحل السبع

| # | الاسم | الزمن التقديري | المخرَج الرئيسي |
|---|---|---|---|
| 0 | تراجع Phase F-1 | 15 د | DivByZero يرمي مباشرة بدون dispatch |
| 1 | بناء `ErrorCatalog` + `ErrorManager` المُحدَّث | 1-2 س | API موحَّد + ~50 قالب أساسي |
| 2 | نقل `Formatter` (caret + colors + ar/en) | 30 د | منسِّق طباعة في `shared/errors` |
| 3 | علم `--explain` و `--lang` و توصيلها | 30 د | علمان يعملان في sad وsadc |
| 4 | ترحيل المفسر (152 throw → `EM.throwRuntime`) | 3-4 جلسات | صفر `throw <Error>("..."` يدوي |
| 5 | ترحيل المترجم + حذف `DiagnosticEngine` | 2 س | حذف ~150 سطر، استبدال كامل |
| 6 | runtime hooks للبرنامج المُترجَم | 1 س | `__sad_runtime_throw` يستدعي EM |
| 7 | توثيق + تنظيف + اختبارات تطابق | 1 س | اختبارات تثبت وحدة الرسائل بين sad/sadc |

---

### المرحلة 0: التراجع عن خطأ Phase F-1 (15 دقيقة)

**الهدف:** فك ربط DivByZero/ModuloByZero عن `--gc/--learn/--prod`.

**الإجراءات:**
1. في `expression_evaluator_binary_ops.cpp`: حذف `handleDivByZero` lambda.
2. استعادة `throw DivisionByZeroError(...)` المباشر في الـ5 مواقع.
3. **عدم** حذف `RuntimeErrorKind` من `dispatch.h` بعد — سيُستخدم لاحقاً
   كمفتاح للـ`ErrorCatalog` (لكن دون منطق سياسة الذاكرة).
4. تحديث ذاكرة `dispatch_unification_phase_e3.md` بأن Phase F-1 خطأ معماري مُتراجَع عنه.

**معيار النجاح:** اختبار `5/0` يرمي خطأ قاتل مستقلاً عن العَلَم المُمرَّر.

---

### المرحلة 1: بناء `ErrorCatalog` + إعادة هندسة `ErrorManager` (1-2 ساعة)

**الهدف:** بناء قاعدة الرسائل + جعل `ErrorManager` نقطة الدخول الوحيدة.

**الإجراءات:**
1. إنشاء `shared/errors/include/error_catalog.h`:
   - `enum class ErrorCode { RUN_*, SEM_*, PARSE_*, OWN_*, SYS_* }`
   - `struct ErrorTemplate` (titleAr/En × brief/detailed/teacher + fixHint + example)
   - `struct RenderContext { SourceLocation; std::map<std::string,std::string> placeholders; }`
   - `class ErrorCatalog` singleton مع `render(code, level, lang, ctx)` و `registerTemplate()`.
2. إنشاء `shared/errors/src/error_catalog.cpp` + `error_catalog_init.cpp`
   (~50 قالب أساسي مع نصوص ar/en × مستويات الشرح).
3. **توسيع `ErrorManager`** ليشمل واجهات القسم 1.5.3:
   - `throwRuntime(code, loc, ctx)` `[[noreturn]]` — يبني النص من Catalog ويرمي `SadException` المناسب.
   - `report*(code, loc, ctx)` — يجمع في batch داخلي.
   - `flush(os)` — يطبع الكل (يستدعي `Formatter` من المرحلة 2).
   - الإعدادات: `setExplanationLevel`, `setLanguage`, `setMemoryPolicy`, `setSourceCode`, `setColors`.
4. إضافة `ErrorManager::instance().initializeDefaults()` في `main()` لكل من `sad` و`sadc`.
5. تحديث `shared/CMakeLists.txt` لإدراج الملفات الجديدة.

**معيار النجاح:** 
- بناء نظيف للطبقة المشتركة وحدها.
- اختبار وحدة: `EM.throwRuntime(RUN_DIVISION_BY_ZERO, loc)` يرمي `DivisionByZeroError` بنص صحيح.
- اختبار وحدة: `EM.reportError(...) → EM.flush()` يطبع رسالة منسَّقة.

---

### المرحلة 2: نقل `Formatter` (caret + colors + ar/en) إلى الطبقة المشتركة (30 دقيقة)

**الهدف:** نقل منطق طباعة `Diagnostic::print` من المترجم إلى `shared/errors`.

**الإجراءات:**
1. إنشاء `shared/errors/include/formatter.h` + `src/formatter.cpp`:
   - `class Formatter` يحوي:
     - `printDiagnostic(os, severity, code, message, loc, source_line, notes, opts)`.
     - منطق caret `^` تحت العمود.
     - ألوان ANSI (نفس الموجود في `compiler_driver_diagnostics.cpp`).
     - تنسيق ثنائي اللغة موحَّد (تختاره `--lang`).
2. ربط `ErrorManager::flush()` بـ `Formatter`.
3. **عدم حذف** `DiagnosticEngine` بعد — يُحذف في المرحلة 5.

**معيار النجاح:** خطأ يُسجَّل عبر `EM.reportError(...)` ثم `EM.flush()` يُنتج
نفس شكل الإخراج الحالي للمترجم (caret + colors + bilingual).

---

### المرحلة 3: علم `--explain` و `--lang` وتوصيلهما (30 دقيقة)

**الهدف:** علمَا CLI يعملان في sad وsadc.

**الإجراءات:**
1. استخراج `ExplanationLevel` من `teacher_mode.cpp` إلى `shared/errors/include/explanation_level.h`.
2. إنشاء `enum class Language { ARABIC, ENGLISH, BOTH }` في نفس الموقع.
3. إضافة دوال parse: `parseExplanationLevel(string)`, `parseLanguage(string)`.
4. تعديل `tools/compiler/compiler_driver_cli.cpp`:
   - معالجة `--explain=<level>` و `--lang=<ar/en/both>`.
   - تخزين في `CompilerOptions` ثم استدعاء `EM.setExplanationLevel(...)` و `EM.setLanguage(...)`.
5. تعديل `tools/sad/main.cpp` (المفسر) بنفس المعالجة.
6. ربط `SmartErrorSystem::enrichDiagnostic` بـ`EM.getExplanationLevel()`.

**معيار النجاح:**
- `sad-run --explain=brief --lang=ar divzero.s` → سطر واحد عربي.
- `sad-run --explain=teacher --lang=both divzero.s` → شرح كامل ثنائي اللغة.

---

### المرحلة 4: ترحيل المفسر — حذف 152 `throw <Error>("...")` يدوي (3-4 جلسات)

**الهدف:** صفر استثناءات يدوية في `interpreter/src/visitors/` — كلها عبر `EM`.

#### المرحلة 4-أ: جرد + تصنيف + قوالب (1 ساعة)

1. سكريبت يستخرج كل `throw <Error>("...")` من `interpreter/src/visitors/`.
2. تصنيف الـ152 موقع:
   - **مكرَّرات:** نفس النص في عدة مواقع → قالب واحد.
   - **متفرِّدات:** نص فريد → قالب خاص.
   - **placeholder-heavy:** يحتاج معاملات.
3. توليد `_scratch/error_migration_plan.csv`: `موقع → ErrorCode مقترح → نص قالب`.
4. إضافة كل القوالب الجديدة إلى `error_catalog_init.cpp`.

#### المرحلة 4-ب: الترحيل البَنِيوي (2-3 جلسات)

لكل ملف visitor:
1. استبدال كل `throw RuntimeError("نص...", pos)` بـ:
   ```cpp
   Sad::Errors::ErrorManager::instance().throwRuntime(
       ErrorCode::RUN_XXX, toLoc(pos), {{"name", varName}, ...});
   ```
2. حذف صياغة الرسائل القديمة (نص الـ`(AR)/(EN)`).
3. اختبار الـvisitor المعدَّل قبل الانتقال للتالي.
4. التحقق بسكريبت: عدد `throw <SubclassOf SadException>("` في `visitors/` = 0.

**معيار النجاح:** صفر `throw <Error>("..."` نصياً في الـvisitors + جميع اختبارات
المفسر تنجح + رسائل الخطأ متَّسقة.

---

### المرحلة 5: ترحيل المترجم + حذف `DiagnosticEngine` كاملاً (2 ساعة)

**الهدف:** المترجم لا يحوي نظام أخطاء — كل شيء عبر `EM`.

**الإجراءات:**
1. **استبدال جميع نداءات `diagnostics_.report_*("...")`** في:
   - `compiler_driver_frontend.cpp` (file not found, unknown file type, ...)
   - `compiler_driver_analysis.cpp` (دمج تلقائي مع dispatch)
   - `compiler_driver_cli.cpp` (invalid target triple, ...)
   - `compiler_driver_android.cpp`
   
   كلها تصبح `Sad::Errors::ErrorManager::instance().reportError(code, loc, ctx);`
2. **استبدال `throw std::runtime_error(...)`** في:
   - `tools/compiler/main_simple.cpp:105` (file not found)
   - `compiler/src/types/c_types.cpp:136,203,319,428` (unsupported C type)
   
   كلها تصبح `EM.throwRuntime(SYS_*, loc, ctx);`
3. **حذف فعلي:**
   - `class DiagnosticEngine` بأكمله من `compiler_driver.h`.
   - `struct Diagnostic` و `enum DiagnosticSeverity`.
   - `compiler_driver_diagnostics.cpp` يُحذف أو يصبح فارغاً.
   - `CompilerDriver::diagnostics_` member يُحذف، يُستبدل بـ`EM.instance()`.
4. **استبدال** `if (diagnostics_.has_errors())` بـ `if (EM.hasFatals())`.
5. **استبدال** `diagnostics_.print_diagnostics(std::cerr)` بـ `EM.flush(std::cerr)`.

**معيار النجاح:**
- `grep -r "DiagnosticEngine" tools/ compiler/` → صفر نتائج.
- `grep -r "diagnostics_\." tools/ compiler/` → صفر نتائج.
- `sadc` يبني ويعمل، رسائل الأخطاء متطابقة شكلاً مع `sad`.

---

### المرحلة 6: runtime hooks للبرنامج المُترجَم (1 ساعة)

**الهدف:** البرنامج بعد ترجمته بـ`sadc` يستخدم نفس `ErrorManager` لرمي أخطاء وقت التشغيل.

**الإجراءات:**
1. إنشاء `runtime/include/sad_runtime_throw.h`:
   ```c
   void __sad_runtime_throw(int code, const char* file, int line, int col, const char* ctx_json);
   ```
2. تنفيذ في `runtime/src/sad_runtime_throw.cpp`:
   - يدعو `Sad::Errors::ErrorManager::instance().throwRuntime(...)`.
3. تعديل `compiler_new/src/llvm/llvm_codegen_*.cpp`:
   - عند توليد كود لـcheck (DivByZero, IndexOOB, NullDeref, ...) يُولَّد call إلى `__sad_runtime_throw` بدل code المخصَّص الحالي.
4. ربط runtime مع المخرَج النهائي.

**معيار النجاح:**
- اختبار: `sadc divzero.s -o divzero.exe; ./divzero.exe` → نفس الرسالة بالضبط
  من `sad-run divzero.s`.

---

### المرحلة 7: التوثيق والتنظيف + اختبارات تطابق (1 ساعة)

**الإجراءات:**
1. تحديث `docs/بنية_المشروع/معمارية_نظام_الأخطاء.md` بالبنية الجديدة.
2. تحديث `docs/بنية_المشروع/نظام_الأخطاء.md`.
3. تحديث ملاحظات الذاكرة `compiler_fix_notes.md` و `dispatch_unification_phase_e3.md`.
4. حذف `RuntimeErrorKind` من `dispatch.h` (لم يعد مستخدماً للسياسة).
5. **اختبارات تطابق جديدة:** `tests/error_unification/`:
   - لكل خطأ شائع (DivByZero, IndexOOB, TypeMismatch, FileNotFound, ...):
     يقارن مخرَج `sad-run` مع مخرَج `./compiled.exe` → يجب أن يكونا متطابقَين حرفياً
     لنفس قيمة `--explain` و `--lang`.
6. تشغيل الاختبارات الشاملة (~900) للتأكد من عدم التراجع.

---

## 8. مخطط Gantt للمراحل

```mermaid
gantt
    title خطة تنفيذ نظام الأخطاء الموحَّد (7 مراحل)
    dateFormat HH:mm
    axisFormat %H:%M
    section الإعداد
    تراجع Phase F-1            :a0, 00:00, 15m
    بناء Catalog + EM المحدَّث  :a1, after a0, 90m
    نقل Formatter              :a2, after a1, 30m
    أعلام explain + lang       :a3, after a2, 30m
    section الترحيل
    جرد 152 throw              :a4, after a3, 60m
    ترحيل visitors             :a5, after a4, 180m
    ترحيل المترجم + حذف DE     :a6, after a5, 120m
    runtime hooks              :a7, after a6, 60m
    section الإنهاء
    توثيق + اختبارات تطابق     :a8, after a7, 60m
```

---

## 9. جدول القرارات الجوهرية

| القرار | الإجابة | المبرر |
|---|---|---|
| هل نحتفظ بـ`dispatch(RuntimeErrorKind)`؟ | لا — يُحذف | لا معنى لجعل DivByZero يعتمد على سياسة الذاكرة |
| هل نحتفظ بـ`dispatch(OwnershipErrorKind)`؟ | نعم | الملكية فعلاً تتأثر بالسياسة |
| هل نُبقي `SadException` في المفسر؟ | نعم | يحتاجه `حاول/امسك` + إيقاف التدفق |
| هل نُبقي `DiagnosticEngine` في المترجم؟ | نعم | يحتاجه تجميع batch + تنسيق CLI |
| هل `ErrorCatalog` يحلّ محل `SmartErrorSystem`؟ | لا — يكمّله | Catalog يُولّد النص، Smart يضيف fixes/cascade |
| هل نحذف 4 ملفات smart؟ | لا | كلها حيّة عبر `enrichDiagnostic` |
| هل نحذف `type_explanations`؟ | يؤجَّل | عضو خامل — يُربط فعلاً أو يُحذف بعد المرحلة 4 |

---

## 10. مخاطر وضمانات

| المخاطرة | الاحتمال | التخفيف |
|---|---|---|
| كسر اختبارات قائمة بسبب تغيير نص الخطأ | عالي | اختبارات `expected` تُحدَّث آلياً + توفير أوضاع `--explain=brief` للتطابق القديم |
| نسيان موقع من الـ152 | متوسط | سكريبت يفحص أن لا `throw <Error>("` بنص حرفي بعد الترحيل |
| تعارض في أسماء `ErrorCode` | منخفض | استخدام prefixes واضحة: `RUN_*`, `SEM_*`, `PARSE_*`, `OWN_*` |
| أداء `render()` في hot paths | منخفض | الرسائل تُبنى فقط عند الخطأ (نادر) |

---

## 11. الفوائد المتوقَّعة

1. **توحيد كامل** بين رسائل المفسر والمترجم.
2. **مستوى تفصيل قابل للضبط** بدون تعديل كود.
3. **سهولة الترجمة** لإضافة لغات جديدة (مكان واحد).
4. **اختبارات أبسط** — تختبر `ErrorCode` لا نص حرفي.
5. **فصل اهتمامات صحيح:** سياسة الذاكرة منفصلة عن صيغة الرسالة.
6. **توثيق ذاتي:** Catalog هو مرجع شامل لكل أخطاء اللغة.

---

## 12. خطوة فورية مقترحة

البدء بـ**المرحلة 0 + 1** في جلسة واحدة (≈ 45 دقيقة):
- تراجع Phase F-1 الخاطئ
- بناء `ErrorCatalog` الأساسي
- اختبار سريع لإثبات المفهوم على DivByZero فقط

ثم وقف للمراجعة قبل الانطلاق في الترحيل الكامل (المرحلة 3).
