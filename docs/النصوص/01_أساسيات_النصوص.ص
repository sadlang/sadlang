# أساسيات النصوص - تعريف النصوص والدمج والطول والنصوص الفارغة
# String Basics - declaration, concatenation, length, empty strings

دالة رئيسية()
    # --- مثال 1: تعريف نص بسيط ---
    اطبع_سطر("=== مثال 1: تعريف نص بسيط ===")
    متغير اسم = "لغة ص"
    اطبع_سطر("الاسم: " + اسم)

    # --- مثال 2: نصوص متعددة ---
    اطبع_سطر("=== مثال 2: نصوص متعددة ===")
    متغير تحية = "مرحبا"
    متغير عالم = "يا عالم"
    اطبع_سطر(تحية)
    اطبع_سطر(عالم)

    # --- مثال 3: دمج نصين ---
    اطبع_سطر("=== مثال 3: دمج نصين ===")
    متغير جملة = تحية + " " + عالم
    اطبع_سطر(جملة)

    # --- مثال 4: دمج عدة نصوص ---
    اطبع_سطر("=== مثال 4: دمج عدة نصوص ===")
    متغير سطر = "أنا" + " " + "أتعلم" + " " + "لغة" + " " + "ص"
    اطبع_سطر(سطر)

    # --- مثال 5: النص الفارغ ---
    اطبع_سطر("=== مثال 5: النص الفارغ ===")
    متغير فارغ = ""
    اطبع_سطر("قبل النص الفارغ")
    اطبع_سطر(فارغ)
    اطبع_سطر("بعد النص الفارغ")

    # --- مثال 6: طول النص ---
    اطبع_سطر("=== مثال 6: طول النص ===")
    متغير لفظة = "برمجة"
    اطبع_سطر("الكلمة: " + لفظة)
    اطبع_سطر("طول الكلمة: " + طول(لفظة))

    # --- مثال 7: دمج أرقام مع نصوص ---
    اطبع_سطر("=== مثال 7: دمج أرقام مع نصوص ===")
    متغير عمر = 25
    اطبع_سطر("العمر: " + عمر)
    متغير سنة = 2026
    اطبع_سطر("السنة: " + سنة)

    # --- مثال 8: بناء جملة كاملة ---
    اطبع_سطر("=== مثال 8: بناء جملة كاملة ===")
    متغير مدينة = "الرياض"
    متغير درجة = 35
    متغير تقرير = "الطقس في " + مدينة + " هو " + درجة + " درجة"
    اطبع_سطر(تقرير)
نهاية
