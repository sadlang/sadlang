# دمج النصوص - بناء جمل من أرقام ونصوص
# String Building - combining numbers with text, building sentences

دالة رئيسية()
    # --- مثال 1: دمج اسم مع رسالة ---
    اطبع_سطر("=== مثال 1: دمج اسم مع رسالة ===")
    متغير الاسم = "أحمد"
    متغير رسالة = "مرحبا يا " + الاسم + "!"
    اطبع_سطر(رسالة)

    # --- مثال 2: بناء عنوان ---
    اطبع_سطر("=== مثال 2: بناء عنوان ===")
    متغير شارع = "شارع الملك فهد"
    متغير رقم_بيت = 42
    متغير عنوان = شارع + " رقم " + رقم_بيت
    اطبع_سطر("العنوان: " + عنوان)

    # --- مثال 3: معلومات طالب ---
    اطبع_سطر("=== مثال 3: معلومات طالب ===")
    متغير اسم_طالب = "سارة"
    متغير درجة1 = 90
    متغير درجة2 = 85
    متغير درجة3 = 95
    متغير مجموع = درجة1 + درجة2 + درجة3
    اطبع_سطر("الطالبة: " + اسم_طالب)
    اطبع_سطر("الدرجة 1: " + درجة1)
    اطبع_سطر("الدرجة 2: " + درجة2)
    اطبع_سطر("الدرجة 3: " + درجة3)
    اطبع_سطر("المجموع: " + مجموع)

    # --- مثال 4: حساب بسيط ---
    اطبع_سطر("=== مثال 4: حساب بسيط ===")
    متغير سعر = 150
    متغير كمية = 3
    متغير الاجمالي = سعر * كمية
    اطبع_سطر("السعر: " + سعر + " ريال")
    اطبع_سطر("الكمية: " + كمية)
    اطبع_سطر("الإجمالي: " + الاجمالي + " ريال")

    # --- مثال 5: بناء جملة متعددة الأجزاء ---
    اطبع_سطر("=== مثال 5: بناء جملة متعددة الأجزاء ===")
    متغير نتيجة = ""
    نتيجة = نتيجة + "لغة ص "
    نتيجة = نتيجة + "هي لغة "
    نتيجة = نتيجة + "برمجة "
    نتيجة = نتيجة + "عربية"
    اطبع_سطر(نتيجة)

    # --- مثال 6: تنسيق وقت ---
    اطبع_سطر("=== مثال 6: تنسيق وقت ===")
    متغير ساعة = 10
    متغير دقيقة = 30
    متغير وقت_منسق = ساعة + ":" + دقيقة
    اطبع_سطر("الوقت: " + وقت_منسق)

    # --- مثال 7: بطاقة تعريف ---
    اطبع_سطر("=== مثال 7: بطاقة تعريف ===")
    متغير الاسم_كامل = "محمد العلي"
    متغير العمر = 30
    متغير المهنة = "مبرمج"
    اطبع_سطر("------------------")
    اطبع_سطر("الاسم: " + الاسم_كامل)
    اطبع_سطر("العمر: " + العمر + " سنة")
    اطبع_سطر("المهنة: " + المهنة)
    اطبع_سطر("------------------")

    # --- مثال 8: ملخص رياضي ---
    اطبع_سطر("=== مثال 8: ملخص رياضي ===")
    متغير عدد1 = 100
    متغير عدد2 = 45
    اطبع_سطر(عدد1 + " + " + عدد2 + " = " + (عدد1 + عدد2))
    اطبع_سطر(عدد1 + " - " + عدد2 + " = " + (عدد1 - عدد2))
    اطبع_سطر(عدد1 + " * " + عدد2 + " = " + (عدد1 * عدد2))
    اطبع_سطر(عدد1 + " / " + عدد2 + " = " + (عدد1 / عدد2))
نهاية
