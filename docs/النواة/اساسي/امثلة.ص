# أمثلة أساسية على تعريف المتغيرات في لغة ص

دالة رئيسية()
    # المثال 1: تعريف عدد صحيح وطباعته
    متغير العمر = 25
    اطبع_سطر("العمر هو: " + العمر)

    # المثال 2: تعريف عدد عشري
    متغير النسبة = 0.75
    اطبع_سطر("النسبة: " + النسبة * 100 + "%")

    # المثال 3: متغير منطقي في شرط
    متغير نشط = صحيح
    إذا (نشط)
        اطبع_سطر("الحالة: نشط")
    وإلا
        اطبع_سطر("الحالة: غير نشط")
    نهاية

    # المثال 4: سلسلة نصية
    متغير الاسم = "سارة"
    اطبع_سطر("مرحباً " + الاسم)

    # المثال 5: مصفوفة أعداد
    متغير درجات = [90، 85، 78]
    اطبع_سطر("الدرجات: " + درجات)

    # المثال 6: خريطة بيانات
    متغير طالب = {"اسم": "ليلى"، "معدل": 95}
    اطبع_سطر("اسم الطالبة: " + طالب["اسم"])

    # المثال 7: تغيير قيمة متغير
    متغير عدد = 10
    عدد = عدد + 5
    اطبع_سطر("القيمة الجديدة: " + عدد)

    # المثال 8: متغير من نوع أي
    متغير قيمة_رقمية = 5
    اطبع_سطر("قيمة أولية: " + قيمة_رقمية)
    متغير قيمة_نصية = "نص جديد"
    اطبع_سطر("قيمة بعد التغيير: " + قيمة_نصية)

    # المثال 9: استخدام متغيرات عربية وإنجليزية معاً
    متغير x = 7
    متغير وصف = "عدد صحيح"
    اطبع_سطر("x = " + x + " (" + وصف + ")")
نهاية
