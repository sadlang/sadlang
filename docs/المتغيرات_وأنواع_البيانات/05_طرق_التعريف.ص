# مثال 5: الطرق المختلفة لتعريف المتغيرات
# المستوى: متوسط
# يوضح جميع الطرق المتاحة لتعريف المتغيرات في لغة ص


دالة رئيسية()
    اطبع_سطر("=== الطريقة 1: متغير (استنتاج النوع) ===")
    متغير اسم = "خالد"
    متغير عمر = 30
    متغير معدل = 4.5
    متغير نشط = صحيح
    اطبع_سطر("اسم: " + اسم)
    اطبع_سطر("عمر: " + عمر)
    اطبع_سطر("معدل: " + معدل)
    اطبع_سطر("نشط: " + نشط)

    اطبع_سطر("")
    اطبع_سطر("=== الطريقة 2: التصريح بالنوع ===")
    رقم عدد_الطلاب = 150
    نص المدرسة = "مدرسة النور"
    عشري النسبة = 89.5
    منطقي مفتوح = صحيح
    اطبع_سطر("عدد الطلاب: " + عدد_الطلاب)
    اطبع_سطر("المدرسة: " + المدرسة)
    اطبع_سطر("النسبة: " + النسبة)
    اطبع_سطر("مفتوح: " + مفتوح)

    اطبع_سطر("")
    اطبع_سطر("=== الطريقة 3: ثابت ===")
    ثابت الحد_الأدنى = 0
    ثابت الحد_الأقصى = 100
    ثابت اسم_النظام = "نظام الإدارة"
    اطبع_سطر("الحد الأدنى: " + الحد_الأدنى)
    اطبع_سطر("الحد الأقصى: " + الحد_الأقصى)
    اطبع_سطر("اسم النظام: " + اسم_النظام)

    اطبع_سطر("")
    اطبع_سطر("=== مقارنة بين الطرق ===")
    متغير أ = 10        # استنتاج تلقائي
    رقم ب = 20          # تصريح بالنوع
    ثابت ج = 30         # ثابت
    اطبع_سطر("أ (متغير): " + أ)
    اطبع_سطر("ب (رقم): " + ب)
    اطبع_سطر("ج (ثابت): " + ج)
نهاية
