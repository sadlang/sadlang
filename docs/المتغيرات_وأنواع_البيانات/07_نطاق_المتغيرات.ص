# مثال 7: نطاق المتغيرات
# المستوى: متقدم
# يوضح كيف يعمل نطاق المتغيرات داخل الكتل والدوال

# === متغير عام ===

دالة حساب_المساحة(طول، عرض)
    متغير المساحة = طول * عرض
    متغير الوحدة = "متر مربع"
    ارجع المساحة
نهاية

دالة اختبار_نطاق()
    متغير ص = 200
    اطبع_سطر("داخل الدالة - س العام: " + س)
    اطبع_سطر("داخل الدالة - ص المحلي: " + ص)
    
    إذا (ص > 100)
        متغير ع = 300
        اطبع_سطر("داخل الشرط - ع: " + ع)
    نهاية


دالة رئيسية()
    متغير مستوى_عام = "عام"
    اطبع_سطر("=== نطاق المتغيرات ===")
    اطبع_سطر("المستوى العام: " + مستوى_عام)

    # === المتغيرات داخل الشروط ===
    متغير درجة = 85

    إذا (درجة >= 90)
        متغير التقدير = "ممتاز"
        اطبع_سطر("التقدير: " + التقدير)
    وإلا_إذا (درجة >= 80)
        متغير التقدير = "جيد جداً"
        اطبع_سطر("التقدير: " + التقدير)
    وإلا
        متغير التقدير = "مقبول"
        اطبع_سطر("التقدير: " + التقدير)
    نهاية

    # === المتغيرات داخل الحلقات ===
    اطبع_سطر("")
    اطبع_سطر("=== المتغيرات في الحلقات ===")
    متغير المجموع = 0

    لكل رقم في [10، 20، 30، 40، 50]
        المجموع += رقم
        متغير المجموع_الحالي = المجموع
        اطبع_سطر("بعد إضافة " + رقم + ": " + المجموع_الحالي)
    نهاية

    اطبع_سطر("المجموع النهائي: " + المجموع)

    # === المتغيرات في الدوال ===
    اطبع_سطر("")
    اطبع_سطر("=== المتغيرات في الدوال ===")


    متغير مساحة1 = حساب_المساحة(10، 5)
    متغير مساحة2 = حساب_المساحة(7، 3)
    اطبع_سطر("المساحة 1: " + مساحة1)
    اطبع_سطر("المساحة 2: " + مساحة2)

    # === تداخل النطاقات ===
    اطبع_سطر("")
    اطبع_سطر("=== تداخل النطاقات ===")
    متغير س = 100

    نهاية

    اختبار_نطاق()
    اطبع_سطر("خارج الدالة - س: " + س)
نهاية
