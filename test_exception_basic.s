# Test Exception Handling - Basic Try-Catch
# اختبار معالجة الاستثناءات - المحاولة والالتقاط الأساسي

اطبع("=== اختبار معالجة الاستثناءات ===");

# Test 1: Basic throw and catch
اطبع("\n--- الاختبار 1: رمي والتقاط بسيط ---");

حاول {
    اطبع("داخل كتلة المحاولة");
    ارمِ "خطأ اختباري" ;
    اطبع("هذا لن يُطبع");
}
امسك (خطأ) {
    اطبع("تم اصطياد الخطأ: ", خطأ);
}

اطبع("البرنامج استمر بعد معالجة الخطأ");

اطبع("\n✅ اكتمل الاختبار بنجاح");
