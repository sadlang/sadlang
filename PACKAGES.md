# 📦 دليل حزم لغة ص — PACKAGES.md

> مرجع شامل لجميع المكتبات المتوفرة في نظام حزم لغة ص (30 مكتبة)

## طريقة الاستيراد

```sad
من مكتبات_خارجية.اسم_المكتبة.src.مكتبة استورد *
```

## فهرس المكتبات

| # | المكتبة | الاسم اللاتيني | التصنيف | الوصف |
|---|---------|---------------|---------|-------|
| 1 | [ألوان](#ألوان) | `alwan` | رسوميات | التعامل مع الألوان — RGB و HSL والمزج واللوحات |
| 2 | [إحصائيات](#إحصائيات) | `ihsaiyat` | رياضيات | إحصائية شاملة — المتوسط والوسيط والانحدار والارتباط |
| 3 | [اختبار](#اختبار) | `ikhtibar` | أدوات تطوير | إطار اختبار وحدات — تأكيدات ومجموعات وطاقم اختبار |
| 4 | [إدخال_إخراج](#إدخال_إخراج) | `idkhal_ikhraj` | إدخال/إخراج | مجاري نصية، ثنائية، مخازن نصية، قارئات الأسطر |
| 5 | [التحقق](#التحقق) | `tahaqquq` | تحقق | التحقق من صحة البيانات — البريد والروابط وكلمات المرور |
| 6 | [بحث_ثنائي](#بحث_ثنائي) | `bahth_thunai` | خوارزميات | البحث الثنائي والإدراج المرتب |
| 7 | [برمجة_وظيفية](#برمجة_وظيفية) | `barmaja_wazifiya` | وظيفية | خريطة، مرشح، اختصار، تأليف، تخزين مؤقت، جزئي |
| 8 | [تاريخ_ووقت](#تاريخ_ووقت) | `tarikh-wa-waqt` | تاريخ/وقت | التقويم الميلادي والهجري والتنسيق والحسابات |
| 9 | [ترميز](#ترميز) | `tarmiz` | ترميز | ست عشري وثنائي وثماني و URL وشفرة قيصر ومورس |
| 10 | [تشفير](#تشفير) | `tashfir` | أمان | التشفير والتجزئة — SHA256، MD5، CRC32، Adler32، HMAC |
| 11 | [تعبيرات_منتظمة](#تعبيرات_منتظمة) | `tabirat-muntazama` | معالجة نصوص | بحث ومطابقة واستبدال وتقسيم |
| 12 | [تعداد_قيم](#تعداد_قيم) | `tadad_qiyam` | أنواع | أنواع تعداد بأسماء وقيم وتكرار |
| 13 | [تكرارات](#تكرارات) | `takararat` | أدوات | أدوات التكرار — سلسلة، ضغط، ضرب، تبديلات، توافيق |
| 14 | [جيسون](#جيسون) | `json` | بيانات | JSON — التحليل، التسلسل، التحقق |
| 15 | [خيوط](#خيوط) | `khuyut` | تزامن | الخيوط — إنشاء، انتظار، تزامن، أقفال، أحداث |
| 16 | [رياضيات](#رياضيات) | `ryadiyat` | رياضيات | مثلثات، لوغاريتمات، أعداد أولية، توافيق |
| 17 | [سجلات](#سجلات) | `sijillat` | أدوات تطوير | نظام تسجيل — مستويات وتصفية ومسجل كائني |
| 18 | [طلبات_ويب](#طلبات_ويب) | `talabat_web` | شبكة | طلبات HTTP — GET، POST، PUT، DELETE |
| 19 | [عشوائي](#عشوائي) | `ashwai` | رياضيات | توليد أرقام عشوائية — موحد، طبيعي، خلط، عينات |
| 20 | [قوالب_نصية](#قوالب_نصية) | `qawalib-nassiya` | نصوص | قوالب نصية — HTML و Markdown و JSON و XML |
| 21 | [كومة_أولويات](#كومة_أولويات) | `kawmat_awlawiyat` | هياكل بيانات | كومة أولويات — إدراج، استخراج، ترتيب |
| 22 | [مجموعات](#مجموعات) | `majmuat` | هياكل بيانات | مكدس وطابور ومجموعة فريدة وقائمة مرتبطة |
| 23 | [محولات](#محولات) | `muhawwilat` | رياضيات | محولات وحدات — حرارة ومسافات وأوزان |
| 24 | [معالجة_متوازية](#معالجة_متوازية) | `mualja_mutawaziya` | تزامن | تجمع العمليات، توزيع المهام، الذاكرة المشتركة |
| 25 | [مقابس](#مقابس) | `maqabis` | شبكة | المقابس — TCP وUDP للخوادم والعملاء |
| 26 | [ملفات_csv](#ملفات_csv) | `malafat_csv` | بيانات | ملفات CSV — قراءة، كتابة، ترويسات |
| 27 | [نسخ](#نسخ) | `nuskh` | أدوات | النسخ السطحي والعميق لجميع أنواع البيانات |
| 28 | [نظام](#نظام) | `nizam` | نظام | معلومات النظام — المنصة، متغيرات البيئة |
| 29 | [نظام_ملفات](#نظام_ملفات) | `nizam_malafat` | نظام | نظام الملفات — المسارات، المجلدات، القراءة، الكتابة |
| 30 | [نماذج_بيانات](#نماذج_بيانات) | `namazij_bayanat` | بيانات | نماذج بيانات — بناء تلقائي، مساواة، تجزئة |

---

## التصنيفات

### 🔢 رياضيات وعلوم
- **إحصائيات** — تحليل إحصائي متقدم
- **رياضيات** — دوال رياضية شاملة
- **عشوائي** — توليد أرقام عشوائية
- **محولات** — تحويل الوحدات

### 📊 هياكل بيانات وخوارزميات
- **بحث_ثنائي** — بحث وإدراج مرتب
- **كومة_أولويات** — كومة صغرى وترتيب
- **مجموعات** — مكدس، طابور، مجموعة، قائمة مرتبطة
- **تكرارات** — أدوات تكرار متقدمة

### 🌐 شبكة وويب
- **طلبات_ويب** — طلبات HTTP/HTTPS
- **مقابس** — برمجة مقابس TCP/UDP
- **جيسون** — تحليل وتسلسل JSON

### 🔒 أمان وتشفير
- **تشفير** — SHA256، MD5، CRC32، HMAC
- **ترميز** — hex، binary، URL، قيصر، مورس
- **التحقق** — التحقق من البريد والروابط وكلمات المرور

### ⚙️ نظام وإدخال/إخراج
- **نظام** — معلومات المنصة والبيئة
- **نظام_ملفات** — عمليات الملفات والمجلدات
- **إدخال_إخراج** — مجاري نصية وثنائية
- **ملفات_csv** — قراءة وكتابة CSV

### 🧵 تزامن ومعالجة متوازية
- **خيوط** — إنشاء وتزامن الخيوط
- **معالجة_متوازية** — تجمع عمليات وذاكرة مشتركة

### 🛠️ أدوات تطوير
- **اختبار** — إطار اختبار وحدات
- **سجلات** — نظام تسجيل
- **ألوان** — التعامل مع الألوان

### 📝 نصوص وبيانات
- **قوالب_نصية** — قوالب HTML/Markdown/JSON
- **تعبيرات_منتظمة** — بحث واستبدال بأنماط
- **نماذج_بيانات** — أصناف بيانات تلقائية
- **تعداد_قيم** — أنواع تعداد
- **نسخ** — نسخ سطحي وعميق

### λ برمجة وظيفية
- **برمجة_وظيفية** — map، filter، reduce، compose

---

## تفاصيل المكتبات

---

### ألوان
> `alwan` — مكتبة للتعامل مع الألوان — RGB و HSL والمزج واللوحات

```sad
من مكتبات_خارجية.ألوان.src.مكتبة استورد *
```

**الدوال الرئيسية:**
| الدالة | الوصف |
|--------|-------|
| `لون_جديد(أحمر، أخضر، أزرق)` | إنشاء لون RGB |
| `مزج(لون1، لون2، نسبة)` | مزج لونين بنسبة |
| `إلى_سداسي(لون)` | تحويل لون إلى HEX |
| `من_سداسي(نص)` | إنشاء لون من HEX |
| `تفتيح(لون، نسبة)` | تفتيح لون |
| `تغميق(لون، نسبة)` | تغميق لون |

**مثال:**
```sad
متغير أحمر = لون_جديد(255، 0، 0)
متغير أزرق = لون_جديد(0، 0، 255)
متغير مزيج = مزج(أحمر، أزرق، 0.5)
اطبع_سطر(إلى_سداسي(مزيج))
```

---

### إحصائيات
> `ihsaiyat` — مكتبة إحصائية شاملة — المتوسط والوسيط والانحدار والارتباط

```sad
من مكتبات_خارجية.إحصائيات.src.مكتبة استورد *
```

**الدوال الرئيسية:**
| الدالة | الوصف |
|--------|-------|
| `المتوسط(قائمة)` | الوسط الحسابي |
| `الوسيط(قائمة)` | القيمة الوسطى |
| `الانحراف_المعياري(قائمة)` | الانحراف المعياري |
| `الارتباط(قائمة1، قائمة2)` | معامل الارتباط |
| `الانحدار_الخطي(س، ص)` | الانحدار الخطي |
| `النسبة_المئوية(قائمة، ن)` | النسبة المئوية الرتبية |

**مثال:**
```sad
متغير بيانات = [10، 20، 30، 40، 50]
اطبع_سطر("المتوسط: " + المتوسط(بيانات))
اطبع_سطر("الوسيط: " + الوسيط(بيانات))
```

---

### اختبار
> `ikhtibar` — إطار اختبار وحدات — تأكيدات ومجموعات وطاقم اختبار

```sad
من مكتبات_خارجية.اختبار.src.مكتبة استورد *
```

**الدوال الرئيسية:**
| الدالة | الوصف |
|--------|-------|
| `تأكد(شرط، رسالة)` | تأكيد أن الشرط صحيح |
| `تأكد_يساوي(فعلي، متوقع، رسالة)` | تأكيد المساواة |
| `تأكد_لا_يساوي(فعلي، متوقع، رسالة)` | تأكيد عدم المساواة |
| `مجموعة_اختبارات(اسم)` | إنشاء مجموعة اختبارات |
| `شغّل_الاختبارات()` | تشغيل جميع الاختبارات |

**مثال:**
```sad
تأكد_يساوي(2 + 2، 4، "الجمع البسيط")
تأكد(صحيح، "القيمة صحيحة")
```

---

### إدخال_إخراج
> `idkhal_ikhraj` — مجاري نصية، ثنائية، مخازن نصية

```sad
من مكتبات_خارجية.إدخال_إخراج.src.مكتبة استورد *
```

**الأصناف الرئيسية:**
| الصنف | الوصف |
|-------|-------|
| `مجرى_نصي()` | مجرى لتجميع النصوص |
| `مجرى_ثنائي()` | مجرى للبيانات الثنائية |

**مثال:**
```sad
متغير م = جديد مجرى_نصي()
م.اكتب("مرحباً ")
م.اكتب("بالعالم")
اطبع_سطر(م.اقرأ_الكل())
```

---

### التحقق
> `tahaqquq` — التحقق من صحة البيانات — البريد والروابط وكلمات المرور

```sad
من مكتبات_خارجية.التحقق.src.مكتبة استورد *
```

**الدوال الرئيسية:**
| الدالة | الوصف |
|--------|-------|
| `هل_بريد_صحيح(نص)` | التحقق من البريد الإلكتروني |
| `هل_رابط_صحيح(نص)` | التحقق من الرابط |
| `قوة_كلمة_المرور(نص)` | تقييم قوة كلمة المرور |
| `هل_في_نطاق(رقم، حد_أدنى، حد_أقصى)` | التحقق من النطاق |
| `لوهن(نص)` | خوارزمية Luhn |

**مثال:**
```sad
اطبع_سطر(هل_بريد_صحيح("test@example.com"))
اطبع_سطر(قوة_كلمة_المرور("P@ssw0rd!"))
```

---

### بحث_ثنائي
> `bahth_thunai` — البحث الثنائي والإدراج المرتب

```sad
من مكتبات_خارجية.بحث_ثنائي.src.مكتبة استورد *
```

**الدوال الرئيسية:**
| الدالة | الوصف |
|--------|-------|
| `بحث_ثنائي(قائمة، قيمة)` | بحث ثنائي عن قيمة |
| `بحث_يسار(قائمة، قيمة)` | نقطة الإدراج اليسرى |
| `بحث_يمين(قائمة، قيمة)` | نقطة الإدراج اليمنى |
| `إدراج_مرتب(قائمة، قيمة)` | إدراج مع الحفاظ على الترتيب |
| `نطاق_بحث(قائمة، من، إلى)` | البحث في نطاق |

**مثال:**
```sad
متغير قائمة = [1، 3، 5، 7، 9]
متغير موقع = بحث_ثنائي(قائمة، 5)
اطبع_سطر("الموقع: " + موقع)
```

---

### برمجة_وظيفية
> `barmaja_wazifiya` — خريطة، مرشح، اختصار، تأليف، تخزين مؤقت

```sad
من مكتبات_خارجية.برمجة_وظيفية.src.مكتبة استورد *
```

**الدوال الرئيسية:**
| الدالة | الوصف |
|--------|-------|
| `خريطة(دالة، قائمة)` | تطبيق دالة على كل عنصر |
| `مرشح(دالة، قائمة)` | تصفية العناصر |
| `اختصار(دالة، قائمة، بداية)` | اختزال القائمة لقيمة واحدة |
| `تأليف(دالة1، دالة2)` | تأليف دالتين |
| `تخزين_مؤقت(دالة)` | حفظ نتائج الاستدعاءات |
| `جزئي(دالة، وسائط)` | تطبيق جزئي |

**مثال:**
```sad
متغير أرقام = [1، 2، 3، 4، 5]
متغير مضاعفة = خريطة((س) => س * 2، أرقام)
متغير زوجية = مرشح((س) => س % 2 == 0، أرقام)
```

---

### تاريخ_ووقت
> `tarikh-wa-waqt` — التقويم الميلادي والهجري والتنسيق والحسابات

```sad
من مكتبات_خارجية.تاريخ_ووقت.src.مكتبة استورد *
```

**الدوال الرئيسية:**
| الدالة | الوصف |
|--------|-------|
| `تاريخ_جديد(سنة، شهر، يوم)` | إنشاء تاريخ ميلادي |
| `الآن()` | التاريخ والوقت الحالي |
| `إلى_هجري(تاريخ)` | تحويل لهجري |
| `فرق_أيام(تاريخ1، تاريخ2)` | الفرق بالأيام |
| `تنسيق(تاريخ، نمط)` | تنسيق التاريخ |

**مثال:**
```sad
متغير اليوم = الآن()
اطبع_سطر(تنسيق(اليوم، "يوم/شهر/سنة"))
```

---

### ترميز
> `tarmiz` — ست عشري وثنائي وثماني و URL وشفرة قيصر ومورس

```sad
من مكتبات_خارجية.ترميز.src.مكتبة استورد *
```

**الدوال الرئيسية:**
| الدالة | الوصف |
|--------|-------|
| `إلى_ست_عشري(رقم)` | تحويل لنظام 16 |
| `من_ست_عشري(نص)` | تحويل من نظام 16 |
| `إلى_ثنائي(رقم)` | تحويل لنظام 2 |
| `ترميز_url(نص)` | ترميز URL |
| `شفرة_قيصر(نص، إزاحة)` | تشفير قيصر |
| `إلى_مورس(نص)` | تحويل لمورس |

**مثال:**
```sad
اطبع_سطر(إلى_ست_عشري(255))     # "ff"
اطبع_سطر(إلى_ثنائي(42))         # "101010"
اطبع_سطر(شفرة_قيصر("ABC"، 3))  # "DEF"
```

---

### تشفير
> `tashfir` — التشفير والتجزئة — SHA256، MD5، CRC32، Adler32، HMAC

```sad
من مكتبات_خارجية.تشفير.src.مكتبة استورد *
```

**الدوال الرئيسية:**
| الدالة | الوصف |
|--------|-------|
| `sha256(نص)` | تجزئة SHA-256 |
| `md5(نص)` | تجزئة MD5 |
| `crc32(نص)` | فحص CRC32 |
| `adler32(نص)` | فحص Adler32 |
| `hmac_sha256(رسالة، مفتاح)` | HMAC-SHA256 |
| `ترميز_سداسي(نص)` | ترميز سداسي عشري |

**مثال:**
```sad
اطبع_سطر(sha256("مرحباً"))
اطبع_سطر(md5("test"))
اطبع_سطر(ترميز_سداسي("A"))
```

---

### تعبيرات_منتظمة
> `tabirat-muntazama` — بحث ومطابقة واستبدال وتقسيم بأنماط

```sad
من مكتبات_خارجية.تعبيرات_منتظمة.src.مكتبة استورد *
```

**الدوال الرئيسية:**
| الدالة | الوصف |
|--------|-------|
| `بحث(نمط، نص)` | البحث عن نمط |
| `طابق(نمط، نص)` | مطابقة كاملة |
| `استبدل(نمط، نص، بديل)` | استبدال النمط |
| `قسّم(نمط، نص)` | تقسيم بالنمط |
| `جميع_المطابقات(نمط، نص)` | كل المطابقات |

**مثال:**
```sad
متغير نتيجة = بحث("[0-9]+", "عمري 25 سنة")
اطبع_سطر(نتيجة)
```

---

### تعداد_قيم
> `tadad_qiyam` — أنواع تعداد بأسماء وقيم وتكرار

```sad
من مكتبات_خارجية.تعداد_قيم.src.مكتبة استورد *
```

**الأصناف الرئيسية:**
| الصنف/الدالة | الوصف |
|-------------|-------|
| `عنصر_تعداد(اسم، قيمة)` | عنصر تعداد واحد |
| `إنشاء_تعداد(اسم، عناصر)` | إنشاء تعداد كامل |

**مثال:**
```sad
متغير الألوان = إنشاء_تعداد("ألوان"، [
    جديد عنصر_تعداد("أحمر"، 1)،
    جديد عنصر_تعداد("أخضر"، 2)،
    جديد عنصر_تعداد("أزرق"، 3)
])
```

---

### تكرارات
> `takararat` — أدوات التكرار — سلسلة، ضغط، ضرب، تبديلات، توافيق

```sad
من مكتبات_خارجية.تكرارات.src.مكتبة استورد *
```

**الدوال الرئيسية:**
| الدالة | الوصف |
|--------|-------|
| `سلسل(قوائم)` | ربط قوائم متعددة |
| `اضغط(قائمة1، قائمة2)` | ضغط قائمتين |
| `ضرب(قائمة1، قائمة2)` | الجداء الديكارتي |
| `تبديلات(قائمة)` | كل التبديلات |
| `توافيق(قائمة، ن)` | التوافيق بحجم N |
| `عدّد(قائمة، بداية)` | ترقيم العناصر |

**مثال:**
```sad
متغير مسلسلة = سلسل([[1، 2]، [3، 4]])  # [1، 2، 3، 4]
متغير مضغوط = اضغط(["أ"، "ب"]، [1، 2])  # [["أ"، 1]، ["ب"، 2]]
```

---

### جيسون
> `json` — JSON — التحليل، التسلسل، التحقق

```sad
من مكتبات_خارجية.جيسون.src.مكتبة استورد *
```

**الدوال الرئيسية:**
| الدالة | الوصف |
|--------|-------|
| `حلل_json(نص)` | تحليل نص JSON |
| `إلى_json(كائن)` | تحويل لنص JSON |
| `هل_json_صحيح(نص)` | التحقق من صحة JSON |

**مثال:**
```sad
متغير بيانات = حلل_json("{\"اسم\": \"أحمد\"}")
اطبع_سطر(بيانات["اسم"])
اطبع_سطر(إلى_json({"عمر": 25}))
```

---

### خيوط
> `khuyut` — إنشاء، انتظار، تزامن، أقفال، أحداث، حواجز

```sad
من مكتبات_خارجية.خيوط.src.مكتبة استورد *
```

**الأصناف الرئيسية:**
| الصنف | الوصف |
|-------|-------|
| `قفل()` | قفل تزامن (mutex) |
| `حدث()` | حدث تنبيه بين الخيوط |
| `حاجز(عدد)` | حاجز لتزامن مجموعة خيوط |
| `مرجع_ذري(قيمة)` | مرجع ذري آمن للخيوط |
| `صف_آمن(حجم)` | صف آمن للخيوط |

**مثال:**
```sad
متغير ق = جديد قفل()
ق.احجز()
# ... عمليات آمنة ...
ق.أطلق()
```

---

### رياضيات
> `ryadiyat` — مثلثات، لوغاريتمات، أعداد أولية، توافيق

```sad
من مكتبات_خارجية.رياضيات.src.مكتبة استورد *
```

**الدوال الرئيسية:**
| الدالة | الوصف |
|--------|-------|
| `جيب(زاوية)` | sin |
| `جيب_تمام(زاوية)` | cos |
| `ظل(زاوية)` | tan |
| `لوغاريتم(رقم)` | ln |
| `هل_أولي(رقم)` | اختبار أولية |
| `عاملي(رقم)` | المضروب (factorial) |
| `قوة(أساس، أس)` | الرفع للقوة |

**مثال:**
```sad
اطبع_سطر(جيب(3.14159 / 2))   # ≈ 1
اطبع_سطر(هل_أولي(17))         # صحيح
اطبع_سطر(عاملي(5))            # 120
```

---

### سجلات
> `sijillat` — نظام تسجيل — مستويات وتصفية ومسجل كائني مع تاريخ وحدود

```sad
من مكتبات_خارجية.سجلات.src.مكتبة استورد *
```

**الدوال الرئيسية:**
| الدالة | الوصف |
|--------|-------|
| `معلومة(رسالة)` | تسجيل معلومة |
| `تحذير(رسالة)` | تسجيل تحذير |
| `خطأ(رسالة)` | تسجيل خطأ |
| `تصحيح(رسالة)` | تسجيل تصحيح |

**مثال:**
```sad
معلومة("بدء التطبيق")
تحذير("ذاكرة منخفضة")
خطأ("فشل الاتصال")
```

---

### طلبات_ويب
> `talabat_web` — طلبات HTTP — GET، POST، PUT، DELETE

```sad
من مكتبات_خارجية.طلبات_ويب.src.مكتبة استورد *
```

**الأصناف الرئيسية:**
| الصنف/الدالة | الوصف |
|-------------|-------|
| `طلب(رابط)` | إنشاء كائن طلب HTTP |
| `تحليل_رابط(نص)` | تحليل عنوان URL |
| `ترميز_رابط(نص)` | ترميز URL |

**مثال:**
```sad
متغير ط = جديد طلب("https://api.example.com")
متغير رابط = تحليل_رابط("https://example.com:443/path")
اطبع_سطر(رابط.البروتوكول)  # "https"
```

---

### عشوائي
> `ashwai` — توليد أرقام عشوائية — موحد، طبيعي، خلط، عينات

```sad
من مكتبات_خارجية.عشوائي.src.مكتبة استورد *
```

**الدوال الرئيسية:**
| الدالة | الوصف |
|--------|-------|
| `عشوائي()` | رقم عشوائي [0، 1) |
| `عشوائي_نطاق(حد_أدنى، حد_أقصى)` | رقم في نطاق |
| `اختر(قائمة)` | عنصر عشوائي |
| `خلط(قائمة)` | خلط القائمة |
| `عينة(قائمة، عدد)` | أخذ عينة |
| `بذرة(رقم)` | تعيين البذرة |

**مثال:**
```sad
بذرة(42)
اطبع_سطر(عشوائي())
اطبع_سطر(عشوائي_نطاق(1، 100))
اطبع_سطر(اختر(["أحمد"، "محمد"، "علي"]))
```

---

### قوالب_نصية
> `qawalib-nassiya` — قوالب نصية — HTML و Markdown و JSON و XML

```sad
من مكتبات_خارجية.قوالب_نصية.src.مكتبة استورد *
```

**الدوال الرئيسية:**
| الدالة | الوصف |
|--------|-------|
| `قالب(نص)` | إنشاء قالب من نص |
| `html(وسم، محتوى)` | إنشاء HTML |
| `markdown_عنوان(نص، مستوى)` | عنوان Markdown |

**مثال:**
```sad
متغير ق = قالب("مرحباً {{اسم}}")
اطبع_سطر(ق.نفّذ({"اسم": "أحمد"}))
```

---

### كومة_أولويات
> `kawmat_awlawiyat` — كومة أولويات — إدراج، استخراج، ترتيب

```sad
من مكتبات_خارجية.كومة_أولويات.src.مكتبة استورد *
```

**الأصناف الرئيسية:**
| الصنف/الدالة | الوصف |
|-------------|-------|
| `قائمة_أولويات()` | صنف كومة صغرى |
| `دمج_مرتب(قائمة1، قائمة2)` | دمج قائمتين مرتبتين |
| `ترتيب_تنازلي(قائمة)` | ترتيب تنازلي |

**مثال:**
```sad
متغير ك = جديد قائمة_أولويات()
ك.أدخل(5)
ك.أدخل(2)
ك.أدخل(8)
اطبع_سطر(ك.استخرج_الأصغر())  # 2
```

---

### مجموعات
> `majmuat` — مكدس وطابور ومجموعة فريدة وقائمة مرتبطة وخريطة مرتبة

```sad
من مكتبات_خارجية.مجموعات.src.مكتبة استورد *
```

**الأصناف الرئيسية:**
| الصنف | الوصف |
|-------|-------|
| `مكدس()` | مكدس (LIFO) |
| `طابور()` | طابور (FIFO) |
| `مجموعة_فريدة()` | مجموعة بدون تكرار |
| `قائمة_مرتبطة()` | قائمة مرتبطة أحادية |
| `خريطة_مرتبة()` | خريطة مرتبة بالمفاتيح |

**مثال:**
```sad
متغير م = جديد مكدس()
م.ادفع(1)
م.ادفع(2)
اطبع_سطر(م.انزع())  # 2
```

---

### محولات
> `muhawwilat` — محولات وحدات — حرارة ومسافات وأوزان ومساحات

```sad
من مكتبات_خارجية.محولات.src.مكتبة استورد *
```

**الدوال الرئيسية:**
| الدالة | الوصف |
|--------|-------|
| `سيلسيوس_إلى_فهرنهايت(درجة)` | °C → °F |
| `كيلو_إلى_ميل(مسافة)` | km → mi |
| `كيلوغرام_إلى_باوند(وزن)` | kg → lb |
| `متر_إلى_قدم(مسافة)` | m → ft |

**مثال:**
```sad
اطبع_سطر(سيلسيوس_إلى_فهرنهايت(100))  # 212
اطبع_سطر(كيلو_إلى_ميل(10))              # ≈ 6.21
```

---

### معالجة_متوازية
> `mualja_mutawaziya` — تجمع العمليات، توزيع المهام، الذاكرة المشتركة

```sad
من مكتبات_خارجية.معالجة_متوازية.src.مكتبة استورد *
```

**الأصناف الرئيسية:**
| الصنف | الوصف |
|-------|-------|
| `تجمع_عمليات(حجم)` | تجمع عمليات متوازية |
| `ذاكرة_مشتركة()` | ذاكرة مشتركة بين العمليات |
| `عملية(دالة)` | عملية واحدة |

**مثال:**
```sad
متغير ت = جديد تجمع_عمليات(4)
متغير نتائج = ت.خريطة((س) => س * 2، [1، 2، 3، 4])
اطبع_سطر(نتائج)
```

---

### مقابس
> `maqabis` — المقابس — TCP وUDP للخوادم والعملاء

```sad
من مكتبات_خارجية.مقابس.src.مكتبة استورد *
```

**الأصناف والدوال الرئيسية:**
| الدالة/الصنف | الوصف |
|-------------|-------|
| `مقبس_tcp(منفذ)` | إنشاء مقبس TCP |
| `مقبس_udp(منفذ)` | إنشاء مقبس UDP |
| `هل_منفذ_صحيح(منفذ)` | التحقق من صحة المنفذ |
| `هل_ip_صحيح(عنوان)` | التحقق من صحة IP |

**مثال:**
```sad
اطبع_سطر(هل_منفذ_صحيح(8080))    # صحيح
اطبع_سطر(هل_ip_صحيح("192.168.1.1"))  # صحيح
```

---

### ملفات_csv
> `malafat_csv` — ملفات CSV — قراءة، كتابة، ترويسات

```sad
من مكتبات_خارجية.ملفات_csv.src.مكتبة استورد *
```

**الدوال الرئيسية:**
| الدالة | الوصف |
|--------|-------|
| `تحليل_csv(نص)` | تحليل نص CSV |
| `تحليل_csv_بترويسة(نص)` | تحليل مع أسماء الأعمدة |
| `إلى_csv(بيانات)` | تحويل لنص CSV |

**مثال:**
```sad
متغير بيانات = تحليل_csv_بترويسة("اسم,عمر\nأحمد,25\nمريم,30")
اطبع_سطر(بيانات)
```

---

### نسخ
> `nuskh` — النسخ السطحي والعميق لجميع أنواع البيانات

```sad
من مكتبات_خارجية.نسخ.src.مكتبة استورد *
```

**الدوال الرئيسية:**
| الدالة | الوصف |
|--------|-------|
| `نسخة(كائن)` | نسخ سطحي |
| `نسخة_عميقة(كائن)` | نسخ عميق |
| `هل_متساويان(أ، ب)` | مقارنة عميقة |

**مثال:**
```sad
متغير أصل = [1، [2، 3]]
متغير نسخ = نسخة_عميقة(أصل)
```

---

### نظام
> `nizam` — معلومات النظام — المنصة، متغيرات البيئة

```sad
من مكتبات_خارجية.نظام.src.مكتبة استورد *
```

**الدوال الرئيسية:**
| الدالة | الوصف |
|--------|-------|
| `اسم_المنصة()` | اسم نظام التشغيل |
| `نوع_المعالج()` | معمارية المعالج |
| `متغير_بيئة(اسم)` | قراءة متغير بيئة |
| `المسار_الحالي()` | مسار العمل الحالي |

**مثال:**
```sad
اطبع_سطر(اسم_المنصة())       # "Windows"
اطبع_سطر(نوع_المعالج())      # "x86_64"
```

---

### نظام_ملفات
> `nizam_malafat` — المسارات، المجلدات، القراءة، الكتابة، التصفح

```sad
من مكتبات_خارجية.نظام_ملفات.src.مكتبة استورد *
```

**الدوال الرئيسية:**
| الدالة | الوصف |
|--------|-------|
| `اقرأ_ملف(مسار)` | قراءة محتوى ملف |
| `اكتب_ملف(مسار، محتوى)` | كتابة ملف |
| `هل_موجود(مسار)` | التحقق من وجود ملف |
| `امتداد(مسار)` | الحصول على الامتداد |
| `اسم_ملف(مسار)` | اسم الملف |
| `مجلد_أب(مسار)` | المجلد الأب |

**مثال:**
```sad
متغير محتوى = اقرأ_ملف("بيانات.txt")
اكتب_ملف("نتائج.txt"، "مرحباً")
```

---

### نماذج_بيانات
> `namazij_bayanat` — أصناف بيانات تلقائية — بناء، مساواة، تجزئة، تسلسل

```sad
من مكتبات_خارجية.نماذج_بيانات.src.مكتبة استورد *
```

**الأصناف الرئيسية:**
| الصنف | الوصف |
|-------|-------|
| `نموذج_بيانات(حقول)` | إنشاء نموذج بيانات |

**مثال:**
```sad
متغير شخص = جديد نموذج_بيانات({"اسم": "أحمد"، "عمر": 25})
اطبع_سطر(شخص.احصل("اسم"))
اطبع_سطر(شخص.إلى_خريطة())
```

---

## ملاحظات مهمة

### صيغة الاستيراد
```sad
# استيراد كل شيء من مكتبة
من مكتبات_خارجية.اسم_المكتبة.src.مكتبة استورد *

# استيراد دالة محددة
من مكتبات_خارجية.اسم_المكتبة.src.مكتبة استورد اسم_الدالة
```

### قيود معروفة
1. **المصفوفات تُمرر بالقيمة** — الدوال الحرة التي تعدّل المصفوفات لا تؤثر على الأصل. استخدم أصنافاً بدلاً منها.
2. **عامل المودولو** — لا تستخدم قيماً أكبر من `2147483647` في عامل `%`.
3. **تقطيع النصوص** — `نص[من..إلى]` غير مدعوم. استخدم حلقة `لكل` بدلاً منها.
4. **الكلمات المحجوزة** — `صحيح`، `خطأ`، `لاشيء`، `تعداد` لا يمكن استخدامها كأسماء متغيرات أو أصناف.

---

## الرخصة

جميع المكتبات مرخصة بموجب **MIT** ومطوّرة بواسطة **Sad Language Team**.
