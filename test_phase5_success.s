# اختبار شامل للبنية الجديدة

# 1. دالة بسيطة
دالة مرحبا()
    اطبع("مرحباً!")
نهاية

# 2. if-else
دالة اختبار_الشرط()
    إذا (12 > 10)
        اطبع("كبير")
   
    نهاية
نهاية

# 3. while
دالة عد_لثلاثة()
    رقم ع = 0
    بينما (ع < 3)
        اطبع(ع)
        ع = ع + 1
    نهاية
نهاية

# التنفيذ
مرحبا()
اختبار_الشرط()
اختبار_الشرط()
عد_لثلاثة()
اطبع("✓ نجح!")
