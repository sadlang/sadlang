# مراجعة وتصويب: إعادة تنظيم بنية مجلّدات خادم LSP (PR #121)

> **المراجِعة:** Amelia (CR صارم) — **التاريخ:** 2026-06-29 — **الفرع:** `chore/lsp-restructure`
> **النطاق:** نقلٌ صرف لبنية `tools/lsp/src/` (نواة/مميّزات) بلا تغيير سلوكيّ.
> **المرجع:** `docs/ENGINEERING_RULES.md` (CW-/BF-).

---

## ملخّص اكتمال النقل (تحقّق أوّليّ)

| البند | المتوقَّع | الفعليّ | الحالة |
|------|---------|--------|-------|
| ملفّات `core/` | 7 (5 engine + 2 utils) | 7 | ✅ |
| ملفّات `features/` | 21 مزوّدًا | 21 (editing 4، comprehension 4، navigation 5، structure 4، quality 4) | ✅ |
| ملفّات `transport/` | 3 (2 cpp + 1 h) | 3 | ✅ |
| مجلّدات قديمة `engine/`، `providers/`، `utils/` | غير موجودة | غير موجودة | ✅ |
| إجمالي `.cpp` تحت `src/` | 30 | 30 | ✅ |
| رصد Git للنقل | renames نظيفة | 30 ملفًّا مرصودًا كـrenames (محتوى 100%) | ✅ |
| مسارات `CMakeLists` المصدريّة | كلّها موجودة | كلّها تشير إلى ملفّات موجودة | ✅ |

النقل **مكتمل وسليم**: لا ملفّ ضائع ولا مكرَّر، والعدد قبل/بعد متطابق.

---

## جدول الملاحظات والتصويبات

| # | القاعدة | الملف | الملاحظة | التصويب المطبَّق | الفئة |
|---|---------|------|---------|------------------|------|
| 1 | CW-06 | `src/features/editing/completion_provider.cpp:28` | `#include "../generated/keywords_generated.h"` مسار نسبيّ بائت: `../generated` من `features/editing/` = `features/generated/` (غير موجود)؛ لم يُترجم إلا صدفةً عبر `-I shared/lexer/generated`. الشقيق `semantic_tokens_provider.cpp:37` يستعمل التضمين النظيف الصحيح. | حُوِّل إلى `#include "keywords_generated.h"` (تضمين نظيف عبر مسار التضمين، مطابق للشقيق). | تضمينات |
| 2 | CW-04 | `src/features/comprehension/semantic_tokens_provider.cpp:31-37` | الملف يوثّق صراحةً لماذا يُضمَّن `keywords_generated.h` نظيفًا — نمطٌ مرجعيّ يخالفه completion. | اتُّخذ مرجعًا، ووُحِّد completion عليه (م1). | اتّساق |
| 3 | README | `tools/lsp/README.md:15` | جدول البيانات الوصفيّ يقول الاعتماد `sad_core (← sad_shared)` بينما CMake (م4) يربط `sad_shared + sad_type_system` مباشرةً لا `sad_core`. | صُحِّح إلى ``sad_shared` + `sad_type_system` + `sad_formatter``. | دقّة README |
| 4 | README | `tools/lsp/README.md:54` | مخطّط الطبقات يُظهر `sad_core / sad_shared` كقاعدة، و`sad_core` لم يَعُد مربوطًا. | صُحِّح إلى `sad_shared (+ sad_type_system)`. | دقّة README |
| 5 | README | `tools/lsp/README.md:91` | مسار مصدر الحقيقة يقول `sad_shared … ──► sad_core ──► sad_lsp_engine` (المفسّر الكامل وسيط) وهو غير دقيق بعد م4. | صُحِّح إلى `sad_shared ──► sad_lsp_engine`. | دقّة README |
| 6 | README | `tools/lsp/README.md:177` | شجرة المجلّدات: تعليق `navigation/` يسرد 4 عناوين (تعريف · مراجع · رموز · روابط) بينما الفئة فيها 5 ملفّات (نقص «رموز-المستند»). تعارض مع السطر 48. | أُضيف «رموز-المستند» ⇒ 5 عناوين مطابقة للملفّات. | اتّساق README |
| 7 | المرجع الحيّ | `docs/بنية_المشروع/الأدوات.md:51-54` | جدول «المجلّدات الفرعية» يذكر `src/providers/`، `src/engine/`، `src/utils/` القديمة (مرجع حيّ خارج تقارير_مؤقته). | حُدِّث الجدول إلى البنية الجديدة: `src/core/`، `src/features/`، `src/transport/` (+ `data/`، `scripts/`). | مرجع متبقٍّ |
| 8 | CW-04 | `tools/lsp/CMakeLists.txt:21` | تعليق الهدف `sad_lsp_engine` يقول «جميع مزودات الميزات» دون ذكر النواة المنقولة. | حُدِّث الوصف ليشمل «النواة + مزوّدات الميزات المصنّفة». | تعليق CMake |
| 9 | README | `tools/lsp/README.md:160` | تعليق CMake في الشجرة `# هدفان: sad_lsp_engine (مكتبة) + sad-lsp (تنفيذيّ)` دقيق؛ تُحقّق فقط. | لا تغيير (صحيح). | دقّة README |
| 10 | اتّساق التصنيف | `CMakeLists.txt:86-89` | editing = completion/signature_help/formatting/on_type_formatting — تصنيف منطقيّ (تحرير). | لا تغيير (صحيح). | تصنيف |
| 11 | اتّساق التصنيف | `CMakeLists.txt:92-95` | comprehension = hover/semantic_tokens/inlay_hints/code_lens — صحيح (فهم لا تحرير). | لا تغيير (صحيح). | تصنيف |
| 12 | اتّساق التصنيف | `CMakeLists.txt:98-102` | navigation = definition/references/symbols/document_symbols/document_links — صحيح. | لا تغيير (صحيح). | تصنيف |
| 13 | اتّساق التصنيف | `CMakeLists.txt:105-108` | structure = call_hierarchy/type_hierarchy/selection_range/folding — صحيح. | لا تغيير (صحيح). | تصنيف |
| 14 | اتّساق التصنيف | `CMakeLists.txt:113-116` | quality = diagnostics/known_issues_detector/rename/code_actions — صحيح. | لا تغيير (صحيح). | تصنيف |
| 15 | CW-04 | `CMakeLists.txt:85` | تعليق فئة editing عربيّ فقط (بقيّة التعليقات ثنائيّة في الملف). | أُبقي كما هو (التعليقات الفئويّة القصيرة عربيّة موحَّدة عبر الفئات الخمس — اتّساق داخليّ). | تعليق CMake |
| 16 | ترتيب | `CMakeLists.txt:72-78` | ترتيب ملفّات core يبدأ بالمحرّك ثمّ المخزن ثمّ الخطّ ثمّ الفهرس/النطاق ثمّ الأدوات — ترتيب منطقيّ تنازليّ. | لا تغيير (صحيح). | ترتيب |
| 17 | اتّساق | `CMakeLists.txt` vs README §5 | ترتيب الفئات في CMake (editing→comprehension→navigation→structure→quality) يطابق README. | لا تغيير (متّسق). | اتّساق |
| 18 | README | `tools/lsp/README.md:197-198` | أسماء سويتات الاختبار الأربع مذكورة؛ تُحقّق مقابل `cmake/tests.cmake`. | لا تغيير (صحيحة). | دقّة README |
| 19 | المرجع الحيّ | `cmake/tests.cmake` | الاختبارات تربط مكتبة `sad_lsp_engine` لا مسارات مصدر مباشرة ⇒ لا مسار قديم. | لا تغيير (سليم). | مرجع متبقٍّ |
| 20 | CW-06 | `src/features/editing/formatting_provider.cpp:22` | `#include "format/sad_formatter.h"` مسار فرعيّ — يُترجم عبر `-I` الخاصّ بـ`sad_formatter`؛ صحيح وغير متأثّر بالنقل. | لا تغيير (سليم). | تضمينات |
| 21 | CW-06 | `src/transport/lsp_main.cpp:44` | `#include "utf8_args.h"` — يقع في `include/` العامّ، غير متأثّر بالنقل. | لا تغيير (سليم). | تضمينات |
| 22 | اكتمال البناء | `CMakeLists.txt:67-117` | كلّ ملفّات `core/` و`features/` الـ28 مدرجة في قائمة مصادر المكتبة؛ لا ملفّ مفقود. | لا تغيير (مكتمل). | اكتمال البناء |
| 23 | اكتمال البناء | `CMakeLists.txt:166-169` | ملفّا `transport/` مدرجان في التنفيذيّ بمساريهما الجديدين الصحيحين. | لا تغيير (سليم). | اكتمال البناء |
| 24 | README | `tools/lsp/README.md:166,171` | `data/` و`scripts/` مذكوران في شجرة المجلّدات بدقّة (المحتوى الفعليّ مطابق). | لا تغيير (صحيح). | دقّة README |
| 25 | README | `tools/lsp/README.md:173` | تعليق `core/` في الشجرة: «المستندات + الفهرس + خطّ التحليل + أدوات» يطابق محتوى الفئة. | لا تغيير (صحيح). | دقّة README |
| 26 | اتّساق | `CMakeLists.txt:69-71` | عنوان قسم النواة ثنائيّ (AR/EN) ويذكر «الأدوات المساعدة» (arabic/uri_utils) المنقولة من utils — توثيق دمج utils في core سليم. | لا تغيير (صحيح). | اتّساق |
| 27 | CW-04 | `tools/lsp/README.md:3-6` | الترويسة تصف القلب المفتوح؛ غير متأثّرة بالنقل. | لا تغيير. | دقّة README |
| 28 | تسمية | جميع ملفّات `features/` | لاحقة `_provider` متّسقة عدا `known_issues_detector` (كاشف لا مزوّد) و`symbols_provider`/`document_symbols_provider` (متمايزان). | لا تغيير (التسمية مقصودة ودقيقة دلاليًّا). | تسمية |
| 29 | README | `tools/lsp/README.md:46-50` | تعليقات الفئات المختصرة في مخطّط الطبقة ③ تطابق الملفّات (طُبّق تصويب navigation في م6 على الشجرة لا هنا — هذا السطر 48 كان صحيحًا أصلًا). | لا تغيير (صحيح). | دقّة README |
| 30 | BF-29 | بناء + اختبار | التحقّق النهائيّ من عدم التراجع بعد كلّ التصويبات. | أُعيد البناء وشُغّلت السويت ⇒ 47/47. | تراجع |
| 31 | المرجع الحيّ | كامل المستودع | بحث شامل عن `src/engine|providers|utils` الحيّة (استثناء تقارير_مؤقته/_bmad-output). | لم يَبْقَ مرجع حيّ بعد م1 وم7 (مطابقة `shared/utils/include` ليست مسار LSP). | مرجع متبقٍّ |

---

## خلاصة

- **إجمالي الملاحظات:** 31 (≥30 مطلوب).
- **مطبَّقة فعليًّا:** 7 (م1، م3، م4، م5، م6، م7، م8) + التحقّق النهائيّ م30.
- **ملاحظات تحقّق «لا تغيير» (مبرَّرة):** البقيّة — البند سليم/صحيح أصلًا، والتبرير مذكور في عمود التصويب.
- **اكتمال النقل:** مؤكَّد (28 منقولة + 3 transport، لا ضائع/مكرَّر، لا مجلّد قديم).
- **لا مسار قديم حيّ:** مؤكَّد بعد م1 و م7.
