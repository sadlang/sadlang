# اختبار الانحدار #13 — صنف كبادئة في معرّفات
# Regression Test #13 — "صنف" as prefix in identifiers causes conflicts
# الأولوية: P1

# === اختبار 1: استخدام بدائل (يجب أن يعمل) ===
ثابت فئة_عادي = 1
ثابت فئة_خاص = 2

إذا (فئة_عادي == 1)
    اطبع("PASS: بديل 'فئة_' يعمل")
وإلا
    اطبع("FAIL: بديل 'فئة_' فشل")
نهاية

# === اختبار 2: ثابت عادي ===
ثابت الحد_الأقصى = 100
إذا (الحد_الأقصى == 100)
    اطبع("PASS: ثوابت عادية تعمل")
وإلا
    اطبع("FAIL: ثوابت عادية فشلت")
نهاية

# === اختبار 3: صنف عادي ===
صنف حيوان
    باني(الاسم)
        هذا.الاسم = الاسم
    نهاية
نهاية

متغير ق = جديد حيوان("قط")
إذا (ق.الاسم == "قط")
    اطبع("PASS: صنف عادي يعمل")
وإلا
    اطبع("FAIL: صنف عادي فشل")
نهاية

اطبع("=== انتهى اختبار #13 ===")
