# اختبار الانحدار #1 — الوصول المزدوج arr[i]["key"] داخل الدوال
# Regression Test #1 — Double index access arr[i]["key"] inside functions
# المشكلة: المفسر ينهار عند الوصول المتسلسل arr[i]["key"] داخل دالة
# الأولوية: P0 (عاجل)

# === التحضير ===
متغير نوافذ = [{"عنوان": "نافذة1", "عرض": 20}, {"عنوان": "نافذة2", "عرض": 30}]

# === اختبار 1: وصول مباشر (يجب أن يعمل) ===
متغير عنوان_مباشر = نوافذ[0]["عنوان"]
إذا (عنوان_مباشر == "نافذة1")
    اطبع("PASS: وصول مباشر يعمل")
وإلا
    اطبع("FAIL: وصول مباشر فشل")
نهاية

# === اختبار 2: وصول داخل دالة (المشكلة الأصلية) ===
دالة احصل_على_عنوان(فهرس)
    ارجع نوافذ[فهرس]["عنوان"]
نهاية

متغير نتيجة = احصل_على_عنوان(0)
إذا (نتيجة == "نافذة1")
    اطبع("PASS: arr[i][key] داخل دالة يعمل")
وإلا
    اطبع("FAIL: arr[i][key] داخل دالة فشل — النتيجة: " + نتيجة)
نهاية

# === اختبار 3: وصول بفهرس متغير ===
دالة احصل_على_عرض(ف)
    ارجع نوافذ[ف]["عرض"]
نهاية

متغير عرض = احصل_على_عرض(1)
إذا (عرض == 30)
    اطبع("PASS: arr[var][key] يعمل")
وإلا
    اطبع("FAIL: arr[var][key] فشل — النتيجة: " + عرض)
نهاية

اطبع("=== انتهى اختبار #1 ===")
