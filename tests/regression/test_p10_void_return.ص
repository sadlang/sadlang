# اختبار الانحدار #10 — ارجع بدون قيمة (void return)
# Regression Test #10 — Return without value causes parse error
# المشكلة: ارجع بدون تعبير يسبب خطأ نحوي
# الأولوية: P0

# === اختبار 1: ارجع مع قيمة (يجب أن يعمل) ===
دالة مع_قيمة()
    ارجع 42
نهاية

متغير ق = مع_قيمة()
إذا (ق == 42)
    اطبع("PASS: ارجع مع قيمة يعمل")
وإلا
    اطبع("FAIL: ارجع مع قيمة فشل")
نهاية

# === اختبار 2: دالة بدون ارجع ===
دالة بدون_ارجع()
    اطبع("PASS: دالة بدون ارجع تعمل")
نهاية
بدون_ارجع()

# === اختبار 3: ارجع بقيمة 0 (حل بديل) ===
دالة أغلق(رقم_واصف)
    إذا (رقم_واصف < 0)
        ارجع 0
    نهاية
    ارجع 1
نهاية

متغير نتيجة = أغلق(-1)
إذا (نتيجة == 0)
    اطبع("PASS: ارجع بقيمة 0 يعمل")
وإلا
    اطبع("FAIL: ارجع بقيمة 0 فشل")
نهاية

اطبع("=== انتهى اختبار #10 ===")
