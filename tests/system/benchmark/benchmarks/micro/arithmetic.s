# بسم الله الرحمن الرحيم
# اختبارات أداء العمليات الحسابية
# Arithmetic Operations Benchmark

استورد وقت من "stdlib/time"
استورد اختبار_أداء من "stdlib/benchmark"

# ═══════════════════════════════════════════════════════
# إعدادات الاختبار
# ═══════════════════════════════════════════════════════

ثابت رقم عدد_التكرارات = 1000000
ثابت رقم جولات_التسخين = 100

# ═══════════════════════════════════════════════════════
# اختبارات الجمع
# ═══════════════════════════════════════════════════════

دالة اختبار_جمع_أعداد_صحيحة()
    رقم أ = 42
    رقم ب = 17
    رقم نتيجة = 0
    
    # التسخين
    لكل _ في مدى(0، جولات_التسخين)
        نتيجة = أ + ب
    نهاية
    
    # القياس
    متغير بداية = وقت.الآن_نانو()
    
    لكل _ في مدى(0، عدد_التكرارات)
        نتيجة = أ + ب
    نهاية
    
    متغير نهاية = وقت.الآن_نانو()
    متغير المدة = نهاية - بداية
    
    ارجع {
        "الاسم": "جمع_أعداد_صحيحة"،
        "التكرارات": عدد_التكرارات،
        "الوقت_الإجمالي_نانو": المدة،
        "متوسط_الوقت_نانو": المدة / عدد_التكرارات
    }
نهاية

دالة اختبار_جمع_أعداد_عشرية()
    عشري أ = 3.14159
    عشري ب = 2.71828
    عشري نتيجة = 0.0
    
    لكل _ في مدى(0، جولات_التسخين)
        نتيجة = أ + ب
    نهاية
    
    متغير بداية = وقت.الآن_نانو()
    
    لكل _ في مدى(0، عدد_التكرارات)
        نتيجة = أ + ب
    نهاية
    
    متغير نهاية = وقت.الآن_نانو()
    متغير المدة = نهاية - بداية
    
    ارجع {
        "الاسم": "جمع_أعداد_عشرية"،
        "التكرارات": عدد_التكرارات،
        "الوقت_الإجمالي_نانو": المدة،
        "متوسط_الوقت_نانو": المدة / عدد_التكرارات
    }
نهاية

# ═══════════════════════════════════════════════════════
# اختبارات الضرب
# ═══════════════════════════════════════════════════════

دالة اختبار_ضرب_أعداد_صحيحة()
    رقم أ = 123
    رقم ب = 456
    رقم نتيجة = 0
    
    لكل _ في مدى(0، جولات_التسخين)
        نتيجة = أ * ب
    نهاية
    
    متغير بداية = وقت.الآن_نانو()
    
    لكل _ في مدى(0، عدد_التكرارات)
        نتيجة = أ * ب
    نهاية
    
    متغير نهاية = وقت.الآن_نانو()
    متغير المدة = نهاية - بداية
    
    ارجع {
        "الاسم": "ضرب_أعداد_صحيحة"،
        "التكرارات": عدد_التكرارات،
        "الوقت_الإجمالي_نانو": المدة،
        "متوسط_الوقت_نانو": المدة / عدد_التكرارات
    }
نهاية

# ═══════════════════════════════════════════════════════
# اختبارات القسمة
# ═══════════════════════════════════════════════════════

دالة اختبار_قسمة_أعداد_صحيحة()
    رقم أ = 1000000
    رقم ب = 7
    رقم نتيجة = 0
    
    لكل _ في مدى(0، جولات_التسخين)
        نتيجة = أ / ب
    نهاية
    
    متغير بداية = وقت.الآن_نانو()
    
    لكل _ في مدى(0، عدد_التكرارات)
        نتيجة = أ / ب
    نهاية
    
    متغير نهاية = وقت.الآن_نانو()
    متغير المدة = نهاية - بداية
    
    ارجع {
        "الاسم": "قسمة_أعداد_صحيحة"،
        "التكرارات": عدد_التكرارات،
        "الوقت_الإجمالي_نانو": المدة،
        "متوسط_الوقت_نانو": المدة / عدد_التكرارات
    }
نهاية

دالة اختبار_باقي_القسمة()
    رقم أ = 1000000
    رقم ب = 7
    رقم نتيجة = 0
    
    لكل _ في مدى(0، جولات_التسخين)
        نتيجة = أ % ب
    نهاية
    
    متغير بداية = وقت.الآن_نانو()
    
    لكل _ في مدى(0، عدد_التكرارات)
        نتيجة = أ % ب
    نهاية
    
    متغير نهاية = وقت.الآن_نانو()
    متغير المدة = نهاية - بداية
    
    ارجع {
        "الاسم": "باقي_القسمة"،
        "التكرارات": عدد_التكرارات،
        "الوقت_الإجمالي_نانو": المدة،
        "متوسط_الوقت_نانو": المدة / عدد_التكرارات
    }
نهاية

# ═══════════════════════════════════════════════════════
# اختبارات مركبة
# ═══════════════════════════════════════════════════════

دالة اختبار_عمليات_مركبة()
    رقم أ = 100
    رقم ب = 50
    رقم ج = 25
    رقم نتيجة = 0
    
    لكل _ في مدى(0، جولات_التسخين)
        نتيجة = (أ + ب) * ج - (أ / ب)
    نهاية
    
    متغير بداية = وقت.الآن_نانو()
    
    لكل _ في مدى(0، عدد_التكرارات)
        نتيجة = (أ + ب) * ج - (أ / ب)
    نهاية
    
    متغير نهاية = وقت.الآن_نانو()
    متغير المدة = نهاية - بداية
    
    ارجع {
        "الاسم": "عمليات_مركبة"،
        "التكرارات": عدد_التكرارات،
        "الوقت_الإجمالي_نانو": المدة،
        "متوسط_الوقت_نانو": المدة / عدد_التكرارات
    }
نهاية

# ═══════════════════════════════════════════════════════
# دالة الطباعة
# ═══════════════════════════════════════════════════════

دالة اطبع_نتيجة(قاموس نتيجة)
    اطبع("  ")
    اطبع(نتيجة["الاسم"])
    اطبع(": ")
    اطبع(نتيجة["متوسط_الوقت_نانو"])
    اطبع(" نانو ثانية/عملية")
    
    # احسب العمليات في الثانية
    عشري عمليات_في_الثانية = 1000000000.0 / نتيجة["متوسط_الوقت_نانو"]
    اطبع(" (")
    اطبع(عمليات_في_الثانية)
    اطبع(" عملية/ثانية)\n")
نهاية

# ═══════════════════════════════════════════════════════
# الدالة الرئيسية
# ═══════════════════════════════════════════════════════

دالة رئيسية()
    اطبع("\n")
    اطبع("╔═══════════════════════════════════════════════════════════╗\n")
    اطبع("║          🏎️  اختبارات أداء العمليات الحسابية              ║\n")
    اطبع("╠═══════════════════════════════════════════════════════════╣\n")
    اطبع("║  التكرارات: ")
    اطبع(عدد_التكرارات)
    اطبع(" لكل اختبار                     ║\n")
    اطبع("╚═══════════════════════════════════════════════════════════╝\n")
    اطبع("\n")
    
    مصفوفة النتائج = []
    
    # تشغيل الاختبارات
    اطبع("📊 نتائج الاختبارات:\n")
    اطبع("─────────────────────────────────────────────────────────────\n")
    
    متغير ن1 = اختبار_جمع_أعداد_صحيحة()
    النتائج.أضف(ن1)
    اطبع_نتيجة(ن1)
    
    متغير ن2 = اختبار_جمع_أعداد_عشرية()
    النتائج.أضف(ن2)
    اطبع_نتيجة(ن2)
    
    متغير ن3 = اختبار_ضرب_أعداد_صحيحة()
    النتائج.أضف(ن3)
    اطبع_نتيجة(ن3)
    
    متغير ن4 = اختبار_قسمة_أعداد_صحيحة()
    النتائج.أضف(ن4)
    اطبع_نتيجة(ن4)
    
    متغير ن5 = اختبار_باقي_القسمة()
    النتائج.أضف(ن5)
    اطبع_نتيجة(ن5)
    
    متغير ن6 = اختبار_عمليات_مركبة()
    النتائج.أضف(ن6)
    اطبع_نتيجة(ن6)
    
    اطبع("─────────────────────────────────────────────────────────────\n")
    اطبع("\n✅ اكتملت جميع الاختبارات!\n")
    
    ارجع 0
نهاية
