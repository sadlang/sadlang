# مشروع_اختبار_التوثيق

> (AR) توثيق مشروع مُستخرج تلقائياً من شجرة AST
> (EN) Project documentation auto-extracted from AST

**(AR) عدد الملفات الموثقة:** 2 — **(AR) إجمالي التصريحات:** 8  
**(EN) Documented files:** 2 — **(EN) Total declarations:** 8

---

## (AR) فهرس الملفات / (EN) File Index

- [`tests\docs_extraction\test_docs_basic.ص`](#tests-docs_extraction-test_docs_basic-ص) — 5 تصريحات
- [`tests\docs_extraction\test_docs_math.ص`](#tests-docs_extraction-test_docs_math-ص) — 3 تصريحات

---

<a id="tests-docs_extraction-test_docs_basic-ص"></a>
## 📄 `tests\docs_extraction\test_docs_basic.ص`

### دالة / function `القيمة_المطلقة`

```sad
دالة القيمة_المطلقة(ن)
```

_مكتبة رياضيات تجريبية لاختبار استخراج التوثيق_

تحتوي على دوال وأصناف وتعدادات وبنى مع تعليقات توثيقية متنوعة
 يحسب القيمة المطلقة لرقم

**(AR) المعاملات / (EN) Parameters**

| (AR) الاسم / (EN) Name | (AR) الوصف / (EN) Description |
|---|---|
| `ن` | الرقم المراد حساب قيمته المطلقة |

**(AR) القيمة المرجعة / (EN) Returns:** القيمة المطلقة (دائماً موجبة أو صفر)

**(AR) أمثلة / (EN) Examples**

```sad
متغير ر = القيمة_المطلقة(-7)
 اطبع_سطر(ر)  # يطبع 7
```

_(AR) منذ / (EN) Since: 1.0.0_

_(AR) المؤلف / (EN) Author: فريق ص_


---

### دالة / function `جمع`

```sad
دالة جمع(أ، ب)
```

_تجمع رقمين معاً_

**(AR) المعاملات / (EN) Parameters**

| (AR) الاسم / (EN) Name | (AR) الوصف / (EN) Description |
|---|---|
| `أ` | الرقم الأول |
| `ب` | الرقم الثاني |

**(AR) القيمة المرجعة / (EN) Returns:** ناتج الجمع


---

### صنف / class `نقطة`

```sad
صنف نقطة
```

_يمثل نقطة في الفضاء ثنائي الأبعاد_

يدعم العمليات الأساسية على النقاط مثل المسافة والمساواة.
 كل نقطة لها إحداثيان: س (أفقي) و ص (رأسي).

**(AR) أمثلة / (EN) Examples**

```sad
متغير ن = نقطة(3، 4)
 اطبع_سطر(ن.المسافة_من_الأصل())
```

_(AR) منذ / (EN) Since: 1.0.0_


**(AR) الأعضاء الموثقة (1)**  
**(EN) Documented members (1)**

#### طريقة / method `المسافة_من_الأصل`

```sad
عام دالة المسافة_من_الأصل() -> لاشيء
```

_تحسب المسافة الإقليدية من نقطة الأصل (0، 0)_

**(AR) القيمة المرجعة / (EN) Returns:** المسافة كرقم عشري غير سالب



---

### تعداد / enum `لون_الإشارة`

```sad
تعداد لون_الإشارة
    أحمر
    أصفر
    أخضر
نهاية
```

_يمثل حالات إشارة المرور_

كل لون له معنى محدد في نظام المرور.

_(AR) منذ / (EN) Since: 1.0.0_


---

### بنية / struct `متجه_3`

```sad
بنية متجه_3
    س
    ص
    ع
نهاية
```

_بنية تخزن إحداثيات ثلاثية الأبعاد بدون وراثة_

مفيدة للحسابات الهندسية الخفيفة.


---

<a id="tests-docs_extraction-test_docs_math-ص"></a>
## 📄 `tests\docs_extraction\test_docs_math.ص`

### دالة / function `مربع`

```sad
دالة مربع(ع)
```

_دالة تحسب مربع رقم_

**(AR) المعاملات / (EN) Parameters**

| (AR) الاسم / (EN) Name | (AR) الوصف / (EN) Description |
|---|---|
| `ع` | الرقم المراد تربيعه |

**(AR) القيمة المرجعة / (EN) Returns:** مربع الرقم


---

### دالة / function `مجموع_قائمة`

```sad
دالة مجموع_قائمة(قائمة)
```

_تطبق دالة لجمع قائمة من الأرقام_

**(AR) المعاملات / (EN) Parameters**

| (AR) الاسم / (EN) Name | (AR) الوصف / (EN) Description |
|---|---|
| `قائمة` | مصفوفة الأرقام |

**(AR) القيمة المرجعة / (EN) Returns:** مجموع جميع العناصر


---

### بنية / struct `مستطيل`

```sad
بنية مستطيل
    عرض
    ارتفاع
نهاية
```

_بنية تمثل مستطيل_


---

