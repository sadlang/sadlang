# ═══════════════════════════════════════════════
# اختبار 10: تعارض الاستعارة المتغيرة
# Test 10: Mutable Borrow Conflict
# ═══════════════════════════════════════════════

دالة رئيسية()
    اطبع_سطر("=== اختبار تعارض الاستعارة ===")
    
    نص رسالة = "مرحبا"
    
    # استعارة مشتركة
    متغير مرجع_قراءة = &رسالة
    اطبع_سطر("استعارة مشتركة نجحت: ")
    اطبع_سطر(مرجع_قراءة)
    
    # محاولة استعارة متغيرة أثناء وجود استعارة مشتركة
    # يجب أن يفشل!
    اطبع_سطر("محاولة استعارة متغيرة أثناء وجود مشتركة...")
    متغير مرجع_كتابة = &متغير رسالة
نهاية
