# اختبار نقل الملكية - يجب أن يكشف خطأ
# Move ownership test - should detect use-after-move error

دالة رئيسية()
    # تعريف نص (نوع نقل)
    نص اسم = "أحمد"

    # نقل الملكية إلى متغير جديد
    نص اسم_جديد = اسم

    # خطأ! استخدام بعد النقل
    اطبع_سطر(اسم)
نهاية
