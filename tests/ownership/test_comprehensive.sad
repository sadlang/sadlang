# ═══════════════════════════════════════════════════════════════════
# الاختبار الشامل: نظام الملكية الكامل في لغة ص
# Comprehensive Test: Complete Ownership System in Sad Language
# ═══════════════════════════════════════════════════════════════════

دالة رئيسية()
    اطبع_سطر("╔══════════════════════════════════════════╗")
    اطبع_سطر("║  اختبار نظام الملكية الشامل - لغة ص     ║")
    اطبع_سطر("╚══════════════════════════════════════════╝")
    اطبع_سطر("")
    
    # ─── 1. الأنواع القابلة للنسخ ───
    اطبع_سطر("📋 1. الأنواع القابلة للنسخ (Copy Types)")
    متغير عدد = 42
    متغير نسخة = عدد
    اطبع("   عدد = ")
    اطبع_سطر(عدد)
    اطبع("   نسخة = ")
    اطبع_سطر(نسخة)
    اطبع_سطر("   ✅ الأعداد تُنسخ ولا تُنقل")
    اطبع_سطر("")
    
    # ─── 2. نقل الملكية ───
    اطبع_سطر("🔄 2. نقل الملكية (Move Semantics)")
    نص رسالة = "مرحبا بالعالم!"
    اطبع("   الرسالة الأصلية: ")
    اطبع_سطر(رسالة)
    متغير رسالة_جديدة = رسالة
    اطبع("   بعد النقل: ")
    اطبع_سطر(رسالة_جديدة)
    اطبع_سطر("   ✅ الملكية انتقلت بنجاح")
    اطبع_سطر("")
    
    # ─── 3. التعديل ───
    اطبع_سطر("✏️ 3. التعديل على المتغيرات (Mutation)")
    متغير عداد = 0
    عداد = 1
    عداد = 2
    عداد = 3
    اطبع("   العداد = ")
    اطبع_سطر(عداد)
    اطبع_سطر("   ✅ التعديل على الأعداد مسموح")
    اطبع_سطر("")
    
    # ─── 4. النطاقات ───
    اطبع_سطر("🔲 4. النطاقات (Scopes)")
    نص رسالة_خارجية = "خارج"
    اذا (صحيح)
        نص رسالة_داخلية = "داخل"
        اطبع("   داخل الكتلة: ")
        اطبع_سطر(رسالة_داخلية)
    نهاية
    اطبع("   خارج الكتلة: ")
    اطبع_سطر(رسالة_خارجية)
    اطبع_سطر("   ✅ تنظيف النطاقات يعمل")
    اطبع_سطر("")
    
    # ─── 5. الاستعارة المشتركة ───
    اطبع_سطر("🔗 5. الاستعارة المشتركة (Shared Borrow)")
    نص اسم = "أحمد"
    متغير مرجع1 = &اسم
    متغير مرجع2 = &اسم
    اطبع("   مرجع1 = ")
    اطبع_سطر(مرجع1)
    اطبع("   مرجع2 = ")
    اطبع_سطر(مرجع2)
    اطبع_سطر("   ✅ استعارات مشتركة متعددة مسموحة")
    اطبع_سطر("")
    
    اطبع_سطر("╔══════════════════════════════════════════╗")
    اطبع_سطر("║  ✅ جميع الاختبارات نجحت!               ║")
    اطبع_سطر("╚══════════════════════════════════════════╝")
نهاية
