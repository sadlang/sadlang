# ═══════════════════════════════════════════════
# اختبار 7: النقل المزدوج (Double Move)
# Test 7: Double Move - moving an already moved variable
# ═══════════════════════════════════════════════

دالة رئيسية()
    نص رسالة = "نص مهم"
    
    # النقل الأول - يجب أن ينجح
    متغير نسخة1 = رسالة
    اطبع_سطر("النقل الأول نجح: ")
    اطبع_سطر(نسخة1)
    
    # النقل الثاني - يجب أن يفشل (المتغير منقول بالفعل)
    اطبع_سطر("محاولة نقل مزدوج...")
    متغير نسخة2 = رسالة
    اطبع_سطر(نسخة2)
نهاية
