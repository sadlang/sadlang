# ═══════════════════════════════════════════════
# اختبار 8: التعديل على المتغيرات (Mutation)
# Test 8: Variable Mutation
# ═══════════════════════════════════════════════

دالة رئيسية()
    # تعديل عدد صحيح (قابل للنسخ) - يجب أن ينجح
    متغير عداد = 0
    عداد = 1
    عداد = 2
    عداد = 3
    اطبع_سطر("العداد بعد التعديلات: ")
    اطبع_سطر(عداد)
    
    # تعديل نص - يجب أن ينجح (تعديل المتغير نفسه)
    نص اسم = "أحمد"
    اسم = "محمد"
    اطبع_سطر("الاسم بعد التعديل: ")
    اطبع_سطر(اسم)
    
    # نقل ثم محاولة تعديل - يجب أن يفشل
    نص رسالة = "مرحبا"
    متغير رسالة_منقولة = رسالة
    اطبع_سطر("محاولة تعديل بعد النقل:")
    رسالة = "وداعا"
نهاية
