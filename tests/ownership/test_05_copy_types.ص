# ═══════════════════════════════════════════════
# اختبار 5: الأنواع القابلة للنسخ (Copy Types)
# Test 5: Copy Types - integers, booleans copy instead of move
# ═══════════════════════════════════════════════

دالة رئيسية()
    # الأعداد الصحيحة قابلة للنسخ - لا تُنقل
    متغير عدد = 42
    متغير نسخة_عدد = عدد
    
    # يجب أن ينجح - الأعداد تُنسخ ولا تُنقل
    اطبع_سطر("العدد الأصلي: ")
    اطبع_سطر(عدد)
    اطبع_سطر("النسخة: ")
    اطبع_سطر(نسخة_عدد)
    
    # المنطقي أيضاً قابل للنسخ
    متغير علم = صحيح
    متغير نسخة_علم = علم
    اطبع_سطر("العلم الأصلي: ")
    اطبع_سطر(علم)
    اطبع_سطر("نسخة العلم: ")
    اطبع_سطر(نسخة_علم)
    
    # النصوص غير قابلة للنسخ - تُنقل
    نص رسالة = "مرحبا"
    متغير رسالة_جديدة = رسالة
    
    # هذا يجب أن يفشل - النص مُنقول
    اطبع_سطر("محاولة استخدام نص بعد النقل:")
    اطبع_سطر(رسالة)
نهاية
