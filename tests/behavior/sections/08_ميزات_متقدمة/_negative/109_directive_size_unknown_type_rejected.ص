# @description `@حجم` على نوعٍ لا حجمَ ثابتَ له يُرفَضُ باسمِه. وكان يُخرِجُ ٨ صامتًا في المترجّمِ و٠ في المفسّرِ — رقمًا يُحجَزُ به أو يُقسَمُ عليه ولا يُشخَّصُ في أيٍّ منهما.
# @expect_error لا يمكن تطبيق 'حجم النوع' على 'صنف'
# @expect_compile_error لا يمكن تطبيق 'حجم النوع' على 'صنف'
# @priority P0

# ============================================================
# (AR) 🔑 «حالةٌ ناقصةٌ تسقطُ إلى افتراضٍ يكذب»: الافتراضُ في الجدولَين
#      المكتوبَين باليدِ كان ٨ و٠ — ولم يكن أحدُهما تشخيصًا. فنوعٌ حجمُه
#      يعتمدُ على تصريحِه (صنفٌ حجمُه حقولُه، متّجهٌ حجمُه خاناتُه، نوعٌ
#      عامٌّ لم يُستبدَل) كان يُجيبُ رقمًا مُخترَعًا.
#
# (AR) وغيابُ `size_bytes` في مصدرِ الحقيقةِ **إعلانٌ لا سهو**: يُولَّدُ منه
#      `kSadTypeSizeUnknown` قيمةً مميّزةً سالبةً لا يمكنُ أن تُقرَأَ حجمًا
#      صحيحًا سهوًا، فيُلزَمُ المُنادي بالتشخيص.
# ============================================================

اطبع_سطر(@حجم(صنف))
