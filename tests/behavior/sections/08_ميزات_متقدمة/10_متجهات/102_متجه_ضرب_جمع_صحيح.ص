# @description «متجه_ضرب_جمع» على حزمٍ صحيحةٍ يُعطي أ×ب+ج بالضبط. كان يستدعي llvm.fma على متّجهٍ صحيحٍ فيُجهِضُ المترجّم، لأنّ الأماميّةَ لا تعرفُ نوعَ العنصرِ ولا تُمرِّرُه.
# @expected 32\n44
# @priority P0
# @skip_interpreter مدمجاتُ المتّجهاتِ استراتيجيّتُها INLINE في المترجّمِ حصرًا — لا نظيرَ لها في المفسّر

# ============================================================
# (AR) 🔑 llvm.fma **عشريٌّ حصرًا**. وطلبُه على ‹4 x i64› يولّدُ اسمًا لا
#      وجودَ له (llvm.fma.v4i64) فيُجهِضُ المترجّم. والذراعُ الصحيحةُ
#      ليست تقريبًا: mul ثمّ add تعبيرٌ **مضبوطٌ** عن أ×ب+ج في الصحيح،
#      إذ لا تقريبَ وسيطًا يُفقَد كما في العشريّ.
# (EN) llvm.fma is float-only; for integer packs mul+add is exact, not an
#      approximation — there is no intermediate rounding to lose.
# ============================================================

دالة جرب()
    متغير أ = متجه(1، 2، 3، 4)
    متغير ب = متجه(5، 6، 7، 8)
    متغير ج = متجه(9، 10، 11، 12)
    متغير ن = متجه_ضرب_جمع(أ، ب، ج)

    # (AR) تُقاسُ خانتان لا واحدة: خانةٌ واحدةٌ قد توافقُ صُدفةً.
    اطبع_سطر(متجه_عنصر(ن، 2))   # 3×7+11 = 32
    اطبع_سطر(متجه_عنصر(ن، 3))   # 4×8+12 = 44
نهاية

جرب()
