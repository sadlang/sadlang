# @description «الأساس()» وأبٌ بلا بانٍ — لا شيءَ يُنفَّذُ ولا يُخفِقُ الرَّبط
# @expected ابن-عمل\nقيمة:5

# ============================================================
# (AR) `الأساس()` في صنفٍ أبوه بلا بانٍ ليسَ خطأً في المصدر: لا شيءَ يُنفَّذُ فحسب.
#
#      والمفسّرُ يحرسُ هذا أصلًا، وكانَ المصرِّفُ بلا حارسٍ نظير — لاتماثلٌ **كامنٌ**
#      لم يظهرْ ما دامَ النداءُ الفارغُ مُلغًى بالكلّيّة. فلمّا صارَ النداءُ يُنفَّذُ
#      بحقٍّ، بُثَّ نداءٌ لرمزٍ لا وجودَ له وأخفقَ الرَّبط:
#          unresolved external symbol أب.بناء
#      لا تشخيصَ لغةٍ ولا ملفَّ خرج.
#
#      والدرسُ أنّ إصلاحًا صحيحًا قد يكشِفُ لاتماثلًا سابقًا؛ فالبذرةُ تُثبّتُ الحالةَ
#      التي كشفَتْه كي لا يعودَ الحارسُ يسقطُ من أحدِ المحرّكَين.
# ============================================================

صنف والد
    دالة عام وصف()
        ارجع "والد"
    نهاية
نهاية

صنف ولد يرث والد
    متغير عام قيمة

    باني(قيمة)
        الأساس()
        اطبع_سطر("ابن-عمل")
        هذا.قيمة = قيمة
    نهاية
نهاية

متغير كائن = ولد(5)
اطبع_سطر("قيمة:" + نص(كائن.قيمة))
