# @description حقلٌ عدميٌّ: الطباعةُ تختلفُ بين المحرّكَين (خفضُ النوعِ غيرِ المُصرَّح)
# @skip_compiler
# @expected لاشيء\nصحيح

# ============================================================
# (AR) @skip_compiler — ثغرةٌ **مُعلَنة** باقيةٌ واحدةٌ لا اثنتان.
#
#      كانَ هنا عيبانِ مُتراكبان. الأوّلُ سُدَّ: «الأساس()» بلا وسائطَ صارَ يُنفِّذُ
#      بانِيَ الأبِ بخاناتٍ عدميّة، بعدَ رفعِ التمييزِ إلى الشجرةِ نفسِها (انظر ١٢٨
#      التي تقيسُ الأثرَ مباشرةً).
#
#      والباقي هو الثاني وحدَه: الحقلُ العدميُّ يطبعُه المفسّرُ `لاشيء` ويطبعُه
#      المصرِّفُ حارسَ العدمِ عددًا — وهي علّةُ **خفضِ النوعِ غيرِ المُصرَّح**
#      (المعامِلُ غيرُ المُصرَّحِ يُخفَضُ إلى صحيح)، تُصلَحُ في حملتِها ولا تُستَرُ هنا
#      بتعديلِ المتوقَّع.
#
#      ويبقى في المفسّرِ أثرٌ ثالثٌ مُعلَن: يطبعُ `لاشيء` بينما `== لاشيء` يعطي
#      `خطأ` — فالطباعةُ والمقارنةُ لا تتّفقانِ على القيمةِ نفسِها.
# ============================================================

صنف والد
    متغير عام سمة

    باني(سمة)
        هذا.سمة = سمة
    نهاية
نهاية

صنف ولد يرث والد
    باني()
        الأساس()
    نهاية
نهاية

متغير كائن = ولد()
اطبع_سطر(نص(كائن.سمة))

# (AR) ضابطٌ: نداءُ الأساسِ بوسيطٍ فعليٍّ يُنفَّذُ ويبلغُ الحقل
صنف والد2
    متغير عام سمة

    باني(سمة)
        هذا.سمة = سمة
    نهاية
نهاية

صنف ولد2 يرث والد2
    باني(قيمة)
        الأساس(قيمة)
    نهاية
نهاية

اطبع_سطر(نص(ولد2(5).سمة == 5))
