# @description ثغرةٌ مُعلَنة: معامِلُ المشتقِّ يحجبُ متغيّرًا عامًّا يكتبُه جسمُ الأب
# @skip_compiler
# @expected عدّاد:0\nمستقلّ:1

# ============================================================
# (AR) @skip_compiler — ثغرةٌ **مُعلَنةٌ** لا يُدَّعى سدُّها، ومُثبَتٌ أنّها ليست
#      انحدارًا: خطُّ الأساسِ (الملفُّ قبلَ هذه الحملةِ، مبنيًّا ومقيسًا) يُعطي
#      المخرَجَ نفسَه.
#
#      نطاقُ البانِي في المفسّرِ **مسطَّحٌ** لا نطاقَ لكلِّ مستوًى. فمعامِلُ
#      `ابن` المسمّى `عدّاد` يشغلُ الاسمَ في ذلكَ النطاقِ طيلةَ السلسلة، فيكتبُ
#      جسمُ `أب` عليه بدلَ المتغيّرِ العامّ، ثمّ يُستعادُ عندَ الخروج ⇒ الزيادةُ
#      تضيعُ. أمّا المصرِّفُ فلكلِّ بانٍ إطارُه، فيبلغُ الاسمُ العامَّ ⇒ `1`.
#
#      وسدُّها يقتضي نطاقًا لكلِّ مستوًى في المفسّرِ لا ترقيعًا: أيُّ حارسٍ يقيسُ
#      «هل هذا الاسمُ معامِلٌ في مكانٍ ما» يبتلعُ كتابةً مشروعةً في مكانٍ آخر.
#
#      وضابطُ «مستقلّ» يعزلُ العلّة: الشكلُ نفسُه بمعامِلٍ لا يوافقُ اسمَ العامِّ
#      ⇒ الزيادةُ تبلغُ ⇒ `1`. فالفارقُ هو تصادمُ الاسمِ وحدَه.
# ============================================================

متغير عدّاد = 0

صنف أب
    متغير عام س = 0

    باني(قيمة)
        عدّاد = عدّاد + 1
    نهاية
نهاية

صنف ابن يرث أب
    باني(عدّاد، قيمة)
        الأساس(قيمة)
    نهاية
نهاية

متغير كائن = ابن(99, 1)
اطبع_سطر("عدّاد:" + نص(عدّاد))

# (AR) الضابطُ المعزول: الاسمُ لا يتصادم
متغير مستقلّ = 0

صنف أب2
    متغير عام س = 0

    باني(قيمة)
        مستقلّ = مستقلّ + 1
    نهاية
نهاية

صنف ابن2 يرث أب2
    باني(علامة، قيمة)
        الأساس(قيمة)
    نهاية
نهاية

متغير كائن2 = ابن2(99, 1)
اطبع_سطر("مستقلّ:" + نص(مستقلّ))
