# @description مالكُ «هذا.طريقة(...)» يُطلَبُ في سلسلةِ الأساسِ لا في المستوى الواحد
# @expected اعرض:2.5

# ============================================================
# (AR) الوراثةُ تجعلُ الصنفَ المحيطَ **غيرَ** مُعرِّفِ الطريقة. والوقوفُ عندَ
#      `فرع.members` وحدَها كانَ يُبقي خانةَ `س` صحيحًا بينما موقعُ النداءِ
#      يُمرِّرُ عشريًّا، فيُجهِضُ `sad-build` بلا تشخيصِ لغةٍ ولا ملفِّ خرج:
#      `Assertion failed: … "Calling a function with a bad signature!"` ثمّ
#      **يعلَقُ** حتّى القتل.
#
#      ولا مُستقبِلَ آخرَ في البذرةِ عمدًا: أيُّ `ك.اعرض(2.5)` يأتي بالحسمِ من
#      رَبطِ المتغيّرِ فيُخفي ما تدّعي البذرةُ فحصَه.
# ============================================================

صنف قاعدة
    دالة اعرض(س)
        اطبع_سطر("اعرض:" + نص(س))
    نهاية
نهاية

صنف فرع يرث قاعدة
    دالة ذاتي()
        هذا.اعرض(2.5)
    نهاية
نهاية

متغير كائن = فرع()
كائن.ذاتي()
