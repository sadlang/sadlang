# @description مُستقبِلٌ عبرَ معامِلِ دالّةٍ حرّةٍ — يُحسَمُ من صنفِ الوسيطِ في موقعِ النداء
# @expected اعرض:2.5

# ============================================================
# (AR) المُستقبِلُ هنا معامِلُ دالّةٍ حرّة، فلا يُحسَمُ صنفُه من إعلانِه. لكنّ
#      المعلومةَ حاضرةٌ في **موقعِ النداء**: `عبر_معامل(مخزن())` يُصرِّحُ بالصنفِ
#      صراحةً — وكانت تُهمَل.
#
#      وعاقبةُ إهمالِها لم تكنْ «ترقيةً فائتة»: المعامِلُ غيرُ المُصرَّحِ يُخفَضُ إلى
#      **صحيح**، فتُمرَّرُ بتّاتُ `٢٫٥` عددًا صحيحًا ⇒ `اعرض:4612811918334230528`
#      بلا تشخيص، بينما المفسّرُ يطبعُ `اعرض:2.5`.
#
#      والرَّبطُ يقينٌ لا تخمين: يُقرَأُ صنفُ الوسيطِ من موقعِ النداء، ومالكٌ لا
#      يُحسَمُ يبقى بلا ترقيةٍ كما كان.
# ============================================================
صنف مخزن
    دالة اعرض(س)
        اطبع_سطر("اعرض:" + نص(س))
    نهاية
نهاية

دالة عبر_معامل(م)
    م.اعرض(2.5)
نهاية

عبر_معامل(مخزن())
