# @description مالكُ «هذا.طريقة(...)» هو الصنفُ المحيط — يقينًا لا تخمينًا
# @expected اعرض:2.5

# ============================================================
# (AR) `هذا` أكثرُ صيغِ نداءِ العضوِ ورودًا، ومالكُه معلومٌ بلا تخمين: الصنفُ الذي
#      نمسحُ عضوَه. وتركُه بلا حسمٍ ليسَ «امتناعًا عن الترقية» — بل كانَ يُجهِضُ
#      المصرِّفَ بلا تشخيصِ لغةٍ ولا ملفِّ خرج:
#      `Assertion failed: … "Calling a function with a bad signature!"`.
#
#      فالخانةُ تبقى صحيحًا (خفضُ المعامِلِ غيرِ المُصرَّح) بينما موقعُ النداءِ
#      يُمرِّرُ عشريًّا ⇒ توقيعٌ لا يُطابِق ⇒ سقوطٌ في LLVM لا في اللغة.
#
#      ولا مُستقبِلَ آخرَ في البذرةِ عمدًا: بإضافةِ `ك.اعرض(2.5)` كانَ الحسمُ يأتي
#      من رَبطِ المتغيّرِ فيختفي العيبُ — فوجودُه يُخفي ما تدّعي البذرةُ فحصَه.
# ============================================================

صنف مخزن
    دالة اعرض(س)
        اطبع_سطر("اعرض:" + نص(س))
    نهاية

    دالة ذاتي()
        هذا.اعرض(2.5)
    نهاية
نهاية

متغير كائن = مخزن()
كائن.ذاتي()
