# @description الاختطافُ مسدودٌ لكلِّ صورِ المُستقبِلِ لا لشكلٍ بعينِه
# @expected حقل=[9]\nفهرسة=[1, 5]\nمتغير=[1, 7]

# ============================================================
# (AR) سدُّ الاختطافِ بعدِّ **أشكالِ العُقَد** يترُكُ بابًا لكلِّ شكلٍ لم يُعَدّ.
#      وقد سُدَّ المتغيّرُ والمصفوفةُ الحرفيّةُ أوّلًا، فبقيَتْ ثلاثُ صورٍ تُختطَف:
#      حقلٌ (`هذا.عناصر.أضف`)، وفهرسةٌ (`ج[0].أضف`)، ونداءٌ متسلسل (`اصنع().أضف`).
#
#      والعلاجُ ليسَ إضافةَ ثلاثةِ أشكالٍ إلى العدّ — بل السؤالَ عن **نوعِ القيمةِ
#      المبنيّةِ** لا عن كتابتِها. فالبذرةُ تحرُسُ الصورَ الثلاثَ معًا: أيُّ ارتدادٍ
#      إلى عدِّ الأشكالِ يُسقِطُ واحدةً منها على الأقلّ.
#
#      والصنفُ المُصادِمُ لا يُنشَأُ قطّ: مجرّدُ وجودِ تعريفِه كانَ يكفي للاختطاف.
#
#      وتُطبَعُ `ج[0]` لا `ج`: طباعةُ المصفوفةِ المتداخلةِ عيبٌ **مستقلٌّ** مُعلَنٌ
#      (يطبعُها المصرِّفُ مؤشّراتٍ خامًّا ولو لم يكنْ في البرنامجِ صنفٌ قطّ)، ولو
#      دخلَ البذرةَ لأخفقَتْ لعلّةٍ لا تشهدُ عليها.
# ============================================================

صنف مخزن
    باني()
    نهاية

    دالة أضف(صحيح س)
        اطبع_سطر("اختُطِف")
    نهاية
نهاية

صنف حاوية
    متغير عناصر = []
    باني()
    نهاية

    دالة اعمل()
        هذا.عناصر.أضف(9)
        اطبع_سطر("حقل=" + نص(هذا.عناصر))
    نهاية
نهاية

متغير ح = حاوية()
ح.اعمل()

متغير ج = [[1]، [2]]
ج[0].أضف(5)
اطبع_سطر("فهرسة=" + نص(ج[0]))

دالة اصنع()
    ارجع [1]
نهاية

متغير ن = اصنع()
ن.أضف(7)
اطبع_سطر("متغير=" + نص(ن))
# (AR) الصورةُ المتسلسلةُ الحقيقيّة: مُستقبِلُها نتيجةُ نداءٍ بلا إسناد. ولا خرجَ لها
#      عمدًا — فهي محروسةٌ **بالنفي**: أيُّ اختطافٍ يطبعُ «اختُطِف» فيكسِرُ المتوقَّع.
اصنع().أضف(8)
