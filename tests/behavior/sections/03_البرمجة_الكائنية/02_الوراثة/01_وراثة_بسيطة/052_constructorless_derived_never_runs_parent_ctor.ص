# @description الفرع بلا بانٍ لا يشغّل باني الأصل — لا الفارغ ولا ذو الطرائق؛ التنفيذ بنداء «الأساس()» الصريح حصرًا
# @expected 0\n0\nشغل\nباني أصل\n9
# @priority P1

# ============================================================
# (AR) 🔑 القاعدة المقيسة (٢٣ آب ٢٠٢٦، المحرّكان متطابقان): باني الأصل
#      لا يُنفَّذ عند إنشاء فرعٍ بلا بانٍ — أبدًا: لا لفرعٍ فارغِ الجسم
#      ولا لفرعٍ يعلنُ طرائق. تنفيذُه بنداء «الأساس()» الصريحِ داخلَ
#      بانِي الفرعِ حصرًا (بذور 126–129 تحرس ذاك النصف).
#
#      ⚠️ وهذه البذرةُ وُلدت من قياسِ وكيلٍ **مغلوط** ادّعى أنّ الفرعَ
#      الفارغَ يرثُ التنفيذَ وذا الطريقةِ لا يرثُه — فسُجِّل «تناقضٌ
#      داخليّ» وهميّ. القياسُ المعاد: السلوكُ موحَّدٌ (لا وراثةَ تنفيذٍ
#      إطلاقًا)، ومصدرُ الحقيقةِ صامتٌ عن وراثةٍ ضمنيّة. فالبذرةُ تثبّت
#      الحكمَ الموحَّدَ لئلّا يعيدَ محرّكٌ فتحَه من طرفٍ واحد، وأيُّ
#      انتقالٍ لوراثةٍ ضمنيّةٍ قرارُ مصدرِ حقيقةٍ لا رقعةُ محرّك.
#      السطران 0/0 يبرهنان أنّ الحقلَ بقي على صفرِ نوعِه في الحالين،
#      والذيلُ (باني أصل/9) يبرهن أنّ «الأساس()» الصريحَ ما زال يعمل.
# ============================================================

صنف أصل
    رقم ع
    باني()
        اطبع_سطر("باني أصل")
        هذا.ع = 9
    نهاية
نهاية

صنف فارغ يرث أصل
نهاية

صنف ذو_طريقة يرث أصل
    دالة شغل()
        اطبع_سطر("شغل")
    نهاية
نهاية

متغير أ = فارغ()
اطبع_سطر(نص(أ.ع))
متغير ب = ذو_طريقة()
اطبع_سطر(نص(ب.ع))
ب.شغل()

صنف صريح يرث أصل
    باني()
        الأساس()
    نهاية
نهاية
متغير ج = صريح()
اطبع_سطر(نص(ج.ع))
