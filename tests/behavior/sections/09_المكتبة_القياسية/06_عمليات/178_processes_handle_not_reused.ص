# @description تطابق: المقبضُ معرّفٌ تصاعديٌّ لا وصفٌ خامّ — مقبضٌ مُغلَقٌ لا يعودُ صالحًا لأنّ النواةَ أعادت استعمالَ الوصف
# @expect_error: غير معروف
# @expect_error_compiled: غير معروف
# @priority P1

# ============================================================
# هذا الاختبارُ يحرسُ **قرارَ تصميمٍ في المصرِّف** لا ميزةً للمستخدم.
#
# أسهلُ تنفيذٍ للمقبضِ في المصرِّف كان: وسمُ النوعِ في البتّاتِ العليا
# ورقمُ الوصفِ في السفلى — بلا جدولٍ أصلًا. وهو يمرُّ كلَّ اختباراتِ
# ١٧٠–١٧٧ بلا استثناء. لكنّه يفترقُ عن المفسّرِ في نقطةٍ واحدةٍ بالضبط
# هي هذه: بعد `اغلق`، تُعيدُ النواةُ استعمالَ رقمِ الوصفِ لأوّلِ فتحٍ
# تالٍ، فيصيرُ المقبضُ **البائتُ** صالحًا من جديدٍ ويشيرُ إلى ملفٍّ آخر.
#
# فتكتبُ إلى الملفِّ الخطأ، أو تُغلقُ وصفًا يملكه جزءٌ آخرُ من البرنامج،
# **بلا خطأٍ ولا تحذير**. وفي المفسّرِ يرمي السطرُ نفسُه لأنّ المعرّفَ
# تصاعديٌّ لا يُعادُ استعمالُه أبدًا.
#
# فهو إذن اختبارُ تطابقٍ بالمعنى الدقيق: يفشلُ في المصرِّفِ وحدَه إن
# انحرف تنفيذُه عن المفسّر، ويمرُّ في المحرّكين معًا حين يتطابقان.
#
# ولا وسمَ منصّةٍ هنا، وهو مقصود: إعادةُ استعمالِ رقمِ الوصفِ ليست خُلُقًا
# لينكسيًّا بل خُلُقُ كلِّ جدولِ وصفاتٍ — و«‏CRT» على ويندوز تفعلُه كذلك.
# فالفخُّ الذي يحرسه هذا الاختبارُ منصوبٌ على المنصّاتِ الثلاث، ولا معنى
# لحراسته على واحدةٍ منها.
# ============================================================
استورد عمليات

متغير أول = افتح_وصفا("مقبض_معاد.tmp", "كتابة")
اغلق(أول)

# فتحٌ جديدٌ: النواةُ تُعطي رقمَ الوصفِ نفسَه الذي حُرِّر للتوّ
متغير ثان = افتح_وصفا("مقبض_معاد.tmp", "كتابة")

# والمقبضُ البائتُ يجب أن يبقى ميّتًا — لا أن يُحيِيَه إعادةُ استعمالِ الوصف
اغلق(أول)
