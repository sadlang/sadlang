# @description فك_تشفير — شعاع ثابت مُسجَّل مسبقًا (لا يعتمد على nonce عشوائي وقت التشغيل)؛ يكشف أي تغيير غير مقصود في خوارزمية SHA-256-CTR
# @expected: Sad
# @priority P1

# ============================================================
# شعاع ثابت مُولَّد مرة واحدة عبر: شفّر("Sad", "K")
#   ⇒ "d1aa9564d7caed55d84a5f" (8 بايت nonce + 3 بايت نصّ مشفَّر، hex)
# مُثبَّت هنا حرفيًّا (لا يُعاد توليده وقت التشغيل) ليكون شعاع تراجع
# (regression) يكشف أي تغيير في: ثوابت SHA-256، بنية nonce/عدّاد، أو
# ترتيب البايتات — بخلاف اختبار 151 الذي يعتمد على شفّر وقت التشغيل.
# ============================================================
استورد تأكيدات

اطبع_سطر(فك_تشفير("d1aa9564d7caed55d84a5f", "K"))
