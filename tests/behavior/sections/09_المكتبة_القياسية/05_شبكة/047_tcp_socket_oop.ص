# @description اختبار صنف مقبس_اتصال (TCP) الكائني من مكتبة شبكات.ص
# @expected صحيح\nصحيح\nصحيح\nخطأ\nصحيح\nصحيح\nخطأ
# @skip_compiler

استورد شبكات

استورد رياضيات

# ── دورة حياة أساسية ──────────────────────────────────────────────
متغير م = مقبس_اتصال()
اطبع_سطر(م.هل_صالح())     # صحيح — تم الإنشاء

# ── ربط بمنفذ عشوائي (0) والتحقق من المنفذ المُخصّص ────────────
م.ربط(0)
اطبع_سطر(م.هل_صالح())     # صحيح — بعد الربط
م.استمع(5)
اطبع_سطر(م.المنفذ() > 0)  # صحيح — منفذ عشوائي > 0
م.أغلق()
اطبع_سطر(م.هل_صالح())     # خطأ — بعد الإغلاق

# ── محاولة اتصال بمنفذ مغلق ─────────────────────────────────────
متغير عميل = مقبس_اتصال()
اطبع_سطر(عميل.هل_صالح())  # صحيح
عميل.حدد_مهلة_الاستقبال(200)
عميل.حدد_مهلة_الإرسال(200)
متغير نجح = عميل.اتصل("127.0.0.1"، 1)
اطبع_سطر(عميل.هل_صالح())  # صحيح — المقبس ما زال مفتوحاً حتى بعد فشل الاتصال
عميل.أغلق()
اطبع_سطر(عميل.هل_صالح())  # خطأ — بعد الإغلاق
