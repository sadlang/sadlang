# @description WebSocket — دورة حياة كاملة: خادم + عميل + بدء + إيقاف
# (AR) ⚠️ كانت أسطرُ المتوقَّعِ الأربعةُ مُسرَّبةً إلى الشيفرة. أُعيدت إلى التوجيه.
# @expected صحيح\nصحيح\n0.0\nصحيح\nخطأ
# @skip_compiler

استورد شبكات

متغير خ = خادم_ويبسوكت()
متغير ع = عميل_ويبسوكت()

# (AR) الحالة الأولية
اطبع_سطر(خ.هل_صالح() و ع.هل_صالح())

# ابدأ الخادم
متغير بدأ = خ.ابدأ(0، "127.0.0.1")
اطبع_سطر(بدأ)

# لا اتصالات بعد
اطبع_سطر(خ.عدد_الاتصالات())

# (AR) العميل غير متصل بهذا الخادم — هل_متصل = خطأ، لكن العميل صالح
اطبع_سطر(خ.يعمل())

# أوقف
خ.أوقف()
اطبع_سطر(خ.يعمل())
