# @description WebSocket — إنشاء خادم وفحص حالته الأولية
# (AR) ⚠️ كانت أسطرُ المتوقَّعِ الأربعةُ مُسرَّبةً إلى الشيفرةِ تحتَ التوجيه، فلم
#      يكن يُفحَصُ إلّا السطرُ الأوّل. أُعيدت إلى موضعِها.
# @expected صحيح\nخطأ\n0.0\n0.0\n0.0

استورد شبكات

متغير خ = خادم_ويبسوكت()

# (AR) صالح بعد الإنشاء
اطبع_سطر(خ.هل_صالح())

# لم يبدأ بعد
اطبع_سطر(خ.يعمل())

# لا اتصالات
اطبع_سطر(خ.عدد_الاتصالات())

# لا غرف
اطبع_سطر(خ.عدد_الغرف())

# المنفذ صفر قبل البدء
اطبع_سطر(خ.المنفذ())
