# @description عدة خوادم على منافذ مختلفة
# @expected صحيح\nصحيح\nصحيح\nصحيح

# ============================================================
# (AR) 🔑 رُفع `@skip_compiler` وحُذفت أسطرُ تسجيلِ المسارِ الثلاثة
#      (انظر شرحَ 072). موضوعُ البذرةِ **تعدّدُ الخوادمِ** لا تسجيلُ المسار،
#      والأسطرُ المحذوفةُ لا تطبعُ شيئًا فعقدُها الرباعيُّ باقٍ.
# (EN) @skip_compiler lifted and the three route lines removed (see 072). The
#      seed's subject is MULTIPLE SERVERS, not route registration, and the
#      removed lines print nothing.
# ============================================================

استورد شبكات

استورد شبكة

دالة معالج1()
    عين_نص_الرد("خادم 1")
نهاية
دالة معالج2()
    عين_نص_الرد("خادم 2")
نهاية
دالة معالج3()
    عين_نص_الرد("خادم 3")
نهاية

متغير خ1 = خادم_ويب(0)
متغير خ2 = خادم_ويب(0)
متغير خ3 = خادم_ويب(0)

اطبع_سطر(خ1.هل_صالح())
اطبع_سطر(خ2.هل_صالح())
اطبع_سطر(خ3.هل_صالح())

خ1.أزل()
خ2.أزل()
خ3.أزل()
اطبع_سطر(صحيح)
