# @description اختبار صنف خادم_ويب الكائني من مكتبة شبكات.ص
# @expected صحيح\nصحيح\nخطأ\nصحيح\nخطأ
# @skip_compiler

استورد شبكات

استورد شبكة

# ── إنشاء الخادم ──────────────────────────────────────────────────
متغير خ = خادم_ويب()
اطبع_سطر(خ.هل_صالح())     # صحيح

# ── تسجيل المسارات (لا تُلقي أخطاء، لا تحجب) ───────────────────
خ.عند_جلب("/مرحبا"، لامدا(طلب، رد)
    عين_نص_الرد(رد، "مرحبا!")
نهاية)

خ.عند_إرسال("/بيانات"، لامدا(طلب، رد)
    عين_رمز_الرد(رد، 201)
    عين_نص_الرد(رد، "تم الإنشاء")
نهاية)

خ.عند_استبدال("/مورد"، لامدا(طلب، رد)
    عين_نص_الرد(رد، "تم التحديث")
نهاية)

خ.عند_حذف("/مورد"، لامدا(طلب، رد)
    عين_نص_الرد(رد، "تم الحذف")
نهاية)

خ.اسمح_بالوصول("*")
اطبع_سطر(خ.هل_صالح())     # صحيح — بعد تسجيل المسارات

# ── إيقاف وإزالة بدون بدء (لا حاجة لـ ابدأ في الاختبار) ────────
خ.أوقف()
خ.أزل()
اطبع_سطر(خ.هل_صالح())     # خطأ — بعد أزل

# ── خادم ثانٍ مع باني مبدئي ────────────────────────────────────
متغير خ2 = خادم_ويب(0)
اطبع_سطر(خ2.هل_صالح())    # صحيح
خ2.أوقف()
خ2.أزل()
اطبع_سطر(خ2.هل_صالح())    # خطأ
