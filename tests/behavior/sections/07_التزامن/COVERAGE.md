# COVERAGE — 07_التزامن (21 ملفاً)

> جدول تدقيق التغطية (GR-01): كل ميزة في هذا القسم ↔ اختبار يغطّيها.
> ✅ مغطّى · ⚠️ جزئي · ❌ فجوة. حدِّثه ضمن DoD أي ميزة جديدة.
> المصدر: مسودة TEST-001 + الجرد الفعلي. **مخاطرة القسم: 9 — الأعلى (ADR-004)**.

| الميزة | الحالة | اختبار إيجابي | اختبار سلبي |
|---|---|---|---|
| خيوط خفيفة (أطلق) | ✅ | `01_خيوط_خفيفة/` (5) | — |
| القنوات (أرسل/استقبل/مهلة) | ✅ | `02_القنوات/` (7) | — |
| اختر (select) | ⚠️ | `03_اختر/` (1) | — |
| مجموعة الانتظار | ✅ | `04_مجموعة_انتظار/` (2) | — |
| القفل (mutex) | ✅ | `05_قفل/` (2) | — |
| المستقبل (future) | ✅ | `06_مستقبل/` (2) | — |
| خط أنابيب (قنوات متسلسلة) | ✅ | `07_خط_أنابيب/` (2) | — |
| اختبارات ضغط | ✅ | `08_ضغط/` (2) | — |
| **حتمية الخرج (وسوم)** | ⚠️ | تُطبَّق في TEST-003/T3 — `@unordered`/`@nondeterministic` | — |

## الفجوات المعروفة

- **بوّابة AC-03 مُستوفاة (2026-06-12):** 20 تشغيلاً متتالياً = نمط فشل واحد ثابت — **صفر رفرفة**.
  المصنّف (`--classify` ×5): 0 لا-حتمي. **لا حاجة لأي وسم `@unordered`/`@nondeterministic`**.
- **5 إخفاقات تكافؤ حقيقية ثابتة** (مفسر ≠ مترجم — ليست رفرفة، تحتاج قصة إصلاح بـ BF-08):
  `057_goroutine_basic`، `058_channels_buffered`، `089_goroutine_closure`،
  `091_waitgroup_goroutines`، `099_channel_cross_function_strings`.
- `اختر` بملف واحد — يحتاج: افتراضي، عدة قنوات جاهزة معاً، قناة مغلقة.
- التكرارات حُسمت (TEST-003): أُزيلت نسخة `01_أساسي/022` ونسخة `04_بمهلة/099` (مطابقة hash؛
  الأصل التاريخي محفوظ في `tests/_archive/dual_execution_legacy/`).
- لا اختبارات سلبية (إرسال لقناة مغلقة، deadlock مكشوف) — تُضاف في `_negative/`.
