# @description (S-TS-P4) ربط نوع Future: نوع(مستقبل())=«مستقبل»، والطرق (.عيّن/.احصل) تعمل (الكائن سليم).
# @expected NT:مستقبل\nGET:42
# @priority P1

# نوع() يتعرّف على المستقبل عبر النوع الموحَّد (getKind()=Future)
متغير م = مستقبل()
اطبع_سطر("NT:" + نوع(م))

# الطرق ما زالت تعمل (type_=OBJECT محفوظ، isObject سليم)
م.عيّن(42)
اطبع_سطر("GET:" + م.احصل())
