# @description بذرة — «أجّل» داخل دالّة مسمّاة ذات التقاطات: كان يُسقَط صامتًا في مسار الإغلاق (بلا تعليمة في الـIR ولا تشخيص). الدالّة نفسها تُنفّذ مؤجّلها في المستوى الأعلى وتُسقطه داخل «أطلق»، لأنّ العامّ يصير محلّيًّا مُلتقَطًا للإغلاق الخارجيّ فتتبدّل شعبة البناء
# @expected جسم\nتنظيف\nجسم2\nتنظيف2
# @priority P1

# ١) دالّة في المستوى الأعلى — «ق» عامّ فلا التقاط ⇒ مسار buildFunction
متغير ق = قناة(4)
دالة عمل()
    أجّل ق.أرسل("تنظيف")
    ق.أرسل("جسم")
نهاية
عمل()
اطبع_سطر(ق.استقبل())
اطبع_سطر(ق.استقبل())

# ٢) الدالّة نفسها داخل «أطلق» — «ق» صار محلّيًّا مُلتقَطًا ⇒ مسار الإغلاق
أطلق
    دالة عمل2()
        أجّل ق.أرسل("تنظيف2")
        ق.أرسل("جسم2")
    نهاية
    عمل2()
نهاية
انتظر_الكل()
اطبع_سطر(ق.استقبل())
اطبع_سطر(ق.استقبل())
