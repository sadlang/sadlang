# @description أنواعُ الخاصّيّةِ الأولى للمصانع — تكافؤُ المفسّرِ والمترجم. المفسّرُ يفحص **بنيةَ القيمة** (setIRPropertyFromValue) فلا يعرف أحجامًا: كلُّ عددٍ صحيحٍ عندَه صحيح. والمترجمُ كان يسأل عن `رقم`/`عشري`/`منطقي` وحدَها ويُسقِط ما عداها إلى SET_PROP_STR ⇒ `طبيعي` و`بايت` تُخزَّنانِ نصًّا فارغًا («قيمة: ""» بدل «قيمة: 4»)، و`لاشيء` تُمرَّر مؤشّرًا صفريًّا فتُخزَّن فارغةً بدل «لاشيء». والأخطرُ: القيمةُ التي لا يُحسَمُ نوعُها وقتَ الترجمة (`6 / 2`) كانت تُعَدُّ «شبيهةَ عنصر» فتصير ابنًا يُسقِطه وقتُ التشغيلِ صامتًا ⇒ عقدةٌ **بلا خاصّيّةٍ إطلاقًا**. الحسمُ بالمفتاحِ وحدَه لا يكفي («قيمة» في مصدرِ الحقيقةِ `عدد أو منطقيّ`)، فأوپكودُ SET_PROP_DYN يمرّرُ الوسمَ والحمولةَ ويحسمُ وقتُ التشغيلِ كالمفسّرِ حرفًا بحرف. تباعُدٌ صامتٌ بلا خطأٍ ولا تحذير — فرقٌ في البكسل وحدَه
# @expected ترقيم_صفحات(قيمة: 3)\nترقيم_صفحات(قيمة: 0)\nترقيم_صفحات(قيمة: 4)\nترقيم_صفحات(قيمة: 6)\nنسبة_عرض(نسبة: 2.5)\nمفتاح(قيمة: صحيح)\nخانة_اختيار(قيمة: خطأ)\nترقيم_صفحات(قيمة: 3)\nصندوق(نصف_قطر: 3)\nرقاقة(نص: "مسوّدة")\nرقاقة(نص: "مسـوّدة")\nترقيم_صفحات(قيمة: "لاشيء")
# @priority P1
استورد رسومات

# ── صحيحٌ افتراضيُّ الحجم (رقم): خطُّ الأساسِ الذي تُقاسُ عليه الأحجامُ المحدَّدة ──
رقم عدد_الصفحات = 3
طباعة_شجرة(ترقيم_صفحات(عدد_الصفحات))

# ── الصفرُ: يجاورُ حالةَ العدمِ حمولةً (بتّاتٌ صفريّة) فيفصلُهما الوسمُ وحدَه ──
طباعة_شجرة(ترقيم_صفحات(0))

# ── أنواعٌ عدديّةٌ محدَّدةُ الحجم: كانت تُخزَّن نصًّا فارغًا ──
طبيعي عدد_الصفحات_غير_الموقع = 4
طباعة_شجرة(ترقيم_صفحات(عدد_الصفحات_غير_الموقع))

طبيعي8 عدد_الصفحات_ثماني_البتات = 6
طباعة_شجرة(ترقيم_صفحات(عدد_الصفحات_ثماني_البتات))

# ── عشريّ ──
عشري نسبة_عرض_الشاشة = 2.5
طباعة_شجرة(نسبة_عرض(نسبة_عرض_الشاشة))

# ── منطقيٌّ خاصّيّةً أولى: «قيمة» في مصدرِ الحقيقةِ «عدد أو منطقيّ» فالوسمُ يفصلُهما ──
طباعة_شجرة(مفتاح(صحيح))

منطقي خانة_الاختيار_مفعلة = خطأ
طباعة_شجرة(خانة_اختيار(خانة_الاختيار_مفعلة))

# ── نوعٌ يُحسَمُ زمنَ التشغيل: كانت العقدةُ تخرج بلا خاصّيّةٍ أصلًا ──
طباعة_شجرة(ترقيم_صفحات(6 / 2))

# ── المعدّلُ بقيمةٍ ديناميّة: حارسٌ ضدّ كسرِ الجارِ المُصلَحِ سابقًا ──
طباعة_شجرة(صندوق().نصف_قطر(6 / 2))

# ── نصٌّ ثابت: يتكرّرُ عن قصدٍ بعد فتحِ SET_PROP_DYN — حارسٌ ضدّ إصلاحٍ يكسرُ الجارَ السليم ──
طباعة_شجرة(رقاقة("مسوّدة"))

# ── نصٌّ يُحسَمُ زمنَ التشغيل: الوسمُ يمرُّ لكلِّ الأنواعِ لا للعدديِّ وحدَه ──
طباعة_شجرة(رقاقة("مسـ" + "وّدة"))

# ── العدمُ خاصّيّةً أولى: كان مؤشّرًا صفريًّا يبلغُ مسارَ النصِّ فيُطبَعُ «قيمة: ""»؛ الآن يُوسَمُ عدمًا ويُنصَّصُ «لاشيء» كالمفسّرِ حرفًا بحرف ──
طباعة_شجرة(ترقيم_صفحات(لاشيء))
