# @description الخاصّيّةُ الأولى للمصانعِ المسجَّلةِ من مصدرِ الحقيقة — تكافؤُ المفسّرِ والمترجم. المصانعُ الـ٤٦ ذاتُ `primary_prop` تكتب وسيطَها الأوّلَ خاصّيّةً لا ابنًا. المفسّرُ يفحص **بنيةَ القيمة** (`isWidgetLike`: ودجت أو مصفوفة) فالصحيحُ عندَه قيمةٌ دائمًا؛ والمترجمُ كان يعدُّ الصحيحَ «شبيهَ عنصر» فيُصدِر `ADD_CHILD(w, 3)` ويُسقطه `sad_ui_runtime` صامتًا (`isRegisteredWidget(3)` كاذب) ⇒ الخاصّيّةُ لا تُضبَط والعنصرُ يُرسَم بالافتراضيّ. عيبٌ لا يُنتج خطأً ولا تحذيرًا — فرقٌ في البكسل وحدَه. يغطّي الاختبارُ النوعَينِ معًا (صحيحٌ ونصّ) لأنّ مسارَ النصِّ كان سليمًا فيفصلُ الانحدارَ عن الكسرِ العامّ
# @expected ترقيم_صفحات(قيمة: 3)\nصندوق_مقيد(أدنى_عرض: 100)\nنسبة_عرض(نسبة: 2)\nرقاقة(نص: "مسوّدة")\nقائمة_ضخمة(عنوان: "أقسام")
# @priority P1
استورد رسومات

# ── صحيحٌ أوّلًا: المسارُ الذي كان يتباعد ──
طباعة_شجرة(ترقيم_صفحات(3))
طباعة_شجرة(صندوق_مقيد(100))
طباعة_شجرة(نسبة_عرض(2))

# ── نصٌّ أوّلًا: المسارُ الذي كان سليمًا — حارسٌ ضدّ إصلاحٍ يكسرُ الجارَ ──
طباعة_شجرة(رقاقة("مسوّدة"))
طباعة_شجرة(قائمة_ضخمة("أقسام"))
