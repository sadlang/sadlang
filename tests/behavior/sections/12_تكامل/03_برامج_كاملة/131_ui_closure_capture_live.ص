# @description قفل انحدار (مترجم) — استدعاء إغلاقٍ حيًّا عبر مسار void* في وقت التشغيل: بانِي صفحةٍ لامدا يلتقط معاملًا يُخزَّن عبر انتقل() ثمّ يُستدعى ببيئته الملتقطة عند الصفحة_الحالية() بعد فناء نطاق التعريف — يثبت أنّ بيئة الإغلاق تعبر جسر sad_* (fn/env عبر thunk) وتعود سليمة، وهو نفس بروتوكول ردود نداء sad_button/sad_add_event
# @expected زر(عنوان: "صفحة_ملتقطة")
# @priority P1
# ملاحظة: كان هنا @skip_interpreter لعيبٍ في المفسّر (callUserFunction لا يحقن
# بيئة الالتقاط ⇒ SEM001 عند إعادة البناء المؤجَّل) — أُصلح الجذر في
# interpreter/src/core/interpreter_core.cpp (حقن getCaptures + إعادة كتابتها،
# تكافؤًا مع executeUserFunctionBody) فرُفع التخطّي والاختبار ثنائيّ المحرّك.
استورد رسومات

دالة رحلة(اسم_الصفحة)
    انتقل(لامدا() => زر(اسم_الصفحة))
نهاية

رحلة("صفحة_ملتقطة")
طباعة_شجرة(الصفحة_الحالية())
