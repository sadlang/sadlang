# @rule gr.decl.import
# @description وحدةٌ وسيطةٌ تستعير «الحد_الأقصى» باسم «السقف» وتستعمله
# @skip_compiler ملفٌّ مساعدٌ لا يُشغَّل مستقلًّا
# @skip_interpreter ملفٌّ مساعدٌ لا يُشغَّل مستقلًّا

من وحدة_قيم استورد الحد_الأقصى كـ السقف

صدّر دالة اقرأ_السقف()
    ارجع السقف
نهاية
