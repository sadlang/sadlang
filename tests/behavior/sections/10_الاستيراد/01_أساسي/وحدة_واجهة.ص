# @skip_compiler وحدةُ واجهةٍ مساعِدة لاختبار 066 — لا تُشغَّل مباشرةً
# @skip_interpreter وحدةُ واجهةٍ مساعِدة
#
# (AR) وحدةٌ لا تُعرّف شيئًا، بل تُعيد بثَّ رموز «وحدة_مستعار» — وهي الشكلُ
#      النمطيُّ لواجهة مكتبةٍ تُخفي تقسيمَها الداخليَّ عن مستعمِلها.

صدّر * من وحدة_مستعار
