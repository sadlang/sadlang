# @skip_compiler وحدة مساعِدة لاختبار 072 — لا تُشغَّل مباشرةً
# @skip_interpreter وحدة مساعِدة
#
# (AR) تُصدّر «أعط» بالاسم نفسِه الذي تُصدّره «وحدة_مصدر_باء» — تصادمٌ مقصود.

صدّر دالة أعط()
    ارجع 1
نهاية
