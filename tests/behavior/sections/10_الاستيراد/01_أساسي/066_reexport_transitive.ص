# @description إعادة التصدير العابرة — استيرادُ رمزٍ من وحدةِ واجهةٍ تُعيد بثَّه عن وحدةٍ أخرى
# @expected مجموع:7
# @priority P1
# @skip_compiler ISSUE-089 — المصرّف لا يُعيد بثَّ الرمز عبر الوحدة الوسيطة (يقعُ على مدمَجٍ يصادف الاسم: «الدالة 'جمع' تنتمي إلى وحدة 'رياضيات' ولم تُستورَد»)

# (AR) «وحدة_واجهة» لا تُعرّف «جمع» بل تُعيد تصديرَها عن «وحدة_مستعار». المفسّرُ
#      يعبُر الوسيطةَ فيجد الرمز؛ والمصرّفُ يقف عندها. تباعُدٌ مقيسٌ حيًّا ومُوثَّقٌ
#      في ISSUE-089 — الاستيرادُ المباشرُ من «وحدة_مستعار» يُصرَّف سليمًا (064/065)،
#      فالعطبُ محصورٌ في العبور لا في مسار الوحدات.
من وحدة_واجهة استورد جمع

اطبع_سطر("مجموع:" + جمع(3، 4))
