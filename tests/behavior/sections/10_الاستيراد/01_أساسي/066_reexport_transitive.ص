# @description إعادة التصدير العابرة — استيرادُ رمزٍ من وحدةِ واجهةٍ تُعيد بثَّه عن وحدةٍ أخرى
# @expected مجموع:7
# @priority P1

# (AR) «وحدة_واجهة» لا تُعرّف «جمع» بل تُعيد تصديرَها عن «وحدة_مستعار». كان
#      المفسّرُ يعبُر الوسيطةَ فيجد الرمزَ والمصرّفُ يقف عندها فيقع على مدمَجٍ
#      يصادف الاسم (ISSUE-089). صار الطوران يُتبعان `ReExportStmt` إلى وحدةِ
#      مصدرها، فالمحرّكان متطابقان. كاشفُ انحدارٍ دائم.
من وحدة_واجهة استورد جمع

اطبع_سطر("مجموع:" + جمع(3، 4))
