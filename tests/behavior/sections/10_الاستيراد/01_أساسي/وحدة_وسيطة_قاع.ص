# @rule gr.decl.import
# @description وحدةٌ شقيقةٌ تستعير «الحد_الأدنى» بالاسم «السقف» نفسِه وتستعمله
# @skip_compiler ملفٌّ مساعدٌ لا يُشغَّل مستقلًّا
# @skip_interpreter ملفٌّ مساعدٌ لا يُشغَّل مستقلًّا

من وحدة_قيم_دنيا استورد الحد_الأدنى كـ السقف

صدّر دالة اقرأ_القاع()
    ارجع السقف
نهاية
