# @description محلّيٌّ يصادف اسمَ عامٍّ نوعُه «أي» يبقى محسوسًا: كان اسمُ المتغيّرِ وحدَه يُسقِط الترجمة (sext %SadDyn) لأنّ مجموعةَ الخاناتِ الديناميّةِ مفتاحُها الاسمُ المجرَّدُ على مستوى الوحدةِ كلِّها
# @expected A\n7\n65\nمرحبا\n=== انتهى ===
# @priority P0

استورد خرائط

# (AR) العامُّ نوعُه «أي» لأنّ مُهيِّئَه نداءٌ يُرجع «أي» — فتدخل خانتُه
#      `dynGlobalSlots_` وهي **مجموعةٌ على مستوى الوحدةِ مفتاحُها الاسمُ المجرَّد**.
دالة أي احضر_ديناميا()
    ارجع 7
نهاية

متغير القيمة = احضر_ديناميا()

# (AR) والمحلّيُّ هنا يصادفُ الاسمَ نفسَه وهو صحيحٌ محسوس. كانت الخلفيّةُ تراه
#      ديناميًّا لمجرّدِ المصادفة، فتُصدِر `sext %SadDyn … to i64` ويسقطُ
#      `verifyModule` بـ«علّةِ مترجمٍ داخليّة». وإعادةُ تسميةِ المتغيّرِ وحدَها
#      كانت تُصلِح الترجمة — فالاسمُ هو ما يقرّر، وهذا ما يقيسه هذا الاختبار.
دالة نص حرف_من_محلي()
    متغير القيمة = 65
    ارجع حرف_من_رمز(القيمة)
نهاية

# (AR) ومعامِلٌ باسمِ العامِّ كذلك: المعاملاتُ تُصرَّح محلّيًّا فتُظلِّلُ العامَّ.
دالة رقم ضاعف(رقم القيمة)
    ارجع القيمة
نهاية

اطبع_سطر(حرف_من_محلي())

# (AR) والعامُّ نفسُه يبقى ديناميًّا سليمًا بعد كلِّ ذلك — الحارسُ يقيس الجهتَين:
#      ألّا يُعدي العامُّ المحلّيَّ، وألّا يفقدَ العامُّ ديناميّتَه بالإصلاح.
اطبع_سطر(القيمة)
اطبع_سطر(ضاعف(65))

دالة نص نص_محلي()
    متغير القيمة = "مرحبا"
    ارجع القيمة
نهاية

اطبع_سطر(نص_محلي())
اطبع_سطر("=== انتهى ===")
