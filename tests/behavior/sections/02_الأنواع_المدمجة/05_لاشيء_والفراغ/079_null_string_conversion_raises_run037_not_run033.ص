# @description تحويلُ «عشري» على عدمٍ يرفعُ RUN037 — رمزًا يخصُّه لا رمزَ الطرائق.
# @priority P1
# @expect_error الدالة المدمجة 'عشري' استُدعيت بوسائط ناقصة أو من نوع خاطئ
# @expect_error_compiled الدالة المدمجة 'عشري' استُدعيت بوسائط ناقصة أو من نوع خاطئ

# ============================================================
# (AR) 🔑 منفذُ التحويلِ ليس منفذَ طريقة، والمفسّرُ يفرّق بينهما مقيسًا:
#      «س.طول()» ⇒ RUN033، و«عشري(س)» ⇒ RUN037. ولو رُفِعا برمزٍ واحدٍ لصار
#      المترجّمُ يرفعُ — وهو الصوابُ — برمزٍ يخالف المفسّر، وهو تباعدٌ أدقُّ من
#      أن يُرى بالعين لكنّ الحارسَ يراه.
#
# (AR) وقبل هذا كان يطبع «0.0»: `atof` على لفظِ «لاشيء» ⇒ صفر. جوابٌ كاذبٌ
#      معقولُ الشكلِ حيث يرفعُ المفسّر.
# (EN) Conversion ports raise RUN037, not RUN033; previously atof answered 0.0.
# ============================================================

متغير نص س = لاشيء
اطبع_سطر(عشري(س))
