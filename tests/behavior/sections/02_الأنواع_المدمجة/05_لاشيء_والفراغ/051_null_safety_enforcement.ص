# @description (S-TS-P9) أمان null: نوع اختياري يقبل لاشيء، ومتغير مُستنتَج يقبل لاشيء (محفوظ)، والوصول عبر ??.
# @expected OPT:عدم\nINF:عدم\nCOAL:42\nKEEP:9
# @priority P1
# @skip_compiler (S-TS-P9) فرض أمان null في طبقة المفسّر عبر isNull؛ codegen المترجم لا يميّز null بعد (نفس فجوة P1/P4). المفسّر صحيح.

# ============================================================
# نوع اختياري صريح يقبل لاشيء (مسموح — لا تحذير)
# ============================================================
متغير اخـ: رقم? = لاشيء
اطبع_سطر("OPT:" + نوع(اخـ))

# ============================================================
# متغير مُستنتَج = لاشيء (النمط السائد المحفوظ — لا تحذير، لا انحدار)
# ============================================================
متغير مس = لاشيء
اطبع_سطر("INF:" + نوع(مس))

# ============================================================
# الوصول الآمن عبر اندماج فارغ ?? على متغير اختياري فارغ
# ============================================================
متغير ق: رقم? = لاشيء
اطبع_سطر("COAL:" + (ق ?? 42))

# قيمة فعلية في اختياري تبقى
متغير ك: رقم? = 9
اطبع_سطر("KEEP:" + (ك ?? 0))

# ملاحظة: `رقم س = لاشيء` (نوع صريح غير اختياري) يُطلِق تحذير أمان null
#         على stderr: «إسناد 'لاشيء' لمتغير من نوع غير اختياري 'رقم'. اجعله 'رقم؟'».
