# @description «لكل» على غيابٍ موسومٍ (فراغ) ⇒ RUN055 بعبارةِ المفسّرِ الحرفيّةِ وخروجٍ ١ — لا SIGSEGV ولا نصَّ «ليست مصفوفة».
# @priority P1
# @expect_error النوع 'غير معروف/unknown' غير قابل للتكرار
# @expect_error_compiled النوع 'غير معروف/unknown' غير قابل للتكرار

# (AR) [RFC عقد الغياب — سطح «لكل»] قِيس 2026-08-23: كان المترجَمُ يرفع
#      نصًّا بلا رمزٍ «فهرسةٌ بعددٍ ... ليست مصفوفة» — سطحٌ كاذب. الحارسُ
#      kRuntimeForeachAbsenceGuard يُبَثُّ قبل آلةِ التكرار ويطابق RUN055.

متغير خ = {"أ": 1}
متغير غ = خ["غائب"]
لكل م في غ
    اطبع_سطر(م)
نهاية
