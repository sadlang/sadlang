# @expected ()\nخالي\nخطأ\n()\nخالي\n()\nخالي\n9\nرقم
# @priority P0

# ============================================================
# (AR) 102 — نداءُ دالّةٍ بلا نوعِ إرجاعٍ يُنتِجُ **قيمةَ الوحدة** «()».
#
#   🔑 وهذه البذرةُ **أُعيدَ إرساؤها** (2026-09-07)، وكانت تُثبِّتُ العقدَ
#      المعاكس: أنّ الخانةَ تحملُ **غيابًا** فتُطبَعُ «لاشيء» و`نوع()`
#      يقولُ «فراغ» و`== لاشيء` **صادقة**. وذلك العقدُ زالَ بزوالِ «فراغ»
#      من اللغة: نداءُ دالّةٍ لا تُصرِّحُ إرجاعًا يُرجِعُ اليومَ قيمةَ
#      الوحدةِ — **قيمةً حقيقيّةً واحدةً**، لا لا‑قيمة. فلم تُحذَفْ
#      البذرةُ ولم تُوسَمْ بتخطٍّ: أُعيدَ إرساؤها على القاعدةِ الجديدة.
#
#   والمرسى قاعدةٌ في مصدرِ الحقيقةِ لا مُخرَجُ مُنفِّذ:
#     • `language-truth/types.yaml ⇒ type.unit` (الاسمُ «خالي»)
#     • `language-truth/backend/value_repr.yaml ⇒ kUnitDisplay = "()"`
#
#   وتقيسُ أربعةَ مواضعَ كانت **تفترقُ** قبلَ الإصلاح: المستوى الأعلى،
#   وإعادةُ الإسنادِ فوقَ خانةٍ رقميّة، وداخلُ دالّة، ثمّ شاهدٌ سالبٌ
#   يُثبِتُ أنّ الخانةَ **تشفى**: قيمةٌ رقميّةٌ بعدَها تعودُ بنوعِها.
#   🔑 وإعادةُ الإسنادِ خاصّةً كانت تنهارُ داخليًّا بـINT007 حتّى
#      2026-09-07: القيمةُ تُخزَّنُ خامًّا في خانةٍ ديناميّةٍ بلا وسم.
# ============================================================

# (AR) دالّةٌ بلا نوعِ إرجاعٍ وبلا `ارجع` — عائدُها قيمةُ الوحدة
دالة فراغية()
نهاية

# ① خانةُ المستوى الأعلى — والوحدةُ ليست «لاشيء»
متغير س = فراغية()
اطبع_سطر(س)
اطبع_سطر(نوع(س))
اطبع_سطر(س == لاشيء)

# ② إعادةُ الإسنادِ فوقَ خانةٍ رقميّة
متغير ع = 1
ع = فراغية()
اطبع_سطر(ع)
اطبع_سطر(نوع(ع))

# ③ الخانةُ المحلّيّةُ داخلَ دالّة — النطاقانِ يتّفقان
دالة عمل()
	متغير م = فراغية()
	اطبع_سطر(م)
	اطبع_سطر(نوع(م))
نهاية
عمل()

# ④ شاهدٌ سالب: القيمةُ الرقميّةُ بعدَ الوحدةِ تعودُ بنوعِها
ع = 9
اطبع_سطر(ع)
اطبع_سطر(نوع(ع))
