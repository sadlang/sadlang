# @description الوسطُ المفقودُ فراغًا يرفعُ SEM011 مرمَّزًا في المحرّكَين: خ["غائب"]["عميق"].
# @requires: اطبع_سطر, خرائط
# @priority P0
# @expect_error النوع 'VOID' لا يدعم الفهرسة []
# @expect_error_compiled النوع 'VOID' لا يدعم الفهرسة []

# ============================================================
# (AR) 🔑 المقيسُ قبل السدّ (٢٠٢٦-٠٨-٢٢): المحرّكانِ يفشلانِ بخروجٍ ١ لكن
#      بهويّتَين مختلفتَين — المفسّرُ يرفعُ **SEM011** المرمَّزَ من الكتالوج
#      («النوع 'VOID' لا يدعم الفهرسة []») بينما يطبعُ المترجَمُ سطرًا بلا
#      رمزٍ أصلًا: «طريقةُ خريطةٍ طُبِّقت على قيمةٍ ليست خريطة». فأداةٌ
#      تُوزِّعُ على رمزِ الخطأ ترى محرّكَين مختلفَين للغةٍ واحدة.
#
#      السدُّ في فرعِ الفشلِ لحارسِ `normalizeMapPtr`: الوسمُ يُفرَّق —
#      غيابٌ (فراغ/عدم) ⇒ رفعُ SEM011 من الكتالوجِ نفسِه الذي يقرأ منه
#      المفسّرُ، واسمُ النوعِ من `Value::getTypeName()` عينِها (VOID/NULL)
#      لا يُهجَّأ في المترجم؛ وسائرُ الأوسامِ المخالفةِ (رقمٌ مكانَ خريطةٍ…)
#      تبقى على رسالةِ عدمِ التطابقِ العامّة.
#
# (AR) ⚠️ حدٌّ معلَنٌ باقٍ: المفسّرُ يسجّلُ الخطأَ **ويُكمِلُ** (يطبعُ
#      «لاشيء» قبلَ التقرير) بينما يُنهي المترجَمُ التنفيذَ عندَ الرفعِ —
#      نموذجا إبلاغٍ مختلفان، والموحَّدُ هنا الرمزُ والنصُّ والخروج ١.
# ============================================================

استورد خرائط

متغير خ = خريطة()
خ["أ"] = 1
اطبع_سطر(خ["غائب"]["عميق"])
