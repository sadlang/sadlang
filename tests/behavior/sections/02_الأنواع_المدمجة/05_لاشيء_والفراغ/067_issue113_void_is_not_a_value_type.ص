# @description (ISSUE-113) «فراغ» نوعُ إرجاعٍ لا نوعُ خانة: التصريحُ به يُرفَض بـSEM040 في المحرّكَين، لا ينهار المترجّم ولا يمرّ صامتًا.
# @priority P0
# @expect_error: SEM040
# @expect_compile_error

# ============================================================
# (AR) لماذا وُلد هذا الاختبار — الشطرُ الثاني من ISSUE-113:
#      لمّا فُصل «عدم» عن «فراغ» بقي الانهيارُ قائمًا على «فراغ» صراحةً،
#      وهو عيبٌ **مستقلٌّ كشفه الفصل** لا بقيّةٌ منه:
#        · المترجّم كان يسقط بـ«Cannot create a null constant of that
#          type! UNREACHABLE» في classes_vtables_ops.cpp — لأنّ Void
#          يُنزَل إلى نوعِ void في LLVM ولا ثابتَ صفريَّ له.
#        · والمفسّرُ كان **يقبله** ويحذّر تحذيرًا كاذبًا ⇒ تباعُدُ محرّكَين.
#      والعلاجُ لم يكن ترقيعَ موضعِ الانهيار: ترقيعُه يُسكِت الأثرَ ويُبقي
#      القبولَ. مصدرُ الحقيقةِ يعرّف «فراغ» بأنّه «نوعُ الإرجاعِ الفارغ — لا
#      قيمة»، فخانةٌ تحمله تناقضٌ في التعريف. فالرفضُ يقع في **المحلّلِ
#      المشترك**، ويتّفق المحرّكان بالبناءِ لا بالمصادفة.
#
# (EN) ISSUE-113 part two: with Null separated, the crash remained on Void
#      itself. Rejected in the shared parser, so both engines agree by
#      construction rather than by coincidence.
#
# (AR) ⚠️ وهذا الاختبارُ **سالبٌ مزدوج** عمدًا: التشخيصُ الجديدُ يُكتَب على
#      stderr، و`runner.py` لا يقرأ stderr عند خروجٍ صفريّ — فاختبارٌ موجبٌ
#      يطبع ويقارن المخرَجَ لا يحرس التشخيصَ أصلًا. القفلُ هنا على **الرفضِ
#      ورمزِه** معًا، لا على أنّ شيئًا لم ينهر.
# ============================================================

متغير فراغ س = لاشيء
اطبع_سطر("لا يجب أن يبلغ هذا السطر")
