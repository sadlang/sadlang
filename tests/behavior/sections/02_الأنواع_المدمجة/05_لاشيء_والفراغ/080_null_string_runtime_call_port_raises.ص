# @description المستقبِلُ العدميُّ في منفذٍ يهبطُ **نداءَ** زمنِ تشغيلٍ لا أوپكودًا — يرفعُ كغيرِه.
# @priority P0
# @expect_error نوع المعامل 'NULL' غير مدعوم في العملية '.عكس()'
# @expect_error_compiled نوع المعامل 'NULL' غير مدعوم في العملية '.عكس()'

# (AR) «عكس» و«كرر» و«حرف_عند» لا تهبطُ إلى أوپكودٍ في الخلفيّة، بل إلى نداءِ
#      دالّةٍ في زمنِ التشغيل. وبابُ مؤشّرِ النصِّ كان يحرسُ بواعثَ الأوپكوداتِ
#      وحدَها، فكان هذا السطرُ **ينهارُ انهيارَ تجزئة** بينما يرفعُ المفسّرُ RUN033.
#      ولم يكشفه شيءٌ من ٠٦٦–٠٧٩: كلُّها تمسُّ منافذَ أوپكود. كشفه حارسُ تكافؤِ
#      المستقبِلِ العدميّ الذي يقرأ قائمةَ المنافذِ من مصدرِ الحقيقةِ لا من قائمةٍ
#      مكتوبةٍ يدًا — وهذا هو الفرقُ الذي جعله يرى ما لم يرَه اختبارٌ مكتوب.
# (EN) reverse/repeat/char_at lower to a runtime CALL, not a backend opcode, so they
#      bypassed the string-pointer door and segfaulted on a null receiver. Found by the
#      SoT-driven null-receiver parity guard, not by any hand-written test.

متغير نص س = لاشيء
اطبع_سطر(س.عكس())
