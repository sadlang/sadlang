# @description المستقبِلُ العدميُّ يرفعُ RUN033 في عائلةِ البحثِ كما في عائلةِ الطول — لا يختلقُ جوابًا.
# @priority P0
# @expect_error نوع المعامل 'NULL' غير مدعوم في العملية '.يحتوي()'
# @expect_error_compiled نوع المعامل 'NULL' غير مدعوم في العملية '.يحتوي()'

# ============================================================
# (AR) 🔑 هذا الاختبارُ وُلِد من نقضٍ: أوّلُ إصلاحٍ لـ«النصِّ العدميّ» جعل
#      `normalizeStringPtr` غلافًا على بابِ **العرض**، فصار هذا البرنامجُ نفسُه
#      يطبع «صحيح» — لأنّ الحارسَ أُبدل بلفظِ «لاشيء» ثمّ بُحث فيه عن «لا» فوُجد.
#      أي أنّ الإصلاحَ حوّل انهيارًا **مرئيًّا** إلى جوابٍ كاذبٍ لا يُرى.
#
# (AR) والاختبارُ 076 لم يكشفه لأنّه يقف عند «.طول()» وحدَها ويخرج قبل غيرها.
#      فالحارسُ يجب أن يمسّ **عائلةً أخرى** لا منفذًا آخرَ من العائلةِ نفسِها.
# (EN) Born from a regression: routing operation ports through the display door
#      made this program answer «صحيح» by searching the null word itself.
# ============================================================

متغير نص س = لاشيء
اطبع_سطر(س.يحتوي("لا"))
