# @description الصفةُ «عدمي» تبلغ موضعَ المعامِلِ وموضعَ الإرجاع — بوّابةٌ واحدةٌ في لاحقةِ النوعِ لا سبعُ بوّاباتٍ متفرّقة.
# @priority P1

# ============================================================
# (AR) لماذا اختبارٌ ثالث: وضعُ الميزةِ في لاحقةِ النوعِ يدّعي بلوغَ كلِّ موضعِ
#      نوع. والادّعاءُ لا يُصدَّق بالبناء: المعامِلُ يسلك مسارًا آخرَ تمامًا
#      (سبعةُ مواضعِ `parseType()` منفصلة)، والإرجاعُ ثالثًا. فيُقاس الموضعان.
# (EN) Placing the feature on the type suffix claims it reaches every type
#      position — parameters and return types take entirely different paths.
# ============================================================

دالة نص عدمي جد(رقم عدمي مفتاح)
    إذا (مفتاح == لاشيء)
        ارجع لاشيء
    نهاية
    ارجع "وجد"
نهاية

اطبع_سطر(جد(لاشيء))
اطبع_سطر(جد(3))
