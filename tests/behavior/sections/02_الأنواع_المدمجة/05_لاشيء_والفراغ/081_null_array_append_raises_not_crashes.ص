# @description الإلحاقُ إلى مصفوفةٍ عدمٍ يرفعُ RUN033 في المحرّكَين — لا ينهار.
# @priority P0
# @expect_error نوع المعامل 'VOID' غير مدعوم في العملية '.أضف()'
# @expect_error_compiled نوع المعامل 'VOID' غير مدعوم في العملية '.أضف()'

# ============================================================
# (AR) 🔑 مسارُ **الكتابةِ** يرفض، ومسارُ العرضِ يُظهِر عدمًا — والخلطُ بينهما
#      يُبدِّل عطبًا يُرى بعطبٍ يُصدَّق.
#
#      قِيس قبلَ هذه البذرة (2026-08-16): المفسّرُ يرفع RUN033 برمزِ خروجٍ ١،
#      والمترجّمُ **يبني برمزِ خروجٍ صفرٍ ثمّ ينهار `rc=139`** بلا حرفٍ واحدٍ
#      من التشخيص. وهو أسوأُ مخرَجٍ ممكن: بناءٌ ناجحٌ لبرنامجٍ يسقط.
#
# (AR) ولم يُبدَّل الانهيارُ بـ«لاشيء» وإن كان أهونَ وأسرعَ إلى رقمٍ أجمل:
#      الإلحاقُ إلى عدمٍ **لا جوابَ صحيحًا له**، وطبعُ لفظٍ معقولٍ مكانَه
#      يجعل البرنامجَ يمضي على كذبة. البذرةُ ٠٧٤ تحرس بابَ العرضِ، وهذه
#      تحرس بابَ العمليّة، ولا تُغني إحداهما عن الأخرى.
#
# (AR) ⚠️ وفخُّ الطبقةِ هنا مقيسٌ لا مُتخيَّل: أوّلُ حارسٍ وُضِع على المعامل
#      **قبل** `normalizeArrayPtr`، فصُرِّف وبُني وظهر في المُخرَجِ — ولم
#      يُفعَّل مرّةً واحدة. لأنّ المعامَل قبلَ التطبيعِ **عنوانُ الخانة**
#      (`leaq "س"(%rip)`) وهو ليس صفرًا أبدًا، والتطبيعُ هو ما يُحمِّل
#      المحتوى. 🔑 **الطبقةُ تُقرأ من المُخرَجِ لا تُستنتَج من الاسم.**
# ============================================================

مصفوفة س
س.أضف(1)
اطبع_سطر("لا ينبغي أن يُطبع هذا السطر")
