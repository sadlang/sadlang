# @description مقارنةُ حرفِ حلقةِ «لكل» على نصٍّ موسومٍ بنصٍّ حرفيٍّ ⇒ تعملُ كالمفسّرِ — متغيّرُ الحلقةِ على المصدرِ المطبَّعِ موسومٌ («أي») لا «رقم» (كان `إذا (م == "ل")` يفشلُ **ترجمةً**، قِيس 2026-08-23).
# @expected وجدت\nتم
# @priority P1

متغير خ = {"أ": "سلم"}
متغير ع = خ.احصل("أ")
لكل م في ع
    إذا (م == "ل")
        اطبع_سطر("وجدت")
    نهاية
نهاية
اطبع_سطر("تم")
