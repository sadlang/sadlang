# @description فراغٌ يعبرُ خانةَ معاملٍ «رقم» ثمّ يُستعمَلُ حسابيًّا ⇒ RUN053 وخروجٌ ١ (عقدُ المفسّر).
# @requires: اطبع_سطر, خرائط
# @priority P1
# @expect_error العملية 'حسابية / arithmetic' تتطلّب قيمة رقمية
# @expect_error_compiled العملية 'حسابية / arithmetic' تتطلّب قيمة رقمية

# ============================================================
# (AR) 🟢 كانت حمراءَ معلنةً على المترجَمِ (٢٠٢٦-٠٨-٢٢) واخضرّت في
#      ٢٠٢٦-٠٨-٢٣ بموجةِ **وسمِ حدِّ المعاملِ المصنَّف**، فأُعيدت من
#      `_regression/` إلى قسمِها وحُذف صفُّها من DECLARED_REDS.tsv
#      (سجلُّ التحديثاتِ [٦]) وفاءً بنصِّها.
#
#      العطبُ المقيسُ قبلَ السدّ: المترجَمُ كان **يفكُّ الفراغَ صفرًا**
#      عندَ حدِّ المعاملِ «رقم» (i64 بلا وسم) ⇒ `س * 2` حسابٌ ساكنٌ لا
#      يمرُّ ببابِ RUN053 ⇒ يطبعُ 0 ويخرجُ 0 — قناعُ الصفرِ خلفَ حدِّ
#      المعامل. والمفسّرُ يرفع RUN053 بخروجٍ ١.
#
#      والسدُّ من طبقةِ **الاستنتاجِ** لا رقعةَ موقعِ نداء: ذراعُ
#      IndexExpr في `inferExprType` تُجيب «أي» للقراءةِ بالقوسِ (الجوابُ
#      يتبع التمثيلَ المبعوث — العنصرُ يُقرأ موسومًا زمنَ التشغيل)،
#      فيرقّي موقعُ النداءِ المعاملَ المصنَّفَ «رقم» إلى «أي» (بوّابةُ
#      الترقيةِ القائمةِ في sir_builder_functions) ويعبرُ **الوسمُ**
#      الحدَّ — والحسمُ عندَ الاستهلاكِ لا عندَ النداء: الحسابُ يمرُّ
#      ببابِ dynBinOp فيرفعُ RUN053، والعرضُ يطبعُ «لاشيء» — كلاهما
#      مطابقٌ للمفسّرِ مقيسًا (وهو ما نصّت البذرةُ أنّ أيَّ حسمٍ عندَ
#      موقعِ النداءِ يكذبُ في أحدِ اتّجاهَيه).
# (EN) Was a declared red on the compiled path; greened 2026-08-23 by the
#      typed-param-boundary TAG wave: the IndexExpr arm in inferExprType
#      answers Any for bracket reads (the answer follows the emitted
#      representation), the existing widening gate promotes the declared
#      «رقم» param to Any, and the TAG crosses the boundary — judged at
#      the CONSUMER: arithmetic raises RUN053 via the dynBinOp door,
#      display prints «لاشيء», both matching the interpreter (measured).
# ============================================================

استورد خرائط

دالة رقم ضاعف(رقم س)
    ارجع س * 2
نهاية

متغير خ = خريطة()
خ["أ"] = 1
متغير غ = خ["غائب"]
اطبع_سطر(ضاعف(غ))
