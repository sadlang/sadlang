# @description إغلاقٌ مصرَّحٌ «عشري» يُرجعُ قراءةَ قوسٍ عشريّةً ⇒ 2.5 «عشري» بلا قسرٍ — التصريحُ المطابقُ لا يغيّر القيمة.
# @requires: اطبع_سطر, خرائط
# @expected 2.5\nعشري
# @priority P2

استورد خرائط

دالة خارجية()
    متغير خ = خريطة()
    خ["ط"] = 2.5
    دالة عشري داخلية()
        ارجع خ["ط"]
    نهاية
    متغير ق = داخلية()
    اطبع_سطر(ق)
    اطبع_سطر(نوع(ق))
نهاية

خارجية()
