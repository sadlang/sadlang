# @description (NS-04) أمان null: الوصول الخام '.عضو' على متغيّر اختياريّ 'T؟' يُطلق تحذير NS-04 ساكنًا (على stderr، لا يُقارَن)؛ الوصول الآمن '؟.' لا يُرصَد. الدالّة غير مُستدعاة فلا تنفيذ زمنيّ.
# @expected OK
# @priority P1
#
# ============================================================
# (AR) 🔑 أُعيد إرساءُ هذه البذرة، وسببُه قاعدةٌ في مصدرِ الحقيقة.
#
#      كانت تكتبُ `م.الطول` — بلا قوسَين، وبهجاءٍ **لا وجودَ له للنصّ**.
#      والمقيسُ في `language-truth/type_methods.yaml`: «الطول» طريقةُ
#      **مصفوفةٍ** (سطر ٢١٢، `target_type: ARRAY`)، والمُعلَنُ للنصِّ هو
#      «طول» — طريقةٌ رتبتُها صفرٌ تُنادى بقوسَيها. فالصيغةُ القديمةُ لم تكن
#      برنامجًا صحيحًا مشتقًّا من القواعد، بل **أثرَ المفسّرِ المحذوف**، وهو
#      المرسى الذي يمنعُه قرارُ المالك.
#
#      وكانت خضراءَ قبلَ حملةِ السدِّ **بآليّةٍ لا بصحّة**: المترجّمُ ينهارُ
#      داخليًّا ويرجعُ صفرًا، والدالّتانِ غيرُ مُستدعاتَين، فيطبعُ الثنائيُّ
#      «OK» فيُحسَبُ نجاحًا. عينُ آليّةِ «١٤ انهيارًا تُقرأُ خضراء».
#
#      ⚠️ وغرضُ البذرةِ محفوظٌ بالكامل — مقيسٌ لا مُدَّعًى (٢٠٢٦-٠٩-٠٣):
#      الترجمةُ تنجح، ويُطبَعُ **تحذيرٌ واحدٌ** «غير آمن» على stderr للوصولِ
#      الخامِّ وحدَه، ولا يُطبَعُ شيءٌ للوصولِ الآمنِ `؟.` (لا إيجابيَّ كاذب)،
#      والمخرَجُ «OK» برمزِ خروجٍ صفر.
# (EN) Re-anchored on the SoT. It used `م.الطول` — a spelling that does not
#      exist for strings: type_methods.yaml declares «الطول» as an ARRAY method
#      and «طول» as the string one, a zero-arity method called with parentheses.
#      The old form was an artefact of the deleted interpreter, not a program
#      derived from the declared rules. It had been green by ACCIDENT: the
#      compiler crashed internally, returned 0, and since the functions are never
#      called the binary printed OK. Measured after the change: compilation
#      succeeds, exactly ONE «غير آمن» warning is printed for the raw access and
#      none for the safe `؟.` one, stdout is OK, exit code 0.
# ============================================================

# ============================================================
# (NS-04) رصد الوصول الخام على T؟ غير المُضيَّق (ADR-NS-001 D11)
#
# (AR) المحلّل المشترك يهبط في التعابير ويرصد الوصول الخام (`م.الطول`) على
#      معامل/متغيّر مُعلَن اختياريًّا `نص؟`. في الصرامة الافتراضية (Warn ≈ --learn)
#      يُطبَع تحذير NS-04 على stderr (خارج مقارنة @expected) والتنفيذ يكمل.
#      الوصول الآمن `؟.` (OptionalChainExpr) **لا يُرصَد** (لا إيجابيّ كاذب).
#
#      الدالّتان غير مُستدعاتين قصدًا: الرصد **ساكن** (يمشي كلّ الدوال بصرف النظر
#      عن الاستدعاء)، فلا تنفيذ زمنيّ ولا انهيار على عدم — مخرج stdout يبقى "OK".
#
# (EN) Shared analyzer descends into expressions and flags raw access (`م.الطول`)
#      on an optional `نص؟`. Safe access `؟.` is NOT flagged. Functions are
#      intentionally uncalled: detection is static, so no runtime null crash.
# ============================================================

# (AR) وصول خام على اختياريّ → تحذير NS-04 ساكن (stderr). دالّة غير مُستدعاة.
دالة خطر(م: نص؟)
    اطبع_سطر(م.طول())
نهاية

# (AR) وصول آمن `؟.` على اختياريّ → **لا** يُرصَد. دالّة غير مُستدعاة.
دالة آمن(ك: نص؟)
    اطبع_سطر(ك؟.طول())
نهاية

اطبع_سطر("OK")
