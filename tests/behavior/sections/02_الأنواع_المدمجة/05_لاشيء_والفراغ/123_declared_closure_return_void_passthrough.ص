# @description إغلاقٌ مصرَّحٌ «رقم» يُرجعُ غيابًا ⇒ «لاشيء» — سدُّ باب RET يشمل مسارَ المغاليق.
# @requires: اطبع_سطر, خرائط
# @expected لاشيء
# @priority P1

استورد خرائط

دالة خارجية()
    متغير خ = خريطة()
    خ["أ"] = 1
    دالة رقم داخلية()
        ارجع خ["غائب"]
    نهاية
    متغير ق = داخلية()
    اطبع_سطر(ق)
نهاية

خارجية()
