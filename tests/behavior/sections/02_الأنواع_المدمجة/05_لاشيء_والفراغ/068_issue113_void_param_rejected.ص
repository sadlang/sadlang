# @description (ISSUE-113) «فراغ» مرفوضٌ معامِلًا كما هو مرفوضٌ متغيّرًا — البوّابةُ على موضعِ الخانةِ لا على شكلِ التصريح.
# @priority P1
# @expect_error: SEM040
# @expect_compile_error

# ============================================================
# (AR) لماذا اختبارٌ ثانٍ: بوّابةٌ تُوضَع في مسارٍ واحدٍ تُخفي أنّ المسارات
#      الأخرى مفتوحة. تصريحُ المتغيّرِ ومعامِلُ الدالّةِ خانتان تُخزَّنان،
#      ويسلكان في المحلّلِ مسارَين مختلفَين تمامًا (سبعةُ مواضعِ
#      `paramType = parseType()` مقابل نقطةِ التقاءٍ واحدةٍ في parseVarDecl).
#      فإجازةُ «فراغ» معامِلًا بعد منعِه متغيّرًا تُعيد الانهيارَ نفسَه من
#      بابٍ آخر، وتُبقي المحرّكَين متباعدَين حيث ظُنَّ أنّهما اتّفقا.
# (EN) A gate on one path hides that the others are open: parameters reach
#      the parser through seven distinct sites, none of them parseVarDecl.
# ============================================================

دالة نص افحص(فراغ قيمة)
    ارجع نوع(قيمة)
نهاية

اطبع_سطر(افحص(لاشيء))
