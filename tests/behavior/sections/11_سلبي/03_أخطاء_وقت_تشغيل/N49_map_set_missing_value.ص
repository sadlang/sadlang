# @description اختبار سلبي — «عين» على خريطةٍ بلا قيمة. كانت الذراعُ تعودُ صامتةً
#   فيخرجُ المصرّفُ بصفرٍ ويُنتجُ ثنائيًّا **بلا التعيين**: صنفُ العيبِ نفسُه في
#   طبقةِ الطرائقِ لا المدمجات. والعددُ يبقى محلّيًّا لأنّ `type_methods.yaml` لا
#   حقلَ `arity` فيه البتّة — سطحٌ يُقاس على حدةٍ ولا يُوسَّع في رقعةِ مدمجات؛
#   والمعروضُ في الرسالةِ عددُ وسائطِ المستخدمِ لا الموضوعَ معها.
# @expect_error
# @expect_compile_error: 'عين'
# @priority P1

دالة رئيسية()
    متغير خريطتي = {}
    خريطتي.عين("مفتاح")
    اطبع_سطر("لا ينبغي أن يُترجَم")
نهاية
