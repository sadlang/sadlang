# @description اختبار سلبي — «اتصل_بـ» بوسيطَين بدل ثلاثة (ينقصُ المنفذ). أذرعُ
#   بانياتِ الشبكةِ كانت تمرّرُ `argOperands` جملةً إلى تعليمةِ SIR بلا أيِّ فحصِ
#   رتبةٍ البتّة، فالنداءُ الناقصُ يُترجَمُ ويُنفَّذ فيقرأ المعاملُ الغائبُ خانةً غيرَ
#   موجودة. ولم يكن في الأماميّةِ أساسٌ يُشتقُّ منه العقدُ — فاشتُقَّ من المفسّرِ
#   حيث `args.size() < 3` مكتوبٌ صراحةً، وبُذِر في `sockets.yaml`، ويُفرَض الآن
#   بـSEM005 على ٥٥ ذراعًا في هذا الملفّ.
# @expect_error
# @expect_compile_error: 'اتصل_بـ'
# @known_red تباعدُ محرّكَين كشفَه هذا السدُّ نفسُه: المترجّمُ صار يرفضُ النداءَ
#   الناقصَ بـSEM005 (صوابٌ — كان يقرأُ معاملًا غيرَ موجود)، والمفسّرُ **يقبلُه
#   ويُرجعُ `خطأ` صامتًا** (`if (args.size() < 3) return Value(false);`) فلا خطأَ
#   ولا تشخيص. الصوابُ أن يرفضَ المحرّكانِ كلاهما؛ وسدُّ جانبِ المفسّرِ يمسُّ
#   خمسةً وخمسينَ موضعًا في `builtin_module_sockets.cpp` و`builtin_module_http.cpp`
#   ويُغيّرُ سلوكَ زمنِ التشغيل، فيُفصَل في دفعةٍ مقيسةٍ على حدة. البذرةُ تصفُ
#   الصوابَ وتحمرُّ اليومَ قصدًا حتّى يُسَدّ.
# @priority P1

استورد مقابس

دالة رئيسية()
    اتصل_بـ(أنشئ_وصلة()، "127.0.0.1")
    اطبع_سطر("لا ينبغي أن يُترجَم")
نهاية
