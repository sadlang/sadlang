# @description اختبار سلبي — «حجز» (malloc) بلا وسيط. كان الرفضُ يُطبَع إلى
#   `std::cerr` **ويمضي البناءُ ناجحًا**: تشخيصٌ لا يحملُه رمزُ خروجٍ ولا تقرؤه
#   أداة، فالحصيلةُ ثنائيٌّ بلا التخصيص. ومعه كان اسمُ المدمجِ يُقارَن ببايتاتٍ
#   مهرَّبةٍ (`"\xd8\xad\xd8\xac\xd8\xb2"`) لا بثابتِ مصدرِ الحقيقة — نسخةٌ ثانيةٌ
#   للاسمِ لا يقرؤها بشرٌ ولا يقيسُها حارس. صارت الرتبةُ من `Ar::FFI::C_MALLOC`
#   والتبعةُ دفعًا إلى `errors_` — أي إفشالَ البناء.
# @expect_error
# @expect_compile_error: 'حجز'
# @priority P1

دالة رئيسية()
    متغير م = حجز()
    اطبع_سطر("لا ينبغي أن يُترجَم")
نهاية
