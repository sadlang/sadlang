# @description اختبار سلبي مزدوج — بتّيّة بمعامل «لاشيء» عبر متغيّر (سدّ إفلات د-2 الذي كشفته المراجعة): قائمة الرفض السكونيّ وُسّعت من 5 أنواع لكلّ الأنواع المعلومة غير العدديّة (عدم/فراغ/حرف/صنف/بنية/تعداد/دالّة/إغلاق/شريحة)؛ المفسّر يرفض زمنيًّا بـRUN053 والمترجم يرفض ترجمةً — النمط 'i64' يطابق «متوقع 'i64'» في رسالة الكتالوج
# @expect_error RUN053
# @expect_compile_error: 'i64'
# @priority P1

استورد نظام_منخفض

متغير س = لاشيء
اطبع_سطر(وافق(س، 1))
