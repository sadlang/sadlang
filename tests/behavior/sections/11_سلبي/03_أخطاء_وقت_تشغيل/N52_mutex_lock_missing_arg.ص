# @description اختبار سلبي — «اقفل» بلا وسيط. كان الرفضُ سطرًا إلى `std::cerr` ثمّ
#   عودةً بسجلٍّ فارغ: لا رمزَ خروجٍ يحملُ الرسالةَ ولا أداةَ تقرؤها، فالبناءُ ينجحُ
#   والقفلُ لا يُقفَل — تبخّرٌ صامتٌ في مدمجِ تزامنٍ حيثُ أثرُه سباقُ بيانات. أحدَ
#   عشرَ مدمجًا في هذا الملفِّ كانت على هذه الحال. الرتبةُ الآن من `arity`
#   (AsyncAdvanced::MUTEX_LOCK) مُشتقّةً من شرطِ الرفضِ في المفسّر.
# @expect_error
# @expect_compile_error: 'اقفل'
# @priority P1

دالة رئيسية()
    اقفل()
    اطبع_سطر("لا ينبغي أن يُترجَم")
نهاية
