# @description اختبار سلبي مزدوج — بتّيّة بمعامل مصفوفة (د-2، توافق المحرّكين): المفسّر يرفض زمنيًّا بـRUN053 (كان toInt64 يُصفّر المصفوفة بصمت)، والمترجم يرفض ترجمةً بـSEM002 — النمط 'i64' يطابق «متوقع 'i64'» في رسالة الكتالوج
# @expect_error RUN053
# @expect_compile_error: 'i64'
# @priority P1

استورد نظام_منخفض

اطبع_سطر(اعكس_البتّات([1، 2]))
