# @description اختبار سلبي — «اقرأ_منفذ16» بلا وسيط. الذراعُ كانت ترفضُ بـ
#   `if (argResults.empty()) return BuildResult("", …);` — عودةٌ بسجلٍّ فارغٍ بلا
#   تسجيلِ خطأ ⇒ النداءُ يتبخّرُ والمصرّفُ يخرجُ بصفر. ولم يرَها الحارسُ لأنّ ذراعَه
#   الخامسةَ كانت تقرأ `size() < N` ولا تقرأ `empty()` — هجاءٌ ثانٍ للفعلِ نفسِه
#   خارجَ بصرِها، فشهدت بالسلامةِ حيثُ لم تنظر. وُسِّعت المرساةُ فظهرت خمسَ عشرةَ
#   ذراعًا من هذا الصنف. الرتبةُ الآن من `arity` في مصدرِ الحقيقة (KernelCpu::CPU_10).
# @expect_error
# @expect_compile_error: 'اقرأ_منفذ16'
# @priority P1

دالة رئيسية()
    اقرأ_منفذ16()
    اطبع_سطر("لا ينبغي أن يُترجَم")
نهاية
