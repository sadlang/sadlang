# @rule: gr.oop.method
# @description: طريقة حدّيّ — اسمٌ ماتَ نطاقُه لا يُورِّثُ صنفَه لِما بعدَه: `قط س` في كتلةٍ (تصريحًا بلا نداء) ثمّ `رقم س = 5` خارجَها ⇒ `انطق(س)` يُرفَعُ RUN033. حارسُ انحدارٍ لـISSUE-195 — رفعُ وسمِ الكائنِ عندَ التعليبِ من خريطةٍ مفتاحُها اسمٌ بلا عزلِ كتلٍ يجعلُ هذا يطبعُ «مواء» برمزِ خروجٍ صفر. ⚠️ ولا نداءَ داخلَ الكتلةِ عمدًا: نداءُ كائنٍ سليمٍ عبرَ «أي» يُرفَعُ اليومَ رفضًا كاذبًا (ISSUE-195) فيقتلُ البرنامجَ قبلَ بلوغِ الحدثِ المُسمّى، فتخضرُّ البذرةُ لغيرِ سببِها
# @priority: P0
# @requires: اطبع_سطر
# @expect_error_compiled: RUN033

صنف قط
    باني() نهاية
    دالة صوت()
        ارجع "مواء"
    نهاية
نهاية

صنف كلب
    باني() نهاية
    دالة صوت()
        ارجع "نباح"
    نهاية
نهاية

دالة انطق(أي ح)
    اطبع_سطر(ح.صوت())
نهاية

دالة رئيسية()
    انطق(كلب())
    إذا (1 == 1)
        قط س = قط()
    نهاية
    رقم س = 5
    انطق(س)
    ارجع 0
نهاية
