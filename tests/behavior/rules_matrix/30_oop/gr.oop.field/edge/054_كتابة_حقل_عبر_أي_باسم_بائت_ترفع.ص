# @rule: gr.oop.field
# @description: حقل حدّيّ — توأمُ `gr.oop.method/edge/058` في منفذِ الكتابة: اسمٌ ماتَ نطاقُه لا يُورِّثُ صنفَه، فكتابةُ حقلٍ عبرَ «أي» يحمل عددًا تُرفَعُ RUN033 ولا تمضي `store` على عنوانٍ مُلفَّق. وهذه الصورةُ تُخرِجُ 0xC0000005 (rc=139) إن رُفِعَ وسمُ الكائنِ من خريطةِ الأسماء (ISSUE-195). ⚠️ ولا نداءَ داخلَ الكتلةِ عمدًا — للعلّةِ المشروحةِ في ٠٥٨
# @priority: P0
# @requires: اطبع_سطر
# @expect_error_compiled: RUN033

صنف قط
    متغير عام اسم = "مشمش"
    باني() نهاية
نهاية

دالة اكتب(أي ح)
    ح.اسم = "بسبس"
نهاية

دالة رئيسية()
    اطبع_سطر("بدأ")
    إذا (1 == 1)
        قط ن = قط()
    نهاية
    رقم ن = 7
    اكتب(ن)
    اطبع_سطر("انتهى")
    ارجع 0
نهاية
