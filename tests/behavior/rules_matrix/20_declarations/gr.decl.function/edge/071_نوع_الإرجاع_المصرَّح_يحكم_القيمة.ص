# @rule: gr.decl.function
# @description: 🔑 التصريحُ يحكم القيمةَ لا العرضَ وحدَه، كما في C: `عشري` تُرجِع صحيحًا ⇒ عشريّ، و`رقم` تُرجِع عشريًّا ⇒ يُقتطَع نحو الصفر. وناتجُ `//` كِنْهُه حقيقةُ زمنِ تشغيلٍ (يفيض فيُرقَّى) فيلزمه فكُّ الوسمِ لا مجرّدُ توسيعٍ ساكن
# @priority: P1
# @requires: اطبع_سطر
# @expected: 2.0
# @expected: 2
# @expected: 2.0
دالة عشري نصفه(القيمة)
    ارجع القيمة // 2
نهاية
دالة رقم مقتطَع(القيمة)
    ارجع القيمة / 2.0
نهاية
دالة عشري بسيط(القيمة)
    ارجع القيمة / 2
نهاية
اطبع_سطر(نصفه(5))
اطبع_سطر(مقتطَع(5))
اطبع_سطر(بسيط(5))
