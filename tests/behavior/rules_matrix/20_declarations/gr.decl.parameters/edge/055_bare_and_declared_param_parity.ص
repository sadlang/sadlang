# @rule: gr.decl.parameters
# @description: حدّي — تكافؤ: معامل بلا نوع ومعامل مُصرَّح «نص» يجب أن ينتجا المخرَج عينه لثلاثة أطراف متسلسلة (صورة نسق() في مكتبة جيسون)
# @priority: P1
# @requires: اطبع_سطر
# @expected: [  ]
# @expected: [  ]

دالة نسق_عار(بادئة، ازاحة، متن)
    ارجع بادئة + ازاحة + متن
نهاية

دالة نص نسق_مصرح(نص بادئة، نص ازاحة، نص متن)
    ارجع بادئة + ازاحة + متن
نهاية

اطبع_سطر(نسق_عار("[", "  ", "]"))
اطبع_سطر(نسق_مصرح("[", "  ", "]"))
