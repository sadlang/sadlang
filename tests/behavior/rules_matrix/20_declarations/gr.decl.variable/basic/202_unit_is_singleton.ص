# @rule: gr.decl.variable
# @description: لنوعِ الوحدةِ قيمةٌ واحدةٌ لا تتعدّد — نسختانِ متساويتانِ دائمًا (مجال ٢)
# @priority: P0
# @requires: اطبع_سطر
# @expected: متساويان\nلا-فرق\nمتساويان-داخل-دالة

# (AR) 🔑 القيمةُ الواحدةُ **لا يجوز أن تتعدّدَ بالتخصيص**. وكانت «()» تُخفَّضُ
#      TUPLE_NEW فيُخصَّصُ لكلِّ نسخةٍ كائنٌ في الكومة، فيُقارَنُ عنوانانِ لا
#      قيمتان. ومقيسٌ (2026-09-07) أنّ الجوابَ كان **يختلفُ باختلافِ النطاق**:
#      «متساويان» في النطاقِ العامِّ و«مختلفان» داخلَ دالّة — برنامجٌ واحدٌ
#      بحكمَين، والفارقُ موضعُ التصريحِ وحدَه. ولذلك يُقاسُ النطاقانِ معًا:
#      الجوابُ الصحيحُ في أحدِهما كان يستُرُ الخاطئَ في الآخر.
# (EN) One value must not multiply by allocation. «()» lowered to a heap tuple,
#      so two units compared as ADDRESSES — and the verdict differed BY SCOPE:
#      equal at global scope, unequal inside a function. One program, two
#      verdicts. Both scopes are measured here; the right answer in one hid the
#      wrong answer in the other.

دالة جرب()
    خالي د = ()
    خالي هـ = ()
    إذا (د == هـ)
        اطبع_سطر("متساويان-داخل-دالة")
    وإلا
        اطبع_سطر("مختلفان-داخل-دالة")
    نهاية
نهاية

خالي أ = ()
خالي ب = ()
إذا (أ == ب)
    اطبع_سطر("متساويان")
وإلا
    اطبع_سطر("مختلفان")
نهاية
إذا (أ != ب)
    اطبع_سطر("مختلفان-بالنفي")
وإلا
    اطبع_سطر("لا-فرق")
نهاية
جرب()
