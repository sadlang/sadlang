# @rule: gr.stmt.for, gr.expr.array_literal
# @description: لكل × المصفوفةِ الحرفيّة — قراءةٌ بالفهرسةِ وجمعٌ (تجنُّبًا لـ«.اضف» المكسورة، ISSUE-001)
# @priority: P2
# @requires: اطبع_سطر
# @expected: 3\n12

دالة رئيسية()
    متغير م = [1، 2، 3]
    متغير مجموع = 0
    لكل ي من 0 الى طول(م) - 1
        مجموع = مجموع + م[ي] * 2
    نهاية
    اطبع_سطر(طول(م))
    اطبع_سطر(مجموع)
نهاية
