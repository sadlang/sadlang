# @rule: gr.lex.comment, gr.lex.identifier, gr.lex.string, gr.lex.fstring, gr.lex.raw_string, gr.lex.lifetime
# @description: تداخل معجميّ: تعليق ومعرّف عربيّ ونصّ ونصّ مقحَم ونصّ خام وبنية بمعامل عمر
# @priority: P2
# @requires: اطبع_سطر
# @expected: مرحبا أحمد\nمسار\tملف\n0

# (تعليق سطريّ يسبق البنية)
بنية مرجع_مؤقّت<'أ>
    رقم قيمة = 0
نهاية

متغير الاسم = "أحمد"
اطبع_سطر(f"مرحبا {الاسم}")   # نصّ مقحَم
اطبع_سطر(r"مسار\tملف")
اطبع_سطر(مرجع_مؤقّت().قيمة)
