# @gap: ISSUE-124
# @description: فكُّ الصفِّ **داخلَ دالّةٍ ومع مُعدِّل** — الشقّان الإضافيّان من ISSUE-124، كانا مُدَّعيَين بلا كاشفٍ يُعيد إنتاجهما (نقدُ أميليا الثالث). المفسّرُ يطبع 3 والمترجمُ يسقط بتأكيدِ LLVM نفسِه.
# @expected: 3

دالة د()
  متغير ثابت (أ، ب) = (1، 2)
  ارجع أ + ب
نهاية
اطبع_سطر(د())
