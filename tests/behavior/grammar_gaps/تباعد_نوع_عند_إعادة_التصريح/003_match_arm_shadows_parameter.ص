# @rule: gr.stmt.match, gr.decl.variable
# @description: كاشفُ فجوة — إعادةُ التصريحِ باسمِ **معامل** داخلَ ذراعِ «طابق». المفسّرُ يكتبُ فوقَ المعامل (9) والمترجِمُ يُبقيه (1). المعاملُ **مستثنًى تحفّظًا** من إعادةِ استعمالِ الموقعِ في م-١٠، فتسلكُ الحالةُ مسلكَ ما قبلَ الإصلاحِ تمامًا — تباعُدٌ **سابقٌ** لا انحدارٌ أُدخل (قِيس بتعطيلِ إعادةِ الاستعمالِ فخرج الناتجُ نفسُه).
# @priority: P2
# @requires: اطبع_سطر
# @gap: ISSUE-103
# @expected: 9

دالة د(ع)
    متغير ن = 1
    طابق ن
        عندما 1:
            متغير ع = 9
    نهاية
    ارجع ع
نهاية

دالة رئيسية()
    اطبع_سطر(د(1))
نهاية
