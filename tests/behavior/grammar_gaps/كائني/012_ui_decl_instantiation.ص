# @gap: تحقّق — إنشاء مكوّن واجهة (الكلمة المفتاحية «واجهة» لتعريف مكوّن UI)
# @description: مكوّن «واجهة» يُسجَّل في ClassManager ويُنشأ بالنداء «عداد()»، وحقول @حالة
#               قابلة للوصول. «واجهة» هي الكلمة المعتمدة لتعريف مكوّنات الواجهة (KEYWORD_UI_DECL).
# @expected: كائن
# @expected: 5

واجهة عداد
    @حالة عدد = 5
    دالة بناء()
        ارجع نص_عنصر(هذا.عدد)
    نهاية
نهاية
متغير ع = عداد()
اطبع_سطر(نوع(ع))
اطبع_سطر(ع.عدد)
