# اختبار السيناريو الدقيق من مشاكل.md — المشكلة 1
نوافذ = [{"عنوان": "نافذة1", "عرض": 20}]

دالة احصل_على_عنوان(فهرس)
    ارجع نوافذ[فهرس]["عنوان"]
نهاية

متغير النتيجة = احصل_على_عنوان(0)
اطبع("العنوان: " + النتيجة)

# الاختبار مع استدعاء من دالة أخرى (عمق 2)
دالة غلاف()
    ارجع احصل_على_عنوان(0)
نهاية

متغير ن2 = غلاف()
اطبع("عبر غلاف: " + ن2)
اطبع("نجح!")
