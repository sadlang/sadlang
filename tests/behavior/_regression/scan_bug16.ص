# === فحص Bug #16: += على حقل كائن داخل مصفوفة ===

صنف عنصر
    باني(اسم)
        هذا.اسم = اسم
        هذا.قيم = []
    نهاية

    دالة اطبع_نفسك()
        اطبع_سطر(هذا.اسم + ": " + هذا.قيم)
    نهاية
نهاية

متغير القائمة = [عنصر("أ") جديد، عنصر("ب") جديد]

# محاولة += على حقل كائن داخل مصفوفة
القائمة[0].قيم += [1، 2]
القائمة[1].قيم += [3]

القائمة[0].اطبع_نفسك()
القائمة[1].اطبع_نفسك()

اطبع_سطر("=== Bug #16 نجح ===")
