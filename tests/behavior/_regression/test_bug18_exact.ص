# اختبار #18 الدقيق — هذا.method() مع += كما في مشاكل.md

صنف مخزن
    باني()
        هذا.بيانات = []
        هذا.أضف("عنصر1")
    نهاية

    دالة أضف(قيمة)
        هذا.بيانات += [قيمة]
    نهاية
نهاية

متغير م = مخزن() جديد
اطبع("الطول: " + م.بيانات.الطول())
اطبع("البيانات: " + م.بيانات)

# هل الطول = 1 أم 0؟
إذا (م.بيانات.الطول() == 1)
    اطبع("نجح - التغييرات حُفظت!")
وإلا
    اطبع("فشل - التغييرات لم تُحفظ!")
نهاية
