# اختبار إعادة إنتاج المشكلة #3: .الطول() crash عند عمق استدعاء >= 2
# Reproduction test for Bug #3: .length() crashes at call depth >= 2

عناصر = [1, 2, 3, 4, 5]

# عمق 1: يعمل
دالة احصل_على_طول()
    ارجع عناصر.الطول()
نهاية

اطبع("عمق 1: " + احصل_على_طول())

# عمق 2: المشكلة الأصلية
دالة دالة_أخرى()
    متغير ط = عناصر.الطول()
    ارجع ط
نهاية

دالة غلاف_عمق2()
    ارجع دالة_أخرى()
نهاية

اطبع("عمق 2: " + غلاف_عمق2())

# عمق 3
دالة غلاف_عمق3()
    ارجع غلاف_عمق2()
نهاية

اطبع("عمق 3: " + غلاف_عمق3())

# الطول مع متغير محلي
دالة طول_محلي()
    متغير م = [10, 20, 30]
    ارجع م.الطول()
نهاية

دالة غلاف_محلي()
    ارجع طول_محلي()
نهاية

اطبع("محلي عمق 2: " + غلاف_محلي())

# الطول مع نص
دالة طول_نص()
    متغير ن = "مرحباً"
    ارجع ن.الطول()
نهاية

دالة غلاف_نص()
    ارجع طول_نص()
نهاية

اطبع("نص عمق 2: " + غلاف_نص())

اطبع("=== جميع اختبارات الطول نجحت ===")
