# وحدةٌ مساعدةٌ لـ m1_imported_module_parity.ص — ليست اختبارًا بذاتها.
# البادئةُ «_» تميّزها عن ملفّات الاختبار كي لا تُجمَع مُشغَّلةً وحدها.
#
# كلُّ دالّةٍ هنا موضوعةٌ عمدًا لتكشف عيبًا كان يظهر **في الوحدة المستوردة وحدها**
# ويختفي لو نُقل الكودُ نفسُه حرفًا بحرف إلى ملفّ الدخول.

# (١) نداءٌ أماميّ: «أ» تنادي «ب» المعرَّفةَ بعدها.
دالة منطقي أمامي(نص س)
    إذا (تال(س))
        ارجع صحيح
    نهاية
    ارجع خطأ
نهاية

دالة منطقي تال(نص س)
    ارجع س == "نعم"
نهاية

# (٢) «لكل» على معاملِ مصفوفة: يلزمه نوعُ عنصرٍ مستنتَجٌ من موقع النداء.
دالة نص اجمع(مصفوفة القائمة)
    نص الناتج = ""
    لكل عنصر في القائمة
        الناتج = الناتج + "[" + عنصر + "]"
    نهاية
    ارجع الناتج
نهاية

# (٣) تمريرُ المصفوفة **معاملًا إلى معامل**: نوعُ العنصر يلزمه أن يعبر قفزةً ثانية.
دالة نص مرّر(نص مهمَل، مصفوفة القائمة)
    ارجع اجمع(القائمة)
نهاية

# (٤) مطابقةُ عنصرٍ بنصٍّ مُنتَجٍ في زمن التشغيل لا بحرفيّة.
دالة منطقي فيها_كلمة(نص الجملة، مصفوفة الأسماء)
    لكل كلمة في تقسيم(الجملة، " ")
        إذا (يحتوي_عنصر(الأسماء، كلمة))
            ارجع صحيح
        نهاية
    نهاية
    ارجع خطأ
نهاية
