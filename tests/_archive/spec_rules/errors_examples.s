# ============================================================
# اختبار: أمثلة الأخطاء / Test: Error Examples
# ============================================================
# الملف: errors_examples.s
# الهدف: اختبار معالجة الأخطاء والاستثناءات
# Purpose: Test error handling and exceptions

اطبع("=== اختبار معالجة الأخطاء ===")

# ============================================================
# اختبار 1: معالجة خطأ بسيط / Test 1: Simple error handling
# ============================================================
اطبع("\n--- اختبار 1: معالجة خطأ بسيط ---")

حاول
    خطأ "رسالة خطأ اختبار"
امسك خطأ خ
    تأكد(خ.رسالة == "رسالة خطأ اختبار")
    اطبع("✓ تم اصطياد الخطأ: " + خ.رسالة)
نهاية

اطبع("✓ اختبار 1 نجح")

# ============================================================
# اختبار 2: خطأ القسمة على صفر / Test 2: Division by zero
# ============================================================
اطبع("\n--- اختبار 2: خطأ القسمة على صفر ---")

دالة رقم قسم(رقم أ، رقم ب)
    إذا ب == 0
        خطأ "لا يمكن القسمة على صفر"
    نهاية
    إرجاع أ / ب
نهاية

حاول
    رقم نتيجة = قسم(10، 0)
    اطبع("هذا السطر لن يتم تنفيذه")
امسك خطأ خ
    اطبع("✓ تم اصطياد خطأ القسمة: " + خ.رسالة)
    تأكد(خ.رسالة == "لا يمكن القسمة على صفر")
نهاية

# استخدام صحيح / Correct usage
رقم نتيجة_صحيحة = قسم(10، 2)
تأكد(نتيجة_صحيحة == 5)

اطبع("✓ اختبار 2 نجح")

# ============================================================
# اختبار 3: خطأ الفهرس / Test 3: Index error
# ============================================================
اطبع("\n--- اختبار 3: خطأ الفهرس ---")

دالة رقم احصل_عنصر(مصفوفة م، رقم فهرس)
    إذا فهرس < 0 || فهرس >= طول(م)
        خطأ ن"فهرس خارج النطاق: {فهرس}"
    نهاية
    إرجاع م[فهرس]
نهاية

مصفوفة أرقام = [1، 2، 3، 4، 5]

حاول
    رقم ع = احصل_عنصر(أرقام، 10)
امسك خطأ خ
    اطبع("✓ تم اصطياد خطأ الفهرس: " + خ.رسالة)
نهاية

# استخدام صحيح / Correct usage
رقم ع_صحيح = احصل_عنصر(أرقام، 2)
تأكد(ع_صحيح == 3)

اطبع("✓ اختبار 3 نجح")

# ============================================================
# اختبار 4: أخطاء متعددة / Test 4: Multiple error types
# ============================================================
اطبع("\n--- اختبار 4: أخطاء متعددة ---")

دالة عملية(نص نوع، رقم قيمة)
    إذا نوع == "قسمة" && قيمة == 0
        خطأ "خطأ_قسمة: لا يمكن القسمة على صفر"
    وإلا_إذا نوع == "جذر" && قيمة < 0
        خطأ "خطأ_قيمة: لا يمكن أخذ جذر عدد سالب"
    وإلا_إذا نوع == "غير_معروف"
        خطأ "خطأ_نوع: نوع العملية غير معروف"
    نهاية
    إرجاع قيمة
نهاية

# اختبار أنواع مختلفة / Test different types
حاول
    عملية("قسمة"، 0)
امسك خطأ خ
    اطبع("✓ خطأ قسمة: " + خ.رسالة)
نهاية

حاول
    عملية("جذر"، -5)
امسك خطأ خ
    اطبع("✓ خطأ قيمة: " + خ.رسالة)
نهاية

حاول
    عملية("غير_معروف"، 10)
امسك خطأ خ
    اطبع("✓ خطأ نوع: " + خ.رسالة)
نهاية

اطبع("✓ اختبار 4 نجح")

# ============================================================
# اختبار 5: معالجة مع finally / Test 5: Handling with finally
# ============================================================
اطبع("\n--- اختبار 5: معالجة مع finally ---")

منطقي تم_تنفيذ_finally = خطأ

حاول
    خطأ "خطأ اختبار"
امسك خطأ خ
    اطبع("✓ تم معالجة الخطأ")
أخيراً
    تم_تنفيذ_finally = صحيح
    اطبع("✓ تم تنفيذ كتلة finally")
نهاية

تأكد(تم_تنفيذ_finally)
اطبع("✓ اختبار 5 نجح")

# ============================================================
# اختبار 6: خطأ في دالة متداخلة / Test 6: Error in nested function
# ============================================================
اطبع("\n--- اختبار 6: خطأ في دالة متداخلة ---")

دالة خارجية()
    دالة داخلية()
        خطأ "خطأ من دالة داخلية"
    نهاية
    
    داخلية()
نهاية

حاول
    خارجية()
امسك خطأ خ
    اطبع("✓ تم اصطياد خطأ من دالة متداخلة: " + خ.رسالة)
نهاية

اطبع("✓ اختبار 6 نجح")

# ============================================================
# اختبار 7: خطأ في صنف / Test 7: Error in class
# ============================================================
اطبع("\n--- اختبار 7: خطأ في صنف ---")

صنف حساب
    رقم رصيد
    
    باني(رقم رصيد_أولي)
        هذا.رصيد = رصيد_أولي
    نهاية
    
    دالة سحب(رقم مبلغ)
        إذا مبلغ > هذا.رصيد
            خطأ "رصيد غير كافٍ"
        نهاية
        هذا.رصيد = هذا.رصيد - مبلغ
    نهاية
نهاية

حساب ح = حساب(100)

حاول
    ح.سحب(150)  # محاولة سحب أكثر من الرصيد
امسك خطأ خ
    اطبع("✓ خطأ في الصنف: " + خ.رسالة)
نهاية

# استخدام صحيح / Correct usage
ح.سحب(50)
تأكد(ح.رصيد == 50)

اطبع("✓ اختبار 7 نجح")

# ============================================================
# اختبار 8: إعادة رمي الخطأ / Test 8: Re-throwing error
# ============================================================
اطبع("\n--- اختبار 8: إعادة رمي الخطأ ---")

دالة معالج_خارجي()
    حاول
        دالة معالج_داخلي()
            خطأ "خطأ أصلي"
        نهاية
        
        معالج_داخلي()
    امسك خطأ خ
        اطبع("معالج داخلي: " + خ.رسالة)
        خطأ "خطأ معاد: " + خ.رسالة
    نهاية
نهاية

حاول
    معالج_خارجي()
امسك خطأ خ
    اطبع("✓ معالج خارجي: " + خ.رسالة)
نهاية

اطبع("✓ اختبار 8 نجح")

# ============================================================
# اختبار 9: خطأ في حلقة / Test 9: Error in loop
# ============================================================
اطبع("\n--- اختبار 9: خطأ في حلقة ---")

رقم أخطاء_معالجة = 0

لكل ع في نطاق(5)
    حاول
        إذا ع % 2 == 0
            خطأ ن"خطأ عند العدد {ع}"
        نهاية
    امسك خطأ خ
        أخطاء_معالجة = أخطاء_معالجة + 1
    نهاية
نهاية

تأكد(أخطاء_معالجة == 3)  # 0، 2، 4
اطبع(ن"✓ تم معالجة {أخطاء_معالجة} أخطاء")
اطبع("✓ اختبار 9 نجح")

# ============================================================
# اختبار 10: تطبيق عملي - التحقق من صحة البيانات / Test 10: Validation
# ============================================================
اطبع("\n--- اختبار 10: التحقق من صحة البيانات ---")

صنف مستخدم
    نص اسم
    رقم عمر
    نص بريد
    
    باني(نص اسم، رقم عمر، نص بريد)
        # التحقق من الاسم / Validate name
        إذا طول(اسم) < 3
            خطأ "الاسم يجب أن يكون 3 أحرف على الأقل"
        نهاية
        
        # التحقق من العمر / Validate age
        إذا عمر < 0 || عمر > 150
            خطأ "العمر غير صحيح"
        نهاية
        
        # التحقق من البريد / Validate email (بسيط)
        إذا !يحتوي(بريد، "@")
            خطأ "البريد الإلكتروني غير صحيح"
        نهاية
        
        هذا.اسم = اسم
        هذا.عمر = عمر
        هذا.بريد = بريد
    نهاية
نهاية

دالة منطقي يحتوي(نص نص، نص جزء)
    إرجاع نص.يحتوي(جزء)
نهاية

# حالات خطأ / Error cases
رقم أخطاء_تحقق = 0

حاول
    مستخدم م1 = مستخدم("أ"، 25، "test@example.com")
امسك خطأ خ
    اطبع("✓ خطأ اسم قصير: " + خ.رسالة)
    أخطاء_تحقق = أخطاء_تحقق + 1
نهاية

حاول
    مستخدم م2 = مستخدم("أحمد"، -5، "test@example.com")
امسك خطأ خ
    اطبع("✓ خطأ عمر غير صحيح: " + خ.رسالة)
    أخطاء_تحقق = أخطاء_تحقق + 1
نهاية

حاول
    مستخدم م3 = مستخدم("أحمد"، 25، "test")
امسك خطأ خ
    اطبع("✓ خطأ بريد غير صحيح: " + خ.رسالة)
    أخطاء_تحقق = أخطاء_تحقق + 1
نهاية

تأكد(أخطاء_تحقق == 3)

# حالة صحيحة / Correct case
مستخدم م_صحيح = مستخدم("أحمد"، 25، "ahmad@example.com")
تأكد(م_صحيح.اسم == "أحمد")

اطبع("✓ اختبار 10 نجح")

# ============================================================
# ملخص / Summary
# ============================================================
اطبع("\n" + "="*60)
اطبع("✓ جميع اختبارات معالجة الأخطاء نجحت!")
اطبع("✓ All error handling tests passed!")
اطبع("="*60)

/*
المخرجات المتوقعة / Expected Output:
=== اختبار معالجة الأخطاء ===

--- اختبار 1: معالجة خطأ بسيط ---
✓ تم اصطياد الخطأ: رسالة خطأ اختبار
✓ اختبار 1 نجح

--- اختبار 2: خطأ القسمة على صفر ---
✓ تم اصطياد خطأ القسمة: لا يمكن القسمة على صفر
✓ اختبار 2 نجح

--- اختبار 3: خطأ الفهرس ---
✓ تم اصطياد خطأ الفهرس: فهرس خارج النطاق: 10
✓ اختبار 3 نجح

--- اختبار 4: أخطاء متعددة ---
✓ خطأ قسمة: خطأ_قسمة: لا يمكن القسمة على صفر
✓ خطأ قيمة: خطأ_قيمة: لا يمكن أخذ جذر عدد سالب
✓ خطأ نوع: خطأ_نوع: نوع العملية غير معروف
✓ اختبار 4 نجح

--- اختبار 5: معالجة مع finally ---
✓ تم معالجة الخطأ
✓ تم تنفيذ كتلة finally
✓ اختبار 5 نجح

--- اختبار 6: خطأ في دالة متداخلة ---
✓ تم اصطياد خطأ من دالة متداخلة: خطأ من دالة داخلية
✓ اختبار 6 نجح

--- اختبار 7: خطأ في صنف ---
✓ خطأ في الصنف: رصيد غير كافٍ
✓ اختبار 7 نجح

--- اختبار 8: إعادة رمي الخطأ ---
معالج داخلي: خطأ أصلي
✓ معالج خارجي: خطأ معاد: خطأ أصلي
✓ اختبار 8 نجح

--- اختبار 9: خطأ في حلقة ---
✓ تم معالجة 3 أخطاء
✓ اختبار 9 نجح

--- اختبار 10: التحقق من صحة البيانات ---
✓ خطأ اسم قصير: الاسم يجب أن يكون 3 أحرف على الأقل
✓ خطأ عمر غير صحيح: العمر غير صحيح
✓ خطأ بريد غير صحيح: البريد الإلكتروني غير صحيح
✓ اختبار 10 نجح

============================================================
✓ جميع اختبارات معالجة الأخطاء نجحت!
✓ All error handling tests passed!
============================================================
*/
