# ═══════════════════════════════════════════
# اختبار ADT مع المترجم (sadc)
# ═══════════════════════════════════════════

# === 1. تعريف تعدادات جبرية ===
تعداد شكل
    دائرة(نصف_القطر)
    مستطيل(عرض، ارتفاع)
    نقطة
نهاية

تعداد نتيجة
    نجاح(قيمة)
    فشل(رسالة)
نهاية

اطبع_سطر("1. تم تعريف التعدادات")

# === 2. إنشاء قيم ===
متغير ش1 = شكل.دائرة(5)
متغير ش2 = شكل.مستطيل(10، 20)
متغير ن1 = نتيجة.نجاح(42)
متغير ن2 = نتيجة.فشل("خطأ")

اطبع_سطر("2. تم إنشاء القيم")

# === 3. مطابقة الأنماط — شكل ===
طابق (ش1)
    عندما شكل.دائرة(نق):
        اطبع_سطر("3. ش1 = دائرة")
    عندما شكل.مستطيل(ع، ا):
        اطبع_سطر("3. ش1 = مستطيل")
    عندما شكل.نقطة:
        اطبع_سطر("3. ش1 = نقطة")
نهاية

طابق (ش2)
    عندما شكل.دائرة(نق):
        اطبع_سطر("4. ش2 = دائرة")
    عندما شكل.مستطيل(ع، ا):
        اطبع_سطر("4. ش2 = مستطيل")
    عندما شكل.نقطة:
        اطبع_سطر("4. ش2 = نقطة")
نهاية

# === 4. مطابقة الأنماط — نتيجة ===
طابق (ن1)
    عندما نتيجة.نجاح(ق):
        اطبع_سطر("5. ن1 = نجاح")
    عندما نتيجة.فشل(ر):
        اطبع_سطر("5. ن1 = فشل")
نهاية

طابق (ن2)
    عندما نتيجة.نجاح(ق):
        اطبع_سطر("6. ن2 = نجاح")
    عندما نتيجة.فشل(ر):
        اطبع_سطر("6. ن2 = فشل")
نهاية

اطبع_سطر("=== انتهى الاختبار بنجاح ===")
