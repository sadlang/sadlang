# ملف اختبار صغير يحاكي test_tuples.ص
متغير نجاح = 0
متغير فشل = 0

دالة تحقق(الاسم، القيمة، المتوقع)
    إذا (القيمة == المتوقع)
        نجاح = نجاح + 1
        اطبع_سطر("✅ " + الاسم)
    وإلا
        فشل = فشل + 1
        اطبع_سطر("❌ " + الاسم + " — توقعنا: " + المتوقع + " حصلنا: " + القيمة)
    نهاية
نهاية

# اختبار 1
متغير ص1 = (1، 2، 3)
تحقق("صف ثلاثي", ص1.0, 1)

اطبع_سطر("نجاح: " + نجاح)
اطبع_سطر("فشل: " + فشل)
