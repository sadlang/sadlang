# ═══════════════════════════════════════════════════
# اختبار نظام الأنواع الموحد
# ═══════════════════════════════════════════════════

اطبع_سطر("=== اختبار 1: متغيرات بأنواع صريحة ===")

# أنواع أساسية مع تعيين صحيح
رقم س = 42
عشري ص = 3.14
نص اسم = "مرحبا"
منطقي علم = صحيح

اطبع_سطر("رقم: " + س)
اطبع_سطر("عشري: " + ص)
اطبع_سطر("نص: " + اسم)
اطبع_سطر("منطقي: " + علم)

اطبع_سطر("=== اختبار 2: التحويل التلقائي ===")

# التحويل التلقائي من رقم إلى عشري
عشري ع = 10
اطبع_سطر("عشري من رقم: " + ع)

# التحويل التلقائي من عشري إلى رقم
رقم ر = 7.5
اطبع_سطر("رقم من عشري: " + ر)

اطبع_سطر("=== اختبار 3: متغيرات بدون نوع صريح ===")

# متغيرات بدون نوع (يجب أن تعمل بدون تحذيرات)
متغير أ = 100
متغير ب = "نص حر"
متغير ج = صحيح

اطبع_سطر("أ = " + أ)
اطبع_سطر("ب = " + ب)
اطبع_سطر("ج = " + ج)

اطبع_سطر("=== اختبار 4: دوال مع أنواع ===")

دالة جمع(رقم أ، رقم ب)
    ارجع أ + ب
نهاية

دالة تحية(نص اسم)
    ارجع "مرحباً يا " + اسم
نهاية

اطبع_سطر("جمع: " + جمع(3، 5))
اطبع_سطر("تحية: " + تحية("أحمد"))

اطبع_سطر("=== اختبار 5: ثوابت مع أنواع ===")

ثابت رقم الحد = 100
ثابت نص الرسالة = "مكتمل"

اطبع_سطر("الحد: " + الحد)
اطبع_سطر("الرسالة: " + الرسالة)

اطبع_سطر("=== جميع الاختبارات نجحت! ===")
