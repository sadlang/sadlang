# ═══════════════════════════════════════════
# اختبار الأنواع الجبرية (ADT) في لغة ص
# ═══════════════════════════════════════════

اطبع_سطر("=== اختبار التعداد الجبري (ADT) ===")

# === 1. تعريف تعداد جبري بسيط ===
تعداد شكل
    دائرة(نصف_القطر)
    مستطيل(عرض، ارتفاع)
    نقطة
نهاية

اطبع_سطر("تم تعريف تعداد شكل بنجاح")

# === 2. إنشاء قيم variant ===
متغير ش1 = شكل.دائرة(5)
متغير ش2 = شكل.مستطيل(10، 20)
متغير ش3 = شكل.نقطة

اطبع_سطر("ش1 (دائرة): " + ش1)
اطبع_سطر("ش2 (مستطيل): " + ش2)
اطبع_سطر("ش3 (نقطة): " + ش3)

# === 3. الوصول للحقول ===
اطبع_سطر("نصف القطر: " + ش1.نصف_القطر)
اطبع_سطر("العرض: " + ش2.عرض)
اطبع_سطر("الارتفاع: " + ش2.ارتفاع)

# === 4. فحص نوع العضو ===
اطبع_سطر("نوع ش1: " + ش1.__عضو__)
اطبع_سطر("تعداد ش1: " + ش1.__تعداد__)

# === 5. مطابقة الأنماط ===
طابق (ش1)
    عندما شكل.دائرة(نق):
        اطبع_سطر("دائرة بنصف قطر: " + نق)
    عندما شكل.مستطيل(ع، ا):
        اطبع_سطر("مستطيل: " + ع + " × " + ا)
    عندما شكل.نقطة:
        اطبع_سطر("نقطة")
نهاية

طابق (ش2)
    عندما شكل.دائرة(نق):
        اطبع_سطر("دائرة بنصف قطر: " + نق)
    عندما شكل.مستطيل(ع، ا):
        اطبع_سطر("مستطيل: " + ع + " × " + ا)
    عندما شكل.نقطة:
        اطبع_سطر("نقطة")
نهاية

طابق (ش3)
    عندما شكل.دائرة(نق):
        اطبع_سطر("دائرة بنصف قطر: " + نق)
    عندما شكل.مستطيل(ع، ا):
        اطبع_سطر("مستطيل: " + ع + " × " + ا)
    عندما شكل.نقطة:
        اطبع_سطر("نقطة")
نهاية

# === 6. تعداد جبري مع حالات متنوعة ===
تعداد نتيجة
    نجاح(قيمة)
    فشل(رسالة)
نهاية

متغير ن1 = نتيجة.نجاح(42)
متغير ن2 = نتيجة.فشل("خطأ في الشبكة")

طابق (ن1)
    عندما نتيجة.نجاح(ق):
        اطبع_سطر("نجاح: " + ق)
    عندما نتيجة.فشل(ر):
        اطبع_سطر("فشل: " + ر)
نهاية

طابق (ن2)
    عندما نتيجة.نجاح(ق):
        اطبع_سطر("نجاح: " + ق)
    عندما نتيجة.فشل(ر):
        اطبع_سطر("فشل: " + ر)
نهاية

# === 7. اختبار __جبري__ ===
اطبع_سطر("شكل جبري: " + شكل.__جبري__)
اطبع_سطر("عدد أعضاء شكل: " + شكل.__عدد__)

اطبع_سطر("=== انتهى الاختبار بنجاح ===")
