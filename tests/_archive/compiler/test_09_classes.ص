# اختبارات الأصناف والبرمجة الكائنية — 35 اختبار
# Class and OOP tests — 35 tests

# T01-T05: صنف بسيط

استورد نصوص

صنف شخص
    باني(اسم، عمر)
        هذا.اسم = اسم
        هذا.عمر = عمر
    نهاية
    
    دالة عرض()
        اطبع_سطر(هذا.اسم + " عمره " + هذا.عمر)
    نهاية
    
    دالة أكبر_من(آخر)
        ارجع هذا.عمر > آخر
    نهاية
نهاية

# T06-T10: صنف حساب
صنف حساب
    باني(رصيد)
        هذا.رصيد = رصيد
    نهاية
    
    دالة إيداع(مبلغ)
        هذا.رصيد += مبلغ
    نهاية
    
    دالة سحب(مبلغ)
        إذا (مبلغ <= هذا.رصيد)
            هذا.رصيد -= مبلغ
            ارجع صحيح
        نهاية
        ارجع خطأ
    نهاية
    
    دالة الرصيد()
        ارجع هذا.رصيد
    نهاية
نهاية

# T11-T15: صنف نقطة
صنف نقطة
    باني(س، ص)
        هذا.س = س
        هذا.ص = ص
    نهاية
    
    دالة مسافة()
        ارجع هذا.س * هذا.س + هذا.ص * هذا.ص
    نهاية
    
    دالة نقل(dx، dy)
        هذا.س += dx
        هذا.ص += dy
    نهاية
    
    دالة عرض()
        اطبع_سطر("(" + هذا.س + "، " + هذا.ص + ")")
    نهاية
نهاية

# T16-T20: صنف مستطيل
صنف مستطيل
    باني(عرض، ارتفاع)
        هذا.عرض = عرض
        هذا.ارتفاع = ارتفاع
    نهاية
    
    دالة مساحة()
        ارجع هذا.عرض * هذا.ارتفاع
    نهاية
    
    دالة محيط()
        ارجع 2 * (هذا.عرض + هذا.ارتفاع)
    نهاية
    
    دالة مربع()
        ارجع هذا.عرض == هذا.ارتفاع
    نهاية
نهاية

# T21-T25: صنف عداد
صنف عداد
    باني()
        هذا.قيمة = 0
    نهاية
    
    دالة زيادة()
        هذا.قيمة += 1
    نهاية
    
    دالة نقصان()
        هذا.قيمة -= 1
    نهاية
    
    دالة إعادة_ضبط()
        هذا.قيمة = 0
    نهاية
    
    دالة القيمة()
        ارجع هذا.قيمة
    نهاية
نهاية

# T26-T30: صنف مكدس
صنف مكدس
    باني()
        هذا.عناصر = []
        هذا.حجم = 0
    نهاية
    
    دالة ادفع(عنصر)
        أضف(هذا.عناصر, عنصر)
        هذا.حجم += 1
    نهاية
    
    دالة الحجم()
        ارجع هذا.حجم
    نهاية
    
    دالة فارغ()
        ارجع هذا.حجم == 0
    نهاية
نهاية

دالة رئيسية()
    # T01: إنشاء كائن
    متغير ش = جديد شخص("أحمد", 25)
    اطبع_سطر(ش.اسم)
    # T02: وصول للحقل
    اطبع_سطر(ش.عمر)
    # T03: استدعاء طريقة
    ش.عرض()
    # T04: تعديل حقل
    ش.عمر = 26
    اطبع_سطر(ش.عمر)
    # T05: طريقة بمقارنة
    اطبع_سطر(ش.أكبر_من(20))
    
    # T06: إنشاء حساب
    متغير ح = جديد حساب(1000)
    اطبع_سطر(ح.الرصيد())
    # T07: إيداع
    ح.إيداع(500)
    اطبع_سطر(ح.الرصيد())
    # T08: سحب ناجح
    اطبع_سطر(ح.سحب(200))
    # T09: رصيد بعد سحب
    اطبع_سطر(ح.الرصيد())
    # T10: سحب فاشل
    اطبع_سطر(ح.سحب(5000))
    
    # T11: نقطة
    متغير ن = جديد نقطة(3, 4)
    ن.عرض()
    # T12: مسافة
    اطبع_سطر(ن.مسافة())
    # T13: نقل
    ن.نقل(1, 1)
    ن.عرض()
    # T14: نقطة صفرية
    متغير صفرية = جديد نقطة(0, 0)
    اطبع_سطر(صفرية.مسافة())
    # T15: نقطة سالبة
    متغير سالبة = جديد نقطة(-3, -4)
    اطبع_سطر(سالبة.مسافة())
    
    # T16: مستطيل
    متغير م = جديد مستطيل(5, 3)
    اطبع_سطر(م.مساحة())
    # T17: محيط
    اطبع_سطر(م.محيط())
    # T18: ليس مربع
    اطبع_سطر(م.مربع())
    # T19: مربع حقيقي
    متغير مرب = جديد مستطيل(4, 4)
    اطبع_سطر(مرب.مربع())
    # T20: مساحة مربع
    اطبع_سطر(مرب.مساحة())
    
    # T21: عداد
    متغير ع = جديد عداد()
    اطبع_سطر(ع.القيمة())
    # T22: زيادة
    ع.زيادة()
    ع.زيادة()
    ع.زيادة()
    اطبع_سطر(ع.القيمة())
    # T23: نقصان
    ع.نقصان()
    اطبع_سطر(ع.القيمة())
    # T24: إعادة ضبط
    ع.إعادة_ضبط()
    اطبع_سطر(ع.القيمة())
    # T25: زيادة بعد ضبط
    ع.زيادة()
    اطبع_سطر(ع.القيمة())
    
    # T26: مكدس
    متغير مك = جديد مكدس()
    اطبع_سطر(مك.فارغ())
    # T27: إضافة للمكدس
    مك.ادفع(10)
    مك.ادفع(20)
    اطبع_سطر(مك.الحجم())
    # T28: ليس فارغ
    اطبع_سطر(مك.فارغ())
    # T29: إضافة أخرى
    مك.ادفع(30)
    اطبع_سطر(مك.الحجم())
    
    # T30: كائنات متعددة
    متغير ش1 = جديد شخص("سارة", 30)
    متغير ش2 = جديد شخص("خالد", 28)
    اطبع_سطر(ش1.اسم)
    اطبع_سطر(ش2.اسم)
    
    # T31: كائن في شرط
    إذا (مرب.مربع())
        اطبع_سطر("T31:هو_مربع")
    نهاية
    
    # T32: كائن في حلقة
    متغير عد = جديد عداد()
    متغير ي = 0
    بينما (ي < 10)
        عد.زيادة()
        ي += 1
    نهاية
    اطبع_سطر(عد.القيمة())
    
    # T33: استدعاء متسلسل
    متغير ن2 = جديد نقطة(1, 1)
    ن2.نقل(2, 3)
    ن2.نقل(1, 1)
    ن2.عرض()
    
    # T34: مقارنة كائنات
    متغير ح1 = جديد حساب(100)
    متغير ح2 = جديد حساب(200)
    إذا (ح2.الرصيد() > ح1.الرصيد())
        اطبع_سطر("T34:الثاني_أكبر")
    نهاية
    
    # T35: كائن معقد
    متغير مكدس2 = جديد مكدس()
    متغير ي2 = 1
    بينما (ي2 <= 5)
        مكدس2.ادفع(ي2 * 10)
        ي2 += 1
    نهاية
    اطبع_سطر(مكدس2.الحجم())
نهاية
