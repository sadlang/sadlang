# اختبارات الحالات الحدية — 25 اختبار
# Edge cases tests — 25 tests

دالة رئيسية()
    # T01: صفر
    اطبع_سطر("T01: " + 0)

    # T02: سالب
    اطبع_سطر("T02: " + (-1))

    # T03: عدد كبير
    اطبع_سطر("T03: " + 999999999)

    # T04: نص فارغ
    متغير فارغ = ""
    اطبع_سطر("T04: [" + فارغ + "]")

    # T05: مصفوفة فارغة
    متغير م_فارغة = []
    اطبع_سطر("T05: " + م_فارغة)

    # T06: مصفوفة عنصر واحد
    متغير م_واحد = [42]
    اطبع_سطر("T06: " + م_واحد)

    # T07: قسمة عادية
    اطبع_سطر("T07: " + (10 / 3))

    # T08: باقي القسمة
    اطبع_سطر("T08: " + (10 % 3))

    # T09: أس صفر
    اطبع_سطر("T09: " + (5 ** 0))

    # T10: صفر أس أي
    اطبع_سطر("T10: " + (0 ** 5))

    # T11: واحد أس كبير
    اطبع_سطر("T11: " + (1 ** 100))

    # T12: جمع عشري
    اطبع_سطر("T12: " + (0.1 + 0.2))

    # T13: ضرب عشري
    اطبع_سطر("T13: " + (0.5 * 4))

    # T14: نص طويل
    متغير طويل = "هذا نص طويل جداً يحتوي على كلمات كثيرة لاختبار معالجة النصوص الطويلة"
    اطبع_سطر("T14: " + طول(طويل))

    # T15: مصفوفة كبيرة
    متغير كبيرة = []
    متغير ي = 0
    بينما (ي < 100)
        كبيرة += [ي]
        ي += 1
    نهاية
    اطبع_سطر("T15 طول: " + طول(كبيرة))

    # T16: تسلسل نصي طويل
    متغير ن = ""
    ي = 0
    بينما (ي < 50)
        ن += "أ"
        ي += 1
    نهاية
    اطبع_سطر("T16 طول: " + طول(ن))

    # T17: حلقة صفر تكرارات
    متغير عدد = 0
    بينما (خطأ)
        عدد += 1
    نهاية
    اطبع_سطر("T17: " + عدد)

    # T18: شرط دائماً خطأ
    إذا (خطأ)
        اطبع_سطر("T18: لن يُطبع")
    وإلا
        اطبع_سطر("T18: صحيح")
    نهاية

    # T19: شرط دائماً صحيح
    إذا (صحيح)
        اطبع_سطر("T19: دائماً")
    نهاية

    # T20: مضروب صفر
    دالة مض(ن)
        إذا (ن <= 1)
            ارجع 1
        نهاية
        ارجع ن * مض(ن - 1)
    نهاية
    اطبع_سطر("T20 مض 0: " + مض(0))

    # T21: سلسلة مقارنات
    اطبع_سطر("T21: " + (1 == 1))
    اطبع_سطر("T21: " + (0 == 0))
    اطبع_سطر("T21: " + (-1 == -1))

    # T22: عمليات على لاشيء
    متغير لا = لاشيء
    اطبع_سطر("T22: " + لا)

    # T23: مصفوفة مختلطة الأنواع
    متغير مختلطة = [1, "نص", صحيح, 3.14, لاشيء]
    اطبع_سطر("T23: " + مختلطة)

    # T24: حلقة تكرار واحد
    متغير ي2 = 0
    بينما (ي2 < 1)
        اطبع_سطر("T24: تكرار واحد")
        ي2 += 1
    نهاية

    # T25: لكل على عنصر واحد
    لكل ع في [99]
        اطبع_سطر("T25: " + ع)
    نهاية
نهاية
