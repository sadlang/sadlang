# اختبارات الإغلاقات — 20 اختبار
# Closure tests — 20 tests

استورد رياضيات

دالة رئيسية()
    # T01: دالة مع متغير محلي
    دالة مضاعف()
        متغير م = 2
        ارجع لامدا س: س * م
    نهاية
    متغير ض = مضاعف()
    اطبع_سطر("مضاعف 5: " + ض(5))

    # T02: دالة مع متغير محلي 3
    دالة مثلث()
        متغير م = 3
        ارجع لامدا س: س * م
    نهاية
    متغير ث = مثلث()
    اطبع_سطر("مثلث 4: " + ث(4))

    # T03: إغلاق مع ثابت
    دالة أضف_ثابت(ق)
        ارجع لامدا س: س + ق
    نهاية
    متغير أضف5 = أضف_ثابت(5)
    اطبع_سطر("أضف5(10): " + أضف5(10))

    # T04: إغلاق مع ثابت آخر
    متغير أضف10 = أضف_ثابت(10)
    اطبع_سطر("أضف10(7): " + أضف10(7))

    # T05: دالة مع عداد
    دالة عداد_صانع()
        متغير عدد = 0
        دالة زد()
            عدد += 1
            ارجع عدد
        نهاية
        ارجع زد
    نهاية
    متغير عداد = عداد_صانع()
    اطبع_سطر("عداد1: " + عداد())
    اطبع_سطر("عداد2: " + عداد())
    اطبع_سطر("عداد3: " + عداد())

    # T06: عدادان مستقلان
    متغير عداد_أ = عداد_صانع()
    متغير عداد_ب = عداد_صانع()
    اطبع_سطر("أ1: " + عداد_أ())
    اطبع_سطر("ب1: " + عداد_ب())
    اطبع_سطر("أ2: " + عداد_أ())
    اطبع_سطر("ب2: " + عداد_ب())

    # T07: إغلاق مع ضرب
    دالة ضارب(م)
        ارجع لامدا س: س * م
    نهاية
    متغير ضرب2 = ضارب(2)
    متغير ضرب3 = ضارب(3)
    اطبع_سطر("ضرب2(7): " + ضرب2(7))
    اطبع_سطر("ضرب3(7): " + ضرب3(7))

    # T08: إغلاق متداخل
    دالة خارجية(أ)
        دالة داخلية(ب)
            ارجع أ + ب
        نهاية
        ارجع داخلية
    نهاية
    متغير جامع = خارجية(10)
    اطبع_سطر("جامع(5): " + جامع(5))
    اطبع_سطر("جامع(20): " + جامع(20))

    # T09: لامدا مباشرة
    متغير مربع = لامدا س: س * س
    اطبع_سطر("مربع 6: " + مربع(6))

    # T10: لامدا هوية
    متغير هوية = لامدا س: س
    اطبع_سطر("هوية 42: " + هوية(42))

    # T11: لامدا ثابتة
    متغير خمسة = لامدا: 5
    اطبع_سطر("خمسة: " + خمسة())

    # T12: لامدا مع مصفوفة  
    متغير أعداد = [1, 2, 3, 4, 5]
    متغير مضاعفة = لامدا س: س * 2
    متغير نتائج = []
    لكل ع في أعداد
        نتائج += [مضاعفة(ع)]
    نهاية
    اطبع_سطر("مضاعفات: " + نتائج)

    # T13: لامدا في شرط
    متغير اختبار = لامدا س: س > 0
    إذا (اختبار(5))
        اطبع_سطر("5 موجب")
    نهاية
    إذا (!اختبار(-3))
        اطبع_سطر("-3 سالب")
    نهاية

    # T14: لامدا جمع
    متغير جمع = لامدا أ، ب: أ + ب
    اطبع_سطر("جمع 3+4: " + جمع(3, 4))

    # T15: لامدا أكبر
    متغير أكبر = لامدا أ، ب: أ > ب ؟ أ : ب
    اطبع_سطر("أكبر 7,3: " + أكبر(7, 3))
    اطبع_سطر("أكبر 2,8: " + أكبر(2, 8))

    # T16: دالة تطبيق
    دالة طبق(دالة_ف، قيمة)
        ارجع دالة_ف(قيمة)
    نهاية
    اطبع_سطر("طبق مربع: " + طبق(مربع, 8))
    اطبع_سطر("طبق هوية: " + طبق(هوية, 99))

    # T17: لامدا نفي
    متغير نفي = لامدا س: -س
    اطبع_سطر("نفي 7: " + نفي(7))

    # T18: دالة مركبة
    دالة مركب(د1، د2)
        ارجع لامدا س: د1(د2(س))
    نهاية
    متغير مربع_ثم_مضاعف = مركب(مضاعفة, مربع)
    اطبع_سطر("مربع ثم مضاعف 3: " + مربع_ثم_مضاعف(3))

    # T19: إغلاق في حلقة
    متغير مجموع = 0
    متغير جامع_ل = لامدا س: س + 1
    متغير ي = 0
    بينما (ي < 5)
        مجموع += جامع_ل(ي)
        ي += 1
    نهاية
    اطبع_سطر("مجموع: " + مجموع)

    # T20: لامدا مع نص
    متغير تحية = لامدا اسم: "مرحباً " + اسم
    اطبع_سطر(تحية("علي"))
نهاية
