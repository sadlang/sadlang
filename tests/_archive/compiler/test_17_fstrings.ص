# اختبارات النصوص المنسقة — 20 اختبار
# F-string tests — 20 tests

دالة رئيسية()
    # T01: نص منسق بسيط — متغير رقمي
    متغير عمر = 25
    اطبع_سطر(نص_م"عمري ${عمر} سنة")

    # T02: نص منسق — متغير نصي
    متغير اسم = "أحمد"
    اطبع_سطر(نص_م"مرحباً ${اسم}")

    # T03: نص منسق — متغيرين
    اطبع_سطر(نص_م"اسمي ${اسم} وعمري ${عمر}")

    # T04: نص منسق — تعبير حسابي
    متغير أ = 5
    متغير ب = 3
    اطبع_سطر(نص_م"المجموع: ${أ + ب}")

    # T05: نص منسق — ضرب
    اطبع_سطر(نص_م"الضرب: ${أ * ب}")

    # T06: نص منسق — قسمة
    اطبع_سطر(نص_م"القسمة: ${أ - ب}")

    # T07: نص منسق في متغير
    متغير رسالة = نص_م"عدد: ${أ}"
    اطبع_سطر(رسالة)

    # T08: نص منسق — منطقي
    متغير فعال = صحيح
    اطبع_سطر(نص_م"الحالة: ${فعال}")

    # T09: نص منسق مع نص ثابت
    اطبع_سطر(نص_م"القيمة هي ${42}")

    # T10: نص منسق — عمليات على متغيرات
    متغير سعر = 100
    متغير خصم = 20
    اطبع_سطر(نص_م"السعر بعد الخصم: ${سعر - خصم}")

    # T11: نص منسق في حلقة
    متغير ي = 0
    بينما (ي < 3)
        اطبع_سطر(نص_م"العدد: ${ي}")
        ي += 1
    نهاية

    # T12: نص منسق في شرط
    متغير درجة = 85
    إذا (درجة > 80)
        اطبع_سطر(نص_م"درجتك ${درجة} — ممتاز")
    نهاية

    # T13: نص منسق مع نصوص
    متغير مدينة = "القاهرة"
    متغير بلد = "مصر"
    اطبع_سطر(نص_م"${مدينة}، ${بلد}")

    # T14: نص منسق مع أرقام كبيرة
    متغير كبير = 1000000
    اطبع_سطر(نص_م"عدد كبير: ${كبير}")

    # T15: نص منسق — صفر
    متغير صفر = 0
    اطبع_سطر(نص_م"صفر: ${صفر}")

    # T16: نص منسق — سالب
    متغير سالب = -10
    اطبع_سطر(نص_م"سالب: ${سالب}")

    # T17: نص منسق متعدد التعبيرات
    متغير س2 = 10
    متغير ص2 = 20
    اطبع_سطر(نص_م"س=${س2}، ص=${ص2}، مجموع=${س2 + ص2}")

    # T18: نص منسق — تعبير مقارنة
    اطبع_سطر(نص_م"هل أ > ب؟ ${أ > ب}")

    # T19: نص منسق فارغ جزئياً
    اطبع_سطر(نص_م"قيمة: ${أ}")

    # T20: نص منسق مع عشري
    متغير عشري = 3.14
    اطبع_سطر(نص_م"باي: ${عشري}")
نهاية
