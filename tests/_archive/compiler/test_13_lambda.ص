# اختبارات اللامدا — 25 اختبار
# Lambda tests — 25 tests

دالة رئيسية()
    # T01: لامدا بسيطة
    متغير مضاعف = لامدا س: س * 2
    اطبع_سطر(مضاعف(5))
    # T02: لامدا ثابتة
    متغير دائمة = لامدا: 42
    اطبع_سطر(دائمة())
    # T03: لامدا مربع
    متغير مربع = لامدا س: س * س
    اطبع_سطر(مربع(7))
    # T04: لامدا جمع
    متغير جمع = لامدا أ، ب: أ + ب
    اطبع_سطر(جمع(3, 4))
    # T05: لامدا طرح
    متغير طرح = لامدا أ، ب: أ - ب
    اطبع_سطر(طرح(10, 3))
    # T06: لامدا مع مقارنة
    متغير أكبر = لامدا أ، ب: أ > ب
    اطبع_سطر(أكبر(5, 3))
    # T07: لامدا مع ثلاث معاملات
    متغير مجموع = لامدا أ، ب، ج: أ + ب + ج
    اطبع_سطر(مجموع(1, 2, 3))
    # T08: لامدا هوية
    متغير هوية = لامدا س: س
    اطبع_سطر(هوية(99))
    # T09: لامدا سالب
    متغير سالب = لامدا س: -س
    اطبع_سطر(سالب(7))
    # T10: لامدا مع عملية مركبة
    متغير مركب = لامدا س: س * 2 + 1
    اطبع_سطر(مركب(5))
    # T11: تمرير لامدا كمعامل
    دالة طبق(دالة_ف، قيمة)
        ارجع دالة_ف(قيمة)
    نهاية
    اطبع_سطر(طبق(لامدا س: س * 3, 7))
    # T12: لامدا في متغير ثم استدعاء
    متغير ف = لامدا س: س + 100
    متغير نتيجة = ف(5)
    اطبع_سطر(نتيجة)
    # T13: لامدا مع صفر
    اطبع_سطر(مضاعف(0))
    # T14: لامدا مع سالب
    اطبع_سطر(مربع(-3))
    # T15: لامدا مع عدد كبير
    اطبع_سطر(مضاعف(500))
    # T16: استدعاء متعدد لنفس اللامدا
    اطبع_سطر(مربع(1))
    اطبع_سطر(مربع(2))
    اطبع_سطر(مربع(3))
    # T19: لامدا أقصى
    متغير أقصى = لامدا أ، ب: أ > ب ؟ أ : ب
    اطبع_سطر(أقصى(10, 20))
    # T20: لامدا أدنى
    متغير أدنى = لامدا أ، ب: أ < ب ؟ أ : ب
    اطبع_سطر(أدنى(10, 20))
    # T21: لامدا باقي
    متغير باقي = لامدا أ، ب: أ % ب
    اطبع_سطر(باقي(17, 5))
    # T22: لامدا متداخلة الاستدعاء
    اطبع_سطر(جمع(مربع(3), مربع(4)))
    # T23: لامدا في شرط
    إذا (أكبر(10, 5))
        اطبع_سطر("T23:الأول_أكبر")
    نهاية
    # T24: لامدا في حلقة
    متغير مج = 0
    متغير ي = 1
    بينما (ي <= 5)
        مج += مضاعف(ي)
        ي += 1
    نهاية
    اطبع_سطر(مج)
    # T25: لامدا نصية
    متغير تحية = لامدا اسم: "مرحبا " + اسم
    اطبع_سطر(تحية("أحمد"))
نهاية
