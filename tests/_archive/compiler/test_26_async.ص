# اختبارات غير متزامن/انتظار — 20 اختبار
# Async/Await tests — 20 tests

# T01-T10: دوال غير متزامنة بسيطة
غير_متزامن دالة جلب_رقم()
    ارجع 42
نهاية

غير_متزامن دالة جلب_نص()
    ارجع "مرحبا"
نهاية

غير_متزامن دالة جمع_غير_متزامن(أ، ب)
    ارجع أ + ب
نهاية

غير_متزامن دالة مضاعف_غير_متزامن(ن)
    ارجع ن * 2
نهاية

غير_متزامن دالة مربع_غير_متزامن(ن)
    ارجع ن * ن
نهاية

غير_متزامن دالة تحية(اسم)
    ارجع "مرحباً " + اسم
نهاية

غير_متزامن دالة منطقي_غير_متزامن(ق)
    ارجع ق > 0
نهاية

غير_متزامن دالة تسلسل()
    متغير أ = انتظر جلب_رقم()
    متغير ب = انتظر مضاعف_غير_متزامن(أ)
    ارجع ب
نهاية

غير_متزامن دالة حساب_معقد(س)
    متغير م = انتظر مربع_غير_متزامن(س)
    متغير ن = انتظر مضاعف_غير_متزامن(م)
    ارجع ن
نهاية

غير_متزامن دالة سلسلة_نصية()
    متغير أ = انتظر تحية("أحمد")
    متغير ب = انتظر تحية("سارة")
    ارجع أ + " و " + ب
نهاية

دالة رئيسية()
    # T01: دالة غير متزامنة تُرجع رقم
    اطبع_سطر("T01: " + انتظر جلب_رقم())

    # T02: دالة غير متزامنة تُرجع نص
    اطبع_سطر("T02: " + انتظر جلب_نص())

    # T03: جمع غير متزامن
    اطبع_سطر("T03: " + انتظر جمع_غير_متزامن(10, 20))

    # T04: مضاعف غير متزامن
    اطبع_سطر("T04: " + انتظر مضاعف_غير_متزامن(25))

    # T05: مربع غير متزامن
    اطبع_سطر("T05: " + انتظر مربع_غير_متزامن(7))

    # T06: تحية غير متزامنة
    اطبع_سطر("T06: " + انتظر تحية("علي"))

    # T07: منطقي غير متزامن — صحيح
    اطبع_سطر("T07: " + انتظر منطقي_غير_متزامن(5))

    # T08: منطقي غير متزامن — خطأ
    اطبع_سطر("T08: " + انتظر منطقي_غير_متزامن(-3))

    # T09: تسلسل انتظارات
    اطبع_سطر("T09: " + انتظر تسلسل())

    # T10: حساب معقد
    اطبع_سطر("T10: " + انتظر حساب_معقد(3))

    # T11: سلسلة نصية
    اطبع_سطر("T11: " + انتظر سلسلة_نصية())

    # T12: انتظار في متغير
    متغير نتيجة = انتظر جلب_رقم()
    اطبع_سطر("T12: " + نتيجة)

    # T13: انتظار مع عملية حسابية
    متغير ر = انتظر جمع_غير_متزامن(100, 200)
    اطبع_سطر("T13: " + ر)

    # T14: انتظار في شرط
    متغير ق = انتظر منطقي_غير_متزامن(10)
    إذا (ق)
        اطبع_سطر("T14: موجب")
    نهاية

    # T15: عدة انتظارات
    متغير أ = انتظر جلب_رقم()
    متغير ب = انتظر مضاعف_غير_متزامن(أ)
    متغير ج = انتظر مربع_غير_متزامن(ب)
    اطبع_سطر("T15: " + ج)

    # T16: انتظار نص ومطابقة
    متغير نص = انتظر جلب_نص()
    إذا (نص == "مرحبا")
        اطبع_سطر("T16: تطابق")
    نهاية

    # T17: تحيات متعددة
    اطبع_سطر("T17: " + انتظر تحية("فاطمة"))
    اطبع_سطر("T17: " + انتظر تحية("خالد"))

    # T18: حساب في حلقة
    متغير مج = 0
    متغير ي = 1
    بينما (ي <= 5)
        مج += انتظر مربع_غير_متزامن(ي)
        ي += 1
    نهاية
    اطبع_سطر("T18 مجموع المربعات: " + مج)

    # T19: انتظار + ثلاثي
    متغير حالة = انتظر منطقي_غير_متزامن(7) ؟ "نعم" : "لا"
    اطبع_سطر("T19: " + حالة)

    # T20: جمع نتائج غير متزامنة
    متغير ن1 = انتظر جلب_رقم()
    متغير ن2 = انتظر جمع_غير_متزامن(ن1, 8)
    اطبع_سطر("T20: " + ن2)
نهاية
