# ======================================================================
# test_pointers_04.s - اختبار المؤشرات الفارغة
# Stage 1 Test: Null Pointers
# ======================================================================
# الوصف / Description:
#   اختبار المؤشرات الفارغة والتحقق منها
#   Test null pointers and checking
#
# Expected Output:
#   NULL
#   NOT_NULL
# ======================================================================

دالة main()
    مؤشر<رقم> ptr = فارغ
    
    # التحقق من المؤشر الفارغ / Check null pointer
    إذا (ptr == فارغ)
        اطبع("NULL")
    نهاية
    
    رقم x = 42
    ptr = &x
    
    # التحقق من المؤشر غير الفارغ / Check non-null pointer
    إذا (ptr != فارغ)
        اطبع("NOT_NULL")
    نهاية
نهاية
