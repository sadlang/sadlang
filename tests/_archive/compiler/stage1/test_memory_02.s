# ======================================================================
# test_memory_02.s - اختبار تخصيص الذاكرة على Heap
# Stage 1 Test: Heap Memory Allocation
# ======================================================================
# الوصف / Description:
#   اختبار تخصيص وتحرير الذاكرة على الـ Heap
#   Test heap memory allocation and deallocation
#
# Expected Output:
#   42
#   SUCCESS
# ======================================================================

دالة main()
    # تخصيص ذاكرة على الـ Heap
    مؤشر<رقم> ptr = خصص(sizeof(رقم))
    
    # التحقق من نجاح التخصيص
    إذا (ptr != فارغ)
        *ptr = 42
        اطبع(*ptr)  # Expected: 42
        
        # تحرير الذاكرة
        حرر(ptr)
        اطبع("SUCCESS")
    نهاية
نهاية
