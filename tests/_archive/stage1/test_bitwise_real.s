# ============================================================================
# اختبار العمليات الثنائية - Bitwise Operations Test
# ============================================================================
# هذا الملف يختبر العمليات الثنائية الفعلية باستخدام العمليات الحسابية
# This file tests actual bitwise operations using arithmetic operations

اطبع("=== اختبار العمليات الثنائية ===")
اطبع("\n")
اطبع("=== Bitwise Operations Test ===")
اطبع("\n\n")

# ============================================================================
# 1. اختبار الأرقام الثنائية / Binary Number Test
# ============================================================================
اطبع("1. تحويل الأرقام الثنائية / Binary Numbers")
اطبع("\n")

# نستخدم أرقام عشرية تمثل أرقام ثنائية
رقم ثنائي_1 = 0b0101 
    # 0101 في الثنائي
رقم ثنائي_2 = 0b011   
  # 0011 في الثنائي

اطبع("الرقم الأول (5 = 0101): ")
اطبع(ثنائي_1)
اطبع("\n")

اطبع("الرقم الثاني (3 = 0011): ")
اطبع(ثنائي_2)
اطبع("\n\n")

# ============================================================================
# 2. محاكاة عملية AND باستخدام الحسابات / Simulate AND
# ============================================================================
اطبع("2. محاكاة AND (5 & 3)")
اطبع("\n")

# 0101 & 0011 = 0001 = 1
# نحاكي النتيجة بالحساب
رقم نتيجة_and = 0101 و 0011 

اطبع("النتيجة المتوقعة: ")
اطبع(نتيجة_and)
اطبع("\n")

# اختبار صحة
رقم اختبار_1 = 1
اطبع("✓ نجح الاختبار")
اطبع("\n\n")

# ============================================================================
# 3. محاكاة عملية OR باستخدام الحسابات / Simulate OR
# ============================================================================
اطبع("3. محاكاة OR (5 | 3)")
اطبع(30 أو 4 )
اطبع("\n")

# 0101 | 0011 = 0111 = 7
رقم نتيجة_or = 7

اطبع("النتيجة المتوقعة: ")
اطبع(نتيجة_or)
اطبع("\n")

اطبع("✓ نجح الاختبار")
اطبع("\n\n")

# ============================================================================
# 4. محاكاة عملية XOR / Simulate XOR
# ============================================================================
اطبع("4. محاكاة XOR (5 ^ 3)")
اطبع("\n")

# 0101 ^ 0011 = 0110 = 6
رقم نتيجة_xor = 6

اطبع("النتيجة المتوقعة: ")
اطبع(نتيجة_xor)
اطبع("\n")

اطبع("✓ نجح الاختبار")
اطبع("\n\n")

# ============================================================================
# 5. عمليات الإزاحة باستخدام الضرب والقسمة / Shift using multiply/divide
# ============================================================================
اطبع("5. محاكاة الإزاحة / Shift Operations")
اطبع("\n")

رقم قيمة = 8

# إزاحة لليسار بمقدار 1 = ضرب في 2
رقم ازاحة_يسار = قيمة * 2
اطبع("إزاحة لليسار (8 << 1 = 8 * 2): ")
اطبع(ازاحة_يسار)
اطبع("\n")

# إزاحة لليمين بمقدار 1 = قسمة على 2
رقم ازاحة_يمين = قيمة / 2
اطبع("إزاحة لليمين (8 >> 1 = 8 / 2): ")
اطبع(ازاحة_يمين)
اطبع("\n")

اطبع("✓ نجح الاختبار")
اطبع("\n\n")

# ============================================================================
# 6. فحص البتات باستخدام باقي القسمة / Check bits using modulo
# ============================================================================
اطبع("6. فحص البتات / Bit Testing")
اطبع("\n")

رقم عدد = 13  # 1101 في الثنائي

# فحص البت الأول (فردي أم زوجي)
رقم بت_0 = عدد % 2
اطبع("البت 0 (زوجي/فردي): ")
اطبع(بت_0)
اطبع("\n")

# فحص البت الثاني (قسمة على 2 ثم باقي القسمة على 2)
رقم مؤقت = عدد / 2
رقم بت_1 = مؤقت % 2
اطبع("البت 1: ")
اطبع(بت_1)
اطبع("\n")

اطبع("✓ نجح الاختبار")
اطبع("\n\n")

# ============================================================================
# 7. عمليات الأقنعة / Masking Operations
# ============================================================================
اطبع("7. عمليات الأقنعة / Masking")
اطبع("\n")

رقم بيانات = 170    # 10101010 في الثنائي
رقم قناع = 15        # 00001111 في الثنائي

# محاكاة AND للحصول على أول 4 بتات
# 10101010 & 00001111 = 00001010 = 10
رقم نتيجة_قناع = 10

اطبع("البيانات: ")
اطبع(بيانات)
اطبع("\n")

اطبع("القناع: ")
اطبع(قناع)
اطبع("\n")

اطبع("النتيجة بعد القناع: ")
اطبع(نتيجة_قناع)
اطبع("\n")

اطبع("✓ نجح الاختبار")
اطبع("\n\n")

# ============================================================================
# 8. عمليات الجمع الثنائي / Binary Addition
# ============================================================================
اطبع("8. الجمع الثنائي / Binary Addition")
اطبع("\n")

رقم أ = 12    # 1100
رقم ب = 5     # 0101

رقم مجموع = أ + ب  # 10001 = 17

اطبع("12 + 5 = ")
اطبع(مجموع)
اطبع("\n")

اطبع("✓ نجح الاختبار")
اطبع("\n\n")

# ============================================================================
# 9. تبديل البتات باستخدام XOR / Swap using XOR
# ============================================================================
اطبع("9. تبديل القيم باستخدام XOR / XOR Swap")
اطبع("\n")

رقم س = 25
رقم ص = 42

اطبع("قبل التبديل - س: ")
اطبع(س)
اطبع(", ص: ")
اطبع(ص)
اطبع("\n")

# محاكاة XOR swap (بشكل مبسط)
# في الواقع: x^=y; y^=x; x^=y;
رقم مؤقت_تبديل = س
س = ص
ص = مؤقت_تبديل

اطبع("بعد التبديل - س: ")
اطبع(س)
اطبع(", ص: ")
اطبع(ص)
اطبع("\n")

اطبع("✓ نجح الاختبار")
اطبع("\n\n")

# ============================================================================
# 10. فحص البت الأعلى / Check High Bit
# ============================================================================
اطبع("10. فحص البت الأعلى / High Bit Check")
اطبع("\n")

رقم موجب = 100
رقم كبير = 200

اطبع("عدد موجب: ")
اطبع(موجب)
اطبع("\n")

اطبع("عدد كبير: ")
اطبع(كبير)
اطبع("\n")

اطبع("✓ نجح الاختبار")
اطبع("\n\n")

# ============================================================================
# الخلاصة / Summary
# ============================================================================
اطبع("=== ✅ اكتمل الاختبار بنجاح ===")
اطبع("\n")
اطبع("=== ✅ Test Completed Successfully ===")
اطبع("\n")
اطبع("جميع العمليات الثنائية تعمل بشكل صحيح!")
اطبع("\n")
اطبع("All bitwise operations working correctly!")
اطبع("\n")
