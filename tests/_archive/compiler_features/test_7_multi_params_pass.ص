# ============================================================================
# اختبار المرحلة 7 — قالب بمعاملَي نوع مستقلَّين (حالة النجاح)
# Phase 7 Test — Template with two independent type parameters (pass case)
#
# (AR) القالب يملك معاملَين: مفتاح (يتطلب قابل_للتجزئة) و قيمة (يتطلب قابل_للعرض).
#      نستدعيه بـ ("اسم"، 42) — يُستدلّ مفتاح=نص و قيمة=رقم.
#      نص يحقق قابل_للتجزئة، ورقم يحقق قابل_للعرض — يجب أن ينجح (EXIT=0).
#
# (EN) Template has two type params: مفتاح (requires قابل_للتجزئة) and
#      قيمة (requires قابل_للعرض).
#      Called with ("name", 42) — inferred: مفتاح=نص, قيمة=رقم.
#      نص satisfies قابل_للتجزئة; رقم satisfies قابل_للعرض → must pass (EXIT=0).
# ============================================================================

# (AR) قالب بمعاملَين مستقلَّين — كلٌّ بقيد خاص
# (EN) Template with two independent type params — each with its own constraint

قالب<نوع مفتاح: قابل_للتجزئة، نوع قيمة: قابل_للعرض>
دالة أدرج(مفتاح م، قيمة ق)
    اطبع_سطر(ق)
نهاية

دالة رئيسية()
    # (AR) يُستدلّ: مفتاح=نص ← يحقق قابل_للتجزئة
    #              قيمة=رقم  ← يحقق قابل_للعرض
    # (EN) Inferred: مفتاح=نص satisfies قابل_للتجزئة
    #               قيمة=رقم  satisfies قابل_للعرض
    أدرج("اسم"، 42)
    ارجع 0
نهاية
