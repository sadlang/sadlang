# ============================================================================
# اختبار Phase 5b — التحقق من اكتمال تنفيذ السمة (يجب أن يفشل)
# Test Phase 5b — impl completeness check (must fail compilation)
# ============================================================================

سمة قابل_للوصف
   دالة وصف()
   دالة عرض_مختصر()
نهاية

صنف نقطة
   متغير عام س
   باني(س)
      هذا.س = س
   نهاية
نهاية

# (AR) ينقص هذا التنفيذ دالة "عرض_مختصر" — يجب أن يكتشفها فاحص الأنواع.
# (EN) This impl is missing "عرض_مختصر" — type checker must catch it.
نفّذ قابل_للوصف لـ نقطة
   دالة وصف()
      ارجع "نقطة(" + هذا.س + ")"
   نهاية
نهاية

دالة رئيسية()
   متغير ن = نقطة(5)
   اطبع_سطر(ن.وصف())
نهاية
