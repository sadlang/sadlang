# ============================================================================
# اختبار Phase 5c — استدلال نوعي للأصناف (حالة فشل القيد)
# Test Phase 5c — class-type inference (constraint violation)
#
# (AR) `نقطة` لا تنفّذ `قابل_للترتيب`. نمرّرها إلى قالب يطلبه.
#      يجب أن يصدر المُحلِّل خطأ "النوع 'نقطة' لا يحقق القيد
#      'قابل_للترتيب' للمعامل 'T'".
# (EN) `نقطة` does NOT implement `قابل_للترتيب`. We pass it to
#      a template requiring it. The checker must report:
#      "Type 'نقطة' does not satisfy constraint 'قابل_للترتيب'".
# ============================================================================

صنف نقطة
   متغير عام س
   متغير عام ص
   باني(س، ص)
      هذا.س = س
      هذا.ص = ص
   نهاية
نهاية

قالب<نوع T: قابل_للترتيب> دالة أكبر(T أ، T ب)
   ارجع أ
نهاية

دالة رئيسية()
   متغير ن1 = نقطة(3، 4) جديد
   متغير ن2 = نقطة(1، 2) جديد
   متغير ن3 = أكبر(ن1، ن2)
   اطبع_سطر(ن3.س)
نهاية
