# =============================================================================
# اختبار قيود القوالب — حالة منتهَكة
# Template Constraints Test — Violated Case
# =============================================================================
# يجب أن يفشل (أو يُصدر خطأ دلالي) لأن `نقطة` لا تنفذ `قابل_للمقارنة`.
# Should fail (or emit semantic error) because `نقطة` doesn't impl `قابل_للمقارنة`.

صنف نقطة
    متغير عام س
    متغير عام ص
    باني(س، ص)
        هذا.س = س
        هذا.ص = ص
    نهاية
نهاية

قالب<نوع T: قابل_للمقارنة>
دالة أكبر(T أ، T ب)
    ارجع أ
نهاية

دالة رئيسية()
    متغير أ = نقطة(1، 2)
    متغير ب = نقطة(3، 4)
    متغير ن = أكبر<نقطة>(أ، ب)
نهاية
