# ============================================================================
# اختبار المرحلة 8 — فحص نوع الإرجاع في قالب دالة (حالة النجاح)
# Phase 8 Test — Template function return type check (pass case)
#
# (AR) قالب دالة يُصرِّح عن نوع إرجاع رقم باستخدام كلمة "ترجع".
#      جسم الدالة يُرجع قيمة رقمية صحيحة — يجب أن يجتاز الفحص (EXIT=0).
#
# (EN) Template function declares return type رقم using "ترجع" keyword.
#      Function body returns a valid numeric value.
#      Type check must succeed (EXIT=0).
# ============================================================================

# (AR) قالب دالة مع نوع إرجاع صريح ": رقم"
# (EN) Template function with explicit return type ": رقم"

قالب<نوع T: قابل_للمقارنة>
دالة أكبر_رقم(T أ، T ب) ترجع رقم
    إذا (أ > ب)
        ارجع 1
    وإلا
        ارجع 0
    نهاية
نهاية

دالة رئيسية()
    # (AR) استدعاء بـ T=رقم — نوع الإرجاع رقم ← جسم يُرجع رقم → OK
    # (EN) Called with T=رقم — return type رقم, body returns رقم → OK
    متغير ن = أكبر_رقم(10، 20)
    اطبع_سطر(ن)
    ارجع 0
نهاية
