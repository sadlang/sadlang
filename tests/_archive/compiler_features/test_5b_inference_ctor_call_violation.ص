# ============================================================================
# اختبار انحدار Phase 5b — استدلال نوعي مع استدعاء باني مباشر (يجب أن يفشل)
# Regression Phase 5b — inferred type from direct constructor calls (must fail)
# ============================================================================

سمة قابل_للترتيب_المخصص
نهاية

قالب<نوع T: قابل_للترتيب_المخصص> دالة رتّب(T أ، T ب)
   ارجع أ
نهاية

صنف نقطة
   متغير عام س
   باني(س)
      هذا.س = س
   نهاية
نهاية

dالة رئيسية()
   # (AR) هنا لا يوجد متغير وسيط: النوع T يُستدل مباشرة من استدعاء الباني.
   # (EN) No intermediate variable: T is inferred directly from constructor calls.
   #      نقطة لا تنفذ السمة، لذا يجب أن يفشل فحص الأنواع.
   متغير ر = رتّب(نقطة(1)، نقطة(2))
   اطبع_سطر("ok")
نهاية
