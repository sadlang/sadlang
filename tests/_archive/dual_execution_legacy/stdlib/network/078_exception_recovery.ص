# @description استخدام الأصناف بعد حالة الخطأ — الاستدامة
# @expected صحيح\nصحيح\nصحيح
# @skip_compiler

استورد شبكات

متغير م = متصفح_ويب()
م.اضبط_المهلة(100)

# طلب أول يفشل
م.استرجع("http://127.0.0.1:1/a")
اطبع_سطر(م.هل_صالح())

# طلب ثانٍ يفشل — العميل ما زال يعمل
م.استرجع("http://127.0.0.1:1/b")
اطبع_سطر(م.هل_صالح())

# طلب ثالث
م.استرجع("http://127.0.0.1:1/c")
اطبع_سطر(م.هل_صالح())

م.أغلق()
