# اختبار Future/Promise - Future/Promise Test
# يختبر النظام الأساسي للعمليات غير المتزامنة
# Tests basic async operations system

# دالة غير متزامنة بسيطة
غير_متزامن دالة حساب_مربع(رقم ن)
    اطبع("جاري حساب مربع", ن)
    إرجاع ن * ن
نهاية

# دالة غير متزامنة مع await
غير_متزامن دالة معالجة_أرقام()
    اطبع_سطر("بدء معالجة الأرقام...")
    
    # استدعاء دوال غير متزامنة
    نص نتيجة1 = انتظر حساب_مربع(5)
    اطبع("نتيجة 1:", نتيجة1)
    
    نص نتيجة2 = انتظر حساب_مربع(10)
    اطبع("نتيجة 2:", نتيجة2)
    
    إرجاع "تمت المعالجة"
نهاية

# الدالة الرئيسية
دالة رئيسية()
    اطبع_سطر("=== اختبار Future/Promise ===")
    
    نص نتيجة_نهائية = معالجة_أرقام()
    اطبع_سطر("النتيجة النهائية:", نتيجة_نهائية)
    
    اطبع_سطر("=== انتهى الاختبار ===")
نهاية
