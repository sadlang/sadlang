# (AR) اختبار استخدام await في دالة غير متزامنة
# (EN) Test using await in async function

# (AR) دالة غير متزامنة مع await
# (EN) Async function with await
غير_متزامن دالة معالجة_البيانات() 
    اطبع("بدء المعالجة...")
    نص بيانات = انتظر جلب_البيانات()
    اطبع("تمت المعالجة:", بيانات)
    ارجع بيانات
نهاية

غير_متزامن دالة جلب_البيانات() 
    اطبع("جاري جلب البيانات...")
    إرجاع "بيانات جاهزة"
نهاية

# (AR) استدعاء الدالة
# (EN) Call the function
دالة رئيسية ()
نص نتيجة = معالجة_البيانات()
اطبع("النتيجة:", نتيجة)
نهاية 