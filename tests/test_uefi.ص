# ===================================================================
# اختبار شامل لبروتوكول الإقلاع الموحد UEFI
# Comprehensive UEFI Boot Protocol Test
# ===================================================================
# هذا الملف يختبر جميع وظائف وحدة UEFI:
#   1. التهيئة والتحكم
#   2. إدارة الذاكرة (صفحات + كتل + خريطة)
#   3. بروتوكول الرسوميات GOP
#   4. خدمات وقت التشغيل (وقت + متغيرات NVRAM)
#   5. نظام الملفات (فتح + قراءة + كتابة + إغلاق)
#   6. بروتوكولات ومعلومات النظام
#   7. التقارير الشاملة
# ===================================================================

متغير نجاح = 0
متغير فشل = 0
متغير إجمالي = 0

دالة اختبار(الاسم، النتيجة، المتوقع)
    إجمالي = إجمالي + 1
    إذا (النتيجة == المتوقع)
        نجاح = نجاح + 1
        اطبع_سطر("✓ " + الاسم)
    وإلا
        فشل = فشل + 1
        اطبع_سطر("✗ " + الاسم + " (متوقع: " + المتوقع + "، حصلنا: " + النتيجة + ")")
    نهاية
نهاية

دالة اختبار_ليس_فارغ(الاسم، النتيجة)
    إجمالي = إجمالي + 1
    إذا (النتيجة != "" && النتيجة != 0)
        نجاح = نجاح + 1
        اطبع_سطر("✓ " + الاسم)
    وإلا
        فشل = فشل + 1
        اطبع_سطر("✗ " + الاسم + " (القيمة فارغة)")
    نهاية
نهاية

اطبع_سطر("╔══════════════════════════════════════════════════════╗")
اطبع_سطر("║   اختبار شامل لبروتوكول الإقلاع الموحد UEFI        ║")
اطبع_سطر("║   Comprehensive UEFI Boot Protocol Test             ║")
اطبع_سطر("╚══════════════════════════════════════════════════════╝")
اطبع_سطر("")

# ===================================================================
# القسم 1: التهيئة والتحكم
# ===================================================================
اطبع_سطر("═══ القسم 1: التهيئة والتحكم ═══")

# اختبار 1.1: قبل التهيئة
متغير حالة_قبل = uefi_is_initialized()
اختبار("1.1 UEFI غير مهيأ مبدئياً", حالة_قبل, 0)

# اختبار 1.2: تهيئة UEFI
متغير نتيجة_تهيئة = uefi_تهيئة()
اختبار("1.2 تهيئة UEFI بنجاح", نتيجة_تهيئة, 0)

# اختبار 1.3: التحقق من التهيئة
متغير حالة_بعد = uefi_هل_مهيأ()
اختبار("1.3 UEFI مهيأ الآن", حالة_بعد, 1)

# اختبار 1.4: خدمات الإقلاع لم تنتهِ بعد
متغير حالة_إقلاع = uefi_خدمات_إقلاع_منتهية()
اختبار("1.4 خدمات الإقلاع لم تنتهِ", حالة_إقلاع, 0)

اطبع_سطر("")

# ===================================================================
# القسم 2: إدارة الذاكرة
# ===================================================================
اطبع_سطر("═══ القسم 2: إدارة الذاكرة ═══")

# اختبار 2.1: تخصيص صفحات
متغير عنوان_صفحات = uefi_تخصيص_صفحات(0, 7, 4)
اختبار_ليس_فارغ("2.1 تخصيص 4 صفحات ذاكرة", عنوان_صفحات)

# اختبار 2.2: تخصيص كتلة
متغير عنوان_كتلة = uefi_تخصيص_كتلة(7, 1024)
اختبار_ليس_فارغ("2.2 تخصيص كتلة 1024 بايت", عنوان_كتلة)

# اختبار 2.3: تحرير كتلة
متغير نتيجة_تحرير = uefi_تحرير_كتلة(عنوان_كتلة)
اختبار("2.3 تحرير الكتلة بنجاح", نتيجة_تحرير, 0)

# اختبار 2.4: تحرير صفحات
متغير نتيجة_تحرير_ص = uefi_تحرير_صفحات(عنوان_صفحات, 4)
اختبار("2.4 تحرير الصفحات بنجاح", نتيجة_تحرير_ص, 0)

# اختبار 2.5: خريطة الذاكرة
متغير نتيجة_خريطة = uefi_خريطة_ذاكرة()
اختبار_ليس_فارغ("2.5 الحصول على خريطة الذاكرة", نتيجة_خريطة)

# اختبار 2.6: مفتاح خريطة الذاكرة
متغير مفتاح = uefi_مفتاح_خريطة()
اختبار_ليس_فارغ("2.6 الحصول على مفتاح الخريطة", مفتاح)

# اختبار 2.7: إجمالي الذاكرة المتاحة
متغير إجمالي_ذاكرة = uefi_ذاكرة_متاحة()
اختبار_ليس_فارغ("2.7 الذاكرة المتاحة > 0", إجمالي_ذاكرة)

اطبع_سطر("")

# ===================================================================
# القسم 3: بروتوكول الرسوميات GOP
# ===================================================================
اطبع_سطر("═══ القسم 3: بروتوكول الرسوميات GOP ═══")

# اختبار 3.1: تهيئة GOP
متغير نتيجة_gop = uefi_تهيئة_رسوميات()
اختبار("3.1 تهيئة GOP بنجاح", نتيجة_gop, 0)

# اختبار 3.2: عدد الأوضاع
متغير عدد_أوضاع = uefi_عدد_أوضاع_رسوميات()
اختبار_ليس_فارغ("3.2 عدد أوضاع GOP > 0", عدد_أوضاع)

# اختبار 3.3: الوضع الحالي
متغير وضع_حالي = uefi_وضع_رسوميات_حالي()
اختبار("3.3 الوضع الحالي = 0 (640x480)", وضع_حالي, 0)

# اختبار 3.4: استعلام وضع
متغير معلومات_وضع = uefi_استعلام_وضع(0)
اختبار_ليس_فارغ("3.4 معلومات الوضع 0", معلومات_وضع)

# اختبار 3.5: تغيير الوضع
متغير نتيجة_وضع = uefi_تعيين_وضع_رسوميات(4)
اختبار("3.5 تعيين وضع 1920x1080", نتيجة_وضع, 0)

# اختبار 3.6: التحقق من الوضع الجديد
متغير وضع_جديد = uefi_وضع_رسوميات_حالي()
اختبار("3.6 الوضع الحالي = 4 (1920x1080)", وضع_جديد, 4)

# اختبار 3.7: استعلام الوضع الجديد
متغير معلومات_4 = uefi_استعلام_وضع(4)
اختبار_ليس_فارغ("3.7 معلومات الوضع 4 (1920x1080)", معلومات_4)

# اختبار 3.8: عنوان إطار الشاشة
متغير عنوان_إطار = uefi_عنوان_إطار()
اختبار_ليس_فارغ("3.8 عنوان إطار الشاشة", عنوان_إطار)

# اختبار 3.9: حجم إطار الشاشة
متغير حجم_إطار = uefi_حجم_إطار()
اختبار_ليس_فارغ("3.9 حجم إطار الشاشة > 0", حجم_إطار)

# اختبار 3.10: ملء الشاشة بلون
متغير نتيجة_ملء = uefi_ملء_شاشة(0, 0, 255)
اختبار("3.10 ملء الشاشة بالأزرق", نتيجة_ملء, 0)

# اختبار 3.11: رسم مستطيل
متغير نتيجة_مستطيل = uefi_رسم_مستطيل(100, 100, 200, 150, 255, 0, 0)
اختبار("3.11 رسم مستطيل أحمر", نتيجة_مستطيل, 0)

اطبع_سطر("")

# ===================================================================
# القسم 4: خدمات وقت التشغيل
# ===================================================================
اطبع_سطر("═══ القسم 4: خدمات وقت التشغيل ═══")

# اختبار 4.1: الوقت الحالي
متغير الوقت = uefi_الوقت()
اختبار_ليس_فارغ("4.1 الحصول على الوقت الحالي", الوقت)

# اختبار 4.2: كتابة متغير NVRAM
متغير نتيجة_كتابة_م = uefi_كتابة_متغير("اسم_النواة", "صOS v1.0")
اختبار("4.2 كتابة متغير NVRAM", نتيجة_كتابة_م, 0)

# اختبار 4.3: قراءة متغير NVRAM
متغير قيمة_متغير = uefi_قراءة_متغير("اسم_النواة")
اختبار("4.3 قراءة متغير NVRAM", قيمة_متغير, "صOS v1.0")

# اختبار 4.4: كتابة متغير ثاني
متغير نتيجة_كتابة2 = uefi_كتابة_متغير("إصدار", "2025.1")
اختبار("4.4 كتابة متغير NVRAM ثاني", نتيجة_كتابة2, 0)

# اختبار 4.5: قراءة المتغير الثاني
متغير قيمة2 = uefi_قراءة_متغير("إصدار")
اختبار("4.5 قراءة المتغير الثاني", قيمة2, "2025.1")

# اختبار 4.6: تحديث متغير موجود
متغير نتيجة_تحديث = uefi_كتابة_متغير("اسم_النواة", "صOS v2.0")
اختبار("4.6 تحديث متغير موجود", نتيجة_تحديث, 0)

# اختبار 4.7: التحقق من التحديث
متغير قيمة_محدثة = uefi_قراءة_متغير("اسم_النواة")
اختبار("4.7 القيمة المحدثة", قيمة_محدثة, "صOS v2.0")

# اختبار 4.8: قراءة متغير غير موجود
متغير قيمة_غير_موجودة = uefi_قراءة_متغير("غير_موجود")
اختبار("4.8 متغير غير موجود = فارغ", قيمة_غير_موجودة, "")

اطبع_سطر("")

# ===================================================================
# القسم 5: نظام الملفات
# ===================================================================
اطبع_سطر("═══ القسم 5: نظام الملفات ═══")

# اختبار 5.1: فتح وحدة التخزين
متغير واصف_جذر = uefi_فتح_وحدة_تخزين()
اختبار_ليس_فارغ("5.1 فتح وحدة التخزين", واصف_جذر + 1)

# اختبار 5.2: فتح ملف جديد
متغير واصف_ملف = uefi_فتح_ملف(واصف_جذر, "kernel.bin", 3)
اختبار_ليس_فارغ("5.2 فتح ملف kernel.bin", واصف_ملف + 1)

# اختبار 5.3: كتابة ملف
متغير بيانات_كتابة = "Hello from صOS UEFI Bootloader!"
متغير كتابة_ناجحة = uefi_كتابة_ملف(واصف_ملف, بيانات_كتابة)
اختبار_ليس_فارغ("5.3 كتابة بيانات إلى الملف", كتابة_ناجحة)

# اختبار 5.4: قراءة ملف
متغير محتوى = uefi_قراءة_ملف(واصف_ملف, 1024)
اختبار("5.4 قراءة محتوى الملف", محتوى, بيانات_كتابة)

# اختبار 5.5: معلومات الملف
متغير معلومات = uefi_معلومات_ملف(واصف_ملف)
اختبار_ليس_فارغ("5.5 الحصول على معلومات الملف", معلومات)

# اختبار 5.6: إغلاق الملف
متغير نتيجة_إغلاق = uefi_إغلاق_ملف(واصف_ملف)
اختبار("5.6 إغلاق الملف بنجاح", نتيجة_إغلاق, 0)

# اختبار 5.7: فتح ملف ثاني
متغير واصف_ملف2 = uefi_فتح_ملف(واصف_جذر, "config.txt", 3)
اختبار_ليس_فارغ("5.7 فتح ملف config.txt", واصف_ملف2 + 1)

# اختبار 5.8: كتابة ملف ثاني
متغير نتيجة_كتابة_ملف2 = uefi_كتابة_ملف(واصف_ملف2, "framebuffer=1920x1080")
اختبار_ليس_فارغ("5.8 كتابة إعدادات", نتيجة_كتابة_ملف2)

# اختبار 5.9: إغلاق الملف الثاني
متغير نتيجة_إغلاق2 = uefi_إغلاق_ملف(واصف_ملف2)
اختبار("5.9 إغلاق الملف الثاني", نتيجة_إغلاق2, 0)

اطبع_سطر("")

# ===================================================================
# القسم 6: بروتوكولات ومعلومات النظام
# ===================================================================
اطبع_سطر("═══ القسم 6: بروتوكولات ومعلومات النظام ═══")

# اختبار 6.1: البحث عن بروتوكول GOP
متغير مقبض_gop = uefi_بحث_بروتوكول("GOP")
اختبار_ليس_فارغ("6.1 البحث عن بروتوكول GOP", مقبض_gop)

# اختبار 6.2: البحث عن بروتوكول نظام الملفات
متغير مقبض_fs = uefi_بحث_بروتوكول("SimpleFS")
اختبار_ليس_فارغ("6.2 البحث عن بروتوكول SimpleFS", مقبض_fs)

# اختبار 6.3: البحث ببروتوكول عربي
متغير مقبض_rsom = uefi_بحث_بروتوكول("رسوميات")
اختبار_ليس_فارغ("6.3 البحث ببروتوكول رسوميات (عربي)", مقبض_rsom)

# اختبار 6.4: إصدار UEFI
متغير إصدار = uefi_إصدار()
اختبار_ليس_فارغ("6.4 إصدار UEFI", إصدار)

# اختبار 6.5: بائع البرنامج الثابت
متغير بائع = uefi_بائع()
اختبار_ليس_فارغ("6.5 بائع البرنامج الثابت", بائع)

# اختبار 6.6: إصدار البائع
متغير إصدار_بائع = uefi_إصدار_بائع()
اختبار_ليس_فارغ("6.6 إصدار البائع", إصدار_بائع)

اطبع_سطر("")

# ===================================================================
# القسم 7: التقارير
# ===================================================================
اطبع_سطر("═══ القسم 7: التقارير ═══")

# اختبار 7.1: تقرير شامل
متغير تقرير_شامل = uefi_تقرير()
اختبار_ليس_فارغ("7.1 التقرير الشامل", تقرير_شامل)

# اختبار 7.2: تقرير الذاكرة
متغير تقرير_ذاكرة = uefi_تقرير_ذاكرة()
اختبار_ليس_فارغ("7.2 تقرير خريطة الذاكرة", تقرير_ذاكرة)

# اختبار 7.3: تقرير GOP
متغير تقرير_gop = uefi_تقرير_رسوميات()
اختبار_ليس_فارغ("7.3 تقرير GOP", تقرير_gop)

اطبع_سطر("")

# ===================================================================
# القسم 8: إنهاء خدمات الإقلاع (آخر اختبار)
# ===================================================================
اطبع_سطر("═══ القسم 8: إنهاء خدمات الإقلاع ═══")

# اختبار 8.1: الحصول على مفتاح الخريطة
متغير مفتاح_خريطة = uefi_مفتاح_خريطة()
اختبار_ليس_فارغ("8.1 مفتاح خريطة الذاكرة", مفتاح_خريطة)

# اختبار 8.2: إنهاء خدمات الإقلاع
متغير نتيجة_إنهاء = uefi_إنهاء_خدمات_إقلاع(مفتاح_خريطة)
اختبار("8.2 إنهاء خدمات الإقلاع بنجاح", نتيجة_إنهاء, 0)

# اختبار 8.3: التحقق من إنهاء خدمات الإقلاع
متغير حالة_إنهاء = uefi_خدمات_إقلاع_منتهية()
اختبار("8.3 خدمات الإقلاع منتهية", حالة_إنهاء, 1)

اطبع_سطر("")

# ===================================================================
# القسم 9: اختبارات سيناريو — محاكاة إقلاع نظام تشغيل
# ===================================================================
اطبع_سطر("═══ القسم 9: سيناريو إقلاع نظام تشغيل ═══")

# إعادة تهيئة لاختبار السيناريو (مهيأ مسبقاً — الرمز 20 = EFI_ALREADY_STARTED)
متغير نتيجة_إعادة = uefi_تهيئة()
اختبار("9.1 UEFI مهيأ مسبقاً (ALREADY_STARTED)", نتيجة_إعادة, 20)

# الخطوة 1: تهيئة GOP للعرض المبكر
متغير gop_سيناريو = uefi_تهيئة_رسوميات()
اختبار("9.2 تهيئة GOP للعرض المبكر", gop_سيناريو, 0)

# الخطوة 2: تعيين دقة عالية
متغير وضع_hd = uefi_تعيين_وضع_رسوميات(4)
اختبار("9.3 تعيين وضع 1920x1080", وضع_hd, 0)

# الخطوة 3: رسم شاشة إقلاع
متغير خلفية = uefi_ملء_شاشة(0, 0, 128)
اختبار("9.4 رسم خلفية زرقاء داكنة", خلفية, 0)

متغير شعار = uefi_رسم_مستطيل(810, 440, 300, 200, 255, 255, 255)
اختبار("9.5 رسم شعار أبيض في الوسط", شعار, 0)

# الخطوة 4: تسجيل معلومات الإقلاع في NVRAM
متغير تسجيل = uefi_كتابة_متغير("آخر_إقلاع", "ناجح")
اختبار("9.6 تسجيل حالة الإقلاع", تسجيل, 0)

# الخطوة 5: فتح قسم النظام
متغير قسم_نظام = uefi_فتح_وحدة_تخزين()
اختبار_ليس_فارغ("9.7 فتح قسم EFI", قسم_نظام + 1)

# الخطوة 6: قراءة ملف إعدادات النواة
متغير ملف_إعدادات = uefi_فتح_ملف(قسم_نظام, "\\EFI\\BOOT\\kernel.cfg", 1)
اختبار_ليس_فارغ("9.8 فتح ملف إعدادات النواة", ملف_إعدادات + 1)

# الخطوة 7: الحصول على خريطة الذاكرة
متغير خريطة_إقلاع = uefi_خريطة_ذاكرة()
اختبار_ليس_فارغ("9.9 خريطة ذاكرة الإقلاع", خريطة_إقلاع)

# الخطوة 8: إجمالي الذاكرة
متغير ذاكرة = uefi_ذاكرة_متاحة()
اختبار_ليس_فارغ("9.10 إجمالي RAM المتاح", ذاكرة)

اطبع_سطر("")

# ===================================================================
# القسم 10: اختبارات باللغة الإنجليزية
# ===================================================================
اطبع_سطر("═══ Section 10: English API Tests ═══")

متغير en_init = uefi_initialize()
اختبار("10.1 uefi_initialize() (already started)", en_init, 20)

متغير en_gop = uefi_init_gop()
اختبار("10.2 uefi_init_gop()", en_gop, 0)

متغير en_modes = uefi_gop_mode_count()
اختبار_ليس_فارغ("10.3 uefi_gop_mode_count() > 0", en_modes)

متغير en_rev = uefi_revision()
اختبار_ليس_فارغ("10.4 uefi_revision()", en_rev)

متغير en_vendor = uefi_firmware_vendor()
اختبار_ليس_فارغ("10.5 uefi_firmware_vendor()", en_vendor)

متغير en_set_var = uefi_set_variable("test_key", "test_value")
اختبار("10.6 uefi_set_variable()", en_set_var, 0)

متغير en_get_var = uefi_get_variable("test_key")
اختبار("10.7 uefi_get_variable()", en_get_var, "test_value")

متغير en_time = uefi_get_time()
اختبار_ليس_فارغ("10.8 uefi_get_time()", en_time)

متغير en_mem = uefi_total_memory()
اختبار_ليس_فارغ("10.9 uefi_total_memory() > 0", en_mem)

متغير en_report = uefi_report()
اختبار_ليس_فارغ("10.10 uefi_report()", en_report)

اطبع_سطر("")

# ===================================================================
# ملخص النتائج
# ===================================================================
اطبع_سطر("╔══════════════════════════════════════════════════════╗")
اطبع_سطر("║                   ملخص النتائج                      ║")
اطبع_سطر("╠══════════════════════════════════════════════════════╣")
اطبع_سطر("║ إجمالي الاختبارات: " + إجمالي)
اطبع_سطر("║ ✓ نجاح: " + نجاح)
اطبع_سطر("║ ✗ فشل: " + فشل)

إذا (فشل == 0)
    اطبع_سطر("║ 🎉 جميع الاختبارات نجحت! — All tests passed!")
وإلا
    اطبع_سطر("║ ⚠ بعض الاختبارات فشلت — Some tests failed")
نهاية

اطبع_سطر("╚══════════════════════════════════════════════════════╝")
