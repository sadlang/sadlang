# ============================================================================
# اختبار المرحلة 9 — سلسلة super-trait بعمق 3 مستويات (حالة النجاح)
# Phase 9 Test — 3-level super-trait chain (pass case)
#
# (AR) سلسلة قيود: ط يرث ب، ب يرث أ. استخدام T: ط يجب أن يفحص 3 مستويات.
#      كل الأنواع تنفّذ السمات المطلوبة — يجب أن يجتاز الفحص (EXIT=0).
#
# (EN) Constraint chain: ط inherits ب, ب inherits أ. Using T: ط must check 3 levels.
#      All types implement required traits — type check must succeed (EXIT=0).
# ============================================================================

# (AR) المستوى 1 (الأساس): سمة قابل_للطباعة
# (EN) Level 1 (base): سمة قابل_للطباعة
سمة قابل_للطباعة
    دالة اطبع()
نهاية

# (AR) المستوى 2: سمة قابل_للتحويل يرث قابل_للطباعة
# (EN) Level 2: سمة قابل_للتحويل inherits قابل_للطباعة
سمة قابل_للتحويل يرث قابل_للطباعة
    دالة حوّل() ترجع نص
نهاية

# (AR) المستوى 3: سمة قابل_للتصدير يرث قابل_للتحويل
# (EN) Level 3: سمة قابل_للتصدير inherits قابل_للتحويل
سمة قابل_للتصدير يرث قابل_للتحويل
    دالة صدّر()
نهاية

# (AR) صنف يُنفّذ السمة الأعلى مستوى
# (EN) Class implementing the highest-level trait
صنف وثيقة
نهاية

نفّذ قابل_للطباعة لـ وثيقة
    دالة اطبع()
        اطبع_سطر("طباعة وثيقة")
    نهاية
نهاية

نفّذ قابل_للتحويل لـ وثيقة
    دالة حوّل()
        ارجع "وثيقة"
    نهاية
نهاية

نفّذ قابل_للتصدير لـ وثيقة
    دالة صدّر()
        اطبع_سطر("تصدير وثيقة")
    نهاية
نهاية

# (AR) قالب دالة بقيد من المستوى 3 — يغطي جميع المستويات الثلاثة
# (EN) Template function constrained at level 3 — covers all 3 levels
قالب<نوع T: قابل_للتصدير>
دالة معالج(T وثيقة_ما)
    اطبع_سطر("معالجة")
نهاية

دالة رئيسية()
    متغير و = وثيقة() جديد
    معالج(و)
    ارجع 0
نهاية
