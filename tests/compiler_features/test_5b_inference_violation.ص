# ============================================================================
# اختبار Phase 5b — استدلال نوعي مع قيد منتهَك (يجب أن يفشل)
# Test Phase 5b — inferred type violates constraint (must fail)
# ============================================================================

سمة قابل_للترتيب_المخصص
نهاية

قالب<نوع T: قابل_للترتيب_المخصص> دالة رتّب(T أ، T ب)
   ارجع أ
نهاية

صنف نقطة
   متغير عام س
   باني(س)
      هذا.س = س
   نهاية
نهاية

دالة رئيسية()
   متغير ن1 = نقطة(1)
   متغير ن2 = نقطة(2)
   # (AR) الاستدلال يعطي T=نقطة، ونقطة لا تُنفّذ "قابل_للترتيب_المخصص" → خطأ.
   # (EN) Inference gives T=نقطة, but نقطة doesn't implement the trait → error.
   متغير ر = رتّب(ن1، ن2)
   اطبع_سطر("ok")
نهاية
