# ============================================================================
# اختبار Phase 5b — استدلال نوعي تلقائي يُمرَّر القيد الذي يفشل
# Test Phase 5b — inferred primitive type passes constraint smoothly
# ============================================================================

# (AR) سمة فارغة (marker) — يعتبر أن النص لا يحققها (لا قواعد ضمنية لها).
# (EN) Empty marker trait — strings don't auto-satisfy unknown markers.
سمة قابل_للضرب
نهاية

نفّذ قابل_للضرب لـ رقم
نهاية

قالب<نوع T: قابل_للضرب> دالة مضاعف(T أ)
   ارجع أ
نهاية

دالة رئيسية()
   # (AR) رقم ينفذ "قابل_للضرب" صراحة → الاستدلال يجب أن ينجح.
   # (EN) رقم explicitly impls "قابل_للضرب" → inference must succeed.
   متغير ن = مضاعف(7)
   اطبع_سطر("النتيجة = " + ن)
نهاية
