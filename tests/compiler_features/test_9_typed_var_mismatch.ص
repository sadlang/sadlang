# ============================================================================
# اختبار المرحلة 9 — فحص أنواع تعيين المتغيرات المعلنة (حالة الانتهاك)
# Phase 9 Test — Typed variable assignment check (mismatch case)
#
# (AR) تعيين قيمة من نوع مختلف لمتغير ذي نوع مُصرَّح — يجب أن يفشل (EXIT=1).
#
# (EN) Assigning a value of wrong type to an explicitly typed variable.
#      Type check must fail (EXIT=1).
# ============================================================================

دالة رئيسية()
    # (AR) تعيين نص لمتغير من نوع رقم → خطأ في النوع
    # (EN) Assigning string to رقم variable → type error
    متغير رقم س = "هذا نص وليس رقم"

    اطبع_سطر(س)
    ارجع 0
نهاية
