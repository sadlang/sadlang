# ============================================================================
# اختبار المرحلة 9 — فحص أنواع تعيين المتغيرات المعلنة (حالة النجاح)
# Phase 9 Test — Typed variable assignment check (pass case)
#
# (AR) تعيين قيم لمتغيرات ذات أنواع مُصرَّحة متطابقة — يجب أن يجتاز (EXIT=0).
#
# (EN) Assigning values to explicitly typed variables with matching types.
#      Type check must succeed (EXIT=0).
# ============================================================================

# (AR) تعيين رقم لمتغير من نوع رقم → OK
# (EN) Assigning integer to رقم variable → OK
متغير رقم س = 42

# (AR) تعيين نص لمتغير من نوع نص → OK
# (EN) Assigning string to نص variable → OK
متغير نص اسم = "أحمد"

# (AR) تعيين منطقي لمتغير من نوع منطقي → OK
# (EN) Assigning boolean to منطقي variable → OK
متغير منطقي نشط = صحيح

دالة رئيسية()
    # (AR) تعيين محلي أيضاً → OK
    # (EN) Local typed assignments → OK
    متغير رقم عمر = 25
    متغير نص رسالة = "مرحباً"
    متغير منطقي علم = خطأ

    اطبع_سطر(س)
    اطبع_سطر(اسم)
    اطبع_سطر(عمر)
    اطبع_سطر(رسالة)
    ارجع 0
نهاية
