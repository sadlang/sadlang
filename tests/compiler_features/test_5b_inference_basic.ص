# ============================================================================
# اختبار Phase 5b — استدلال نوعي تلقائي بدون <...>
# Test Phase 5b — automatic type-arg inference without explicit <...>
# ============================================================================

قالب<نوع T: قابل_للمقارنة> دالة أكبر(T أ، T ب)
   إذا (أ > ب)
      ارجع أ
   نهاية
   ارجع ب
نهاية

دالة رئيسية()
   # (AR) لا نُمرّر <رقم> صراحة — يجب أن يستنتجها المترجم.
   # (EN) We don't pass <رقم> — the compiler must infer it.
   متغير ن = أكبر(7، 3)
   اطبع_سطر("الأكبر = " + ن)
نهاية
