# اختبار مكتبة البرمجة منخفضة المستوى
# Test Low-Level OS Development Library

اطبع_سطر("══════════════════════════════════════════════")
اطبع_سطر("  اختبار مكتبة البرمجة منخفضة المستوى v5.0")
اطبع_سطر("══════════════════════════════════════════════")

# 1. التقرير الشامل
اطبع_سطر("\n--- 1. التقرير الشامل ---")
متغير تقرير = منخفض_تقرير()
اطبع_سطر(تقرير)

# 2. العمليات البتية
اطبع_سطر("--- 2. العمليات البتية ---")
متغير أ = 12
متغير ب = 10
اطبع_سطر("AND: " + bit_and(أ، ب))
اطبع_سطر("OR:  " + bit_or(أ، ب))
اطبع_سطر("XOR: " + bit_xor(أ، ب))
اطبع_سطر("NOT: " + bit_not(أ))
اطبع_سطر("SHL: " + bit_shl(1، 4))
اطبع_سطر("SHR: " + bit_shr(16، 2))
اطبع_سطر("TEST bit 2: " + bit_test(أ، 2))
اطبع_سطر("SET bit 0:  " + bit_set(أ، 0))
اطبع_سطر("CLR bit 3:  " + bit_clear(أ، 3))

# 3. معلومات المعالج
اطبع_سطر("\n--- 3. معلومات المعالج ---")
متغير معلومات_معالج = معالج_معلومات()
اطبع_سطر(معلومات_معالج)

# 4. عداد TSC
اطبع_سطر("\n--- 4. عداد الأداء (TSC) ---")
متغير قبل = معالج_عداد()
متغير بعد = معالج_عداد()
اطبع_سطر("TSC قبل: " + قبل)
اطبع_سطر("TSC بعد: " + بعد)
اطبع_سطر("الفرق: " + (بعد - قبل))

# 5. معلومات الذاكرة
اطبع_سطر("\n--- 5. معلومات الذاكرة ---")
متغير تقرير_ذاكرة = ذاكرة_معلومات()
اطبع_سطر(تقرير_ذاكرة)

# 6. معلومات GDT
اطبع_سطر("\n--- 6. معلومات GDT ---")
متغير تقرير_gdt = gdt_معلومات()
اطبع_سطر(تقرير_gdt)

# 7. معلومات المقاطعات
اطبع_سطر("\n--- 7. معلومات المقاطعات ---")
متغير تقرير_مقاطعات = مقاطعات_معلومات()
اطبع_سطر(تقرير_مقاطعات)

# 8. معلومات الترحيل
اطبع_سطر("\n--- 8. معلومات الترحيل ---")
متغير تقرير_ترحيل = ترحيل_معلومات()
اطبع_سطر(تقرير_ترحيل)

# 9. معلومات PCI
اطبع_سطر("\n--- 9. معلومات PCI ---")
متغير تقرير_pci = pci_معلومات()
اطبع_سطر(تقرير_pci)

# 10. معلومات DMA
اطبع_سطر("\n--- 10. معلومات DMA ---")
متغير تقرير_dma = dma_معلومات()
اطبع_سطر(تقرير_dma)

# 11. معلومات الشاشة
اطبع_سطر("\n--- 11. معلومات الشاشة ---")
متغير تقرير_شاشة = شاشة_معلومات()
اطبع_سطر(تقرير_شاشة)

# 12. معلومات ACPI
اطبع_سطر("\n--- 12. معلومات ACPI ---")
متغير تقرير_acpi = acpi_معلومات()
اطبع_سطر(تقرير_acpi)

# 13. معلومات التزامن
اطبع_سطر("\n--- 13. معلومات التزامن ---")
متغير تقرير_تزامن = تزامن_معلومات()
اطبع_سطر(تقرير_تزامن)

# 14. معلومات المجدول
اطبع_سطر("\n--- 14. معلومات المجدول ---")
متغير تقرير_مجدول = مجدول_معلومات()
اطبع_سطر(تقرير_مجدول)

# 15. معلومات الإقلاع
اطبع_سطر("\n--- 15. معلومات الإقلاع ---")
متغير تقرير_إقلاع = إقلاع_معلومات()
اطبع_سطر(تقرير_إقلاع)

# 16. معلومات VFS
اطبع_سطر("\n--- 16. معلومات نظام الملفات ---")
متغير تقرير_ملفات = ملفات_معلومات()
اطبع_سطر(تقرير_ملفات)

# 17. معلومات APIC
اطبع_سطر("\n--- 17. معلومات APIC ---")
متغير تقرير_apic = apic_معلومات()
اطبع_سطر(تقرير_apic)

# 18. معلومات HPET
اطبع_سطر("\n--- 18. معلومات HPET ---")
متغير تقرير_hpet = hpet_معلومات()
اطبع_سطر(تقرير_hpet)

# 19. معلومات استدعاءات النظام
اطبع_سطر("\n--- 19. معلومات Syscall ---")
متغير تقرير_syscall = syscall_معلومات()
اطبع_سطر(تقرير_syscall)

اطبع_سطر("\n══════════════════════════════════════════════")
اطبع_سطر("  ✓ تم اختبار جميع الوحدات بنجاح!")
اطبع_سطر("══════════════════════════════════════════════")
