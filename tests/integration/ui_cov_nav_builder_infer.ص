# @description: م-تحكّم UICore حارس نوع البانِي (Amelia LOW-1) — دالّة تُرجع حاوية (عمود().ابن) تُستنتَج Pointer فتُقبَل كبانٍ (انتقل(صفحة)) وكلقطة (استبدل(صفحة()))؛ تكافؤ headless
# @expected: عمود\n  زر(عنوان: "أ")\nعمود\n  زر(عنوان: "أ")
استورد رسومات
دالة صفحة()
    ارجع عمود().ابن(زر("أ"))
نهاية
# كبانٍ: تمرير الدالّة (تُستنتَج Function ⇒ تُقبَل، تُستدعى كلّ رسم)
انتقل(صفحة)
طباعة_شجرة(الصفحة_الحالية())
# كلقطة: استدعاء الدالّة (تُستنتَج Pointer من عمود() ⇒ تُقبَل كعنصر)
استبدل(صفحة())
طباعة_شجرة(الصفحة_الحالية())
