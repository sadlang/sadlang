# @description: م-تحكّم UICore نموذج البانِي (م1-ج) — انتقل/استبدل(دالّة تُرجع عنصرًا) يخزّن بانيًا يُستدعى عند كلّ رسم؛ عودة تُعيد استدعاءه (تفاعليّة)، تكافؤ headless
# @expected: زر(عنوان: "أ")\nنص(محتوى: "ب")\nزر(عنوان: "أ")\nنص(محتوى: "ب")
استورد رسومات
دالة صفحة_أ()
    ارجع زر("أ")
نهاية
دالة صفحة_ب()
    ارجع نص_عنصر("ب")
نهاية
انتقل(صفحة_أ)
طباعة_شجرة(الصفحة_الحالية())
انتقل(صفحة_ب)
طباعة_شجرة(الصفحة_الحالية())
عودة()
طباعة_شجرة(الصفحة_الحالية())
استبدل(صفحة_ب)
طباعة_شجرة(الصفحة_الحالية())
