# @description: م-تحكّم UICore الحارس البنيويّ — طباعة_شجرة(الصفحة_الحالية()) على فارغ/بعد انتقل/عودة، تكافؤ headless
# @expected: زر(عنوان: "ص1")\nنص_عنصر(محتوى: "ص2")\nزر(عنوان: "ص1")
استورد رسومات
طباعة_شجرة(الصفحة_الحالية())
انتقل(زر("ص1"))
طباعة_شجرة(الصفحة_الحالية())
انتقل(نص_عنصر("ص2"))
طباعة_شجرة(الصفحة_الحالية())
عودة()
طباعة_شجرة(الصفحة_الحالية())
عودة_للبداية()
طباعة_شجرة(الصفحة_الحالية())
