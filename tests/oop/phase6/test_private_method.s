صنف حاسبة
    خاص رقم نتيجة
    
    باني()
        نتيجة = 0
    نهاية
    
    خاص دالة رقم احسب_داخلي(رقم عدد)
        رجع عدد * 2
    نهاية
    
    عام دالة رقم احسب(رقم عدد)
        نتيجة = احسب_داخلي(عدد)
        رجع نتيجة
    نهاية
نهاية

حاسبة حاسبتي
حاسبتي = جديد حاسبة()

اطبع(حاسبتي.احسب_داخلي(5))
