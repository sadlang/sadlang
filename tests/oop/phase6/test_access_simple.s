# Test Basic Access Modifiers

صنف حساب 
    خاص رقم رصيد
    عام نص اسم
    
    باني(نص الاسم) 
        رصيد = 1000
        اسم = الاسم
        اطبع("فتح حساب لـ: " + اسم)
    نهاية
    
    عام دالة رقم احصل_الرصيد() 
        ارجع رصيد
    نهاية
نهاية

# إنشاء كائن
حساب ح
ح = جديد حساب("أحمد")

# الوصول للحقل العام (يجب أن ينجح)
اطبع("الاسم: " + ح.اسم)

# استدعاء طريقة عامة (يجب أن ينجح)
رقم الرصيد
الرصيد = ح.احصل_الرصيد()
اطبع("الرصيد: " + حول_الى_نص(الرصيد))

اطبع("✅ اختبار Access Modifiers ناجح")
