صنف نقطة
    عام رقم س
    عام رقم ص
    
    باني(رقم س_اولي، رقم ص_اولي)
        س = س_اولي
        ص = ص_اولي
    نهاية
    
    عام دالة رقم احصل_س()
        رجع س
    نهاية
    
    عام دالة رقم احصل_ص()
        رجع ص
    نهاية
نهاية

نقطة نقطتي
نقطتي = جديد نقطة(10، 20)

اطبع("الوصول للحقول العامة:")
اطبع(نقطتي.س)
اطبع(نقطتي.ص)

اطبع("استدعاء الطرق العامة:")
اطبع(نقطتي.احصل_س())
اطبع(نقطتي.احصل_ص())

نقطتي.س = 30
نقطتي.ص = 40
اطبع("بعد التعديل:")
اطبع(نقطتي.س)
اطبع(نقطتي.ص)
