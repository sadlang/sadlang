صنف حساب
    خاص رقم رصيد
    عام نص اسم
    
    باني(نص الاسم)
        رصيد = 1000
        اسم = الاسم
        اطبع("فتح حساب")
    نهاية
    
    عام دالة رقم احصل_الرصيد()
        ارجع رصيد
    نهاية
نهاية

حساب ح
ح = جديد حساب("أحمد")

اطبع(ح.اسم)

رقم الرصيد
الرصيد = ح.احصل_الرصيد()
اطبع(حول_الى_نص(الرصيد))
