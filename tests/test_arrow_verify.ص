# اختبار الدوال السهمية

# اختبار 1: دالة سهمية بمعامل واحد
متغير مضاعف = x => x * 2
اطبع("اختبار 1: " + مضاعف(5))

# اختبار 2: دالة سهمية بدون معاملات
متغير تحية = () => "مرحبا"
اطبع("اختبار 2: " + تحية())

# اختبار 3: دالة سهمية بمعاملين
متغير جمع = (أ، ب) => أ + ب
اطبع("اختبار 3: " + جمع(3، 4))
