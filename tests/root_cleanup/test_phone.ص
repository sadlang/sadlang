من مكتبات_خارجية.تعبيرات_منتظمة.src.مكتبة استورد تطابق

اطبع_سطر("اختبار الهاتف")
# نمط الهاتف السعودي - قد يسبب حلقة لانهائية بسبب التعقيد
متغير نمط = "^(\\+966|0)(5[0-9]{8})$"
اطبع_سطر("نمط جاهز، بدء المطابقة...")
اطبع(تطابق(نمط, "0551234567", 0))
اطبع_سطر(" - انتهى")
