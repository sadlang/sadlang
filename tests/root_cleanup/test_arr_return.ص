## Test: after obj.array[index] = value, is obj still valid when returned from a function?

صنف حالة_ت
    مصفوفة المجموعات
    باني()
        هذا.المجموعات = [[-1, -1], [-1, -1]]
    نهاية
نهاية

دالة ابحث(نص ن)
    متغير الحالة = جديد حالة_ت()
    اطبع("قبل تعيين: الحالة.المجموعات[0] = ")
    اطبع_سطر(الحالة.المجموعات[0])
    ## هذا هو الخط المشكوك فيه
    الحالة.المجموعات[0] = [3, 7]
    اطبع("بعد تعيين: الحالة.المجموعات[0] = ")
    اطبع_سطر(الحالة.المجموعات[0])
    ارجع الحالة
نهاية

متغير ن = ابحث("مرحبا")
اطبع("نتيجة.المجموعات[0] = ")
اطبع_سطر(ن.المجموعات[0])
