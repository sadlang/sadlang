# ============================================================
# اختبار: مكتبة الرسوم الأساسية / Test: Basic Graphics Library
# ============================================================
# الملف: graphics_hello.s
# الهدف: اختبار الميزات الأساسية لمكتبة الرسوم
# Purpose: Test basic features of the graphics library

استورد رسوم

# ============================================================
# اختبار 1: إنشاء نافذة بسيطة / Test 1: Create simple window
# ============================================================
اطبع("=== اختبار 1: إنشاء نافذة ===")
نافذة ن1 = نافذة(400، 300، "اختبار أساسي")
تأكد(ن1 != لاغ)
تأكد(ن1.مفتوحة())
ن1.أغلق()
تأكد(!ن1.مفتوحة())
اطبع("✓ النافذة تم إنشاءها وإغلاقها بنجاح")

# ============================================================
# اختبار 2: رسم أشكال أساسية / Test 2: Draw basic shapes
# ============================================================
اطبع("\n=== اختبار 2: رسم أشكال أساسية ===")
نافذة ن2 = نافذة(600، 400، "أشكال")
لوحة ل = ن2.احصل_لوحة()

رقم إطارات = 0
بينما ن2.مفتوحة() && إطارات < 3
    ن2.عالج_أحداث()
    ن2.امسح(لون.أبيض())
    
    # مستطيل أحمر / Red rectangle
    ل.ارسم_مستطيل(50، 50، 100، 80، لون.أحمر()، صحيح)
    
    # دائرة زرقاء / Blue circle
    ل.ارسم_دائرة(300، 200، 60، لون.أزرق()، خطأ)
    
    # خط أخضر / Green line
    ل.ارسم_خط(100، 300، 500، 300، لون.أخضر()، 3)
    
    ن2.اعرض()
    إطارات = إطارات + 1
نهاية

ن2.أغلق()
اطبع(ن"✓ تم رسم {إطارات} إطار بنجاح")

# ============================================================
# اختبار 3: اختبار الألوان / Test 3: Color test
# ============================================================
اطبع("\n=== اختبار 3: اختبار الألوان ===")
نافذة ن3 = نافذة(400، 200، "ألوان")
لوحة ل3 = ن3.احصل_لوحة()

لون أحمر = لون.أحمر()
تأكد(أحمر.ح == 255 && أحمر.خ == 0 && أحمر.ز == 0)

لون مخصص = لون(128، 64، 192)
تأكد(مخصص.ح == 128 && مخصص.خ == 64 && مخصص.ز == 192)

لون شفاف = لون(255، 0، 0، 128)
تأكد(شفاف.ش == 128)

إطارات = 0
بينما ن3.مفتوحة() && إطارات < 2
    ن3.عالج_أحداث()
    ن3.امسح(لون.أبيض())
    
    # رسم مستطيلات بألوان مختلفة / Draw rectangles with different colors
    ل3.ارسم_مستطيل(10، 50، 80، 100، أحمر، صحيح)
    ل3.ارسم_مستطيل(110، 50، 80، 100، لون.أخضر()، صحيح)
    ل3.ارسم_مستطيل(210، 50، 80، 100، لون.أزرق()، صحيح)
    ل3.ارسم_مستطيل(310، 50، 80، 100، مخصص، صحيح)
    
    ن3.اعرض()
    إطارات = إطارات + 1
نهاية

ن3.أغلق()
اطبع("✓ جميع الألوان تعمل بشكل صحيح")

# ============================================================
# اختبار 4: رسم نصوص / Test 4: Text rendering
# ============================================================
اطبع("\n=== اختبار 4: رسم نصوص ===")
نافذة ن4 = نافذة(500، 300، "نصوص")
لوحة ل4 = ن4.احصل_لوحة()

إطارات = 0
بينما ن4.مفتوحة() && إطارات < 3
    ن4.عالج_أحداث()
    ن4.امسح(لون.أبيض())
    
    # رسم نصوص بأحجام مختلفة / Draw text with different sizes
    ل4.ارسم_نص("مرحباً بالعالم!", 50، 50، لون.أسود()، 24)
    ل4.ارسم_نص("Hello World!", 50، 100، لون.أزرق()، 20)
    ل4.ارسم_نص("لغة 'ص'", 50، 150، لون.أحمر()، 18)
    ل4.ارسم_نص(ن"الإطار: {إطارات}", 50، 200، لون.أخضر()، 16)
    
    ن4.اعرض()
    إطارات = إطارات + 1
نهاية

ن4.أغلق()
اطبع("✓ النصوص يتم رسمها بشكل صحيح")

# ============================================================
# اختبار 5: معالجة الأحداث / Test 5: Event handling
# ============================================================
اطبع("\n=== اختبار 5: معالجة الأحداث (محاكاة) ===")
نافذة ن5 = نافذة(400، 300، "أحداث")
لوحة ل5 = ن5.احصل_لوحة()

رقم نقرات = 0
رقم ضغطات_مفاتيح = 0
إطارات = 0

بينما ن5.مفتوحة() && إطارات < 5
    # محاكاة معالجة الأحداث / Simulate event processing
    # في التنفيذ الفعلي، سيتم استخدام ن5.احصل_أحداث()
    # In actual implementation, would use ن5.احصل_أحداث()
    
    ن5.امسح(لون.أبيض())
    
    ل5.ارسم_نص(ن"النقرات: {نقرات}", 50، 50، لون.أسود()، 18)
    ل5.ارسم_نص(ن"ضغطات المفاتيح: {ضغطات_مفاتيح}", 50، 100، لون.أسود()، 18)
    ل5.ارسم_نص(ن"الإطارات: {إطارات}", 50، 150، لون.أسود()، 18)
    
    ن5.اعرض()
    
    # محاكاة أحداث / Simulate events
    إذا إطارات == 2
        نقرات = نقرات + 1
    نهاية
    إذا إطارات == 3
        ضغطات_مفاتيح = ضغطات_مفاتيح + 1
    نهاية
    
    إطارات = إطارات + 1
نهاية

ن5.أغلق()
اطبع("✓ معالجة الأحداث تعمل (محاكاة)")

# ============================================================
# اختبار 6: رسم متقدم / Test 6: Advanced drawing
# ============================================================
اطبع("\n=== اختبار 6: رسم متقدم ===")
نافذة ن6 = نافذة(600، 600، "رسم متقدم")
لوحة ل6 = ن6.احصل_لوحة()

إطارات = 0
بينما ن6.مفتوحة() && إطارات < 4
    ن6.عالج_أحداث()
    ن6.امسح(لون.أبيض())
    
    # رسم شبكة / Draw grid
    لكل ع في نطاق(0، 600، 50)
        ل6.ارسم_خط(ع، 0، ع، 600، لون(200، 200، 200)، 1)
        ل6.ارسم_خط(0، ع، 600، ع، لون(200، 200، 200)، 1)
    نهاية
    
    # رسم دوائر متحدة المركز / Draw concentric circles
    لكل نق في نطاق(20، 200، 20)
        لون ل = لون(255 - نق، 100، 100 + نق)
        ل6.ارسم_دائرة(300، 300، نق، ل، خطأ)
    نهاية
    
    # رسم مستطيلات دوارة (محاكاة) / Draw rotating rectangles (simulated)
    لكل ح في نطاق(0، 360، 45)
        رقم س = 300 + 150 * رياضيات.جيب_تمام(ح * رياضيات.ط / 180)
        رقم ص = 300 + 150 * رياضيات.جيب(ح * رياضيات.ط / 180)
        ل6.ارسم_مستطيل(س - 10، ص - 10، 20، 20، لون.أحمر()، صحيح)
    نهاية
    
    ن6.اعرض()
    إطارات = إطارات + 1
نهاية

ن6.أغلق()
اطبع("✓ الرسم المتقدم يعمل بشكل صحيح")

# ============================================================
# اختبار 7: كائن رسومي بسيط / Test 7: Simple sprite object
# ============================================================
اطبع("\n=== اختبار 7: كائن رسومي (محاكاة) ===")

صنف كرة
    رقم س
    رقم ص
    رقم نصف_قطر
    لون لون
    
    باني(رقم س، رقم ص، رقم نق)
        هذا.س = س
        هذا.ص = ص
        هذا.نصف_قطر = نق
        هذا.لون = لون.أحمر()
    نهاية
    
    دالة ارسم(لوحة ل)
        ل.ارسم_دائرة(هذا.س، هذا.ص، هذا.نصف_قطر، هذا.لون، صحيح)
    نهاية
    
    دالة تحرك(رقم دس، رقم دص)
        هذا.س = هذا.س + دس
        هذا.ص = هذا.ص + دص
    نهاية
نهاية

نافذة ن7 = نافذة(400، 400، "كائن رسومي")
لوحة ل7 = ن7.احصل_لوحة()

كرة ك = كرة(200، 200، 30)

إطارات = 0
بينما ن7.مفتوحة() && إطارات < 10
    ن7.عالج_أحداث()
    ن7.امسح(لون.أبيض())
    
    # تحريك الكرة / Move ball
    ك.تحرك(5، 3)
    
    # ارتداد من الحواف / Bounce from edges
    إذا ك.س > 400 || ك.س < 0
        ك.تحرك(-10، 0)
    نهاية
    إذا ك.ص > 400 || ك.ص < 0
        ك.تحرك(0، -6)
    نهاية
    
    # رسم الكرة / Draw ball
    ك.ارسم(ل7)
    
    # رسم مسار / Draw trail
    ل7.ارسم_نص(ن"الإطار: {إطارات}", 10، 10، لون.أسود()، 16)
    ل7.ارسم_نص(ن"الموقع: ({ك.س}, {ك.ص})", 10، 35، لون.أسود()، 14)
    
    ن7.اعرض()
    إطارات = إطارات + 1
نهاية

ن7.أغلق()
اطبع("✓ الكائن الرسومي يتحرك ويرسم بشكل صحيح")

# ============================================================
# ملخص الاختبارات / Test Summary
# ============================================================
اطبع("\n" + "="*60)
اطبع("✓ جميع الاختبارات نجحت!")
اطبع("✓ All tests passed!")
اطبع("="*60)

/*
المخرجات المتوقعة / Expected Output:
=== اختبار 1: إنشاء نافذة ===
✓ النافذة تم إنشاءها وإغلاقها بنجاح

=== اختبار 2: رسم أشكال أساسية ===
✓ تم رسم 3 إطار بنجاح

=== اختبار 3: اختبار الألوان ===
✓ جميع الألوان تعمل بشكل صحيح

=== اختبار 4: رسم نصوص ===
✓ النصوص يتم رسمها بشكل صحيح

=== اختبار 5: معالجة الأحداث (محاكاة) ===
✓ معالجة الأحداث تعمل (محاكاة)

=== اختبار 6: رسم متقدم ===
✓ الرسم المتقدم يعمل بشكل صحيح

=== اختبار 7: كائن رسومي (محاكاة) ===
✓ الكائن الرسومي يتحرك ويرسم بشكل صحيح

============================================================
✓ جميع الاختبارات نجحت!
✓ All tests passed!
============================================================

ملاحظات التنفيذ / Implementation Notes:
- يجب أن تعمل مكتبة الرسوم مع SDL2 أو Win32/Direct2D
- يجب أن تدعم الألوان RGB و RGBA
- يجب أن تدعم الأشكال الأساسية والنصوص
- يجب أن تعالج الأحداث بشكل صحيح
- يجب أن تدعم OOP لإنشاء كائنات رسومية مخصصة
*/
