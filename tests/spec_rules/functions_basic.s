// ============================================================
// اختبار: الدوال الأساسية / Test: Basic Functions
// ============================================================
// الملف: functions_basic.s
// الهدف: اختبار تعريف واستدعاء الدوال
// Purpose: Test function definition and invocation

اطبع("=== اختبار الدوال الأساسية ===")

// ============================================================
// اختبار 1: دالة بسيطة بدون معاملات / Test 1: Simple function
// ============================================================
دالة مرحبا()
    اطبع("مرحباً بالعالم!")
نهاية

مرحبا()
اطبع("✓ اختبار 1: الدالة البسيطة نجحت")

// ============================================================
// اختبار 2: دالة بمعامل واحد / Test 2: Function with one parameter
// ============================================================
دالة تحية(نص اسم)
    اطبع(ن"مرحباً يا {اسم}!")
نهاية

تحية("أحمد")
تحية("فاطمة")
اطبع("✓ اختبار 2: الدالة بمعامل واحد نجحت")

// ============================================================
// اختبار 3: دالة بقيمة إرجاع / Test 3: Function with return
// ============================================================
دالة رقم جمع(رقم أ، رقم ب)
    إرجاع أ + ب
نهاية

رقم نتيجة = جمع(10، 5)
تأكد(نتيجة == 15)

رقم نتيجة2 = جمع(100، 50)
تأكد(نتيجة2 == 150)

اطبع("✓ اختبار 3: الدالة بقيمة إرجاع نجحت")

// ============================================================
// اختبار 4: دوال متعددة المعاملات / Test 4: Multiple parameters
// ============================================================
دالة رقم مساحة_مستطيل(رقم عرض، رقم ارتفاع)
    إرجاع عرض * ارتفاع
نهاية

رقم مساحة1 = مساحة_مستطيل(10، 5)
رقم مساحة2 = مساحة_مستطيل(20، 15)

تأكد(مساحة1 == 50)
تأكد(مساحة2 == 300)

اطبع("✓ اختبار 4: دوال متعددة المعاملات نجحت")

// ============================================================
// اختبار 5: معامل افتراضي / Test 5: Default parameter
// ============================================================
دالة قوة(رقم أساس، رقم أس = 2)
    إرجاع أساس ** أس
نهاية

رقم مربع = قوة(5)        // استخدام القيمة الافتراضية 2
رقم مكعب = قوة(5، 3)     // تجاوز القيمة الافتراضية

تأكد(مربع == 25)
تأكد(مكعب == 125)

اطبع("✓ اختبار 5: المعامل الافتراضي نجح")

// ============================================================
// اختبار 6: دوال متداخلة / Test 6: Nested functions
// ============================================================
دالة رقم عملية_مركبة(رقم س)
    دالة رقم ضعف(رقم ع)
        إرجاع ع * 2
    نهاية
    
    دالة رقم مربع(رقم ع)
        إرجاع ع * ع
    نهاية
    
    إرجاع مربع(ضعف(س))
نهاية

رقم ناتج = عملية_مركبة(3)  // (3*2)^2 = 36
تأكد(ناتج == 36)

اطبع("✓ اختبار 6: الدوال المتداخلة نجحت")

// ============================================================
// اختبار 7: دالة تستدعي دالة أخرى / Test 7: Function calls function
// ============================================================
دالة رقم ضرب(رقم أ، رقم ب)
    إرجاع أ * ب
نهاية

دالة رقم مساحة_دائرة(رقم نصف_قطر)
    رقم ط = 3.14159
    إرجاع ضرب(ط، ضرب(نصف_قطر، نصف_قطر))
نهاية

رقم مساحة = مساحة_دائرة(5)
// تقريباً: 3.14159 * 25 = 78.53975
تأكد(مساحة > 78 && مساحة < 79)

اطبع("✓ اختبار 7: دالة تستدعي دالة أخرى نجحت")

// ============================================================
// اختبار 8: دالة تعيد قيم متعددة / Test 8: Multiple return values
// ============================================================
دالة مصفوفة قسمة_وباقي(رقم أ، رقم ب)
    رقم قسمة = أ / ب
    رقم باقي = أ % ب
    إرجاع [قسمة، باقي]
نهاية

مصفوفة نتيجة = قسمة_وباقي(17، 5)
تأكد(نتيجة[0] == 3)   // القسمة
تأكد(نتيجة[1] == 2)   // الباقي

اطبع("✓ اختبار 8: القيم المتعددة نجحت")

// ============================================================
// اختبار 9: دالة معاملات متغيرة / Test 9: Variadic function
// ============================================================
دالة رقم مجموع(*أرقام)
    رقم نتيجة = 0
    لكل رقم في أرقام
        نتيجة = نتيجة + رقم
    نهاية
    إرجاع نتيجة
نهاية

رقم م1 = مجموع(1، 2، 3)
رقم م2 = مجموع(10، 20، 30، 40، 50)

تأكد(م1 == 6)
تأكد(م2 == 150)

اطبع("✓ اختبار 9: المعاملات المتغيرة نجحت")

// ============================================================
// اختبار 10: دالة تكرارية / Test 10: Recursive function
// ============================================================
دالة رقم فاكتوريال(رقم ن)
    إذا ن <= 1
        إرجاع 1
    نهاية
    إرجاع ن * فاكتوريال(ن - 1)
نهاية

تأكد(فاكتوريال(0) == 1)
تأكد(فاكتوريال(1) == 1)
تأكد(فاكتوريال(5) == 120)  // 5! = 120
تأكد(فاكتوريال(6) == 720)  // 6! = 720

اطبع("✓ اختبار 10: الدالة التكرارية نجحت")

// ============================================================
// ملخص / Summary
// ============================================================
اطبع("\n" + "="*60)
اطبع("✓ جميع اختبارات الدوال الأساسية نجحت!")
اطبع("✓ All basic function tests passed!")
اطبع("="*60)

/*
المخرجات المتوقعة / Expected Output:
=== اختبار الدوال الأساسية ===
مرحباً بالعالم!
✓ اختبار 1: الدالة البسيطة نجحت
مرحباً يا أحمد!
مرحباً يا فاطمة!
✓ اختبار 2: الدالة بمعامل واحد نجحت
✓ اختبار 3: الدالة بقيمة إرجاع نجحت
✓ اختبار 4: دوال متعددة المعاملات نجحت
✓ اختبار 5: المعامل الافتراضي نجح
✓ اختبار 6: الدوال المتداخلة نجحت
✓ اختبار 7: دالة تستدعي دالة أخرى نجحت
✓ اختبار 8: القيم المتعددة نجحت
✓ اختبار 9: المعاملات المتغيرة نجحت
✓ اختبار 10: الدالة التكرارية نجحت

============================================================
✓ جميع اختبارات الدوال الأساسية نجحت!
✓ All basic function tests passed!
============================================================
*/
