# (AR) اختبار دالة غير متزامنة بسيطة
# (EN) Simple async function test

# (AR) دالة غير متزامنة بسيطة
# (EN) Simple async function
غير_متزامن دالة جلب_البيانات() 
    اطبع_سطر("جاري جلب البيانات...")
    إرجاع "البيانات جاهزة"
نهاية

# (AR) اختبار استدعاء الدالة غير المتزامنة
# (EN) Test calling async function
دالة رئيسية ()
نص نتيجة = جلب_البيانات()
اطبع_سطر(نتيجة)
نهاية