# اختبارات المولدات — 20 اختبار
# Generator tests — 20 tests

# T01: مولد بسيط
مولد أرقام()
    أنتج 1
    أنتج 2
    أنتج 3
نهاية

# T02: مولد مربعات
مولد مربعات(ن)
    متغير ي = 1
    بينما (ي <= ن)
        أنتج ي * ي
        ي += 1
    نهاية
نهاية

# T03: مولد زوجي
مولد أعداد_زوجية(حد)
    متغير ع = 2
    بينما (ع <= حد)
        أنتج ع
        ع += 2
    نهاية
نهاية

# T04: مولد فردي
مولد أعداد_فردية(حد)
    متغير ع = 1
    بينما (ع <= حد)
        أنتج ع
        ع += 2
    نهاية
نهاية

# T05: مولد عد تنازلي
مولد تنازلي(من)
    متغير ع = من
    بينما (ع > 0)
        أنتج ع
        ع -= 1
    نهاية
نهاية

# T06: مولد مضاعفات
مولد مضاعفات(ع، حد)
    متغير ن = ع
    بينما (ن <= حد)
        أنتج ن
        ن += ع
    نهاية
نهاية

# T07: مولد ثابت
مولد قيمة_ثابتة(ق، عدد)
    متغير ي = 0
    بينما (ي < عدد)
        أنتج ق
        ي += 1
    نهاية
نهاية

# T08: مولد نطاق
مولد نطاق(من، إلى)
    متغير ع = من
    بينما (ع <= إلى)
        أنتج ع
        ع += 1
    نهاية
نهاية

# T09: مولد أس
مولد قوى(أساس، عدد)
    متغير نتيجة = 1
    متغير ي = 0
    بينما (ي < عدد)
        أنتج نتيجة
        نتيجة *= أساس
        ي += 1
    نهاية
نهاية

# T10: مولد فيبوناتشي
مولد فيبوناتشي(عدد)
    متغير أ = 0
    متغير ب = 1
    متغير ي = 0
    بينما (ي < عدد)
        أنتج أ
        متغير ت = أ + ب
        أ = ب
        ب = ت
        ي += 1
    نهاية
نهاية

دالة رئيسية()
    # T01: استهلاك مولد بسيط
    اطبع_سطر(أرقام())
    # T02: مربعات 5
    اطبع_سطر(مربعات(5))
    # T03: زوجي حتى 10
    اطبع_سطر(أعداد_زوجية(10))
    # T04: فردي حتى 10
    اطبع_سطر(أعداد_فردية(9))
    # T05: تنازلي من 5
    اطبع_سطر(تنازلي(5))
    # T06: مضاعفات 3 حتى 15
    اطبع_سطر(مضاعفات(3, 15))
    # T07: قيمة ثابتة
    اطبع_سطر(قيمة_ثابتة(7, 4))
    # T08: نطاق
    اطبع_سطر(نطاق(1, 5))
    # T09: قوى 2
    اطبع_سطر(قوى(2, 5))
    # T10: فيبوناتشي 8
    اطبع_سطر(فيبوناتشي(8))
    # T11: مربعات 3
    اطبع_سطر(مربعات(3))
    # T12: زوجي حتى 6
    اطبع_سطر(أعداد_زوجية(6))
    # T13: فردي حتى 7
    اطبع_سطر(أعداد_فردية(7))
    # T14: تنازلي من 3
    اطبع_سطر(تنازلي(3))
    # T15: مضاعفات 5 حتى 25
    اطبع_سطر(مضاعفات(5, 25))
    # T16: نطاق 10-15
    اطبع_سطر(نطاق(10, 15))
    # T17: قوى 3
    اطبع_سطر(قوى(3, 4))
    # T18: فيبوناتشي 5
    اطبع_سطر(فيبوناتشي(5))
    # T19: مربع 1
    اطبع_سطر(مربعات(1))
    # T20: نطاق عنصر واحد
    اطبع_سطر(نطاق(5, 5))
نهاية
