# اختبارات التعبير الثلاثي — 25 اختبار
# Ternary expression tests — 25 tests

دالة رئيسية()
    # T01: ثلاثي بسيط — صحيح
    متغير أ = صحيح ? 1 : 0
    اطبع_سطر("ثلاثي صحيح: " + أ)

    # T02: ثلاثي بسيط — خطأ
    متغير ب = خطأ ? 1 : 0
    اطبع_سطر("ثلاثي خطأ: " + ب)

    # T03: ثلاثي مع مقارنة >
    متغير س = 10
    متغير ج = س > 5 ? "كبير" : "صغير"
    اطبع_سطر("مقارنة >: " + ج)

    # T04: ثلاثي مع مقارنة <
    متغير د = س < 5 ? "صغير" : "كبير"
    اطبع_سطر("مقارنة <: " + د)

    # T05: ثلاثي مع ==
    متغير هـ = س == 10 ? "عشرة" : "غير ذلك"
    اطبع_سطر("مقارنة ==: " + هـ)

    # T06: ثلاثي مع !=
    متغير و = س != 10 ? "مختلف" : "متساوي"
    اطبع_سطر("مقارنة !=: " + و)

    # T07: ثلاثي حسابي
    متغير ز = س > 0 ? س * 2 : س * -1
    اطبع_سطر("ثلاثي حسابي: " + ز)

    # T08: ثلاثي مع نص
    متغير اسم = "أحمد"
    متغير تحية = اسم == "أحمد" ? "مرحباً أحمد" : "مرحباً ضيف"
    اطبع_سطر(تحية)

    # T09: ثلاثي في طباعة مباشرة
    اطبع_سطر(5 > 3 ? "صحيح" : "خطأ")

    # T10: ثلاثي مع عمليات حسابية
    متغير ح = 15
    اطبع_سطر("أكبر: " + (ح > س ? ح : س))

    # T11: ثلاثي معكوس
    اطبع_سطر("أصغر: " + (ح < س ? ح : س))

    # T12: ثلاثي مع 0
    متغير ط = 0
    اطبع_سطر("صفر: " + (ط == 0 ? "صفر" : "غير صفر"))

    # T13: ثلاثي سالب
    متغير ي = -5
    اطبع_سطر("سالب: " + (ي < 0 ? "سالب" : "موجب"))

    # T14: ثلاثي مع >=
    اطبع_سطر(">=: " + (س >= 10 ? "نعم" : "لا"))

    # T15: ثلاثي مع <=
    اطبع_سطر("<=: " + (س <= 10 ? "نعم" : "لا"))

    # T16: ثلاثي مع قيمة عددية
    متغير ك = 100
    متغير ل = ك > 50 ? ك - 50 : ك + 50
    اطبع_سطر("نتيجة: " + ل)

    # T17: ثلاثي في حلقة
    متغير ي2 = 0
    بينما (ي2 < 5)
        اطبع_سطر("عدد " + ي2 + ": " + (ي2 % 2 == 0 ? "زوجي" : "فردي"))
        ي2 += 1
    نهاية

    # T18: ثلاثي مع منطقي و
    متغير م = 7
    اطبع_سطر("و: " + (م > 5 و م < 10 ? "بين" : "خارج"))

    # T19: ثلاثي مع منطقي أو
    اطبع_سطر("أو: " + (م == 5 أو م == 7 ? "مطابق" : "مختلف"))

    # T20: ثلاثي متداخل
    متغير ن = 50
    متغير حجم = ن > 100 ? "كبير" : (ن > 50 ? "متوسط" : "صغير")
    اطبع_سطر("حجم: " + حجم)

    # T21: ثلاثي مع قسمة
    اطبع_سطر("قسمة: " + (ك % 3 == 0 ? "يقبل" : "لا يقبل"))

    # T22: ثلاثي بقيم عشرية
    متغير ف = 3.14
    اطبع_سطر("عشري: " + (ف > 3 ? "أكبر من 3" : "أصغر"))

    # T23: ثلاثي بسلاسل
    متغير ص = "مرحبا"
    اطبع_سطر("نص: " + (ص == "مرحبا" ? "تطابق" : "اختلاف"))

    # T24: ثلاثي في إسناد متغير
    متغير قيمة = 42
    متغير نوع = قيمة > 0 ? "موجب" : (قيمة < 0 ? "سالب" : "صفر")
    اطبع_سطر("نوع: " + نوع)

    # T25: ثلاثي مع عمليات نصية
    متغير طول = 5
    اطبع_سطر("طول: " + (طول > 3 ? "طويل" : "قصير"))
نهاية
