# اختبارات الدوال — 35 اختبار
# Function tests — 35 tests

# T01: دالة بدون معاملات
دالة تحية()
    اطبع_سطر("مرحبا")
نهاية

# T02: دالة بمعامل واحد
دالة مربع(ع)
    ارجع ع * ع
نهاية

# T03: دالة بمعاملين
دالة جمع(أ، ب)
    ارجع أ + ب
نهاية

# T04: دالة ثلاث معاملات
دالة مجموع3(أ، ب، ج)
    ارجع أ + ب + ج
نهاية

# T05: دالة تكعيب
دالة مكعب(ع)
    ارجع ع * ع * ع
نهاية

# T06: دالة القيمة المطلقة
دالة مطلق(ع)
    إذا (ع < 0)
        ارجع -ع
    نهاية
    ارجع ع
نهاية

# T07: دالة الحد الأقصى
دالة أقصى(أ، ب)
    إذا (أ > ب)
        ارجع أ
    نهاية
    ارجع ب
نهاية

# T08: دالة الحد الأدنى
دالة أدنى(أ، ب)
    إذا (أ < ب)
        ارجع أ
    نهاية
    ارجع ب
نهاية

# T09: دالة تكرارية (مضروب)
دالة مضروب(ع)
    إذا (ع <= 1)
        ارجع 1
    نهاية
    ارجع ع * مضروب(ع - 1)
نهاية

# T10: دالة فيبوناتشي
دالة فيب(ع)
    إذا (ع <= 1)
        ارجع ع
    نهاية
    ارجع فيب(ع - 1) + فيب(ع - 2)
نهاية

# T11: دالة زوجي
دالة زوجي(ع)
    ارجع ع % 2 == 0
نهاية

# T12: دالة فردي
دالة فردي(ع)
    ارجع ع % 2 != 0
نهاية

# T13: دالة مع متغير محلي
دالة حساب(أ، ب)
    متغير نتيجة = أ * ب + أ
    ارجع نتيجة
نهاية

# T14: دالة سلسلة نصية
دالة تحية_اسم(اسم)
    ارجع "مرحبا " + اسم
نهاية

# T15: دالة أس
دالة أس(أساس، قوة)
    متغير نتيجة = 1
    متغير ع = 0
    بينما (ع < قوة)
        نتيجة *= أساس
        ع += 1
    نهاية
    ارجع نتيجة
نهاية

# T16: دالة عد أسفل
دالة عد_أسفل(من)
    متغير ن = من
    متغير نتيجة = 0
    بينما (ن > 0)
        نتيجة += ن
        ن -= 1
    نهاية
    ارجع نتيجة
نهاية

# T17: دالة تتحقق من موجب
دالة موجب(ع)
    ارجع ع > 0
نهاية

# T18: دالة ق.م.أ
دالة ق_م_أ(أ، ب)
    بينما (ب != 0)
        متغير ت = ب
        ب = أ % ب
        أ = ت
    نهاية
    ارجع أ
نهاية

# T19: دالة تحسب المتوسط
دالة متوسط(أ، ب، ج)
    ارجع (أ + ب + ج) / 3
نهاية

# T20: دالة تبديل إشارة
دالة عكس_إشارة(ع)
    ارجع -ع
نهاية

دالة رئيسية()
    # T01
    تحية()
    # T02
    اطبع_سطر(مربع(7))
    # T03
    اطبع_سطر(جمع(15, 25))
    # T04
    اطبع_سطر(مجموع3(10, 20, 30))
    # T05
    اطبع_سطر(مكعب(3))
    # T06
    اطبع_سطر(مطلق(-42))
    اطبع_سطر(مطلق(42))
    # T07
    اطبع_سطر(أقصى(10, 20))
    # T08
    اطبع_سطر(أدنى(10, 20))
    # T09
    اطبع_سطر(مضروب(5))
    اطبع_سطر(مضروب(0))
    # T10
    اطبع_سطر(فيب(10))
    # T11
    اطبع_سطر(زوجي(4))
    اطبع_سطر(زوجي(7))
    # T12
    اطبع_سطر(فردي(3))
    اطبع_سطر(فردي(8))
    # T13
    اطبع_سطر(حساب(3, 5))
    # T14
    اطبع_سطر(تحية_اسم("أحمد"))
    # T15
    اطبع_سطر(أس(2, 10))
    # T16
    اطبع_سطر(عد_أسفل(10))
    # T17
    اطبع_سطر(موجب(5))
    اطبع_سطر(موجب(-3))
    # T18
    اطبع_سطر(ق_م_أ(12, 8))
    # T19
    اطبع_سطر(متوسط(10, 20, 30))
    # T20
    اطبع_سطر(عكس_إشارة(42))
    اطبع_سطر(عكس_إشارة(-7))
    # T21: استدعاء متداخل
    اطبع_سطر(جمع(مربع(3), مربع(4)))
    # T22: استدعاء في شرط
    إذا (زوجي(10))
        اطبع_سطر("T22:زوجي")
    نهاية
    # T23: استدعاء في حلقة
    متغير مج = 0
    متغير ع = 1
    بينما (ع <= 5)
        مج += مربع(ع)
        ع += 1
    نهاية
    اطبع_سطر(مج)
    # T24: دالة كتعبير
    متغير ن = مربع(5) + مكعب(2)
    اطبع_سطر(ن)
    # T25: سلسلة استدعاءات
    اطبع_سطر(مطلق(عكس_إشارة(7)))
    # T26: دالة مع ق.م.أ
    اطبع_سطر(ق_م_أ(48, 18))
    # T27: فيبوناتشي 7
    اطبع_سطر(فيب(7))
    # T28: مضروب 7
    اطبع_سطر(مضروب(7))
    # T29: أقصى متداخل
    اطبع_سطر(أقصى(أقصى(3, 7), أقصى(5, 2)))
    # T30: أدنى متداخل
    اطبع_سطر(أدنى(أدنى(3, 7), أدنى(5, 2)))
    # T31: دالة مع صفر
    اطبع_سطر(مربع(0))
    # T32: دالة مع سالب
    اطبع_سطر(مربع(-5))
    # T33: أس كبير
    اطبع_سطر(أس(3, 5))
    # T34: عد أسفل من 5
    اطبع_سطر(عد_أسفل(5))
    # T35: متوسط متساوي
    اطبع_سطر(متوسط(5, 5, 5))
نهاية
