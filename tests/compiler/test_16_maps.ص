# اختبارات الخرائط — 25 اختبار
# Map/Dictionary tests — 25 tests

دالة رئيسية()
    # T01: إنشاء خريطة فارغة
    متغير خ = {}
    اطبع_سطر("خريطة فارغة: " + خ)

    # T02: خريطة بعنصر واحد
    متغير خ2 = {"اسم": "أحمد"}
    اطبع_سطر("عنصر واحد: " + خ2["اسم"])

    # T03: خريطة بعناصر متعددة
    متغير شخص = {"اسم": "سارة"، "عمر": 25}
    اطبع_سطر("اسم: " + شخص["اسم"])
    اطبع_سطر("عمر: " + شخص["عمر"])

    # T04: تعديل قيمة
    شخص["عمر"] = 26
    اطبع_سطر("عمر جديد: " + شخص["عمر"])

    # T05: إضافة عنصر جديد
    شخص["مدينة"] = "الرياض"
    اطبع_سطر("مدينة: " + شخص["مدينة"])

    # T06: خريطة أرقام
    متغير أرقام = {"واحد": 1، "اثنان": 2، "ثلاثة": 3}
    اطبع_سطر("واحد: " + أرقام["واحد"])
    اطبع_سطر("ثلاثة: " + أرقام["ثلاثة"])

    # T07: خريطة مع حساب
    متغير مجموع = أرقام["واحد"] + أرقام["اثنان"] + أرقام["ثلاثة"]
    اطبع_سطر("مجموع: " + مجموع)

    # T08: خريطة منطقية
    متغير إعدادات = {"فعال": صحيح، "مرئي": خطأ}
    اطبع_سطر("فعال: " + إعدادات["فعال"])
    اطبع_سطر("مرئي: " + إعدادات["مرئي"])

    # T09: تحديث إعداد
    إعدادات["مرئي"] = صحيح
    اطبع_سطر("مرئي محدث: " + إعدادات["مرئي"])

    # T10: خريطة نقاط
    متغير نقاط = {"رياضيات": 95، "علوم": 88، "لغة": 92}
    اطبع_سطر("رياضيات: " + نقاط["رياضيات"])

    # T11: تعديل نقطة
    نقاط["علوم"] = 90
    اطبع_سطر("علوم محدثة: " + نقاط["علوم"])

    # T12: خريطة مع شرط
    إذا (نقاط["رياضيات"] > 90)
        اطبع_سطر("رياضيات ممتاز")
    نهاية

    # T13: خريطة في حلقة
    متغير عدادات = {"أ": 0، "ب": 0، "ج": 0}
    متغير ي = 0
    بينما (ي < 5)
        عدادات["أ"] = عدادات["أ"] + 1
        ي += 1
    نهاية
    اطبع_سطر("عداد أ: " + عدادات["أ"])

    # T14: خريطة أسعار
    متغير أسعار = {"تفاح": 5، "موز": 3، "برتقال": 4}
    اطبع_سطر("سعر تفاح: " + أسعار["تفاح"])

    # T15: حساب المجموع
    متغير إجمالي = أسعار["تفاح"] + أسعار["موز"] + أسعار["برتقال"]
    اطبع_سطر("إجمالي: " + إجمالي)

    # T16: خريطة ألوان
    متغير ألوان = {"سماء": "أزرق"، "عشب": "أخضر"، "شمس": "أصفر"}
    اطبع_سطر("لون السماء: " + ألوان["سماء"])

    # T17: تحديث لون
    ألوان["سماء"] = "رمادي"
    اطبع_سطر("سماء محدثة: " + ألوان["سماء"])

    # T18: خريطة عمليات
    متغير بيانات = {"قيمة": 10}
    بيانات["قيمة"] = بيانات["قيمة"] * 2
    اطبع_سطر("مضاعف: " + بيانات["قيمة"])

    # T19: خريطة مع دالة
    متغير قاموس = {"كلمة": "مرحبا"}
    اطبع_سطر("كلمة: " + قاموس["كلمة"])

    # T20: خريطة مختلطة
    متغير مختلط = {"نص": "أهلاً"، "رقم": 42، "منطقي": صحيح}
    اطبع_سطر("نص: " + مختلط["نص"])
    اطبع_سطر("رقم: " + مختلط["رقم"])
    اطبع_سطر("منطقي: " + مختلط["منطقي"])

    # T21: تحديث عناصر متعددة
    مختلط["رقم"] = 100
    مختلط["نص"] = "مرحباً"
    اطبع_سطر("رقم محدث: " + مختلط["رقم"])
    اطبع_سطر("نص محدث: " + مختلط["نص"])

    # T22: خريطة حالات
    متغير حالات = {"نشط": 5، "متوقف": 2، "معلق": 1}
    اطبع_سطر("نشط: " + حالات["نشط"])

    # T23: زيادة حالة
    حالات["نشط"] = حالات["نشط"] + 1
    اطبع_سطر("نشط بعد الزيادة: " + حالات["نشط"])

    # T24: خريطة أيام
    متغير أيام = {"سبت": 1، "أحد": 2، "اثنين": 3}
    اطبع_سطر("يوم السبت: " + أيام["سبت"])

    # T25: خريطة مع مقارنة
    إذا (أيام["سبت"] == 1)
        اطبع_سطر("السبت هو اليوم الأول")
    نهاية
نهاية
