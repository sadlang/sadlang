# اختبارات الأنواع والتحويلات — 20 اختبار
# Type conversions and type system tests — 20 tests

دالة رئيسية()
    # T01: رقم صحيح
    متغير ر = 42
    اطبع_سطر("T01 نوع رقم: " + ر)

    # T02: عشري
    متغير ع = 3.14
    اطبع_سطر("T02 عشري: " + ع)

    # T03: نص
    متغير ن = "مرحبا"
    اطبع_سطر("T03 نص: " + ن)

    # T04: منطقي صحيح
    متغير م_ص = صحيح
    اطبع_سطر("T04 صحيح: " + م_ص)

    # T05: منطقي خطأ
    متغير م_خ = خطأ
    اطبع_سطر("T05 خطأ: " + م_خ)

    # T06: لاشيء
    متغير لا = لاشيء
    اطبع_سطر("T06 لاشيء: " + لا)

    # T07: رقم + عشري
    اطبع_سطر("T07 رقم+عشري: " + (5 + 3.14))

    # T08: رقم إلى نص
    اطبع_سطر("T08 رقم→نص: " + ("العدد: " + 42))

    # T09: عشري إلى نص
    اطبع_سطر("T09 عشري→نص: " + ("القيمة: " + 3.14))

    # T10: منطقي إلى نص
    اطبع_سطر("T10 منطقي→نص: " + ("الحالة: " + صحيح))

    # T11: مصفوفة أرقام
    متغير أرقام = [1, 2, 3, 4, 5]
    اطبع_سطر("T11 مصفوفة: " + أرقام)

    # T12: مصفوفة نصوص
    متغير نصوص = ["أ"، "ب"، "ج"]
    اطبع_سطر("T12 نصوص: " + نصوص)

    # T13: مصفوفة مختلطة
    متغير مخ = [1, "نص", صحيح, 3.14]
    اطبع_سطر("T13 مختلطة: " + مخ)

    # T14: خريطة
    متغير خ = {"مفتاح": "قيمة"}
    اطبع_سطر("T14 خريطة: " + خ["مفتاح"])

    # T15: كائن
    صنف نقطة_ت()
        باني(س، ص)
            هذا.س = س
            هذا.ص = ص
        نهاية
    نهاية
    متغير ن1 = جديد نقطة_ت(3, 4)
    اطبع_سطر("T15 نقطة: " + ن1.س + "," + ن1.ص)

    # T16: حساب مع أنواع مختلفة
    متغير أ = 10
    متغير ب = 3.5
    اطبع_سطر("T16 جمع: " + (أ + ب))
    اطبع_سطر("T16 ضرب: " + (أ * ب))

    # T17: تسلسل نصوص
    متغير اسم = "أحمد"
    متغير عمر = 25
    اطبع_سطر("T17: " + اسم + " عمره " + عمر)

    # T18: ثوابت
    ثابت باي = 3.14159
    ثابت ه = 2.71828
    اطبع_سطر("T18 باي: " + باي)
    اطبع_سطر("T18 ه: " + ه)

    # T19: نوع المصفوفة — طول
    اطبع_سطر("T19 طول: " + طول(أرقام))
    اطبع_سطر("T19 طول نصوص: " + طول(نصوص))

    # T20: عمليات على أنواع مختلفة في حلقة
    متغير قيم = [1, 2.5, 3, 4.5, 5]
    متغير مج = 0
    لكل ق في قيم
        مج += ق
    نهاية
    اطبع_سطر("T20 مجموع: " + مج)
نهاية
