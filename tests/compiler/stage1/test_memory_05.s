# ======================================================================
# test_memory_05.s - اختبار تخصيص ذاكرة كبيرة
# Stage 1 Test: Large Memory Allocations
# ======================================================================
# الوصف / Description:
#   اختبار تخصيص كميات كبيرة من الذاكرة
#   Test allocating large amounts of memory
#
# Expected Output:
#   SUCCESS
#   FREED
# ======================================================================

دالة main()
    # تخصيص 1MB
    مؤشر<رقم> ptr = خصص(1024 * 1024)
    
    إذا (ptr != فارغ)
        اطبع("SUCCESS")
        
        # التحقق من إمكانية الكتابة
        *ptr = 42
        
        # تحرير الذاكرة
        حرر(ptr)
        اطبع("FREED")
    وإلا
        اطبع("FAILED")
    نهاية
نهاية
