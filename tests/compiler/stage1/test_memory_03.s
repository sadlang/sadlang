# ======================================================================
# test_memory_03.s - اختبار نسخ الذاكرة
# Stage 1 Test: Memory Copy
# ======================================================================
# الوصف / Description:
#   اختبار نسخ البيانات بين مناطق الذاكرة
#   Test copying data between memory regions
#
# Expected Output:
#   1
#   2
#   3
# ======================================================================

دالة main()
    # مصفوفة المصدر
    مصفوفة src = [1، 2، 3، 4، 5]
    
    # مصفوفة الوجهة
    مصفوفة dest = [0، 0، 0، 0، 0]
    
    # نسخ أول 3 عناصر
    نسخ_ذاكرة(&dest[0]، &src[0]، 3)
    
    # التحقق من النسخ
    اطبع(dest[0])  # Expected: 1
    اطبع(dest[1])  # Expected: 2
    اطبع(dest[2])  # Expected: 3
نهاية
