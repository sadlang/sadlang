# اختبارات نطاق المتغيرات — 25 اختبار
# Variable scoping tests — 25 tests

متغير متغير_عام = "متغير عام"

دالة مستوى_أول()
    متغير محلي = "محلي"
    ارجع محلي
نهاية

دالة مستوى_متداخل()
    متغير أ = 1
    دالة داخلية()
        متغير ب = 2
        ارجع أ + ب
    نهاية
    ارجع داخلية()
نهاية

دالة تعديل_محلي()
    متغير س = 10
    س += 5
    ارجع س
نهاية

دالة متغيرات_مشابهة()
    متغير س = 100
    إذا (صحيح)
        متغير ص = 200
        اطبع_سطر("داخلي ص: " + ص)
    نهاية
    ارجع س
نهاية

دالة حلقة_نطاق()
    متغير مجموع = 0
    لكل ي في [1, 2, 3, 4, 5]
        مجموع += ي
    نهاية
    ارجع مجموع
نهاية

دالة ظل()
    متغير أ = 10
    دالة داخلية2()
        متغير أ = 20
        ارجع أ
    نهاية
    ارجع داخلية2()
نهاية

دالة نطاق_شرطي()
    متغير نتيجة = ""
    متغير س = 5
    إذا (س > 3)
        نتيجة = "كبير"
    وإلا
        نتيجة = "صغير"
    نهاية
    ارجع نتيجة
نهاية

دالة نطاق_بينما()
    متغير ع = 0
    متغير ي = 0
    بينما (ي < 10)
        ع += ي
        ي += 1
    نهاية
    ارجع ع
نهاية

صنف نطاق_صنف()
    باني()
        هذا.قيمة = 42
    نهاية

    دالة احصل()
        ارجع هذا.قيمة
    نهاية

    دالة عيّن(ق)
        هذا.قيمة = ق
    نهاية
نهاية

دالة ألتقط_مرجع()
    متغير عداد = 0
    دالة زد()
        عداد += 1
        ارجع عداد
    نهاية
    زد()
    زد()
    زد()
    ارجع عداد
نهاية

دالة رئيسية()
    # T01: متغير عام
    اطبع_سطر("T01: " + متغير_عام)

    # T02: متغير محلي في دالة
    اطبع_سطر("T02: " + مستوى_أول())

    # T03: نطاق متداخل
    اطبع_سطر("T03: " + مستوى_متداخل())

    # T04: تعديل محلي
    اطبع_سطر("T04: " + تعديل_محلي())

    # T05: متغيرات مشابهه
    اطبع_سطر("T05: " + متغيرات_مشابهة())

    # T06: نطاق في حلقة
    اطبع_سطر("T06: " + حلقة_نطاق())

    # T07: تظليل متغير
    اطبع_سطر("T07: " + ظل())

    # T08: نطاق شرطي
    اطبع_سطر("T08: " + نطاق_شرطي())

    # T09: نطاق بينما
    اطبع_سطر("T09: " + نطاق_بينما())

    # T10: نطاق صنف
    متغير كائن = جديد نطاق_صنف()
    اطبع_سطر("T10: " + كائن.احصل())

    # T11: تعديل صنف
    كائن.عيّن(100)
    اطبع_سطر("T11: " + كائن.احصل())

    # T12: التقاط بمرجع
    اطبع_سطر("T12: " + ألتقط_مرجع())

    # T13: متغيرات متعددة في نطاق
    متغير أ = 1
    متغير ب = 2
    متغير ج = 3
    اطبع_سطر("T13: " + (أ + ب + ج))

    # T14: إعادة تعيين
    أ = 100
    اطبع_سطر("T14: " + أ)

    # T15: نطاق حلقة لكل
    متغير مج = 0
    لكل ق في [10, 20, 30]
        مج += ق
    نهاية
    اطبع_سطر("T15: " + مج)

    # T16: متغير في شرط
    متغير حالة = "غير محدد"
    إذا (أ > 50)
        حالة = "كبير"
    وإلا
        حالة = "صغير"
    نهاية
    اطبع_سطر("T16: " + حالة)

    # T17: ثابت عام
    ثابت ط = 3.14
    اطبع_سطر("T17: " + ط)

    # T18: نطاق حاول
    متغير نتيجة_حاول = "لا شيء"
    حاول
        نتيجة_حاول = "نجح"
    امسك (الاستثناء)
        نتيجة_حاول = "فشل"
    نهاية
    اطبع_سطر("T18: " + نتيجة_حاول)

    # T19: متغيرات بنفس الاسم في دوال مختلفة
    دالة د1()
        متغير س = 10
        ارجع س
    نهاية
    دالة د2()
        متغير س = 20
        ارجع س
    نهاية
    اطبع_سطر("T19: " + د1() + "، " + د2())

    # T20: متغير في حلقة بينما
    متغير ي2 = 0
    متغير ع2 = 1
    بينما (ي2 < 5)
        ع2 *= 2
        ي2 += 1
    نهاية
    اطبع_سطر("T20: " + ع2)

    # T21: تداخل شروط
    متغير ن = 75
    متغير وصف = ""
    إذا (ن >= 0)
        إذا (ن >= 50)
            إذا (ن >= 75)
                وصف = "ممتاز"
            وإلا
                وصف = "جيد"
            نهاية
        وإلا
            وصف = "مقبول"
        نهاية
    وإلا
        وصف = "سالب"
    نهاية
    اطبع_سطر("T21: " + وصف)

    # T22: دالة تستخدم متغير عام
    دالة استخدم_عام()
        ارجع متغير_عام
    نهاية
    اطبع_سطر("T22: " + استخدم_عام())

    # T23: إغلاق بسيط
    متغير قاعدة = 100
    دالة أضف_للقاعدة(ق)
        ارجع قاعدة + ق
    نهاية
    اطبع_سطر("T23: " + أضف_للقاعدة(50))

    # T24: متغير بعد حلقة
    متغير ح_ع = 0
    لكل ق2 في [5, 10, 15, 20]
        ح_ع += ق2
    نهاية
    # المتغير لا يزال متاحاً
    اطبع_سطر("T24: " + ح_ع)

    # T25: نطاقات متعددة
    متغير ر = 0
    بينما (ر < 3)
        متغير داخلي = ر * 10
        اطبع_سطر("T25 حلقة: " + داخلي)
        ر += 1
    نهاية
نهاية
