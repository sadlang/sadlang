دالة رئيسية()
    # نقل الملكية
    نص رسالة = "مرحبا"
    متغير نسخة = رسالة
    اطبع_سطر(نسخة)

    # نسخ الأنواع البسيطة
    رقم س = 42
    رقم ص = س
    اطبع_سطر(ن"س = {س}، ص = {ص}")

    # الاستعارة
    نص اسم = "أحمد"
    متغير مرجع = &اسم
    اطبع_سطر(مرجع)
نهاية
