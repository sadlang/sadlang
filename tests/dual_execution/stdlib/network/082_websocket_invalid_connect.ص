# @description WebSocket — محاولة اتصال بعنوان غير صالح يفشل
# @expected خطأ\nخطأ

استورد شبكات

متغير ع = عميل_ويبسوكت()

# (AR) عنوان لا يستجيب — يجب أن يفشل بدون توقف للأبد
ع.اضبط_مهلة_الاستقبال(500)
متغير نتيجة = ع.اتصل("ws://127.0.0.1:1")
اطبع_سطر(نتيجة)
اطبع_سطر(ع.هل_متصل())
