# @description وصلة_آمنة (HTTPS) — مضيف واحد مع عدة طلبات
# @expected صحيح\nصحيح\nخطأ
# @skip_compiler

استورد شبكات

# إنشاء وصلة آمنة (سيُنشئ رابط https://127.0.0.1:1)
متغير و1 = وصلة_آمنة("127.0.0.1"، 1)
و1.اضبط_المهلة(150)
اطبع_سطر(صحيح)

# طلب GET — يفشل (لا يوجد خادم HTTPS)
و1.استرجع("/مسار")
اطبع_سطر(صحيح)

# يجب أن يكون الطلب فشل
اطبع_سطر(و1.ناجح())

و1.أغلق()
