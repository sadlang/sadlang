# @description WebSocket — استقبل بمهلة قصيرة بدون اتصال يُرجع نص فارغ
# (AR) ملاحظة: لا نستخدم @expected هنا — يكفي تطابق المفسر مع المترجم
#       لأن اطبع_سطر("") ينتج سطراً فارغاً واحداً يُجرَّد بـ rstrip

استورد شبكات

متغير ع = عميل_ويبسوكت()

# (AR) لا اتصال — مهلة قصيرة، لا يجب أن يُعلق
متغير ر = ع.استقبل(100)
اطبع_سطر(ر)
