# @description التنظيف التلقائي عبر أغلق() المكرر
# @expected صحيح\nخطأ\nخطأ
# @skip_compiler

استورد شبكات

متغير م = مقبس_اتصال()
اطبع_سطر(م.هل_صالح())

# إغلاق أول
م.أغلق()
اطبع_سطر(م.هل_صالح())

# إغلاق ثانٍ — يجب ألا يسبب crash (idempotent)
م.أغلق()
اطبع_سطر(م.هل_صالح())
