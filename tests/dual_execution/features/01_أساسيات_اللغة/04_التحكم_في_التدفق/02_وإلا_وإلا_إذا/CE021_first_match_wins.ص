# @description CE-021: وإلا إذا متسلسل — أول شرط صحيح فقط يُنفَّذ
# @expected الأول
# @priority P1

# ============================================================
# اختبار: عند تطابق أكثر من شرط في سلسلة وإلا إذا
# يجب أن يُنفَّذ الفرع الأول فقط (الأكثر علواً)
# ============================================================

متغير ن = 10

# الشرط الأول صحيح، لكن أيضاً (ن > 0) و (ن > 5)
إذا (ن > 0)
    اطبع_سطر("الأول")
وإلا إذا (ن > 5)
    اطبع_سطر("الثاني")
وإلا إذا (ن > 8)
    اطبع_سطر("الثالث")
نهاية
