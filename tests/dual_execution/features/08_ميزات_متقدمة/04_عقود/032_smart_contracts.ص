# @description اختبار العقود الذكية — عقد محفظة مع إيداع وسحب
# @expected 100\n150\n120
# @priority P3

عقد محفظة
    متغير عام رصيد
    باني(رصيد_ابتدائي)
        هذا.رصيد = رصيد_ابتدائي
    نهاية
    دالة عام إيداع(مبلغ)
        هذا.رصيد = هذا.رصيد + مبلغ
    نهاية
    دالة عام سحب(مبلغ)
        إذا (مبلغ > هذا.رصيد)
            ارمي "رصيد غير كافي"
        نهاية
        هذا.رصيد = هذا.رصيد - مبلغ
    نهاية
نهاية

متغير م = محفظة(100)
اطبع_سطر(م.رصيد)
م.إيداع(50)
اطبع_سطر(م.رصيد)
م.سحب(30)
اطبع_سطر(م.رصيد)
