# ═══════════════════════════════════════════════════════════════════
# اختبارات متقدمة لتكرار القنوات في المترجم (sadc)
# ═══════════════════════════════════════════════════════════════════
# ملاحظة: القناة المُترجمة ذات مخزن واحد — فقط آخر قيمة مُرسلة تُحفظ.
#         هذه الاختبارات مصممة لتعمل مع هذا القيد.
# ═══════════════════════════════════════════════════════════════════

# ─────────────────────────────────────────────
# اختبار 1: قيمة واحدة بسيطة
# ─────────────────────────────────────────────
متغير ق1 = قناة(10)
channel_send(ق1، 42)
channel_close(ق1)

اطبع_سطر("اختبار 1: قيمة واحدة")
لكل ع في ق1
    اطبع_سطر(ع)
نهاية
اطبع_سطر("نجاح")

# ─────────────────────────────────────────────
# اختبار 2: إرسال متعدد → آخر قيمة فقط
# ─────────────────────────────────────────────
متغير ق2 = قناة(10)
channel_send(ق2، 10)
channel_send(ق2، 20)
channel_send(ق2، 99)
channel_close(ق2)

اطبع_سطر("اختبار 2: آخر قيمة")
لكل ع في ق2
    اطبع_سطر(ع)
نهاية
اطبع_سطر("نجاح")

# ─────────────────────────────────────────────
# اختبار 3: قناة فارغة مغلقة — لا تكرار
# ─────────────────────────────────────────────
متغير ق3 = قناة(10)
channel_close(ق3)

اطبع_سطر("اختبار 3: فارغة مغلقة")
متغير تم_تكرار = 0
لكل ع في ق3
    تم_تكرار = 1
نهاية
اطبع_سطر(تم_تكرار)
اطبع_سطر("نجاح")

# ─────────────────────────────────────────────
# اختبار 4: قيمة صفر (حالة حدية)
# ─────────────────────────────────────────────
متغير ق4 = قناة(10)
channel_send(ق4، 0)
channel_close(ق4)

اطبع_سطر("اختبار 4: قيمة صفر")
لكل ع في ق4
    اطبع_سطر(ع)
نهاية
اطبع_سطر("نجاح")

# ─────────────────────────────────────────────
# اختبار 5: عمليات حسابية على القيمة المستقبلة
# ─────────────────────────────────────────────
متغير ق5 = قناة(10)
channel_send(ق5، 7)
channel_close(ق5)

اطبع_سطر("اختبار 5: حسابات")
لكل ع في ق5
    متغير مضاعف = ع * 2
    متغير مجموع = ع + 100
    اطبع_سطر(مضاعف)
    اطبع_سطر(مجموع)
نهاية
اطبع_سطر("نجاح")

# ─────────────────────────────────────────────
# اختبار 6: قناتان مستقلتان
# ─────────────────────────────────────────────
متغير قأ = قناة(10)
متغير قب = قناة(10)
channel_send(قأ، 111)
channel_send(قب، 222)
channel_close(قأ)
channel_close(قب)

اطبع_سطر("اختبار 6: قناتان")
لكل ع في قأ
    اطبع_سطر(ع)
نهاية
لكل ع في قب
    اطبع_سطر(ع)
نهاية
اطبع_سطر("نجاح")

# ─────────────────────────────────────────────
# اختبار 7: قيمة سالبة
# ─────────────────────────────────────────────
متغير ق7 = قناة(10)
channel_send(ق7، 0 - 42)
channel_close(ق7)

اطبع_سطر("اختبار 7: قيمة سالبة")
لكل ع في ق7
    اطبع_سطر(ع)
نهاية
اطبع_سطر("نجاح")

# ─────────────────────────────────────────────
# اختبار 8: قيمة كبيرة
# ─────────────────────────────────────────────
متغير ق8 = قناة(10)
channel_send(ق8، 999999)
channel_close(ق8)

اطبع_سطر("اختبار 8: قيمة كبيرة")
لكل ع في ق8
    اطبع_سطر(ع)
نهاية
اطبع_سطر("نجاح")
