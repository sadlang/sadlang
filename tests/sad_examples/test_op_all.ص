# اختبار شامل مبسط — بدون فهرسة ولا وراثة

صنف متجه
    باني(س، ص)
        هذا.س = س
        هذا.ص = ص
    نهاية

    عامل +(آخر)
        ارجع جديد متجه(هذا.س + آخر.س، هذا.ص + آخر.ص)
    نهاية

    عامل -(آخر)
        ارجع جديد متجه(هذا.س - آخر.س، هذا.ص - آخر.ص)
    نهاية

    عامل *(آخر)
        ارجع جديد متجه(هذا.س * آخر.س، هذا.ص * آخر.ص)
    نهاية

    عامل ==(آخر)
        ارجع هذا.س == آخر.س و هذا.ص == آخر.ص
    نهاية

    عامل !=(آخر)
        ارجع هذا.س != آخر.س أو هذا.ص != آخر.ص
    نهاية

    عامل <(آخر)
        ارجع (هذا.س * هذا.س + هذا.ص * هذا.ص) < (آخر.س * آخر.س + آخر.ص * آخر.ص)
    نهاية

    عامل -()
        ارجع جديد متجه(-هذا.س، -هذا.ص)
    نهاية

    عامل نص()
        ارجع "(" + هذا.س + "," + هذا.ص + ")"
    نهاية

    عامل منطقي()
        ارجع هذا.س != 0 أو هذا.ص != 0
    نهاية
نهاية

صنف مضاعف
    باني(معامل)
        هذا.معامل = معامل
    نهاية

    عامل ()(قيمة)
        ارجع قيمة * هذا.معامل
    نهاية
نهاية

# اختبار 1: +
متغير أ = جديد متجه(1، 2)
متغير ب = جديد متجه(3، 4)
متغير ج = أ + ب
اطبع_سطر(ج)

# اختبار 2: -
متغير د = ب - أ
اطبع_سطر(د)

# اختبار 3: *
متغير هـ = أ * ب
اطبع_سطر(هـ)

# اختبار 4: ==
إذا (أ == جديد متجه(1، 2))
    اطبع_سطر("متساويان")
نهاية

# اختبار 5: !=
إذا (أ != ب)
    اطبع_سطر("مختلفان")
نهاية

# اختبار 6: <
إذا (أ < ب)
    اطبع_سطر("أ أصغر")
نهاية

# اختبار 7: سالب -
متغير سالب = -أ
اطبع_سطر(سالب)

# اختبار 8: نص + كائن
اطبع_سطر("أ = " + أ)

# اختبار 9: منطقي في شرط
إذا (أ)
    اطبع_سطر("غير صفري")
نهاية

# اختبار 10: عامل الاستدعاء
متغير ض = جديد مضاعف(3)
اطبع_سطر(ض(7))

# اختبار 11: اطبع_سطر(كائن) مع __op_tostring__
اطبع_سطر(أ)
