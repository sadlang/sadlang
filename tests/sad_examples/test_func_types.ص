# ═══════════════════════════════════════════════════
# اختبار فحص أنواع الدوال (معاملات + إرجاع)
# ═══════════════════════════════════════════════════

اطبع_سطر("=== اختبار 1: استدعاء صحيح ===")

دالة جمع(رقم أ، رقم ب)
    ارجع أ + ب
نهاية

# استدعاء صحيح - بدون تحذيرات
متغير نتيجة = جمع(3، 5)
اطبع_سطر("3 + 5 = " + نتيجة)

اطبع_سطر("=== اختبار 2: استدعاء بنوع خاطئ ===")

# استدعاء بنص بدل رقم - يجب أن يظهر تحذير
متغير نتيجة2 = جمع("مرحبا"، 5)
اطبع_سطر("نتيجة2 = " + نتيجة2)

اطبع_سطر("=== اختبار 3: نوع إرجاع خاطئ ===")

دالة رقم احسب()
    ارجع "ليس رقم"
نهاية

متغير ر = احسب()
اطبع_سطر("ر = " + ر)

اطبع_سطر("=== اختبار 4: دالة بدون أنواع ===")

# دالة بدون أنواع - بدون تحذيرات
دالة مرنة(س، ص)
    ارجع س + ص
نهاية

اطبع_سطر("مرنة: " + مرنة(1، 2))
اطبع_سطر("مرنة: " + مرنة("أ"، "ب"))

اطبع_سطر("=== جميع الاختبارات نجحت! ===")
