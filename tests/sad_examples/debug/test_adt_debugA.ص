تعداد شكل
    دائرة(نصف_القطر)
    مستطيل(عرض، ارتفاع)
    نقطة
نهاية

تعداد نتيجة
    نجاح(قيمة)
    فشل(رسالة)
نهاية

متغير ش1 = شكل.دائرة(42)
متغير ش2 = شكل.مستطيل(10، 20)
متغير ش3 = شكل.نقطة

# أول match
طابق (ش1)
    عندما شكل.دائرة(نق):
        اطبع_سطر("A:" + نق)
    عندما شكل.مستطيل(ع، ا):
        اطبع_سطر("A:مستطيل")
    عندما شكل.نقطة:
        اطبع_سطر("A:نقطة")
نهاية

# ثاني match
طابق (ش2)
    عندما شكل.دائرة(نق):
        اطبع_سطر("B:دائرة")
    عندما شكل.مستطيل(ع، ا):
        اطبع_سطر("B:" + ع)
    عندما شكل.نقطة:
        اطبع_سطر("B:نقطة")
نهاية

# ثالث match
طابق (ش3)
    عندما شكل.دائرة(نق):
        اطبع_سطر("C:دائرة")
    عندما شكل.مستطيل(ع، ا):
        اطبع_سطر("C:مستطيل")
    عندما شكل.نقطة:
        اطبع_سطر("C:نقطة")
نهاية

دالة وصف(ش)
    طابق (ش)
        عندما شكل.دائرة(نق):
            اطبع_سطر("ف:" + نق)
        عندما شكل.مستطيل(ع، ا):
            اطبع_سطر("ف:م")
        عندما شكل.نقطة:
            اطبع_سطر("ف:ن")
    نهاية
نهاية

وصف(ش1)

متغير ن = نتيجة.نجاح(100)
طابق (ن)
    عندما نتيجة.نجاح(ق):
        اطبع_سطر("4:ن=" + ق)
    عندما نتيجة.فشل(ر):
        اطبع_سطر("4:ف")
نهاية

دالة اصنع(النوع)
    إذا (النوع == 1)
        ارجع شكل.دائرة(99)
    وإلا
        ارجع شكل.مستطيل(30، 40)
    نهاية
نهاية

متغير ش4 = اصنع(1)
متغير ش5 = اصنع(2)

طابق (ش4)
    عندما شكل.دائرة(نق):
        اطبع_سطر("5:دائرة=" + نق)
    افتراضي:
        اطبع_سطر("5:آخر")
نهاية

طابق (ش5)
    عندما شكل.مستطيل(ع، ا):
        اطبع_سطر("6:مستطيل=" + ع + "×" + ا)
    افتراضي:
        اطبع_سطر("6:آخر")
نهاية

اطبع_سطر("تم")
