تعداد نتيجة
    نجاح(قيمة)
    فشل(رسالة)
نهاية

تعداد شكل
    دائرة(نصف_القطر)
    مستطيل(عرض، ارتفاع)
    نقطة
نهاية

متغير ش1 = شكل.دائرة(42)
اطبع_سطر("قبل")
طابق (ش1)
    عندما شكل.دائرة(نق):
        اطبع_سطر("دائرة=" + نق)
    افتراضي:
        اطبع_سطر("آخر")
نهاية
اطبع_سطر("بعد")
