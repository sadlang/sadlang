// ============================================================================
// اختبارات شاملة لدوال النصوص - String Functions Comprehensive Tests
// ============================================================================
// الملف: test_string_functions.s
// الهدف: اختبار جميع دوال النصوص الـ 12 بشكل شامل
// التاريخ: نوفمبر 22، 2025
// ============================================================================

اطبع_سطر("╔════════════════════════════════════════════════════════════╗")
اطبع_سطر("║         اختبارات دوال النصوص - String Tests             ║")
اطبع_سطر("║         Testing String Functions Library                  ║")
اطبع_سطر("╚════════════════════════════════════════════════════════════╝")
اطبع_سطر("")

رقم عدد_الاختبارات = 0
رقم اختبارات_ناجحة = 0
رقم اختبارات_فاشلة = 0

// ============================================================================
// دالة مساعدة للتحقق من النتائج
// Helper function to verify results
// ============================================================================
دالة اختبر(نص اسم_الاختبار، منطقي نتيجة) {
    عدد_الاختبارات = عدد_الاختبارات + 1
    
    إذا نتيجة {
        اطبع_سطر("✅ نجح / PASS: " + اسم_الاختبار)
        اختبارات_ناجحة = اختبارات_ناجحة + 1
    } وإلا {
        اطبع_سطر("❌ فشل / FAIL: " + اسم_الاختبار)
        اختبارات_فاشلة = اختبارات_فاشلة + 1
    }
}

// ============================================================================
// 1. اختبارات دالة length (طول)
// ============================================================================
اطبع_سطر("▶ 1. اختبار دالة length / طول")
اطبع_سطر("─────────────────────────────────────")

نص نص1 = "مرحبا"
رقم طول1 = length(نص1)
اختبر("length: نص عربي 'مرحبا'", طول1 == 5)

نص نص2 = "Hello"
رقم طول2 = length(نص2)
اختبر("length: نص إنجليزي 'Hello'", طول2 == 5)

نص نص3 = ""
رقم طول3 = length(نص3)
اختبر("length: نص فارغ", طول3 == 0)

نص نص4 = "مرحبا بك في لغة ص"
رقم طول4 = length(نص4)
اختبر("length: جملة طويلة", طول4 > 10)

اطبع_سطر("")

// ============================================================================
// 2. اختبارات دالة find (ابحث)
// ============================================================================
اطبع_سطر("▶ 2. اختبار دالة find / ابحث")
اطبع_سطر("─────────────────────────────────────")

نص نص_بحث = "مرحبا بك في لغة ص"
رقم موقع1 = find(نص_بحث، "بك")
اختبر("find: يجد كلمة موجودة", موقع1 >= 0)

رقم موقع2 = find(نص_بحث، "xyz")
اختبر("find: لا يجد كلمة غير موجودة", موقع2 == -1)

رقم موقع3 = find(نص_بحث، "مرحبا")
اختبر("find: يجد في البداية", موقع3 == 0)

رقم موقع4 = find(نص_بحث، "ص")
اختبر("find: يجد في النهاية", موقع4 > 0)

اطبع_سطر("")

// ============================================================================
// 3. اختبارات دالة replace (استبدل)
// ============================================================================
اطبع_سطر("▶ 3. اختبار دالة replace / استبدل")
اطبع_سطر("─────────────────────────────────────")

نص نص_أصلي = "مرحبا بالعالم"
نص نص_جديد1 = replace(نص_أصلي، "العالم"، "الجميع")
اختبر("replace: استبدال عادي", contains(نص_جديد1، "الجميع"))

نص نص_جديد2 = replace(نص_أصلي، "xyz"، "abc")
اختبر("replace: لا يستبدل غير موجود", نص_جديد2 == نص_أصلي)

نص نص_جديد3 = replace("Hello World"، "World"، "Everyone")
اختبر("replace: استبدال إنجليزي", contains(نص_جديد3، "Everyone"))

اطبع_سطر("")

// ============================================================================
// 4. اختبارات دالة substring (قطعة)
// ============================================================================
اطبع_سطر("▶ 4. اختبار دالة substring / قطعة")
اطبع_سطر("─────────────────────────────────────")

نص نص_طويل = "مرحبا بالعالم"
نص جزء1 = substring(نص_طويل، 0، 5)
اختبر("substring: من البداية", جزء1 == "مرحبا")

نص جزء2 = substring(نص_طويل، 7، 13)
اختبر("substring: من الوسط", contains(جزء2، "العالم"))

نص جزء3 = substring("Hello World"، 0، 5)
اختبر("substring: إنجليزي", جزء3 == "Hello")

اطبع_سطر("")

// ============================================================================
// 5. اختبارات دالة toLower (لأصغر)
// ============================================================================
اطبع_سطر("▶ 5. اختبار دالة toLower / لأصغر")
اطبع_سطر("─────────────────────────────────────")

نص نص_كبير = "HELLO WORLD"
نص نص_صغير1 = toLower(نص_كبير)
اختبر("toLower: تحويل إنجليزي", نص_صغير1 == "hello world")

نص نص_مختلط = "HeLLo WoRLd"
نص نص_صغير2 = toLower(نص_مختلط)
اختبر("toLower: تحويل مختلط", نص_صغير2 == "hello world")

نص نص_صغير_بالفعل = "already lower"
نص نص_صغير3 = toLower(نص_صغير_بالفعل)
اختبر("toLower: نص صغير بالفعل", نص_صغير3 == نص_صغير_بالفعل)

اطبع_سطر("")

// ============================================================================
// 6. اختبارات دالة toUpper (لأكبر)
// ============================================================================
اطبع_سطر("▶ 6. اختبار دالة toUpper / لأكبر")
اطبع_سطر("─────────────────────────────────────")

نص نص_صغير = "hello world"
نص نص_كبير1 = toUpper(نص_صغير)
اختبر("toUpper: تحويل إنجليزي", نص_كبير1 == "HELLO WORLD")

نص نص_مختلط2 = "HeLLo WoRLd"
نص نص_كبير2 = toUpper(نص_مختلط2)
اختبر("toUpper: تحويل مختلط", نص_كبير2 == "HELLO WORLD")

نص نص_كبير_بالفعل = "ALREADY UPPER"
نص نص_كبير3 = toUpper(نص_كبير_بالفعل)
اختبر("toUpper: نص كبير بالفعل", نص_كبير3 == نص_كبير_بالفعل)

اطبع_سطر("")

// ============================================================================
// 7. اختبارات دالة trim (قلّم)
// ============================================================================
اطبع_سطر("▶ 7. اختبار دالة trim / قلّم")
اطبع_سطر("─────────────────────────────────────")

نص نص_مسافات = "   مرحبا   "
نص نص_مقلّم1 = trim(نص_مسافات)
اختبر("trim: إزالة مسافات من الطرفين", نص_مقلّم1 == "مرحبا")

نص نص_مسافات2 = "   Hello World   "
نص نص_مقلّم2 = trim(نص_مسافات2)
اختبر("trim: إنجليزي مع مسافات", نص_مقلّم2 == "Hello World")

نص نص_بدون_مسافات = "NoSpaces"
نص نص_مقلّم3 = trim(نص_بدون_مسافات)
اختبر("trim: نص بدون مسافات", نص_مقلّم3 == نص_بدون_مسافات)

اطبع_سطر("")

// ============================================================================
// 8. اختبارات دالة split (قسّم)
// ============================================================================
اطبع_سطر("▶ 8. اختبار دالة split / قسّم")
اطبع_سطر("─────────────────────────────────────")

نص جملة = "أحمد،محمد،فاطمة"
مصفوفة أجزاء1 = split(جملة، "،")
اختبر("split: تقسيم بفاصلة عربية", size(أجزاء1) == 3)

نص جملة2 = "apple,banana,orange"
مصفوفة أجزاء2 = split(جملة2، ",")
اختبر("split: تقسيم بفاصلة", size(أجزاء2) == 3)

نص جملة3 = "one-two-three-four"
مصفوفة أجزاء3 = split(جملة3، "-")
اختبر("split: تقسيم بشرطة", size(أجزاء3) == 4)

اطبع_سطر("")

// ============================================================================
// 9. اختبارات دالة join (اربط)
// ============================================================================
اطبع_سطر("▶ 9. اختبار دالة join / اربط")
اطبع_سطر("─────────────────────────────────────")

مصفوفة كلمات1 = ["أحمد"، "محمد"، "فاطمة"]
نص مربوط1 = join(كلمات1، " - ")
اختبر("join: ربط بمسافة وشرطة", contains(مربوط1، " - "))

مصفوفة كلمات2 = ["apple"، "banana"، "orange"]
نص مربوط2 = join(كلمات2، ",")
اختبر("join: ربط بفاصلة", contains(مربوط2، "apple"))

مصفوفة كلمات3 = ["one"]
نص مربوط3 = join(كلمات3، "-")
اختبر("join: مصفوفة بعنصر واحد", مربوط3 == "one")

اطبع_سطر("")

// ============================================================================
// 10. اختبارات دالة startsWith (يبدأ_بـ)
// ============================================================================
اطبع_سطر("▶ 10. اختبار دالة startsWith / يبدأ_بـ")
اطبع_سطر("─────────────────────────────────────")

نص نص_اختبار = "مرحبا بالعالم"
منطقي يبدأ1 = startsWith(نص_اختبار، "مرحبا")
اختبر("startsWith: يبدأ بالفعل", يبدأ1)

منطقي يبدأ2 = startsWith(نص_اختبار، "بالعالم")
اختبر("startsWith: لا يبدأ", !يبدأ2)

منطقي يبدأ3 = startsWith("Hello World"، "Hello")
اختبر("startsWith: إنجليزي يبدأ", يبدأ3)

اطبع_سطر("")

// ============================================================================
// 11. اختبارات دالة endsWith (ينتهي_بـ)
// ============================================================================
اطبع_سطر("▶ 11. اختبار دالة endsWith / ينتهي_بـ")
اطبع_سطر("─────────────────────────────────────")

نص نص_اختبار2 = "مرحبا بالعالم"
منطقي ينتهي1 = endsWith(نص_اختبار2، "بالعالم")
اختبر("endsWith: ينتهي بالفعل", ينتهي1)

منطقي ينتهي2 = endsWith(نص_اختبار2، "مرحبا")
اختبر("endsWith: لا ينتهي", !ينتهي2)

منطقي ينتهي3 = endsWith("Hello World"، "World")
اختبر("endsWith: إنجليزي ينتهي", ينتهي3)

اطبع_سطر("")

// ============================================================================
// 12. اختبارات دالة contains (يحتوي)
// ============================================================================
اطبع_سطر("▶ 12. اختبار دالة contains / يحتوي")
اطبع_سطر("─────────────────────────────────────")

نص نص_اختبار3 = "مرحبا بالعالم الجميل"
منطقي يحتوي1 = contains(نص_اختبار3، "العالم")
اختبر("contains: يحتوي بالفعل", يحتوي1)

منطقي يحتوي2 = contains(نص_اختبار3، "xyz")
اختبر("contains: لا يحتوي", !يحتوي2)

منطقي يحتوي3 = contains("Hello Beautiful World"، "Beautiful")
اختبر("contains: إنجليزي يحتوي", يحتوي3)

اطبع_سطر("")

// ============================================================================
// 13. اختبارات حالات خاصة - Edge Cases
// ============================================================================
اطبع_سطر("▶ 13. اختبارات الحالات الخاصة")
اطبع_سطر("─────────────────────────────────────")

نص فارغ = ""
رقم طول_فارغ = length(فارغ)
اختبر("Edge: طول نص فارغ", طول_فارغ == 0)

نص نص_طويل_جدا = "مرحبا بك في لغة ص البرمجية العربية الجديدة والمميزة"
رقم طول_طويل = length(نص_طويل_جدا)
اختبر("Edge: نص طويل", طول_طويل > 30)

نص نص_خاص = "!@#$%^&*()"
رقم طول_خاص = length(نص_خاص)
اختبر("Edge: رموز خاصة", طول_خاص == 10)

اطبع_سطر("")

// ============================================================================
// 14. اختبارات تكامل - Integration Tests
// ============================================================================
اطبع_سطر("▶ 14. اختبارات التكامل")
اطبع_سطر("─────────────────────────────────────")

// دمج عدة دوال
نص نص_مركب = "   HELLO WORLD   "
نص نتيجة_مركبة = trim(toLower(نص_مركب))
اختبر("Integration: trim + toLower", نتيجة_مركبة == "hello world")

// تقسيم وربط
نص جملة_طويلة = "أحمد،محمد،فاطمة"
مصفوفة أسماء = split(جملة_طويلة، "،")
نص أسماء_مرتبطة = join(أسماء، " و ")
اختبر("Integration: split + join", contains(أسماء_مرتبطة، " و "))

// بحث واستبدال
نص نص_للتعديل = "مرحبا بك يا صديقي"
إذا contains(نص_للتعديل، "صديقي") {
    نص_للتعديل = replace(نص_للتعديل، "صديقي"، "أخي")
}
اختبر("Integration: contains + replace", contains(نص_للتعديل، "أخي"))

اطبع_سطر("")

// ============================================================================
// 15. اختبارات الأداء - Performance Tests
// ============================================================================
اطبع_سطر("▶ 15. اختبارات الأداء")
اطبع_سطر("─────────────────────────────────────")

نص نص_كبير = "هذا نص طويل جداً يحتوي على الكثير من الكلمات والجمل لاختبار أداء دوال النصوص"
رقم بداية = 0
رقم عدد_التكرارات = 100

// اختبار سرعة length
بداية = 0
لـ (رقم ك = 0؛ ك < عدد_التكرارات؛ ك = ك + 1) {
    رقم طول_مؤقت = length(نص_كبير)
}
اختبر("Performance: length 100x", صحيح)

// اختبار سرعة toUpper
لـ (رقم ك = 0؛ ك < 50؛ ك = ك + 1) {
    نص مؤقت = toUpper(نص_كبير)
}
اختبر("Performance: toUpper 50x", صحيح)

اطبع_سطر("")

// ============================================================================
// النتائج النهائية - Final Results
// ============================================================================
اطبع_سطر("╔════════════════════════════════════════════════════════════╗")
اطبع_سطر("║                  النتائج النهائية - Results               ║")
اطبع_سطر("╚════════════════════════════════════════════════════════════╝")
اطبع_سطر("")

اطبع_سطر("📊 إحصائيات الاختبارات / Test Statistics:")
اطبع_سطر("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━")
اطبع("   إجمالي الاختبارات / Total Tests: ")
اطبع_سطر(عدد_الاختبارات)
اطبع("   ✅ نجحت / Passed: ")
اطبع_سطر(اختبارات_ناجحة)
اطبع("   ❌ فشلت / Failed: ")
اطبع_سطر(اختبارات_فاشلة)

رقم_حقيقي نسبة_النجاح = (اختبارات_ناجحة * 100) / عدد_الاختبارات
اطبع("   📈 نسبة النجاح / Success Rate: ")
اطبع(نسبة_النجاح)
اطبع_سطر("%")

اطبع_سطر("")

إذا اختبارات_فاشلة == 0 {
    اطبع_سطر("🎉 ممتاز! جميع الاختبارات نجحت!")
    اطبع_سطر("🎉 Excellent! All tests passed!")
} وإلا {
    اطبع_سطر("⚠️  يوجد اختبارات فاشلة، يرجى المراجعة")
    اطبع_سطر("⚠️  Some tests failed, please review")
}

اطبع_سطر("")
اطبع_سطر("════════════════════════════════════════════════════════════")
اطبع_سطر("           اكتمل اختبار دوال النصوص بنجاح!")
اطبع_سطر("        String Functions Testing Completed!")
اطبع_سطر("════════════════════════════════════════════════════════════")
