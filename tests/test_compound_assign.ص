# اختبار عمليات الإسناد المركبة

# اختبار 1: +=
متغير س = 10
س += 5
اطبع("اختبار 1 - += : " + س)

# اختبار 2: -=
متغير ع = 20
ع -= 7
اطبع("اختبار 2 - -= : " + ع)

# اختبار 3: *=
متغير ص = 6
ص *= 3
اطبع("اختبار 3 - *= : " + ص)

# اختبار 4: /=
متغير ل = 100
ل /= 4
اطبع("اختبار 4 - /= : " + ل)

# اختبار 5: += مع نصوص
متغير نص1 = "مرحبا"
نص1 += " يا عالم"
اطبع("اختبار 5 - += نص: " + نص1)

# اختبار 6: += متعدد
متغير م = 0
م += 1
م += 2
م += 3
اطبع("اختبار 6 - += متعدد: " + م)

# اختبار 7: ثابت += يجب أن يفشل
ثابت ثا = 50
حاول
    ثا += 10
    اطبع("فشل - تم تعديل ثابت بـ +=!")
امسك (خ)
    اطبع("اختبار 7 - نجح: ثابت محمي من +=")
نهاية
