# 📚 Sad Language OOP Quick Reference

## Class Definition (تعريف الصنف)

```sad
صنف اسم_الصنف
    // Fields
    عام رقم حقل_عام
    خاص نص حقل_خاص
    محمي منطقي حقل_محمي
    
    // Static Fields
    ساكن رقم حقل_ثابت
    
    // Constructor
    باني(رقم معامل)
        حقل_عام = معامل
    نهاية
    
    // Methods
    دالة رقم طريقة(نص معامل)
        ارجع 10
    نهاية
    
    // Static Methods
    ساكن دالة نص طريقة_ثابتة()
        ارجع "نص"
    نهاية
نهاية
```

## Inheritance (الوراثة)

```sad
صنف صنف_قاعدة
    عام رقم قيمة
    
    باني(رقم ق)
        قيمة = ق
    نهاية
    
    دالة اطبع_قيمة()
        اطبع(قيمة)
    نهاية
نهاية

صنف صنف_مشتق : صنف_قاعدة
    عام نص اسم
    
    باني(رقم ق، نص ا)
        قاعدة.باني(ق)  // Call parent constructor
        اسم = ا
    نهاية
    
    دالة اطبع_قيمة()  // Override
        اطبع("القيمة:")
        اطبع(قيمة)
    نهاية
نهاية
```

## Access Modifiers (مُعدلات الوصول)

| Modifier | Arabic | Accessible From |
|----------|--------|----------------|
| `public` | `عام` | Everywhere ✅ |
| `private` | `خاص` | Same class only 🔒 |
| `protected` | `محمي` | Class + derived classes 👨‍👦 |

### Examples

```sad
صنف مثال
    عام رقم عام          // ✅ Accessible everywhere
    خاص نص خاص            // 🔒 Only in this class
    محمي منطقي محمي       // 👨‍👦 Class + derived classes
نهاية

رقم كائن = جديد مثال()
كائن.عام = 10        // ✅ OK
كائن.خاص = "text"    // ❌ Error: private member
```

## Static Members (الأعضاء الثابتة)

```sad
صنف عداد
    ساكن رقم العدد_الكلي = 0
    عام رقم قيمة
    
    باني(رقم ق)
        قيمة = ق
        العدد_الكلي = العدد_الكلي + 1
    نهاية
    
    ساكن دالة رقم احصل_العدد()
        ارجع العدد_الكلي
    نهاية
نهاية

// Usage
رقم ع1 = جديد عداد(10)
رقم ع2 = جديد عداد(20)

اطبع(عداد.العدد_الكلي)       // Output: 2
اطبع(عداد.احصل_العدد())      // Output: 2
```

## Object Creation & Usage

```sad
// Create object
رقم كائن = جديد اسم_الصنف(معامل1، معامل2)

// Access fields
كائن.حقل = قيمة
رقم قيمة = كائن.حقل

// Call methods
كائن.طريقة(معامل)
رقم نتيجة = كائن.طريقة_ترجع()

// Static access
اسم_الصنف.حقل_ثابت
اسم_الصنف.طريقة_ثابتة()
```

## Complete Example (مثال كامل)

```sad
صنف حساب_بنكي
    خاص نص رقم_الحساب
    خاص رقم الرصيد
    ساكن رقم عدد_الحسابات = 0
    
    باني(نص رقم، رقم رصيد_اولي)
        رقم_الحساب = رقم
        الرصيد = رصيد_اولي
        عدد_الحسابات = عدد_الحسابات + 1
        اطبع("تم فتح حساب رقم:")
        اطبع(رقم_الحساب)
    نهاية
    
    عام دالة ايداع(رقم مبلغ)
        الرصيد = الرصيد + مبلغ
        اطبع("تم إيداع:")
        اطبع(مبلغ)
    نهاية
    
    عام دالة سحب(رقم مبلغ)
        اذا مبلغ <= الرصيد {
            الرصيد = الرصيد - مبلغ
            اطبع("تم السحب:")
            اطبع(مبلغ)
        } والا {
            اطبع("الرصيد غير كافٍ")
        }
    نهاية
    
    عام دالة رقم احصل_الرصيد()
        ارجع الرصيد
    نهاية
    
    ساكن دالة رقم احصل_عدد_الحسابات()
        ارجع عدد_الحسابات
    نهاية
نهاية

// Usage
رقم حساب1 = جديد حساب_بنكي("ACC001"، 1000)
حساب1.ايداع(500)
حساب1.سحب(200)
اطبع("الرصيد:")
اطبع(حساب1.احصل_الرصيد())

رقم حساب2 = جديد حساب_بنكي("ACC002"، 2000)

اطبع("إجمالي الحسابات:")
اطبع(حساب_بنكي.احصل_عدد_الحسابات())
```

## Keywords Reference

| English | Arabic | Usage |
|---------|--------|-------|
| `class` | `صنف` | Class definition |
| `new` | `جديد` | Object creation |
| `constructor` | `باني` | Constructor |
| `function` | `دالة` | Method |
| `return` | `ارجع` | Return value |
| `public` | `عام` | Public access |
| `private` | `خاص` | Private access |
| `protected` | `محمي` | Protected access |
| `static` | `ساكن` | Static member |
| `base` | `قاعدة` | Base class call |

## Common Patterns

### Singleton Pattern
```sad
صنف مفرد
    ساكن مفرد المثيل
    خاص منطقي مهيأ = كاذب
    
    باني()
        اذا مهيأ {
            اطبع("خطأ: المفرد مُهيأ مسبقاً")
        } والا {
            مهيأ = صحيح
        }
    نهاية
    
    ساكن دالة مفرد احصل_المثيل()
        اذا المثيل == فارغ {
            المثيل = جديد مفرد()
        }
        ارجع المثيل
    نهاية
نهاية
```

### Factory Pattern
```sad
صنف مصنع_الحيوانات
    ساكن دالة حيوان انشئ_حيوان(نص نوع)
        اذا نوع == "كلب" {
            ارجع جديد كلب()
        } والا اذا نوع == "قطة" {
            ارجع جديد قطة()
        }
        ارجع فارغ
    نهاية
نهاية
```

---

**Version**: Sad Language v1.2  
**Last Updated**: November 27, 2025  
**OOP Support**: ✅ Full (Phases 4-6.2 Complete)
