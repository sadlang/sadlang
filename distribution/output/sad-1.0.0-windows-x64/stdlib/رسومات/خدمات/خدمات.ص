# مكتبة خدمات ص — نقطة الدخول الرئيسية
# ===================================================================
# الاستخدام:
#   استورد * من خدمات
#   استورد * من خدمات.شبكة
# ===================================================================

# لا نستورد شيئاً هنا — المستخدم يستورد الخدمة المحددة
# هذا الملف موجود فقط لتمكين `من خدمات استورد ...`

صدّر ثابت _إصدار_خدمات = "4.0.0"
