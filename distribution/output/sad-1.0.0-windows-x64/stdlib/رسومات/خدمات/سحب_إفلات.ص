# ===================================================================
# خدمة سحب وإفلات — Drag & Drop Service
# stdlib/خدمات/سحب_إفلات.ص
# ===================================================================
# الاستخدام:
#   استورد * من خدمات.سحب_إفلات
# ===================================================================

استورد * من _محرك_واجهات

## تسجيل معالج إفلات الملفات
## @param دالة_معالجة الدالة التي تستقبل مسار الملف
صدّر دالة عند_إفلات(دالة_معالجة)
    عند_إسقاط(دالة_معالجة)
نهاية
