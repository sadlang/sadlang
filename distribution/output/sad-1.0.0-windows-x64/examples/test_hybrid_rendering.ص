# بسم الله الرحمن الرحيم
# ===================================================================
# اختبار نظام العرض الهجين (Hybrid Rendering System Test)
# ===================================================================
# يختبر: طريقة_عرض للعناصر المختلفة
# ===================================================================

من واجهات استورد *

# ─── اختبار 1: عناصر أساسية ───
اطبع_سطر("=== اختبار نظام العرض الهجين ===")

# ─── التطبيق ───
متغير التطبيق = هيكل(
    شريط: شريط_تطبيق(عنوان: "اختبار العرض الهجين"),
    محتوى: عمود([
        # عنوان
        نص("نظام العرض الهجين", حجم: 28),
        فاصل(ارتفاع: 20),
        
        # أزرار بطرق عرض مختلفة
        صف([
            عمود([
                نص("تلقائي", حجم: 14),
                زر("زر 1", لون_خلفية: "أزرق", طريقة_عرض: "تلقائي")
            ]),
            عمود([
                نص("أصلي", حجم: 14),
                زر("زر 2", لون_خلفية: "أخضر", طريقة_عرض: "أصلي")
            ]),
            عمود([
                نص("مرسوم", حجم: 14),
                زر("زر 3", لون_خلفية: "برتقالي", طريقة_عرض: "مرسوم")
            ])
        ], تباعد: 20),
        
        فاصل(ارتفاع: 30),
        
        # حقول إدخال
        نص("حقول الإدخال:", حجم: 20),
        حقل(تلميح: "حقل إدخال عادي"),
        
        فاصل(ارتفاع: 30),
        
        # معلومات النظام
        نص("معلومات:", حجم: 16, لون: "رمادي"),
        نص("النظام يختار أفضل طريقة عرض"),
        نص("Desktop: SDL2 Canvas"),
        نص("Android: Native + Canvas"),
        نص("iOS: SwiftUI + Canvas"),
        نص("Web: HTML/CSS + Canvas")
        
    ], تباعد: 10)
)

# تشغيل التطبيق
شغّل(التطبيق)

اطبع_سطر("=== انتهى الاختبار ===")
