# ═══════════════════════════════════════════════════
# اختبار شامل — تقليص الكلمات المحجوزة إلى 40
# ═══════════════════════════════════════════════════

اطبع_سطر("=== اختبار 1: الكلمات المحجوزة الأساسية ===")

# دالة — ارجع — نهاية
دالة جمع(أ، ب)
   ارجع أ + ب
نهاية

اطبع_سطر("جمع(3, 5) = " + جمع(3، 5))

# متغير — ثابت
متغير س = 10
ثابت ثمن = 99

اطبع_سطر("س = " + س + "، ثمن = " + ثمن)

# ──────────────────────────────────────────────────
اطبع_سطر("=== اختبار 2: التحكم ===")

# إذا — وإلا — نهاية
إذا (س > 5)
   اطبع_سطر("س أكبر من 5")
وإلا
   اطبع_سطر("س أصغر أو يساوي 5")
نهاية

# بينما — توقف — استمر
متغير ع = 0
بينما (ع < 10)
   ع = ع + 1
   إذا (ع == 3)
      استمر
   نهاية
   إذا (ع == 7)
      توقف
   نهاية
نهاية
اطبع_سطر("بينما توقف عند ع = " + ع)

# لكل — في
لكل عنصر في [10، 20، 30]
   اطبع_سطر("عنصر: " + عنصر)
نهاية

# ──────────────────────────────────────────────────
اطبع_سطر("=== اختبار 3: أسماء الأنواع كمتغيرات ===")

# هذه الكلمات لم تعد محجوزة ويمكن استخدامها كأسماء متغيرات
متغير رقم = 42
متغير نص = "مرحبا"
متغير مصفوفة = [1، 2، 3]
متغير منطقي = صحيح

اطبع_سطر("رقم = " + رقم)
اطبع_سطر("نص = " + نص)
اطبع_سطر("مصفوفة = " + مصفوفة)
اطبع_سطر("منطقي = " + منطقي)

# ──────────────────────────────────────────────────
اطبع_سطر("=== اختبار 4: صنف — باني — جديد — هذا ===")

صنف حيوان
   متغير الاسم = ""
   متغير العمر = 0

   باني(اسم، عمر)
      هذا.الاسم = اسم
      هذا.العمر = عمر
   نهاية

   دالة وصف()
      ارجع هذا.الاسم + " عمره " + هذا.العمر
   نهاية
نهاية

متغير قط = جديد حيوان("قط"، 3)
اطبع_سطر(قط.وصف())

# ──────────────────────────────────────────────────
اطبع_سطر("=== اختبار 5: وراثة — يرث ===")

صنف كلب يرث حيوان
   باني(اسم، عمر)
      الأساس(اسم، عمر)
   نهاية

   دالة نبح()
      ارجع هذا.الاسم + " يقول: هو هو!"
   نهاية
نهاية

متغير ك = جديد كلب("ريكس"، 5)
اطبع_سطر(ك.وصف())
اطبع_سطر(ك.نبح())

# ──────────────────────────────────────────────────
اطبع_سطر("=== اختبار 6: حاول — امسك — أخيراً ===")

حاول
   متغير نتيجة = 10 / 0
امسك (الخطأ)
   اطبع_سطر("تم مسك خطأ: " + الخطأ)
أخيراً
   اطبع_سطر("كتلة أخيراً تنفذ دائماً")
نهاية

# ──────────────────────────────────────────────────
اطبع_سطر("=== اختبار 7: طابق — عندما — افتراضي ===")

متغير يوم = "الاثنين"
طابق (يوم)
   عندما "السبت":
      اطبع_سطر("عطلة!")
   عندما "الاثنين":
      اطبع_سطر("بداية الأسبوع")
   افتراضي:
      اطبع_سطر("يوم عادي")
نهاية

# ──────────────────────────────────────────────────
اطبع_سطر("=== اختبار 8: العوامل المنطقية ===")

متغير أ = صحيح
متغير ب = خطأ

اطبع_سطر("أ و ب = " + (أ و ب))
اطبع_سطر("أ أو ب = " + (أ أو ب))
اطبع_سطر("ليس أ = " + (ليس أ))

# ──────────────────────────────────────────────────
اطبع_سطر("=== اختبار 9: القيم المحجوزة ===")

متغير ص = صحيح
متغير خ = خطأ
متغير ل = لاشيء

اطبع_سطر("صحيح = " + ص)
اطبع_سطر("خطأ = " + خ)
اطبع_سطر("لاشيء = " + ل)

# ──────────────────────────────────────────────────
اطبع_سطر("=== اختبار 10: بنية — تعداد ===")

بنية نقطة
   خاصية س
   خاصية ص
نهاية

اطبع_سطر("بنية نقطة معرّفة بنجاح")

# ──────────────────────────────────────────────────
اطبع_سطر("=== اختبار 11: استورد — من — كـ — صدّر ===")
# هذه ستعمل فقط مع ملفات حقيقية، لكن التحقق من عدم وجود خطأ نحوي
اطبع_سطر("كلمات الوحدات محفوظة كمحجوزة")

# ──────────────────────────────────────────────────
اطبع_سطر("=== اختبار 12: وصول — عام — خاص — محمي — مجرد ===")

صنف شكل
   عام دالة مساحة()
      ارجع 0
   نهاية
نهاية

اطبع_سطر("صنف شكل بدالة عامة - نجح")

# ──────────────────────────────────────────────────
اطبع_سطر("")
اطبع_سطر("✅ جميع الاختبارات نجحت!")
اطبع_سطر("📊 40 كلمة محجوزة + 3 عوامل منطقية")
