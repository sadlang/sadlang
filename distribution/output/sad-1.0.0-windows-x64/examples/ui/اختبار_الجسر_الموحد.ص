# بسم الله الرحمن الرحيم
# ═══════════════════════════════════════════════════════════════
# اختبار الجسر الموحد للواجهات — يختبر sad_ui_runtime
# ═══════════════════════════════════════════════════════════════
# الملف: اختبار_الجسر_الموحد.ص
# الوصف: يختبر ربط وظائف C الخاصة بنظام الواجهات الموحد
#         عبر جميع المنصات (سطح المكتب / أندرويد / ويب / iOS / macOS)
# ═══════════════════════════════════════════════════════════════
# ملاحظة: يستخدم الدوال المدمجة مباشرة (بدون استيراد واجهات)
# ═══════════════════════════════════════════════════════════════

# ═══════════════════════════════════════════════════════════════
#                     متغيرات الحالة
# ═══════════════════════════════════════════════════════════════

متغير العداد = 0

# ═══════════════════════════════════════════════════════════════
#                     دالة بناء الواجهة
# ═══════════════════════════════════════════════════════════════

دالة بناء_الواجهة()
    # الحاوية الرئيسية: عمود
    متغير الجذر = عمود()

    # ─── عنوان الاختبار ───
    متغير العنوان = نص("🌍 اختبار الجسر الموحد للواجهات")
    اضف_طفل(الجذر، العنوان)

    # ─── صف أزرار ───
    متغير صف_ازرار = صف()
    اضف_طفل(الجذر، صف_ازرار)

    متغير زر1 = زر("زيادة +1")
    اضف_طفل(صف_ازرار، زر1)

    متغير زر2 = زر("تصفير")
    اضف_طفل(صف_ازرار، زر2)

    # ─── بطاقة النتيجة ───
    متغير بطاقة_النتيجة = بطاقة()
    اضف_طفل(الجذر، بطاقة_النتيجة)

    متغير نص_النتيجة = نص("العداد: " + العداد)
    اضف_طفل(بطاقة_النتيجة، نص_النتيجة)

    # ─── حقل إدخال ───
    متغير حقل = حقل_نص("اكتب شيئاً هنا...")
    اضف_طفل(الجذر، حقل)

    # ─── مربع اختيار ───
    متغير اختيار = مربع_اختيار("تفعيل الإشعارات")
    اضف_طفل(الجذر، اختيار)

    ارجع الجذر
نهاية

# ═══════════════════════════════════════════════════════════════
#                     تشغيل التطبيق
# ═══════════════════════════════════════════════════════════════

اطبع_سطر("═══ اختبار الجسر الموحد للواجهات ═══")
اطبع_سطر("المنصات المدعومة: سطح المكتب | أندرويد | iOS | macOS | ويب")
اطبع_سطر("جاري بناء شجرة العناصر...")

متغير الشجرة = بناء_الواجهة()
اطبع_سطر("تم بناء شجرة العناصر بنجاح!")
اطبع_سطر("جاري تشغيل الواجهة...")

_محرك_واجهات(الشجرة)

اطبع_سطر("تم إغلاق التطبيق بنجاح.")
