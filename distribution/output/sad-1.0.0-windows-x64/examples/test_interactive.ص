# بسم الله الرحمن الرحيم
# اختبار التفاعل — أزرار تعمل فعلاً مع أحداث

من رسومات.واجهات استورد *

# ─── دالة تُنفذ عند الضغط ───
دالة عند_ضغط_أول()
    اطبع_سطر("✓ تم الضغط على الزر الأول!")
نهاية

دالة عند_ضغط_ثاني()
    اطبع_سطر("✓ تم الضغط على الزر الثاني!")
نهاية

# ─── بناء الواجهة ───

متغير التطبيق = عمود([
    نص("اختبار التفاعل", حجم: 28, لون: "#1565C0"),
    نص("مرر المؤشر فوق العناصر — ثم اضغط", حجم: 14, لون: "#666666"),
    
    زر("اضغط هنا!", لون_خلفية: "#4CAF50", لون_نص: "#FFFFFF",
        عند_الضغط: عند_ضغط_أول),
    
    زر("زر آخر", لون_خلفية: "#2196F3", لون_نص: "#FFFFFF",
        عند_الضغط: عند_ضغط_ثاني),
    
    صف([
        بطاقة(عمود([
            نص("بطاقة تفاعلية", حجم: 18, لون: "#E91E63"),
            نص("مرر المؤشر علي")
        ])),
        بطاقة(عمود([
            نص("بطاقة ثانية", حجم: 18, لون: "#9C27B0"),
            زر("زر داخل بطاقة", لون_خلفية: "#FF5722", لون_نص: "#FFFFFF")
        ]))
    ]),
    
    شريط_تقدم(75),
    
    حقل(تلميح: "اكتب هنا...")
])

# ─── تشغيل النافذة ───
شغّل(التطبيق)
