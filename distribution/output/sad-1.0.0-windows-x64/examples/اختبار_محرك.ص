# اختبار محرك الرسم
استورد * من رسومات.محرك.واجهة_محرك
استورد * من رسومات.محرك.محرك_نواة
استورد * من رسومات.محرك.مدير_نوافذ
استورد * من رسومات.محرك.أحداث
استورد * من رسومات.رسم.لوحة

ثابت عرض_شاشة = 1024
ثابت ارتفاع_شاشة = 768
ثابت أبيض = 16777215
ثابت أسود = 0
ثابت أحمر = 16711680
ثابت أخضر = 65280
ثابت أزرق_داكن = 1318946

دالة رئيسية()
    # ١. إنشاء المحرك
    متغير شاشة = جديد محرك_نواة()
    شاشة.تهيئة(عرض_شاشة, ارتفاع_شاشة)
    اطبع_سطر("✓ محرك النواة تم تهيئته")

    # ٢. مسح الشاشة
    شاشة.مسح(أزرق_داكن)
    اطبع_سطر("✓ مسح الشاشة")

    # ٣. رسم مباشر
    شاشة.ارسم_مستطيل(5, 5, عرض_شاشة - 10, ارتفاع_شاشة - 10, أبيض)
    شاشة.ارسم_نص(20, 20, "SadOS", أبيض, 3)
    شاشة.ارسم_مستطيل_ممتلئ(20, 80, 200, 100, أحمر)
    شاشة.ارسم_دائرة(350, 130, 50, أخضر)
    شاشة.ارسم_دائرة_ممتلئة(500, 130, 50, أبيض)
    شاشة.ارسم_خط(20, 200, 700, 200, أبيض)
    اطبع_سطر("✓ رسم مباشر بالمحرك")

    # ٤. Canvas API
    متغير لوحة = جديد لوحة_رسم(عرض_شاشة, ارتفاع_شاشة)
    متغير فرشاة1 = جديد فرشاة()
    فرشاة1.لون = جديد لون_ر(255, 100, 100, 255)
    فرشاة1.نمط = "ملء"
    لوحة.ارسم_مستطيل(جديد مستطيل_ر(20, 220, 300, 150), فرشاة1)
    لوحة.ارسم_دائرة(جديد نقطة(500, 300), 60, فرشاة1)
    شاشة.نفّذ_لوحة(لوحة)
    اطبع_سطر("✓ Canvas API يعمل")

    # ٥. مدير النوافذ
    متغير مدير = جديد مدير_نوافذ(شاشة)
    متغير ن1 = مدير.أنشئ_نافذة("Terminal", 50, 450, 350, 250)
    ن1.نشطة = صحيح
    اطبع_سطر("✓ مدير النوافذ يعمل")

    # ٦. نظام الأحداث
    متغير طابور = جديد طابور_أحداث()
    متغير حدث1 = جديد حدث_مفتاح(حدث_ضغط_مفتاح, مفتاح_إدخال)
    طابور.أضف(حدث1)
    متغير حدث2 = جديد حدث_فأرة(حدث_حركة_فأرة, 100, 200, 0)
    طابور.أضف(حدث2)
    متغير حدث3 = جديد حدث_مفتاح(حدث_رفع_مفتاح, مفتاح_هروب)
    طابور.أضف(حدث3)
    اطبع_سطر("✓ طابور الأحداث: " + طابور.الحجم() + " أحداث")
    # اختبار تصفية الأحداث
    متغير مفاتيح = طابور.تصفية(حدث_ضغط_مفتاح)
    اطبع_سطر("✓ تصفية: " + مفاتيح.طول() + " حدث مفتاح")
    # اختبار peek
    متغير أول = طابور.الأول()
    اطبع_سطر("✓ إلقاء نظرة: نوع الأول = " + أول.نوع)
    # اختبار استهلاك الأحداث
    متغير ح1 = طابور.التالي()
    متغير ح2 = طابور.التالي()
    متغير ح3 = طابور.التالي()
    اطبع_سطر("✓ استهلاك: نوع1=" + ح1.نوع + " نوع2=" + ح2.نوع + " نوع3=" + ح3.نوع)
    اطبع_سطر("✓ فارغ بعد الاستهلاك: " + طابور.فارغ())

    # ٧. أحداث المحرك
    اطبع_سطر("✓ المحرك يعمل: " + شاشة.يعمل())
    شاشة.أوقف()
    اطبع_سطر("✓ المحرك بعد الإيقاف: " + شاشة.يعمل())

    شاشة.تبديل()
    اطبع_سطر("═══════════════════════════")
    اطبع_سطر("✅ جميع الاختبارات نجحت!")
نهاية
