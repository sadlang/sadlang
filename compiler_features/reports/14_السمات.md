# تقرير الميزة 14: السمات (Traits)

## الحالة: ✅ جزئي — يحتاج كلمات مفتاحية

## الملفات الموجودة
- `compiler_new/include/types/trait_system.h` (439 سطر) — نظام السمات
- `compiler_new/src/types/trait_system.cpp` — التنفيذ
- `stdlib/core/traits.sad` — سمات أساسية

## ما يحتاج إضافة
1. كلمة `سمة` → `KEYWORD_TRAIT` في الـ Lexer
2. كلمة `نفّذ` / `نفذ` → `KEYWORD_IMPL` في الـ Lexer
3. صيغة `نفّذ سمة لـ نوع { ... }` في المحلل

## معايير القبول
- [x] نظام سمات في الأنواع
- [x] سمات مع default implementations
- [x] سمات في stdlib
- [ ] كلمة `سمة` في الـ Lexer
- [ ] كلمة `نفّذ` في الـ Lexer
